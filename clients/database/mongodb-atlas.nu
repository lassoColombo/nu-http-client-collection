# Auto-generated client for MongoDB Atlas Administration API v2.0
# Source: https://raw.githubusercontent.com/mongodb/openapi/main/openapi/v2.json
# Auth: --token flag or $env.MONGODB_ATLAS_ADMINISTRATION_API_TOKEN

const BASE_URL = "https://cloud.mongodb.com"
const DEFAULT_AUTH = "bearer"

# Build auth: returns {headers: record, query: string}
def build-auth [token?: string, auth_scheme?: string]: nothing -> record {
  let token_val = if ($token != null) and ($token | is-not-empty) { $token } else { $env | get -o MONGODB_ATLAS_ADMINISTRATION_API_TOKEN | default "" }
  let scheme = ($auth_scheme | default "bearer")
  if ($scheme == "none") or ($token_val | is-empty) { return {headers: {}, query: ""} }
  match $scheme {
    "digest" => { {headers: {Authorization: $"Digest ($token_val)"}, query: ""} }
    "bearer" => { {headers: {Authorization: $"Bearer ($token_val)"}, query: ""} }
    "none" => { {headers: {}, query: ""} }
    _ => { {headers: {Authorization: $"Bearer ($token_val)"}, query: ""} }
  }
}

# Serialize a single query parameter based on collection style
def serialize-qp [name: string, value: any, style: string]: nothing -> list<string> {
  if ($value == null) { return [] }
  let n = ($name | url encode)
  let is_list = ($value | describe | str starts-with "list")
  if ($value | describe | str starts-with "record") { return ($value | transpose k v | each { $"($n)[($in.k | into string | url encode)]=($in.v | into string | url encode)" }) }
  if not $is_list { return [$"($n)=($value | into string | url encode)"] }
  match $style {
    "multi" => { $value | each {|v| $"($n)=($v | into string | url encode)" } }
    "csv" => { let joined = ($value | each { $in | into string | url encode } | str join ","); [$"($n)=($joined)"] }
    "ssv" => { let joined = ($value | each { $in | into string | url encode } | str join "%20"); [$"($n)=($joined)"] }
    "tsv" => { let joined = ($value | each { $in | into string | url encode } | str join "%09"); [$"($n)=($joined)"] }
    "pipes" => { let joined = ($value | each { $in | into string | url encode } | str join "|"); [$"($n)=($joined)"] }
    "deepObject" => { $value | each {|v| $"($n)[]=($v | into string | url encode)" } }
    _ => { $value | each {|v| $"($n)=($v | into string | url encode)" } }
  }
}

# Build URL from base, path, and optional query string
def build-url [base: string, path: string, query?: string]: nothing -> string {
  let parsed = ($base | url parse | reject params)
  let full_path = if ($path | is-empty) { $parsed.path } else { [$parsed.path $path] | str join "/" | str replace --all --regex '/+' '/' }
  let result = ($parsed | upsert path $full_path)
  if ($query != null) and ($query | is-not-empty) { $result | upsert query $query | url join } else { $result | url join }
}

# Execute HTTP request with method dispatch
def do-request [method: string, url: string, auth: record, insecure: bool, raw: bool, dry_run: bool, max_time?: duration, allow_errors?: bool, content_type?: string, body?: any]: nothing -> any {
  let req_url = if ($auth.query | is-not-empty) { if ($url | str contains "?") { $"($url)&($auth.query)" } else { $"($url)?($auth.query)" } } else { $url }
  let timeout = ($max_time | default 30min)
  let ct = ($content_type | default "application/json")
  if $dry_run { return {method: $method, url: $req_url, headers: $auth.headers, query_string: $auth.query, content_type: $ct, timeout: $timeout, body: $body} }
  let resp = match $method {
    "get" => { http get --headers $auth.headers --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url }
    "head" => { http head --headers $auth.headers --max-time $timeout --insecure=$insecure $req_url }
    "options" => { http options --headers $auth.headers --max-time $timeout --insecure=$insecure $req_url }
    "post" => { http post --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "put" => { http put --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "patch" => { http patch --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "delete" => { if ($body | is-empty) { http delete --headers $auth.headers --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url } else { http delete --headers $auth.headers --content-type $ct --data $body --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url } }
  }
  if ($method in ["head" "options"]) { return $resp }
  if $allow_errors { $resp } else if $resp.status == 204 { null } else if $resp.status >= 400 { error make --unspanned { msg: $"HTTP ($resp.status): ($resp.body)" } } else { $resp.body }
}

def base-url-completer [] { ["https://cloud.mongodb.com"] }
def auth-scheme-completer [] { ["digest" "bearer"] }

# Completers for enum parameters
def accept-completer [] { ["application/vnd.atlas.2023-01-01+json" "application/vnd.atlas.2023-11-15+json"] }
def status-completer [] { ["CLOSED" "OPEN" "TRACKING"] }
def accept-completer-1 [] { ["application/vnd.atlas.2023-01-01+json" "application/vnd.atlas.2024-05-30+json"] }
def accept-completer-2 [] { ["application/vnd.atlas.2023-01-01+json" "application/vnd.atlas.2023-10-01+json"] }
def accept-completer-3 [] { ["application/vnd.atlas.2023-01-01+json" "application/vnd.atlas.2023-02-01+json" "application/vnd.atlas.2024-08-05+json"] }
def accept-completer-4 [] { ["application/vnd.atlas.2023-01-01+json" "application/vnd.atlas.2023-02-01+json" "application/vnd.atlas.2024-08-05+json" "application/vnd.atlas.2024-10-23+json"] }
def accept-completer-5 [] { ["application/vnd.atlas.2023-01-01+json" "application/vnd.atlas.2023-02-01+json"] }
def accept-completer-6 [] { ["application/vnd.atlas.2023-01-01+json" "application/vnd.atlas.2024-08-05+json"] }
def state-completer [] { ["FAILED" "FINALIZING" "INDEX_BUILD_FAILED" "IN_PROGRESS" "NOT_FOUND" "NOT_RESTORED" "NOT_STARTED" "ROLLBACK" "SUCCESSFUL" "UNSUPPORTED"] }
def status-completer-1 [] { ["REJECTED"] }
def accept-completer-7 [] { ["application/vnd.atlas.2023-01-01+json" "application/vnd.atlas.2024-05-30+json" "application/vnd.atlas.2025-03-12+json"] }
def completed-completer [] { ["all" "false" "true"] }
def accept-completer-8 [] { ["application/vnd.atlas.2023-01-01+gzip" "application/vnd.atlas.2023-02-01+gzip"] }
def providerName-completer [] { ["AWS" "AZURE" "GCP"] }
def type-completer [] { ["ONLINE_ARCHIVE" "USER"] }
def cloudProvider-completer [] { ["AWS" "AZURE" "GCP" "TENANT"] }
def defaultStatus-completer [] { ["DEFAULT"] }
def accept-completer-9 [] { ["application/vnd.atlas.2023-02-01+json" "application/vnd.atlas.preview+json"] }
def accept-completer-10 [] { ["application/vnd.atlas.2025-03-12+json" "application/vnd.atlas.preview+json"] }
def accept-completer-11 [] { ["application/vnd.atlas.2023-01-01+json" "application/vnd.atlas.2025-02-19+json"] }
def accept-completer-12 [] { ["application/vnd.atlas.2023-01-01+csv" "application/vnd.atlas.2023-01-01+json"] }
def sortBy-completer [] { ["END_DATE" "START_DATE"] }
def orderBy-completer [] { ["asc" "desc"] }

# List all available API commands with their parameters
export def commands []: nothing -> table {
  let builtin_flags = ["base-url" "token" "auth-scheme" "insecure" "max-time" "raw" "allow-errors" "dry-run" "accept" "help"]
  let mod_name = (scope modules | where { $in.commands | any { $in.name == "atlas get" } } | get name | first)
  let mod_cmds = (scope modules | where name == $mod_name | get commands | first)
  let cmd_ids = ($mod_cmds | where name not-in [$mod_name "commands"] | get decl_id)
  scope commands | where decl_id in $cmd_ids | each {|cmd|
    let sig = $cmd.signatures | values | first
    let params = $sig
      | where parameter_type not-in ["input" "output"]
      | where parameter_name not-in $builtin_flags
      | select parameter_name parameter_type syntax_shape is_optional description
    let return_type = ($sig | where parameter_type == "output" | get -o syntax_shape | first | default "any")
    {
      name: ($cmd.name | str replace $"($mod_name) " "")
      description: $cmd.description
      extra_description: $cmd.extra_description
      return_type: $return_type
      params: $params
    }
  }
}

# Return the Status of This MongoDB Application
#
# GET /api/atlas/v2
# operationId: getSystemStatus
export def "atlas get" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/api/atlas/v2" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Alert Configuration Matchers Field Names
#
# GET /api/atlas/v2/alertConfigs/matchers/fieldNames
# operationId: listAlertConfigMatcherFieldNames
export def "atlas-alert-configs-matchers-field-names listAlertConfigMatcherFieldNames" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/api/atlas/v2/alertConfigs/matchers/fieldNames" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Authorized Clusters in All Projects
#
# GET /api/atlas/v2/clusters
# operationId: listClusterDetails
export def "atlas-clusters listClusterDetails" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/api/atlas/v2/clusters" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Descriptions of User-Configurable Project Limits
#
# GET /api/atlas/v2/defaultGroupLimits
# operationId: listDefaultGroupLimits
export def "atlas-default-group-limits listDefaultGroupLimits" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/api/atlas/v2/defaultGroupLimits" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One User-Configurable Project Limit and Description
#
# GET /api/atlas/v2/defaultGroupLimits/{limitName}
# operationId: getDefaultGroupLimit
export def "atlas-default-group-limits get" [
  limitName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/defaultGroupLimits/($limitName)" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Event Types
#
# GET /api/atlas/v2/eventTypes
# operationId: listEventTypes
export def "atlas-event-types listEventTypes" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/api/atlas/v2/eventTypes" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete One Federation Settings Instance
#
# DELETE /api/atlas/v2/federationSettings/{federationSettingsId}
# operationId: deleteFederationSetting
export def "atlas-federation-settings delete" [
  federationSettingsId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/api/atlas/v2/federationSettings/($federationSettingsId)")
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Organization Configurations from One Federation
#
# GET /api/atlas/v2/federationSettings/{federationSettingsId}/connectedOrgConfigs
# operationId: listFederationSettingConnectedOrgConfigs
export def "atlas-federation-settings-connected-org-configs listFederationSettingConnectedOrgConfigs" [
  federationSettingsId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/federationSettings/($federationSettingsId)/connectedOrgConfigs" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove One Organization Configuration from One Federation
#
# DELETE /api/atlas/v2/federationSettings/{federationSettingsId}/connectedOrgConfigs/{orgId}
# operationId: removeFederationSettingConnectedOrgConfig
export def "atlas-federation-settings-connected-org-configs removeFederationSettingConnectedOrgConfig" [
  federationSettingsId: string
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/federationSettings/($federationSettingsId)/connectedOrgConfigs/($orgId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Organization Configuration from One Federation
#
# GET /api/atlas/v2/federationSettings/{federationSettingsId}/connectedOrgConfigs/{orgId}
# operationId: getFederationSettingConnectedOrgConfig
export def "atlas-federation-settings-connected-org-configs get" [
  federationSettingsId: string
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/federationSettings/($federationSettingsId)/connectedOrgConfigs/($orgId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Organization Configuration in One Federation
#
# PATCH /api/atlas/v2/federationSettings/{federationSettingsId}/connectedOrgConfigs/{orgId}
# operationId: updateFederationSettingConnectedOrgConfig
export def "atlas-federation-settings-connected-org-configs updateFederationSettingConnectedOrgConfig" [
  federationSettingsId: string
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/federationSettings/($federationSettingsId)/connectedOrgConfigs/($orgId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Role Mappings from One Organization
#
# GET /api/atlas/v2/federationSettings/{federationSettingsId}/connectedOrgConfigs/{orgId}/roleMappings
# operationId: listFederationSettingConnectedOrgConfigRoleMappings
export def "atlas-federation-settings-connected-org-configs-role-mappings listFederationSettingConnectedOrgConfigRoleMappings" [
  federationSettingsId: string
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/federationSettings/($federationSettingsId)/connectedOrgConfigs/($orgId)/roleMappings" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Role Mapping in One Organization Configuration
#
# POST /api/atlas/v2/federationSettings/{federationSettingsId}/connectedOrgConfigs/{orgId}/roleMappings
# operationId: createFederationSettingConnectedOrgConfigRoleMapping
export def "atlas-federation-settings-connected-org-configs-role-mappings createFederationSettingConnectedOrgConfigRoleMapping" [
  federationSettingsId: string
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/federationSettings/($federationSettingsId)/connectedOrgConfigs/($orgId)/roleMappings" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Remove One Role Mapping from One Organization
#
# DELETE /api/atlas/v2/federationSettings/{federationSettingsId}/connectedOrgConfigs/{orgId}/roleMappings/{id}
# operationId: deleteFederationSettingConnectedOrgConfigRoleMapping
export def "atlas-federation-settings-connected-org-configs-role-mappings delete" [
  federationSettingsId: string
  id: string
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/federationSettings/($federationSettingsId)/connectedOrgConfigs/($orgId)/roleMappings/($id)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Role Mapping from One Organization
#
# GET /api/atlas/v2/federationSettings/{federationSettingsId}/connectedOrgConfigs/{orgId}/roleMappings/{id}
# operationId: getFederationSettingConnectedOrgConfigRoleMapping
export def "atlas-federation-settings-connected-org-configs-role-mappings get" [
  federationSettingsId: string
  id: string
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/federationSettings/($federationSettingsId)/connectedOrgConfigs/($orgId)/roleMappings/($id)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Role Mapping in One Organization
#
# PUT /api/atlas/v2/federationSettings/{federationSettingsId}/connectedOrgConfigs/{orgId}/roleMappings/{id}
# operationId: updateFederationSettingConnectedOrgConfigRoleMapping
export def "atlas-federation-settings-connected-org-configs-role-mappings updateFederationSettingConnectedOrgConfigRoleMapping" [
  federationSettingsId: string
  id: string
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/federationSettings/($federationSettingsId)/connectedOrgConfigs/($orgId)/roleMappings/($id)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Identity Providers in One Federation
#
# GET /api/atlas/v2/federationSettings/{federationSettingsId}/identityProviders
# operationId: listFederationSettingIdentityProviders
export def "atlas-federation-settings-identity-providers listFederationSettingIdentityProviders" [
  federationSettingsId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --protocol: list # The protocols of the target identity providers.
  --idpType: list # The types of the target identity providers.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "protocol" $protocol "multi") (serialize-qp "idpType" $idpType "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/federationSettings/($federationSettingsId)/identityProviders" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Identity Provider
#
# POST /api/atlas/v2/federationSettings/{federationSettingsId}/identityProviders
# operationId: createFederationSettingIdentityProvider
export def "atlas-federation-settings-identity-providers createFederationSettingIdentityProvider" [
  federationSettingsId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/federationSettings/($federationSettingsId)/identityProviders" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-11-15+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-11-15+json" $body
}

# Delete One Identity Provider
#
# DELETE /api/atlas/v2/federationSettings/{federationSettingsId}/identityProviders/{identityProviderId}
# Docs: https://www.mongodb.com/docs/atlas/security/manage-org-mapping/ — Manage Organization Mapping for Federated Authentication
# operationId: deleteFederationSettingIdentityProvider
export def "atlas-federation-settings-identity-providers delete" [
  federationSettingsId: string
  identityProviderId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/federationSettings/($federationSettingsId)/identityProviders/($identityProviderId)" $qp)
  let accept_val = "application/vnd.atlas.2023-11-15+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Identity Provider by ID
#
# GET /api/atlas/v2/federationSettings/{federationSettingsId}/identityProviders/{identityProviderId}
# operationId: getFederationSettingIdentityProvider
export def "atlas-federation-settings-identity-providers get" [
  federationSettingsId: string
  identityProviderId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/federationSettings/($federationSettingsId)/identityProviders/($identityProviderId)" $qp)
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Identity Provider
#
# PATCH /api/atlas/v2/federationSettings/{federationSettingsId}/identityProviders/{identityProviderId}
# operationId: updateFederationSettingIdentityProvider
export def "atlas-federation-settings-identity-providers updateFederationSettingIdentityProvider" [
  federationSettingsId: string
  identityProviderId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/federationSettings/($federationSettingsId)/identityProviders/($identityProviderId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Revoke JWKS from One OIDC Identity Provider
#
# DELETE /api/atlas/v2/federationSettings/{federationSettingsId}/identityProviders/{identityProviderId}/jwks
# Docs: https://www.mongodb.com/docs/atlas/security-oidc/#revoke-jwks — Configure OIDC Authorization
# operationId: revokeFederationSettingIdentityProviderJwks
export def "atlas-federation-settings-identity-providers-jwks revokeFederationSettingIdentityProviderJwks" [
  federationSettingsId: string
  identityProviderId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/federationSettings/($federationSettingsId)/identityProviders/($identityProviderId)/jwks" $qp)
  let accept_val = "application/vnd.atlas.2023-11-15+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Metadata of One Identity Provider
#
# GET /api/atlas/v2/federationSettings/{federationSettingsId}/identityProviders/{identityProviderId}/metadata.xml
# operationId: getFederationSettingIdentityProviderMetadata
export def "atlas-federation-settings-identity-providers-metadataxml get" [
  federationSettingsId: string
  identityProviderId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/api/atlas/v2/federationSettings/($federationSettingsId)/identityProviders/($identityProviderId)/metadata.xml")
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Projects
#
# GET /api/atlas/v2/groups
# operationId: listGroups
export def "atlas-groups listGroups" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/api/atlas/v2/groups" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Project
#
# POST /api/atlas/v2/groups
# operationId: createGroup
export def "atlas-groups createGroup" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --projectOwnerId: string # Unique 24-hexadecimal digit string that identifies the MongoDB Cloud user to whom to grant the Project Owner role on the specified project. If you set this parameter, it overrides the default value of the oldest Organization Owner.
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "projectOwnerId" $projectOwnerId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/api/atlas/v2/groups" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return One Project by Name
#
# GET /api/atlas/v2/groups/byName/{groupName}
# operationId: getGroupByName
export def "atlas-groups-by-name get" [
  groupName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/byName/($groupName)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove One Project
#
# DELETE /api/atlas/v2/groups/{groupId}
# operationId: deleteGroup
export def "atlas-groups delete" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Project
#
# GET /api/atlas/v2/groups/{groupId}
# operationId: getGroup
export def "atlas-groups get" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Project
#
# PATCH /api/atlas/v2/groups/{groupId}
# operationId: updateGroup
export def "atlas-groups updateGroup" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Add One MongoDB Cloud User to One Project
#
# POST /api/atlas/v2/groups/{groupId}/access
# DEPRECATED
# operationId: addGroupAccessUser
@deprecated
export def "atlas-groups-access addGroupAccessUser" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/access" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-02-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-02-01+json" $body
}

# Return All Project IP Access List Entries
#
# GET /api/atlas/v2/groups/{groupId}/accessList
# Docs: https://docs.atlas.mongodb.com/security/ip-access-list/#std-label-access-list — IP access list
# operationId: listGroupAccessListEntries
export def "atlas-groups-access-list listGroupAccessListEntries" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/accessList" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add Entries to Project IP Access List
#
# POST /api/atlas/v2/groups/{groupId}/accessList
# Docs: https://docs.atlas.mongodb.com/security/ip-access-list/#std-label-access-list — IP access list
# operationId: createGroupAccessListEntry
export def "atlas-groups-access-list createGroupAccessListEntry" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/accessList" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Remove One Entry from One Project IP Access List
#
# DELETE /api/atlas/v2/groups/{groupId}/accessList/{entryValue}
# Docs: https://docs.atlas.mongodb.com/security/ip-access-list/#std-label-access-list — IP access list
# operationId: deleteGroupAccessListEntry
export def "atlas-groups-access-list delete" [
  groupId: string
  entryValue: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/accessList/($entryValue)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Project IP Access List Entry
#
# GET /api/atlas/v2/groups/{groupId}/accessList/{entryValue}
# Docs: https://docs.atlas.mongodb.com/security/ip-access-list/#std-label-access-list — IP access list
# operationId: getGroupAccessListEntry
export def "atlas-groups-access-list get" [
  groupId: string
  entryValue: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/accessList/($entryValue)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Status of One Project IP Access List Entry
#
# GET /api/atlas/v2/groups/{groupId}/accessList/{entryValue}/status
# operationId: getGroupAccessListStatus
export def "atlas-groups-access-list-status get" [
  groupId: string
  entryValue: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/accessList/($entryValue)/status" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Pre-Filtered Activity Feed Link for One Project
#
# GET /api/atlas/v2/groups/{groupId}/activityFeed
# operationId: getGroupActivityFeed
export def "atlas-groups-activity-feed get" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --eventType: list # Category of incident recorded at this moment in time.  **IMPORTANT**: The complete list of event type values changes frequently.
  --maxDate: string # End date and time for events to include in the activity feed link. ISO 8601 timestamp format in UTC. (format: date-time)
  --minDate: string # Start date and time for events to include in the activity feed link. ISO 8601 timestamp format in UTC. (format: date-time)
  --clusterName: list # Human-readable label that identifies the cluster.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "eventType" $eventType "multi") (serialize-qp "maxDate" $maxDate "scalar") (serialize-qp "minDate" $minDate "scalar") (serialize-qp "clusterName" $clusterName "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/activityFeed" $qp)
  let accept_val = "application/vnd.atlas.2025-03-12+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return AI Model API Keys for One Group
#
# GET /api/atlas/v2/groups/{groupId}/aiModelApiKeys
# operationId: listGroupAiModelApiKeys
export def "atlas-groups-ai-model-api-keys listGroupAiModelApiKeys" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/aiModelApiKeys" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create New AI Model API Key
#
# POST /api/atlas/v2/groups/{groupId}/aiModelApiKeys
# operationId: createGroupAiModelApiKey
export def "atlas-groups-ai-model-api-keys createGroupAiModelApiKey" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/aiModelApiKeys" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.preview+json" $body
}

# Delete Existing AI Model API Key
#
# DELETE /api/atlas/v2/groups/{groupId}/aiModelApiKeys/{apiKeyId}
# operationId: deleteGroupAiModelApiKey
export def "atlas-groups-ai-model-api-keys delete" [
  groupId: string
  apiKeyId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/aiModelApiKeys/($apiKeyId)" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Single AI Model API Key for One Group
#
# GET /api/atlas/v2/groups/{groupId}/aiModelApiKeys/{apiKeyId}
# operationId: getGroupAiModelApiKey
export def "atlas-groups-ai-model-api-keys get" [
  groupId: string
  apiKeyId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/aiModelApiKeys/($apiKeyId)" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update Existing AI Model API Key
#
# PATCH /api/atlas/v2/groups/{groupId}/aiModelApiKeys/{apiKeyId}
# operationId: updateGroupAiModelApiKey
export def "atlas-groups-ai-model-api-keys updateGroupAiModelApiKey" [
  groupId: string
  apiKeyId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/aiModelApiKeys/($apiKeyId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.preview+json" $body
}

# Return AI Model Rate Limits for One Group
#
# GET /api/atlas/v2/groups/{groupId}/aiModelRateLimits
# operationId: listGroupAiModelRateLimits
export def "atlas-groups-ai-model-rate-limits listGroupAiModelRateLimits" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/aiModelRateLimits" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Single AI Model Rate Limit for One Group
#
# GET /api/atlas/v2/groups/{groupId}/aiModelRateLimits/{modelGroupName}
# operationId: getGroupAiModelRateLimit
export def "atlas-groups-ai-model-rate-limits get" [
  groupId: string
  modelGroupName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/aiModelRateLimits/($modelGroupName)" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update AI Model Rate Limit
#
# PATCH /api/atlas/v2/groups/{groupId}/aiModelRateLimits/{modelGroupName}
# operationId: updateGroupAiModelRateLimit
export def "atlas-groups-ai-model-rate-limits updateGroupAiModelRateLimit" [
  groupId: string
  modelGroupName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/aiModelRateLimits/($modelGroupName)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.preview+json" $body
}

# Reset AI Model Rate Limit for One Model Group
#
# POST /api/atlas/v2/groups/{groupId}/aiModelRateLimits/{modelGroupName}:reset
# operationId: resetGroupAiModelRateLimit
export def "atlas-groups-ai-model-rate-limits resetGroupAiModelRateLimit" [
  groupId: string
  modelGroupName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/aiModelRateLimits/($modelGroupName):reset" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Reset AI Model Rate Limits for Group
#
# POST /api/atlas/v2/groups/{groupId}/aiModelRateLimits:reset
# operationId: resetGroupAiModelRateLimits
export def "atlas-groups-ai-model-rate-limits-reset resetGroupAiModelRateLimits" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/aiModelRateLimits:reset" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Alert Configurations in One Project
#
# GET /api/atlas/v2/groups/{groupId}/alertConfigs
# operationId: listGroupAlertConfigs
export def "atlas-groups-alert-configs listGroupAlertConfigs" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/alertConfigs" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Alert Configuration in One Project
#
# POST /api/atlas/v2/groups/{groupId}/alertConfigs
# operationId: createGroupAlertConfig
export def "atlas-groups-alert-configs createGroupAlertConfig" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/alertConfigs" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Remove One Alert Configuration from One Project
#
# DELETE /api/atlas/v2/groups/{groupId}/alertConfigs/{alertConfigId}
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Alert-Configurations/operation/listAlertConfigurations — Return All Alert Configurations for One Project
# operationId: deleteGroupAlertConfig
export def "atlas-groups-alert-configs delete" [
  groupId: string
  alertConfigId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/alertConfigs/($alertConfigId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Alert Configuration from One Project
#
# GET /api/atlas/v2/groups/{groupId}/alertConfigs/{alertConfigId}
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Alert-Configurations/operation/listAlertConfigurations — Return All Alert Configurations for One Project
# operationId: getGroupAlertConfig
export def "atlas-groups-alert-configs get" [
  groupId: string
  alertConfigId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/alertConfigs/($alertConfigId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Toggle State of One Alert Configuration in One Project
#
# PATCH /api/atlas/v2/groups/{groupId}/alertConfigs/{alertConfigId}
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Alert-Configurations/operation/updateAlertConfiguration — Update One Alert Configuration
# operationId: toggleGroupAlertConfig
export def "atlas-groups-alert-configs toggleGroupAlertConfig" [
  groupId: string
  alertConfigId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/alertConfigs/($alertConfigId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Update One Alert Configuration in One Project
#
# PUT /api/atlas/v2/groups/{groupId}/alertConfigs/{alertConfigId}
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Alert-Configurations/operation/toggleAlertConfiguration — Toggle One State of One Alert Configuration in One Project
# operationId: updateGroupAlertConfig
export def "atlas-groups-alert-configs updateGroupAlertConfig" [
  groupId: string
  alertConfigId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/alertConfigs/($alertConfigId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Open Alerts for One Alert Configuration
#
# GET /api/atlas/v2/groups/{groupId}/alertConfigs/{alertConfigId}/alerts
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Alert-Configurations/operation/listAlertConfigurations — Return All Alert Configurations for One Project
# operationId: getGroupAlertConfigAlerts
export def "atlas-groups-alert-configs-alerts get" [
  groupId: string
  alertConfigId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/alertConfigs/($alertConfigId)/alerts" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Alerts from One Project
#
# GET /api/atlas/v2/groups/{groupId}/alerts
# operationId: listGroupAlerts
export def "atlas-groups-alerts listGroupAlerts" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --status: string@status-completer # Status of the alerts to return. Omit this parameter to return all alerts in all statuses. TRACKING indicates the alert condition exists but has not persisted for the minimum notification delay. OPEN indicates the alert condition currently exists. CLOSED indicates the alert condition has been resolved.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "status" $status "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/alerts" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Alert from One Project
#
# GET /api/atlas/v2/groups/{groupId}/alerts/{alertId}
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Alerts/operation/listAlerts — Return All Alerts from One Project
# operationId: getGroupAlert
export def "atlas-groups-alerts get" [
  groupId: string
  alertId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/alerts/($alertId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Acknowledge One Alert from One Project
#
# PATCH /api/atlas/v2/groups/{groupId}/alerts/{alertId}
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Alerts/operation/listAlerts — Return All Alerts from One Project
# operationId: acknowledgeGroupAlert
export def "atlas-groups-alerts acknowledgeGroupAlert" [
  groupId: string
  alertId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-1 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/alerts/($alertId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Alert Configurations Set for One Alert
#
# GET /api/atlas/v2/groups/{groupId}/alerts/{alertId}/alertConfigs
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Alerts/operation/listAlerts — Return All Alerts from One Project
# operationId: getGroupAlertAlertConfigs
export def "atlas-groups-alerts-alert-configs get" [
  groupId: string
  alertId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/alerts/($alertId)/alertConfigs" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Organization API Keys Assigned to One Project
#
# GET /api/atlas/v2/groups/{groupId}/apiKeys
# Docs: https://docs.atlas.mongodb.com/configure-api-access/#std-label-atlas-prog-api-key — Programmatic API Keys
# operationId: listGroupApiKeys
export def "atlas-groups-api-keys listGroupApiKeys" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/apiKeys" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create and Assign One Organization API Key to One Project
#
# POST /api/atlas/v2/groups/{groupId}/apiKeys
# operationId: createGroupApiKey
export def "atlas-groups-api-keys createGroupApiKey" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/apiKeys" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Unassign One Organization API Key from One Project
#
# DELETE /api/atlas/v2/groups/{groupId}/apiKeys/{apiUserId}
# Docs: https://docs.atlas.mongodb.com/configure-api-access/#std-label-atlas-prog-api-key — Programmatic API Keys
# operationId: removeGroupApiKey
export def "atlas-groups-api-keys removeGroupApiKey" [
  groupId: string
  apiUserId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/apiKeys/($apiUserId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update Organization API Key Roles for One Project
#
# PATCH /api/atlas/v2/groups/{groupId}/apiKeys/{apiUserId}
# operationId: updateGroupApiKeyRoles
export def "atlas-groups-api-keys updateGroupApiKeyRoles" [
  groupId: string
  apiUserId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/apiKeys/($apiUserId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Assign One Organization API Key to One Project
#
# POST /api/atlas/v2/groups/{groupId}/apiKeys/{apiUserId}
# operationId: addGroupApiKey
export def "atlas-groups-api-keys addGroupApiKey" [
  groupId: string
  apiUserId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/apiKeys/($apiUserId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return Auditing Configuration for One Project
#
# GET /api/atlas/v2/groups/{groupId}/auditLog
# operationId: getGroupAuditLog
export def "atlas-groups-audit-log get" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/auditLog" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update Auditing Configuration for One Project
#
# PATCH /api/atlas/v2/groups/{groupId}/auditLog
# operationId: updateGroupAuditLog
export def "atlas-groups-audit-log updateGroupAuditLog" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/auditLog" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return One Custom DNS Configuration for Atlas Clusters on AWS
#
# GET /api/atlas/v2/groups/{groupId}/awsCustomDNS
# operationId: getGroupAwsCustomDns
export def "atlas-groups-aws-custom-dns get" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/awsCustomDNS" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update State of One Custom DNS Configuration for Atlas Clusters on AWS
#
# PATCH /api/atlas/v2/groups/{groupId}/awsCustomDNS
# operationId: toggleGroupAwsCustomDns
export def "atlas-groups-aws-custom-dns toggleGroupAwsCustomDns" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/awsCustomDNS" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Snapshot Export Buckets
#
# GET /api/atlas/v2/groups/{groupId}/backup/exportBuckets
# operationId: listGroupBackupExportBuckets
export def "atlas-groups-backup-export-buckets listGroupBackupExportBuckets" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-1 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/backup/exportBuckets" $qp)
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Snapshot Export Bucket
#
# POST /api/atlas/v2/groups/{groupId}/backup/exportBuckets
# operationId: createGroupBackupExportBucket
export def "atlas-groups-backup-export-buckets createGroupBackupExportBucket" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-1 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/backup/exportBuckets" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Delete One Snapshot Export Bucket
#
# DELETE /api/atlas/v2/groups/{groupId}/backup/exportBuckets/{exportBucketId}
# operationId: deleteGroupBackupExportBucket
export def "atlas-groups-backup-export-buckets delete" [
  groupId: string
  exportBucketId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/backup/exportBuckets/($exportBucketId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Snapshot Export Bucket
#
# GET /api/atlas/v2/groups/{groupId}/backup/exportBuckets/{exportBucketId}
# operationId: getGroupBackupExportBucket
export def "atlas-groups-backup-export-buckets get" [
  groupId: string
  exportBucketId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-1 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/backup/exportBuckets/($exportBucketId)" $qp)
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Export Bucket Private Networking Settings
#
# PATCH /api/atlas/v2/groups/{groupId}/backup/exportBuckets/{exportBucketId}
# operationId: updateGroupBackupExportBucket
export def "atlas-groups-backup-export-buckets updateGroupBackupExportBucket" [
  groupId: string
  exportBucketId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/backup/exportBuckets/($exportBucketId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-05-30+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-05-30+json" $body
}

# Return Object Storage Private Endpoints for Cloud Backups for One Cloud Provider in One Project
#
# GET /api/atlas/v2/groups/{groupId}/backup/{cloudProvider}/privateEndpoints
# operationId: listGroupBackupPrivateEndpoints
export def "atlas-groups-backup-private-endpoints listGroupBackupPrivateEndpoints" [
  groupId: string
  cloudProvider: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/backup/($cloudProvider)/privateEndpoints" $qp)
  let accept_val = "application/vnd.atlas.2024-11-13+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Object Storage Private Endpoint for Cloud Backups for One Cloud Provider in One Project
#
# POST /api/atlas/v2/groups/{groupId}/backup/{cloudProvider}/privateEndpoints
# operationId: createGroupBackupPrivateEndpoint
export def "atlas-groups-backup-private-endpoints createGroupBackupPrivateEndpoint" [
  groupId: string
  cloudProvider: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/backup/($cloudProvider)/privateEndpoints" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-11-13+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-11-13+json" $body
}

# Delete One Object Storage Private Endpoint for Cloud Backups for One Cloud Provider from One Project
#
# DELETE /api/atlas/v2/groups/{groupId}/backup/{cloudProvider}/privateEndpoints/{endpointId}
# operationId: deleteGroupBackupPrivateEndpoint
export def "atlas-groups-backup-private-endpoints delete" [
  groupId: string
  cloudProvider: string
  endpointId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/backup/($cloudProvider)/privateEndpoints/($endpointId)" $qp)
  let accept_val = "application/vnd.atlas.2024-11-13+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Object Storage Private Endpoint for Cloud Backups for One Cloud Provider in One Project
#
# GET /api/atlas/v2/groups/{groupId}/backup/{cloudProvider}/privateEndpoints/{endpointId}
# operationId: getGroupBackupPrivateEndpoint
export def "atlas-groups-backup-private-endpoints get" [
  groupId: string
  cloudProvider: string
  endpointId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/backup/($cloudProvider)/privateEndpoints/($endpointId)" $qp)
  let accept_val = "application/vnd.atlas.2024-11-13+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Disable Backup Compliance Policy Settings
#
# DELETE /api/atlas/v2/groups/{groupId}/backupCompliancePolicy
# operationId: disableGroupBackupCompliancePolicy
export def "atlas-groups-backup-compliance-policy disableGroupBackupCompliancePolicy" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/backupCompliancePolicy" $qp)
  let accept_val = "application/vnd.atlas.2024-11-13+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Backup Compliance Policy Settings
#
# GET /api/atlas/v2/groups/{groupId}/backupCompliancePolicy
# operationId: getGroupBackupCompliancePolicy
export def "atlas-groups-backup-compliance-policy get" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-2 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/backupCompliancePolicy" $qp)
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update Backup Compliance Policy Settings
#
# PUT /api/atlas/v2/groups/{groupId}/backupCompliancePolicy
# operationId: updateGroupBackupCompliancePolicy
export def "atlas-groups-backup-compliance-policy updateGroupBackupCompliancePolicy" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-2 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --overwriteBackupPolicies: oneof<nothing, bool> # Flag that indicates whether to overwrite non complying backup policies with the new data protection settings or not. (default: true)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "overwriteBackupPolicies" $overwriteBackupPolicies "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/backupCompliancePolicy" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Cloud Provider Access Roles
#
# GET /api/atlas/v2/groups/{groupId}/cloudProviderAccess
# operationId: listGroupCloudProviderAccess
export def "atlas-groups-cloud-provider-access listGroupCloudProviderAccess" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/cloudProviderAccess" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Cloud Provider Access Role
#
# POST /api/atlas/v2/groups/{groupId}/cloudProviderAccess
# Docs: https://www.mongodb.com/docs/atlas/security/cloud-provider-access/ — Set Up Access to Cloud Providers
# operationId: createGroupCloudProviderAccess
export def "atlas-groups-cloud-provider-access createGroupCloudProviderAccess" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/cloudProviderAccess" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Deauthorize One Cloud Provider Access Role
#
# DELETE /api/atlas/v2/groups/{groupId}/cloudProviderAccess/{cloudProvider}/{roleId}
# operationId: deauthorizeGroupCloudProviderAccessRole
export def "atlas-groups-cloud-provider-access deauthorizeGroupCloudProviderAccessRole" [
  groupId: string
  cloudProvider: string
  roleId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/cloudProviderAccess/($cloudProvider)/($roleId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Cloud Provider Access Role
#
# GET /api/atlas/v2/groups/{groupId}/cloudProviderAccess/{roleId}
# operationId: getGroupCloudProviderAccess
export def "atlas-groups-cloud-provider-access get" [
  groupId: string
  roleId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/cloudProviderAccess/($roleId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Authorize One Cloud Provider Access Role
#
# PATCH /api/atlas/v2/groups/{groupId}/cloudProviderAccess/{roleId}
# Docs: https://www.mongodb.com/docs/atlas/security/cloud-provider-access/ — Set Up Access to Cloud Providers
# operationId: authorizeGroupCloudProviderAccessRole
export def "atlas-groups-cloud-provider-access authorizeGroupCloudProviderAccessRole" [
  groupId: string
  roleId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/cloudProviderAccess/($roleId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Validate One Cluster Configuration
#
# POST /api/atlas/v2/groups/{groupId}/clusterConfigurations:validate
# operationId: validateGroupClusterConfigurations
export def "atlas-groups-cluster-configurations-validate validateGroupClusterConfigurations" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusterConfigurations:validate" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2025-03-12+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2025-03-12+json" $body
}

# Return All Clusters in One Project
#
# GET /api/atlas/v2/groups/{groupId}/clusters
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Flex-Clusters/operation/listFlexClusters — List Flex Clusters
# operationId: listGroupClusters
export def "atlas-groups-clusters listGroupClusters" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-3 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --includeDeletedWithRetainedBackups: oneof<nothing, bool> # Flag that indicates whether to return Clusters with retain backups. (default: false)
  --Use-Effective-Instance-Fields: oneof<nothing, bool> # Controls how hardware specification fields are returned in the response. When set to true, returns the original client-specified values and provides separate effective fields showing current operational values. When false (default), hardware specification fields show current operational values directly. Primarily used for autoscaling compatibility.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "includeDeletedWithRetainedBackups" $includeDeletedWithRetainedBackups "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters" $qp)
  let extra_headers = {"Use-Effective-Instance-Fields": $Use_Effective_Instance_Fields} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Cluster in One Project
#
# POST /api/atlas/v2/groups/{groupId}/clusters
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Flex-Clusters/operation/createFlexCluster — Create Flex Cluster
# operationId: createGroupCluster
export def "atlas-groups-clusters createGroupCluster" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-4 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --Use-Effective-Instance-Fields: oneof<nothing, bool> # Controls how hardware specification fields are returned in the response after cluster creation. When set to true, returns the original client-specified values and provides separate effective fields showing current operational values. When false (default), hardware specification fields show current operational values directly. Primarily used for autoscaling compatibility.
  --Use-Effective-Fields-Replication-Specs: oneof<nothing, bool> # Controls how `replicationSpecs` fields are returned in the response. When set to `true`, stores the client's view of `replicationSpecs` and returns it in `replicationSpecs`, while the actual cluster state (including auto-scaled hardware and auto-added shards) is returned in `effectiveReplicationSpecs`. When `false` (default), `replicationSpecs` contains the actual cluster state.
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Use-Effective-Instance-Fields": $Use_Effective_Instance_Fields, "Use-Effective-Fields-Replication-Specs": $Use_Effective_Fields_Replication_Specs} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Cloud Provider Regions
#
# GET /api/atlas/v2/groups/{groupId}/clusters/provider/regions
# operationId: listGroupClusterProviderRegions
export def "atlas-groups-clusters-provider-regions listGroupClusterProviderRegions" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --providers: list # Cloud providers whose regions to retrieve. When you specify multiple providers, the response can return only tiers and regions that support multi-cloud clusters.
  --tier: string # Cluster tier for which to retrieve the regions.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "providers" $providers "multi") (serialize-qp "tier" $tier "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/provider/regions" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Upgrade One Shared-Tier Cluster
#
# POST /api/atlas/v2/groups/{groupId}/clusters/tenantUpgrade
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Flex-Clusters/operation/upgradeFlexCluster — Upgrade Flex Cluster
# operationId: upgradeGroupClusterTenantUpgrade
export def "atlas-groups-clusters-tenant-upgrade upgradeGroupClusterTenantUpgrade" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/tenantUpgrade" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Upgrade One Shared-Tier Cluster to One Serverless Instance
#
# POST /api/atlas/v2/groups/{groupId}/clusters/tenantUpgradeToServerless
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Flex-Clusters/operation/upgradeFlexCluster — Upgrade Flex Cluster
# operationId: upgradeGroupClusterTenantUpgradeToServerless
@deprecated
export def "atlas-groups-clusters-tenant-upgrade-to-serverless upgradeGroupClusterTenantUpgradeToServerless" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/tenantUpgradeToServerless" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Remove One Cluster from One Project
#
# DELETE /api/atlas/v2/groups/{groupId}/clusters/{clusterName}
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Flex-Clusters/operation/deleteFlexCluster — Delete Flex Cluster
# operationId: deleteGroupCluster
export def "atlas-groups-clusters delete" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-5 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --retainBackups: oneof<nothing, bool> # Flag that indicates whether to retain backup snapshots for the deleted dedicated cluster.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "retainBackups" $retainBackups "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)" $qp)
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Cluster from One Project
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Flex-Clusters/operation/getFlexCluster — Get Flex Cluster
# operationId: getGroupCluster
export def "atlas-groups-clusters get" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-3 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --Use-Effective-Instance-Fields: oneof<nothing, bool> # Controls how hardware specification fields are returned in the response. When set to true, returns the original client-specified values and provides separate effective fields showing current operational values. When false (default), hardware specification fields show current operational values directly. Primarily used for autoscaling compatibility.
  --Use-Effective-Fields-Replication-Specs: oneof<nothing, bool> # Controls how `replicationSpecs` are returned in the response. When set to `true`, returns the client-specified view in `replicationSpecs` and the actual cluster state in `effectiveReplicationSpecs`. When `false` (default), `replicationSpecs` contains the actual cluster state.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)" $qp)
  let extra_headers = {"Use-Effective-Instance-Fields": $Use_Effective_Instance_Fields, "Use-Effective-Fields-Replication-Specs": $Use_Effective_Fields_Replication_Specs} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Cluster in One Project
#
# PATCH /api/atlas/v2/groups/{groupId}/clusters/{clusterName}
# operationId: updateGroupCluster
export def "atlas-groups-clusters updateGroupCluster" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-4 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --Use-Effective-Instance-Fields: oneof<nothing, bool> # Controls how hardware specification fields are returned in the response after cluster updates. When set to true, returns the original client-specified values and provides separate effective fields showing current operational values. When false (default), hardware specification fields show current operational values directly. Note: When using this header with autoscaling enabled, MongoDB ignores `replicationSpecs` changes during updates. To intentionally override the `replicationSpecs`, disable this header.
  --Use-Effective-Fields-Replication-Specs: oneof<nothing, bool> # Controls how `replicationSpecs` fields are returned in the response. When set to `true`, stores the client's view of `replicationSpecs` and returns it in `replicationSpecs`, while the actual cluster state (including auto-scaled hardware and auto-added shards) is returned in `effectiveReplicationSpecs`. When `false` (default), `replicationSpecs` contains the actual cluster state.
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Use-Effective-Instance-Fields": $Use_Effective_Instance_Fields, "Use-Effective-Fields-Replication-Specs": $Use_Effective_Fields_Replication_Specs} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return Auto Scaling Configuration for One Sharded Cluster
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/autoScalingConfiguration
# DEPRECATED
# operationId: autoGroupClusterScalingConfiguration
@deprecated
export def "atlas-groups-clusters-auto-scaling-configuration autoGroupClusterScalingConfiguration" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/autoScalingConfiguration" $qp)
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Snapshot Export Jobs
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/backup/exports
# operationId: listGroupClusterBackupExports
export def "atlas-groups-clusters-backup-exports listGroupClusterBackupExports" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/backup/exports" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Snapshot Export Job
#
# POST /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/backup/exports
# operationId: createGroupClusterBackupExport
export def "atlas-groups-clusters-backup-exports createGroupClusterBackupExport" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/backup/exports" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return One Snapshot Export Job
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/backup/exports/{exportId}
# operationId: getGroupClusterBackupExport
export def "atlas-groups-clusters-backup-exports get" [
  groupId: string
  clusterName: string
  exportId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/backup/exports/($exportId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Restore Jobs for One Cluster
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/backup/restoreJobs
# operationId: listGroupClusterBackupRestoreJobs
export def "atlas-groups-clusters-backup-restore-jobs listGroupClusterBackupRestoreJobs" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/backup/restoreJobs" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Restore Job of One Cluster
#
# POST /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/backup/restoreJobs
# operationId: createGroupClusterBackupRestoreJob
export def "atlas-groups-clusters-backup-restore-jobs createGroupClusterBackupRestoreJob" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/backup/restoreJobs" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Cancel One Restore Job for One Cluster
#
# DELETE /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/backup/restoreJobs/{restoreJobId}
# operationId: cancelGroupClusterBackupRestoreJob
export def "atlas-groups-clusters-backup-restore-jobs cancelGroupClusterBackupRestoreJob" [
  groupId: string
  clusterName: string
  restoreJobId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/backup/restoreJobs/($restoreJobId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Restore Job for One Cluster
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/backup/restoreJobs/{restoreJobId}
# operationId: getGroupClusterBackupRestoreJob
export def "atlas-groups-clusters-backup-restore-jobs get" [
  groupId: string
  clusterName: string
  restoreJobId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/backup/restoreJobs/($restoreJobId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove All Cloud Backup Schedules
#
# DELETE /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/backup/schedule
# operationId: deleteGroupClusterBackupSchedule
export def "atlas-groups-clusters-backup-schedule delete" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-6 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/backup/schedule" $qp)
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Cloud Backup Schedule
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/backup/schedule
# operationId: getGroupClusterBackupSchedule
export def "atlas-groups-clusters-backup-schedule get" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-6 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/backup/schedule" $qp)
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update Cloud Backup Schedule for One Cluster
#
# PATCH /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/backup/schedule
# operationId: updateGroupClusterBackupSchedule
export def "atlas-groups-clusters-backup-schedule updateGroupClusterBackupSchedule" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-6 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/backup/schedule" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Replica Set Cloud Backups
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/backup/snapshots
# operationId: listGroupClusterBackupSnapshots
export def "atlas-groups-clusters-backup-snapshots listGroupClusterBackupSnapshots" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --pointInTimeUtcSeconds: int # Desired point in time, expressed as the number of seconds that have elapsed since the UNIX epoch. If specified, returns the closest snapshot created before that point in time. Mutually exclusive with `oplogTs` and `oplogInc`. (format: int64)
  --oplogTs: int # Oplog timestamp that represents the desired point in time. This is the first part of an Oplog timestamp. Must be used with `oplogInc`. Mutually exclusive with `pointInTimeUtcSeconds`. (format: int64)
  --oplogInc: int # Oplog operation number that represents the desired point in time. This is the second part of an Oplog timestamp. Must be used with `oplogTs`. Mutually exclusive with `pointInTimeUtcSeconds`. (format: int64)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "pointInTimeUtcSeconds" $pointInTimeUtcSeconds "scalar") (serialize-qp "oplogTs" $oplogTs "scalar") (serialize-qp "oplogInc" $oplogInc "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/backup/snapshots" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Take One On-Demand Snapshot
#
# POST /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/backup/snapshots
# operationId: takeGroupClusterBackupSnapshots
export def "atlas-groups-clusters-backup-snapshots takeGroupClusterBackupSnapshots" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/backup/snapshots" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Remove One Sharded Cluster Cloud Backup
#
# DELETE /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/backup/snapshots/shardedCluster/{snapshotId}
# operationId: deleteGroupClusterBackupSnapshotShardedCluster
export def "atlas-groups-clusters-backup-snapshots-sharded-cluster delete" [
  groupId: string
  clusterName: string
  snapshotId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/backup/snapshots/shardedCluster/($snapshotId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Sharded Cluster Cloud Backup
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/backup/snapshots/shardedCluster/{snapshotId}
# operationId: getGroupClusterBackupSnapshotShardedCluster
export def "atlas-groups-clusters-backup-snapshots-sharded-cluster get" [
  groupId: string
  clusterName: string
  snapshotId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/backup/snapshots/shardedCluster/($snapshotId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Sharded Cluster Cloud Backups
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/backup/snapshots/shardedClusters
# operationId: listGroupClusterBackupSnapshotShardedClusters
export def "atlas-groups-clusters-backup-snapshots-sharded-clusters listGroupClusterBackupSnapshotShardedClusters" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/backup/snapshots/shardedClusters" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove One Replica Set Cloud Backup
#
# DELETE /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/backup/snapshots/{snapshotId}
# operationId: deleteGroupClusterBackupSnapshot
export def "atlas-groups-clusters-backup-snapshots delete" [
  groupId: string
  clusterName: string
  snapshotId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/backup/snapshots/($snapshotId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Replica Set Cloud Backup
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/backup/snapshots/{snapshotId}
# operationId: getGroupClusterBackupSnapshot
export def "atlas-groups-clusters-backup-snapshots get" [
  groupId: string
  clusterName: string
  snapshotId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/backup/snapshots/($snapshotId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update Expiration Date for One Cloud Backup
#
# PATCH /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/backup/snapshots/{snapshotId}
# operationId: updateGroupClusterBackupSnapshot
export def "atlas-groups-clusters-backup-snapshots updateGroupClusterBackupSnapshot" [
  groupId: string
  clusterName: string
  snapshotId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/backup/snapshots/($snapshotId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return Databases in One Snapshot
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/backup/snapshots/{snapshotId}/databases
# operationId: listGroupClusterBackupSnapshotDatabases
export def "atlas-groups-clusters-backup-snapshots-databases listGroupClusterBackupSnapshotDatabases" [
  groupId: string
  clusterName: string
  snapshotId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/backup/snapshots/($snapshotId)/databases" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Database in One Snapshot
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/backup/snapshots/{snapshotId}/databases/{databaseName}
# operationId: getGroupClusterBackupSnapshotDatabase
export def "atlas-groups-clusters-backup-snapshots-databases get" [
  groupId: string
  clusterName: string
  snapshotId: string
  databaseName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/backup/snapshots/($snapshotId)/databases/($databaseName)" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Collections in One Database in One Snapshot
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/backup/snapshots/{snapshotId}/databases/{databaseName}/collections
# operationId: listGroupClusterBackupSnapshotDatabaseCollections
export def "atlas-groups-clusters-backup-snapshots-databases-collections listGroupClusterBackupSnapshotDatabaseCollections" [
  groupId: string
  clusterName: string
  snapshotId: string
  databaseName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/backup/snapshots/($snapshotId)/databases/($databaseName)/collections" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Collection in One Database in One Snapshot
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/backup/snapshots/{snapshotId}/databases/{databaseName}/collections/{collectionName}
# operationId: getGroupClusterBackupSnapshotDatabaseCollection
export def "atlas-groups-clusters-backup-snapshots-databases-collections get" [
  groupId: string
  clusterName: string
  snapshotId: string
  databaseName: string
  collectionName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/backup/snapshots/($snapshotId)/databases/($databaseName)/collections/($collectionName)" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Legacy Backup Checkpoints
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/backupCheckpoints
# DEPRECATED
# operationId: listGroupClusterBackupCheckpoints
@deprecated
export def "atlas-groups-clusters-backup-checkpoints listGroupClusterBackupCheckpoints" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/backupCheckpoints" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Legacy Backup Checkpoint
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/backupCheckpoints/{checkpointId}
# DEPRECATED
# operationId: getGroupClusterBackupCheckpoint
@deprecated
export def "atlas-groups-clusters-backup-checkpoints get" [
  groupId: string
  checkpointId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/backupCheckpoints/($checkpointId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Pinned Namespaces
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/collStats/pinned
# operationId: listGroupClusterCollStatPinnedNamespaces
export def "atlas-groups-clusters-coll-stats-pinned listGroupClusterCollStatPinnedNamespaces" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/collStats/pinned" $qp)
  let accept_val = "application/vnd.atlas.2023-11-15+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add Pinned Namespaces
#
# PATCH /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/collStats/pinned
# operationId: updateGroupClusterCollStatPinnedNamespaces
export def "atlas-groups-clusters-coll-stats-pinned updateGroupClusterCollStatPinnedNamespaces" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/collStats/pinned" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-11-15+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-11-15+json" $body
}

# Pin Namespaces
#
# PUT /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/collStats/pinned
# operationId: pinGroupClusterCollStatPinnedNamespaces
export def "atlas-groups-clusters-coll-stats-pinned pinGroupClusterCollStatPinnedNamespaces" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/collStats/pinned" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-11-15+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-11-15+json" $body
}

# Unpin Namespaces
#
# PATCH /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/collStats/unpin
# operationId: unpinGroupClusterCollStatUnpinNamespaces
export def "atlas-groups-clusters-coll-stats-unpin unpinGroupClusterCollStatUnpinNamespaces" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/collStats/unpin" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-11-15+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-11-15+json" $body
}

# Return All Collection Restore Jobs for One Cluster
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/collectionRestoreJobs
# operationId: listGroupClusterCollectionRestoreJobs
export def "atlas-groups-clusters-collection-restore-jobs listGroupClusterCollectionRestoreJobs" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/collectionRestoreJobs" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Collection Restore Job
#
# POST /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/collectionRestoreJobs
# operationId: createGroupClusterCollectionRestoreJob
export def "atlas-groups-clusters-collection-restore-jobs createGroupClusterCollectionRestoreJob" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/collectionRestoreJobs" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.preview+json" $body
}

# Return One Collection Restore Job for One Cluster
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/collectionRestoreJobs/{jobId}
# operationId: getGroupClusterCollectionRestoreJob
export def "atlas-groups-clusters-collection-restore-jobs get" [
  groupId: string
  clusterName: string
  jobId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/collectionRestoreJobs/($jobId)" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Collection States for One Collection Restore Job
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/collectionRestoreJobs/{jobId}/collections
# operationId: listGroupClusterCollectionRestoreJobCollections
export def "atlas-groups-clusters-collection-restore-jobs-collections listGroupClusterCollectionRestoreJobCollections" [
  groupId: string
  clusterName: string
  jobId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --state: string@state-completer # Collection-level state to filter by.
  --sourceNamespace: string # Source namespace to filter by (e.g. `db.collection`).
  --targetNamespace: string # Target namespace to filter by (e.g. `db.collection`).
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "state" $state "scalar") (serialize-qp "sourceNamespace" $sourceNamespace "scalar") (serialize-qp "targetNamespace" $targetNamespace "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/collectionRestoreJobs/($jobId)/collections" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Collection State for One Collection Restore Job
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/collectionRestoreJobs/{jobId}/collections/{sourceNamespace}
# operationId: getGroupClusterCollectionRestoreJobCollection
export def "atlas-groups-clusters-collection-restore-jobs-collections get" [
  groupId: string
  clusterName: string
  jobId: string
  sourceNamespace: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/collectionRestoreJobs/($jobId)/collections/($sourceNamespace)" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Atlas Search Index
#
# POST /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/fts/indexes
# DEPRECATED
# Docs: https://dochub.mongodb.org/core/index-definitions-fts — Atlas Search Indexes
# operationId: createGroupClusterFtsIndex
@deprecated
export def "atlas-groups-clusters-fts-indexes createGroupClusterFtsIndex" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/fts/indexes" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Atlas Search Indexes for One Collection
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/fts/indexes/{databaseName}/{collectionName}
# DEPRECATED
# Docs: https://dochub.mongodb.org/core/index-definitions-fts — Atlas Search Indexes
# operationId: listGroupClusterFtsIndex
@deprecated
export def "atlas-groups-clusters-fts-indexes listGroupClusterFtsIndex" [
  groupId: string
  clusterName: string
  collectionName: string
  databaseName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/fts/indexes/($databaseName)/($collectionName)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove One Atlas Search Index
#
# DELETE /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/fts/indexes/{indexId}
# DEPRECATED
# Docs: https://dochub.mongodb.org/core/index-definitions-fts — Atlas Search Indexes
# operationId: deleteGroupClusterFtsIndex
@deprecated
export def "atlas-groups-clusters-fts-indexes delete" [
  groupId: string
  clusterName: string
  indexId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/fts/indexes/($indexId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Atlas Search Index
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/fts/indexes/{indexId}
# DEPRECATED
# Docs: https://dochub.mongodb.org/core/index-definitions-fts — Atlas Search Indexes
# operationId: getGroupClusterFtsIndex
@deprecated
export def "atlas-groups-clusters-fts-indexes get" [
  groupId: string
  clusterName: string
  indexId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/fts/indexes/($indexId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Atlas Search Index
#
# PATCH /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/fts/indexes/{indexId}
# DEPRECATED
# Docs: https://dochub.mongodb.org/core/index-definitions-fts — Atlas Search Indexes
# operationId: updateGroupClusterFtsIndex
@deprecated
export def "atlas-groups-clusters-fts-indexes updateGroupClusterFtsIndex" [
  groupId: string
  clusterName: string
  indexId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/fts/indexes/($indexId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return One Managed Namespace in One Global Cluster
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/globalWrites
# Docs: https://www.mongodb.com/docs/atlas/global-clusters/ — Global Clusters
# operationId: getGroupClusterGlobalWrites
export def "atlas-groups-clusters-global-writes get" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-3 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/globalWrites" $qp)
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove All Custom Zone Mappings from One Global Cluster
#
# DELETE /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/globalWrites/customZoneMapping
# Docs: https://www.mongodb.com/docs/atlas/global-clusters/ — Global Clusters
# operationId: deleteGroupClusterGlobalWriteCustomZoneMapping
export def "atlas-groups-clusters-global-writes-custom-zone-mapping delete" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-3 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/globalWrites/customZoneMapping" $qp)
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add One Custom Zone Mapping to One Global Cluster
#
# POST /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/globalWrites/customZoneMapping
# Docs: https://www.mongodb.com/docs/atlas/global-clusters/ — Global Clusters
# operationId: createGroupClusterGlobalWriteCustomZoneMapping
export def "atlas-groups-clusters-global-writes-custom-zone-mapping createGroupClusterGlobalWriteCustomZoneMapping" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-3 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/globalWrites/customZoneMapping" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Remove One Managed Namespace from One Global Cluster
#
# DELETE /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/globalWrites/managedNamespaces
# Docs: https://www.mongodb.com/docs/atlas/global-clusters/ — Global Clusters
# operationId: deleteGroupClusterGlobalWriteManagedNamespaces
export def "atlas-groups-clusters-global-writes-managed-namespaces delete" [
  clusterName: string
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-3 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --db: string # Human-readable label that identifies the database that contains the collection.
  --collection: string # Human-readable label that identifies the collection associated with the managed namespace.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "db" $db "scalar") (serialize-qp "collection" $collection "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/globalWrites/managedNamespaces" $qp)
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Managed Namespace in One Global Cluster
#
# POST /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/globalWrites/managedNamespaces
# Docs: https://www.mongodb.com/docs/atlas/global-clusters/ — Global Clusters
# operationId: createGroupClusterGlobalWriteManagedNamespace
export def "atlas-groups-clusters-global-writes-managed-namespaces createGroupClusterGlobalWriteManagedNamespace" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-3 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/globalWrites/managedNamespaces" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Create One Rolling Index
#
# POST /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/index
# Docs: https://docs.mongodb.com/manual/tutorial/build-indexes-on-replica-sets/ — Rolling Index Builds on Replica Sets
# operationId: createGroupClusterIndexRollingIndex
export def "atlas-groups-clusters-index createGroupClusterIndexRollingIndex" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/index" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Online Archives for One Cluster
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/onlineArchives
# Docs: https://docs.atlas.mongodb.com/online-archive/manage-online-archive/#std-label-manage-online-archive — Online Archive
# operationId: listGroupClusterOnlineArchives
export def "atlas-groups-clusters-online-archives listGroupClusterOnlineArchives" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/onlineArchives" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Online Archive
#
# POST /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/onlineArchives
# Docs: https://docs.atlas.mongodb.com/online-archive/manage-online-archive/#std-label-manage-online-archive — Online Archive
# operationId: createGroupClusterOnlineArchive
export def "atlas-groups-clusters-online-archives createGroupClusterOnlineArchive" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/onlineArchives" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Download Online Archive Query Logs
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/onlineArchives/queryLogs.gz
# Docs: https://docs.atlas.mongodb.com/online-archive/manage-online-archive/#std-label-manage-online-archive — Online Archive
# operationId: downloadGroupClusterOnlineArchiveQueryLogs
export def "atlas-groups-clusters-online-archives-query-logsgz downloadGroupClusterOnlineArchiveQueryLogs" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --startDate: int # Date and time that specifies the starting point for the range of log messages to return. This resource expresses this value in the number of seconds that have elapsed since the [UNIX epoch](https://en.wikipedia.org/wiki/Unix_time). (format: int64, e.g. 1636481348)
  --endDate: int # Date and time that specifies the end point for the range of log messages to return. This resource expresses this value in the number of seconds that have elapsed since the [UNIX epoch](https://en.wikipedia.org/wiki/Unix_time). (format: int64, e.g. 1636481348)
  --archiveOnly: oneof<nothing, bool> # Flag that indicates whether to download logs for queries against your online archive only or both your online archive and cluster. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "startDate" $startDate "scalar") (serialize-qp "endDate" $endDate "scalar") (serialize-qp "archiveOnly" $archiveOnly "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/onlineArchives/queryLogs.gz" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+gzip"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove One Online Archive
#
# DELETE /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/onlineArchives/{archiveId}
# Docs: https://docs.atlas.mongodb.com/online-archive/manage-online-archive/#std-label-manage-online-archive — Online Archive
# operationId: deleteGroupClusterOnlineArchive
export def "atlas-groups-clusters-online-archives delete" [
  groupId: string
  archiveId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/onlineArchives/($archiveId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Online Archive
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/onlineArchives/{archiveId}
# Docs: https://docs.atlas.mongodb.com/online-archive/manage-online-archive/#std-label-manage-online-archive — Online Archive
# operationId: getGroupClusterOnlineArchive
export def "atlas-groups-clusters-online-archives get" [
  groupId: string
  archiveId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/onlineArchives/($archiveId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Online Archive
#
# PATCH /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/onlineArchives/{archiveId}
# Docs: https://docs.atlas.mongodb.com/online-archive/manage-online-archive/#std-label-manage-online-archive — Online Archive
# operationId: updateGroupClusterOnlineArchive
export def "atlas-groups-clusters-online-archives updateGroupClusterOnlineArchive" [
  groupId: string
  archiveId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/onlineArchives/($archiveId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# End One Outage Simulation
#
# DELETE /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/outageSimulation
# Docs: https://dochub.mongodb.org/core/test-regional-outage — Cluster Outage Simulation
# operationId: endGroupClusterOutageSimulation
export def "atlas-groups-clusters-outage-simulation endGroupClusterOutageSimulation" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/outageSimulation" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Outage Simulation
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/outageSimulation
# Docs: https://dochub.mongodb.org/core/test-regional-outage — Cluster Outage Simulation
# operationId: getGroupClusterOutageSimulation
export def "atlas-groups-clusters-outage-simulation get" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/outageSimulation" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Start One Outage Simulation
#
# POST /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/outageSimulation
# Docs: https://dochub.mongodb.org/core/test-regional-outage — Cluster Outage Simulation
# operationId: startGroupClusterOutageSimulation
export def "atlas-groups-clusters-outage-simulation startGroupClusterOutageSimulation" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/outageSimulation" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Suggested Indexes to Drop
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/performanceAdvisor/dropIndexSuggestions
# operationId: listGroupClusterPerformanceAdvisorDropIndexSuggestions
export def "atlas-groups-clusters-performance-advisor-drop-index-suggestions listGroupClusterPerformanceAdvisorDropIndexSuggestions" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/performanceAdvisor/dropIndexSuggestions")
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Schema Advice
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/performanceAdvisor/schemaAdvice
# operationId: listGroupClusterPerformanceAdvisorSchemaAdvice
export def "atlas-groups-clusters-performance-advisor-schema-advice listGroupClusterPerformanceAdvisorSchemaAdvice" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/performanceAdvisor/schemaAdvice")
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Suggested Indexes
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/performanceAdvisor/suggestedIndexes
# operationId: listGroupClusterPerformanceAdvisorSuggestedIndexes
export def "atlas-groups-clusters-performance-advisor-suggested-indexes listGroupClusterPerformanceAdvisorSuggestedIndexes" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --processIds: list # Process IDs from which to retrieve suggested indexes. A `processId` is a combination of host and port that serves the MongoDB process. The host must be the hostname, FQDN, IPv4 address, or IPv6 address of the host that runs the MongoDB process (`mongod` or `mongos`). The port must be the IANA port on which the MongoDB process listens for requests. To include multiple `processIds`, pass the parameter multiple times delimited with an ampersand (`&`) between each `processId`.
  --namespaces: list # Namespaces from which to retrieve suggested indexes. A namespace consists of one database and one collection resource written as `.`: `<database>.<collection>`. To include multiple namespaces, pass the parameter multiple times delimited with an ampersand (`&`) between each namespace. Omit this parameter to return results for all namespaces.
  --since: int # Date and time from which the query retrieves the suggested indexes. This parameter expresses its value in the number of milliseconds that have elapsed since the [UNIX epoch](https://en.wikipedia.org/wiki/Unix_time).  - If you don't specify the **until** parameter, the endpoint returns data covering from the **since** value and the current time. - If you specify neither the **since** nor the **until** parameters, the endpoint returns data from the previous 24 hours. (format: int64)
  --until: int # Date and time up until which the query retrieves the suggested indexes. This parameter expresses its value in the number of milliseconds that have elapsed since the [UNIX epoch](https://en.wikipedia.org/wiki/Unix_time).  - If you specify the **until** parameter, you must specify the **since** parameter. - If you specify neither the **since** nor the **until** parameters, the endpoint returns data from the previous 24 hours. (format: int64)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "processIds" $processIds "multi") (serialize-qp "namespaces" $namespaces "multi") (serialize-qp "since" $since "scalar") (serialize-qp "until" $until "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/performanceAdvisor/suggestedIndexes" $qp)
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Advanced Configuration Options for One Cluster
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/processArgs
# Docs: https://www.mongodb.com/docs/atlas/global-clusters/ — Global Clusters
# operationId: getGroupClusterProcessArgs
export def "atlas-groups-clusters-process-args get" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-6 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/processArgs" $qp)
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update Advanced Configuration Options for One Cluster
#
# PATCH /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/processArgs
# Docs: https://www.mongodb.com/docs/atlas/global-clusters/ — Global Clusters
# operationId: updateGroupClusterProcessArgs
export def "atlas-groups-clusters-process-args updateGroupClusterProcessArgs" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-6 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/processArgs" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return Query Statistic Summaries
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/queryShapeInsights/summaries
# operationId: listGroupClusterQueryShapeInsightSummaries
export def "atlas-groups-clusters-query-shape-insights-summaries listGroupClusterQueryShapeInsightSummaries" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --since: int # Date and time from which to retrieve query shape statistics. This parameter expresses its value in the number of milliseconds that have elapsed since the [UNIX epoch](https://en.wikipedia.org/wiki/Unix_time).  - If you don't specify the **until** parameter, the endpoint returns data covering from the **since** value and the current time. - If you specify neither the **since** nor the **until** parameters, the endpoint returns data from the previous 24 hours. (format: int64)
  --until: int # Date and time up until which to retrieve query shape statistics. This parameter expresses its value in the number of milliseconds that have elapsed since the [UNIX epoch](https://en.wikipedia.org/wiki/Unix_time).  - If you specify the **until** parameter, you must specify the **since** parameter. - If you specify neither the **since** nor the **until** parameters, the endpoint returns data from the previous 24 hours. (format: int64)
  --processIds: list # Process IDs from which to retrieve query shape statistics. A `processId` is a combination of host and port that serves the MongoDB process. The host must be the hostname, FQDN, IPv4 address, or IPv6 address of the host that runs the MongoDB process (`mongod` or `mongos`). The port must be the IANA port on which the MongoDB process listens for requests. To include multiple `processId`, pass the parameter multiple times delimited with an ampersand (`&`) between each `processId`.
  --namespaces: list # Namespaces from which to retrieve query shape statistics. A namespace consists of one database and one collection resource written as `.`: `<database>.<collection>`. To include multiple namespaces, pass the parameter multiple times delimited with an ampersand (`&`) between each namespace. Omit this parameter to return results for all namespaces.
  --commands: list # Retrieve query shape statistics matching specified MongoDB commands. To include multiple commands, pass the parameter multiple times delimited with an ampersand (`&`) between each command. The currently supported parameters are find, distinct, and aggregate. Omit this parameter to return results for all supported commands.
  --nSummaries: int # Maximum number of query statistic summaries to return. (format: int64, default: 100)
  --series: list # Query shape statistics data series to retrieve. A series represents a specific metric about query execution. To include multiple series, pass the parameter multiple times delimited with an ampersand (`&`) between each series. Omit this parameter to return results for all available series.
  --queryShapeHashes: list # A list of SHA256 hashes of desired query shapes, output by MongoDB commands like `$queryStats` and $explain or slow query logs. To include multiple series, pass the parameter multiple times delimited with an ampersand (`&`) between each series. Omit this parameter to return results for all available series.
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "since" $since "scalar") (serialize-qp "until" $until "scalar") (serialize-qp "processIds" $processIds "multi") (serialize-qp "namespaces" $namespaces "multi") (serialize-qp "commands" $commands "multi") (serialize-qp "nSummaries" $nSummaries "scalar") (serialize-qp "series" $series "multi") (serialize-qp "queryShapeHashes" $queryShapeHashes "multi") (serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/queryShapeInsights/summaries" $qp)
  let accept_val = "application/vnd.atlas.2025-03-12+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Query Shape Details
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/queryShapeInsights/{queryShapeHash}/details
# operationId: getGroupClusterQueryShapeInsightDetails
export def "atlas-groups-clusters-query-shape-insights-details get" [
  groupId: string
  clusterName: string
  queryShapeHash: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --since: int # Date and time from which to retrieve query shape statistics. This parameter expresses its value in the number of milliseconds that have elapsed since the [UNIX epoch](https://en.wikipedia.org/wiki/Unix_time).  - If you don't specify the **until** parameter, the endpoint returns data covering from the **since** value and the current time. - If you specify neither the **since** nor the **until** parameters, the endpoint returns data from the previous 24 hours. (format: int64)
  --until: int # Date and time up until which to retrieve query shape statistics. This parameter expresses its value in the number of milliseconds that have elapsed since the [UNIX epoch](https://en.wikipedia.org/wiki/Unix_time).  - If you specify the **until** parameter, you must specify the **since** parameter. - If you specify neither the **since** nor the **until** parameters, the endpoint returns data from the previous 24 hours. (format: int64)
  --processIds: list # Process IDs from which to retrieve query shape statistics. A `processId` is a combination of host and port that serves the MongoDB process. The host must be the hostname, FQDN, IPv4 address, or IPv6 address of the host that runs the MongoDB process (`mongod` or `mongos`). The port must be the IANA port on which the MongoDB process listens for requests. To include multiple `processIds`, pass the parameter multiple times delimited with an ampersand (`&`) between each `processId`.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "since" $since "scalar") (serialize-qp "until" $until "scalar") (serialize-qp "processIds" $processIds "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/queryShapeInsights/($queryShapeHash)/details" $qp)
  let accept_val = "application/vnd.atlas.2025-03-12+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Query Shapes
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/queryShapes
# operationId: listGroupClusterQueryShapes
export def "atlas-groups-clusters-query-shapes listGroupClusterQueryShapes" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --status: string@status-completer-1 # The status of query shapes to retrieve. Only REJECTED status is supported. If omitted, defaults to REJECTED. (default: REJECTED)
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "status" $status "scalar") (serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/queryShapes" $qp)
  let accept_val = "application/vnd.atlas.2025-03-12+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Query Shape
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/queryShapes/{queryShapeHash}
# operationId: getGroupClusterQueryShape
export def "atlas-groups-clusters-query-shapes get" [
  groupId: string
  clusterName: string
  queryShapeHash: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/queryShapes/($queryShapeHash)" $qp)
  let accept_val = "application/vnd.atlas.2025-03-12+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update Query Shape Rejection Status
#
# PATCH /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/queryShapes/{queryShapeHash}
# operationId: updateGroupClusterQueryShape
export def "atlas-groups-clusters-query-shapes updateGroupClusterQueryShape" [
  groupId: string
  clusterName: string
  queryShapeHash: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/queryShapes/($queryShapeHash)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2025-03-12+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2025-03-12+json" $body
}

# Test Failover for One Cluster
#
# POST /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/restartPrimaries
# operationId: restartGroupClusterPrimaries
export def "atlas-groups-clusters-restart-primaries restartGroupClusterPrimaries" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-5 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/restartPrimaries" $qp)
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Legacy Backup Restore Jobs
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/restoreJobs
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/backup/cloud-backup/scheduling/#std-label-cloud-provider-backup-schedule — Cloud Backup Documentation
# operationId: listGroupClusterRestoreJobs
@deprecated
export def "atlas-groups-clusters-restore-jobs listGroupClusterRestoreJobs" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --batchId: string # Unique 24-hexadecimal digit string that identifies the batch of restore jobs to return. Timestamp in ISO 8601 date and time format in UTC when creating a restore job for a sharded cluster, Application creates a separate job for each shard, plus another for the config host. Each of these jobs comprise one batch. A restore job for a replica set can't be part of a batch.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "batchId" $batchId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/restoreJobs" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Legacy Backup Restore Job
#
# POST /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/restoreJobs
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/backup/cloud-backup/scheduling/#std-label-cloud-provider-backup-schedule — Cloud Backup Documentation
# operationId: createGroupClusterRestoreJob
@deprecated
export def "atlas-groups-clusters-restore-jobs createGroupClusterRestoreJob" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/restoreJobs" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return One Legacy Backup Restore Job
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/restoreJobs/{jobId}
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/backup/cloud-backup/scheduling/#std-label-cloud-provider-backup-schedule — Cloud Backup Documentation
# operationId: getGroupClusterRestoreJob
@deprecated
export def "atlas-groups-clusters-restore-jobs get" [
  groupId: string
  clusterName: string
  jobId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/restoreJobs/($jobId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete Search Nodes
#
# DELETE /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/search/deployment
# operationId: deleteGroupClusterSearchDeployment
export def "atlas-groups-clusters-search-deployment delete" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-1 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/search/deployment" $qp)
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Search Nodes
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/search/deployment
# operationId: getGroupClusterSearchDeployment
export def "atlas-groups-clusters-search-deployment get" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-7 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/search/deployment" $qp)
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update Search Nodes
#
# PATCH /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/search/deployment
# operationId: updateGroupClusterSearchDeployment
export def "atlas-groups-clusters-search-deployment updateGroupClusterSearchDeployment" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-1 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/search/deployment" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Create Search Nodes
#
# POST /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/search/deployment
# operationId: createGroupClusterSearchDeployment
export def "atlas-groups-clusters-search-deployment createGroupClusterSearchDeployment" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-1 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/search/deployment" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Atlas Search Indexes for One Cluster
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/search/indexes
# Docs: https://dochub.mongodb.org/core/index-definitions-fts — Atlas Search Indexes
# operationId: listGroupClusterSearchIndexes
export def "atlas-groups-clusters-search-indexes listGroupClusterSearchIndexes" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/search/indexes" $qp)
  let accept_val = "application/vnd.atlas.2024-05-30+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Atlas Search Index
#
# POST /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/search/indexes
# Docs: https://dochub.mongodb.org/core/index-definitions-fts — Atlas Search Indexes
# operationId: createGroupClusterSearchIndex
export def "atlas-groups-clusters-search-indexes createGroupClusterSearchIndex" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/search/indexes" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-05-30+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-05-30+json" $body
}

# Return All Atlas Search Indexes for One Collection
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/search/indexes/{databaseName}/{collectionName}
# Docs: https://dochub.mongodb.org/core/index-definitions-fts — Atlas Search Indexes
# operationId: listGroupClusterSearchIndex
export def "atlas-groups-clusters-search-indexes listGroupClusterSearchIndex" [
  groupId: string
  clusterName: string
  collectionName: string
  databaseName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/search/indexes/($databaseName)/($collectionName)" $qp)
  let accept_val = "application/vnd.atlas.2024-05-30+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove One Atlas Search Index by Name
#
# DELETE /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/search/indexes/{databaseName}/{collectionName}/{indexName}
# Docs: https://dochub.mongodb.org/core/index-definitions-fts — Atlas Search Indexes
# operationId: deleteGroupClusterSearchIndexByName
export def "atlas-groups-clusters-search-indexes delete-by-groupId-clusterName-collectionName-databaseName-indexName" [
  groupId: string
  clusterName: string
  collectionName: string
  databaseName: string
  indexName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/search/indexes/($databaseName)/($collectionName)/($indexName)" $qp)
  let accept_val = "application/vnd.atlas.2024-05-30+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Atlas Search Index by Name
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/search/indexes/{databaseName}/{collectionName}/{indexName}
# Docs: https://dochub.mongodb.org/core/index-definitions-fts — Atlas Search Indexes
# operationId: getGroupClusterSearchIndexByName
export def "atlas-groups-clusters-search-indexes get-by-groupId-clusterName-collectionName-databaseName-indexName" [
  groupId: string
  clusterName: string
  collectionName: string
  databaseName: string
  indexName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/search/indexes/($databaseName)/($collectionName)/($indexName)" $qp)
  let accept_val = "application/vnd.atlas.2024-05-30+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Atlas Search Index by Name
#
# PATCH /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/search/indexes/{databaseName}/{collectionName}/{indexName}
# Docs: https://dochub.mongodb.org/core/index-definitions-fts — Atlas Search Indexes
# operationId: updateGroupClusterSearchIndexByName
export def "atlas-groups-clusters-search-indexes updateGroupClusterSearchIndexByName" [
  groupId: string
  clusterName: string
  collectionName: string
  databaseName: string
  indexName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/search/indexes/($databaseName)/($collectionName)/($indexName)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-05-30+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-05-30+json" $body
}

# Remove One Atlas Search Index by ID
#
# DELETE /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/search/indexes/{indexId}
# Docs: https://dochub.mongodb.org/core/index-definitions-fts — Atlas Search Indexes
# operationId: deleteGroupClusterSearchIndex
export def "atlas-groups-clusters-search-indexes delete-by-groupId-clusterName-indexId" [
  groupId: string
  clusterName: string
  indexId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/search/indexes/($indexId)" $qp)
  let accept_val = "application/vnd.atlas.2024-05-30+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Atlas Search Index by ID
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/search/indexes/{indexId}
# Docs: https://dochub.mongodb.org/core/index-definitions-fts — Atlas Search Indexes
# operationId: getGroupClusterSearchIndex
export def "atlas-groups-clusters-search-indexes get-by-groupId-clusterName-indexId" [
  groupId: string
  clusterName: string
  indexId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/search/indexes/($indexId)" $qp)
  let accept_val = "application/vnd.atlas.2024-05-30+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Atlas Search Index by ID
#
# PATCH /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/search/indexes/{indexId}
# Docs: https://dochub.mongodb.org/core/index-definitions-fts — Atlas Search Indexes
# operationId: updateGroupClusterSearchIndex
export def "atlas-groups-clusters-search-indexes updateGroupClusterSearchIndex" [
  groupId: string
  clusterName: string
  indexId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/search/indexes/($indexId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-05-30+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-05-30+json" $body
}

# Return One Snapshot Schedule
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/snapshotSchedule
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/backup/cloud-backup/scheduling/#std-label-cloud-provider-backup-schedule — Cloud Backup Documentation
# operationId: getGroupClusterSnapshotSchedule
@deprecated
export def "atlas-groups-clusters-snapshot-schedule get" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/snapshotSchedule" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update Snapshot Schedule for One Cluster
#
# PATCH /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/snapshotSchedule
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/backup/cloud-backup/scheduling/#std-label-cloud-provider-backup-schedule — Cloud Backup Documentation
# operationId: updateGroupClusterSnapshotSchedule
@deprecated
export def "atlas-groups-clusters-snapshot-schedule updateGroupClusterSnapshotSchedule" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/snapshotSchedule" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Legacy Backup Snapshots
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/snapshots
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/backup/cloud-backup/scheduling/#std-label-cloud-provider-backup-schedule — Cloud Backup Documentation
# operationId: listGroupClusterSnapshots
@deprecated
export def "atlas-groups-clusters-snapshots listGroupClusterSnapshots" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --completed: string@completed-completer # Human-readable label that specifies whether to return only completed, incomplete, or all snapshots. By default, MongoDB Cloud only returns completed snapshots. (default: true)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "completed" $completed "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/snapshots" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove One Legacy Backup Snapshot
#
# DELETE /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/snapshots/{snapshotId}
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/backup/cloud-backup/scheduling/#std-label-cloud-provider-backup-schedule — Cloud Backup Documentation
# operationId: deleteGroupClusterSnapshot
@deprecated
export def "atlas-groups-clusters-snapshots delete" [
  groupId: string
  clusterName: string
  snapshotId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/snapshots/($snapshotId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Legacy Backup Snapshot
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/snapshots/{snapshotId}
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/backup/cloud-backup/scheduling/#std-label-cloud-provider-backup-schedule — Cloud Backup Documentation
# operationId: getGroupClusterSnapshot
@deprecated
export def "atlas-groups-clusters-snapshots get" [
  groupId: string
  clusterName: string
  snapshotId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/snapshots/($snapshotId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update Expiration Date for One Legacy Backup Snapshot
#
# PATCH /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/snapshots/{snapshotId}
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/backup/cloud-backup/scheduling/#std-label-cloud-provider-backup-schedule — Cloud Backup Documentation
# operationId: updateGroupClusterSnapshot
@deprecated
export def "atlas-groups-clusters-snapshots updateGroupClusterSnapshot" [
  groupId: string
  clusterName: string
  snapshotId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/snapshots/($snapshotId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return Status of All Cluster Operations
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/status
# operationId: getGroupClusterStatus
export def "atlas-groups-clusters-status get" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/status" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Ranked Namespaces from One Cluster
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/{clusterView}/collStats/namespaces
# operationId: getGroupClusterCollStatNamespaces
export def "atlas-groups-clusters-coll-stats-namespaces get" [
  groupId: string
  clusterName: string
  clusterView: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --start: string # Date and time when MongoDB Cloud begins reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**. (format: date-time)
  --end: string # Date and time when MongoDB Cloud stops reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**. (format: date-time)
  --period: string # Duration over which Atlas reports the metrics. This parameter expresses its value in the ISO 8601 duration format in UTC. Include this parameter when you do not set **start** and **end**. (e.g. PT10H)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "start" $start "scalar") (serialize-qp "end" $end "scalar") (serialize-qp "period" $period "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/($clusterView)/collStats/namespaces" $qp)
  let accept_val = "application/vnd.atlas.2023-11-15+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Cluster-Level Query Latency
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{clusterName}/{clusterView}/{databaseName}/{collectionName}/collStats/measurements
# operationId: listGroupClusterCollStatMeasurements
export def "atlas-groups-clusters-coll-stats-measurements listGroupClusterCollStatMeasurements" [
  groupId: string
  clusterName: string
  clusterView: string
  databaseName: string
  collectionName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --metrics: list # List that contains the metrics that you want to retrieve for the associated data series. If you don't set this parameter, this resource returns data series for all Coll Stats Latency metrics.
  --start: string # Date and time when MongoDB Cloud begins reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**. (format: date-time)
  --end: string # Date and time when MongoDB Cloud stops reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**. (format: date-time)
  --period: string # Duration over which Atlas reports the metrics. This parameter expresses its value in the ISO 8601 duration format in UTC. Include this parameter when you do not set **start** and **end**. (e.g. PT10H)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "metrics" $metrics "multi") (serialize-qp "start" $start "scalar") (serialize-qp "end" $end "scalar") (serialize-qp "period" $period "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName)/($clusterView)/($databaseName)/($collectionName)/collStats/measurements" $qp)
  let accept_val = "application/vnd.atlas.2023-11-15+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Grant MongoDB Employee Cluster Access for One Cluster
#
# POST /api/atlas/v2/groups/{groupId}/clusters/{clusterName}:grantMongoDBEmployeeAccess
# operationId: grantGroupClusterMongoDbEmployeeAccess
export def "atlas-groups-clusters grantGroupClusterMongoDbEmployeeAccess" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName):grantMongoDBEmployeeAccess" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-08-05+json" $body
}

# Pin Feature Compatibility Version for One Cluster in One Project
#
# POST /api/atlas/v2/groups/{groupId}/clusters/{clusterName}:pinFeatureCompatibilityVersion
# operationId: pinGroupClusterFeatureCompatibilityVersion
export def "atlas-groups-clusters pinGroupClusterFeatureCompatibilityVersion" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName):pinFeatureCompatibilityVersion" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-05-30+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-05-30+json" $body
}

# Revoke MongoDB Employee Cluster Access for One Cluster
#
# POST /api/atlas/v2/groups/{groupId}/clusters/{clusterName}:revokeMongoDBEmployeeAccess
# operationId: revokeGroupClusterMongoDbEmployeeAccess
export def "atlas-groups-clusters revokeGroupClusterMongoDbEmployeeAccess" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName):revokeMongoDBEmployeeAccess" $qp)
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Unpin Feature Compatibility Version for One Cluster in One Project
#
# POST /api/atlas/v2/groups/{groupId}/clusters/{clusterName}:unpinFeatureCompatibilityVersion
# operationId: unpinGroupClusterFeatureCompatibilityVersion
export def "atlas-groups-clusters unpinGroupClusterFeatureCompatibilityVersion" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($clusterName):unpinFeatureCompatibilityVersion" $qp)
  let accept_val = "application/vnd.atlas.2024-05-30+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download Logs for One Cluster Host in One Project
#
# GET /api/atlas/v2/groups/{groupId}/clusters/{hostName}/logs/{logName}.gz
# operationId: downloadGroupClusterLog
export def "atlas-groups-clusters-logs downloadGroupClusterLog" [
  groupId: string
  hostName: string
  logName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-8 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --endDate: int # Specifies the date and time for the ending point of the range of log messages to retrieve, in the number of seconds that have elapsed since the UNIX epoch. This value will default to 24 hours after the start date. If the start date is also unspecified, the value will default to the time of the request. (format: int64)
  --startDate: int # Specifies the date and time for the starting point of the range of log messages to retrieve, in the number of seconds that have elapsed since the UNIX epoch. This value will default to 24 hours prior to the end date. If the end date is also unspecified, the value will default to 24 hours prior to the time of the request. (format: int64)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "endDate" $endDate "scalar") (serialize-qp "startDate" $startDate "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/clusters/($hostName)/logs/($logName).gz" $qp)
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+gzip")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Metric Names
#
# GET /api/atlas/v2/groups/{groupId}/collStats/metrics
# operationId: listGroupCollStatMetrics
export def "atlas-groups-coll-stats-metrics listGroupCollStatMetrics" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/collStats/metrics" $qp)
  let accept_val = "application/vnd.atlas.2023-11-15+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Network Peering Containers in One Project for One Cloud Provider
#
# GET /api/atlas/v2/groups/{groupId}/containers
# operationId: listGroupContainers
export def "atlas-groups-containers listGroupContainers" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --providerName: string@providerName-completer # Cloud service provider that serves the desired network peering containers. (default: AWS)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "providerName" $providerName "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/containers" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Network Peering Container
#
# POST /api/atlas/v2/groups/{groupId}/containers
# operationId: createGroupContainer
export def "atlas-groups-containers createGroupContainer" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/containers" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Network Peering Containers in One Project
#
# GET /api/atlas/v2/groups/{groupId}/containers/all
# operationId: listGroupContainerAll
export def "atlas-groups-containers-all listGroupContainerAll" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/containers/all" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove One Network Peering Container
#
# DELETE /api/atlas/v2/groups/{groupId}/containers/{containerId}
# operationId: deleteGroupContainer
export def "atlas-groups-containers delete" [
  groupId: string
  containerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/containers/($containerId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Network Peering Container
#
# GET /api/atlas/v2/groups/{groupId}/containers/{containerId}
# operationId: getGroupContainer
export def "atlas-groups-containers get" [
  groupId: string
  containerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/containers/($containerId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Network Peering Container
#
# PATCH /api/atlas/v2/groups/{groupId}/containers/{containerId}
# operationId: updateGroupContainer
export def "atlas-groups-containers updateGroupContainer" [
  groupId: string
  containerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/containers/($containerId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Custom Roles in One Project
#
# GET /api/atlas/v2/groups/{groupId}/customDBRoles/roles
# operationId: listGroupCustomDbRoleRoles
export def "atlas-groups-custom-db-roles-roles listGroupCustomDbRoleRoles" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/customDBRoles/roles" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Custom Role
#
# POST /api/atlas/v2/groups/{groupId}/customDBRoles/roles
# operationId: createGroupCustomDbRoleRole
export def "atlas-groups-custom-db-roles-roles createGroupCustomDbRoleRole" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/customDBRoles/roles" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Remove One Custom Role from One Project
#
# DELETE /api/atlas/v2/groups/{groupId}/customDBRoles/roles/{roleName}
# operationId: deleteGroupCustomDbRoleRole
export def "atlas-groups-custom-db-roles-roles delete" [
  groupId: string
  roleName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/customDBRoles/roles/($roleName)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Custom Role in One Project
#
# GET /api/atlas/v2/groups/{groupId}/customDBRoles/roles/{roleName}
# operationId: getGroupCustomDbRoleRole
export def "atlas-groups-custom-db-roles-roles get" [
  groupId: string
  roleName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/customDBRoles/roles/($roleName)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Custom Role in One Project
#
# PATCH /api/atlas/v2/groups/{groupId}/customDBRoles/roles/{roleName}
# operationId: updateGroupCustomDbRoleRole
export def "atlas-groups-custom-db-roles-roles updateGroupCustomDbRoleRole" [
  groupId: string
  roleName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/customDBRoles/roles/($roleName)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Federated Database Instances in One Project
#
# GET /api/atlas/v2/groups/{groupId}/dataFederation
# operationId: listGroupDataFederation
export def "atlas-groups-data-federation listGroupDataFederation" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --type: string@type-completer # Type of Federated Database Instances to return. (default: USER)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "type" $type "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/dataFederation" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Federated Database Instance in One Project
#
# POST /api/atlas/v2/groups/{groupId}/dataFederation
# operationId: createGroupDataFederation
export def "atlas-groups-data-federation createGroupDataFederation" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --skipRoleValidation: oneof<nothing, bool> # Flag that indicates whether this request should check if the requesting IAM role can read from the S3 bucket. AWS checks if the role can list the objects in the bucket before writing to it. Some IAM roles only need write permissions. This flag allows you to skip that check. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "skipRoleValidation" $skipRoleValidation "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/dataFederation" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Remove One Federated Database Instance from One Project
#
# DELETE /api/atlas/v2/groups/{groupId}/dataFederation/{tenantName}
# operationId: deleteGroupDataFederation
export def "atlas-groups-data-federation delete" [
  groupId: string
  tenantName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/dataFederation/($tenantName)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Federated Database Instance in One Project
#
# GET /api/atlas/v2/groups/{groupId}/dataFederation/{tenantName}
# operationId: getGroupDataFederation
export def "atlas-groups-data-federation get" [
  groupId: string
  tenantName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/dataFederation/($tenantName)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Federated Database Instance in One Project
#
# PATCH /api/atlas/v2/groups/{groupId}/dataFederation/{tenantName}
# operationId: updateGroupDataFederation
export def "atlas-groups-data-federation updateGroupDataFederation" [
  groupId: string
  tenantName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --skipRoleValidation: oneof<nothing, bool> # Flag that indicates whether this request should check if the requesting IAM role can read from the S3 bucket. AWS checks if the role can list the objects in the bucket before writing to it. Some IAM roles only need write permissions. This flag allows you to skip that check.
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "skipRoleValidation" $skipRoleValidation "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/dataFederation/($tenantName)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Query Limits for One Federated Database Instance
#
# GET /api/atlas/v2/groups/{groupId}/dataFederation/{tenantName}/limits
# operationId: listGroupDataFederationLimits
export def "atlas-groups-data-federation-limits listGroupDataFederationLimits" [
  groupId: string
  tenantName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/dataFederation/($tenantName)/limits" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete One Query Limit for One Federated Database Instance
#
# DELETE /api/atlas/v2/groups/{groupId}/dataFederation/{tenantName}/limits/{limitName}
# operationId: deleteGroupDataFederationLimit
export def "atlas-groups-data-federation-limits delete" [
  groupId: string
  tenantName: string
  limitName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/dataFederation/($tenantName)/limits/($limitName)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Federated Database Instance Query Limit for One Project
#
# GET /api/atlas/v2/groups/{groupId}/dataFederation/{tenantName}/limits/{limitName}
# operationId: getGroupDataFederationLimit
export def "atlas-groups-data-federation-limits get" [
  groupId: string
  tenantName: string
  limitName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/dataFederation/($tenantName)/limits/($limitName)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Configure One Query Limit for One Federated Database Instance
#
# PATCH /api/atlas/v2/groups/{groupId}/dataFederation/{tenantName}/limits/{limitName}
# operationId: setGroupDataFederationLimit
export def "atlas-groups-data-federation-limits setGroupDataFederationLimit" [
  groupId: string
  tenantName: string
  limitName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/dataFederation/($tenantName)/limits/($limitName)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Download Query Logs for One Federated Database Instance
#
# GET /api/atlas/v2/groups/{groupId}/dataFederation/{tenantName}/queryLogs.gz
# operationId: downloadGroupDataFederationQueryLogs
export def "atlas-groups-data-federation-query-logsgz downloadGroupDataFederationQueryLogs" [
  groupId: string
  tenantName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --endDate: int # Timestamp that specifies the end point for the range of log messages to download.  MongoDB Cloud expresses this timestamp in the number of seconds that have elapsed since the UNIX epoch. (format: int64, e.g. 1636481348)
  --startDate: int # Timestamp that specifies the starting point for the range of log messages to download. MongoDB Cloud expresses this timestamp in the number of seconds that have elapsed since the UNIX epoch. (format: int64, e.g. 1636466948)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "endDate" $endDate "scalar") (serialize-qp "startDate" $startDate "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/dataFederation/($tenantName)/queryLogs.gz" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+gzip"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Database Users in One Project
#
# GET /api/atlas/v2/groups/{groupId}/databaseUsers
# operationId: listGroupDatabaseUsers
export def "atlas-groups-database-users listGroupDatabaseUsers" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/databaseUsers" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Database User in One Project
#
# POST /api/atlas/v2/groups/{groupId}/databaseUsers
# Docs: https://cloud.mongodb.com/support — Contact MongoDB Support
# operationId: createGroupDatabaseUser
export def "atlas-groups-database-users createGroupDatabaseUser" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/databaseUsers" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Remove One Database User from One Project
#
# DELETE /api/atlas/v2/groups/{groupId}/databaseUsers/{databaseName}/{username}
# operationId: deleteGroupDatabaseUser
export def "atlas-groups-database-users delete" [
  groupId: string
  databaseName: string
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/databaseUsers/($databaseName)/($username)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Database User from One Project
#
# GET /api/atlas/v2/groups/{groupId}/databaseUsers/{databaseName}/{username}
# operationId: getGroupDatabaseUser
export def "atlas-groups-database-users get" [
  groupId: string
  databaseName: string
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/databaseUsers/($databaseName)/($username)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Database User in One Project
#
# PATCH /api/atlas/v2/groups/{groupId}/databaseUsers/{databaseName}/{username}
# operationId: updateGroupDatabaseUser
export def "atlas-groups-database-users updateGroupDatabaseUser" [
  groupId: string
  databaseName: string
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/databaseUsers/($databaseName)/($username)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All X.509 Certificates Assigned to One Database User
#
# GET /api/atlas/v2/groups/{groupId}/databaseUsers/{username}/certs
# operationId: listGroupDatabaseUserCerts
export def "atlas-groups-database-users-certs listGroupDatabaseUserCerts" [
  groupId: string
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/databaseUsers/($username)/certs" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One X.509 Certificate for One Database User
#
# POST /api/atlas/v2/groups/{groupId}/databaseUsers/{username}/certs
# Docs: https://www.mongodb.com/docs/atlas/security-self-managed-x509/#std-label-self-managed-x509 — Self-Managed X.509
# operationId: createGroupDatabaseUserCert
export def "atlas-groups-database-users-certs createGroupDatabaseUserCert" [
  groupId: string
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/databaseUsers/($username)/certs" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return Database Access History for One Cluster by Cluster Name
#
# GET /api/atlas/v2/groups/{groupId}/dbAccessHistory/clusters/{clusterName}
# Docs: https://docs.atlas.mongodb.com/access-tracking/ — Database Access History
# operationId: getGroupDbAccessHistoryCluster
export def "atlas-groups-db-access-history-clusters get" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --authResult: oneof<nothing, bool> # Flag that indicates whether the response returns the successful authentication attempts only.
  --end: int # Date and time when to stop retrieving database history. If you specify **end**, you must also specify **start**. This parameter uses UNIX epoch time in milliseconds. (format: int64)
  --ipAddress: string # One Internet Protocol address that attempted to authenticate with the database.
  --nLogs: int # Maximum number of lines from the log to return. (format: int32, default: 20000)
  --start: int # Date and time when MongoDB Cloud begins retrieving database history. If you specify **start**, you must also specify **end**. This parameter uses UNIX epoch time in milliseconds. (format: int64)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "authResult" $authResult "scalar") (serialize-qp "end" $end "scalar") (serialize-qp "ipAddress" $ipAddress "scalar") (serialize-qp "nLogs" $nLogs "scalar") (serialize-qp "start" $start "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/dbAccessHistory/clusters/($clusterName)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Database Access History for One Cluster by Hostname
#
# GET /api/atlas/v2/groups/{groupId}/dbAccessHistory/processes/{hostname}
# Docs: https://docs.atlas.mongodb.com/access-tracking/ — Database Access History
# operationId: getGroupDbAccessHistoryProcess
export def "atlas-groups-db-access-history-processes get" [
  groupId: string
  hostname: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --authResult: oneof<nothing, bool> # Flag that indicates whether the response returns the successful authentication attempts only.
  --end: int # Date and time when to stop retrieving database history. If you specify **end**, you must also specify **start**. This parameter uses UNIX epoch time in milliseconds. (format: int64)
  --ipAddress: string # One Internet Protocol address that attempted to authenticate with the database.
  --nLogs: int # Maximum number of lines from the log to return. (format: int32, default: 20000)
  --start: int # Date and time when MongoDB Cloud begins retrieving database history. If you specify **start**, you must also specify **end**. This parameter uses UNIX epoch time in milliseconds. (format: int64)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "authResult" $authResult "scalar") (serialize-qp "end" $end "scalar") (serialize-qp "ipAddress" $ipAddress "scalar") (serialize-qp "nLogs" $nLogs "scalar") (serialize-qp "start" $start "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/dbAccessHistory/processes/($hostname)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Configuration for Encryption at Rest Using Customer-Managed Keys for One Project
#
# GET /api/atlas/v2/groups/{groupId}/encryptionAtRest
# operationId: getGroupEncryptionAtRest
export def "atlas-groups-encryption-at-rest get" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/encryptionAtRest" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update Encryption at Rest Configuration in One Project
#
# PATCH /api/atlas/v2/groups/{groupId}/encryptionAtRest
# operationId: updateGroupEncryptionAtRest
export def "atlas-groups-encryption-at-rest updateGroupEncryptionAtRest" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/encryptionAtRest" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return Private Endpoints for Encryption at Rest Using Customer Key Management for One Cloud Provider in One Project
#
# GET /api/atlas/v2/groups/{groupId}/encryptionAtRest/{cloudProvider}/privateEndpoints
# operationId: listGroupEncryptionAtRestPrivateEndpoints
export def "atlas-groups-encryption-at-rest-private-endpoints listGroupEncryptionAtRestPrivateEndpoints" [
  groupId: string
  cloudProvider: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/encryptionAtRest/($cloudProvider)/privateEndpoints" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Private Endpoint for Encryption at Rest Using Customer Key Management for One Cloud Provider in One Project
#
# POST /api/atlas/v2/groups/{groupId}/encryptionAtRest/{cloudProvider}/privateEndpoints
# operationId: createGroupEncryptionAtRestPrivateEndpoint
export def "atlas-groups-encryption-at-rest-private-endpoints createGroupEncryptionAtRestPrivateEndpoint" [
  groupId: string
  cloudProvider: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/encryptionAtRest/($cloudProvider)/privateEndpoints" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Delete One Private Endpoint for Encryption at Rest Using Customer Key Management for One Cloud Provider from One Project
#
# DELETE /api/atlas/v2/groups/{groupId}/encryptionAtRest/{cloudProvider}/privateEndpoints/{endpointId}
# operationId: requestGroupEncryptionAtRestPrivateEndpointDeletion
export def "atlas-groups-encryption-at-rest-private-endpoints requestGroupEncryptionAtRestPrivateEndpointDeletion" [
  groupId: string
  cloudProvider: string
  endpointId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/encryptionAtRest/($cloudProvider)/privateEndpoints/($endpointId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Private Endpoint for Encryption at Rest Using Customer Key Management for One Cloud Provider in One Project
#
# GET /api/atlas/v2/groups/{groupId}/encryptionAtRest/{cloudProvider}/privateEndpoints/{endpointId}
# operationId: getGroupEncryptionAtRestPrivateEndpoint
export def "atlas-groups-encryption-at-rest-private-endpoints get" [
  groupId: string
  cloudProvider: string
  endpointId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/encryptionAtRest/($cloudProvider)/privateEndpoints/($endpointId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Events from One Project
#
# GET /api/atlas/v2/groups/{groupId}/events
# operationId: listGroupEvents
export def "atlas-groups-events listGroupEvents" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --clusterNames: list # Human-readable label that identifies the cluster.
  --eventType: list # Category of incident recorded at this moment in time.  **IMPORTANT**: The complete list of event type values changes frequently.
  --excludedEventType: list # Category of event that you would like to exclude from query results, such as `CLUSTER_CREATED`.  **IMPORTANT**: Event type names change frequently. Verify that you specify the event type correctly by checking the complete list of event types.
  --includeRaw: oneof<nothing, bool> # Flag that indicates whether to include the raw document in the output. The raw document contains additional meta information about the event. (default: false)
  --maxDate: string # Date and time from when MongoDB Cloud stops returning events. This parameter uses the ISO 8601 timestamp format in UTC. (format: date-time)
  --minDate: string # Date and time from when MongoDB Cloud starts returning events. This parameter uses the ISO 8601 timestamp format in UTC. (format: date-time)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "clusterNames" $clusterNames "multi") (serialize-qp "eventType" $eventType "multi") (serialize-qp "excludedEventType" $excludedEventType "multi") (serialize-qp "includeRaw" $includeRaw "scalar") (serialize-qp "maxDate" $maxDate "scalar") (serialize-qp "minDate" $minDate "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/events" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Event from One Project
#
# GET /api/atlas/v2/groups/{groupId}/events/{eventId}
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Events/operation/listProjectEvents — Return Events from One Project
# operationId: getGroupEvent
export def "atlas-groups-events get" [
  groupId: string
  eventId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --includeRaw: oneof<nothing, bool> # Flag that indicates whether to include the raw document in the output. The raw document contains additional meta information about the event. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "includeRaw" $includeRaw "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/events/($eventId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Flex Clusters from One Project
#
# GET /api/atlas/v2/groups/{groupId}/flexClusters
# operationId: listGroupFlexClusters
export def "atlas-groups-flex-clusters listGroupFlexClusters" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/flexClusters" $qp)
  let accept_val = "application/vnd.atlas.2024-11-13+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Flex Cluster in One Project
#
# POST /api/atlas/v2/groups/{groupId}/flexClusters
# operationId: createGroupFlexCluster
export def "atlas-groups-flex-clusters createGroupFlexCluster" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/flexClusters" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-11-13+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-11-13+json" $body
}

# Remove One Flex Cluster from One Project
#
# DELETE /api/atlas/v2/groups/{groupId}/flexClusters/{name}
# operationId: deleteGroupFlexCluster
export def "atlas-groups-flex-clusters delete" [
  groupId: string
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/flexClusters/($name)" $qp)
  let accept_val = "application/vnd.atlas.2024-11-13+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Flex Cluster from One Project
#
# GET /api/atlas/v2/groups/{groupId}/flexClusters/{name}
# operationId: getGroupFlexCluster
export def "atlas-groups-flex-clusters get" [
  groupId: string
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/flexClusters/($name)" $qp)
  let accept_val = "application/vnd.atlas.2024-11-13+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Flex Cluster in One Project
#
# PATCH /api/atlas/v2/groups/{groupId}/flexClusters/{name}
# operationId: updateGroupFlexCluster
export def "atlas-groups-flex-clusters updateGroupFlexCluster" [
  groupId: string
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/flexClusters/($name)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-11-13+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-11-13+json" $body
}

# Download One Flex Cluster Snapshot
#
# POST /api/atlas/v2/groups/{groupId}/flexClusters/{name}/backup/download
# operationId: downloadGroupFlexClusterBackup
export def "atlas-groups-flex-clusters-backup-download downloadGroupFlexClusterBackup" [
  name: string
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/flexClusters/($name)/backup/download" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-11-13+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-11-13+json" $body
}

# Return All Restore Jobs for One Flex Cluster
#
# GET /api/atlas/v2/groups/{groupId}/flexClusters/{name}/backup/restoreJobs
# operationId: listGroupFlexClusterBackupRestoreJobs
export def "atlas-groups-flex-clusters-backup-restore-jobs listGroupFlexClusterBackupRestoreJobs" [
  groupId: string
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/flexClusters/($name)/backup/restoreJobs" $qp)
  let accept_val = "application/vnd.atlas.2024-11-13+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Restore Job for One Flex Cluster
#
# POST /api/atlas/v2/groups/{groupId}/flexClusters/{name}/backup/restoreJobs
# operationId: createGroupFlexClusterBackupRestoreJob
export def "atlas-groups-flex-clusters-backup-restore-jobs createGroupFlexClusterBackupRestoreJob" [
  groupId: string
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/flexClusters/($name)/backup/restoreJobs" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-11-13+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-11-13+json" $body
}

# Return One Restore Job for One Flex Cluster
#
# GET /api/atlas/v2/groups/{groupId}/flexClusters/{name}/backup/restoreJobs/{restoreJobId}
# operationId: getGroupFlexClusterBackupRestoreJob
export def "atlas-groups-flex-clusters-backup-restore-jobs get" [
  groupId: string
  name: string
  restoreJobId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/flexClusters/($name)/backup/restoreJobs/($restoreJobId)" $qp)
  let accept_val = "application/vnd.atlas.2024-11-13+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Snapshots for One Flex Cluster
#
# GET /api/atlas/v2/groups/{groupId}/flexClusters/{name}/backup/snapshots
# operationId: listGroupFlexClusterBackupSnapshots
export def "atlas-groups-flex-clusters-backup-snapshots listGroupFlexClusterBackupSnapshots" [
  groupId: string
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/flexClusters/($name)/backup/snapshots" $qp)
  let accept_val = "application/vnd.atlas.2024-11-13+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Snapshot for One Flex Cluster
#
# GET /api/atlas/v2/groups/{groupId}/flexClusters/{name}/backup/snapshots/{snapshotId}
# operationId: getGroupFlexClusterBackupSnapshot
export def "atlas-groups-flex-clusters-backup-snapshots get" [
  groupId: string
  name: string
  snapshotId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/flexClusters/($name)/backup/snapshots/($snapshotId)" $qp)
  let accept_val = "application/vnd.atlas.2024-11-13+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Upgrade One Flex Cluster
#
# POST /api/atlas/v2/groups/{groupId}/flexClusters:tenantUpgrade
# operationId: tenantGroupFlexClusterUpgrade
export def "atlas-groups-flex-clusters-tenant-upgrade tenantGroupFlexClusterUpgrade" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/flexClusters:tenantUpgrade" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-11-13+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-11-13+json" $body
}

# Return All Atlas Search Metric Types for One Process
#
# GET /api/atlas/v2/groups/{groupId}/hosts/{processId}/fts/metrics
# operationId: listGroupHostFtsMetrics
export def "atlas-groups-hosts-fts-metrics listGroupHostFtsMetrics" [
  processId: string
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/hosts/($processId)/fts/metrics" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Atlas Search Index Metrics for One Namespace
#
# GET /api/atlas/v2/groups/{groupId}/hosts/{processId}/fts/metrics/indexes/{databaseName}/{collectionName}/measurements
# operationId: listGroupHostFtsMetricIndexMeasurements
export def "atlas-groups-hosts-fts-metrics-indexes-measurements listGroupHostFtsMetricIndexMeasurements" [
  processId: string
  databaseName: string
  collectionName: string
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --granularity: string # Duration that specifies the interval at which Atlas reports the metrics. This parameter expresses its value in the ISO 8601 duration format in UTC. (e.g. PT1M)
  --period: string # Duration over which Atlas reports the metrics. This parameter expresses its value in the ISO 8601 duration format in UTC. Include this parameter when you do not set **start** and **end**. (e.g. PT10H)
  --start: string # Date and time when MongoDB Cloud begins reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**. (format: date-time)
  --end: string # Date and time when MongoDB Cloud stops reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**. (format: date-time)
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --metrics: list # List that contains the measurements that MongoDB Atlas reports for the associated data series.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "granularity" $granularity "scalar") (serialize-qp "period" $period "scalar") (serialize-qp "start" $start "scalar") (serialize-qp "end" $end "scalar") (serialize-qp "envelope" $envelope "scalar") (serialize-qp "metrics" $metrics "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/hosts/($processId)/fts/metrics/indexes/($databaseName)/($collectionName)/measurements" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Atlas Search Metrics for One Index in One Namespace
#
# GET /api/atlas/v2/groups/{groupId}/hosts/{processId}/fts/metrics/indexes/{databaseName}/{collectionName}/{indexName}/measurements
# operationId: getGroupHostFtsMetricIndexMeasurements
export def "atlas-groups-hosts-fts-metrics-indexes-measurements get" [
  processId: string
  indexName: string
  databaseName: string
  collectionName: string
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --granularity: string # Duration that specifies the interval at which Atlas reports the metrics. This parameter expresses its value in the ISO 8601 duration format in UTC. (e.g. PT1M)
  --period: string # Duration over which Atlas reports the metrics. This parameter expresses its value in the ISO 8601 duration format in UTC. Include this parameter when you do not set **start** and **end**. (e.g. PT10H)
  --start: string # Date and time when MongoDB Cloud begins reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**. (format: date-time)
  --end: string # Date and time when MongoDB Cloud stops reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**. (format: date-time)
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --metrics: list # List that contains the measurements that MongoDB Atlas reports for the associated data series.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "granularity" $granularity "scalar") (serialize-qp "period" $period "scalar") (serialize-qp "start" $start "scalar") (serialize-qp "end" $end "scalar") (serialize-qp "envelope" $envelope "scalar") (serialize-qp "metrics" $metrics "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/hosts/($processId)/fts/metrics/indexes/($databaseName)/($collectionName)/($indexName)/measurements" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Atlas Search Hardware and Status Metrics
#
# GET /api/atlas/v2/groups/{groupId}/hosts/{processId}/fts/metrics/measurements
# operationId: listGroupHostFtsMetricMeasurements
export def "atlas-groups-hosts-fts-metrics-measurements listGroupHostFtsMetricMeasurements" [
  processId: string
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --granularity: string # Duration that specifies the interval at which Atlas reports the metrics. This parameter expresses its value in the ISO 8601 duration format in UTC. (e.g. PT1M)
  --period: string # Duration over which Atlas reports the metrics. This parameter expresses its value in the ISO 8601 duration format in UTC. Include this parameter when you do not set **start** and **end**. (e.g. PT10H)
  --start: string # Date and time when MongoDB Cloud begins reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**. (format: date-time)
  --end: string # Date and time when MongoDB Cloud stops reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**. (format: date-time)
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --metrics: list # List that contains the metrics that you want MongoDB Atlas to report for the associated data series. If you don't set this parameter, this resource returns all hardware and status metrics for the associated data series.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "granularity" $granularity "scalar") (serialize-qp "period" $period "scalar") (serialize-qp "start" $start "scalar") (serialize-qp "end" $end "scalar") (serialize-qp "envelope" $envelope "scalar") (serialize-qp "metrics" $metrics "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/hosts/($processId)/fts/metrics/measurements" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Active Third-Party Service Integrations
#
# GET /api/atlas/v2/groups/{groupId}/integrations
# operationId: listGroupIntegrations
export def "atlas-groups-integrations listGroupIntegrations" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/integrations" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove One Third-Party Service Integration
#
# DELETE /api/atlas/v2/groups/{groupId}/integrations/{integrationType}
# operationId: deleteGroupIntegration
export def "atlas-groups-integrations delete" [
  integrationType: string
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/integrations/($integrationType)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Third-Party Service Integration
#
# GET /api/atlas/v2/groups/{groupId}/integrations/{integrationType}
# operationId: getGroupIntegration
export def "atlas-groups-integrations get" [
  groupId: string
  integrationType: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/integrations/($integrationType)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Third-Party Service Integration
#
# POST /api/atlas/v2/groups/{groupId}/integrations/{integrationType}
# operationId: createGroupIntegration
export def "atlas-groups-integrations createGroupIntegration" [
  integrationType: string
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/integrations/($integrationType)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Update One Third-Party Service Integration
#
# PUT /api/atlas/v2/groups/{groupId}/integrations/{integrationType}
# operationId: updateGroupIntegration
export def "atlas-groups-integrations updateGroupIntegration" [
  integrationType: string
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/integrations/($integrationType)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Invitations in One Project
#
# GET /api/atlas/v2/groups/{groupId}/invites
# DEPRECATED
# operationId: listGroupInvites
@deprecated
export def "atlas-groups-invites listGroupInvites" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --username: string # Email address of the user account invited to this project. (format: email)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "username" $username "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/invites" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Invitation in One Project
#
# PATCH /api/atlas/v2/groups/{groupId}/invites
# DEPRECATED
# operationId: updateGroupInvites
@deprecated
export def "atlas-groups-invites updateGroupInvites" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/invites" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Create Invitation for One MongoDB Cloud User in One Project
#
# POST /api/atlas/v2/groups/{groupId}/invites
# DEPRECATED
# operationId: createGroupInvite
@deprecated
export def "atlas-groups-invites createGroupInvite" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/invites" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Remove One Invitation from One Project
#
# DELETE /api/atlas/v2/groups/{groupId}/invites/{invitationId}
# DEPRECATED
# operationId: deleteGroupInvite
@deprecated
export def "atlas-groups-invites delete" [
  groupId: string
  invitationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/invites/($invitationId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Invitation in One Project by Invitation ID
#
# GET /api/atlas/v2/groups/{groupId}/invites/{invitationId}
# DEPRECATED
# operationId: getGroupInvite
@deprecated
export def "atlas-groups-invites get" [
  groupId: string
  invitationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/invites/($invitationId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Invitation in One Project by Invitation ID
#
# PATCH /api/atlas/v2/groups/{groupId}/invites/{invitationId}
# DEPRECATED
# operationId: updateGroupInviteById
@deprecated
export def "atlas-groups-invites updateGroupInviteById" [
  groupId: string
  invitationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/invites/($invitationId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All IP Addresses for One Project
#
# GET /api/atlas/v2/groups/{groupId}/ipAddresses
# operationId: getGroupIpAddresses
export def "atlas-groups-ip-addresses get" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/ipAddresses" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Limits for One Project
#
# GET /api/atlas/v2/groups/{groupId}/limits
# operationId: listGroupLimits
export def "atlas-groups-limits listGroupLimits" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/limits" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove One Project Limit
#
# DELETE /api/atlas/v2/groups/{groupId}/limits/{limitName}
# operationId: deleteGroupLimit
export def "atlas-groups-limits delete" [
  limitName: string
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/limits/($limitName)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Limit for One Project
#
# GET /api/atlas/v2/groups/{groupId}/limits/{limitName}
# operationId: getGroupLimit
export def "atlas-groups-limits get" [
  limitName: string
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/limits/($limitName)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Set One Project Limit
#
# PATCH /api/atlas/v2/groups/{groupId}/limits/{limitName}
# operationId: setGroupLimit
export def "atlas-groups-limits setGroupLimit" [
  limitName: string
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/limits/($limitName)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Create One Migration for One Local Managed Cluster to MongoDB Atlas
#
# POST /api/atlas/v2/groups/{groupId}/liveMigrations
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Cloud-Migration-Service/operation/validateMigration — Validate Migration
# operationId: createGroupLiveMigration
export def "atlas-groups-live-migrations createGroupLiveMigration" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-1 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/liveMigrations" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Validate One Migration Request
#
# POST /api/atlas/v2/groups/{groupId}/liveMigrations/validate
# operationId: validateGroupLiveMigrations
export def "atlas-groups-live-migrations-validate validateGroupLiveMigrations" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-1 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/liveMigrations/validate" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return One Migration Validation Job
#
# GET /api/atlas/v2/groups/{groupId}/liveMigrations/validate/{validationId}
# operationId: getGroupLiveMigrationValidateStatus
export def "atlas-groups-live-migrations-validate get" [
  groupId: string
  validationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/liveMigrations/validate/($validationId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Migration Job
#
# GET /api/atlas/v2/groups/{groupId}/liveMigrations/{liveMigrationId}
# operationId: getGroupLiveMigration
export def "atlas-groups-live-migrations get" [
  groupId: string
  liveMigrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/liveMigrations/($liveMigrationId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Cut Over One Migrated Cluster
#
# PUT /api/atlas/v2/groups/{groupId}/liveMigrations/{liveMigrationId}/cutover
# operationId: cutoverGroupLiveMigration
export def "atlas-groups-live-migrations-cutover cutoverGroupLiveMigration" [
  groupId: string
  liveMigrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/liveMigrations/($liveMigrationId)/cutover" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Active Log Integrations
#
# GET /api/atlas/v2/groups/{groupId}/logIntegrations
# operationId: listGroupLogIntegrations
export def "atlas-groups-log-integrations listGroupLogIntegrations" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --integrationType: string # Optional filter by integration type (e.g., `S3_LOG_EXPORT`).
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "integrationType" $integrationType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/logIntegrations" $qp)
  let accept_val = "application/vnd.atlas.2025-03-12+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Log Integration
#
# POST /api/atlas/v2/groups/{groupId}/logIntegrations
# operationId: createGroupLogIntegration
export def "atlas-groups-log-integrations createGroupLogIntegration" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/logIntegrations" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2025-03-12+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2025-03-12+json" $body
}

# Remove One Log Integration
#
# DELETE /api/atlas/v2/groups/{groupId}/logIntegrations/{id}
# operationId: deleteGroupLogIntegration
export def "atlas-groups-log-integrations delete" [
  groupId: string
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/logIntegrations/($id)" $qp)
  let accept_val = "application/vnd.atlas.2025-03-12+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Log Integration
#
# GET /api/atlas/v2/groups/{groupId}/logIntegrations/{id}
# operationId: getGroupLogIntegration
export def "atlas-groups-log-integrations get" [
  groupId: string
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/logIntegrations/($id)" $qp)
  let accept_val = "application/vnd.atlas.2025-03-12+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Log Integration
#
# PUT /api/atlas/v2/groups/{groupId}/logIntegrations/{id}
# operationId: updateGroupLogIntegration
export def "atlas-groups-log-integrations updateGroupLogIntegration" [
  groupId: string
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/logIntegrations/($id)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2025-03-12+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2025-03-12+json" $body
}

# Reset One Maintenance Window for One Project
#
# DELETE /api/atlas/v2/groups/{groupId}/maintenanceWindow
# operationId: resetGroupMaintenanceWindow
export def "atlas-groups-maintenance-window resetGroupMaintenanceWindow" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/maintenanceWindow" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Maintenance Window for One Project
#
# GET /api/atlas/v2/groups/{groupId}/maintenanceWindow
# operationId: getGroupMaintenanceWindow
export def "atlas-groups-maintenance-window get" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/maintenanceWindow" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Maintenance Window for One Project
#
# PATCH /api/atlas/v2/groups/{groupId}/maintenanceWindow
# operationId: updateGroupMaintenanceWindow
export def "atlas-groups-maintenance-window updateGroupMaintenanceWindow" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/maintenanceWindow" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Toggle Automatic Deferral of Maintenance for One Project
#
# POST /api/atlas/v2/groups/{groupId}/maintenanceWindow/autoDefer
# operationId: toggleGroupMaintenanceWindowAutoDefer
export def "atlas-groups-maintenance-window-auto-defer toggleGroupMaintenanceWindowAutoDefer" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/maintenanceWindow/autoDefer" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Defer One Maintenance Window for One Project
#
# POST /api/atlas/v2/groups/{groupId}/maintenanceWindow/defer
# operationId: deferGroupMaintenanceWindow
export def "atlas-groups-maintenance-window-defer deferGroupMaintenanceWindow" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/maintenanceWindow/defer" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Managed Slow Operation Threshold Status
#
# GET /api/atlas/v2/groups/{groupId}/managedSlowMs
# operationId: getGroupManagedSlowMs
export def "atlas-groups-managed-slow-ms get" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/managedSlowMs" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Disable Managed Slow Operation Threshold
#
# DELETE /api/atlas/v2/groups/{groupId}/managedSlowMs/disable
# operationId: disableGroupManagedSlowMs
export def "atlas-groups-managed-slow-ms-disable disableGroupManagedSlowMs" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/managedSlowMs/disable" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Enable Managed Slow Operation Threshold
#
# POST /api/atlas/v2/groups/{groupId}/managedSlowMs/enable
# operationId: enableGroupManagedSlowMs
export def "atlas-groups-managed-slow-ms-enable enableGroupManagedSlowMs" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/managedSlowMs/enable" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Active Metric Integrations
#
# GET /api/atlas/v2/groups/{groupId}/metricIntegrations
# operationId: listGroupMetricIntegrations
export def "atlas-groups-metric-integrations listGroupMetricIntegrations" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --integrationType: string # Optional filter by integration type (e.g., `OTEL`). When specified, `providerType` must also be specified.
  --providerType: string # Optional filter by provider type (e.g., `CUSTOM`). When specified, `integrationType` must also be specified.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "integrationType" $integrationType "scalar") (serialize-qp "providerType" $providerType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/metricIntegrations" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Metric Integration
#
# POST /api/atlas/v2/groups/{groupId}/metricIntegrations
# operationId: createGroupMetricIntegration
export def "atlas-groups-metric-integrations createGroupMetricIntegration" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/metricIntegrations" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.preview+json" $body
}

# Remove One Metric Integration
#
# DELETE /api/atlas/v2/groups/{groupId}/metricIntegrations/{metricIntegrationId}
# operationId: deleteGroupMetricIntegration
export def "atlas-groups-metric-integrations delete" [
  groupId: string
  metricIntegrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/metricIntegrations/($metricIntegrationId)" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Metric Integration
#
# GET /api/atlas/v2/groups/{groupId}/metricIntegrations/{metricIntegrationId}
# operationId: getGroupMetricIntegration
export def "atlas-groups-metric-integrations get" [
  groupId: string
  metricIntegrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/metricIntegrations/($metricIntegrationId)" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Metric Integration
#
# PUT /api/atlas/v2/groups/{groupId}/metricIntegrations/{metricIntegrationId}
# operationId: updateGroupMetricIntegration
export def "atlas-groups-metric-integrations updateGroupMetricIntegration" [
  groupId: string
  metricIntegrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/metricIntegrations/($metricIntegrationId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.preview+json" $body
}

# Return All Available MongoDB LTS Versions for Clusters in One Project
#
# GET /api/atlas/v2/groups/{groupId}/mongoDBVersions
# operationId: getGroupMongoDbVersions
export def "atlas-groups-mongo-db-versions get" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --cloudProvider: string@cloudProvider-completer # Filter results to only one cloud provider.
  --instanceSize: string # Filter results to only one instance size. (e.g. M10)
  --defaultStatus: string@defaultStatus-completer # Filter results to only the default values per tier. This value must be DEFAULT.
  --itemsPerPage: int # Number of items that the response returns per page. (format: int64, default: 100, e.g. 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "cloudProvider" $cloudProvider "scalar") (serialize-qp "instanceSize" $instanceSize "scalar") (serialize-qp "defaultStatus" $defaultStatus "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/mongoDBVersions" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Network Peering Connections in One Project
#
# GET /api/atlas/v2/groups/{groupId}/peers
# operationId: listGroupPeers
export def "atlas-groups-peers listGroupPeers" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --providerName: string@providerName-completer # Cloud service provider to use for this VPC peering connection. (default: AWS)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "providerName" $providerName "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/peers" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Network Peering Connection
#
# POST /api/atlas/v2/groups/{groupId}/peers
# Docs: https://docs.atlas.mongodb.com/reference/api/vpc-create-peering-connection/#prerequisites — Azure Network Peering Prerequisites
# operationId: createGroupPeer
export def "atlas-groups-peers createGroupPeer" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/peers" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Remove One Network Peering Connection
#
# DELETE /api/atlas/v2/groups/{groupId}/peers/{peerId}
# operationId: deleteGroupPeer
export def "atlas-groups-peers delete" [
  groupId: string
  peerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/peers/($peerId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Network Peering Connection in One Project
#
# GET /api/atlas/v2/groups/{groupId}/peers/{peerId}
# operationId: getGroupPeer
export def "atlas-groups-peers get" [
  groupId: string
  peerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/peers/($peerId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Network Peering Connection
#
# PATCH /api/atlas/v2/groups/{groupId}/peers/{peerId}
# operationId: updateGroupPeer
export def "atlas-groups-peers updateGroupPeer" [
  groupId: string
  peerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/peers/($peerId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Data Lake Pipelines in One Project
#
# GET /api/atlas/v2/groups/{groupId}/pipelines
# DEPRECATED
# operationId: listGroupPipelines
@deprecated
export def "atlas-groups-pipelines listGroupPipelines" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/pipelines" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Data Lake Pipeline
#
# POST /api/atlas/v2/groups/{groupId}/pipelines
# DEPRECATED
# Docs: https://dochub.mongodb.org/core/data-lake-deprecation — Data Lake Pipelines are deprecated. Please see Data Lake Deprecation Guide.
# operationId: createGroupPipeline
@deprecated
export def "atlas-groups-pipelines createGroupPipeline" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/pipelines" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Remove One Data Lake Pipeline
#
# DELETE /api/atlas/v2/groups/{groupId}/pipelines/{pipelineName}
# DEPRECATED
# operationId: deleteGroupPipeline
@deprecated
export def "atlas-groups-pipelines delete" [
  groupId: string
  pipelineName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/pipelines/($pipelineName)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Data Lake Pipeline
#
# GET /api/atlas/v2/groups/{groupId}/pipelines/{pipelineName}
# DEPRECATED
# operationId: getGroupPipeline
@deprecated
export def "atlas-groups-pipelines get" [
  groupId: string
  pipelineName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/pipelines/($pipelineName)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Data Lake Pipeline
#
# PATCH /api/atlas/v2/groups/{groupId}/pipelines/{pipelineName}
# DEPRECATED
# operationId: updateGroupPipeline
@deprecated
export def "atlas-groups-pipelines updateGroupPipeline" [
  groupId: string
  pipelineName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/pipelines/($pipelineName)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Ingestion Schedules for One Data Lake Pipeline
#
# GET /api/atlas/v2/groups/{groupId}/pipelines/{pipelineName}/availableSchedules
# DEPRECATED
# operationId: getGroupPipelineAvailableSchedules
@deprecated
export def "atlas-groups-pipelines-available-schedules get" [
  groupId: string
  pipelineName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/pipelines/($pipelineName)/availableSchedules" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Backup Snapshots for One Data Lake Pipeline
#
# GET /api/atlas/v2/groups/{groupId}/pipelines/{pipelineName}/availableSnapshots
# DEPRECATED
# operationId: getGroupPipelineAvailableSnapshots
@deprecated
export def "atlas-groups-pipelines-available-snapshots get" [
  groupId: string
  pipelineName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --completedAfter: string # Date and time after which MongoDB Cloud created the snapshot. If specified, MongoDB Cloud returns available backup snapshots created after this time and date only. This parameter expresses its value in the ISO 8601 timestamp format in UTC. (format: date-time)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "completedAfter" $completedAfter "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/pipelines/($pipelineName)/availableSnapshots" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Pause One Data Lake Pipeline
#
# POST /api/atlas/v2/groups/{groupId}/pipelines/{pipelineName}/pause
# DEPRECATED
# operationId: pauseGroupPipeline
@deprecated
export def "atlas-groups-pipelines-pause pauseGroupPipeline" [
  groupId: string
  pipelineName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/pipelines/($pipelineName)/pause" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Resume One Data Lake Pipeline
#
# POST /api/atlas/v2/groups/{groupId}/pipelines/{pipelineName}/resume
# DEPRECATED
# operationId: resumeGroupPipeline
@deprecated
export def "atlas-groups-pipelines-resume resumeGroupPipeline" [
  groupId: string
  pipelineName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/pipelines/($pipelineName)/resume" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Data Lake Pipeline Runs in One Project
#
# GET /api/atlas/v2/groups/{groupId}/pipelines/{pipelineName}/runs
# DEPRECATED
# operationId: listGroupPipelineRuns
@deprecated
export def "atlas-groups-pipelines-runs listGroupPipelineRuns" [
  groupId: string
  pipelineName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --createdBefore: string # If specified, Atlas returns only Data Lake Pipeline runs initiated before this time and date. (format: date-time)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "createdBefore" $createdBefore "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/pipelines/($pipelineName)/runs" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete One Pipeline Run Dataset
#
# DELETE /api/atlas/v2/groups/{groupId}/pipelines/{pipelineName}/runs/{pipelineRunId}
# DEPRECATED
# operationId: deleteGroupPipelineRun
@deprecated
export def "atlas-groups-pipelines-runs delete" [
  groupId: string
  pipelineName: string
  pipelineRunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/pipelines/($pipelineName)/runs/($pipelineRunId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Data Lake Pipeline Run
#
# GET /api/atlas/v2/groups/{groupId}/pipelines/{pipelineName}/runs/{pipelineRunId}
# DEPRECATED
# operationId: getGroupPipelineRun
@deprecated
export def "atlas-groups-pipelines-runs get" [
  groupId: string
  pipelineName: string
  pipelineRunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/pipelines/($pipelineName)/runs/($pipelineRunId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Trigger On-Demand Snapshot Ingestion
#
# POST /api/atlas/v2/groups/{groupId}/pipelines/{pipelineName}/trigger
# DEPRECATED
# operationId: triggerGroupPipeline
@deprecated
export def "atlas-groups-pipelines-trigger triggerGroupPipeline" [
  groupId: string
  pipelineName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/pipelines/($pipelineName)/trigger" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Create One Private Endpoint Service for One Provider
#
# POST /api/atlas/v2/groups/{groupId}/privateEndpoint/endpointService
# Docs: https://dochub.mongodb.org/core/private-endpoint-concepts — Private Endpoint Concepts
# operationId: createGroupPrivateEndpointEndpointService
export def "atlas-groups-private-endpoint-endpoint-service createGroupPrivateEndpointEndpointService" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/privateEndpoint/endpointService" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Update One Private Endpoint Service for One Provider
#
# PATCH /api/atlas/v2/groups/{groupId}/privateEndpoint/endpointService/{endpointServiceId}
# operationId: updateGroupPrivateEndpointEndpointService
export def "atlas-groups-private-endpoint-endpoint-service updateGroupPrivateEndpointEndpointService" [
  groupId: string
  endpointServiceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/privateEndpoint/endpointService/($endpointServiceId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return Regionalized Private Endpoint Status
#
# GET /api/atlas/v2/groups/{groupId}/privateEndpoint/regionalMode
# operationId: getGroupPrivateEndpointRegionalMode
export def "atlas-groups-private-endpoint-regional-mode get" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/privateEndpoint/regionalMode" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Toggle Regionalized Private Endpoint Status
#
# PATCH /api/atlas/v2/groups/{groupId}/privateEndpoint/regionalMode
# operationId: toggleGroupPrivateEndpointRegionalMode
export def "atlas-groups-private-endpoint-regional-mode toggleGroupPrivateEndpointRegionalMode" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/privateEndpoint/regionalMode" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Private Endpoints for One Serverless Instance
#
# GET /api/atlas/v2/groups/{groupId}/privateEndpoint/serverless/instance/{instanceName}/endpoint
# DEPRECATED
# operationId: listGroupPrivateEndpointServerlessInstanceEndpoint
@deprecated
export def "atlas-groups-private-endpoint-serverless-instance-endpoint listGroupPrivateEndpointServerlessInstanceEndpoint" [
  groupId: string
  instanceName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/privateEndpoint/serverless/instance/($instanceName)/endpoint" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Private Endpoint for One Serverless Instance
#
# POST /api/atlas/v2/groups/{groupId}/privateEndpoint/serverless/instance/{instanceName}/endpoint
# DEPRECATED
# Docs: https://dochub.mongodb.org/core/serverless-private-endpoint — Set Up a Private Endpoint for a Serverless Instance Tutorial
# operationId: createGroupPrivateEndpointServerlessInstanceEndpoint
@deprecated
export def "atlas-groups-private-endpoint-serverless-instance-endpoint createGroupPrivateEndpointServerlessInstanceEndpoint" [
  groupId: string
  instanceName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/privateEndpoint/serverless/instance/($instanceName)/endpoint" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Remove One Private Endpoint for One Serverless Instance
#
# DELETE /api/atlas/v2/groups/{groupId}/privateEndpoint/serverless/instance/{instanceName}/endpoint/{endpointId}
# DEPRECATED
# operationId: deleteGroupPrivateEndpointServerlessInstanceEndpoint
@deprecated
export def "atlas-groups-private-endpoint-serverless-instance-endpoint delete" [
  groupId: string
  instanceName: string
  endpointId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/privateEndpoint/serverless/instance/($instanceName)/endpoint/($endpointId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Private Endpoint for One Serverless Instance
#
# GET /api/atlas/v2/groups/{groupId}/privateEndpoint/serverless/instance/{instanceName}/endpoint/{endpointId}
# DEPRECATED
# operationId: getGroupPrivateEndpointServerlessInstanceEndpoint
@deprecated
export def "atlas-groups-private-endpoint-serverless-instance-endpoint get" [
  groupId: string
  instanceName: string
  endpointId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/privateEndpoint/serverless/instance/($instanceName)/endpoint/($endpointId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Private Endpoint for One Serverless Instance
#
# PATCH /api/atlas/v2/groups/{groupId}/privateEndpoint/serverless/instance/{instanceName}/endpoint/{endpointId}
# DEPRECATED
# operationId: updateGroupPrivateEndpointServerlessInstanceEndpoint
@deprecated
export def "atlas-groups-private-endpoint-serverless-instance-endpoint updateGroupPrivateEndpointServerlessInstanceEndpoint" [
  groupId: string
  instanceName: string
  endpointId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/privateEndpoint/serverless/instance/($instanceName)/endpoint/($endpointId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Private Endpoint Services for One Provider
#
# GET /api/atlas/v2/groups/{groupId}/privateEndpoint/{cloudProvider}/endpointService
# operationId: listGroupPrivateEndpointEndpointService
export def "atlas-groups-private-endpoint-endpoint-service listGroupPrivateEndpointEndpointService" [
  groupId: string
  cloudProvider: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/privateEndpoint/($cloudProvider)/endpointService" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove One Private Endpoint Service for One Provider
#
# DELETE /api/atlas/v2/groups/{groupId}/privateEndpoint/{cloudProvider}/endpointService/{endpointServiceId}
# operationId: deleteGroupPrivateEndpointEndpointService
export def "atlas-groups-private-endpoint-endpoint-service delete" [
  groupId: string
  cloudProvider: string
  endpointServiceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/privateEndpoint/($cloudProvider)/endpointService/($endpointServiceId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Private Endpoint Service for One Provider
#
# GET /api/atlas/v2/groups/{groupId}/privateEndpoint/{cloudProvider}/endpointService/{endpointServiceId}
# operationId: getGroupPrivateEndpointEndpointService
export def "atlas-groups-private-endpoint-endpoint-service get" [
  groupId: string
  cloudProvider: string
  endpointServiceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/privateEndpoint/($cloudProvider)/endpointService/($endpointServiceId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Private Endpoint for One Provider
#
# POST /api/atlas/v2/groups/{groupId}/privateEndpoint/{cloudProvider}/endpointService/{endpointServiceId}/endpoint
# operationId: createGroupPrivateEndpointEndpointServiceEndpoint
export def "atlas-groups-private-endpoint-endpoint-service-endpoint createGroupPrivateEndpointEndpointServiceEndpoint" [
  groupId: string
  cloudProvider: string
  endpointServiceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/privateEndpoint/($cloudProvider)/endpointService/($endpointServiceId)/endpoint" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Remove One Private Endpoint for One Provider
#
# DELETE /api/atlas/v2/groups/{groupId}/privateEndpoint/{cloudProvider}/endpointService/{endpointServiceId}/endpoint/{endpointId}
# operationId: deleteGroupPrivateEndpointEndpointServiceEndpoint
export def "atlas-groups-private-endpoint-endpoint-service-endpoint delete" [
  groupId: string
  cloudProvider: string
  endpointId: string
  endpointServiceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/privateEndpoint/($cloudProvider)/endpointService/($endpointServiceId)/endpoint/($endpointId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Private Endpoint for One Provider
#
# GET /api/atlas/v2/groups/{groupId}/privateEndpoint/{cloudProvider}/endpointService/{endpointServiceId}/endpoint/{endpointId}
# operationId: getGroupPrivateEndpointEndpointServiceEndpoint
export def "atlas-groups-private-endpoint-endpoint-service-endpoint get" [
  groupId: string
  cloudProvider: string
  endpointId: string
  endpointServiceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/privateEndpoint/($cloudProvider)/endpointService/($endpointServiceId)/endpoint/($endpointId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Verify Connect via Peering-Only Mode for One Project
#
# GET /api/atlas/v2/groups/{groupId}/privateIpMode
# DEPRECATED
# Docs: https://docs.atlas.mongodb.com/reference/faq/connection-changes/ — Changes to Connection Strings
# operationId: verifyGroupPrivateIpMode
@deprecated
export def "atlas-groups-private-ip-mode verifyGroupPrivateIpMode" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/privateIpMode" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Disable Connect via Peering-Only Mode for One Project
#
# PATCH /api/atlas/v2/groups/{groupId}/privateIpMode
# DEPRECATED
# Docs: https://docs.atlas.mongodb.com/reference/faq/connection-changes/ — Changes to Connection Strings
# operationId: disableGroupPrivateIpModePeering
@deprecated
export def "atlas-groups-private-ip-mode disableGroupPrivateIpModePeering" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/privateIpMode" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Federated Database Instance and Online Archive Private Endpoints in One Project
#
# GET /api/atlas/v2/groups/{groupId}/privateNetworkSettings/endpointIds
# operationId: listGroupPrivateNetworkSettingEndpointIds
export def "atlas-groups-private-network-settings-endpoint-ids listGroupPrivateNetworkSettingEndpointIds" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/privateNetworkSettings/endpointIds" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Federated Database Instance and Online Archive Private Endpoint for One Project
#
# POST /api/atlas/v2/groups/{groupId}/privateNetworkSettings/endpointIds
# Docs: https://www.mongodb.com/docs/atlas/data-federation/tutorial/config-private-endpoint/ — Atlas Data Federation Regions and Service Names
# operationId: createGroupPrivateNetworkSettingEndpointId
export def "atlas-groups-private-network-settings-endpoint-ids createGroupPrivateNetworkSettingEndpointId" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/privateNetworkSettings/endpointIds" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Remove One Federated Database Instance and Online Archive Private Endpoint from One Project
#
# DELETE /api/atlas/v2/groups/{groupId}/privateNetworkSettings/endpointIds/{endpointId}
# operationId: deleteGroupPrivateNetworkSettingEndpointId
export def "atlas-groups-private-network-settings-endpoint-ids delete" [
  groupId: string
  endpointId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/privateNetworkSettings/endpointIds/($endpointId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Federated Database Instance and Online Archive Private Endpoint in One Project
#
# GET /api/atlas/v2/groups/{groupId}/privateNetworkSettings/endpointIds/{endpointId}
# operationId: getGroupPrivateNetworkSettingEndpointId
export def "atlas-groups-private-network-settings-endpoint-ids get" [
  groupId: string
  endpointId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/privateNetworkSettings/endpointIds/($endpointId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All MongoDB Processes in One Project
#
# GET /api/atlas/v2/groups/{groupId}/processes
# operationId: listGroupProcesses
export def "atlas-groups-processes listGroupProcesses" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/processes" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One MongoDB Process by ID
#
# GET /api/atlas/v2/groups/{groupId}/processes/{processId}
# operationId: getGroupProcess
export def "atlas-groups-processes get" [
  groupId: string
  processId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/processes/($processId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Ranked Namespaces from One Host
#
# GET /api/atlas/v2/groups/{groupId}/processes/{processId}/collStats/namespaces
# operationId: getGroupProcessCollStatNamespaces
export def "atlas-groups-processes-coll-stats-namespaces get" [
  groupId: string
  processId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --start: string # Date and time when MongoDB Cloud begins reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**. (format: date-time)
  --end: string # Date and time when MongoDB Cloud stops reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**. (format: date-time)
  --period: string # Duration over which Atlas reports the metrics. This parameter expresses its value in the ISO 8601 duration format in UTC. Include this parameter when you do not set **start** and **end**. (e.g. PT10H)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "start" $start "scalar") (serialize-qp "end" $end "scalar") (serialize-qp "period" $period "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/processes/($processId)/collStats/namespaces" $qp)
  let accept_val = "application/vnd.atlas.2023-11-15+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Available Databases for One MongoDB Process
#
# GET /api/atlas/v2/groups/{groupId}/processes/{processId}/databases
# Docs: https://www.mongodb.com/docs/atlas/reference/limitations/ — Operational Limits
# operationId: listGroupProcessDatabases
export def "atlas-groups-processes-databases listGroupProcessDatabases" [
  groupId: string
  processId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/processes/($processId)/databases" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Database for One MongoDB Process
#
# GET /api/atlas/v2/groups/{groupId}/processes/{processId}/databases/{databaseName}
# operationId: getGroupProcessDatabase
export def "atlas-groups-processes-databases get" [
  groupId: string
  databaseName: string
  processId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/processes/($processId)/databases/($databaseName)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Measurements for One Database in One MongoDB Process
#
# GET /api/atlas/v2/groups/{groupId}/processes/{processId}/databases/{databaseName}/measurements
# operationId: getGroupProcessDatabaseMeasurements
export def "atlas-groups-processes-databases-measurements get" [
  groupId: string
  databaseName: string
  processId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --m: list # One or more types of measurement to request for this MongoDB process. If omitted, the resource returns all measurements. To specify multiple values for `m`, repeat the `m` parameter for each value. Specify measurements that apply to the specified host. MongoDB Cloud returns an error if you specified any invalid measurements.
  --granularity: string # Duration that specifies the interval at which Atlas reports the metrics. This parameter expresses its value in the ISO 8601 duration format in UTC. (e.g. PT1M)
  --period: string # Duration over which Atlas reports the metrics. This parameter expresses its value in the ISO 8601 duration format in UTC. Include this parameter when you do not set **start** and **end**. (e.g. PT10H)
  --start: string # Date and time when MongoDB Cloud begins reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**. (format: date-time)
  --end: string # Date and time when MongoDB Cloud stops reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**. (format: date-time)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "m" $m "multi") (serialize-qp "granularity" $granularity "scalar") (serialize-qp "period" $period "scalar") (serialize-qp "start" $start "scalar") (serialize-qp "end" $end "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/processes/($processId)/databases/($databaseName)/measurements" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Available Disks for One MongoDB Process
#
# GET /api/atlas/v2/groups/{groupId}/processes/{processId}/disks
# operationId: listGroupProcessDisks
export def "atlas-groups-processes-disks listGroupProcessDisks" [
  groupId: string
  processId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/processes/($processId)/disks" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Measurements for One Disk
#
# GET /api/atlas/v2/groups/{groupId}/processes/{processId}/disks/{partitionName}
# operationId: getGroupProcessDisk
export def "atlas-groups-processes-disks get" [
  partitionName: string
  groupId: string
  processId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/processes/($processId)/disks/($partitionName)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Measurements of One Disk for One MongoDB Process
#
# GET /api/atlas/v2/groups/{groupId}/processes/{processId}/disks/{partitionName}/measurements
# operationId: getGroupProcessDiskMeasurements
export def "atlas-groups-processes-disks-measurements get" [
  groupId: string
  partitionName: string
  processId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --m: list # One or more types of measurement to request for this MongoDB process. If omitted, the resource returns all measurements. To specify multiple values for `m`, repeat the `m` parameter for each value. Specify measurements that apply to the specified host. MongoDB Cloud returns an error if you specified any invalid measurements.
  --granularity: string # Duration that specifies the interval at which Atlas reports the metrics. This parameter expresses its value in the ISO 8601 duration format in UTC. (e.g. PT1M)
  --period: string # Duration over which Atlas reports the metrics. This parameter expresses its value in the ISO 8601 duration format in UTC. Include this parameter when you do not set **start** and **end**. (e.g. PT10H)
  --start: string # Date and time when MongoDB Cloud begins reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**. (format: date-time)
  --end: string # Date and time when MongoDB Cloud stops reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**. (format: date-time)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "m" $m "multi") (serialize-qp "granularity" $granularity "scalar") (serialize-qp "period" $period "scalar") (serialize-qp "start" $start "scalar") (serialize-qp "end" $end "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/processes/($processId)/disks/($partitionName)/measurements" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Measurements for One MongoDB Process
#
# GET /api/atlas/v2/groups/{groupId}/processes/{processId}/measurements
# operationId: getGroupProcessMeasurements
export def "atlas-groups-processes-measurements get" [
  groupId: string
  processId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --m: list # One or more types of measurement to request for this MongoDB process. If omitted, the resource returns all measurements. To specify multiple values for `m`, repeat the `m` parameter for each value. Specify measurements that apply to the specified host. MongoDB Cloud returns an error if you specified any invalid measurements.
  --period: string # Duration over which Atlas reports the metrics. This parameter expresses its value in the ISO 8601 duration format in UTC. Include this parameter when you do not set **start** and **end**. (e.g. PT10H)
  --granularity: string # Duration that specifies the interval at which Atlas reports the metrics. This parameter expresses its value in the ISO 8601 duration format in UTC. (e.g. PT1M)
  --start: string # Date and time when MongoDB Cloud begins reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**. (format: date-time)
  --end: string # Date and time when MongoDB Cloud stops reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**. (format: date-time)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "m" $m "multi") (serialize-qp "period" $period "scalar") (serialize-qp "granularity" $granularity "scalar") (serialize-qp "start" $start "scalar") (serialize-qp "end" $end "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/processes/($processId)/measurements" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Namespaces for One Host
#
# GET /api/atlas/v2/groups/{groupId}/processes/{processId}/performanceAdvisor/namespaces
# operationId: listGroupProcessPerformanceAdvisorNamespaces
export def "atlas-groups-processes-performance-advisor-namespaces listGroupProcessPerformanceAdvisorNamespaces" [
  groupId: string
  processId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --duration: int # Length of time expressed during which the query finds suggested indexes among the managed namespaces in the cluster. This parameter expresses its value in milliseconds.  - If you don't specify the **since** parameter, the endpoint returns data covering the duration before the current time. - If you specify neither the **duration** nor **since** parameters, the endpoint returns data from the previous 24 hours. (format: int64)
  --since: int # Date and time from which the query retrieves the suggested indexes. This parameter expresses its value in the number of milliseconds that have elapsed since the [UNIX epoch](https://en.wikipedia.org/wiki/Unix_time).  - If you don't specify the **duration** parameter, the endpoint returns data covering from the **since** value and the current time. - If you specify neither the **duration** nor the **since** parameters, the endpoint returns data from the previous 24 hours. (format: int64)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "duration" $duration "scalar") (serialize-qp "since" $since "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/processes/($processId)/performanceAdvisor/namespaces" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Slow Queries
#
# GET /api/atlas/v2/groups/{groupId}/processes/{processId}/performanceAdvisor/slowQueryLogs
# operationId: listGroupProcessPerformanceAdvisorSlowQueryLogs
export def "atlas-groups-processes-performance-advisor-slow-query-logs listGroupProcessPerformanceAdvisorSlowQueryLogs" [
  groupId: string
  processId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --duration: int # Length of time expressed during which the query finds slow queries among the managed namespaces in the cluster. This parameter expresses its value in milliseconds.  - If you don't specify the **since** parameter, the endpoint returns data covering the duration before the current time. - If you specify neither the **duration** nor **since** parameters, the endpoint returns data from the previous 24 hours. (format: int64)
  --namespaces: list # Namespaces from which to retrieve slow queries. A namespace consists of one database and one collection resource written as `.`: `<database>.<collection>`. To include multiple namespaces, pass the parameter multiple times delimited with an ampersand (`&`) between each namespace. Omit this parameter to return results for all namespaces.
  --nLogs: int # Maximum number of lines from the log to return. (format: int64, default: 20000)
  --since: int # Date and time from which the query retrieves the slow queries. This parameter expresses its value in the number of milliseconds that have elapsed since the [UNIX epoch](https://en.wikipedia.org/wiki/Unix_time).  - If you don't specify the **duration** parameter, the endpoint returns data covering from the **since** value and the current time. - If you specify neither the **duration** nor the **since** parameters, the endpoint returns data from the previous 24 hours. (format: int64)
  --includeMetrics: oneof<nothing, bool> # Whether or not to include metrics extracted from the slow query log as separate fields. (default: false)
  --includeReplicaState: oneof<nothing, bool> # Whether or not to include the replica state of the host when the slow query log was generated as a separate field. (default: false)
  --includeOpType: oneof<nothing, bool> # Whether or not to include the operation type (read/write/command) extracted from the slow query log as a separate field. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "duration" $duration "scalar") (serialize-qp "namespaces" $namespaces "multi") (serialize-qp "nLogs" $nLogs "scalar") (serialize-qp "since" $since "scalar") (serialize-qp "includeMetrics" $includeMetrics "scalar") (serialize-qp "includeReplicaState" $includeReplicaState "scalar") (serialize-qp "includeOpType" $includeOpType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/processes/($processId)/performanceAdvisor/slowQueryLogs" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Suggested Indexes
#
# GET /api/atlas/v2/groups/{groupId}/processes/{processId}/performanceAdvisor/suggestedIndexes
# operationId: listGroupProcessPerformanceAdvisorSuggestedIndexes
export def "atlas-groups-processes-performance-advisor-suggested-indexes listGroupProcessPerformanceAdvisorSuggestedIndexes" [
  groupId: string
  processId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --duration: int # Length of time expressed during which the query finds suggested indexes among the managed namespaces in the cluster. This parameter expresses its value in milliseconds.  - If you don't specify the **since** parameter, the endpoint returns data covering the duration before the current time. - If you specify neither the **duration** nor **since** parameters, the endpoint returns data from the previous 24 hours. (format: int64)
  --namespaces: list # Namespaces from which to retrieve suggested indexes. A namespace consists of one database and one collection resource written as `.`: `<database>.<collection>`. To include multiple namespaces, pass the parameter multiple times delimited with an ampersand (`&`) between each namespace. Omit this parameter to return results for all namespaces.
  --nExamples: int # Maximum number of example queries that benefit from the suggested index. (format: int64, default: 5)
  --nIndexes: int # Number that indicates the maximum indexes to suggest. (format: int64)
  --since: int # Date and time from which the query retrieves the suggested indexes. This parameter expresses its value in the number of milliseconds that have elapsed since the [UNIX epoch](https://en.wikipedia.org/wiki/Unix_time).  - If you don't specify the **duration** parameter, the endpoint returns data covering from the **since** value and the current time. - If you specify neither the **duration** nor the **since** parameters, the endpoint returns data from the previous 24 hours. (format: int64)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "duration" $duration "scalar") (serialize-qp "namespaces" $namespaces "multi") (serialize-qp "nExamples" $nExamples "scalar") (serialize-qp "nIndexes" $nIndexes "scalar") (serialize-qp "since" $since "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/processes/($processId)/performanceAdvisor/suggestedIndexes" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Host-Level Query Latency
#
# GET /api/atlas/v2/groups/{groupId}/processes/{processId}/{databaseName}/{collectionName}/collStats/measurements
# operationId: listGroupProcessCollStatMeasurements
export def "atlas-groups-processes-coll-stats-measurements listGroupProcessCollStatMeasurements" [
  groupId: string
  processId: string
  databaseName: string
  collectionName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --metrics: list # List that contains the metrics that you want to retrieve for the associated data series. If you don't set this parameter, this resource returns data series for all Coll Stats Latency metrics.
  --start: string # Date and time when MongoDB Cloud begins reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**. (format: date-time)
  --end: string # Date and time when MongoDB Cloud stops reporting the metrics. This parameter expresses its value in the ISO 8601 timestamp format in UTC. Include this parameter when you do not set **period**. (format: date-time)
  --period: string # Duration over which Atlas reports the metrics. This parameter expresses its value in the ISO 8601 duration format in UTC. Include this parameter when you do not set **start** and **end**. (e.g. PT10H)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "metrics" $metrics "multi") (serialize-qp "start" $start "scalar") (serialize-qp "end" $end "scalar") (serialize-qp "period" $period "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/processes/($processId)/($databaseName)/($collectionName)/collStats/measurements" $qp)
  let accept_val = "application/vnd.atlas.2023-11-15+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Disable Push-Based Log Export for One Project
#
# DELETE /api/atlas/v2/groups/{groupId}/pushBasedLogExport
# DEPRECATED
# operationId: deleteGroupPushBasedLogExport
@deprecated
export def "atlas-groups-push-based-log-export delete" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/pushBasedLogExport" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Push-Based Log Export Configuration in One Project
#
# GET /api/atlas/v2/groups/{groupId}/pushBasedLogExport
# DEPRECATED
# operationId: getGroupPushBasedLogExport
@deprecated
export def "atlas-groups-push-based-log-export get" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/pushBasedLogExport" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Push-Based Log Export Configuration in One Project
#
# PATCH /api/atlas/v2/groups/{groupId}/pushBasedLogExport
# DEPRECATED
# operationId: updateGroupPushBasedLogExport
@deprecated
export def "atlas-groups-push-based-log-export updateGroupPushBasedLogExport" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/pushBasedLogExport" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Create One Push-Based Log Export Configuration in One Project
#
# POST /api/atlas/v2/groups/{groupId}/pushBasedLogExport
# DEPRECATED
# operationId: createGroupPushBasedLogExport
@deprecated
export def "atlas-groups-push-based-log-export createGroupPushBasedLogExport" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/pushBasedLogExport" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Load Sample Dataset into One Cluster
#
# POST /api/atlas/v2/groups/{groupId}/sampleDatasetLoad/{name}
# operationId: requestGroupSampleDatasetLoad
export def "atlas-groups-sample-dataset-load requestGroupSampleDatasetLoad" [
  groupId: string
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/sampleDatasetLoad/($name)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Status of Sample Dataset Load for One Cluster
#
# GET /api/atlas/v2/groups/{groupId}/sampleDatasetLoad/{sampleDatasetId}
# operationId: getGroupSampleDatasetLoad
export def "atlas-groups-sample-dataset-load get" [
  groupId: string
  sampleDatasetId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/sampleDatasetLoad/($sampleDatasetId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Cluster Description from Sandbox Template Configuration
#
# GET /api/atlas/v2/groups/{groupId}/sandbox/{sandboxConfigId}:generateClusterDescription
# operationId: generateGroupSandboxClusterDescription
export def "atlas-groups-sandbox generateGroupSandboxClusterDescription" [
  groupId: string
  sandboxConfigId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/sandbox/($sandboxConfigId):generateClusterDescription" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Serverless Instances in One Project
#
# GET /api/atlas/v2/groups/{groupId}/serverless
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Flex-Clusters/operation/listFlexClusters — List Flex Clusters
# operationId: listGroupServerlessInstances
@deprecated
export def "atlas-groups-serverless listGroupServerlessInstances" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/serverless" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Restore Jobs for One Serverless Instance
#
# GET /api/atlas/v2/groups/{groupId}/serverless/{clusterName}/backup/restoreJobs
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Flex-Restore-Jobs/operation/listFlexBackupRestoreJobs — List Flex Backup Restore Jobs
# operationId: listGroupServerlessBackupRestoreJobs
@deprecated
export def "atlas-groups-serverless-backup-restore-jobs listGroupServerlessBackupRestoreJobs" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/serverless/($clusterName)/backup/restoreJobs" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Restore Job for One Serverless Instance
#
# POST /api/atlas/v2/groups/{groupId}/serverless/{clusterName}/backup/restoreJobs
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Flex-Restore-Jobs/operation/createFlexBackupRestoreJob — Create Flex Backup Restore Job
# operationId: createGroupServerlessBackupRestoreJob
@deprecated
export def "atlas-groups-serverless-backup-restore-jobs createGroupServerlessBackupRestoreJob" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/serverless/($clusterName)/backup/restoreJobs" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return One Restore Job for One Serverless Instance
#
# GET /api/atlas/v2/groups/{groupId}/serverless/{clusterName}/backup/restoreJobs/{restoreJobId}
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Flex-Restore-Jobs/operation/getFlexBackupRestoreJob — Get Flex Backup Restore Job
# operationId: getGroupServerlessBackupRestoreJob
@deprecated
export def "atlas-groups-serverless-backup-restore-jobs get" [
  groupId: string
  clusterName: string
  restoreJobId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/serverless/($clusterName)/backup/restoreJobs/($restoreJobId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Snapshots of One Serverless Instance
#
# GET /api/atlas/v2/groups/{groupId}/serverless/{clusterName}/backup/snapshots
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Flex-Snapshots/operation/listFlexBackups — List Flex Backups
# operationId: listGroupServerlessBackupSnapshots
@deprecated
export def "atlas-groups-serverless-backup-snapshots listGroupServerlessBackupSnapshots" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/serverless/($clusterName)/backup/snapshots" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Snapshot of One Serverless Instance
#
# GET /api/atlas/v2/groups/{groupId}/serverless/{clusterName}/backup/snapshots/{snapshotId}
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Flex-Snapshots/operation/getFlexBackup — Get Flex Backup
# operationId: getGroupServerlessBackupSnapshot
@deprecated
export def "atlas-groups-serverless-backup-snapshots get" [
  groupId: string
  clusterName: string
  snapshotId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/serverless/($clusterName)/backup/snapshots/($snapshotId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Serverless Auto-Indexing Status
#
# GET /api/atlas/v2/groups/{groupId}/serverless/{clusterName}/performanceAdvisor/autoIndexing
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Serverless-Instances/operation/createServerlessInstance — Create Serverless Instance
# operationId: getGroupServerlessPerformanceAdvisorAutoIndexing
@deprecated
export def "atlas-groups-serverless-performance-advisor-auto-indexing get" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/serverless/($clusterName)/performanceAdvisor/autoIndexing" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Set Serverless Auto-Indexing Status
#
# POST /api/atlas/v2/groups/{groupId}/serverless/{clusterName}/performanceAdvisor/autoIndexing
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Serverless-Instances/operation/createServerlessInstance — Create Serverless Instance
# operationId: setGroupServerlessPerformanceAdvisorAutoIndexing
@deprecated
export def "atlas-groups-serverless-performance-advisor-auto-indexing setGroupServerlessPerformanceAdvisorAutoIndexing" [
  groupId: string
  clusterName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --enable: oneof<nothing, bool> # Value that we want to set for the Serverless Auto Indexing toggle.
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "enable" $enable "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/serverless/($clusterName)/performanceAdvisor/autoIndexing" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Serverless Instance from One Project
#
# GET /api/atlas/v2/groups/{groupId}/serverless/{name}
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Flex-Clusters/operation/getFlexCluster — Get Flex Cluster
# operationId: getGroupServerlessInstance
@deprecated
export def "atlas-groups-serverless get" [
  groupId: string
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/serverless/($name)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Project Service Accounts
#
# GET /api/atlas/v2/groups/{groupId}/serviceAccounts
# operationId: listGroupServiceAccounts
export def "atlas-groups-service-accounts listGroupServiceAccounts" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/serviceAccounts" $qp)
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Project Service Account
#
# POST /api/atlas/v2/groups/{groupId}/serviceAccounts
# operationId: createGroupServiceAccount
export def "atlas-groups-service-accounts createGroupServiceAccount" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/serviceAccounts" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-08-05+json" $body
}

# Remove One Project Service Account
#
# DELETE /api/atlas/v2/groups/{groupId}/serviceAccounts/{clientId}
# operationId: deleteGroupServiceAccount
export def "atlas-groups-service-accounts delete" [
  clientId: string
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/serviceAccounts/($clientId)" $qp)
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Project Service Account
#
# GET /api/atlas/v2/groups/{groupId}/serviceAccounts/{clientId}
# operationId: getGroupServiceAccount
export def "atlas-groups-service-accounts get" [
  groupId: string
  clientId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/serviceAccounts/($clientId)" $qp)
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Project Service Account
#
# PATCH /api/atlas/v2/groups/{groupId}/serviceAccounts/{clientId}
# operationId: updateGroupServiceAccount
export def "atlas-groups-service-accounts updateGroupServiceAccount" [
  clientId: string
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/serviceAccounts/($clientId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-08-05+json" $body
}

# Return All Access List Entries for One Project Service Account
#
# GET /api/atlas/v2/groups/{groupId}/serviceAccounts/{clientId}/accessList
# operationId: listGroupServiceAccountAccessList
export def "atlas-groups-service-accounts-access-list listGroupServiceAccountAccessList" [
  groupId: string
  clientId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/serviceAccounts/($clientId)/accessList" $qp)
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add Access List Entries for One Project Service Account
#
# POST /api/atlas/v2/groups/{groupId}/serviceAccounts/{clientId}/accessList
# operationId: createGroupServiceAccountAccessList
export def "atlas-groups-service-accounts-access-list createGroupServiceAccountAccessList" [
  groupId: string
  clientId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/serviceAccounts/($clientId)/accessList" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-08-05+json" $body
}

# Remove One Access List Entry from One Project Service Account
#
# DELETE /api/atlas/v2/groups/{groupId}/serviceAccounts/{clientId}/accessList/{ipAddress}
# operationId: deleteGroupServiceAccountAccessListEntry
export def "atlas-groups-service-accounts-access-list delete" [
  groupId: string
  clientId: string
  ipAddress: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/serviceAccounts/($clientId)/accessList/($ipAddress)" $qp)
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Project Service Account Secret
#
# POST /api/atlas/v2/groups/{groupId}/serviceAccounts/{clientId}/secrets
# operationId: createGroupServiceAccountSecret
export def "atlas-groups-service-accounts-secrets createGroupServiceAccountSecret" [
  groupId: string
  clientId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/serviceAccounts/($clientId)/secrets" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-08-05+json" $body
}

# Delete One Project Service Account Secret
#
# DELETE /api/atlas/v2/groups/{groupId}/serviceAccounts/{clientId}/secrets/{secretId}
# operationId: deleteGroupServiceAccountSecret
export def "atlas-groups-service-accounts-secrets delete" [
  clientId: string
  secretId: string
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/serviceAccounts/($clientId)/secrets/($secretId)" $qp)
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Assign One Service Account to One Project
#
# POST /api/atlas/v2/groups/{groupId}/serviceAccounts/{clientId}:invite
# operationId: inviteGroupServiceAccount
export def "atlas-groups-service-accounts inviteGroupServiceAccount" [
  clientId: string
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/serviceAccounts/($clientId):invite" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-08-05+json" $body
}

# Return Project Settings
#
# GET /api/atlas/v2/groups/{groupId}/settings
# operationId: getGroupSettings
export def "atlas-groups-settings get" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/settings" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update Project Settings
#
# PATCH /api/atlas/v2/groups/{groupId}/settings
# operationId: updateGroupSettings
export def "atlas-groups-settings updateGroupSettings" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/settings" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Standby Links for One Project
#
# GET /api/atlas/v2/groups/{groupId}/standbyLinks
# operationId: listGroupStandbyLinks
export def "atlas-groups-standby-links listGroupStandbyLinks" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/standbyLinks" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create Standby Link
#
# POST /api/atlas/v2/groups/{groupId}/standbyLinks
# operationId: createGroupStandbyLink
export def "atlas-groups-standby-links createGroupStandbyLink" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether to wrap the response in an envelope.
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/standbyLinks" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.preview+json" $body
}

# Delete Standby Link
#
# DELETE /api/atlas/v2/groups/{groupId}/standbyLinks/{standbyLinkId}
# operationId: deleteGroupStandbyLink
export def "atlas-groups-standby-links delete" [
  groupId: string
  standbyLinkId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether to wrap the response in an envelope.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/standbyLinks/($standbyLinkId)" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Standby Link
#
# GET /api/atlas/v2/groups/{groupId}/standbyLinks/{standbyLinkId}
# operationId: getGroupStandbyLink
export def "atlas-groups-standby-links get" [
  groupId: string
  standbyLinkId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether to wrap the response in an envelope.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/standbyLinks/($standbyLinkId)" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Standby Link Failovers
#
# GET /api/atlas/v2/groups/{groupId}/standbyLinks/{standbyLinkId}/failovers
# operationId: listGroupStandbyLinkFailovers
export def "atlas-groups-standby-links-failovers listGroupStandbyLinkFailovers" [
  groupId: string
  standbyLinkId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/standbyLinks/($standbyLinkId)/failovers" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create Standby Link Failover
#
# POST /api/atlas/v2/groups/{groupId}/standbyLinks/{standbyLinkId}/failovers
# operationId: createGroupStandbyLinkFailover
export def "atlas-groups-standby-links-failovers createGroupStandbyLinkFailover" [
  groupId: string
  standbyLinkId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether to wrap the response in an envelope.
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/standbyLinks/($standbyLinkId)/failovers" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.preview+json" $body
}

# Return One Standby Link Failover
#
# GET /api/atlas/v2/groups/{groupId}/standbyLinks/{standbyLinkId}/failovers/{failoverId}
# operationId: getGroupStandbyLinkFailover
export def "atlas-groups-standby-links-failovers get" [
  groupId: string
  standbyLinkId: string
  failoverId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether to wrap the response in an envelope.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/standbyLinks/($standbyLinkId)/failovers/($failoverId)" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Inspect Failover Readiness for One Standby Link
#
# GET /api/atlas/v2/groups/{groupId}/standbyLinks/{standbyLinkId}:inspectFailoverReadiness
# operationId: inspectGroupStandbyLinkFailoverReadiness
export def "atlas-groups-standby-links inspectGroupStandbyLinkFailoverReadiness" [
  groupId: string
  standbyLinkId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether to wrap the response in an envelope.
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/standbyLinks/($standbyLinkId):inspectFailoverReadiness" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Stream Workspaces in One Project
#
# GET /api/atlas/v2/groups/{groupId}/streams
# operationId: listGroupStreamWorkspaces
export def "atlas-groups-streams listGroupStreamWorkspaces" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams" $qp)
  let accept_val = "application/vnd.atlas.2023-02-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Stream Workspace
#
# POST /api/atlas/v2/groups/{groupId}/streams
# operationId: createGroupStreamWorkspace
export def "atlas-groups-streams createGroupStreamWorkspace" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-02-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-02-01+json" $body
}

# Return Account ID and VPC ID for One Project and Region
#
# GET /api/atlas/v2/groups/{groupId}/streams/accountDetails
# operationId: getGroupStreamAccountDetails
export def "atlas-groups-streams-account-details get" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --cloudProvider: string # One of "aws", "azure" or "gcp".
  --regionName: string # The cloud provider specific region name, i.e. "US_EAST_1" for cloud provider "aws".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "cloudProvider" $cloudProvider "scalar") (serialize-qp "regionName" $regionName "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/accountDetails" $qp)
  let accept_val = "application/vnd.atlas.2024-11-13+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Active Incoming VPC Peering Connections
#
# GET /api/atlas/v2/groups/{groupId}/streams/activeVpcPeeringConnections
# operationId: listGroupStreamActiveVpcPeeringConnections
export def "atlas-groups-streams-active-vpc-peering-connections listGroupStreamActiveVpcPeeringConnections" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/activeVpcPeeringConnections" $qp)
  let accept_val = "application/vnd.atlas.2024-11-13+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Private Link Connections
#
# GET /api/atlas/v2/groups/{groupId}/streams/privateLinkConnections
# operationId: listGroupStreamPrivateLinkConnections
export def "atlas-groups-streams-private-link-connections listGroupStreamPrivateLinkConnections" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/privateLinkConnections" $qp)
  let accept_val = "application/vnd.atlas.2023-02-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Private Link Connection
#
# POST /api/atlas/v2/groups/{groupId}/streams/privateLinkConnections
# operationId: createGroupStreamPrivateLinkConnection
export def "atlas-groups-streams-private-link-connections createGroupStreamPrivateLinkConnection" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/privateLinkConnections" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-02-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-02-01+json" $body
}

# Delete One Private Link Connection
#
# DELETE /api/atlas/v2/groups/{groupId}/streams/privateLinkConnections/{connectionId}
# operationId: deleteGroupStreamPrivateLinkConnection
export def "atlas-groups-streams-private-link-connections delete" [
  groupId: string
  connectionId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/privateLinkConnections/($connectionId)" $qp)
  let accept_val = "application/vnd.atlas.2023-02-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Private Link Connection
#
# GET /api/atlas/v2/groups/{groupId}/streams/privateLinkConnections/{connectionId}
# operationId: getGroupStreamPrivateLinkConnection
export def "atlas-groups-streams-private-link-connections get" [
  groupId: string
  connectionId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/privateLinkConnections/($connectionId)" $qp)
  let accept_val = "application/vnd.atlas.2023-02-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All VPC Peering Connections
#
# GET /api/atlas/v2/groups/{groupId}/streams/vpcPeeringConnections
# operationId: listGroupStreamVpcPeeringConnections
export def "atlas-groups-streams-vpc-peering-connections listGroupStreamVpcPeeringConnections" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --requesterAccountId: string # The Account ID of the VPC Peering connection/s.
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "requesterAccountId" $requesterAccountId "scalar") (serialize-qp "envelope" $envelope "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/vpcPeeringConnections" $qp)
  let accept_val = "application/vnd.atlas.2023-02-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete One VPC Peering Connection
#
# DELETE /api/atlas/v2/groups/{groupId}/streams/vpcPeeringConnections/{id}
# operationId: deleteGroupStreamVpcPeeringConnection
export def "atlas-groups-streams-vpc-peering-connections delete" [
  groupId: string
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/vpcPeeringConnections/($id)" $qp)
  let accept_val = "application/vnd.atlas.2023-02-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Accept One Incoming VPC Peering Connection
#
# POST /api/atlas/v2/groups/{groupId}/streams/vpcPeeringConnections/{id}:accept
# operationId: acceptGroupStreamVpcPeeringConnection
export def "atlas-groups-streams-vpc-peering-connections acceptGroupStreamVpcPeeringConnection" [
  groupId: string
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/vpcPeeringConnections/($id):accept" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-02-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-02-01+json" $body
}

# Reject One Incoming VPC Peering Connection
#
# POST /api/atlas/v2/groups/{groupId}/streams/vpcPeeringConnections/{id}:reject
# operationId: rejectGroupStreamVpcPeeringConnection
export def "atlas-groups-streams-vpc-peering-connections rejectGroupStreamVpcPeeringConnection" [
  groupId: string
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/vpcPeeringConnections/($id):reject" $qp)
  let accept_val = "application/vnd.atlas.2023-02-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete One Stream Workspace
#
# DELETE /api/atlas/v2/groups/{groupId}/streams/{tenantName}
# operationId: deleteGroupStreamWorkspace
export def "atlas-groups-streams delete" [
  groupId: string
  tenantName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/($tenantName)" $qp)
  let accept_val = "application/vnd.atlas.2023-02-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Stream Workspace
#
# GET /api/atlas/v2/groups/{groupId}/streams/{tenantName}
# operationId: getGroupStreamWorkspace
export def "atlas-groups-streams get" [
  groupId: string
  tenantName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeConnections: oneof<nothing, bool> # Flag to indicate whether connections information should be included in the stream workspace.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeConnections" $includeConnections "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/($tenantName)" $qp)
  let accept_val = "application/vnd.atlas.2023-02-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Stream Workspace
#
# PATCH /api/atlas/v2/groups/{groupId}/streams/{tenantName}
# operationId: updateGroupStreamWorkspace
export def "atlas-groups-streams updateGroupStreamWorkspace" [
  groupId: string
  tenantName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-9 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/($tenantName)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/vnd.atlas.2023-02-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-02-01+json" $body
}

# Download Audit Logs for One Atlas Stream Processing Workspace
#
# GET /api/atlas/v2/groups/{groupId}/streams/{tenantName}/auditLogs
# operationId: downloadGroupStreamAuditLogs
export def "atlas-groups-streams-audit-logs downloadGroupStreamAuditLogs" [
  groupId: string
  tenantName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --endDate: int # Timestamp that specifies the end point for the range of log messages to download.  MongoDB Cloud expresses this timestamp in the number of seconds that have elapsed since the UNIX epoch. (format: int64, e.g. 1636481348)
  --startDate: int # Timestamp that specifies the starting point for the range of log messages to download. MongoDB Cloud expresses this timestamp in the number of seconds that have elapsed since the UNIX epoch. (format: int64, e.g. 1636466948)
  --spName: string # Name of the stream processor to download logs for. An empty string will download logs for all stream processors in the workspace.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "endDate" $endDate "scalar") (serialize-qp "startDate" $startDate "scalar") (serialize-qp "spName" $spName "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/($tenantName)/auditLogs" $qp)
  let accept_val = "application/vnd.atlas.2023-02-01+gzip"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Connections of the Stream Workspaces
#
# GET /api/atlas/v2/groups/{groupId}/streams/{tenantName}/connections
# operationId: listGroupStreamConnections
export def "atlas-groups-streams-connections listGroupStreamConnections" [
  groupId: string
  tenantName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/($tenantName)/connections" $qp)
  let accept_val = "application/vnd.atlas.2023-02-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Stream Connection
#
# POST /api/atlas/v2/groups/{groupId}/streams/{tenantName}/connections
# operationId: createGroupStreamConnection
export def "atlas-groups-streams-connections createGroupStreamConnection" [
  groupId: string
  tenantName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/($tenantName)/connections" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-02-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-02-01+json" $body
}

# Delete One Stream Connection
#
# DELETE /api/atlas/v2/groups/{groupId}/streams/{tenantName}/connections/{connectionName}
# operationId: deleteGroupStreamConnection
export def "atlas-groups-streams-connections delete" [
  groupId: string
  tenantName: string
  connectionName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/($tenantName)/connections/($connectionName)" $qp)
  let accept_val = "application/vnd.atlas.2023-02-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Stream Connection
#
# GET /api/atlas/v2/groups/{groupId}/streams/{tenantName}/connections/{connectionName}
# operationId: getGroupStreamConnection
export def "atlas-groups-streams-connections get" [
  groupId: string
  tenantName: string
  connectionName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/($tenantName)/connections/($connectionName)" $qp)
  let accept_val = "application/vnd.atlas.2023-02-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Stream Connection
#
# PATCH /api/atlas/v2/groups/{groupId}/streams/{tenantName}/connections/{connectionName}
# operationId: updateGroupStreamConnection
export def "atlas-groups-streams-connections updateGroupStreamConnection" [
  groupId: string
  tenantName: string
  connectionName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/($tenantName)/connections/($connectionName)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-02-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-02-01+json" $body
}

# Create One Failover Stream Connection
#
# POST /api/atlas/v2/groups/{groupId}/streams/{tenantName}/connections/{connectionName}/failoverConnections
# operationId: createGroupStreamConnectionFailoverConnection
export def "atlas-groups-streams-connections-failover-connections createGroupStreamConnectionFailoverConnection" [
  groupId: string
  tenantName: string
  connectionName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/($tenantName)/connections/($connectionName)/failoverConnections" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.preview+json" $body
}

# Create One Stream Processor
#
# POST /api/atlas/v2/groups/{groupId}/streams/{tenantName}/processor
# operationId: createGroupStreamProcessor
export def "atlas-groups-streams-processor createGroupStreamProcessor" [
  groupId: string
  tenantName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/($tenantName)/processor" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-05-30+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-05-30+json" $body
}

# Delete One Stream Processor
#
# DELETE /api/atlas/v2/groups/{groupId}/streams/{tenantName}/processor/{processorName}
# operationId: deleteGroupStreamProcessor
export def "atlas-groups-streams-processor delete" [
  groupId: string
  tenantName: string
  processorName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/($tenantName)/processor/($processorName)" $qp)
  let accept_val = "application/vnd.atlas.2024-05-30+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Stream Processor
#
# GET /api/atlas/v2/groups/{groupId}/streams/{tenantName}/processor/{processorName}
# operationId: getGroupStreamProcessor
export def "atlas-groups-streams-processor get" [
  groupId: string
  tenantName: string
  processorName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/($tenantName)/processor/($processorName)" $qp)
  let accept_val = "application/vnd.atlas.2024-05-30+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Stream Processor
#
# PATCH /api/atlas/v2/groups/{groupId}/streams/{tenantName}/processor/{processorName}
# operationId: updateGroupStreamProcessor
export def "atlas-groups-streams-processor updateGroupStreamProcessor" [
  groupId: string
  tenantName: string
  processorName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/($tenantName)/processor/($processorName)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-05-30+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-05-30+json" $body
}

# Start One Stream Processor
#
# POST /api/atlas/v2/groups/{groupId}/streams/{tenantName}/processor/{processorName}:start
# operationId: startGroupStreamProcessor
export def "atlas-groups-streams-processor startGroupStreamProcessor" [
  groupId: string
  tenantName: string
  processorName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/($tenantName)/processor/($processorName):start" $qp)
  let accept_val = "application/vnd.atlas.2024-05-30+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Start One Stream Processor With Options
#
# POST /api/atlas/v2/groups/{groupId}/streams/{tenantName}/processor/{processorName}:startWith
# operationId: startGroupStreamProcessorWith
export def "atlas-groups-streams-processor startGroupStreamProcessorWith" [
  groupId: string
  tenantName: string
  processorName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-10 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/($tenantName)/processor/($processorName):startWith" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/vnd.atlas.2025-03-12+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2025-03-12+json" $body
}

# Stop One Stream Processor
#
# POST /api/atlas/v2/groups/{groupId}/streams/{tenantName}/processor/{processorName}:stop
# operationId: stopGroupStreamProcessor
export def "atlas-groups-streams-processor stopGroupStreamProcessor" [
  groupId: string
  tenantName: string
  processorName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/($tenantName)/processor/($processorName):stop" $qp)
  let accept_val = "application/vnd.atlas.2024-05-30+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Stream Processors in One Stream Workspace
#
# GET /api/atlas/v2/groups/{groupId}/streams/{tenantName}/processors
# operationId: getGroupStreamProcessors
export def "atlas-groups-streams-processors get" [
  groupId: string
  tenantName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "includeCount" $includeCount "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/($tenantName)/processors" $qp)
  let accept_val = "application/vnd.atlas.2024-05-30+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download Operational Logs for One Atlas Stream Processing Workspace
#
# GET /api/atlas/v2/groups/{groupId}/streams/{tenantName}:downloadOperationalLogs
# operationId: downloadGroupStreamOperationalLogs
export def "atlas-groups-streams downloadGroupStreamOperationalLogs" [
  groupId: string
  tenantName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --endDate: int # Timestamp that specifies the end point for the range of log messages to download.  MongoDB Cloud expresses this timestamp in the number of seconds that have elapsed since the UNIX epoch. (format: int64, e.g. 1636481348)
  --startDate: int # Timestamp that specifies the starting point for the range of log messages to download. MongoDB Cloud expresses this timestamp in the number of seconds that have elapsed since the UNIX epoch. (format: int64, e.g. 1636466948)
  --spName: string # Name of the stream processor to download logs for. An empty string will download logs for all stream processors in the workspace.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "endDate" $endDate "scalar") (serialize-qp "startDate" $startDate "scalar") (serialize-qp "spName" $spName "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams/($tenantName):downloadOperationalLogs" $qp)
  let accept_val = "application/vnd.atlas.2025-03-12+gzip"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Stream Workspace with Sample Connections
#
# POST /api/atlas/v2/groups/{groupId}/streams:withSampleConnections
# operationId: withGroupStreamSampleConnections
export def "atlas-groups-streams-with-sample-connections withGroupStreamSampleConnections" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/streams:withSampleConnections" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-08-05+json" $body
}

# Return All Teams in One Project
#
# GET /api/atlas/v2/groups/{groupId}/teams
# operationId: listGroupTeams
export def "atlas-groups-teams listGroupTeams" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/teams" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add Multiple Teams to One Project
#
# POST /api/atlas/v2/groups/{groupId}/teams
# Docs: https://docs.atlas.mongodb.com/access/manage-teams-in-orgs/#std-label-manage-teams — Manage Organization Teams
# operationId: addGroupTeams
export def "atlas-groups-teams addGroupTeams" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/teams" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Remove One Team from One Project
#
# DELETE /api/atlas/v2/groups/{groupId}/teams/{teamId}
# Docs: https://docs.atlas.mongodb.com/access/manage-teams-in-orgs/#std-label-manage-teams — Manage Organization Teams
# operationId: removeGroupTeam
export def "atlas-groups-teams removeGroupTeam" [
  groupId: string
  teamId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/teams/($teamId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Team in One Project
#
# GET /api/atlas/v2/groups/{groupId}/teams/{teamId}
# operationId: getGroupTeam
export def "atlas-groups-teams get" [
  groupId: string
  teamId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/teams/($teamId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update Team Roles in One Project
#
# PATCH /api/atlas/v2/groups/{groupId}/teams/{teamId}
# Docs: https://docs.atlas.mongodb.com/access/manage-teams-in-orgs/#std-label-manage-teams — Manage Organization Teams
# operationId: updateGroupTeam
export def "atlas-groups-teams updateGroupTeam" [
  groupId: string
  teamId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/teams/($teamId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return LDAP or X.509 Configuration
#
# GET /api/atlas/v2/groups/{groupId}/userSecurity
# operationId: getGroupUserSecurity
export def "atlas-groups-user-security get" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/userSecurity" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update LDAP or X.509 Configuration
#
# PATCH /api/atlas/v2/groups/{groupId}/userSecurity
# operationId: updateGroupUserSecurity
export def "atlas-groups-user-security updateGroupUserSecurity" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/userSecurity" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Disable Customer-Managed X.509
#
# DELETE /api/atlas/v2/groups/{groupId}/userSecurity/customerX509
# operationId: disableGroupUserSecurityCustomerX509
export def "atlas-groups-user-security-customer-x509 disableGroupUserSecurityCustomerX509" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/userSecurity/customerX509" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove LDAP User to DN Mapping
#
# DELETE /api/atlas/v2/groups/{groupId}/userSecurity/ldap/userToDNMapping
# operationId: deleteGroupUserSecurityLdapUserToDnMapping
export def "atlas-groups-user-security-ldap-user-to-dn-mapping delete" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/userSecurity/ldap/userToDNMapping" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Verify LDAP Configuration in One Project
#
# POST /api/atlas/v2/groups/{groupId}/userSecurity/ldap/verify
# operationId: verifyGroupUserSecurityLdap
export def "atlas-groups-user-security-ldap-verify verifyGroupUserSecurityLdap" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/userSecurity/ldap/verify" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return Status of LDAP Configuration Verification in One Project
#
# GET /api/atlas/v2/groups/{groupId}/userSecurity/ldap/verify/{requestId}
# operationId: getGroupUserSecurityLdapVerify
export def "atlas-groups-user-security-ldap-verify get" [
  groupId: string
  requestId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/userSecurity/ldap/verify/($requestId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All MongoDB Cloud Users in One Project
#
# GET /api/atlas/v2/groups/{groupId}/users
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Projects/operation/createProjectInvitation — Deprecated: Invite One MongoDB Cloud User to Join One Project
# operationId: listGroupUsers
@deprecated --flag orgMembershipStatus
export def "atlas-groups-users listGroupUsers" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-11 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --flattenTeams: oneof<nothing, bool> # Flag that indicates whether the returned list should include users who belong to a team with a role in this project. You might not have assigned the individual users a role in this project. If `"flattenTeams" : false`, this resource returns only users with a role in the project.  If `"flattenTeams" : true`, this resource returns both users with roles in the project and users who belong to teams with roles in the project. (default: false)
  --includeOrgUsers: oneof<nothing, bool> # Flag that indicates whether the returned list should include users with implicit access to the project, the Organization Owner or Organization Read Only role. You might not have assigned the individual users a role in this project. If `"includeOrgUsers": false`, this resource returns only users with a role in the project. If `"includeOrgUsers": true`, this resource returns both users with roles in the project and users who have implicit access to the project through their organization role. (default: false)
  --orgMembershipStatus: string # Deprecated: Use `orgMembershipStatuses` instead. Organization membership status to filter users by. Allowed values: `ACTIVE`, `PENDING`, `INVITATION_EXPIRED`, `INVITATION_REJECTED`. If you exclude this parameter, this resource returns ACTIVE and PENDING users. Not supported in deprecated versions. (DEPRECATED, e.g. ACTIVE)
  --orgMembershipStatuses: list # Organization membership status to filter users by. You can supply this parameter multiple times. Allowed values: `ACTIVE`, `PENDING`, `INVITATION_EXPIRED`, `INVITATION_REJECTED`. Replaces the deprecated `orgMembershipStatus` parameter. If you exclude this parameter, this resource returns ACTIVE and PENDING users. Cannot be combined with `orgMembershipStatus`. Not supported in deprecated versions.
  --username: string # Email address to filter users by. Not supported in deprecated versions. (format: email)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "flattenTeams" $flattenTeams "scalar") (serialize-qp "includeOrgUsers" $includeOrgUsers "scalar") (serialize-qp "orgMembershipStatus" $orgMembershipStatus "scalar") (serialize-qp "orgMembershipStatuses" $orgMembershipStatuses "multi") (serialize-qp "username" $username "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/users" $qp)
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add One MongoDB Cloud User to One Project
#
# POST /api/atlas/v2/groups/{groupId}/users
# operationId: addGroupUsers
export def "atlas-groups-users addGroupUsers" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/users" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2025-02-19+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2025-02-19+json" $body
}

# Remove One MongoDB Cloud User from One Project
#
# DELETE /api/atlas/v2/groups/{groupId}/users/{userId}
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Projects/operation/createProjectInvitation — Deprecated: Invite One MongoDB Cloud User to Join One Project
# operationId: removeGroupUser
export def "atlas-groups-users removeGroupUser" [
  groupId: string
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-11 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/users/($userId)" $qp)
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One MongoDB Cloud User in One Project
#
# GET /api/atlas/v2/groups/{groupId}/users/{userId}
# operationId: getGroupUser
export def "atlas-groups-users get" [
  groupId: string
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --orgMembershipStatuses: list # Organization membership status to filter users by. You can supply this parameter multiple times. Allowed values: `ACTIVE`, `PENDING`, `INVITATION_EXPIRED`, `INVITATION_REJECTED`. If you exclude this parameter, this resource returns ACTIVE and PENDING users. Not supported in deprecated versions.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "orgMembershipStatuses" $orgMembershipStatuses "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/users/($userId)" $qp)
  let accept_val = "application/vnd.atlas.2025-02-19+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update Project Roles for One MongoDB Cloud User
#
# PUT /api/atlas/v2/groups/{groupId}/users/{userId}/roles
# DEPRECATED
# operationId: updateGroupUserRoles
@deprecated
export def "atlas-groups-users-roles updateGroupUserRoles" [
  groupId: string
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/users/($userId)/roles" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Add One Project Role to One MongoDB Cloud User
#
# POST /api/atlas/v2/groups/{groupId}/users/{userId}:addRole
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Projects/operation/createProjectInvitation — Deprecated: Invite One MongoDB Cloud User to Join One Project
# operationId: addGroupUserRole
export def "atlas-groups-users addGroupUserRole" [
  groupId: string
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/users/($userId):addRole" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2025-02-19+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2025-02-19+json" $body
}

# Remove One Project Role from One MongoDB Cloud User
#
# POST /api/atlas/v2/groups/{groupId}/users/{userId}:removeRole
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Projects/operation/createProjectInvitation — Deprecated: Invite One MongoDB Cloud User to Join One Project
# operationId: removeGroupUserRole
export def "atlas-groups-users removeGroupUserRole" [
  groupId: string
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId)/users/($userId):removeRole" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2025-02-19+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2025-02-19+json" $body
}

# Migrate One Project to Another Organization
#
# POST /api/atlas/v2/groups/{groupId}:migrate
# operationId: migrateGroup
export def "atlas-groups migrateGroup" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/groups/($groupId):migrate" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-05-30+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-05-30+json" $body
}

# Return All Organizations
#
# GET /api/atlas/v2/orgs
# operationId: listOrgs
export def "atlas-orgs listOrgs" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --name: string # Human-readable label of the organization to use to filter the returned list. Performs a case-insensitive search for an organization that starts with the specified name.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "name" $name "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/api/atlas/v2/orgs" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Organization
#
# POST /api/atlas/v2/orgs
# Docs: https://www.mongodb.com/docs/atlas/billing/#configure-a-paying-organization — Configure a Paying Organization
# operationId: createOrg
export def "atlas-orgs createOrg" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/api/atlas/v2/orgs" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Remove One Organization
#
# DELETE /api/atlas/v2/orgs/{orgId}
# operationId: deleteOrg
export def "atlas-orgs delete" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Organization
#
# GET /api/atlas/v2/orgs/{orgId}
# operationId: getOrg
export def "atlas-orgs get" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Organization
#
# PATCH /api/atlas/v2/orgs/{orgId}
# operationId: updateOrg
export def "atlas-orgs updateOrg" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return Pre-Filtered Activity Feed Link for One Organization
#
# GET /api/atlas/v2/orgs/{orgId}/activityFeed
# operationId: getOrgActivityFeed
export def "atlas-orgs-activity-feed get" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --eventType: list # Category of incident recorded at this moment in time.  **IMPORTANT**: The complete list of event type values changes frequently.
  --maxDate: string # End date and time for events to include in the activity feed link. ISO 8601 timestamp format in UTC. (format: date-time)
  --minDate: string # Start date and time for events to include in the activity feed link. ISO 8601 timestamp format in UTC. (format: date-time)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "eventType" $eventType "multi") (serialize-qp "maxDate" $maxDate "scalar") (serialize-qp "minDate" $minDate "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/activityFeed" $qp)
  let accept_val = "application/vnd.atlas.2025-03-12+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return AI Model API Keys for One Organization
#
# GET /api/atlas/v2/orgs/{orgId}/aiModelApiKeys
# operationId: listOrgAiModelApiKeys
export def "atlas-orgs-ai-model-api-keys listOrgAiModelApiKeys" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/aiModelApiKeys" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Single AI Model API Key for One Organization
#
# GET /api/atlas/v2/orgs/{orgId}/aiModelApiKeys/{apiKeyId}
# operationId: getOrgAiModelApiKey
export def "atlas-orgs-ai-model-api-keys get" [
  orgId: string
  apiKeyId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/aiModelApiKeys/($apiKeyId)" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return AI Model Rate Limits for One Organization
#
# GET /api/atlas/v2/orgs/{orgId}/aiModelRateLimits
# operationId: listOrgAiModelRateLimits
export def "atlas-orgs-ai-model-rate-limits listOrgAiModelRateLimits" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/aiModelRateLimits" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Single AI Model Rate Limit for One Organization
#
# GET /api/atlas/v2/orgs/{orgId}/aiModelRateLimits/{modelGroupName}
# operationId: getOrgAiModelRateLimit
export def "atlas-orgs-ai-model-rate-limits get" [
  orgId: string
  modelGroupName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/aiModelRateLimits/($modelGroupName)" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Organization API Keys
#
# GET /api/atlas/v2/orgs/{orgId}/apiKeys
# Docs: https://docs.atlas.mongodb.com/configure-api-access/#std-label-atlas-prog-api-key — Programmatic API Keys
# operationId: listOrgApiKeys
export def "atlas-orgs-api-keys listOrgApiKeys" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/apiKeys" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Organization API Key
#
# POST /api/atlas/v2/orgs/{orgId}/apiKeys
# Docs: https://docs.atlas.mongodb.com/configure-api-access/#std-label-atlas-prog-api-key — Programmatic API Keys
# operationId: createOrgApiKey
export def "atlas-orgs-api-keys createOrgApiKey" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/apiKeys" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Remove One Organization API Key
#
# DELETE /api/atlas/v2/orgs/{orgId}/apiKeys/{apiUserId}
# Docs: https://docs.atlas.mongodb.com/configure-api-access/#std-label-enable-api-access-list — Configure Atlas API Access
# operationId: deleteOrgApiKey
export def "atlas-orgs-api-keys delete" [
  orgId: string
  apiUserId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/apiKeys/($apiUserId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Organization API Key
#
# GET /api/atlas/v2/orgs/{orgId}/apiKeys/{apiUserId}
# Docs: https://docs.atlas.mongodb.com/configure-api-access/#std-label-atlas-prog-api-key — Programmatic API Keys
# operationId: getOrgApiKey
export def "atlas-orgs-api-keys get" [
  orgId: string
  apiUserId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/apiKeys/($apiUserId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Organization API Key
#
# PATCH /api/atlas/v2/orgs/{orgId}/apiKeys/{apiUserId}
# Docs: https://docs.atlas.mongodb.com/configure-api-access/#std-label-atlas-prog-api-key — Programmatic API Keys
# operationId: updateOrgApiKey
export def "atlas-orgs-api-keys updateOrgApiKey" [
  orgId: string
  apiUserId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/apiKeys/($apiUserId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Access List Entries for One Organization API Key
#
# GET /api/atlas/v2/orgs/{orgId}/apiKeys/{apiUserId}/accessList
# Docs: https://docs.atlas.mongodb.com/configure-api-access/#view-the-details-of-one-api-key-in-one-organization — Programmatic API Keys
# operationId: listOrgApiKeyAccessListEntries
export def "atlas-orgs-api-keys-access-list listOrgApiKeyAccessListEntries" [
  orgId: string
  apiUserId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/apiKeys/($apiUserId)/accessList" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Access List Entry for One Organization API Key
#
# POST /api/atlas/v2/orgs/{orgId}/apiKeys/{apiUserId}/accessList
# operationId: createOrgApiKeyAccessListEntry
export def "atlas-orgs-api-keys-access-list createOrgApiKeyAccessListEntry" [
  orgId: string
  apiUserId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/apiKeys/($apiUserId)/accessList" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Remove One Access List Entry for One Organization API Key
#
# DELETE /api/atlas/v2/orgs/{orgId}/apiKeys/{apiUserId}/accessList/{ipAddress}
# Docs: https://docs.atlas.mongodb.com/configure-api-access/#std-label-enable-api-access-list — Configure Atlas API Access
# operationId: deleteOrgApiKeyAccessListEntry
export def "atlas-orgs-api-keys-access-list delete" [
  orgId: string
  apiUserId: string
  ipAddress: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/apiKeys/($apiUserId)/accessList/($ipAddress)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Access List Entry for One Organization API Key
#
# GET /api/atlas/v2/orgs/{orgId}/apiKeys/{apiUserId}/accessList/{ipAddress}
# Docs: https://docs.atlas.mongodb.com/configure-api-access/#std-label-enable-api-access-list — Configure Atlas API Access
# operationId: getOrgApiKeyAccessListEntry
export def "atlas-orgs-api-keys-access-list get" [
  orgId: string
  ipAddress: string
  apiUserId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/apiKeys/($apiUserId)/accessList/($ipAddress)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Associated Invoices
#
# GET /api/atlas/v2/orgs/{orgId}/associatedInvoices
# operationId: getOrgAssociatedInvoices
export def "atlas-orgs-associated-invoices get" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --month: int # The month for which to retrieve invoices (1-12). Defaults to current month. (format: int64)
  --year: int # The year for which to retrieve invoices. Defaults to current year. (format: int64)
  --includeLinkedOrgs: oneof<nothing, bool> # Whether to include invoices from linked organizations. Defaults to false.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "month" $month "scalar") (serialize-qp "year" $year "scalar") (serialize-qp "includeLinkedOrgs" $includeLinkedOrgs "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/associatedInvoices" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Cost Explorer Query Process
#
# POST /api/atlas/v2/orgs/{orgId}/billing/costExplorer/usage
# operationId: createOrgBillingCostExplorerUsageProcess
export def "atlas-orgs-billing-cost-explorer-usage createOrgBillingCostExplorerUsageProcess" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/billing/costExplorer/usage" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return Usage Details for One Cost Explorer Query
#
# GET /api/atlas/v2/orgs/{orgId}/billing/costExplorer/usage/{token}
# operationId: getOrgBillingCostExplorerUsage
export def "atlas-orgs-billing-cost-explorer-usage get" [
  orgId: string
  token: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-12 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/billing/costExplorer/usage/($token)" $qp)
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+csv")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Events from One Organization
#
# GET /api/atlas/v2/orgs/{orgId}/events
# operationId: listOrgEvents
export def "atlas-orgs-events listOrgEvents" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --eventType: list # Category of incident recorded at this moment in time.  **IMPORTANT**: The complete list of event type values changes frequently.
  --includeRaw: oneof<nothing, bool> # Flag that indicates whether to include the raw document in the output. The raw document contains additional meta information about the event. (default: false)
  --maxDate: string # Date and time from when MongoDB Cloud stops returning events. This parameter uses the ISO 8601 timestamp format in UTC. (format: date-time)
  --minDate: string # Date and time from when MongoDB Cloud starts returning events. This parameter uses the ISO 8601 timestamp format in UTC. (format: date-time)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "eventType" $eventType "multi") (serialize-qp "includeRaw" $includeRaw "scalar") (serialize-qp "maxDate" $maxDate "scalar") (serialize-qp "minDate" $minDate "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/events" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Event from One Organization
#
# GET /api/atlas/v2/orgs/{orgId}/events/{eventId}
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Events/operation/listOrganizationEvents — Return Events from One Organization
# operationId: getOrgEvent
export def "atlas-orgs-events get" [
  orgId: string
  eventId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --includeRaw: oneof<nothing, bool> # Flag that indicates whether to include the raw document in the output. The raw document contains additional meta information about the event. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "includeRaw" $includeRaw "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/events/($eventId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Federation Settings for One Organization
#
# GET /api/atlas/v2/orgs/{orgId}/federationSettings
# operationId: getOrgFederationSettings
export def "atlas-orgs-federation-settings get" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/federationSettings" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Projects in One Organization
#
# GET /api/atlas/v2/orgs/{orgId}/groups
# operationId: getOrgGroups
export def "atlas-orgs-groups get" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --name: string # Human-readable label of the project to use to filter the returned list. Performs a case-insensitive search for a project within the organization which is prefixed by the specified name.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "name" $name "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/groups" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Invitations in One Organization
#
# GET /api/atlas/v2/orgs/{orgId}/invites
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/MongoDB-Cloud-Users/operation/listOrganizationUsers — Return All MongoDB Cloud Users in One Organization
# operationId: listOrgInvites
@deprecated
export def "atlas-orgs-invites listOrgInvites" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --username: string # Email address of the user account invited to this organization. If you exclude this parameter, this resource returns all pending invitations. (format: email)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "username" $username "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/invites" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Invitation in One Organization
#
# PATCH /api/atlas/v2/orgs/{orgId}/invites
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/MongoDB-Cloud-Users/operation/updateOrganizationUser — Update One MongoDB Cloud User in One Organization
# operationId: updateOrgInvites
@deprecated
export def "atlas-orgs-invites updateOrgInvites" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/invites" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Create Invitation for One MongoDB Cloud User in One Organization
#
# POST /api/atlas/v2/orgs/{orgId}/invites
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/MongoDB-Cloud-Users/operation/createOrganizationUser — Add One MongoDB Cloud User to One
# operationId: createOrgInvite
@deprecated
export def "atlas-orgs-invites createOrgInvite" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/invites" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Remove One Invitation from One Organization
#
# DELETE /api/atlas/v2/orgs/{orgId}/invites/{invitationId}
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/MongoDB-Cloud-Users/operation/removeOrganizationUser — Remove One MongoDB Cloud User in One Organization
# operationId: deleteOrgInvite
@deprecated
export def "atlas-orgs-invites delete" [
  orgId: string
  invitationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/invites/($invitationId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Invitation in One Organization by Invitation ID
#
# GET /api/atlas/v2/orgs/{orgId}/invites/{invitationId}
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/MongoDB-Cloud-Users/operation/getOrganizationUser — Return One MongoDB Cloud User in One Organization
# operationId: getOrgInvite
@deprecated
export def "atlas-orgs-invites get" [
  orgId: string
  invitationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/invites/($invitationId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Invitation in One Organization by Invitation ID
#
# PATCH /api/atlas/v2/orgs/{orgId}/invites/{invitationId}
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/MongoDB-Cloud-Users/operation/updateOrganizationUser — Update One MongoDB Cloud User in One Organization
# operationId: updateOrgInviteById
@deprecated
export def "atlas-orgs-invites updateOrgInviteById" [
  orgId: string
  invitationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/invites/($invitationId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Invoices for One Organization
#
# GET /api/atlas/v2/orgs/{orgId}/invoices
# operationId: listOrgInvoices
export def "atlas-orgs-invoices listOrgInvoices" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --viewLinkedInvoices: oneof<nothing, bool> # Flag that indicates whether to return linked invoices in the `linkedInvoices` field. (default: true)
  --statusNames: list # Statuses of the invoice to be retrieved. Omit to return invoices of all statuses.
  --fromDate: string # Retrieve the invoices the `startDates` of which are greater than or equal to the `fromDate`. If omit, the invoices return will go back to earliest `startDate`. (format: date, e.g. 2023-01-01)
  --toDate: string # Retrieve the invoices the `endDates` of which are smaller than or equal to the `toDate`. If omit, the invoices return will go further to latest `endDate`. (format: date, e.g. 2023-01-01)
  --sortBy: string@sortBy-completer # Field used to sort the returned invoices by. Use in combination with `orderBy` parameter to control the order of the result. (default: END_DATE)
  --orderBy: string@orderBy-completer # Field used to order the returned invoices by. Use in combination of `sortBy` parameter to control the order of the result. (default: desc, e.g. desc)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "viewLinkedInvoices" $viewLinkedInvoices "scalar") (serialize-qp "statusNames" $statusNames "multi") (serialize-qp "fromDate" $fromDate "scalar") (serialize-qp "toDate" $toDate "scalar") (serialize-qp "sortBy" $sortBy "scalar") (serialize-qp "orderBy" $orderBy "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/invoices" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Pending Invoices for One Organization
#
# GET /api/atlas/v2/orgs/{orgId}/invoices/pending
# operationId: listOrgInvoicePending
export def "atlas-orgs-invoices-pending listOrgInvoicePending" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/invoices/pending" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Invoice for One Organization
#
# GET /api/atlas/v2/orgs/{orgId}/invoices/{invoiceId}
# operationId: getOrgInvoice
export def "atlas-orgs-invoices get" [
  orgId: string
  invoiceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-12 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/invoices/($invoiceId)" $qp)
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+csv")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Invoice as CSV for One Organization
#
# GET /api/atlas/v2/orgs/{orgId}/invoices/{invoiceId}/csv
# operationId: getOrgInvoiceCsv
export def "atlas-orgs-invoices-csv get" [
  orgId: string
  invoiceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/invoices/($invoiceId)/csv" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+csv"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Line Items for One Invoice by Invoice ID
#
# GET /api/atlas/v2/orgs/{orgId}/invoices/{invoiceId}/lineItems:search
# operationId: searchOrgInvoiceLineItems
export def "atlas-orgs-invoices-line-items-search searchOrgInvoiceLineItems" [
  orgId: string
  invoiceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/invoices/($invoiceId)/lineItems:search" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-08-05+json" $body
}

# Generate and Download Invoice Report
#
# POST /api/atlas/v2/orgs/{orgId}/invoices/{invoiceId}:generateAndDownloadReport
# operationId: generateOrgInvoiceAndDownloadReport
export def "atlas-orgs-invoices generateOrgInvoiceAndDownloadReport" [
  orgId: string
  invoiceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/invoices/($invoiceId):generateAndDownloadReport")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.preview+gzip"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.preview+json" $body
}

# Return All Projects Available for Migration
#
# GET /api/atlas/v2/orgs/{orgId}/liveMigrations/availableProjects
# operationId: listOrgLiveMigrationAvailableProjects
export def "atlas-orgs-live-migrations-available-projects listOrgLiveMigrationAvailableProjects" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/liveMigrations/availableProjects" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove One Link-Token
#
# DELETE /api/atlas/v2/orgs/{orgId}/liveMigrations/linkTokens
# operationId: deleteOrgLiveMigrationLinkTokens
export def "atlas-orgs-live-migrations-link-tokens delete" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/liveMigrations/linkTokens" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Link-Token
#
# POST /api/atlas/v2/orgs/{orgId}/liveMigrations/linkTokens
# operationId: createOrgLiveMigrationLinkToken
export def "atlas-orgs-live-migrations-link-tokens createOrgLiveMigrationLinkToken" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/liveMigrations/linkTokens" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Non-Compliant Resources
#
# GET /api/atlas/v2/orgs/{orgId}/nonCompliantResources
# Docs: https://dochub.mongodb.org/core/atlas-resource-policies — Atlas Resource Policies
# operationId: getOrgNonCompliantResources
export def "atlas-orgs-non-compliant-resources get" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/nonCompliantResources" $qp)
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Atlas Resource Policies
#
# GET /api/atlas/v2/orgs/{orgId}/resourcePolicies
# Docs: https://dochub.mongodb.org/core/atlas-resource-policies — Atlas Resource Policies
# operationId: listOrgResourcePolicies
export def "atlas-orgs-resource-policies listOrgResourcePolicies" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/resourcePolicies" $qp)
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Atlas Resource Policy
#
# POST /api/atlas/v2/orgs/{orgId}/resourcePolicies
# Docs: https://dochub.mongodb.org/core/atlas-resource-policies — Atlas Resource Policies
# operationId: createOrgResourcePolicy
export def "atlas-orgs-resource-policies createOrgResourcePolicy" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/resourcePolicies" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-08-05+json" $body
}

# Delete One Atlas Resource Policy
#
# DELETE /api/atlas/v2/orgs/{orgId}/resourcePolicies/{resourcePolicyId}
# Docs: https://dochub.mongodb.org/core/atlas-resource-policies — Atlas Resource Policies
# operationId: deleteOrgResourcePolicy
export def "atlas-orgs-resource-policies delete" [
  orgId: string
  resourcePolicyId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/resourcePolicies/($resourcePolicyId)" $qp)
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Atlas Resource Policy
#
# GET /api/atlas/v2/orgs/{orgId}/resourcePolicies/{resourcePolicyId}
# Docs: https://dochub.mongodb.org/core/atlas-resource-policies — Atlas Resource Policies
# operationId: getOrgResourcePolicy
export def "atlas-orgs-resource-policies get" [
  orgId: string
  resourcePolicyId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/resourcePolicies/($resourcePolicyId)" $qp)
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Atlas Resource Policy
#
# PATCH /api/atlas/v2/orgs/{orgId}/resourcePolicies/{resourcePolicyId}
# Docs: https://dochub.mongodb.org/core/atlas-resource-policies — Atlas Resource Policies
# operationId: updateOrgResourcePolicy
export def "atlas-orgs-resource-policies updateOrgResourcePolicy" [
  orgId: string
  resourcePolicyId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/resourcePolicies/($resourcePolicyId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-08-05+json" $body
}

# Validate One Atlas Resource Policy
#
# POST /api/atlas/v2/orgs/{orgId}/resourcePolicies:validate
# Docs: https://dochub.mongodb.org/core/atlas-resource-policies — Atlas Resource Policies
# operationId: validateOrgResourcePolicies
export def "atlas-orgs-resource-policies-validate validateOrgResourcePolicies" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/resourcePolicies:validate" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-08-05+json" $body
}

# Return Sandbox Configuration IDs for an Organization
#
# GET /api/atlas/v2/orgs/{orgId}/sandboxConfig
# operationId: listOrgSandboxConfig
export def "atlas-orgs-sandbox-config listOrgSandboxConfig" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/sandboxConfig" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Sandbox Configuration for an Organization
#
# POST /api/atlas/v2/orgs/{orgId}/sandboxConfig
# operationId: createOrgSandboxConfig
export def "atlas-orgs-sandbox-config createOrgSandboxConfig" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/sandboxConfig" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.preview+json" $body
}

# Delete the Default Sandbox Configuration and Disable Sandbox
#
# DELETE /api/atlas/v2/orgs/{orgId}/sandboxConfig/{sandboxConfigId}
# operationId: deleteOrgSandboxConfig
export def "atlas-orgs-sandbox-config delete" [
  orgId: string
  sandboxConfigId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/sandboxConfig/($sandboxConfigId)")
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Default Sandbox Configuration for One Organization
#
# GET /api/atlas/v2/orgs/{orgId}/sandboxConfig/{sandboxConfigId}
# operationId: getOrgSandboxConfig
export def "atlas-orgs-sandbox-config get" [
  orgId: string
  sandboxConfigId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/sandboxConfig/($sandboxConfigId)" $qp)
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update an Existing Sandbox Configuration
#
# PATCH /api/atlas/v2/orgs/{orgId}/sandboxConfig/{sandboxConfigId}
# operationId: updateOrgSandboxConfig
export def "atlas-orgs-sandbox-config updateOrgSandboxConfig" [
  orgId: string
  sandboxConfigId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/sandboxConfig/($sandboxConfigId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.preview+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.preview+json" $body
}

# Return All Organization Service Accounts
#
# GET /api/atlas/v2/orgs/{orgId}/serviceAccounts
# operationId: listOrgServiceAccounts
export def "atlas-orgs-service-accounts listOrgServiceAccounts" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/serviceAccounts" $qp)
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Organization Service Account
#
# POST /api/atlas/v2/orgs/{orgId}/serviceAccounts
# operationId: createOrgServiceAccount
export def "atlas-orgs-service-accounts createOrgServiceAccount" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/serviceAccounts" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-08-05+json" $body
}

# Delete One Organization Service Account
#
# DELETE /api/atlas/v2/orgs/{orgId}/serviceAccounts/{clientId}
# operationId: deleteOrgServiceAccount
export def "atlas-orgs-service-accounts delete" [
  clientId: string
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/serviceAccounts/($clientId)" $qp)
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Organization Service Account
#
# GET /api/atlas/v2/orgs/{orgId}/serviceAccounts/{clientId}
# operationId: getOrgServiceAccount
export def "atlas-orgs-service-accounts get" [
  orgId: string
  clientId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/serviceAccounts/($clientId)" $qp)
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One Organization Service Account
#
# PATCH /api/atlas/v2/orgs/{orgId}/serviceAccounts/{clientId}
# operationId: updateOrgServiceAccount
export def "atlas-orgs-service-accounts updateOrgServiceAccount" [
  clientId: string
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/serviceAccounts/($clientId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-08-05+json" $body
}

# Return All Access List Entries for One Organization Service Account
#
# GET /api/atlas/v2/orgs/{orgId}/serviceAccounts/{clientId}/accessList
# operationId: listOrgServiceAccountAccessList
export def "atlas-orgs-service-accounts-access-list listOrgServiceAccountAccessList" [
  orgId: string
  clientId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/serviceAccounts/($clientId)/accessList" $qp)
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add Access List Entries for One Organization Service Account
#
# POST /api/atlas/v2/orgs/{orgId}/serviceAccounts/{clientId}/accessList
# operationId: createOrgServiceAccountAccessList
export def "atlas-orgs-service-accounts-access-list createOrgServiceAccountAccessList" [
  orgId: string
  clientId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/serviceAccounts/($clientId)/accessList" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-08-05+json" $body
}

# Remove One Access List Entry from One Organization Service Account
#
# DELETE /api/atlas/v2/orgs/{orgId}/serviceAccounts/{clientId}/accessList/{ipAddress}
# operationId: deleteOrgServiceAccountAccessListEntry
export def "atlas-orgs-service-accounts-access-list delete" [
  orgId: string
  clientId: string
  ipAddress: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/serviceAccounts/($clientId)/accessList/($ipAddress)" $qp)
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Service Account Project Assignments
#
# GET /api/atlas/v2/orgs/{orgId}/serviceAccounts/{clientId}/groups
# operationId: getOrgServiceAccountGroups
export def "atlas-orgs-service-accounts-groups get" [
  orgId: string
  clientId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/serviceAccounts/($clientId)/groups" $qp)
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Organization Service Account Secret
#
# POST /api/atlas/v2/orgs/{orgId}/serviceAccounts/{clientId}/secrets
# operationId: createOrgServiceAccountSecret
export def "atlas-orgs-service-accounts-secrets createOrgServiceAccountSecret" [
  orgId: string
  clientId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/serviceAccounts/($clientId)/secrets" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2024-08-05+json" $body
}

# Delete One Organization Service Account Secret
#
# DELETE /api/atlas/v2/orgs/{orgId}/serviceAccounts/{clientId}/secrets/{secretId}
# operationId: deleteOrgServiceAccountSecret
export def "atlas-orgs-service-accounts-secrets delete" [
  clientId: string
  secretId: string
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/serviceAccounts/($clientId)/secrets/($secretId)" $qp)
  let accept_val = "application/vnd.atlas.2024-08-05+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return Settings for One Organization
#
# GET /api/atlas/v2/orgs/{orgId}/settings
# operationId: getOrgSettings
export def "atlas-orgs-settings get" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/settings" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update Settings for One Organization
#
# PATCH /api/atlas/v2/orgs/{orgId}/settings
# operationId: updateOrgSettings
export def "atlas-orgs-settings updateOrgSettings" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/settings" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All Teams in One Organization
#
# GET /api/atlas/v2/orgs/{orgId}/teams
# Docs: https://docs.atlas.mongodb.com/access/manage-teams-in-orgs/#std-label-manage-teams — Manage Organization Teams
# operationId: listOrgTeams
export def "atlas-orgs-teams listOrgTeams" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/teams" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One Team in One Organization
#
# POST /api/atlas/v2/orgs/{orgId}/teams
# Docs: https://docs.atlas.mongodb.com/access/manage-teams-in-orgs/#std-label-manage-teams — Manage Organization Teams
# operationId: createOrgTeam
export def "atlas-orgs-teams createOrgTeam" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/teams" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return One Team by Name
#
# GET /api/atlas/v2/orgs/{orgId}/teams/byName/{teamName}
# Docs: https://docs.atlas.mongodb.com/access/manage-teams-in-orgs/#std-label-manage-teams — Manage Organization Teams
# operationId: getOrgTeamByName
export def "atlas-orgs-teams-by-name get" [
  orgId: string
  teamName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/teams/byName/($teamName)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove One Team from One Organization
#
# DELETE /api/atlas/v2/orgs/{orgId}/teams/{teamId}
# Docs: https://docs.atlas.mongodb.com/access/manage-teams-in-orgs/#std-label-manage-teams — Manage Organization Teams
# operationId: deleteOrgTeam
export def "atlas-orgs-teams delete" [
  orgId: string
  teamId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/teams/($teamId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Team by ID
#
# GET /api/atlas/v2/orgs/{orgId}/teams/{teamId}
# Docs: https://docs.atlas.mongodb.com/access/manage-teams-in-orgs/#std-label-manage-teams — Manage Organization Teams
# operationId: getOrgTeam
export def "atlas-orgs-teams get" [
  orgId: string
  teamId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/teams/($teamId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Rename One Team
#
# PATCH /api/atlas/v2/orgs/{orgId}/teams/{teamId}
# Docs: https://docs.atlas.mongodb.com/access/manage-teams-in-orgs/#std-label-manage-teams — Manage Organization Teams
# operationId: renameOrgTeam
export def "atlas-orgs-teams renameOrgTeam" [
  orgId: string
  teamId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/teams/($teamId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return All MongoDB Cloud Users Assigned to One Team
#
# GET /api/atlas/v2/orgs/{orgId}/teams/{teamId}/users
# Docs: https://docs.atlas.mongodb.com/access/manage-teams-in-orgs/#std-label-manage-teams — Manage Organization Teams
# operationId: listOrgTeamUsers
@deprecated --flag orgMembershipStatus
export def "atlas-orgs-teams-users listOrgTeamUsers" [
  orgId: string
  teamId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-11 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --username: string # Email address to filter users by. Not supported in deprecated versions. (format: email)
  --orgMembershipStatus: string # Deprecated: Use `orgMembershipStatuses` instead. Organization membership status to filter users by. Allowed values: `ACTIVE`, `PENDING`, `INVITATION_EXPIRED`, `INVITATION_REJECTED`. If you exclude this parameter, this resource returns ACTIVE and PENDING users. Not supported in deprecated versions. (DEPRECATED, e.g. ACTIVE)
  --orgMembershipStatuses: list # Organization membership status to filter users by. You can supply this parameter multiple times. Allowed values: `ACTIVE`, `PENDING`, `INVITATION_EXPIRED`, `INVITATION_REJECTED`. Replaces the deprecated `orgMembershipStatus` parameter. If you exclude this parameter, this resource returns ACTIVE and PENDING users. Cannot be combined with `orgMembershipStatus`. Not supported in deprecated versions.
  --userId: string # Unique 24-hexadecimal digit string to filter users by. Not supported in deprecated versions.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "username" $username "scalar") (serialize-qp "orgMembershipStatus" $orgMembershipStatus "scalar") (serialize-qp "orgMembershipStatuses" $orgMembershipStatuses "multi") (serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/teams/($teamId)/users" $qp)
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Assign MongoDB Cloud Users in One Organization to One Team
#
# POST /api/atlas/v2/orgs/{orgId}/teams/{teamId}/users
# DEPRECATED
# Docs: https://docs.atlas.mongodb.com/access/manage-teams-in-orgs/#std-label-manage-teams — Manage Organization Teams
# operationId: addOrgTeamUsers
@deprecated
export def "atlas-orgs-teams-users addOrgTeamUsers" [
  orgId: string
  teamId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/teams/($teamId)/users" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Remove One MongoDB Cloud User from One Team
#
# DELETE /api/atlas/v2/orgs/{orgId}/teams/{teamId}/users/{userId}
# DEPRECATED
# Docs: https://docs.atlas.mongodb.com/access/manage-teams-in-orgs/#std-label-manage-teams — Manage Organization Teams
# operationId: removeOrgTeamUserFromTeam
@deprecated
export def "atlas-orgs-teams-users removeOrgTeamUserFromTeam" [
  orgId: string
  teamId: string
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/teams/($teamId)/users/($userId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add One MongoDB Cloud User to One Team
#
# POST /api/atlas/v2/orgs/{orgId}/teams/{teamId}:addUser
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Projects/operation/updateProjectInvitation — Deprecated: Invite One MongoDB Cloud User to Join One Project endpoint
# operationId: addOrgTeamUser
export def "atlas-orgs-teams addOrgTeamUser" [
  orgId: string
  teamId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/teams/($teamId):addUser" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2025-02-19+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2025-02-19+json" $body
}

# Remove One MongoDB Cloud User from One Team
#
# POST /api/atlas/v2/orgs/{orgId}/teams/{teamId}:removeUser
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Projects/operation/createProjectInvitation — Deprecated: Invite One MongoDB Cloud User to Join One Project
# operationId: removeOrgTeamUser
export def "atlas-orgs-teams removeOrgTeamUser" [
  orgId: string
  teamId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/teams/($teamId):removeUser" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2025-02-19+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2025-02-19+json" $body
}

# Return All MongoDB Cloud Users in One Organization
#
# GET /api/atlas/v2/orgs/{orgId}/users
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Projects/operation/createProjectInvitation — Deprecated: Invite One MongoDB Cloud User to Join One Project
# operationId: listOrgUsers
@deprecated --flag orgMembershipStatus
export def "atlas-orgs-users listOrgUsers" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-11 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --username: string # Email address to filter users by. Not supported in deprecated versions. (format: email)
  --orgMembershipStatus: string # Deprecated: Use `orgMembershipStatuses` instead. Organization membership status to filter users by. Allowed values: `ACTIVE`, `PENDING`, `INVITATION_EXPIRED`, `INVITATION_REJECTED`. If you exclude this parameter, this resource returns ACTIVE and PENDING users. Not supported in deprecated versions. (DEPRECATED, e.g. ACTIVE)
  --orgMembershipStatuses: list # Organization membership status to filter users by. You can supply this parameter multiple times. Allowed values: `ACTIVE`, `PENDING`, `INVITATION_EXPIRED`, `INVITATION_REJECTED`. Replaces the deprecated `orgMembershipStatus` parameter. If you exclude this parameter, this resource returns ACTIVE and PENDING users. Cannot be combined with `orgMembershipStatus`. Not supported in deprecated versions.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "username" $username "scalar") (serialize-qp "orgMembershipStatus" $orgMembershipStatus "scalar") (serialize-qp "orgMembershipStatuses" $orgMembershipStatuses "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/users" $qp)
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add One MongoDB Cloud User to One Organization
#
# POST /api/atlas/v2/orgs/{orgId}/users
# operationId: createOrgUser
export def "atlas-orgs-users createOrgUser" [
  orgId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/users" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2025-02-19+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2025-02-19+json" $body
}

# Remove One MongoDB Cloud User from One Organization
#
# DELETE /api/atlas/v2/orgs/{orgId}/users/{userId}
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Projects/operation/createProjectInvitation — Deprecated: Invite One MongoDB Cloud User to Join One Project
# operationId: removeOrgUser
export def "atlas-orgs-users removeOrgUser" [
  orgId: string
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-11 # Response content type
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/users/($userId)" $qp)
  let accept_val = ($accept | default "application/vnd.atlas.2023-01-01+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One MongoDB Cloud User in One Organization
#
# GET /api/atlas/v2/orgs/{orgId}/users/{userId}
# operationId: getOrgUser
export def "atlas-orgs-users get" [
  orgId: string
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --orgMembershipStatuses: list # Organization membership status to filter users by. You can supply this parameter multiple times. Allowed values: `ACTIVE`, `PENDING`, `INVITATION_EXPIRED`, `INVITATION_REJECTED`. If you exclude this parameter, this resource returns ACTIVE and PENDING users. Not supported in deprecated versions.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "orgMembershipStatuses" $orgMembershipStatuses "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/users/($userId)" $qp)
  let accept_val = "application/vnd.atlas.2025-02-19+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update One MongoDB Cloud User in One Organization
#
# PATCH /api/atlas/v2/orgs/{orgId}/users/{userId}
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Projects/operation/createProjectInvitation — Deprecated: Invite One MongoDB Cloud User to Join One Project
# operationId: updateOrgUser
export def "atlas-orgs-users updateOrgUser" [
  orgId: string
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/users/($userId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2025-02-19+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2025-02-19+json" $body
}

# Update Organization Roles for One MongoDB Cloud User
#
# PUT /api/atlas/v2/orgs/{orgId}/users/{userId}/roles
# DEPRECATED
# operationId: updateOrgUserRoles
@deprecated
export def "atlas-orgs-users-roles updateOrgUserRoles" [
  orgId: string
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/users/($userId)/roles" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Add One Organization Role to One MongoDB Cloud User
#
# POST /api/atlas/v2/orgs/{orgId}/users/{userId}:addRole
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Projects/operation/createProjectInvitation — Deprecated: Invite One MongoDB Cloud User to Join One Project
# operationId: addOrgUserRole
export def "atlas-orgs-users addOrgUserRole" [
  orgId: string
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/users/($userId):addRole" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2025-02-19+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2025-02-19+json" $body
}

# Remove One Organization Role from One MongoDB Cloud User
#
# POST /api/atlas/v2/orgs/{orgId}/users/{userId}:removeRole
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Projects/operation/createProjectInvitation — Invite One MongoDB Cloud User to Join One Project
# operationId: removeOrgUserRole
export def "atlas-orgs-users removeOrgUserRole" [
  orgId: string
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/orgs/($orgId)/users/($userId):removeRole" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2025-02-19+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2025-02-19+json" $body
}

# Return All Rate Limits
#
# GET /api/atlas/v2/rateLimits
# operationId: listRateLimits
export def "atlas-rate-limits listRateLimits" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-10 # Response content type
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --groupId: string # Unique 24-hexadecimal digit string that identifies the Atlas Project to request rate limits for. When this parameter is provided, only group scoped endpoint sets are returned and the limits returned are applicable to the specified project. The requesting user must have the Project Read Only role for the specified project. (e.g. 32b6e34b3d91647abb20e7b8)
  --orgId: string # Unique 24-hexadecimal digit string that identifies the Atlas Organization to request rate limits for. When this parameter is provided, only organization scoped endpoint sets are returned and the limits returned are applicable to the specified organization. The requesting user must have the Organization Read Only role for the specified organization. (e.g. 32b6e34b3d91647abb20e7b8)
  --userId: string # A string that identifies the Atlas user to request rate limits for. The ID can for example be the Service Account Client ID or the API Public Key. When this parameter is provided, only user scoped endpoint sets are returned and the limits returned are applicable to the specified user. The requesting user must be the same as the specified user. (e.g. mdb_sa_id_1234567890abcdef12345678)
  --ipAddress: string # An IP address to request rate limits for. When this parameter is provided, only IP scoped endpoint sets are returned and the limits returned are applicable to the specified IP address. The requesting user must have the same IP address as the one provided in the request. (e.g. 127.0.0.1)
  --name: string # Filters the returned endpoint sets by the provided endpoint set name. Multiple names may be provided, for example `/rateLimits?name=Name1&name=Name2`. For names that use spaces, replace the space with its URL-encoded value (`%20`). (e.g. Rate Limits Inspection)
  --endpointPath: string # Filters the returned endpoint sets by the provided endpoint path. Multiple paths may be provided, for example `/rateLimits?endpointPath=%2Fapi%2Fatlas%2Fv2%2Fclusters&endpointPath=%2Fapi%2Fatlas%2Fv2%2Fgroups%2F%7BgroupId%7D%2F`. Replace `/`, `{` and `}` with their URL-encoded values (`%2F`, `%7B` and `%7D` respectively). (e.g. /api/atlas/v2/clusters)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "envelope" $envelope "scalar") (serialize-qp "groupId" $groupId "scalar") (serialize-qp "orgId" $orgId "scalar") (serialize-qp "userId" $userId "scalar") (serialize-qp "ipAddress" $ipAddress "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "endpointPath" $endpointPath "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/api/atlas/v2/rateLimits" $qp)
  let accept_val = ($accept | default "application/vnd.atlas.2025-03-12+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Rate Limit
#
# GET /api/atlas/v2/rateLimits/{endpointSetId}
# operationId: getRateLimit
export def "atlas-rate-limits get" [
  endpointSetId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-10 # Response content type
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --groupId: string # Unique 24-hexadecimal digit string that identifies the Atlas Project to request rate limits for. When this parameter is provided, the limits returned are applicable to the specified project. The requesting user must have the Project Read Only role for the specified project. (e.g. 32b6e34b3d91647abb20e7b8)
  --orgId: string # Unique 24-hexadecimal digit string that identifies the Atlas Organization to request rate limits for. When this parameter is provided, the limits returned are applicable to the specified organization. The requesting user must have the Organization Read Only role for the specified organization. (e.g. 32b6e34b3d91647abb20e7b8)
  --userId: string # A string that identifies the Atlas user to request rate limits for. The ID can for example be the Service Account Client ID or the API Public Key. When this parameter is provided, the limits returned are applicable to the specified  user. The requesting user must be the same as the specified user. (e.g. mdb_sa_id_1234567890abcdef12345678)
  --ipAddress: string # An IP address to request rate limits for. When this parameter is provided, the limits returned are applicable to the specified IP address. The requesting user must have the same IP address as the one provided in the request. (e.g. 127.0.0.1)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "envelope" $envelope "scalar") (serialize-qp "groupId" $groupId "scalar") (serialize-qp "orgId" $orgId "scalar") (serialize-qp "userId" $userId "scalar") (serialize-qp "ipAddress" $ipAddress "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/rateLimits/($endpointSetId)" $qp)
  let accept_val = ($accept | default "application/vnd.atlas.2025-03-12+json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Stock Keeping Units
#
# GET /api/atlas/v2/skus
# operationId: listSkus
export def "atlas-skus listSkus" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --includeCount: oneof<nothing, bool> # Flag that indicates whether the response returns the total number of items (`totalCount`) in the response. (default: true)
  --itemsPerPage: int # Number of items that the response returns per page. (default: 100)
  --pageNum: int # Number of the page that displays the current set of the total objects that the response returns. (default: 1)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "includeCount" $includeCount "scalar") (serialize-qp "itemsPerPage" $itemsPerPage "scalar") (serialize-qp "pageNum" $pageNum "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/api/atlas/v2/skus" $qp)
  let accept_val = "application/vnd.atlas.2025-03-12+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One Stock Keeping Unit
#
# GET /api/atlas/v2/skus/{skuId}
# operationId: getSku
export def "atlas-skus get" [
  skuId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/skus/($skuId)" $qp)
  let accept_val = "application/vnd.atlas.2025-03-12+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return All Control Plane IP Addresses
#
# GET /api/atlas/v2/unauth/controlPlaneIPAddresses
# operationId: listControlPlaneIpAddresses
export def "atlas-unauth-control-plane-ip-addresses listControlPlaneIpAddresses" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/api/atlas/v2/unauth/controlPlaneIPAddresses" $qp)
  let accept_val = "application/vnd.atlas.2023-11-15+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create One MongoDB Cloud User
#
# POST /api/atlas/v2/users
# DEPRECATED
# operationId: createUser
@deprecated
export def "atlas-users createUser" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/api/atlas/v2/users" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/vnd.atlas.2023-01-01+json" $body
}

# Return One MongoDB Cloud User by Username
#
# GET /api/atlas/v2/users/byName/{userName}
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Programmatic-API-Keys/operation/getApiKey — Return One Organization API Key
# operationId: getUserByName
@deprecated
export def "atlas-users-by-name get" [
  userName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/users/byName/($userName)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return One MongoDB Cloud User by ID
#
# GET /api/atlas/v2/users/{userId}
# DEPRECATED
# Docs: https://www.mongodb.com/docs/atlas/reference/api-resources-spec/v2/#tag/Programmatic-API-Keys/operation/getApiKey — Return One Organization API Key
# operationId: getUser
@deprecated
export def "atlas-users get" [
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --envelope: oneof<nothing, bool> # Flag that indicates whether Application wraps the response in an `envelope` JSON object. Some API clients cannot access the HTTP response headers or status code. To remediate this, set envelope=true in the query. Endpoints that return a list of results use the results object as an envelope. Application adds the status parameter to the response body. (default: false)
  --pretty: oneof<nothing, bool> # Flag that indicates whether the response body should be in the prettyprint format. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "envelope" $envelope "scalar") (serialize-qp "pretty" $pretty "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/api/atlas/v2/users/($userId)" $qp)
  let accept_val = "application/vnd.atlas.2023-01-01+json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}
