# Auto-generated client for OpenSearch API Specification v0.2.0
# Source: https://github.com/opensearch-project/opensearch-api-specification/releases/latest/download/opensearch-openapi.yaml
# Auth: --token flag or $env.OPENSEARCH_API_SPECIFICATION_TOKEN

const BASE_URL = "http://localhost"
const DEFAULT_AUTH = "bearer"

# Build auth: returns {headers: record, query: string}
def build-auth [token?: string, auth_scheme?: string]: nothing -> record {
  let token_val = if ($token != null) and ($token | is-not-empty) { $token } else { $env | get -o OPENSEARCH_API_SPECIFICATION_TOKEN | default "" }
  let scheme = ($auth_scheme | default "bearer")
  if ($scheme == "none") or ($token_val | is-empty) { return {headers: {}, query: ""} }
  match $scheme {
    "none" => { {headers: {}, query: ""} }
    _ => { {headers: {Authorization: $"Bearer ($token_val)"}, query: ""} }
  }
}

# Serialize a single query parameter based on collection style
def serialize-qp [name: string, value: any, style: string]: nothing -> list<string> {
  if ($value == null) { return [] }
  let n = ($name | url encode)
  let is_list = ($value | describe | str starts-with "list")
  if ($value | describe | str starts-with "record") { return ($value | transpose k v | each { $"($n)[($in.k | into string | url encode)]=($in.v | into string | url encode)" }) }
  if not $is_list { return [$"($n)=($value | into string | url encode)"] }
  match $style {
    "multi" => { $value | each {|v| $"($n)=($v | into string | url encode)" } }
    "csv" => { let joined = ($value | each { $in | into string | url encode } | str join ","); [$"($n)=($joined)"] }
    "ssv" => { let joined = ($value | each { $in | into string | url encode } | str join "%20"); [$"($n)=($joined)"] }
    "tsv" => { let joined = ($value | each { $in | into string | url encode } | str join "%09"); [$"($n)=($joined)"] }
    "pipes" => { let joined = ($value | each { $in | into string | url encode } | str join "|"); [$"($n)=($joined)"] }
    "deepObject" => { $value | each {|v| $"($n)[]=($v | into string | url encode)" } }
    _ => { $value | each {|v| $"($n)=($v | into string | url encode)" } }
  }
}

# Build URL from base, path, and optional query string
def build-url [base: string, path: string, query?: string]: nothing -> string {
  let parsed = ($base | url parse | reject params)
  let full_path = if ($path | is-empty) { $parsed.path } else { [$parsed.path $path] | str join "/" | str replace --all --regex '/+' '/' }
  let result = ($parsed | upsert path $full_path)
  if ($query != null) and ($query | is-not-empty) { $result | upsert query $query | url join } else { $result | url join }
}

# Execute HTTP request with method dispatch
def do-request [method: string, url: string, auth: record, insecure: bool, raw: bool, max_time?: duration, allow_errors?: bool, content_type?: string, body?: any]: nothing -> any {
  let req_url = if ($auth.query | is-not-empty) { if ($url | str contains "?") { $"($url)&($auth.query)" } else { $"($url)?($auth.query)" } } else { $url }
  let timeout = ($max_time | default 30min)
  let ct = ($content_type | default "application/json")
  let resp = match $method {
    "get" => { http get --headers $auth.headers --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url }
    "head" => { http head --headers $auth.headers --max-time $timeout --insecure=$insecure $req_url }
    "options" => { http options --headers $auth.headers --max-time $timeout --insecure=$insecure $req_url }
    "post" => { http post --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "put" => { http put --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "patch" => { http patch --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "delete" => { if ($body | is-empty) { http delete --headers $auth.headers --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url } else { http delete --headers $auth.headers --content-type $ct --data $body --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url } }
  }
  if ($method in ["head" "options"]) { return $resp }
  if $allow_errors { $resp } else if $resp.status == 204 { null } else if $resp.status >= 400 { error make --unspanned { msg: $"HTTP ($resp.status): ($resp.body)" } } else { $resp.body }
}

def base-url-completer [] { ["http://localhost"] }
def auth-scheme-completer [] { ["bearer"] }

# Completers for enum parameters
def level-completer [] { ["awareness_attributes" "cluster" "indices" "shards"] }
def default-operator-completer [] { ["AND" "OR" "and" "or"] }
def type-completer [] { ["cpu" "latency" "memory"] }
def sort-completer [] { ["asc" "desc"] }
def level-completer-1 [] { ["cluster" "indices" "shards"] }
def suggest-mode-completer [] { ["always" "missing" "popular"] }
def operation-completer [] { ["max" "min" "sum"] }
def protocol-completer [] { ["aws_sigv4" "http"] }
def access-mode-completer [] { ["private" "public" "restricted"] }
def model-format-completer [] { ["ONNX" "TORCH_SCRIPT"] }
def config-type-completer [] { ["chime" "email" "email_group" "microsoft_teams" "ses_account" "slack" "smtp_account" "sns" "webhook"] }
def sortOrder-completer [] { ["asc" "desc"] }
def type-completer-1 [] { ["geo_point" "geo_shape"] }
def check-on-startup-completer [] { ["checksum" "false" "true"] }
def group-by-completer [] { ["nodes" "none" "parents"] }
def resiliency-mode-completer [] { ["enforced" "monitor" "soft"] }

# List all available API commands with their parameters
export def commands []: nothing -> table {
  let builtin_flags = ["base-url" "token" "auth-scheme" "insecure" "max-time" "raw" "allow-errors" "accept" "help"]
  let mod_name = (scope modules | where { $in.commands | any { $in.name == "api info0" } } | get name | first)
  let mod_cmds = (scope modules | where name == $mod_name | get commands | first)
  let cmd_ids = ($mod_cmds | where name not-in [$mod_name "commands"] | get decl_id)
  scope commands | where decl_id in $cmd_ids | each {|cmd|
    let sig = $cmd.signatures | values | first
    let params = $sig
      | where parameter_type not-in ["input" "output"]
      | where parameter_name not-in $builtin_flags
      | select parameter_name parameter_type syntax_shape is_optional description
    let return_type = ($sig | where parameter_type == "output" | get -o syntax_shape | first | default "any")
    {
      name: ($cmd.name | str replace $"($mod_name) " "")
      description: $cmd.description
      extra_description: $cmd.extra_description
      return_type: $return_type
      params: $params
    }
  }
}

# Returns basic information about the cluster.
#
# GET /
# Docs: https://opensearch.org/docs/latest
# operationId: info.0
export def "api info0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns whether the cluster is running.
#
# HEAD /
# Docs: https://opensearch.org/docs/latest
# operationId: ping.0
export def "api ping0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns an alias.
#
# GET /_alias
# Docs: https://opensearch.org/docs/latest/im-plugin/index-alias/
# operationId: indices.get_alias.0
export def "alias alias0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --local: oneof<nothing, bool> # If `true`, the request retrieves information from the local node only. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_alias" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or updates an alias.
#
# PUT /_alias
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/update-alias/
# operationId: indices.put_alias.0
# --filter shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
@deprecated --flag master-timeout
export def "alias alias0-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --alias: string
  --filter: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
  --index: string
  --index-routing: string # The routing value for the document.
  --is-write-index: oneof<nothing, bool> # If `true`, sets the write index or data stream for the alias. If an alias points to multiple indexes or data streams and `is_write_index` isn't set, the alias rejects write requests. If an index alias points to one index and `is_write_index` isn't set, the index automatically acts as the write index. Data stream aliases don't automatically set a write data stream, even if the alias points to one data stream.
  --routing: string # The routing value for the document.
  --search-routing: string # The routing value for the document.
  --is-hidden: oneof<nothing, bool> # If `true`, the alias will be hidden`. (default: false)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_alias" $qp)
  let body = {alias: $alias, filter: $filter, index: $index, index_routing: $index_routing, is_write_index: $is_write_index, routing: $routing, search_routing: $search_routing, is_hidden: $is_hidden} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns an alias.
#
# GET /_alias/{name}
# Docs: https://opensearch.org/docs/latest/im-plugin/index-alias/
# operationId: indices.get_alias.1
export def "alias alias1-by-name" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --local: oneof<nothing, bool> # If `true`, the request retrieves information from the local node only. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_alias/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about whether a particular alias exists.
#
# HEAD /_alias/{name}
# Docs: https://opensearch.org/docs/latest
# operationId: indices.exists_alias.0
export def "alias alias0-by-name" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, requests that include a missing data stream or index in the target indexes or data streams return an error.
  --local: oneof<nothing, bool> # If `true`, the request retrieves information from the local node only. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_alias/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or updates an alias.
#
# POST /_alias/{name}
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/update-alias/
# operationId: indices.put_alias.1
# --filter shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
@deprecated --flag master-timeout
export def "alias alias1-by-name-1" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --alias: string
  --filter: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
  --index: string
  --index-routing: string # The routing value for the document.
  --is-write-index: oneof<nothing, bool> # If `true`, sets the write index or data stream for the alias. If an alias points to multiple indexes or data streams and `is_write_index` isn't set, the alias rejects write requests. If an index alias points to one index and `is_write_index` isn't set, the index automatically acts as the write index. Data stream aliases don't automatically set a write data stream, even if the alias points to one data stream.
  --routing: string # The routing value for the document.
  --search-routing: string # The routing value for the document.
  --is-hidden: oneof<nothing, bool> # If `true`, the alias will be hidden`. (default: false)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_alias/($name)" $qp)
  let body = {alias: $alias, filter: $filter, index: $index, index_routing: $index_routing, is_write_index: $is_write_index, routing: $routing, search_routing: $search_routing, is_hidden: $is_hidden} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Creates or updates an alias.
#
# PUT /_alias/{name}
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/update-alias/
# operationId: indices.put_alias.2
# --filter shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
@deprecated --flag master-timeout
export def "alias alias2-by-name" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --alias: string
  --filter: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
  --index: string
  --index-routing: string # The routing value for the document.
  --is-write-index: oneof<nothing, bool> # If `true`, sets the write index or data stream for the alias. If an alias points to multiple indexes or data streams and `is_write_index` isn't set, the alias rejects write requests. If an index alias points to one index and `is_write_index` isn't set, the index automatically acts as the write index. Data stream aliases don't automatically set a write data stream, even if the alias points to one data stream.
  --routing: string # The routing value for the document.
  --search-routing: string # The routing value for the document.
  --is-hidden: oneof<nothing, bool> # If `true`, the alias will be hidden`. (default: false)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_alias/($name)" $qp)
  let body = {alias: $alias, filter: $filter, index: $index, index_routing: $index_routing, is_write_index: $is_write_index, routing: $routing, search_routing: $search_routing, is_hidden: $is_hidden} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Updates index aliases.
#
# POST /_aliases
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/alias/
# operationId: indices.update_aliases.0
# --actions item shape: {add?: record, remove?: record, remove_index?: record}
@deprecated --flag master-timeout
export def "aliases aliases0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --actions: list # Actions to perform. — item shape: {add?: record, remove?: record, remove_index?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_aliases" $qp)
  let body = {actions: $actions} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Creates or updates an alias.
#
# POST /_aliases/{name}
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/update-alias/
# operationId: indices.put_alias.3
# --filter shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
@deprecated --flag master-timeout
export def "aliases alias3" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --alias: string
  --filter: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
  --index: string
  --index-routing: string # The routing value for the document.
  --is-write-index: oneof<nothing, bool> # If `true`, sets the write index or data stream for the alias. If an alias points to multiple indexes or data streams and `is_write_index` isn't set, the alias rejects write requests. If an index alias points to one index and `is_write_index` isn't set, the index automatically acts as the write index. Data stream aliases don't automatically set a write data stream, even if the alias points to one data stream.
  --routing: string # The routing value for the document.
  --search-routing: string # The routing value for the document.
  --is-hidden: oneof<nothing, bool> # If `true`, the alias will be hidden`. (default: false)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_aliases/($name)" $qp)
  let body = {alias: $alias, filter: $filter, index: $index, index_routing: $index_routing, is_write_index: $is_write_index, routing: $routing, search_routing: $search_routing, is_hidden: $is_hidden} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Creates or updates an alias.
#
# PUT /_aliases/{name}
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/update-alias/
# operationId: indices.put_alias.4
# --filter shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
@deprecated --flag master-timeout
export def "aliases alias4" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --alias: string
  --filter: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
  --index: string
  --index-routing: string # The routing value for the document.
  --is-write-index: oneof<nothing, bool> # If `true`, sets the write index or data stream for the alias. If an alias points to multiple indexes or data streams and `is_write_index` isn't set, the alias rejects write requests. If an index alias points to one index and `is_write_index` isn't set, the index automatically acts as the write index. Data stream aliases don't automatically set a write data stream, even if the alias points to one data stream.
  --routing: string # The routing value for the document.
  --search-routing: string # The routing value for the document.
  --is-hidden: oneof<nothing, bool> # If `true`, the alias will be hidden`. (default: false)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_aliases/($name)" $qp)
  let body = {alias: $alias, filter: $filter, index: $index, index_routing: $index_routing, is_write_index: $is_write_index, routing: $routing, search_routing: $search_routing, is_hidden: $is_hidden} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Performs the analysis process on a text and return the tokens breakdown of the text.
#
# GET /_analyze
# Docs: https://opensearch.org/docs/latest/api-reference/analyze-apis/perform-text-analysis/
# operationId: indices.analyze.0
export def "analyze indicesanalyze0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --index: string # The name of the index to scope the operation.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --analyzer: string # The name of the analyzer that should be applied to the provided `text`. This could be a built-in analyzer, or an analyzer that's been configured in the index.
  --attributes: list # Array of token attributes used to filter the output of the `explain` parameter.
  --char-filter: list # Array of character filters used to preprocess characters before the tokenizer.
  --explain: oneof<nothing, bool> # If `true`, the response includes token attributes and additional details.
  --field: string # The path to a field or an array of paths. Some APIs support wildcards in the path, which allows you to select multiple fields.
  --filter: list # Array of token filters used to apply after the tokenizer.
  --normalizer: string # Normalizer to use to convert text into a single token.
  --text: any
  --tokenizer: any
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "index" $index "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_analyze" $qp)
  let body = {analyzer: $analyzer, attributes: $attributes, char_filter: $char_filter, explain: $explain, field: $field, filter: $filter, normalizer: $normalizer, text: $text, tokenizer: $tokenizer} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Performs the analysis process on a text and return the tokens breakdown of the text.
#
# POST /_analyze
# Docs: https://opensearch.org/docs/latest/api-reference/analyze-apis/perform-text-analysis/
# operationId: indices.analyze.1
export def "analyze indicesanalyze1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --index: string # The name of the index to scope the operation.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --analyzer: string # The name of the analyzer that should be applied to the provided `text`. This could be a built-in analyzer, or an analyzer that's been configured in the index.
  --attributes: list # Array of token attributes used to filter the output of the `explain` parameter.
  --char-filter: list # Array of character filters used to preprocess characters before the tokenizer.
  --explain: oneof<nothing, bool> # If `true`, the response includes token attributes and additional details.
  --field: string # The path to a field or an array of paths. Some APIs support wildcards in the path, which allows you to select multiple fields.
  --filter: list # Array of token filters used to apply after the tokenizer.
  --normalizer: string # Normalizer to use to convert text into a single token.
  --text: any
  --tokenizer: any
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "index" $index "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_analyze" $qp)
  let body = {analyzer: $analyzer, attributes: $attributes, char_filter: $char_filter, explain: $explain, field: $field, filter: $filter, normalizer: $normalizer, text: $text, tokenizer: $tokenizer} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Allows to perform multiple index/update/delete operations in a single request.
#
# POST /_bulk
# Docs: https://opensearch.org/docs/latest/api-reference/document-apis/bulk/
# operationId: bulk.0
export def "bulk bulk0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: string # `true` or `false` to return the `_source` field or not, or a list of fields to return.
  --source-excludes: string # A comma-separated list of source fields to exclude from the response.
  --source-includes: string # A comma-separated list of source fields to include in the response.
  --pipeline: string # ID of the pipeline to use to preprocess incoming documents. If the index has a default ingest pipeline specified, then setting the value to `_none` disables the default ingest pipeline for this request. If a final pipeline is configured it will always run, regardless of the value of this parameter.
  --refresh: string # If `true`, OpenSearch refreshes the affected shards to make this operation visible to search, if `wait_for` then wait for a refresh to make this operation visible to search, if `false` do nothing with refreshes. Valid values: `true`, `false`, `wait_for`.
  --require-alias: oneof<nothing, bool> # If `true`, the request's actions must target an index alias. (default: false)
  --routing: string # Custom value used to route operations to a specific shard.
  --timeout: string # Period each action waits for the following operations: automatic index creation, dynamic mapping updates, waiting for active shards.
  --type: string # Default document type for items which don't provide one.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to all or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "scalar") (serialize-qp "_source_excludes" $source_excludes "scalar") (serialize-qp "_source_includes" $source_includes "scalar") (serialize-qp "pipeline" $pipeline "scalar") (serialize-qp "refresh" $refresh "scalar") (serialize-qp "require_alias" $require_alias "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_bulk" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/x-ndjson" $body
}

# Allows to perform multiple index/update/delete operations in a single request.
#
# PUT /_bulk
# Docs: https://opensearch.org/docs/latest/api-reference/document-apis/bulk/
# operationId: bulk.1
export def "bulk bulk1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: string # `true` or `false` to return the `_source` field or not, or a list of fields to return.
  --source-excludes: string # A comma-separated list of source fields to exclude from the response.
  --source-includes: string # A comma-separated list of source fields to include in the response.
  --pipeline: string # ID of the pipeline to use to preprocess incoming documents. If the index has a default ingest pipeline specified, then setting the value to `_none` disables the default ingest pipeline for this request. If a final pipeline is configured it will always run, regardless of the value of this parameter.
  --refresh: string # If `true`, OpenSearch refreshes the affected shards to make this operation visible to search, if `wait_for` then wait for a refresh to make this operation visible to search, if `false` do nothing with refreshes. Valid values: `true`, `false`, `wait_for`.
  --require-alias: oneof<nothing, bool> # If `true`, the request's actions must target an index alias. (default: false)
  --routing: string # Custom value used to route operations to a specific shard.
  --timeout: string # Period each action waits for the following operations: automatic index creation, dynamic mapping updates, waiting for active shards.
  --type: string # Default document type for items which don't provide one.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to all or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "scalar") (serialize-qp "_source_excludes" $source_excludes "scalar") (serialize-qp "_source_includes" $source_includes "scalar") (serialize-qp "pipeline" $pipeline "scalar") (serialize-qp "refresh" $refresh "scalar") (serialize-qp "require_alias" $require_alias "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_bulk" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/x-ndjson" $body
}

# Allows to perform multiple index/update/delete operations using request response streaming.
#
# POST /_bulk/stream
# Docs: https://opensearch.org/docs/latest/api-reference/document-apis/bulk-streaming/
# operationId: bulk_stream.0
export def "bulk-stream stream0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: string # `true` or `false` to return the `_source` field or not, or a list of fields to return.
  --source-excludes: string # A comma-separated list of source fields to exclude from the response.
  --source-includes: string # A comma-separated list of source fields to include in the response.
  --batch-interval: string # Specifies for how long bulk operations should be accumulated into a batch before sending the batch to data nodes.
  --batch-size: int # Specifies how many bulk operations should be accumulated into a batch before sending the batch to data nodes. (format: int64)
  --pipeline: string # ID of the pipeline to use to preprocess incoming documents. If the index has a default ingest pipeline specified, then setting the value to `_none` disables the default ingest pipeline for this request. If a final pipeline is configured it will always run, regardless of the value of this parameter.
  --refresh: string # If `true`, OpenSearch refreshes the affected shards to make this operation visible to search, if `wait_for` then wait for a refresh to make this operation visible to search, if `false` do nothing with refreshes. Valid values: `true`, `false`, `wait_for`.
  --require-alias: oneof<nothing, bool> # If `true`, the request's actions must target an index alias. (default: false)
  --routing: string # Custom value used to route operations to a specific shard.
  --timeout: string # Period each action waits for the following operations: automatic index creation, dynamic mapping updates, waiting for active shards.
  --type: string # Default document type for items which don't provide one.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to all or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "scalar") (serialize-qp "_source_excludes" $source_excludes "scalar") (serialize-qp "_source_includes" $source_includes "scalar") (serialize-qp "batch_interval" $batch_interval "scalar") (serialize-qp "batch_size" $batch_size "scalar") (serialize-qp "pipeline" $pipeline "scalar") (serialize-qp "refresh" $refresh "scalar") (serialize-qp "require_alias" $require_alias "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_bulk/stream" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/x-ndjson" $body
}

# Allows to perform multiple index/update/delete operations using request response streaming.
#
# PUT /_bulk/stream
# Docs: https://opensearch.org/docs/latest/api-reference/document-apis/bulk-streaming/
# operationId: bulk_stream.1
export def "bulk-stream stream1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: string # `true` or `false` to return the `_source` field or not, or a list of fields to return.
  --source-excludes: string # A comma-separated list of source fields to exclude from the response.
  --source-includes: string # A comma-separated list of source fields to include in the response.
  --batch-interval: string # Specifies for how long bulk operations should be accumulated into a batch before sending the batch to data nodes.
  --batch-size: int # Specifies how many bulk operations should be accumulated into a batch before sending the batch to data nodes. (format: int64)
  --pipeline: string # ID of the pipeline to use to preprocess incoming documents. If the index has a default ingest pipeline specified, then setting the value to `_none` disables the default ingest pipeline for this request. If a final pipeline is configured it will always run, regardless of the value of this parameter.
  --refresh: string # If `true`, OpenSearch refreshes the affected shards to make this operation visible to search, if `wait_for` then wait for a refresh to make this operation visible to search, if `false` do nothing with refreshes. Valid values: `true`, `false`, `wait_for`.
  --require-alias: oneof<nothing, bool> # If `true`, the request's actions must target an index alias. (default: false)
  --routing: string # Custom value used to route operations to a specific shard.
  --timeout: string # Period each action waits for the following operations: automatic index creation, dynamic mapping updates, waiting for active shards.
  --type: string # Default document type for items which don't provide one.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to all or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "scalar") (serialize-qp "_source_excludes" $source_excludes "scalar") (serialize-qp "_source_includes" $source_includes "scalar") (serialize-qp "batch_interval" $batch_interval "scalar") (serialize-qp "batch_size" $batch_size "scalar") (serialize-qp "pipeline" $pipeline "scalar") (serialize-qp "refresh" $refresh "scalar") (serialize-qp "require_alias" $require_alias "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_bulk/stream" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/x-ndjson" $body
}

# Clears all or specific caches for one or more indexes.
#
# POST /_cache/clear
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/clear-index-cache/
# operationId: indices.clear_cache.0
export def "cache-clear cache0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --fielddata: oneof<nothing, bool> # If `true`, clears the fields cache. Use the `fields` parameter to clear the cache of specific fields only.
  --qp-fields: string # Comma-separated list of field names used to limit the `fielddata` parameter.
  --file: oneof<nothing, bool> # If `true`, clears the unused entries from the file cache on nodes with the Search role. (default: false)
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --index: list # Comma-separated list of indexes; use `_all` or empty string to perform the operation on all indexes.
  --qp-query: oneof<nothing, bool> # If `true`, clears the query cache.
  --request: oneof<nothing, bool> # If `true`, clears the request cache.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "fielddata" $fielddata "scalar") (serialize-qp "fields" $qp_fields "scalar") (serialize-qp "file" $file "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "index" $index "multi") (serialize-qp "query" $qp_query "scalar") (serialize-qp "request" $request "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cache/clear" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns help for the Cat APIs.
#
# GET /_cat
# Docs: https://opensearch.org/docs/latest/api-reference/cat/index/
# operationId: cat.help.0
export def "cat cathelp0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cat" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Shows information about aliases currently configured to indexes, including filter and routing information.
#
# GET /_cat/aliases
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-aliases/
# operationId: cat.aliases.0
export def "cat-aliases cataliases0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --expand-wildcards: string
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --local: oneof<nothing, bool> # Whether to return information from the local node only instead of from the cluster manager node. (default: false)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "s" $s "multi") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cat/aliases" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Shows information about aliases currently configured to indexes, including filter and routing information.
#
# GET /_cat/aliases/{name}
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-aliases/
# operationId: cat.aliases.1
export def "cat-aliases cataliases1" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --expand-wildcards: string
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --local: oneof<nothing, bool> # Whether to return information from the local node only instead of from the cluster manager node. (default: false)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "s" $s "multi") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_cat/aliases/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Provides a snapshot of how many shards are allocated to each data node and how much disk space they are using.
#
# GET /_cat/allocation
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-allocation/
# operationId: cat.allocation.0
@deprecated --flag master-timeout
export def "cat-allocation catallocation0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --bytes: string # The units used to display byte values.
  --cluster-manager-timeout: string # A timeout for connection to the cluster manager node.
  --format: string # A short version of the HTTP `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --local: oneof<nothing, bool> # Returns local information but does not retrieve the state from cluster-manager node. (default: false)
  --master-timeout: string # A timeout for connection to the cluster manager node. (DEPRECATED)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "bytes" $bytes "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "s" $s "multi") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cat/allocation" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Provides a snapshot of how many shards are allocated to each data node and how much disk space they are using.
#
# GET /_cat/allocation/{node_id}
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-allocation/
# operationId: cat.allocation.1
@deprecated --flag master-timeout
export def "cat-allocation catallocation1" [
  node_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --bytes: string # The units used to display byte values.
  --cluster-manager-timeout: string # A timeout for connection to the cluster manager node.
  --format: string # A short version of the HTTP `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --local: oneof<nothing, bool> # Returns local information but does not retrieve the state from cluster-manager node. (default: false)
  --master-timeout: string # A timeout for connection to the cluster manager node. (DEPRECATED)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "bytes" $bytes "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "s" $s "multi") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_cat/allocation/($node_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about the cluster-manager node.
#
# GET /_cat/cluster_manager
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-cluster_manager/
# operationId: cat.cluster_manager.0
@deprecated --flag master-timeout
export def "cat-cluster-manager manager0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # A timeout for connection to the cluster manager node.
  --format: string # A short version of the HTTP `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --local: oneof<nothing, bool> # Returns local information but does not retrieve the state from the cluster manager node. (default: false)
  --master-timeout: string # A timeout for connection to the cluster manager node. (DEPRECATED)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "s" $s "multi") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cat/cluster_manager" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Provides quick access to the document count of the entire cluster or of an individual index.
#
# GET /_cat/count
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-count/
# operationId: cat.count.0
export def "cat-count catcount0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "s" $s "multi") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cat/count" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Provides quick access to the document count of the entire cluster or of an individual index.
#
# GET /_cat/count/{index}
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-count/
# operationId: cat.count.1
export def "cat-count catcount1" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "s" $s "multi") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_cat/count/($index)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Shows how much heap memory is currently being used by field data on every data node in the cluster.
#
# GET /_cat/fielddata
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-field-data/
# operationId: cat.fielddata.0
export def "cat-fielddata catfielddata0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --bytes: string # The units used to display byte values.
  --qp-fields: string # A comma-separated list of fields used to limit the amount of returned information.
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "bytes" $bytes "scalar") (serialize-qp "fields" $qp_fields "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "s" $s "multi") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cat/fielddata" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Shows how much heap memory is currently being used by field data on every data node in the cluster.
#
# GET /_cat/fielddata/{fields}
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-field-data/
# operationId: cat.fielddata.1
export def "cat-fielddata catfielddata1" [
  fields: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --bytes: string # The units used to display byte values.
  --qp-fields: string # A comma-separated list of fields used to limit the amount of returned information.
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "bytes" $bytes "scalar") (serialize-qp "fields" $qp_fields "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "s" $s "multi") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_cat/fielddata/($fields)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns a concise representation of the cluster health.
#
# GET /_cat/health
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-health/
# operationId: cat.health.0
export def "cat-health cathealth0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --time: string # The unit used to display time values.
  --ts: oneof<nothing, bool> # When `true`, returns `HH:MM:SS` and Unix epoch timestamps. (default: true)
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "s" $s "multi") (serialize-qp "time" $time "scalar") (serialize-qp "ts" $ts "scalar") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cat/health" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Lists information related to indexes, that is, how much disk space they are using, how many shards they have, their health status, and so on.
#
# GET /_cat/indices
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-indices/
# operationId: cat.indices.0
@deprecated --flag master-timeout
export def "cat-indices catindices0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --bytes: string # The units used to display byte values.
  --cluster-manager-timeout: string # The amount of time allowed to establish a connection to the cluster manager node.
  --expand-wildcards: string
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --health: string # Limits indexes based on their health status. Supported values are `green`, `yellow`, and `red`.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --include-unloaded-segments: oneof<nothing, bool> # Whether to include information from segments not loaded into memory. (default: false)
  --local: oneof<nothing, bool> # Returns local information but does not retrieve the state from the cluster manager node. (default: false)
  --master-timeout: string # The amount of time allowed to establish a connection to the cluster manager node. (DEPRECATED)
  --pri: oneof<nothing, bool> # When `true`, returns information only from the primary shards. (default: false)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --time: string # Specifies the time units.
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "bytes" $bytes "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "health" $health "scalar") (serialize-qp "help" $help "scalar") (serialize-qp "include_unloaded_segments" $include_unloaded_segments "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pri" $pri "scalar") (serialize-qp "s" $s "multi") (serialize-qp "time" $time "scalar") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cat/indices" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Lists information related to indexes, that is, how much disk space they are using, how many shards they have, their health status, and so on.
#
# GET /_cat/indices/{index}
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-indices/
# operationId: cat.indices.1
@deprecated --flag master-timeout
export def "cat-indices catindices1" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --bytes: string # The units used to display byte values.
  --cluster-manager-timeout: string # The amount of time allowed to establish a connection to the cluster manager node.
  --expand-wildcards: string
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --health: string # Limits indexes based on their health status. Supported values are `green`, `yellow`, and `red`.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --include-unloaded-segments: oneof<nothing, bool> # Whether to include information from segments not loaded into memory. (default: false)
  --local: oneof<nothing, bool> # Returns local information but does not retrieve the state from the cluster manager node. (default: false)
  --master-timeout: string # The amount of time allowed to establish a connection to the cluster manager node. (DEPRECATED)
  --pri: oneof<nothing, bool> # When `true`, returns information only from the primary shards. (default: false)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --time: string # Specifies the time units.
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "bytes" $bytes "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "health" $health "scalar") (serialize-qp "help" $help "scalar") (serialize-qp "include_unloaded_segments" $include_unloaded_segments "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pri" $pri "scalar") (serialize-qp "s" $s "multi") (serialize-qp "time" $time "scalar") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_cat/indices/($index)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about the cluster-manager node.
#
# GET /_cat/master
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-cluster_manager/
# operationId: cat.master.0
@deprecated
@deprecated --flag master-timeout
export def "cat-master catmaster0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time allowed to establish a connection to the cluster manager node.
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --local: oneof<nothing, bool> # Returns local information but does not retrieve the state from the cluster manager node. (default: false)
  --master-timeout: string # The amount of time allowed to establish a connection to the cluster manager node. (DEPRECATED)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "s" $s "multi") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cat/master" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about custom node attributes.
#
# GET /_cat/nodeattrs
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-nodeattrs/
# operationId: cat.nodeattrs.0
@deprecated --flag master-timeout
export def "cat-nodeattrs catnodeattrs0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time allowed to establish a connection to the cluster manager node.
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --local: oneof<nothing, bool> # Returns local information but does not retrieve the state from the cluster manager node. (default: false)
  --master-timeout: string # The amount of time allowed to establish a connection to the cluster manager node. (DEPRECATED)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "s" $s "multi") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cat/nodeattrs" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns basic statistics about the performance of cluster nodes.
#
# GET /_cat/nodes
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-nodes/
# operationId: cat.nodes.0
@deprecated --flag local
@deprecated --flag master-timeout
export def "cat-nodes catnodes0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --bytes: string # The units used to display byte values.
  --cluster-manager-timeout: string # The amount of time allowed to establish a connection to the cluster manager node.
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --full-id: string # When `true`, returns the full node ID. When `false`, returns the shortened node ID.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --local: oneof<nothing, bool> # Returns local information but does not retrieve the state from the cluster manager node. (DEPRECATED, default: false)
  --master-timeout: string # The amount of time allowed to establish a connection to the cluster manager node. (DEPRECATED)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --time: string # Specifies the time units, for example, `5d` or `7h`. For more information, see [Supported units](https://opensearch.org/docs/latest/api-reference/units/).
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "bytes" $bytes "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "full_id" $full_id "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "s" $s "multi") (serialize-qp "time" $time "scalar") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cat/nodes" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns a concise representation of the cluster's pending tasks.
#
# GET /_cat/pending_tasks
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-pending-tasks/
# operationId: cat.pending_tasks.0
@deprecated --flag master-timeout
export def "cat-pending-tasks tasks0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time allowed to establish a connection to the cluster manager node.
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --local: oneof<nothing, bool> # Returns local information but does not retrieve the state from the cluster manager node. (default: false)
  --master-timeout: string # The amount of time allowed to establish a connection to the cluster manager node. (DEPRECATED)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --time: string # Specifies the time units, for example, `5d` or `7h`. For more information, see [Supported units](https://opensearch.org/docs/latest/api-reference/units/).
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "s" $s "multi") (serialize-qp "time" $time "scalar") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cat/pending_tasks" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Lists one or several CAT point-in-time segments.
#
# GET /_cat/pit_segments
# Docs: https://opensearch.org/docs/latest/search-plugins/point-in-time-api/
# operationId: cat.pit_segments.0
export def "cat-pit-segments segments0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --bytes: string # The units used to display byte values.
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  pit_id: list
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "bytes" $bytes "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "s" $s "multi") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cat/pit_segments" $qp)
  let body = {pit_id: $pit_id} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Lists all active CAT point-in-time segments.
#
# GET /_cat/pit_segments/_all
# Docs: https://opensearch.org/docs/latest/search-plugins/point-in-time-api/
# operationId: cat.all_pit_segments.0
export def "cat-pit-segments-all segments0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --bytes: string # The units used to display byte values.
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "bytes" $bytes "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "s" $s "multi") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cat/pit_segments/_all" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about the names, components, and versions of the installed plugins.
#
# GET /_cat/plugins
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-plugins/
# operationId: cat.plugins.0
@deprecated --flag master-timeout
export def "cat-plugins catplugins0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time allowed to establish a connection to the cluster manager node.
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --local: oneof<nothing, bool> # Returns local information but does not retrieve the state from the cluster manager node. (default: false)
  --master-timeout: string # The amount of time allowed to establish a connection to the cluster manager node. (DEPRECATED)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "s" $s "multi") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cat/plugins" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns all completed and ongoing index and shard recoveries.
#
# GET /_cat/recovery
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-plugins/
# operationId: cat.recovery.0
export def "cat-recovery catrecovery0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --active-only: oneof<nothing, bool> # If `true`, the response only includes ongoing shard recoveries. (default: false)
  --bytes: string # The units used to display byte values.
  --detailed: oneof<nothing, bool> # When `true`, includes detailed information about shard recoveries. (default: false)
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --index: list # A comma-separated list of data streams, indexes, and aliases used to limit the request. Supports wildcards (`*`). To target all data streams and indexes, omit this parameter or use `*` or `_all`.
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --time: string # Specifies the time units, for example, `5d` or `7h`. For more information, see [Supported units](https://opensearch.org/docs/latest/api-reference/units/).
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "active_only" $active_only "scalar") (serialize-qp "bytes" $bytes "scalar") (serialize-qp "detailed" $detailed "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "index" $index "multi") (serialize-qp "s" $s "multi") (serialize-qp "time" $time "scalar") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cat/recovery" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns all completed and ongoing index and shard recoveries.
#
# GET /_cat/recovery/{index}
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-plugins/
# operationId: cat.recovery.1
export def "cat-recovery catrecovery1" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --active-only: oneof<nothing, bool> # If `true`, the response only includes ongoing shard recoveries. (default: false)
  --bytes: string # The units used to display byte values.
  --detailed: oneof<nothing, bool> # When `true`, includes detailed information about shard recoveries. (default: false)
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --index: list # A comma-separated list of data streams, indexes, and aliases used to limit the request. Supports wildcards (`*`). To target all data streams and indexes, omit this parameter or use `*` or `_all`.
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --time: string # Specifies the time units, for example, `5d` or `7h`. For more information, see [Supported units](https://opensearch.org/docs/latest/api-reference/units/).
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "active_only" $active_only "scalar") (serialize-qp "bytes" $bytes "scalar") (serialize-qp "detailed" $detailed "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "index" $index "multi") (serialize-qp "s" $s "multi") (serialize-qp "time" $time "scalar") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_cat/recovery/($index)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about all snapshot repositories for a cluster.
#
# GET /_cat/repositories
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-repositories/
# operationId: cat.repositories.0
@deprecated --flag master-timeout
export def "cat-repositories catrepositories0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time allowed to establish a connection to the cluster manager node.
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --local: oneof<nothing, bool> # Returns local information but does not retrieve the state from the cluster manager node. (default: false)
  --master-timeout: string # The amount of time allowed to establish a connection to the cluster manager node. (DEPRECATED)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "s" $s "multi") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cat/repositories" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about active and last-completed segment replication events on each replica shard, including related shard-level metrics.  These metrics provide information about how far behind the primary shard the replicas are lagging.
#
# GET /_cat/segment_replication
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-segment-replication/
# operationId: cat.segment_replication.0
export def "cat-segment-replication replication0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --active-only: oneof<nothing, bool> # When `true`, the response only includes ongoing segment replication events. (default: false)
  --allow-no-indices: oneof<nothing, bool> # Whether to ignore the index if a wildcard index expression resolves to no concrete indexes. This includes the `_all` string or when no indexes have been specified.
  --bytes: string # The units used to display byte values.
  --completed-only: oneof<nothing, bool> # When `true`, the response only includes the last-completed segment replication events. (default: false)
  --detailed: oneof<nothing, bool> # When `true`, the response includes additional metrics for each stage of a segment replication event. (default: false)
  --expand-wildcards: string
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --ignore-throttled: oneof<nothing, bool> # Whether specified concrete, expanded, or aliased indexes should be ignored when throttled.
  --ignore-unavailable: oneof<nothing, bool> # Whether the specified concrete indexes should be ignored when missing or closed.
  --index: list # A comma-separated list of data streams, indexes, and aliases used to limit the request. Supports wildcards (`*`). To target all data streams and indexes, omit this parameter or use `*` or `_all`.
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --shards: list # A comma-separated list of shards to display.
  --time: string # Specifies the time units, for example, `5d` or `7h`. For more information, see [Supported units](https://opensearch.org/docs/latest/api-reference/units/).
  --timeout: string # The operation timeout.
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "active_only" $active_only "scalar") (serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "bytes" $bytes "scalar") (serialize-qp "completed_only" $completed_only "scalar") (serialize-qp "detailed" $detailed "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "ignore_throttled" $ignore_throttled "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "index" $index "multi") (serialize-qp "s" $s "multi") (serialize-qp "shards" $shards "multi") (serialize-qp "time" $time "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cat/segment_replication" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about active and last-completed segment replication events on each replica shard, including related shard-level metrics.  These metrics provide information about how far behind the primary shard the replicas are lagging.
#
# GET /_cat/segment_replication/{index}
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-segment-replication/
# operationId: cat.segment_replication.1
export def "cat-segment-replication replication1" [
  index: list
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --active-only: oneof<nothing, bool> # When `true`, the response only includes ongoing segment replication events. (default: false)
  --allow-no-indices: oneof<nothing, bool> # Whether to ignore the index if a wildcard index expression resolves to no concrete indexes. This includes the `_all` string or when no indexes have been specified.
  --bytes: string # The units used to display byte values.
  --completed-only: oneof<nothing, bool> # When `true`, the response only includes the last-completed segment replication events. (default: false)
  --detailed: oneof<nothing, bool> # When `true`, the response includes additional metrics for each stage of a segment replication event. (default: false)
  --expand-wildcards: string
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --ignore-throttled: oneof<nothing, bool> # Whether specified concrete, expanded, or aliased indexes should be ignored when throttled.
  --ignore-unavailable: oneof<nothing, bool> # Whether the specified concrete indexes should be ignored when missing or closed.
  --index: list # A comma-separated list of data streams, indexes, and aliases used to limit the request. Supports wildcards (`*`). To target all data streams and indexes, omit this parameter or use `*` or `_all`.
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --shards: list # A comma-separated list of shards to display.
  --time: string # Specifies the time units, for example, `5d` or `7h`. For more information, see [Supported units](https://opensearch.org/docs/latest/api-reference/units/).
  --timeout: string # The operation timeout.
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "active_only" $active_only "scalar") (serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "bytes" $bytes "scalar") (serialize-qp "completed_only" $completed_only "scalar") (serialize-qp "detailed" $detailed "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "ignore_throttled" $ignore_throttled "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "index" $index "multi") (serialize-qp "s" $s "multi") (serialize-qp "shards" $shards "multi") (serialize-qp "time" $time "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_cat/segment_replication/($index)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Provides low-level information about the segments in the shards of an index.
#
# GET /_cat/segments
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-segments/
# operationId: cat.segments.0
@deprecated --flag master-timeout
export def "cat-segments catsegments0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --bytes: string # The units used to display byte values.
  --cluster-manager-timeout: string # The amount of time allowed to establish a connection to the cluster manager node.
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --master-timeout: string # The amount of time allowed to establish a connection to the cluster manager node. (DEPRECATED)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "bytes" $bytes "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "s" $s "multi") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cat/segments" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Provides low-level information about the segments in the shards of an index.
#
# GET /_cat/segments/{index}
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-segments/
# operationId: cat.segments.1
@deprecated --flag master-timeout
export def "cat-segments catsegments1" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --bytes: string # The units used to display byte values.
  --cluster-manager-timeout: string # The amount of time allowed to establish a connection to the cluster manager node.
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --master-timeout: string # The amount of time allowed to establish a connection to the cluster manager node. (DEPRECATED)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "bytes" $bytes "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "s" $s "multi") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_cat/segments/($index)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Lists the states of all primary and replica shards and how they are distributed.
#
# GET /_cat/shards
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-shards/
# operationId: cat.shards.0
@deprecated --flag master-timeout
export def "cat-shards catshards0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --bytes: string # The units used to display byte values.
  --cluster-manager-timeout: string # The amount of time allowed to establish a connection to the cluster manager node.
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --local: oneof<nothing, bool> # Returns local information but does not retrieve the state from the cluster manager node. (default: false)
  --master-timeout: string # The amount of time allowed to establish a connection to the cluster manager node. (DEPRECATED)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --time: string
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "bytes" $bytes "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "s" $s "multi") (serialize-qp "time" $time "scalar") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cat/shards" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Lists the states of all primary and replica shards and how they are distributed.
#
# GET /_cat/shards/{index}
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-shards/
# operationId: cat.shards.1
@deprecated --flag master-timeout
export def "cat-shards catshards1" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --bytes: string # The units used to display byte values.
  --cluster-manager-timeout: string # The amount of time allowed to establish a connection to the cluster manager node.
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --local: oneof<nothing, bool> # Returns local information but does not retrieve the state from the cluster manager node. (default: false)
  --master-timeout: string # The amount of time allowed to establish a connection to the cluster manager node. (DEPRECATED)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --time: string
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "bytes" $bytes "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "s" $s "multi") (serialize-qp "time" $time "scalar") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_cat/shards/($index)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Lists all of the snapshots stored in a specific repository.
#
# GET /_cat/snapshots
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-snapshots/
# operationId: cat.snapshots.0
@deprecated --flag master-timeout
export def "cat-snapshots catsnapshots0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time allowed to establish a connection to the cluster manager node.
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --ignore-unavailable: oneof<nothing, bool> # When `true`, the response does not include information from unavailable snapshots. (default: false)
  --master-timeout: string # The amount of time allowed to establish a connection to the cluster manager node. (DEPRECATED)
  --repository: string # A comma-separated list of snapshot repositories used to limit the request. Accepts wildcard expressions. `_all` returns all repositories. If any repository fails during the request, OpenSearch returns an error.
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --time: string # Specifies the time units, for example, `5d` or `7h`. For more information, see [Supported units](https://opensearch.org/docs/latest/api-reference/units/).
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "repository" $repository "scalar") (serialize-qp "s" $s "multi") (serialize-qp "time" $time "scalar") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cat/snapshots" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Lists all of the snapshots stored in a specific repository.
#
# GET /_cat/snapshots/{repository}
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-snapshots/
# operationId: cat.snapshots.1
@deprecated --flag master-timeout
export def "cat-snapshots catsnapshots1" [
  repository: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time allowed to establish a connection to the cluster manager node.
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --ignore-unavailable: oneof<nothing, bool> # When `true`, the response does not include information from unavailable snapshots. (default: false)
  --master-timeout: string # The amount of time allowed to establish a connection to the cluster manager node. (DEPRECATED)
  --repository: string # A comma-separated list of snapshot repositories used to limit the request. Accepts wildcard expressions. `_all` returns all repositories. If any repository fails during the request, OpenSearch returns an error.
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --time: string # Specifies the time units, for example, `5d` or `7h`. For more information, see [Supported units](https://opensearch.org/docs/latest/api-reference/units/).
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "repository" $repository "scalar") (serialize-qp "s" $s "multi") (serialize-qp "time" $time "scalar") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_cat/snapshots/($repository)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Lists the progress of all tasks currently running on the cluster.
#
# GET /_cat/tasks
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-tasks/
# operationId: cat.tasks.0
export def "cat-tasks cattasks0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --actions: list # The task action names used to limit the response.
  --detailed: oneof<nothing, bool> # If `true`, the response includes detailed information about shard recoveries. (default: false)
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --nodes: list # A comma-separated list of node IDs or names used to limit the returned information.  Use `_local` to return information from the node to which you're connecting, specify a specific node from which to get information, or keep the parameter empty to get information from all nodes.
  --parent-task-id: string # The parent task identifier, which is used to limit the response.
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --time: string # Specifies the time units, for example, `5d` or `7h`. For more information, see [Supported units](https://opensearch.org/docs/latest/api-reference/units/).
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "actions" $actions "multi") (serialize-qp "detailed" $detailed "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "nodes" $nodes "multi") (serialize-qp "parent_task_id" $parent_task_id "scalar") (serialize-qp "s" $s "multi") (serialize-qp "time" $time "scalar") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cat/tasks" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Lists the names, patterns, order numbers, and version numbers of index templates.
#
# GET /_cat/templates
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-templates/
# operationId: cat.templates.0
@deprecated --flag master-timeout
export def "cat-templates cattemplates0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time allowed to establish a connection to the cluster manager node.
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --local: oneof<nothing, bool> # Returns local information but does not retrieve the state from the cluster manager node. (default: false)
  --master-timeout: string # The amount of time allowed to establish a connection to the cluster manager node. (DEPRECATED)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "s" $s "multi") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cat/templates" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Lists the names, patterns, order numbers, and version numbers of index templates.
#
# GET /_cat/templates/{name}
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-templates/
# operationId: cat.templates.1
@deprecated --flag master-timeout
export def "cat-templates cattemplates1" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time allowed to establish a connection to the cluster manager node.
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --local: oneof<nothing, bool> # Returns local information but does not retrieve the state from the cluster manager node. (default: false)
  --master-timeout: string # The amount of time allowed to establish a connection to the cluster manager node. (DEPRECATED)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "s" $s "multi") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_cat/templates/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns cluster-wide thread pool statistics per node. By default the active, queued, and rejected statistics are returned for all thread pools.
#
# GET /_cat/thread_pool
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-thread-pool/
# operationId: cat.thread_pool.0
@deprecated --flag master-timeout
export def "cat-thread-pool pool0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # A timeout for connection to the cluster manager node.
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --local: oneof<nothing, bool> # Returns local information but does not retrieve the state from the cluster manager node. (default: false)
  --master-timeout: string # The amount of time allowed to establish a connection to the cluster manager node. (DEPRECATED)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --size: int # The multiplier in which to display values. (format: int32)
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "s" $s "multi") (serialize-qp "size" $size "scalar") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cat/thread_pool" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns cluster-wide thread pool statistics per node. By default the active, queued, and rejected statistics are returned for all thread pools.
#
# GET /_cat/thread_pool/{thread_pool_patterns}
# Docs: https://opensearch.org/docs/latest/api-reference/cat/cat-thread-pool/
# operationId: cat.thread_pool.1
@deprecated --flag master-timeout
export def "cat-thread-pool pool1" [
  thread_pool_patterns: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # A timeout for connection to the cluster manager node.
  --format: string # A short version of the `Accept` header, such as `json` or `yaml`.
  --h: list # A comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Returns help information. (default: false)
  --local: oneof<nothing, bool> # Returns local information but does not retrieve the state from the cluster manager node. (default: false)
  --master-timeout: string # The amount of time allowed to establish a connection to the cluster manager node. (DEPRECATED)
  --s: list # A comma-separated list of column names or column aliases to sort by.
  --size: int # The multiplier in which to display values. (format: int32)
  --v: oneof<nothing, bool> # Enables verbose mode, which displays column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "s" $s "multi") (serialize-qp "size" $size "scalar") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_cat/thread_pool/($thread_pool_patterns)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Explains how shards are allocated in the current cluster and provides an explanation for why unassigned shards can't be allocated to a node.
#
# GET /_cluster/allocation/explain
# Docs: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-allocation/
# operationId: cluster.allocation_explain.0
export def "cluster-allocation-explain explain0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --include-disk-info: oneof<nothing, bool> # When `true`, returns information about disk usage and shard sizes. (default: false)
  --include-yes-decisions: oneof<nothing, bool> # When `true`, returns any `YES` decisions in the allocation explanation. `YES` decisions indicate when a particular shard allocation attempt was successful for the given node. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --current-node: string # Specifies the node ID or the name of the node to only explain a shard that is currently located on the specified node.
  --index: string
  --primary: oneof<nothing, bool> # When `true`, returns a routing explanation for the primary shard based on the node ID.
  --shard: int # Specifies the ID of the shard that you would like an explanation for. (format: int32)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "include_disk_info" $include_disk_info "scalar") (serialize-qp "include_yes_decisions" $include_yes_decisions "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cluster/allocation/explain" $qp)
  let body = {current_node: $current_node, index: $index, primary: $primary, shard: $shard} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Explains how shards are allocated in the current cluster and provides an explanation for why unassigned shards can't be allocated to a node.
#
# POST /_cluster/allocation/explain
# Docs: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-allocation/
# operationId: cluster.allocation_explain.1
export def "cluster-allocation-explain explain1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --include-disk-info: oneof<nothing, bool> # When `true`, returns information about disk usage and shard sizes. (default: false)
  --include-yes-decisions: oneof<nothing, bool> # When `true`, returns any `YES` decisions in the allocation explanation. `YES` decisions indicate when a particular shard allocation attempt was successful for the given node. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --current-node: string # Specifies the node ID or the name of the node to only explain a shard that is currently located on the specified node.
  --index: string
  --primary: oneof<nothing, bool> # When `true`, returns a routing explanation for the primary shard based on the node ID.
  --shard: int # Specifies the ID of the shard that you would like an explanation for. (format: int32)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "include_disk_info" $include_disk_info "scalar") (serialize-qp "include_yes_decisions" $include_yes_decisions "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cluster/allocation/explain" $qp)
  let body = {current_node: $current_node, index: $index, primary: $primary, shard: $shard} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Recommissions a decommissioned zone.
#
# DELETE /_cluster/decommission/awareness
# Docs: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-decommission/#example-decommissioning-and-recommissioning-a-zone
# operationId: cluster.delete_decommission_awareness.0
export def "cluster-decommission-awareness awareness0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cluster/decommission/awareness" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves the decommission status for all zones.
#
# GET /_cluster/decommission/awareness/{awareness_attribute_name}/_status
# Docs: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-decommission/#example-getting-zone-decommission-status
# operationId: cluster.get_decommission_awareness.0
export def "cluster-decommission-awareness-status awareness0" [
  awareness_attribute_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_cluster/decommission/awareness/($awareness_attribute_name)/_status" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Decommissions a cluster zone based on awareness. This can greatly benefit multi-zone deployments, where awareness attributes can aid in applying new upgrades to a cluster in a controlled fashion.
#
# PUT /_cluster/decommission/awareness/{awareness_attribute_name}/{awareness_attribute_value}
# Docs: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-decommission/#example-decommissioning-and-recommissioning-a-zone
# operationId: cluster.put_decommission_awareness.0
export def "cluster-decommission-awareness awareness0-by-awareness_attribute_name-awareness_attribute_value" [
  awareness_attribute_name: string
  awareness_attribute_value: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_cluster/decommission/awareness/($awareness_attribute_name)/($awareness_attribute_value)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns basic information about the health of the cluster.
#
# GET /_cluster/health
# Docs: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-health/
# operationId: cluster.health.0
@deprecated --flag master-timeout
export def "cluster-health clusterhealth0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --awareness-attribute: string # The name of the awareness attribute for which to return the cluster health status (for example, `zone`). Applicable only if `level` is set to `awareness_attributes`.
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --expand-wildcards: string
  --level: string@level-completer
  --local: oneof<nothing, bool> # Whether to return information from the local node only instead of from the cluster manager node. (default: false)
  --master-timeout: string # DEPRECATED
  --timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --wait-for-active-shards: string
  --wait-for-events: string
  --wait-for-no-initializing-shards: oneof<nothing, bool> # Whether to wait until there are no initializing shards in the cluster. (default: false)
  --wait-for-no-relocating-shards: oneof<nothing, bool> # Whether to wait until there are no relocating shards in the cluster. (default: false)
  --wait-for-nodes: string # Waits until the specified number of nodes (`N`) is available. Accepts `>=N`, `<=N`, `>N`, and `<N`. You can also use `ge(N)`, `le(N)`, `gt(N)`, and `lt(N)` notation.
  --wait-for-status: string # Waits until the cluster health reaches the specified status or better.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "awareness_attribute" $awareness_attribute "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "wait_for_events" $wait_for_events "scalar") (serialize-qp "wait_for_no_initializing_shards" $wait_for_no_initializing_shards "scalar") (serialize-qp "wait_for_no_relocating_shards" $wait_for_no_relocating_shards "scalar") (serialize-qp "wait_for_nodes" $wait_for_nodes "scalar") (serialize-qp "wait_for_status" $wait_for_status "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cluster/health" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns basic information about the health of the cluster.
#
# GET /_cluster/health/{index}
# Docs: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-health/
# operationId: cluster.health.1
@deprecated --flag master-timeout
export def "cluster-health clusterhealth1" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --awareness-attribute: string # The name of the awareness attribute for which to return the cluster health status (for example, `zone`). Applicable only if `level` is set to `awareness_attributes`.
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --expand-wildcards: string
  --level: string@level-completer
  --local: oneof<nothing, bool> # Whether to return information from the local node only instead of from the cluster manager node. (default: false)
  --master-timeout: string # DEPRECATED
  --timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --wait-for-active-shards: string
  --wait-for-events: string
  --wait-for-no-initializing-shards: oneof<nothing, bool> # Whether to wait until there are no initializing shards in the cluster. (default: false)
  --wait-for-no-relocating-shards: oneof<nothing, bool> # Whether to wait until there are no relocating shards in the cluster. (default: false)
  --wait-for-nodes: string # Waits until the specified number of nodes (`N`) is available. Accepts `>=N`, `<=N`, `>N`, and `<N`. You can also use `ge(N)`, `le(N)`, `gt(N)`, and `lt(N)` notation.
  --wait-for-status: string # Waits until the cluster health reaches the specified status or better.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "awareness_attribute" $awareness_attribute "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "wait_for_events" $wait_for_events "scalar") (serialize-qp "wait_for_no_initializing_shards" $wait_for_no_initializing_shards "scalar") (serialize-qp "wait_for_no_relocating_shards" $wait_for_no_relocating_shards "scalar") (serialize-qp "wait_for_nodes" $wait_for_nodes "scalar") (serialize-qp "wait_for_status" $wait_for_status "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_cluster/health/($index)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about hot threads on each node in the cluster.
#
# GET /_cluster/nodes/{node_id}/hot_threads
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/api-reference/nodes-apis/nodes-hot-threads/
# operationId: nodes.hot_threads.2
@deprecated
export def "cluster-nodes-hot-threads threads2" [
  node_id: list
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --ignore-idle-threads: oneof<nothing, bool> # Whether to show threads that are in known-idle places, such as waiting on a socket select or pulling from an empty task queue. (default: true)
  --interval: string # The time interval between thread stack trace samples.
  --snapshots: int # The number of thread stack trace samples to collect. (format: int32, default: 10)
  --threads: int # The number of threads to provide information for. (format: int32, default: 3)
  --timeout: string # The amount of time to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --type: string # The type to sample.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "ignore_idle_threads" $ignore_idle_threads "scalar") (serialize-qp "interval" $interval "scalar") (serialize-qp "snapshots" $snapshots "scalar") (serialize-qp "threads" $threads "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_cluster/nodes/($node_id)/hot_threads" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about hot threads on each node in the cluster.
#
# GET /_cluster/nodes/hot_threads
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/api-reference/nodes-apis/nodes-hot-threads/
# operationId: nodes.hot_threads.0
@deprecated
export def "cluster-nodes-hot-threads threads0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --ignore-idle-threads: oneof<nothing, bool> # Whether to show threads that are in known-idle places, such as waiting on a socket select or pulling from an empty task queue. (default: true)
  --interval: string # The time interval between thread stack trace samples.
  --snapshots: int # The number of thread stack trace samples to collect. (format: int32, default: 10)
  --threads: int # The number of threads to provide information for. (format: int32, default: 3)
  --timeout: string # The amount of time to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --type: string # The type to sample.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "ignore_idle_threads" $ignore_idle_threads "scalar") (serialize-qp "interval" $interval "scalar") (serialize-qp "snapshots" $snapshots "scalar") (serialize-qp "threads" $threads "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cluster/nodes/hot_threads" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns a list of pending cluster-level tasks, such as index creation, mapping updates, or new allocations.
#
# GET /_cluster/pending_tasks
# Docs: https://opensearch.org/docs/latest
# operationId: cluster.pending_tasks.0
@deprecated --flag master-timeout
export def "cluster-pending-tasks tasks0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --local: oneof<nothing, bool> # When `true`, the request retrieves information from the local node only. When `false`, information is retrieved from the cluster manager node. (default: false)
  --master-timeout: string # DEPRECATED
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cluster/pending_tasks" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Allows to manually change the allocation of individual shards in the cluster.
#
# POST /_cluster/reroute
# Docs: https://opensearch.org/docs/latest
# operationId: cluster.reroute.0
# --commands item shape: {cancel?: record, move?: record, allocate_replica?: record, allocate_stale_primary?: record, allocate_empty_primary?: record}
@deprecated --flag master-timeout
export def "cluster-reroute clusterreroute0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --dry-run: oneof<nothing, bool> # When `true`, the request simulates the operation and returns the resulting state.
  --explain: oneof<nothing, bool> # When `true`, the response contains an explanation of why reroute certain commands can or cannot be executed.
  --master-timeout: string # DEPRECATED
  --metric: string # Limits the information returned to the specified metrics.
  --retry-failed: oneof<nothing, bool> # When `true`, retries shard allocation if it was blocked because of too many subsequent failures.
  --timeout: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --commands: list # Defines the reroute commands to perform, either `move`, `cancel`, or `allocate`. — item shape: {cancel?: record, move?: record, allocate_replica?: record, allocate_stale_primary?: record, allocate_empty_primary?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "dry_run" $dry_run "scalar") (serialize-qp "explain" $explain "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "metric" $metric "scalar") (serialize-qp "retry_failed" $retry_failed "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cluster/reroute" $qp)
  let body = {commands: $commands} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Fetches weighted shard routing weights.
#
# GET /_cluster/routing/awareness/{attribute}/weights
# Docs: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-awareness/#example-getting-weights-for-all-zones
# operationId: cluster.get_weighted_routing.0
export def "cluster-routing-awareness-weights routing0-by-attribute" [
  attribute: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_cluster/routing/awareness/($attribute)/weights" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Updates weighted shard routing weights.
#
# PUT /_cluster/routing/awareness/{attribute}/weights
# Docs: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-awareness/#example-weighted-round-robin-search
# operationId: cluster.put_weighted_routing.0
export def "cluster-routing-awareness-weights routing0-by-attribute-1" [
  attribute: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --version: int # format: int64
  --weights: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_cluster/routing/awareness/($attribute)/weights" $qp)
  let body = {_version: $version, weights: $weights} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete weighted shard routing weights.
#
# DELETE /_cluster/routing/awareness/weights
# Docs: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-awareness/#example-deleting-weights
# operationId: cluster.delete_weighted_routing.0
export def "cluster-routing-awareness-weights routing0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --version: int # format: int64
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cluster/routing/awareness/weights" $qp)
  let body = {_version: $version} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns cluster settings.
#
# GET /_cluster/settings
# Docs: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-settings/
# operationId: cluster.get_settings.0
@deprecated --flag master-timeout
export def "cluster-settings settings0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --flat-settings: oneof<nothing, bool> # Whether to return settings in the flat form, which can improve readability, especially for heavily nested settings. For example, the flat form of `"cluster": { "max_shards_per_node": 500 }` is `"cluster.max_shards_per_node": "500"`. (default: false)
  --include-defaults: oneof<nothing, bool> # When `true`, returns default cluster settings from the local node. (default: false)
  --master-timeout: string # DEPRECATED
  --timeout: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "include_defaults" $include_defaults "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cluster/settings" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Updates the cluster settings.
#
# PUT /_cluster/settings
# Docs: https://opensearch.org/docs/latest/api-reference/cluster-settings/
# operationId: cluster.put_settings.0
@deprecated --flag master-timeout
export def "cluster-settings settings0-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --flat-settings: oneof<nothing, bool> # Whether to return settings in the flat form, which can improve readability, especially for heavily nested settings. For example, the flat form of `"cluster": { "max_shards_per_node": 500 }` is `"cluster.max_shards_per_node": "500"`. (default: false)
  --master-timeout: string # DEPRECATED
  --timeout: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --persistent: record
  --transient: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cluster/settings" $qp)
  let body = {persistent: $persistent, transient: $transient} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns comprehensive information about the state of the cluster.
#
# GET /_cluster/state
# Docs: https://opensearch.org/docs/latest
# operationId: cluster.state.0
@deprecated --flag master-timeout
export def "cluster-state clusterstate0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # Whether to ignore a wildcard index expression that resolves into no concrete indexes. This includes the `_all` string or when no indexes have been specified.
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --expand-wildcards: string
  --flat-settings: oneof<nothing, bool> # Whether to return settings in the flat form, which can improve readability, especially for heavily nested settings. For example, the flat form of `"cluster": { "max_shards_per_node": 500 }` is `"cluster.max_shards_per_node": "500"`. (default: false)
  --ignore-unavailable: oneof<nothing, bool> # Whether the specified concrete indexes should be ignored when unavailable (missing or closed).
  --local: oneof<nothing, bool> # Whether to return information from the local node only instead of from the cluster manager node. (default: false)
  --master-timeout: string # DEPRECATED
  --wait-for-metadata-version: int # Wait for the metadata version to be equal or greater than the specified metadata version. (format: int64)
  --wait-for-timeout: string # The maximum time to wait for `wait_for_metadata_version` before timing out.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "wait_for_metadata_version" $wait_for_metadata_version "scalar") (serialize-qp "wait_for_timeout" $wait_for_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cluster/state" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns comprehensive information about the state of the cluster.
#
# GET /_cluster/state/{metric}
# Docs: https://opensearch.org/docs/latest
# operationId: cluster.state.1
@deprecated --flag master-timeout
export def "cluster-state clusterstate1" [
  metric: list
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # Whether to ignore a wildcard index expression that resolves into no concrete indexes. This includes the `_all` string or when no indexes have been specified.
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --expand-wildcards: string
  --flat-settings: oneof<nothing, bool> # Whether to return settings in the flat form, which can improve readability, especially for heavily nested settings. For example, the flat form of `"cluster": { "max_shards_per_node": 500 }` is `"cluster.max_shards_per_node": "500"`. (default: false)
  --ignore-unavailable: oneof<nothing, bool> # Whether the specified concrete indexes should be ignored when unavailable (missing or closed).
  --local: oneof<nothing, bool> # Whether to return information from the local node only instead of from the cluster manager node. (default: false)
  --master-timeout: string # DEPRECATED
  --wait-for-metadata-version: int # Wait for the metadata version to be equal or greater than the specified metadata version. (format: int64)
  --wait-for-timeout: string # The maximum time to wait for `wait_for_metadata_version` before timing out.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "wait_for_metadata_version" $wait_for_metadata_version "scalar") (serialize-qp "wait_for_timeout" $wait_for_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_cluster/state/($metric)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns comprehensive information about the state of the cluster.
#
# GET /_cluster/state/{metric}/{index}
# Docs: https://opensearch.org/docs/latest
# operationId: cluster.state.2
@deprecated --flag master-timeout
export def "cluster-state clusterstate2" [
  index: string
  metric: list
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # Whether to ignore a wildcard index expression that resolves into no concrete indexes. This includes the `_all` string or when no indexes have been specified.
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --expand-wildcards: string
  --flat-settings: oneof<nothing, bool> # Whether to return settings in the flat form, which can improve readability, especially for heavily nested settings. For example, the flat form of `"cluster": { "max_shards_per_node": 500 }` is `"cluster.max_shards_per_node": "500"`. (default: false)
  --ignore-unavailable: oneof<nothing, bool> # Whether the specified concrete indexes should be ignored when unavailable (missing or closed).
  --local: oneof<nothing, bool> # Whether to return information from the local node only instead of from the cluster manager node. (default: false)
  --master-timeout: string # DEPRECATED
  --wait-for-metadata-version: int # Wait for the metadata version to be equal or greater than the specified metadata version. (format: int64)
  --wait-for-timeout: string # The maximum time to wait for `wait_for_metadata_version` before timing out.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "wait_for_metadata_version" $wait_for_metadata_version "scalar") (serialize-qp "wait_for_timeout" $wait_for_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_cluster/state/($metric)/($index)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns a high-level overview of cluster statistics.
#
# GET /_cluster/stats
# Docs: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-stats/
# operationId: cluster.stats.0
export def "cluster-stats clusterstats0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --flat-settings: oneof<nothing, bool> # Whether to return settings in the flat form, which can improve readability, especially for heavily nested settings. For example, the flat form of `"cluster": { "max_shards_per_node": 500 }` is `"cluster.max_shards_per_node": "500"`. (default: false)
  --timeout: string # The amount of time to wait for each node to respond. If a node does not respond before its timeout expires, the response does not include its stats. However, timed out nodes are included in the response's `_nodes.failed` property. Defaults to no timeout.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cluster/stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns a high-level overview of cluster statistics.
#
# GET /_cluster/stats/{metric}/{index_metric}/nodes/{node_id}
# Docs: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-stats/
# operationId: cluster.stats.3
export def "cluster-stats-nodes clusterstats3" [
  index_metric: list
  metric: list
  node_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --flat-settings: oneof<nothing, bool> # Whether to return settings in the flat form, which can improve readability, especially for heavily nested settings. For example, the flat form of `"cluster": { "max_shards_per_node": 500 }` is `"cluster.max_shards_per_node": "500"`. (default: false)
  --timeout: string # The amount of time to wait for each node to respond. If a node does not respond before its timeout expires, the response does not include its stats. However, timed out nodes are included in the response's `_nodes.failed` property. Defaults to no timeout.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_cluster/stats/($metric)/($index_metric)/nodes/($node_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns a high-level overview of cluster statistics.
#
# GET /_cluster/stats/{metric}/nodes/{node_id}
# Docs: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-stats/
# operationId: cluster.stats.2
export def "cluster-stats-nodes clusterstats2" [
  metric: list
  node_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --flat-settings: oneof<nothing, bool> # Whether to return settings in the flat form, which can improve readability, especially for heavily nested settings. For example, the flat form of `"cluster": { "max_shards_per_node": 500 }` is `"cluster.max_shards_per_node": "500"`. (default: false)
  --timeout: string # The amount of time to wait for each node to respond. If a node does not respond before its timeout expires, the response does not include its stats. However, timed out nodes are included in the response's `_nodes.failed` property. Defaults to no timeout.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_cluster/stats/($metric)/nodes/($node_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns a high-level overview of cluster statistics.
#
# GET /_cluster/stats/nodes/{node_id}
# Docs: https://opensearch.org/docs/latest/api-reference/cluster-api/cluster-stats/
# operationId: cluster.stats.1
export def "cluster-stats-nodes clusterstats1" [
  node_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --flat-settings: oneof<nothing, bool> # Whether to return settings in the flat form, which can improve readability, especially for heavily nested settings. For example, the flat form of `"cluster": { "max_shards_per_node": 500 }` is `"cluster.max_shards_per_node": "500"`. (default: false)
  --timeout: string # The amount of time to wait for each node to respond. If a node does not respond before its timeout expires, the response does not include its stats. However, timed out nodes are included in the response's `_nodes.failed` property. Defaults to no timeout.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_cluster/stats/nodes/($node_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Clears any cluster voting configuration exclusions.
#
# DELETE /_cluster/voting_config_exclusions
# Docs: https://opensearch.org/docs/latest
# operationId: cluster.delete_voting_config_exclusions.0
export def "cluster-voting-config-exclusions exclusions0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --wait-for-removal: oneof<nothing, bool> # Specifies whether to wait for all excluded nodes to be removed from the cluster before clearing the voting configuration exclusions list. When `true`, all excluded nodes are removed from the cluster before this API takes any action. When `false`, the voting configuration exclusions list is cleared even if some excluded nodes are still in the cluster. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "wait_for_removal" $wait_for_removal "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cluster/voting_config_exclusions" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Updates the cluster voting configuration by excluding certain node IDs or names.
#
# POST /_cluster/voting_config_exclusions
# Docs: https://opensearch.org/docs/latest
# operationId: cluster.post_voting_config_exclusions.0
export def "cluster-voting-config-exclusions exclusions0-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --node-ids: string # A comma-separated list of node IDs to exclude from the voting configuration. When using this setting, you cannot also specify `node_names`. Either `node_ids` or `node_names` are required to receive a valid response.
  --node-names: string # A comma-separated list of node names to exclude from the voting configuration. When using this setting, you cannot also specify `node_ids`. Either `node_ids` or `node_names` are required to receive a valid response.
  --timeout: string # When adding a voting configuration exclusion, the API waits for the specified nodes to be excluded from the voting configuration before returning a response. If the timeout expires before the appropriate condition is satisfied, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "node_ids" $node_ids "scalar") (serialize-qp "node_names" $node_names "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cluster/voting_config_exclusions" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns one or more component templates.
#
# GET /_component_template
# Docs: https://opensearch.org/docs/latest
# operationId: cluster.get_component_template.0
@deprecated --flag master-timeout
export def "component-template template0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --flat-settings: oneof<nothing, bool> # Whether to return settings in the flat form, which can improve readability, especially for heavily nested settings. For example, the flat form of `"cluster": { "max_shards_per_node": 500 }` is `"cluster.max_shards_per_node": "500"`. (default: false)
  --local: oneof<nothing, bool> # When `true`, the request retrieves information from the local node only. When `false`, information is retrieved from the cluster manager node. (default: false)
  --master-timeout: string # DEPRECATED
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_component_template" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Deletes a component template.
#
# DELETE /_component_template/{name}
# Docs: https://opensearch.org/docs/latest
# operationId: cluster.delete_component_template.0
@deprecated --flag master-timeout
export def "component-template template0-by-name" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --master-timeout: string # DEPRECATED
  --timeout: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_component_template/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns one or more component templates.
#
# GET /_component_template/{name}
# Docs: https://opensearch.org/docs/latest
# operationId: cluster.get_component_template.1
@deprecated --flag master-timeout
export def "component-template template1-by-name" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --flat-settings: oneof<nothing, bool> # Whether to return settings in the flat form, which can improve readability, especially for heavily nested settings. For example, the flat form of `"cluster": { "max_shards_per_node": 500 }` is `"cluster.max_shards_per_node": "500"`. (default: false)
  --local: oneof<nothing, bool> # When `true`, the request retrieves information from the local node only. When `false`, information is retrieved from the cluster manager node. (default: false)
  --master-timeout: string # DEPRECATED
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_component_template/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about whether a particular component template exist.
#
# HEAD /_component_template/{name}
# Docs: https://opensearch.org/docs/latest
# operationId: cluster.exists_component_template.0
@deprecated --flag master-timeout
export def "component-template template0-by-name-1" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --local: oneof<nothing, bool> # When `true`, the request retrieves information from the local node only. When `false, information is retrieved from the cluster manager node. (default: false)
  --master-timeout: string # DEPRECATED
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_component_template/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or updates a component template.
#
# POST /_component_template/{name}
# Docs: https://opensearch.org/docs/latest/im-plugin/index-templates/#use-component-templates-to-create-an-index-template
# operationId: cluster.put_component_template.0
# --template shape: {aliases?: record, mappings?: record, settings?: record, defaults?: record, data_stream?: string}
@deprecated --flag master-timeout
export def "component-template template0-by-name-2" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --create: oneof<nothing, bool> # When `true`, this request cannot replace or update existing component templates. (default: false)
  --master-timeout: string # DEPRECATED
  --timeout: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --allow-auto-create: oneof<nothing, bool> # This setting overrides the `action.auto_create_index` cluster setting. When set to `true` in a component template, indexes are automatically created using that template. When set to `false`, data streams matching the component template must always be explicitly created.
  template: record # The state configuration of an index. — shape: {aliases?: record, mappings?: record, settings?: record, defaults?: record, data_stream?: string}
  --version: int # format: int64
  --meta: record # The custom metadata attached to a resource.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "create" $create "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_component_template/($name)" $qp)
  let body = {allow_auto_create: $allow_auto_create, template: $template, version: $version, _meta: $meta} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Creates or updates a component template.
#
# PUT /_component_template/{name}
# Docs: https://opensearch.org/docs/latest/im-plugin/index-templates/#use-component-templates-to-create-an-index-template
# operationId: cluster.put_component_template.1
# --template shape: {aliases?: record, mappings?: record, settings?: record, defaults?: record, data_stream?: string}
@deprecated --flag master-timeout
export def "component-template template1-by-name-1" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --create: oneof<nothing, bool> # When `true`, this request cannot replace or update existing component templates. (default: false)
  --master-timeout: string # DEPRECATED
  --timeout: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --allow-auto-create: oneof<nothing, bool> # This setting overrides the `action.auto_create_index` cluster setting. When set to `true` in a component template, indexes are automatically created using that template. When set to `false`, data streams matching the component template must always be explicitly created.
  template: record # The state configuration of an index. — shape: {aliases?: record, mappings?: record, settings?: record, defaults?: record, data_stream?: string}
  --version: int # format: int64
  --meta: record # The custom metadata attached to a resource.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "create" $create "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_component_template/($name)" $qp)
  let body = {allow_auto_create: $allow_auto_create, template: $template, version: $version, _meta: $meta} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns number of documents matching a query.
#
# GET /_count
# Docs: https://opensearch.org/docs/latest/api-reference/count/
# operationId: count.0
# --query shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
export def "count count0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --analyze-wildcard: oneof<nothing, bool> # If `true`, wildcard and prefix queries are analyzed. This parameter can only be used when the `q` query string parameter is specified. (default: false)
  --analyzer: string # Analyzer to use for the query string. This parameter can only be used when the `q` query string parameter is specified.
  --default-operator: string@default-operator-completer # The default operator for query string query: `AND` or `OR`. This parameter can only be used when the `q` query string parameter is specified.
  --df: string # Field to use as default where no field prefix is given in the query string. This parameter can only be used when the `q` query string parameter is specified.
  --expand-wildcards: string
  --ignore-throttled: oneof<nothing, bool> # If `true`, concrete, expanded or aliased indexes are ignored when frozen.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --lenient: oneof<nothing, bool> # If `true`, format-based query failures (such as providing text to a numeric field) in the query string will be ignored.
  --min-score: float # Sets the minimum `_score` value that documents must have to be included in the result. (format: float)
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --q: string # Query in the Lucene query string syntax.
  --routing: string # Custom value used to route operations to a specific shard.
  --terminate-after: int # Maximum number of documents to collect for each shard. If a query reaches this limit, OpenSearch terminates the query early. OpenSearch collects documents before sorting. (format: int32)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "analyze_wildcard" $analyze_wildcard "scalar") (serialize-qp "analyzer" $analyzer "scalar") (serialize-qp "default_operator" $default_operator "scalar") (serialize-qp "df" $df "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_throttled" $ignore_throttled "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "lenient" $lenient "scalar") (serialize-qp "min_score" $min_score "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "terminate_after" $terminate_after "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_count" $qp)
  let body = {query: $body_query} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns number of documents matching a query.
#
# POST /_count
# Docs: https://opensearch.org/docs/latest/api-reference/count/
# operationId: count.1
# --query shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
export def "count count1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --analyze-wildcard: oneof<nothing, bool> # If `true`, wildcard and prefix queries are analyzed. This parameter can only be used when the `q` query string parameter is specified. (default: false)
  --analyzer: string # Analyzer to use for the query string. This parameter can only be used when the `q` query string parameter is specified.
  --default-operator: string@default-operator-completer # The default operator for query string query: `AND` or `OR`. This parameter can only be used when the `q` query string parameter is specified.
  --df: string # Field to use as default where no field prefix is given in the query string. This parameter can only be used when the `q` query string parameter is specified.
  --expand-wildcards: string
  --ignore-throttled: oneof<nothing, bool> # If `true`, concrete, expanded or aliased indexes are ignored when frozen.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --lenient: oneof<nothing, bool> # If `true`, format-based query failures (such as providing text to a numeric field) in the query string will be ignored.
  --min-score: float # Sets the minimum `_score` value that documents must have to be included in the result. (format: float)
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --q: string # Query in the Lucene query string syntax.
  --routing: string # Custom value used to route operations to a specific shard.
  --terminate-after: int # Maximum number of documents to collect for each shard. If a query reaches this limit, OpenSearch terminates the query early. OpenSearch collects documents before sorting. (format: int32)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "analyze_wildcard" $analyze_wildcard "scalar") (serialize-qp "analyzer" $analyzer "scalar") (serialize-qp "default_operator" $default_operator "scalar") (serialize-qp "df" $df "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_throttled" $ignore_throttled "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "lenient" $lenient "scalar") (serialize-qp "min_score" $min_score "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "terminate_after" $terminate_after "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_count" $qp)
  let body = {query: $body_query} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns all dangling indexes.
#
# GET /_dangling
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/dangling-index/
# operationId: dangling_indices.list_dangling_indices.0
export def "dangling indices0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_dangling" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Deletes the specified dangling index.
#
# DELETE /_dangling/{index_uuid}
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/dangling-index/
# operationId: dangling_indices.delete_dangling_index.0
@deprecated --flag master-timeout
export def "dangling index0-by-index_uuid" [
  index_uuid: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --accept-data-loss: oneof<nothing, bool> # Must be set to true in order to delete the dangling index.
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --master-timeout: string # Specify timeout for connection to cluster manager. (DEPRECATED)
  --timeout: string # Explicit operation timeout.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "accept_data_loss" $accept_data_loss "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_dangling/($index_uuid)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Imports the specified dangling index.
#
# POST /_dangling/{index_uuid}
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/dangling-index/
# operationId: dangling_indices.import_dangling_index.0
@deprecated --flag master-timeout
export def "dangling index0-by-index_uuid-1" [
  index_uuid: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --accept-data-loss: oneof<nothing, bool> # Must be set to true in order to import the dangling index.
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --master-timeout: string # Specify timeout for connection to cluster manager. (DEPRECATED)
  --timeout: string # Explicit operation timeout.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "accept_data_loss" $accept_data_loss "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_dangling/($index_uuid)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns data streams.
#
# GET /_data_stream
# Docs: https://opensearch.org/docs/latest/im-plugin/data-streams/
# operationId: indices.get_data_stream.0
export def "data-stream stream0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_data_stream" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Provides statistics on operations happening in a data stream.
#
# GET /_data_stream/_stats
# Docs: https://opensearch.org/docs/latest/im-plugin/data-streams/
# operationId: indices.data_streams_stats.0
export def "data-stream-stats stats0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_data_stream/_stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Deletes a data stream.
#
# DELETE /_data_stream/{name}
# Docs: https://opensearch.org/docs/latest/im-plugin/data-streams/
# operationId: indices.delete_data_stream.0
export def "data-stream stream0-by-name" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_data_stream/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns data streams.
#
# GET /_data_stream/{name}
# Docs: https://opensearch.org/docs/latest/im-plugin/data-streams/
# operationId: indices.get_data_stream.1
export def "data-stream stream1" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_data_stream/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or updates a data stream.
#
# PUT /_data_stream/{name}
# Docs: https://opensearch.org/docs/latest/im-plugin/data-streams/
# operationId: indices.create_data_stream.0
export def "data-stream stream0-by-name-1" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_data_stream/($name)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Provides statistics on operations happening in a data stream.
#
# GET /_data_stream/{name}/_stats
# Docs: https://opensearch.org/docs/latest/im-plugin/data-streams/
# operationId: indices.data_streams_stats.1
export def "data-stream-stats stats1" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_data_stream/($name)/_stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Changes the number of requests per second for a particular Delete By Query operation.
#
# POST /_delete_by_query/{task_id}/_rethrottle
# Docs: https://opensearch.org/docs/latest
# operationId: delete_by_query_rethrottle.0
export def "delete-by-query-rethrottle rethrottle0" [
  task_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --requests-per-second: float # The throttle for this request in sub-requests per second. (format: float)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "requests_per_second" $requests_per_second "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_delete_by_query/($task_id)/_rethrottle" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns the information about the capabilities of fields among multiple indexes.
#
# GET /_field_caps
# Docs: https://opensearch.org/docs/latest/field-types/supported-field-types/alias/#using-aliases-in-field-capabilities-api-operations
# operationId: field_caps.0
# --index_filter shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
export def "field-caps caps0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with foo but no index starts with bar.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`.
  --qp-fields: string # Comma-separated list of fields to retrieve capabilities for. Wildcard (`*`) expressions are supported.
  --ignore-unavailable: oneof<nothing, bool> # If `true`, missing or closed indexes are not included in the response.
  --include-unmapped: oneof<nothing, bool> # If `true`, unmapped fields are included in the response. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-fields: any
  --index-filter: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "fields" $qp_fields "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "include_unmapped" $include_unmapped "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_field_caps" $qp)
  let body = {fields: $body_fields, index_filter: $index_filter} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns the information about the capabilities of fields among multiple indexes.
#
# POST /_field_caps
# Docs: https://opensearch.org/docs/latest/field-types/supported-field-types/alias/#using-aliases-in-field-capabilities-api-operations
# operationId: field_caps.1
# --index_filter shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
export def "field-caps caps1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with foo but no index starts with bar.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`.
  --qp-fields: string # Comma-separated list of fields to retrieve capabilities for. Wildcard (`*`) expressions are supported.
  --ignore-unavailable: oneof<nothing, bool> # If `true`, missing or closed indexes are not included in the response.
  --include-unmapped: oneof<nothing, bool> # If `true`, unmapped fields are included in the response. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-fields: any
  --index-filter: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "fields" $qp_fields "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "include_unmapped" $include_unmapped "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_field_caps" $qp)
  let body = {fields: $body_fields, index_filter: $index_filter} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Performs the flush operation on one or more indexes.
#
# GET /_flush
# Docs: https://opensearch.org/docs/latest
# operationId: indices.flush.0
export def "flush indicesflush0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --force: oneof<nothing, bool> # If `true`, the request forces a flush even if there are no changes to commit to the index.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --wait-if-ongoing: oneof<nothing, bool> # If `true`, the flush operation blocks until execution when another flush operation is running. If `false`, OpenSearch returns an error if you request a flush when another flush operation is running. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "force" $force "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "wait_if_ongoing" $wait_if_ongoing "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_flush" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Performs the flush operation on one or more indexes.
#
# POST /_flush
# Docs: https://opensearch.org/docs/latest
# operationId: indices.flush.1
export def "flush indicesflush1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --force: oneof<nothing, bool> # If `true`, the request forces a flush even if there are no changes to commit to the index.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --wait-if-ongoing: oneof<nothing, bool> # If `true`, the flush operation blocks until execution when another flush operation is running. If `false`, OpenSearch returns an error if you request a flush when another flush operation is running. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "force" $force "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "wait_if_ongoing" $wait_if_ongoing "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_flush" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Performs the force merge operation on one or more indexes.
#
# POST /_forcemerge
# Docs: https://opensearch.org/docs/latest
# operationId: indices.forcemerge.0
export def "forcemerge indicesforcemerge0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # Whether to ignore if a wildcard indexes expression resolves into no concrete indexes. (This includes `_all` string or when no indexes have been specified)
  --expand-wildcards: string # Whether to expand wildcard expression to concrete indexes that are open, closed or both.
  --flush: oneof<nothing, bool> # Specify whether the index should be flushed after performing the operation. (default: true)
  --ignore-unavailable: oneof<nothing, bool> # Whether specified concrete indexes should be ignored when unavailable (missing or closed)
  --max-num-segments: int # The number of larger segments into which smaller segments are merged. Set this parameter to 1 to merge all segments into one segment. The default behavior is to perform the merge as necessary. (format: int64)
  --only-expunge-deletes: oneof<nothing, bool> # Specify whether the operation should only expunge deleted documents
  --primary-only: oneof<nothing, bool> # Specify whether the operation should only perform on primary shards. Defaults to false. (default: false)
  --wait-for-completion: oneof<nothing, bool> # Should the request wait until the force merge is completed. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "flush" $flush "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "max_num_segments" $max_num_segments "scalar") (serialize-qp "only_expunge_deletes" $only_expunge_deletes "scalar") (serialize-qp "primary_only" $primary_only "scalar") (serialize-qp "wait_for_completion" $wait_for_completion "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_forcemerge" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns an index template.
#
# GET /_index_template
# Docs: https://opensearch.org/docs/latest/im-plugin/index-templates/
# operationId: indices.get_index_template.0
@deprecated --flag master-timeout
export def "index-template template0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --flat-settings: oneof<nothing, bool> # If `true`, returns settings in flat format. (default: false)
  --local: oneof<nothing, bool> # If `true`, the request retrieves information from the local node only. Defaults to false, which means information is retrieved from the cluster-manager node. (default: false)
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_index_template" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Simulate resolving the given template name or body.
#
# POST /_index_template/_simulate
# Docs: https://opensearch.org/docs/latest
# operationId: indices.simulate_template.0
# --template shape: {aliases?: record, mappings?: record, settings?: record}
# --data_stream shape: {hidden?: bool, allow_custom_routing?: bool, timestamp_field?: record}
@deprecated --flag master-timeout
export def "index-template-simulate template0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cause: string # User defined reason for dry-run creating the new template for simulation purposes. (default: false)
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --create: oneof<nothing, bool> # If `true`, the template passed in the body is only used if no existing templates match the same index patterns. If `false`, the simulation uses the template with the highest priority. Note that the template is not permanently added or updated in either case; it is only used for the simulation. (default: false)
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  index_patterns: any # A comma-separated list of aliases to retrieve. Supports wildcards (`*`).  To retrieve all aliases, omit this parameter or use `*` or `_all`.
  --composed-of: list # An ordered list of component template names. Component templates are merged in the order specified, meaning that the last component template specified has the highest precedence.
  --template: record # The summary configuration for an index template. — shape: {aliases?: record, mappings?: record, settings?: record}
  --version: int # format: int64
  --priority: int # The priority to determine index template precedence when a new data stream or index is created. The index template with the highest priority is chosen. If no priority is specified the template is treated as though it is of priority 0 (lowest priority). This number is not automatically generated by OpenSearch. (format: int64)
  --meta: record # The custom metadata attached to a resource.
  --allow-auto-create: oneof<nothing, bool> # Whether the template allows automatic index creation.
  --data-stream: record # The data stream configuration for an index template. — shape: {hidden?: bool, allow_custom_routing?: bool, timestamp_field?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cause" $cause "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "create" $create "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_index_template/_simulate" $qp)
  let body = {index_patterns: $index_patterns, composed_of: $composed_of, template: $template, version: $version, priority: $priority, _meta: $meta, allow_auto_create: $allow_auto_create, data_stream: $data_stream} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Simulate matching the given index name against the index templates in the system.
#
# POST /_index_template/_simulate_index/{name}
# Docs: https://opensearch.org/docs/latest
# operationId: indices.simulate_index_template.0
# --template shape: {aliases?: record, mappings?: record, settings?: record}
# --data_stream shape: {hidden?: bool, allow_custom_routing?: bool, timestamp_field?: record}
@deprecated --flag master-timeout
export def "index-template-simulate-index template0" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --allow-auto-create: oneof<nothing, bool> # This setting overrides the value of the `action.auto_create_index` cluster setting. If set to `true` in a template, then indexes can be automatically created using that template even if auto-creation of indexes is disabled using `actions.auto_create_index`. If set to `false`, then indexes or data streams matching the template must always be explicitly created, and may never be automatically created.
  --index-patterns: any # A comma-separated list of data streams, indexes, and aliases used to limit the request. Supports wildcards (`*`). To target all data streams and indexes, omit this parameter or use `*` or `_all`.
  --composed-of: list # An ordered list of component template names. Component templates are merged in the order specified, meaning that the last component template specified has the highest precedence.
  --template: record # shape: {aliases?: record, mappings?: record, settings?: record}
  --data-stream: record # The data stream configuration for an index template. — shape: {hidden?: bool, allow_custom_routing?: bool, timestamp_field?: record}
  --priority: int # Priority to determine index template precedence when a new data stream or index is created. The index template with the highest priority is chosen. If no priority is specified the template is treated as though it is of priority 0 (lowest priority). This number is not automatically generated by OpenSearch.
  --version: int # format: int64
  --meta: record # The custom metadata attached to a resource.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_index_template/_simulate_index/($name)" $qp)
  let body = {allow_auto_create: $allow_auto_create, index_patterns: $index_patterns, composed_of: $composed_of, template: $template, data_stream: $data_stream, priority: $priority, version: $version, _meta: $meta} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Simulate resolving the given template name or body.
#
# POST /_index_template/_simulate/{name}
# Docs: https://opensearch.org/docs/latest
# operationId: indices.simulate_template.1
# --template shape: {aliases?: record, mappings?: record, settings?: record}
# --data_stream shape: {hidden?: bool, allow_custom_routing?: bool, timestamp_field?: record}
@deprecated --flag master-timeout
export def "index-template-simulate template1" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cause: string # User defined reason for dry-run creating the new template for simulation purposes. (default: false)
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --create: oneof<nothing, bool> # If `true`, the template passed in the body is only used if no existing templates match the same index patterns. If `false`, the simulation uses the template with the highest priority. Note that the template is not permanently added or updated in either case; it is only used for the simulation. (default: false)
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  index_patterns: any # A comma-separated list of aliases to retrieve. Supports wildcards (`*`).  To retrieve all aliases, omit this parameter or use `*` or `_all`.
  --composed-of: list # An ordered list of component template names. Component templates are merged in the order specified, meaning that the last component template specified has the highest precedence.
  --template: record # The summary configuration for an index template. — shape: {aliases?: record, mappings?: record, settings?: record}
  --version: int # format: int64
  --priority: int # The priority to determine index template precedence when a new data stream or index is created. The index template with the highest priority is chosen. If no priority is specified the template is treated as though it is of priority 0 (lowest priority). This number is not automatically generated by OpenSearch. (format: int64)
  --meta: record # The custom metadata attached to a resource.
  --allow-auto-create: oneof<nothing, bool> # Whether the template allows automatic index creation.
  --data-stream: record # The data stream configuration for an index template. — shape: {hidden?: bool, allow_custom_routing?: bool, timestamp_field?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cause" $cause "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "create" $create "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_index_template/_simulate/($name)" $qp)
  let body = {index_patterns: $index_patterns, composed_of: $composed_of, template: $template, version: $version, priority: $priority, _meta: $meta, allow_auto_create: $allow_auto_create, data_stream: $data_stream} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes an index template.
#
# DELETE /_index_template/{name}
# Docs: https://opensearch.org/docs/latest/im-plugin/index-templates/#delete-a-template
# operationId: indices.delete_index_template.0
@deprecated --flag master-timeout
export def "index-template template0-by-name" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_index_template/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns an index template.
#
# GET /_index_template/{name}
# Docs: https://opensearch.org/docs/latest/im-plugin/index-templates/
# operationId: indices.get_index_template.1
@deprecated --flag master-timeout
export def "index-template template1-by-name" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --flat-settings: oneof<nothing, bool> # If `true`, returns settings in flat format. (default: false)
  --local: oneof<nothing, bool> # If `true`, the request retrieves information from the local node only. Defaults to false, which means information is retrieved from the cluster-manager node. (default: false)
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_index_template/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about whether a particular index template exists.
#
# HEAD /_index_template/{name}
# Docs: https://opensearch.org/docs/latest/im-plugin/index-templates/
# operationId: indices.exists_index_template.0
@deprecated --flag master-timeout
export def "index-template template0-by-name-1" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --flat-settings: oneof<nothing, bool> # Return settings in flat format. (default: false)
  --local: oneof<nothing, bool> # Return local information, do not retrieve the state from cluster-manager node. (default: false)
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_index_template/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or updates an index template.
#
# POST /_index_template/{name}
# Docs: https://opensearch.org/docs/latest/im-plugin/index-templates/
# operationId: indices.put_index_template.0
# --template shape: {aliases?: record, mappings?: record, settings?: record}
# --data_stream shape: {hidden?: bool, allow_custom_routing?: bool, timestamp_field?: record}
@deprecated --flag master-timeout
export def "index-template template0-by-name-2" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cause: string # User defined reason for creating/updating the index template. (default: false)
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --create: oneof<nothing, bool> # If `true`, this request cannot replace or update existing index templates. (default: false)
  --master-timeout: string # Operation timeout for connection to cluster-manager node. (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --index-patterns: any # A comma-separated list of data streams, indexes, and aliases used to limit the request. Supports wildcards (`*`). To target all data streams and indexes, omit this parameter or use `*` or `_all`.
  --composed-of: list # An ordered list of component template names. Component templates are merged in the order specified, meaning that the last component template specified has the highest precedence.
  --template: record # shape: {aliases?: record, mappings?: record, settings?: record}
  --data-stream: record # The data stream configuration for an index template. — shape: {hidden?: bool, allow_custom_routing?: bool, timestamp_field?: record}
  --priority: int # Priority to determine index template precedence when a new data stream or index is created. The index template with the highest priority is chosen. If no priority is specified the template is treated as though it is of priority 0 (lowest priority). This number is not automatically generated by OpenSearch.
  --version: int # format: int64
  --meta: record # The custom metadata attached to a resource.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cause" $cause "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "create" $create "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_index_template/($name)" $qp)
  let body = {index_patterns: $index_patterns, composed_of: $composed_of, template: $template, data_stream: $data_stream, priority: $priority, version: $version, _meta: $meta} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Creates or updates an index template.
#
# PUT /_index_template/{name}
# Docs: https://opensearch.org/docs/latest/im-plugin/index-templates/
# operationId: indices.put_index_template.1
# --template shape: {aliases?: record, mappings?: record, settings?: record}
# --data_stream shape: {hidden?: bool, allow_custom_routing?: bool, timestamp_field?: record}
@deprecated --flag master-timeout
export def "index-template template1-by-name-1" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cause: string # User defined reason for creating/updating the index template. (default: false)
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --create: oneof<nothing, bool> # If `true`, this request cannot replace or update existing index templates. (default: false)
  --master-timeout: string # Operation timeout for connection to cluster-manager node. (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --index-patterns: any # A comma-separated list of data streams, indexes, and aliases used to limit the request. Supports wildcards (`*`). To target all data streams and indexes, omit this parameter or use `*` or `_all`.
  --composed-of: list # An ordered list of component template names. Component templates are merged in the order specified, meaning that the last component template specified has the highest precedence.
  --template: record # shape: {aliases?: record, mappings?: record, settings?: record}
  --data-stream: record # The data stream configuration for an index template. — shape: {hidden?: bool, allow_custom_routing?: bool, timestamp_field?: record}
  --priority: int # Priority to determine index template precedence when a new data stream or index is created. The index template with the highest priority is chosen. If no priority is specified the template is treated as though it is of priority 0 (lowest priority). This number is not automatically generated by OpenSearch.
  --version: int # format: int64
  --meta: record # The custom metadata attached to a resource.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cause" $cause "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "create" $create "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_index_template/($name)" $qp)
  let body = {index_patterns: $index_patterns, composed_of: $composed_of, template: $template, data_stream: $data_stream, priority: $priority, version: $version, _meta: $meta} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns an ingest pipeline.
#
# GET /_ingest/pipeline
# Docs: https://opensearch.org/docs/latest/api-reference/ingest-apis/get-ingest/
# operationId: ingest.get_pipeline.0
@deprecated --flag master-timeout
export def "ingest-pipeline pipeline0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time allowed to establish a connection to the cluster manager node.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_ingest/pipeline" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Simulates an ingest pipeline with example documents.
#
# GET /_ingest/pipeline/_simulate
# Docs: https://opensearch.org/docs/latest/api-reference/ingest-apis/simulate-ingest/
# operationId: ingest.simulate.0
# --docs item shape: {_id?: string, _index?: string, _source: any}
# --pipeline shape: {description?: string, on_failure?: list, processors?: list, version?: int, _meta?: record}
export def "ingest-pipeline-simulate ingestsimulate0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --verbose: oneof<nothing, bool> # When `true`, the response includes output data for each processor in the pipeline (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --docs: list # A list of sample documents to test in the pipeline. — item shape: {_id?: string, _index?: string, _source: any}
  --pipeline: record # shape: {description?: string, on_failure?: list, processors?: list, version?: int, _meta?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "verbose" $verbose "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_ingest/pipeline/_simulate" $qp)
  let body = {docs: $docs, pipeline: $pipeline} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Simulates an ingest pipeline with example documents.
#
# POST /_ingest/pipeline/_simulate
# Docs: https://opensearch.org/docs/latest/api-reference/ingest-apis/simulate-ingest/
# operationId: ingest.simulate.1
# --docs item shape: {_id?: string, _index?: string, _source: any}
# --pipeline shape: {description?: string, on_failure?: list, processors?: list, version?: int, _meta?: record}
export def "ingest-pipeline-simulate ingestsimulate1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --verbose: oneof<nothing, bool> # When `true`, the response includes output data for each processor in the pipeline (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --docs: list # A list of sample documents to test in the pipeline. — item shape: {_id?: string, _index?: string, _source: any}
  --pipeline: record # shape: {description?: string, on_failure?: list, processors?: list, version?: int, _meta?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "verbose" $verbose "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_ingest/pipeline/_simulate" $qp)
  let body = {docs: $docs, pipeline: $pipeline} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes an ingest pipeline.
#
# DELETE /_ingest/pipeline/{id}
# Docs: https://opensearch.org/docs/latest/api-reference/ingest-apis/delete-ingest/
# operationId: ingest.delete_pipeline.0
@deprecated --flag master-timeout
export def "ingest-pipeline pipeline0-by-id" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time allowed to establish a connection to the cluster manager node.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # The amount of time to wait for a response.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_ingest/pipeline/($id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns an ingest pipeline.
#
# GET /_ingest/pipeline/{id}
# Docs: https://opensearch.org/docs/latest/api-reference/ingest-apis/get-ingest/
# operationId: ingest.get_pipeline.1
@deprecated --flag master-timeout
export def "ingest-pipeline pipeline1" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time allowed to establish a connection to the cluster manager node.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_ingest/pipeline/($id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or updates an ingest pipeline.
#
# PUT /_ingest/pipeline/{id}
# Docs: https://opensearch.org/docs/latest/api-reference/ingest-apis/create-update-ingest/
# operationId: ingest.put_pipeline.0
# --on_failure item shape: {attachment?: any, append?: any, csv?: any, convert?: any, date?: any, date_index_name?: any, dot_expander?: any, fail?: any, foreach?: any, json?: any, user_agent?: any, kv?: any, geoip?: any, grok?: any, gsub?: any, join?: any, lowercase?: any, remove?: any, rename?: any, script?: any, set?: any, sort?: any, split?: any, trim?: any, uppercase?: any, urldecode?: any, bytes?: any, dissect?: any, set_security_user?: any, pipeline?: any, drop?: any, circle?: any, text_embedding?: any}
# --processors item shape: {attachment?: any, append?: any, csv?: any, convert?: any, date?: any, date_index_name?: any, dot_expander?: any, fail?: any, foreach?: any, json?: any, user_agent?: any, kv?: any, geoip?: any, grok?: any, gsub?: any, join?: any, lowercase?: any, remove?: any, rename?: any, script?: any, set?: any, sort?: any, split?: any, trim?: any, uppercase?: any, urldecode?: any, bytes?: any, dissect?: any, set_security_user?: any, pipeline?: any, drop?: any, circle?: any, text_embedding?: any}
@deprecated --flag master-timeout
export def "ingest-pipeline pipeline0-by-id-1" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time allowed to establish a connection to the cluster manager node.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # The amount of time to wait for a response.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --meta: record # The custom metadata attached to a resource.
  --description: string # The description for the ingest pipeline.
  --on-failure: list # The new processor to run immediately after another processor fails. The processors specified in the `on_failure` parameter run sequentially in the order specified.  If no pipeline are specified in `on_failure` array is specified, OpenSearch will not attempt to run the pipeline's remaining processors. — item shape: {attachment?: any, append?: any, csv?: any, convert?: any, date?: any, date_index_name?: any, dot_expander?: any, fail?: any, foreach?: any, json?: any, user_agent?: any, kv?: any, geoip?: any, grok?: any, gsub?: any, join?: any, lowercase?: any, remove?: any, rename?: any, script?: any, set?: any, sort?: any, split?: any, trim?: any, uppercase?: any, urldecode?: any, bytes?: any, dissect?: any, set_security_user?: any, pipeline?: any, drop?: any, circle?: any, text_embedding?: any}
  --processors: list # The processors used to perform transformations on documents before indexing. Processors run sequentially in the order specified. — item shape: {attachment?: any, append?: any, csv?: any, convert?: any, date?: any, date_index_name?: any, dot_expander?: any, fail?: any, foreach?: any, json?: any, user_agent?: any, kv?: any, geoip?: any, grok?: any, gsub?: any, join?: any, lowercase?: any, remove?: any, rename?: any, script?: any, set?: any, sort?: any, split?: any, trim?: any, uppercase?: any, urldecode?: any, bytes?: any, dissect?: any, set_security_user?: any, pipeline?: any, drop?: any, circle?: any, text_embedding?: any}
  --version: int # format: int64
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_ingest/pipeline/($id)" $qp)
  let body = {_meta: $meta, description: $description, on_failure: $on_failure, processors: $processors, version: $version} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Simulates an ingest pipeline with example documents.
#
# GET /_ingest/pipeline/{id}/_simulate
# Docs: https://opensearch.org/docs/latest/api-reference/ingest-apis/simulate-ingest/
# operationId: ingest.simulate.2
# --docs item shape: {_id?: string, _index?: string, _source: any}
# --pipeline shape: {description?: string, on_failure?: list, processors?: list, version?: int, _meta?: record}
export def "ingest-pipeline-simulate ingestsimulate2" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --verbose: oneof<nothing, bool> # When `true`, the response includes output data for each processor in the pipeline (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --docs: list # A list of sample documents to test in the pipeline. — item shape: {_id?: string, _index?: string, _source: any}
  --pipeline: record # shape: {description?: string, on_failure?: list, processors?: list, version?: int, _meta?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "verbose" $verbose "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_ingest/pipeline/($id)/_simulate" $qp)
  let body = {docs: $docs, pipeline: $pipeline} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Simulates an ingest pipeline with example documents.
#
# POST /_ingest/pipeline/{id}/_simulate
# Docs: https://opensearch.org/docs/latest/api-reference/ingest-apis/simulate-ingest/
# operationId: ingest.simulate.3
# --docs item shape: {_id?: string, _index?: string, _source: any}
# --pipeline shape: {description?: string, on_failure?: list, processors?: list, version?: int, _meta?: record}
export def "ingest-pipeline-simulate ingestsimulate3" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --verbose: oneof<nothing, bool> # When `true`, the response includes output data for each processor in the pipeline (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --docs: list # A list of sample documents to test in the pipeline. — item shape: {_id?: string, _index?: string, _source: any}
  --pipeline: record # shape: {description?: string, on_failure?: list, processors?: list, version?: int, _meta?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "verbose" $verbose "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_ingest/pipeline/($id)/_simulate" $qp)
  let body = {docs: $docs, pipeline: $pipeline} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns a list of built-in grok patterns.
#
# GET /_ingest/processor/grok
# Docs: https://opensearch.org/docs/latest
# operationId: ingest.processor_grok.0
export def "ingest-processor-grok grok0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --s: oneof<nothing, bool> # Determines how to sort returned grok patterns by key name. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "s" $s "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_ingest/processor/grok" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves the top queries based on the given metric type (latency, CPU, or memory).
#
# GET /_insights/top_queries
# operationId: insights.top_queries.0
export def "insights-top-queries queries0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --type: string@type-completer # Get top n queries by a specific metric.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "type" $type "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_insights/top_queries" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns help for the List APIs.
#
# GET /_list
# Docs: https://opensearch.org/docs/latest/api-reference/list/index/
# operationId: list.help.0
export def "list listhelp0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_list" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns paginated information about indexes including number of primaries and replicas, document counts, disk size.
#
# GET /_list/indices
# Docs: https://opensearch.org/docs/latest/api-reference/list/list-indices/
# operationId: list.indices.0
@deprecated --flag master-timeout
export def "list-indices listindices0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --bytes: string # The unit used to display byte values.
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --expand-wildcards: string # The type of index that wildcard patterns can match.
  --format: string # A short version of the Accept header, such as `JSON`, `YAML`.
  --h: list # Comma-separated list of column names to display.
  --health: string # The health status used to limit returned indexes. By default, the response includes indexes of any health status.
  --help: oneof<nothing, bool> # Return help information. (default: false)
  --include-unloaded-segments: oneof<nothing, bool> # If `true`, the response includes information from segments that are not loaded into memory. (default: false)
  --local: oneof<nothing, bool> # Return local information, do not retrieve the state from cluster-manager node. (default: false)
  --master-timeout: string # Operation timeout for connection to cluster-manager node. (DEPRECATED)
  --next-token: string # Token to retrieve next page of indexes.
  --pri: oneof<nothing, bool> # If `true`, the response only includes information from primary shards. (default: false)
  --s: list # Comma-separated list of column names or column aliases to sort by.
  --size: int # Maximum number of indexes to be displayed in a page. (format: int32)
  --qp-sort: string@sort-completer # Defines order in which indexes will be displayed. Accepted values are `asc` and `desc`. If `desc`, most recently created indexes would be displayed first.
  --time: string # The unit used to display time values.
  --v: oneof<nothing, bool> # Verbose mode. Display column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "bytes" $bytes "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "health" $health "scalar") (serialize-qp "help" $help "scalar") (serialize-qp "include_unloaded_segments" $include_unloaded_segments "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "next_token" $next_token "scalar") (serialize-qp "pri" $pri "scalar") (serialize-qp "s" $s "multi") (serialize-qp "size" $size "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "time" $time "scalar") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_list/indices" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns paginated information about indexes including number of primaries and replicas, document counts, disk size.
#
# GET /_list/indices/{index}
# Docs: https://opensearch.org/docs/latest/api-reference/list/list-indices/
# operationId: list.indices.1
@deprecated --flag master-timeout
export def "list-indices listindices1" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --bytes: string # The unit used to display byte values.
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --expand-wildcards: string # The type of index that wildcard patterns can match.
  --format: string # A short version of the Accept header, such as `JSON`, `YAML`.
  --h: list # Comma-separated list of column names to display.
  --health: string # The health status used to limit returned indexes. By default, the response includes indexes of any health status.
  --help: oneof<nothing, bool> # Return help information. (default: false)
  --include-unloaded-segments: oneof<nothing, bool> # If `true`, the response includes information from segments that are not loaded into memory. (default: false)
  --local: oneof<nothing, bool> # Return local information, do not retrieve the state from cluster-manager node. (default: false)
  --master-timeout: string # Operation timeout for connection to cluster-manager node. (DEPRECATED)
  --next-token: string # Token to retrieve next page of indexes.
  --pri: oneof<nothing, bool> # If `true`, the response only includes information from primary shards. (default: false)
  --s: list # Comma-separated list of column names or column aliases to sort by.
  --size: int # Maximum number of indexes to be displayed in a page. (format: int32)
  --qp-sort: string@sort-completer # Defines order in which indexes will be displayed. Accepted values are `asc` and `desc`. If `desc`, most recently created indexes would be displayed first.
  --time: string # The unit used to display time values.
  --v: oneof<nothing, bool> # Verbose mode. Display column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "bytes" $bytes "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "health" $health "scalar") (serialize-qp "help" $help "scalar") (serialize-qp "include_unloaded_segments" $include_unloaded_segments "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "next_token" $next_token "scalar") (serialize-qp "pri" $pri "scalar") (serialize-qp "s" $s "multi") (serialize-qp "size" $size "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "time" $time "scalar") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_list/indices/($index)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns paginated details of shard allocation on nodes.
#
# GET /_list/shards
# Docs: https://opensearch.org/docs/latest/api-reference/list/list-shards/
# operationId: list.shards.0
@deprecated --flag master-timeout
export def "list-shards listshards0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --bytes: string # The unit used to display byte values.
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --format: string # A short version of the Accept header, such as `JSON`, `YAML`.
  --h: list # Comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Return help information. (default: false)
  --local: oneof<nothing, bool> # Return local information, do not retrieve the state from cluster-manager node. (default: false)
  --master-timeout: string # Operation timeout for connection to cluster-manager node. (DEPRECATED)
  --next-token: string # Token to retrieve next page of shards.
  --s: list # Comma-separated list of column names or column aliases to sort by.
  --size: int # Maximum number of shards to be displayed in a page. (format: int32)
  --qp-sort: string@sort-completer # Defines order in which shards will be displayed. Accepted values are `asc` and `desc`. If `desc`, most recently created shards would be displayed first.
  --time: string # The unit in which to display time values.
  --v: oneof<nothing, bool> # Verbose mode. Display column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "bytes" $bytes "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "next_token" $next_token "scalar") (serialize-qp "s" $s "multi") (serialize-qp "size" $size "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "time" $time "scalar") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_list/shards" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns paginated details of shard allocation on nodes.
#
# GET /_list/shards/{index}
# Docs: https://opensearch.org/docs/latest/api-reference/list/list-shards/
# operationId: list.shards.1
@deprecated --flag master-timeout
export def "list-shards listshards1" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --bytes: string # The unit used to display byte values.
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --format: string # A short version of the Accept header, such as `JSON`, `YAML`.
  --h: list # Comma-separated list of column names to display.
  --help: oneof<nothing, bool> # Return help information. (default: false)
  --local: oneof<nothing, bool> # Return local information, do not retrieve the state from cluster-manager node. (default: false)
  --master-timeout: string # Operation timeout for connection to cluster-manager node. (DEPRECATED)
  --next-token: string # Token to retrieve next page of shards.
  --s: list # Comma-separated list of column names or column aliases to sort by.
  --size: int # Maximum number of shards to be displayed in a page. (format: int32)
  --qp-sort: string@sort-completer # Defines order in which shards will be displayed. Accepted values are `asc` and `desc`. If `desc`, most recently created shards would be displayed first.
  --time: string # The unit in which to display time values.
  --v: oneof<nothing, bool> # Verbose mode. Display column headers. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "bytes" $bytes "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "h" $h "multi") (serialize-qp "help" $help "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "next_token" $next_token "scalar") (serialize-qp "s" $s "multi") (serialize-qp "size" $size "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "time" $time "scalar") (serialize-qp "v" $v "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_list/shards/($index)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Deletes the default feature store.
#
# DELETE /_ltr
# operationId: ltr.delete_default_store.0
export def "ltr store0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_ltr" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Lists all available feature stores.
#
# GET /_ltr
# operationId: ltr.list_stores.0
export def "ltr stores0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_ltr" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates the default feature store.
#
# PUT /_ltr
# operationId: ltr.create_default_store.0
export def "ltr store0-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_ltr" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves cache statistics for all feature stores.
#
# GET /_ltr/_cachestats
# operationId: ltr.cache_stats.0
export def "ltr-cachestats stats0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_ltr/_cachestats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Clears the store caches.
#
# POST /_ltr/_clearcache
# operationId: ltr.clear_cache.0
export def "ltr-clearcache cache0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_ltr/_clearcache" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Deletes a feature store with the specified name.
#
# DELETE /_ltr/{store}
# operationId: ltr.delete_store.0
export def "ltr store0-by-store" [
  store: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_ltr/($store)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Checks if a store exists.
#
# GET /_ltr/{store}
# operationId: ltr.get_store.0
export def "ltr store0-by-store-1" [
  store: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_ltr/($store)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates a new feature store with the specified name.
#
# PUT /_ltr/{store}
# operationId: ltr.create_store.0
export def "ltr store0-by-store-2" [
  store: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_ltr/($store)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Clears the store caches.
#
# POST /_ltr/{store}/_clearcache
# operationId: ltr.clear_cache.1
export def "ltr-clearcache cache1" [
  store: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_ltr/($store)/_clearcache" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns mappings for one or more indexes.
#
# GET /_mapping
# Docs: https://opensearch.org/docs/latest/field-types/index/#get-a-mapping
# operationId: indices.get_mapping.0
@deprecated --flag master-timeout
export def "mapping mapping0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --index: string # Comma-separated list of data streams, indexes, and aliases used to limit the request. Supports wildcards (`*`). To target all data streams and indexes, omit this parameter or use `*` or `_all`.
  --local: oneof<nothing, bool> # If `true`, the request retrieves information from the local node only. (default: false)
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "index" $index "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_mapping" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns mapping for one or more fields.
#
# GET /_mapping/field/{fields}
# Docs: https://opensearch.org/docs/latest/field-types/index/
# operationId: indices.get_field_mapping.0
export def "mapping-field mapping0" [
  fields: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --include-defaults: oneof<nothing, bool> # If `true`, return all default settings in the response.
  --local: oneof<nothing, bool> # If `true`, the request retrieves information from the local node only. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "include_defaults" $include_defaults "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_mapping/field/($fields)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Allows to get multiple documents in one request.
#
# GET /_mget
# Docs: https://opensearch.org/docs/latest/api-reference/document-apis/multi-get/
# operationId: mget.0
# --docs item shape: {_id: string, _index?: string, routing?: string, _source?: any, stored_fields?: any, version?: int, version_type?: any}
export def "mget mget0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: string # Set to `true` or `false` to return the `_source` field or not, or a list of fields to return.
  --source-excludes: string # A comma-separated list of source fields to exclude from the response. You can also use this parameter to exclude fields from the subset specified in `_source_includes` query parameter.
  --source-includes: string # A comma-separated list of source fields to include in the response. If this parameter is specified, only these source fields are returned. You can exclude fields from this subset using the `_source_excludes` query parameter. If the `_source` parameter is `false`, this parameter is ignored.
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --realtime: oneof<nothing, bool> # If `true`, the request is real-time as opposed to near-real-time.
  --refresh: string # If `true`, the request refreshes relevant shards before retrieving documents.
  --routing: string # Custom value used to route operations to a specific shard.
  --stored-fields: string # If `true`, retrieves the document fields stored in the index rather than the document `_source`.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --docs: list # The documents you want to retrieve. Required if no index is specified in the request URI. — item shape: {_id: string, _index?: string, routing?: string, _source?: any, stored_fields?: any, version?: int, version_type?: any}
  --ids: any
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "scalar") (serialize-qp "_source_excludes" $source_excludes "scalar") (serialize-qp "_source_includes" $source_includes "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "realtime" $realtime "scalar") (serialize-qp "refresh" $refresh "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "stored_fields" $stored_fields "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_mget" $qp)
  let body = {docs: $docs, ids: $ids} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Allows to get multiple documents in one request.
#
# POST /_mget
# Docs: https://opensearch.org/docs/latest/api-reference/document-apis/multi-get/
# operationId: mget.1
# --docs item shape: {_id: string, _index?: string, routing?: string, _source?: any, stored_fields?: any, version?: int, version_type?: any}
export def "mget mget1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: string # Set to `true` or `false` to return the `_source` field or not, or a list of fields to return.
  --source-excludes: string # A comma-separated list of source fields to exclude from the response. You can also use this parameter to exclude fields from the subset specified in `_source_includes` query parameter.
  --source-includes: string # A comma-separated list of source fields to include in the response. If this parameter is specified, only these source fields are returned. You can exclude fields from this subset using the `_source_excludes` query parameter. If the `_source` parameter is `false`, this parameter is ignored.
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --realtime: oneof<nothing, bool> # If `true`, the request is real-time as opposed to near-real-time.
  --refresh: string # If `true`, the request refreshes relevant shards before retrieving documents.
  --routing: string # Custom value used to route operations to a specific shard.
  --stored-fields: string # If `true`, retrieves the document fields stored in the index rather than the document `_source`.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --docs: list # The documents you want to retrieve. Required if no index is specified in the request URI. — item shape: {_id: string, _index?: string, routing?: string, _source?: any, stored_fields?: any, version?: int, version_type?: any}
  --ids: any
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "scalar") (serialize-qp "_source_excludes" $source_excludes "scalar") (serialize-qp "_source_includes" $source_includes "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "realtime" $realtime "scalar") (serialize-qp "refresh" $refresh "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "stored_fields" $stored_fields "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_mget" $qp)
  let body = {docs: $docs, ids: $ids} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Allows to execute several search operations in one request.
#
# GET /_msearch
# Docs: https://opensearch.org/docs/latest/api-reference/multi-search/
# operationId: msearch.0
export def "msearch msearch0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --ccs-minimize-roundtrips: oneof<nothing, bool> # If `true`, network round-trips between the coordinating node and remote clusters are minimized for cross-cluster search requests. (default: true)
  --max-concurrent-searches: int # Maximum number of concurrent searches the multi search API can execute. (format: int32)
  --max-concurrent-shard-requests: int # Maximum number of concurrent shard requests that each sub-search request executes per node. (format: int32, default: 5)
  --pre-filter-shard-size: int # Defines a threshold that enforces a pre-filter roundtrip to prefilter search shards based on query rewriting if the number of shards the search request expands to exceeds the threshold. This filter roundtrip can limit the number of shards significantly if for instance a shard can not match any documents based on its rewrite method i.e., if date filters are mandatory to match but the shard bounds and the query are disjoint. (format: int32)
  --rest-total-hits-as-int: oneof<nothing, bool> # If `true`, `hits.total` are returned as an integer in the response. Defaults to false, which returns an object. (default: false)
  --search-type: string # Indicates whether global term and document frequencies should be used when scoring returned documents.
  --typed-keys: oneof<nothing, bool> # Specifies whether aggregation and suggester names should be prefixed by their respective types in the response.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "ccs_minimize_roundtrips" $ccs_minimize_roundtrips "scalar") (serialize-qp "max_concurrent_searches" $max_concurrent_searches "scalar") (serialize-qp "max_concurrent_shard_requests" $max_concurrent_shard_requests "scalar") (serialize-qp "pre_filter_shard_size" $pre_filter_shard_size "scalar") (serialize-qp "rest_total_hits_as_int" $rest_total_hits_as_int "scalar") (serialize-qp "search_type" $search_type "scalar") (serialize-qp "typed_keys" $typed_keys "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_msearch" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/x-ndjson" $body
}

# Allows to execute several search operations in one request.
#
# POST /_msearch
# Docs: https://opensearch.org/docs/latest/api-reference/multi-search/
# operationId: msearch.1
export def "msearch msearch1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --ccs-minimize-roundtrips: oneof<nothing, bool> # If `true`, network round-trips between the coordinating node and remote clusters are minimized for cross-cluster search requests. (default: true)
  --max-concurrent-searches: int # Maximum number of concurrent searches the multi search API can execute. (format: int32)
  --max-concurrent-shard-requests: int # Maximum number of concurrent shard requests that each sub-search request executes per node. (format: int32, default: 5)
  --pre-filter-shard-size: int # Defines a threshold that enforces a pre-filter roundtrip to prefilter search shards based on query rewriting if the number of shards the search request expands to exceeds the threshold. This filter roundtrip can limit the number of shards significantly if for instance a shard can not match any documents based on its rewrite method i.e., if date filters are mandatory to match but the shard bounds and the query are disjoint. (format: int32)
  --rest-total-hits-as-int: oneof<nothing, bool> # If `true`, `hits.total` are returned as an integer in the response. Defaults to false, which returns an object. (default: false)
  --search-type: string # Indicates whether global term and document frequencies should be used when scoring returned documents.
  --typed-keys: oneof<nothing, bool> # Specifies whether aggregation and suggester names should be prefixed by their respective types in the response.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "ccs_minimize_roundtrips" $ccs_minimize_roundtrips "scalar") (serialize-qp "max_concurrent_searches" $max_concurrent_searches "scalar") (serialize-qp "max_concurrent_shard_requests" $max_concurrent_shard_requests "scalar") (serialize-qp "pre_filter_shard_size" $pre_filter_shard_size "scalar") (serialize-qp "rest_total_hits_as_int" $rest_total_hits_as_int "scalar") (serialize-qp "search_type" $search_type "scalar") (serialize-qp "typed_keys" $typed_keys "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_msearch" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/x-ndjson" $body
}

# Allows to execute several search template operations in one request.
#
# GET /_msearch/template
# Docs: https://opensearch.org/docs/latest/search-plugins/search-template/
# operationId: msearch_template.0
export def "msearch-template template0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --ccs-minimize-roundtrips: oneof<nothing, bool> # If `true`, network round-trips are minimized for cross-cluster search requests. (default: true)
  --max-concurrent-searches: int # Maximum number of concurrent searches the API can run. (format: int32)
  --rest-total-hits-as-int: oneof<nothing, bool> # If `true`, the response returns `hits.total` as an integer. If `false`, it returns `hits.total` as an object. (default: false)
  --search-type: string # The type of the search operation. Available options: `query_then_fetch`, `dfs_query_then_fetch`.
  --typed-keys: oneof<nothing, bool> # If `true`, the response prefixes aggregation and suggester names with their respective types.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "ccs_minimize_roundtrips" $ccs_minimize_roundtrips "scalar") (serialize-qp "max_concurrent_searches" $max_concurrent_searches "scalar") (serialize-qp "rest_total_hits_as_int" $rest_total_hits_as_int "scalar") (serialize-qp "search_type" $search_type "scalar") (serialize-qp "typed_keys" $typed_keys "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_msearch/template" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/x-ndjson" $body
}

# Allows to execute several search template operations in one request.
#
# POST /_msearch/template
# Docs: https://opensearch.org/docs/latest/search-plugins/search-template/
# operationId: msearch_template.1
export def "msearch-template template1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --ccs-minimize-roundtrips: oneof<nothing, bool> # If `true`, network round-trips are minimized for cross-cluster search requests. (default: true)
  --max-concurrent-searches: int # Maximum number of concurrent searches the API can run. (format: int32)
  --rest-total-hits-as-int: oneof<nothing, bool> # If `true`, the response returns `hits.total` as an integer. If `false`, it returns `hits.total` as an object. (default: false)
  --search-type: string # The type of the search operation. Available options: `query_then_fetch`, `dfs_query_then_fetch`.
  --typed-keys: oneof<nothing, bool> # If `true`, the response prefixes aggregation and suggester names with their respective types.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "ccs_minimize_roundtrips" $ccs_minimize_roundtrips "scalar") (serialize-qp "max_concurrent_searches" $max_concurrent_searches "scalar") (serialize-qp "rest_total_hits_as_int" $rest_total_hits_as_int "scalar") (serialize-qp "search_type" $search_type "scalar") (serialize-qp "typed_keys" $typed_keys "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_msearch/template" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/x-ndjson" $body
}

# Returns multiple termvectors in one request.
#
# GET /_mtermvectors
# Docs: https://opensearch.org/docs/latest
# operationId: mtermvectors.0
# --docs item shape: {_id: string, _index?: string, doc?: any, fields?: any, field_statistics?: bool, filter?: record, offsets?: bool, payloads?: bool, positions?: bool, routing?: string, term_statistics?: bool, version?: int, version_type?: any}
export def "mtermvectors mtermvectors0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --field-statistics: oneof<nothing, bool> # If `true`, the response includes the document count, sum of document frequencies, and sum of total term frequencies. (default: true)
  --qp-fields: string # Comma-separated list or wildcard expressions of fields to include in the statistics. Used as the default list unless a specific field list is provided in the `completion_fields` or `fielddata_fields` parameters.
  --ids: list # A comma-separated list of documents ids. You must define ids as parameter or set "ids" or "docs" in the request body
  --offsets: oneof<nothing, bool> # If `true`, the response includes term offsets. (default: true)
  --payloads: oneof<nothing, bool> # If `true`, the response includes term payloads. (default: true)
  --positions: oneof<nothing, bool> # If `true`, the response includes term positions. (default: true)
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --realtime: oneof<nothing, bool> # If `true`, the request is real-time as opposed to near-real-time. (default: true)
  --routing: string # Custom value used to route operations to a specific shard.
  --term-statistics: oneof<nothing, bool> # If `true`, the response includes term frequency and document frequency. (default: false)
  --version: int # If `true`, returns the document version as part of a hit. (format: int64)
  --version-type: string # Specific version type.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --docs: list # Array of existing or artificial documents. — item shape: {_id: string, _index?: string, doc?: any, fields?: any, field_statistics?: bool, filter?: record, offsets?: bool, payloads?: bool, positions?: bool, routing?: string, term_statistics?: bool, version?: int, version_type?: any}
  --ids: list # Simplified syntax to specify documents by their ID if they're in the same index.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "field_statistics" $field_statistics "scalar") (serialize-qp "fields" $qp_fields "scalar") (serialize-qp "ids" $ids "multi") (serialize-qp "offsets" $offsets "scalar") (serialize-qp "payloads" $payloads "scalar") (serialize-qp "positions" $positions "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "realtime" $realtime "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "term_statistics" $term_statistics "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "version_type" $version_type "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_mtermvectors" $qp)
  let body = {docs: $docs, ids: $ids} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns multiple termvectors in one request.
#
# POST /_mtermvectors
# Docs: https://opensearch.org/docs/latest
# operationId: mtermvectors.1
# --docs item shape: {_id: string, _index?: string, doc?: any, fields?: any, field_statistics?: bool, filter?: record, offsets?: bool, payloads?: bool, positions?: bool, routing?: string, term_statistics?: bool, version?: int, version_type?: any}
export def "mtermvectors mtermvectors1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --field-statistics: oneof<nothing, bool> # If `true`, the response includes the document count, sum of document frequencies, and sum of total term frequencies. (default: true)
  --qp-fields: string # Comma-separated list or wildcard expressions of fields to include in the statistics. Used as the default list unless a specific field list is provided in the `completion_fields` or `fielddata_fields` parameters.
  --ids: list # A comma-separated list of documents ids. You must define ids as parameter or set "ids" or "docs" in the request body
  --offsets: oneof<nothing, bool> # If `true`, the response includes term offsets. (default: true)
  --payloads: oneof<nothing, bool> # If `true`, the response includes term payloads. (default: true)
  --positions: oneof<nothing, bool> # If `true`, the response includes term positions. (default: true)
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --realtime: oneof<nothing, bool> # If `true`, the request is real-time as opposed to near-real-time. (default: true)
  --routing: string # Custom value used to route operations to a specific shard.
  --term-statistics: oneof<nothing, bool> # If `true`, the response includes term frequency and document frequency. (default: false)
  --version: int # If `true`, returns the document version as part of a hit. (format: int64)
  --version-type: string # Specific version type.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --docs: list # Array of existing or artificial documents. — item shape: {_id: string, _index?: string, doc?: any, fields?: any, field_statistics?: bool, filter?: record, offsets?: bool, payloads?: bool, positions?: bool, routing?: string, term_statistics?: bool, version?: int, version_type?: any}
  --ids: list # Simplified syntax to specify documents by their ID if they're in the same index.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "field_statistics" $field_statistics "scalar") (serialize-qp "fields" $qp_fields "scalar") (serialize-qp "ids" $ids "multi") (serialize-qp "offsets" $offsets "scalar") (serialize-qp "payloads" $payloads "scalar") (serialize-qp "positions" $positions "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "realtime" $realtime "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "term_statistics" $term_statistics "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "version_type" $version_type "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_mtermvectors" $qp)
  let body = {docs: $docs, ids: $ids} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns information about nodes in the cluster.
#
# GET /_nodes
# Docs: https://opensearch.org/docs/latest/api-reference/nodes-apis/nodes-info/
# operationId: nodes.info.0
export def "nodes nodesinfo0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --flat-settings: oneof<nothing, bool> # When `true`, returns settings in flat format. (default: false)
  --timeout: string # The amount of time to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_nodes" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about nodes in the cluster.
#
# GET /_nodes/{node_id_or_metric}
# Docs: https://opensearch.org/docs/latest/api-reference/nodes-apis/nodes-info/
# operationId: nodes.info.1
export def "nodes nodesinfo1" [
  node_id_or_metric: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --flat-settings: oneof<nothing, bool> # When `true`, returns settings in flat format. (default: false)
  --timeout: string # The amount of time to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_nodes/($node_id_or_metric)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about nodes in the cluster.
#
# GET /_nodes/{node_id}/{metric}
# Docs: https://opensearch.org/docs/latest/api-reference/nodes-apis/nodes-info/
# operationId: nodes.info.3
export def "nodes nodesinfo3" [
  metric: list
  node_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --flat-settings: oneof<nothing, bool> # When `true`, returns settings in flat format. (default: false)
  --timeout: string # The amount of time to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_nodes/($node_id)/($metric)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about hot threads on each node in the cluster.
#
# GET /_nodes/{node_id}/hot_threads
# Docs: https://opensearch.org/docs/latest/api-reference/nodes-apis/nodes-hot-threads/
# operationId: nodes.hot_threads.6
export def "nodes-hot-threads threads6" [
  node_id: list
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --ignore-idle-threads: oneof<nothing, bool> # Whether to show threads that are in known-idle places, such as waiting on a socket select or pulling from an empty task queue. (default: true)
  --interval: string # The time interval between thread stack trace samples.
  --snapshots: int # The number of thread stack trace samples to collect. (format: int32, default: 10)
  --threads: int # The number of threads to provide information for. (format: int32, default: 3)
  --timeout: string # The amount of time to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --type: string # The type to sample.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "ignore_idle_threads" $ignore_idle_threads "scalar") (serialize-qp "interval" $interval "scalar") (serialize-qp "snapshots" $snapshots "scalar") (serialize-qp "threads" $threads "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_nodes/($node_id)/hot_threads" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Reloads secure settings.
#
# POST /_nodes/{node_id}/reload_secure_settings
# Docs: https://opensearch.org/docs/latest/api-reference/nodes-apis/nodes-reload-secure/
# operationId: nodes.reload_secure_settings.1
export def "nodes-reload-secure-settings settings1" [
  node_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --timeout: string # The amount of time to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --secure-settings-password: string # The password for authentication.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_nodes/($node_id)/reload_secure_settings" $qp)
  let body = {secure_settings_password: $secure_settings_password} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns statistical information about nodes in the cluster.
#
# GET /_nodes/{node_id}/stats
# Docs: https://opensearch.org/docs/latest/api-reference/nodes-apis/nodes-usage/
# operationId: nodes.stats.3
export def "nodes-stats nodesstats3" [
  node_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --completion-fields: string # A comma-separated list or wildcard expressions of fields to include in field data and suggest statistics.
  --fielddata-fields: string # A comma-separated list or wildcard expressions of fields to include in field data statistics.
  --qp-fields: string # A comma-separated list or wildcard expressions of fields to include in the statistics.
  --groups: list # A comma-separated list of search groups to include in the search statistics.
  --include-segment-file-sizes: oneof<nothing, bool> # When `true`,  reports the aggregated disk usage of each one of the Lucene index files (only applies if segment stats are requested). (default: false)
  --level: string@level-completer-1 # Indicates whether statistics are aggregated at the cluster, index, or shard level.
  --timeout: string # The amount of time to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --types: list # A comma-separated list of document types for the indexing index metric.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "completion_fields" $completion_fields "scalar") (serialize-qp "fielddata_fields" $fielddata_fields "scalar") (serialize-qp "fields" $qp_fields "scalar") (serialize-qp "groups" $groups "multi") (serialize-qp "include_segment_file_sizes" $include_segment_file_sizes "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "types" $types "multi") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_nodes/($node_id)/stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns statistical information about nodes in the cluster.
#
# GET /_nodes/{node_id}/stats/{metric}
# Docs: https://opensearch.org/docs/latest/api-reference/nodes-apis/nodes-usage/
# operationId: nodes.stats.4
export def "nodes-stats nodesstats4" [
  metric: list
  node_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --completion-fields: string # A comma-separated list or wildcard expressions of fields to include in field data and suggest statistics.
  --fielddata-fields: string # A comma-separated list or wildcard expressions of fields to include in field data statistics.
  --qp-fields: string # A comma-separated list or wildcard expressions of fields to include in the statistics.
  --groups: list # A comma-separated list of search groups to include in the search statistics.
  --include-segment-file-sizes: oneof<nothing, bool> # When `true`,  reports the aggregated disk usage of each one of the Lucene index files (only applies if segment stats are requested). (default: false)
  --level: string@level-completer-1 # Indicates whether statistics are aggregated at the cluster, index, or shard level.
  --timeout: string # The amount of time to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --types: list # A comma-separated list of document types for the indexing index metric.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "completion_fields" $completion_fields "scalar") (serialize-qp "fielddata_fields" $fielddata_fields "scalar") (serialize-qp "fields" $qp_fields "scalar") (serialize-qp "groups" $groups "multi") (serialize-qp "include_segment_file_sizes" $include_segment_file_sizes "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "types" $types "multi") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_nodes/($node_id)/stats/($metric)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns statistical information about nodes in the cluster.
#
# GET /_nodes/{node_id}/stats/{metric}/{index_metric}
# Docs: https://opensearch.org/docs/latest/api-reference/nodes-apis/nodes-usage/
# operationId: nodes.stats.5
export def "nodes-stats nodesstats5" [
  index_metric: list
  metric: list
  node_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --completion-fields: string # A comma-separated list or wildcard expressions of fields to include in field data and suggest statistics.
  --fielddata-fields: string # A comma-separated list or wildcard expressions of fields to include in field data statistics.
  --qp-fields: string # A comma-separated list or wildcard expressions of fields to include in the statistics.
  --groups: list # A comma-separated list of search groups to include in the search statistics.
  --include-segment-file-sizes: oneof<nothing, bool> # When `true`,  reports the aggregated disk usage of each one of the Lucene index files (only applies if segment stats are requested). (default: false)
  --level: string@level-completer-1 # Indicates whether statistics are aggregated at the cluster, index, or shard level.
  --timeout: string # The amount of time to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --types: list # A comma-separated list of document types for the indexing index metric.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "completion_fields" $completion_fields "scalar") (serialize-qp "fielddata_fields" $fielddata_fields "scalar") (serialize-qp "fields" $qp_fields "scalar") (serialize-qp "groups" $groups "multi") (serialize-qp "include_segment_file_sizes" $include_segment_file_sizes "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "types" $types "multi") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_nodes/($node_id)/stats/($metric)/($index_metric)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns low-level information about REST actions usage on nodes.
#
# GET /_nodes/{node_id}/usage
# Docs: https://opensearch.org/docs/latest
# operationId: nodes.usage.2
export def "nodes-usage nodesusage2" [
  node_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_nodes/($node_id)/usage" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns low-level information about REST actions usage on nodes.
#
# GET /_nodes/{node_id}/usage/{metric}
# Docs: https://opensearch.org/docs/latest
# operationId: nodes.usage.3
export def "nodes-usage nodesusage3" [
  metric: list
  node_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_nodes/($node_id)/usage/($metric)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about hot threads on each node in the cluster.
#
# GET /_nodes/hot_threads
# Docs: https://opensearch.org/docs/latest/api-reference/nodes-apis/nodes-hot-threads/
# operationId: nodes.hot_threads.4
export def "nodes-hot-threads threads4" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --ignore-idle-threads: oneof<nothing, bool> # Whether to show threads that are in known-idle places, such as waiting on a socket select or pulling from an empty task queue. (default: true)
  --interval: string # The time interval between thread stack trace samples.
  --snapshots: int # The number of thread stack trace samples to collect. (format: int32, default: 10)
  --threads: int # The number of threads to provide information for. (format: int32, default: 3)
  --timeout: string # The amount of time to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --type: string # The type to sample.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "ignore_idle_threads" $ignore_idle_threads "scalar") (serialize-qp "interval" $interval "scalar") (serialize-qp "snapshots" $snapshots "scalar") (serialize-qp "threads" $threads "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_nodes/hot_threads" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Reloads secure settings.
#
# POST /_nodes/reload_secure_settings
# Docs: https://opensearch.org/docs/latest/api-reference/nodes-apis/nodes-reload-secure/
# operationId: nodes.reload_secure_settings.0
export def "nodes-reload-secure-settings settings0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --timeout: string # The amount of time to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --secure-settings-password: string # The password for authentication.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_nodes/reload_secure_settings" $qp)
  let body = {secure_settings_password: $secure_settings_password} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns statistical information about nodes in the cluster.
#
# GET /_nodes/stats
# Docs: https://opensearch.org/docs/latest/api-reference/nodes-apis/nodes-usage/
# operationId: nodes.stats.0
export def "nodes-stats nodesstats0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --completion-fields: string # A comma-separated list or wildcard expressions of fields to include in field data and suggest statistics.
  --fielddata-fields: string # A comma-separated list or wildcard expressions of fields to include in field data statistics.
  --qp-fields: string # A comma-separated list or wildcard expressions of fields to include in the statistics.
  --groups: list # A comma-separated list of search groups to include in the search statistics.
  --include-segment-file-sizes: oneof<nothing, bool> # When `true`,  reports the aggregated disk usage of each one of the Lucene index files (only applies if segment stats are requested). (default: false)
  --level: string@level-completer-1 # Indicates whether statistics are aggregated at the cluster, index, or shard level.
  --timeout: string # The amount of time to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --types: list # A comma-separated list of document types for the indexing index metric.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "completion_fields" $completion_fields "scalar") (serialize-qp "fielddata_fields" $fielddata_fields "scalar") (serialize-qp "fields" $qp_fields "scalar") (serialize-qp "groups" $groups "multi") (serialize-qp "include_segment_file_sizes" $include_segment_file_sizes "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "types" $types "multi") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_nodes/stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns statistical information about nodes in the cluster.
#
# GET /_nodes/stats/{metric}
# Docs: https://opensearch.org/docs/latest/api-reference/nodes-apis/nodes-usage/
# operationId: nodes.stats.1
export def "nodes-stats nodesstats1" [
  metric: list
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --completion-fields: string # A comma-separated list or wildcard expressions of fields to include in field data and suggest statistics.
  --fielddata-fields: string # A comma-separated list or wildcard expressions of fields to include in field data statistics.
  --qp-fields: string # A comma-separated list or wildcard expressions of fields to include in the statistics.
  --groups: list # A comma-separated list of search groups to include in the search statistics.
  --include-segment-file-sizes: oneof<nothing, bool> # When `true`,  reports the aggregated disk usage of each one of the Lucene index files (only applies if segment stats are requested). (default: false)
  --level: string@level-completer-1 # Indicates whether statistics are aggregated at the cluster, index, or shard level.
  --timeout: string # The amount of time to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --types: list # A comma-separated list of document types for the indexing index metric.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "completion_fields" $completion_fields "scalar") (serialize-qp "fielddata_fields" $fielddata_fields "scalar") (serialize-qp "fields" $qp_fields "scalar") (serialize-qp "groups" $groups "multi") (serialize-qp "include_segment_file_sizes" $include_segment_file_sizes "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "types" $types "multi") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_nodes/stats/($metric)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns statistical information about nodes in the cluster.
#
# GET /_nodes/stats/{metric}/{index_metric}
# Docs: https://opensearch.org/docs/latest/api-reference/nodes-apis/nodes-usage/
# operationId: nodes.stats.2
export def "nodes-stats nodesstats2" [
  index_metric: list
  metric: list
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --completion-fields: string # A comma-separated list or wildcard expressions of fields to include in field data and suggest statistics.
  --fielddata-fields: string # A comma-separated list or wildcard expressions of fields to include in field data statistics.
  --qp-fields: string # A comma-separated list or wildcard expressions of fields to include in the statistics.
  --groups: list # A comma-separated list of search groups to include in the search statistics.
  --include-segment-file-sizes: oneof<nothing, bool> # When `true`,  reports the aggregated disk usage of each one of the Lucene index files (only applies if segment stats are requested). (default: false)
  --level: string@level-completer-1 # Indicates whether statistics are aggregated at the cluster, index, or shard level.
  --timeout: string # The amount of time to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --types: list # A comma-separated list of document types for the indexing index metric.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "completion_fields" $completion_fields "scalar") (serialize-qp "fielddata_fields" $fielddata_fields "scalar") (serialize-qp "fields" $qp_fields "scalar") (serialize-qp "groups" $groups "multi") (serialize-qp "include_segment_file_sizes" $include_segment_file_sizes "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "types" $types "multi") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_nodes/stats/($metric)/($index_metric)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns low-level information about REST actions usage on nodes.
#
# GET /_nodes/usage
# Docs: https://opensearch.org/docs/latest
# operationId: nodes.usage.0
export def "nodes-usage nodesusage0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_nodes/usage" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns low-level information about REST actions usage on nodes.
#
# GET /_nodes/usage/{metric}
# Docs: https://opensearch.org/docs/latest
# operationId: nodes.usage.1
export def "nodes-usage nodesusage1" [
  metric: list
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_nodes/usage/($metric)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves information about the SSL configuration.
#
# GET /_opendistro/_security/sslinfo
# operationId: security.get_sslinfo.0
export def "opendistro-security-sslinfo sslinfo0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --show-dn: oneof<nothing, bool> # Whether to include all domain names in the response.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "show_dn" $show_dn "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/sslinfo" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Performs an asynchronous search.
#
# POST /_plugins/_asynchronous_search
# Docs: https://opensearch.org/docs/latest/search-plugins/async/index/#rest-api
# operationId: asynchronous_search.search.0
# --collapse shape: {field: string, inner_hits?: any, max_concurrent_group_searches?: int}
# --rank shape: {rrf?: any}
# --post_filter shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
# --query shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
# --slice shape: {field?: string, id: int, max: int}
# --suggest shape: {text?: string}
# --pit shape: {id: string, keep_alive?: string}
export def "plugins-asynchronous-search searchsearch0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --index: string # The name of the index to be searched. Can be an individual name, a comma-separated list of indexes, or a wildcard expression of index names.
  --keep-alive: string # The amount of time that the result is saved in the cluster. For example, `2d` means that the results are stored in the cluster for 48 hours.  The saved search results are deleted after this period or if the search is canceled. Note that this includes the query execution time.  If the query exceeds this amount of time, the process cancels this query automatically.
  --keep-on-completion: oneof<nothing, bool> # Whether to save the results in the cluster after the search is complete. You can examine the stored results at a later time.
  --wait-for-completion-timeout: string # The amount of time to wait for the results. You can poll the remaining results based on an ID. The maximum value is 300 seconds. Default is `1s`.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --aggregations: record # Defines the aggregations that are run as part of the search request.
  --collapse: record # shape: {field: string, inner_hits?: any, max_concurrent_group_searches?: int}
  --explain: oneof<nothing, bool> # If `true`, returns detailed information about score computation as part of a hit.
  --ext: record # Configuration of search extensions defined by OpenSearch plugins.
  --body-from: float # Starting document offset. Needs to be non-negative. By default, you cannot page through more than 10,000 hits using the `from` and `size` parameters. To page through more hits, use the `search_after` parameter.
  --highlight: any
  --track-total-hits: any # The number of hits matching the query. When `true`, the exact number of hits is returned at the cost of some performance. When `false`, the response does not include the total number of hits matching the query. Default is `10,000` hits.
  --indices-boost: list # Boosts the `_score` of documents from specified indexes.
  --docvalue-fields: list # Array of wildcard (`*`) patterns. The request returns doc values for field names matching these patterns in the `hits.fields` property of the response.
  --rank: record # The container for ranking configuration. — shape: {rrf?: any}
  --min-score: float # Minimum `_score` for matching documents. Documents with a lower `_score` are not included in the search results.
  --post-filter: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
  --profile: oneof<nothing, bool> # Set to `true` to return detailed timing information about the execution of individual components in a search request. NOTE: This is a debugging tool and adds significant overhead to search execution.
  --body-query: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
  --script-fields: record # Retrieve a script evaluation (based on different fields) for each hit.
  --search-after: list
  --size: float # The number of hits to return. By default, you cannot page through more than 10,000 hits using the `from` and `size` parameters. To page through more hits, use the `search_after` parameter.
  --slice: record # The configuration for a sliced scroll request. — shape: {field?: string, id: int, max: int}
  --body-sort: any
  --body-source: any # Defines how to fetch a source. Fetching can be disabled entirely, or the source can be filtered.
  --body-fields: list # Array of wildcard (`*`) patterns. The request returns values for field names matching these patterns in the `hits.fields` property of the response.
  --suggest: record # shape: {text?: string}
  --terminate-after: int # Maximum number of documents to collect for each shard. If a query reaches this limit, OpenSearch terminates the query early. OpenSearch collects documents before sorting. Use with caution. OpenSearch applies this parameter to each shard handling the request. When possible, let OpenSearch perform early termination automatically. Avoid specifying this parameter for requests that target data streams with backing indexes across multiple data tiers. If set to `0` (default), the query does not terminate early. (format: int32)
  --timeout: string # Specifies the period of time to wait for a response from each shard. If no response is received before the timeout expires, the request fails and returns an error. Defaults to no timeout.
  --track-scores: oneof<nothing, bool> # If `true`, calculate and return document scores, even if the scores are not used for sorting.
  --version: oneof<nothing, bool> # If `true`, returns document version as part of a hit.
  --seq-no-primary-term: oneof<nothing, bool> # If `true`, returns sequence number and primary term of the last modification of each hit.
  --stored-fields: any
  --pit: record # shape: {id: string, keep_alive?: string}
  --stats: list # Stats groups to associate with the search. Each group maintains a statistics aggregation for its associated searches. You can retrieve these stats using the indexes stats API.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "index" $index "scalar") (serialize-qp "keep_alive" $keep_alive "scalar") (serialize-qp "keep_on_completion" $keep_on_completion "scalar") (serialize-qp "wait_for_completion_timeout" $wait_for_completion_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_asynchronous_search" $qp)
  let body = {aggregations: $aggregations, collapse: $collapse, explain: $explain, ext: $ext, from: $body_from, highlight: $highlight, track_total_hits: $track_total_hits, indices_boost: $indices_boost, docvalue_fields: $docvalue_fields, rank: $rank, min_score: $min_score, post_filter: $post_filter, profile: $profile, query: $body_query, script_fields: $script_fields, search_after: $search_after, size: $size, slice: $slice, sort: $body_sort, _source: $body_source, fields: $body_fields, suggest: $suggest, terminate_after: $terminate_after, timeout: $timeout, track_scores: $track_scores, version: $version, seq_no_primary_term: $seq_no_primary_term, stored_fields: $stored_fields, pit: $pit, stats: $stats} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes any responses from an asynchronous search.
#
# DELETE /_plugins/_asynchronous_search/{id}
# Docs: https://opensearch.org/docs/latest/search-plugins/async/index/#delete-searches-and-results
# operationId: asynchronous_search.delete.0
export def "plugins-asynchronous-search searchdelete0" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_asynchronous_search/($id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Gets partial responses from an asynchronous search.
#
# GET /_plugins/_asynchronous_search/{id}
# Docs: https://opensearch.org/docs/latest/search-plugins/async/index/#get-partial-results
# operationId: asynchronous_search.get.0
export def "plugins-asynchronous-search searchget0" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_asynchronous_search/($id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Monitors any asynchronous searches that are `running`, `completed`, or `persisted`.
#
# GET /_plugins/_asynchronous_search/stats
# Docs: https://opensearch.org/docs/latest/search-plugins/async/index/#monitor-stats
# operationId: asynchronous_search.stats.0
export def "plugins-asynchronous-search-stats searchstats0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_asynchronous_search/stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates a new workflow template.
#
# POST /_plugins/_flow_framework/workflow
# Docs: https://opensearch.org/docs/latest/automating-configurations/api/create-workflow/
# operationId: flow_framework.create.0
# --version shape: {template?: string, compatibility?: list}
export def "plugins-flow-framework-workflow frameworkcreate0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --provision: oneof<nothing, bool> # default: false
  --reprovision: oneof<nothing, bool> # default: false
  --update-fields: oneof<nothing, bool> # default: false
  --use-case: string # Specifies the workflow template to use.
  --validation: string # default: all
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  name: string # The name of the workflow.
  --description: string # A description of the workflow.
  --use-case: string # A use case ID, which can be used with the Search Workflow API to find related workflows.
  --version: record # A key-value map with two fields, template, which identifies the template version, and compatibility, which identifies a list of minimum required OpenSearch versions. — shape: {template?: string, compatibility?: list}
  --workflows: record # A map of workflows. Presently, only the provision key is supported. The value for the workflow key is a key-value map that includes fields for `user_params` and lists of nodes and edges.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "provision" $provision "scalar") (serialize-qp "reprovision" $reprovision "scalar") (serialize-qp "update_fields" $update_fields "scalar") (serialize-qp "use_case" $use_case "scalar") (serialize-qp "validation" $validation "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_flow_framework/workflow" $qp)
  let body = {name: $name, description: $description, use_case: $use_case, version: $version, workflows: $workflows} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Search for workflows by using a query matching a field.
#
# GET /_plugins/_flow_framework/workflow/_search
# Docs: https://opensearch.org/docs/latest/automating-configurations/api/provision-workflow/
# operationId: flow_framework.search.1
# --query shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
export def "plugins-flow-framework-workflow-search frameworksearch1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_flow_framework/workflow/_search" $qp)
  let body = {query: $body_query} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Search for workflows by using a query matching a field.
#
# POST /_plugins/_flow_framework/workflow/_search
# Docs: https://opensearch.org/docs/latest/automating-configurations/api/provision-workflow/
# operationId: flow_framework.search.0
# --query shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
export def "plugins-flow-framework-workflow-search frameworksearch0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_flow_framework/workflow/_search" $qp)
  let body = {query: $body_query} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves available workflow steps.
#
# GET /_plugins/_flow_framework/workflow/_steps
# Docs: https://opensearch.org/docs/latest/automating-configurations/api/get-workflow-steps/
# operationId: flow_framework.get_steps.0
export def "plugins-flow-framework-workflow-steps steps0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --workflow-step: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "workflow_step" $workflow_step "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_flow_framework/workflow/_steps" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Deletes a workflow template.
#
# DELETE /_plugins/_flow_framework/workflow/{workflow_id}
# Docs: https://opensearch.org/docs/latest/automating-configurations/api/delete-workflow/
# operationId: flow_framework.delete.0
export def "plugins-flow-framework-workflow frameworkdelete0" [
  workflow_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --clear-status: oneof<nothing, bool> # default: false
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "clear_status" $clear_status "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_flow_framework/workflow/($workflow_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves a workflow template.
#
# GET /_plugins/_flow_framework/workflow/{workflow_id}
# Docs: https://opensearch.org/docs/latest/automating-configurations/api/get-workflow/
# operationId: flow_framework.get.0
export def "plugins-flow-framework-workflow frameworkget0" [
  workflow_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_flow_framework/workflow/($workflow_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Updates a workflow template that has not been provisioned.
#
# PUT /_plugins/_flow_framework/workflow/{workflow_id}
# Docs: https://opensearch.org/docs/latest/automating-configurations/api/create-workflow/
# operationId: flow_framework.update.0
# --version shape: {template?: string, compatibility?: list}
export def "plugins-flow-framework-workflow frameworkupdate0" [
  workflow_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --provision: oneof<nothing, bool> # default: false
  --reprovision: oneof<nothing, bool> # default: false
  --update-fields: oneof<nothing, bool> # default: false
  --use-case: string # Specifies the workflow template to use.
  --validation: string # default: all
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --name: string # The name of the workflow.
  --description: string # A description of the workflow.
  --use-case: string # A use case ID, which can be used with the Search Workflow API to find related workflows.
  --workflows: record
  --version: record # A key-value map with two fields, template, which identifies the template version, and compatibility, which identifies a list of minimum required OpenSearch versions. — shape: {template?: string, compatibility?: list}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "provision" $provision "scalar") (serialize-qp "reprovision" $reprovision "scalar") (serialize-qp "update_fields" $update_fields "scalar") (serialize-qp "use_case" $use_case "scalar") (serialize-qp "validation" $validation "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_flow_framework/workflow/($workflow_id)" $qp)
  let body = {name: $name, description: $description, use_case: $use_case, workflows: $workflows, version: $version} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deprovision workflow's resources when you no longer need them.
#
# POST /_plugins/_flow_framework/workflow/{workflow_id}/_deprovision
# Docs: https://opensearch.org/docs/latest/automating-configurations/api/deprovision-workflow/
# operationId: flow_framework.deprovision.0
export def "plugins-flow-framework-workflow-deprovision frameworkdeprovision0" [
  workflow_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-delete: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_delete" $allow_delete "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_flow_framework/workflow/($workflow_id)/_deprovision" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Provisioning a workflow. This API is also executed when the Create or Update Workflow API is called with the provision parameter set to true.
#
# POST /_plugins/_flow_framework/workflow/{workflow_id}/_provision
# Docs: https://opensearch.org/docs/latest/automating-configurations/api/provision-workflow/
# operationId: flow_framework.provision.0
export def "plugins-flow-framework-workflow-provision frameworkprovision0" [
  workflow_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_flow_framework/workflow/($workflow_id)/_provision" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves the current workflow provisioning status.
#
# GET /_plugins/_flow_framework/workflow/{workflow_id}/_status
# Docs: https://opensearch.org/docs/latest/automating-configurations/api/get-workflow-status/
# operationId: flow_framework.get_status.0
export def "plugins-flow-framework-workflow-status status0" [
  workflow_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --all: oneof<nothing, bool> # Whether to return all fields in the response. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "all" $all "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_flow_framework/workflow/($workflow_id)/_status" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Search for workflows by using a query matching a field.
#
# GET /_plugins/_flow_framework/workflow/state/_search
# Docs: https://opensearch.org/docs/latest/automating-configurations/api/search-workflow-state/
# operationId: flow_framework.search_state.1
# --query shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
export def "plugins-flow-framework-workflow-state-search state1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_flow_framework/workflow/state/_search" $qp)
  let body = {query: $body_query} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Search for workflows by using a query matching a field.
#
# POST /_plugins/_flow_framework/workflow/state/_search
# Docs: https://opensearch.org/docs/latest/automating-configurations/api/search-workflow-state/
# operationId: flow_framework.search_state.0
# --query shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
export def "plugins-flow-framework-workflow-state-search state0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_flow_framework/workflow/state/_search" $qp)
  let body = {query: $body_query} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Adds a policy to an index.
#
# POST /_plugins/_ism/add
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#add-policy
# operationId: ism.add_policy.0
export def "plugins-ism-add policy0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --index: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  policy_id: string # The ID of the policy to add.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "index" $index "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ism/add" $qp)
  let body = {policy_id: $policy_id} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Adds a policy to an index.
#
# POST /_plugins/_ism/add/{index}
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#add-policy
# operationId: ism.add_policy.1
export def "plugins-ism-add policy1" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --index: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  policy_id: string # The ID of the policy to add.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "index" $index "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ism/add/($index)" $qp)
  let body = {policy_id: $policy_id} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Updates the managed index policy to a new policy.
#
# POST /_plugins/_ism/change_policy
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#update-managed-index-policy
# operationId: ism.change_policy.0
# --include item shape: {state?: string}
export def "plugins-ism-change-policy policy0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --index: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  policy_id: string # The ID of the new policy.
  --state: string # The state to transition to.
  --include: list # The states to include in the policy change. — item shape: {state?: string}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "index" $index "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ism/change_policy" $qp)
  let body = {policy_id: $policy_id, state: $state, include: $include} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Updates the managed index policy to a new policy.
#
# POST /_plugins/_ism/change_policy/{index}
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#update-managed-index-policy
# operationId: ism.change_policy.1
# --include item shape: {state?: string}
export def "plugins-ism-change-policy policy1" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --index: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  policy_id: string # The ID of the new policy.
  --state: string # The state to transition to.
  --include: list # The states to include in the policy change. — item shape: {state?: string}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "index" $index "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ism/change_policy/($index)" $qp)
  let body = {policy_id: $policy_id, state: $state, include: $include} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves the currently applied policy on the specified indexes.
#
# GET /_plugins/_ism/explain
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#explain-index
# operationId: ism.explain_policy.0
export def "plugins-ism-explain policy0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ism/explain" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves the currently applied policy on the specified indexes.
#
# POST /_plugins/_ism/explain
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#explain-index
# operationId: ism.explain_policy.1
export def "plugins-ism-explain policy1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ism/explain" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves the currently applied policy on the specified indexes.
#
# GET /_plugins/_ism/explain/{index}
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#explain-index
# operationId: ism.explain_policy.3
export def "plugins-ism-explain policy3" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ism/explain/($index)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves the currently applied policy on the specified indexes.
#
# POST /_plugins/_ism/explain/{index}
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#explain-index
# operationId: ism.explain_policy.4
export def "plugins-ism-explain policy4" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ism/explain/($index)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves the policies.
#
# GET /_plugins/_ism/policies
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#get-policy
# operationId: ism.get_policies.0
export def "plugins-ism-policies policies0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ism/policies" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or updates policies.
#
# PUT /_plugins/_ism/policies
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#create-policy
# operationId: ism.put_policies.0
# --policy shape: {policy_id?: string, description?: string, last_updated_time?: int, schema_version?: float, error_notification?: any, default_state?: string, states?: list, ism_template?: any}
export def "plugins-ism-policies policies0-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --if-primary-term: float # Only perform the operation if the document has this primary term.
  --if-seq-no: int # Only perform the operation if the document has this sequence number. (format: int64)
  --policyID: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --policy: record # The policy defines how an index is managed throughout its lifetime. — shape: {policy_id?: string, description?: string, last_updated_time?: int, schema_version?: float, error_notification?: any, default_state?: string, states?: list, ism_template?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "if_primary_term" $if_primary_term "scalar") (serialize-qp "if_seq_no" $if_seq_no "scalar") (serialize-qp "policyID" $policyID "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ism/policies" $qp)
  let body = {policy: $policy} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes a policy.
#
# DELETE /_plugins/_ism/policies/{policy_id}
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#delete-policy
# operationId: ism.delete_policy.0
export def "plugins-ism-policies policy0-by-policy_id" [
  policy_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ism/policies/($policy_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves a specific policy.
#
# GET /_plugins/_ism/policies/{policy_id}
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#put-policy
# operationId: ism.get_policy.0
export def "plugins-ism-policies policy0-by-policy_id-1" [
  policy_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ism/policies/($policy_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Checks for the existence of a policy.
#
# HEAD /_plugins/_ism/policies/{policy_id}
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#get-policy
# operationId: ism.exists_policy.0
export def "plugins-ism-policies policy0-by-policy_id-2" [
  policy_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ism/policies/($policy_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or updates a policy.
#
# PUT /_plugins/_ism/policies/{policy_id}
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#create-policy
# operationId: ism.put_policy.0
# --policy shape: {policy_id?: string, description?: string, last_updated_time?: int, schema_version?: float, error_notification?: any, default_state?: string, states?: list, ism_template?: any}
export def "plugins-ism-policies policy0-by-policy_id-3" [
  policy_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --if-primary-term: float # Only perform the operation if the document has this primary term.
  --if-seq-no: int # Only perform the operation if the document has this sequence number. (format: int64)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --policy: record # The policy defines how an index is managed throughout its lifetime. — shape: {policy_id?: string, description?: string, last_updated_time?: int, schema_version?: float, error_notification?: any, default_state?: string, states?: list, ism_template?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "if_primary_term" $if_primary_term "scalar") (serialize-qp "if_seq_no" $if_seq_no "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ism/policies/($policy_id)" $qp)
  let body = {policy: $policy} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Removes a policy from an index.
#
# POST /_plugins/_ism/remove
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#remove-policy
# operationId: ism.remove_policy.0
export def "plugins-ism-remove policy0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --index: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "index" $index "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ism/remove" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Removes a policy from an index.
#
# POST /_plugins/_ism/remove/{index}
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#remove-policy
# operationId: ism.remove_policy.1
export def "plugins-ism-remove policy1" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --index: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "index" $index "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ism/remove/($index)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retries the failed action for an index.
#
# POST /_plugins/_ism/retry
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#retry-failed-index
# operationId: ism.retry_index.0
export def "plugins-ism-retry index0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --index: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  state: string # The state to retry.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "index" $index "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ism/retry" $qp)
  let body = {state: $state} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retries the failed action for an index.
#
# POST /_plugins/_ism/retry/{index}
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#retry-failed-index
# operationId: ism.retry_index.1
export def "plugins-ism-retry index1" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --index: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  state: string # The state to retry.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "index" $index "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ism/retry/($index)" $qp)
  let body = {state: $state} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Provides information about the current status of the k-NN plugin.
#
# GET /_plugins/_knn/{node_id}/stats
# Docs: https://opensearch.org/docs/latest/search-plugins/knn/api/#stats
# operationId: knn.stats.2
export def "plugins-knn-stats knnstats2" [
  node_id: list
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --timeout: string # Operation timeout.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_knn/($node_id)/stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Provides information about the current status of the k-NN plugin.
#
# GET /_plugins/_knn/{node_id}/stats/{stat}
# Docs: https://opensearch.org/docs/latest/search-plugins/knn/api/#stats
# operationId: knn.stats.3
export def "plugins-knn-stats knnstats3" [
  node_id: list
  stat: list
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --timeout: string # Operation timeout.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_knn/($node_id)/stats/($stat)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Use an OpenSearch query to search for models in the index.
#
# GET /_plugins/_knn/models/_search
# Docs: https://opensearch.org/docs/latest/search-plugins/knn/api/#search-model
# operationId: knn.search_models.0
export def "plugins-knn-models-search models0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: list # Set to `true` or `false` to return the `_source` field or not, or a list of fields to return.
  --source-excludes: list # List of fields to exclude from the returned `_source` field.
  --source-includes: list # List of fields to extract and return from the `_source` field.
  --allow-no-indices: oneof<nothing, bool> # Whether to ignore if a wildcard indexes expression resolves into no concrete indexes. (This includes `_all` string or when no indexes have been specified).
  --allow-partial-search-results: oneof<nothing, bool> # Indicate if an error should be returned if there is a partial search failure or timeout. (default: true)
  --analyze-wildcard: oneof<nothing, bool> # Specify whether wildcard and prefix queries should be analyzed. (default: false)
  --analyzer: string # The analyzer to use for the query string.
  --batched-reduce-size: int # The number of shard results that should be reduced at once on the coordinating node. This value should be used as a protection mechanism to reduce the memory overhead per search request if the potential number of shards in the request can be large. (format: int32, default: 512)
  --ccs-minimize-roundtrips: oneof<nothing, bool> # Indicates whether network round-trips should be minimized as part of cross-cluster search requests execution. (default: true)
  --default-operator: string@default-operator-completer # The default operator for query string query (AND or OR).
  --df: string # The field to use as default where no field prefix is given in the query string.
  --docvalue-fields: list # Comma-separated list of fields to return as the docvalue representation of a field for each hit.
  --expand-wildcards: string # Whether to expand wildcard expression to concrete indexes that are open, closed or both.
  --explain: oneof<nothing, bool> # Specify whether to return detailed information about score computation as part of a hit.
  --qp-from: int # Starting offset. (format: int32, default: 0)
  --ignore-throttled: oneof<nothing, bool> # Whether specified concrete, expanded or aliased indexes should be ignored when throttled.
  --ignore-unavailable: oneof<nothing, bool> # Whether specified concrete indexes should be ignored when unavailable (missing or closed).
  --lenient: oneof<nothing, bool> # Specify whether format-based query failures (such as providing text to a numeric field) should be ignored.
  --max-concurrent-shard-requests: int # The number of concurrent shard requests per node this search executes concurrently. This value should be used to limit the impact of the search on the cluster in order to limit the number of concurrent shard requests. (format: int32, default: 5)
  --pre-filter-shard-size: int # Threshold that enforces a pre-filter round-trip to prefilter search shards based on query rewriting if the number of shards the search request expands to exceeds the threshold. This filter round-trip can limit the number of shards significantly if for instance a shard can not match any documents based on its rewrite method, that is if date filters are mandatory to match but the shard bounds and the query are disjoint. (format: int32)
  --preference: string # Specify the node or shard the operation should be performed on. (default: random)
  --q: string # Query in the Lucene query string syntax.
  --request-cache: oneof<nothing, bool> # Specify if request cache should be used for this request or not, defaults to index level setting.
  --rest-total-hits-as-int: oneof<nothing, bool> # Indicates whether `hits.total` should be rendered as an integer or an object in the rest search response. (default: false)
  --routing: string # Comma-separated list of specific routing values.
  --scroll: string # Specify how long a consistent view of the index should be maintained for scrolled search.
  --search-type: string # Search operation type.
  --seq-no-primary-term: oneof<nothing, bool> # Specify whether to return sequence number and primary term of the last modification of each hit.
  --size: int # Number of hits to return. (format: int32, default: 10)
  --qp-sort: list # Comma-separated list of <field>:<direction> pairs.
  --stats: list # Specific 'tag' of the request for logging and statistical purposes.
  --stored-fields: list # Comma-separated list of stored fields to return.
  --suggest-field: string # Specify which field to use for suggestions.
  --suggest-mode: string@suggest-mode-completer # Specify suggest mode.
  --suggest-size: int # How many suggestions to return in response. (format: int32)
  --suggest-text: string # The source text for which the suggestions should be returned.
  --terminate-after: int # The maximum number of documents to collect for each shard, upon reaching which the query execution will terminate early. (format: int32)
  --timeout: string # Operation timeout.
  --track-scores: oneof<nothing, bool> # Whether to calculate and return scores even if they are not used for sorting.
  --track-total-hits: oneof<nothing, bool> # Indicate if the number of documents that match the query should be tracked.
  --typed-keys: oneof<nothing, bool> # Specify whether aggregation and suggester names should be prefixed by their respective types in the response.
  --version: oneof<nothing, bool> # Whether to return document version as part of a hit.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "multi") (serialize-qp "_source_excludes" $source_excludes "multi") (serialize-qp "_source_includes" $source_includes "multi") (serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "allow_partial_search_results" $allow_partial_search_results "scalar") (serialize-qp "analyze_wildcard" $analyze_wildcard "scalar") (serialize-qp "analyzer" $analyzer "scalar") (serialize-qp "batched_reduce_size" $batched_reduce_size "scalar") (serialize-qp "ccs_minimize_roundtrips" $ccs_minimize_roundtrips "scalar") (serialize-qp "default_operator" $default_operator "scalar") (serialize-qp "df" $df "scalar") (serialize-qp "docvalue_fields" $docvalue_fields "multi") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "explain" $explain "scalar") (serialize-qp "from" $qp_from "scalar") (serialize-qp "ignore_throttled" $ignore_throttled "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "lenient" $lenient "scalar") (serialize-qp "max_concurrent_shard_requests" $max_concurrent_shard_requests "scalar") (serialize-qp "pre_filter_shard_size" $pre_filter_shard_size "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "request_cache" $request_cache "scalar") (serialize-qp "rest_total_hits_as_int" $rest_total_hits_as_int "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "scroll" $scroll "scalar") (serialize-qp "search_type" $search_type "scalar") (serialize-qp "seq_no_primary_term" $seq_no_primary_term "scalar") (serialize-qp "size" $size "scalar") (serialize-qp "sort" $qp_sort "multi") (serialize-qp "stats" $stats "multi") (serialize-qp "stored_fields" $stored_fields "multi") (serialize-qp "suggest_field" $suggest_field "scalar") (serialize-qp "suggest_mode" $suggest_mode "scalar") (serialize-qp "suggest_size" $suggest_size "scalar") (serialize-qp "suggest_text" $suggest_text "scalar") (serialize-qp "terminate_after" $terminate_after "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "track_scores" $track_scores "scalar") (serialize-qp "track_total_hits" $track_total_hits "scalar") (serialize-qp "typed_keys" $typed_keys "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_knn/models/_search" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Use an OpenSearch query to search for models in the index.
#
# POST /_plugins/_knn/models/_search
# Docs: https://opensearch.org/docs/latest/search-plugins/knn/api/#search-model
# operationId: knn.search_models.1
export def "plugins-knn-models-search models1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: list # Set to `true` or `false` to return the `_source` field or not, or a list of fields to return.
  --source-excludes: list # List of fields to exclude from the returned `_source` field.
  --source-includes: list # List of fields to extract and return from the `_source` field.
  --allow-no-indices: oneof<nothing, bool> # Whether to ignore if a wildcard indexes expression resolves into no concrete indexes. (This includes `_all` string or when no indexes have been specified).
  --allow-partial-search-results: oneof<nothing, bool> # Indicate if an error should be returned if there is a partial search failure or timeout. (default: true)
  --analyze-wildcard: oneof<nothing, bool> # Specify whether wildcard and prefix queries should be analyzed. (default: false)
  --analyzer: string # The analyzer to use for the query string.
  --batched-reduce-size: int # The number of shard results that should be reduced at once on the coordinating node. This value should be used as a protection mechanism to reduce the memory overhead per search request if the potential number of shards in the request can be large. (format: int32, default: 512)
  --ccs-minimize-roundtrips: oneof<nothing, bool> # Indicates whether network round-trips should be minimized as part of cross-cluster search requests execution. (default: true)
  --default-operator: string@default-operator-completer # The default operator for query string query (AND or OR).
  --df: string # The field to use as default where no field prefix is given in the query string.
  --docvalue-fields: list # Comma-separated list of fields to return as the docvalue representation of a field for each hit.
  --expand-wildcards: string # Whether to expand wildcard expression to concrete indexes that are open, closed or both.
  --explain: oneof<nothing, bool> # Specify whether to return detailed information about score computation as part of a hit.
  --qp-from: int # Starting offset. (format: int32, default: 0)
  --ignore-throttled: oneof<nothing, bool> # Whether specified concrete, expanded or aliased indexes should be ignored when throttled.
  --ignore-unavailable: oneof<nothing, bool> # Whether specified concrete indexes should be ignored when unavailable (missing or closed).
  --lenient: oneof<nothing, bool> # Specify whether format-based query failures (such as providing text to a numeric field) should be ignored.
  --max-concurrent-shard-requests: int # The number of concurrent shard requests per node this search executes concurrently. This value should be used to limit the impact of the search on the cluster in order to limit the number of concurrent shard requests. (format: int32, default: 5)
  --pre-filter-shard-size: int # Threshold that enforces a pre-filter round-trip to prefilter search shards based on query rewriting if the number of shards the search request expands to exceeds the threshold. This filter round-trip can limit the number of shards significantly if for instance a shard can not match any documents based on its rewrite method, that is if date filters are mandatory to match but the shard bounds and the query are disjoint. (format: int32)
  --preference: string # Specify the node or shard the operation should be performed on. (default: random)
  --q: string # Query in the Lucene query string syntax.
  --request-cache: oneof<nothing, bool> # Specify if request cache should be used for this request or not, defaults to index level setting.
  --rest-total-hits-as-int: oneof<nothing, bool> # Indicates whether `hits.total` should be rendered as an integer or an object in the rest search response. (default: false)
  --routing: string # Comma-separated list of specific routing values.
  --scroll: string # Specify how long a consistent view of the index should be maintained for scrolled search.
  --search-type: string # Search operation type.
  --seq-no-primary-term: oneof<nothing, bool> # Specify whether to return sequence number and primary term of the last modification of each hit.
  --size: int # Number of hits to return. (format: int32, default: 10)
  --qp-sort: list # Comma-separated list of <field>:<direction> pairs.
  --stats: list # Specific 'tag' of the request for logging and statistical purposes.
  --stored-fields: list # Comma-separated list of stored fields to return.
  --suggest-field: string # Specify which field to use for suggestions.
  --suggest-mode: string@suggest-mode-completer # Specify suggest mode.
  --suggest-size: int # How many suggestions to return in response. (format: int32)
  --suggest-text: string # The source text for which the suggestions should be returned.
  --terminate-after: int # The maximum number of documents to collect for each shard, upon reaching which the query execution will terminate early. (format: int32)
  --timeout: string # Operation timeout.
  --track-scores: oneof<nothing, bool> # Whether to calculate and return scores even if they are not used for sorting.
  --track-total-hits: oneof<nothing, bool> # Indicate if the number of documents that match the query should be tracked.
  --typed-keys: oneof<nothing, bool> # Specify whether aggregation and suggester names should be prefixed by their respective types in the response.
  --version: oneof<nothing, bool> # Whether to return document version as part of a hit.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "multi") (serialize-qp "_source_excludes" $source_excludes "multi") (serialize-qp "_source_includes" $source_includes "multi") (serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "allow_partial_search_results" $allow_partial_search_results "scalar") (serialize-qp "analyze_wildcard" $analyze_wildcard "scalar") (serialize-qp "analyzer" $analyzer "scalar") (serialize-qp "batched_reduce_size" $batched_reduce_size "scalar") (serialize-qp "ccs_minimize_roundtrips" $ccs_minimize_roundtrips "scalar") (serialize-qp "default_operator" $default_operator "scalar") (serialize-qp "df" $df "scalar") (serialize-qp "docvalue_fields" $docvalue_fields "multi") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "explain" $explain "scalar") (serialize-qp "from" $qp_from "scalar") (serialize-qp "ignore_throttled" $ignore_throttled "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "lenient" $lenient "scalar") (serialize-qp "max_concurrent_shard_requests" $max_concurrent_shard_requests "scalar") (serialize-qp "pre_filter_shard_size" $pre_filter_shard_size "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "request_cache" $request_cache "scalar") (serialize-qp "rest_total_hits_as_int" $rest_total_hits_as_int "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "scroll" $scroll "scalar") (serialize-qp "search_type" $search_type "scalar") (serialize-qp "seq_no_primary_term" $seq_no_primary_term "scalar") (serialize-qp "size" $size "scalar") (serialize-qp "sort" $qp_sort "multi") (serialize-qp "stats" $stats "multi") (serialize-qp "stored_fields" $stored_fields "multi") (serialize-qp "suggest_field" $suggest_field "scalar") (serialize-qp "suggest_mode" $suggest_mode "scalar") (serialize-qp "suggest_size" $suggest_size "scalar") (serialize-qp "suggest_text" $suggest_text "scalar") (serialize-qp "terminate_after" $terminate_after "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "track_scores" $track_scores "scalar") (serialize-qp "track_total_hits" $track_total_hits "scalar") (serialize-qp "typed_keys" $typed_keys "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_knn/models/_search" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Create and train a model that can be used for initializing k-NN native library indexes during indexing.
#
# POST /_plugins/_knn/models/_train
# Docs: https://opensearch.org/docs/latest/search-plugins/knn/api/#train-model
# operationId: knn.train_model.0
# --method shape: {name: string, space_type?: string, engine?: string, parameters?: record}
export def "plugins-knn-models-train model0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --preference: string # Preferred node to execute training.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  training_index: string
  training_field: string
  dimension: int # format: int32
  --max-training-vector-count: int # format: int32
  --search-size: int # format: int32
  --description: string
  --mode: string
  --compression-level: string
  --method: record # shape: {name: string, space_type?: string, engine?: string, parameters?: record}
  --spaceType: string
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "preference" $preference "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_knn/models/_train" $qp)
  let body = {training_index: $training_index, training_field: $training_field, dimension: $dimension, max_training_vector_count: $max_training_vector_count, search_size: $search_size, description: $description, mode: $mode, compression_level: $compression_level, method: $method, spaceType: $spaceType} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Used to delete a particular model in the cluster.
#
# DELETE /_plugins/_knn/models/{model_id}
# Docs: https://opensearch.org/docs/latest/search-plugins/knn/api/#delete-model
# operationId: knn.delete_model.0
export def "plugins-knn-models model0-by-model_id" [
  model_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_knn/models/($model_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Used to retrieve information about models present in the cluster.
#
# GET /_plugins/_knn/models/{model_id}
# Docs: https://opensearch.org/docs/latest/search-plugins/knn/api/#get-model
# operationId: knn.get_model.0
export def "plugins-knn-models model0-by-model_id-1" [
  model_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_knn/models/($model_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create and train a model that can be used for initializing k-NN native library indexes during indexing.
#
# POST /_plugins/_knn/models/{model_id}/_train
# Docs: https://opensearch.org/docs/latest/search-plugins/knn/api/#train-model
# operationId: knn.train_model.1
# --method shape: {name: string, space_type?: string, engine?: string, parameters?: record}
export def "plugins-knn-models-train model1" [
  model_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --preference: string # Preferred node to execute training.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  training_index: string
  training_field: string
  dimension: int # format: int32
  --max-training-vector-count: int # format: int32
  --search-size: int # format: int32
  --description: string
  --mode: string
  --compression-level: string
  --method: record # shape: {name: string, space_type?: string, engine?: string, parameters?: record}
  --spaceType: string
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "preference" $preference "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_knn/models/($model_id)/_train" $qp)
  let body = {training_index: $training_index, training_field: $training_field, dimension: $dimension, max_training_vector_count: $max_training_vector_count, search_size: $search_size, description: $description, mode: $mode, compression_level: $compression_level, method: $method, spaceType: $spaceType} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Provides information about the current status of the k-NN plugin.
#
# GET /_plugins/_knn/stats
# Docs: https://opensearch.org/docs/latest/search-plugins/knn/api/#stats
# operationId: knn.stats.0
export def "plugins-knn-stats knnstats0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --timeout: string # Operation timeout.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_knn/stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Provides information about the current status of the k-NN plugin.
#
# GET /_plugins/_knn/stats/{stat}
# Docs: https://opensearch.org/docs/latest/search-plugins/knn/api/#stats
# operationId: knn.stats.1
export def "plugins-knn-stats knnstats1" [
  stat: list
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --timeout: string # Operation timeout.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_knn/stats/($stat)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Preloads native library files into memory, reducing initial search latency for specified indexes.
#
# GET /_plugins/_knn/warmup/{index}
# Docs: https://opensearch.org/docs/latest/search-plugins/knn/api/#warmup-operation
# operationId: knn.warmup.0
export def "plugins-knn-warmup knnwarmup0" [
  index: list
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_knn/warmup/($index)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Provides information about the current status of the LTR plugin.
#
# GET /_plugins/_ltr/{node_id}/stats
# operationId: ltr.stats.2
export def "plugins-ltr-stats ltrstats2" [
  node_id: list
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --timeout: string # The time in milliseconds to wait for a response.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ltr/($node_id)/stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Provides information about the current status of the LTR plugin.
#
# GET /_plugins/_ltr/{node_id}/stats/{stat}
# operationId: ltr.stats.3
export def "plugins-ltr-stats ltrstats3" [
  node_id: list
  stat: list
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --timeout: string # The time in milliseconds to wait for a response.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ltr/($node_id)/stats/($stat)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Provides information about the current status of the LTR plugin.
#
# GET /_plugins/_ltr/stats
# operationId: ltr.stats.0
export def "plugins-ltr-stats ltrstats0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --timeout: string # The time in milliseconds to wait for a response.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ltr/stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Provides information about the current status of the LTR plugin.
#
# GET /_plugins/_ltr/stats/{stat}
# operationId: ltr.stats.1
export def "plugins-ltr-stats ltrstats1" [
  stat: list
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --timeout: string # The time in milliseconds to wait for a response.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ltr/stats/($stat)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Execute an algorithm.
#
# POST /_plugins/_ml/_execute/{algorithm_name}
# operationId: ml.execute_algorithm.0
# --aggregations item shape: {sum?: record, max?: record, field?: string}
export def "plugins-ml-execute algorithm0" [
  algorithm_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --operation: string@operation-completer # The calculator operation.
  --input-data: list # The input data.
  --index-name: string # The index name.
  --attribute-field-names: list # The attribute field names
  --aggregations: list # item shape: {sum?: record, max?: record, field?: string}
  --time-field-name: string # The time field name.
  --start-time: int # The start time. (format: int64)
  --end-time: int # The end time. (format: int64)
  --min-time-interval: int # The minimum time interval. (format: int64)
  --num-outputs: int # The number of outputs. (format: int64)
  --metrics: list # The metrics input.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/_execute/($algorithm_name)" $qp)
  let body = {operation: $operation, input_data: $input_data, index_name: $index_name, attribute_field_names: $attribute_field_names, aggregations: $aggregations, time_field_name: $time_field_name, start_time: $start_time, end_time: $end_time, min_time_interval: $min_time_interval, num_outputs: $num_outputs, metrics: $metrics} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Predicts new data with trained model.
#
# POST /_plugins/_ml/_predict/{algorithm_name}/{model_id}
# operationId: ml.predict.0
# --input_query shape: {_source?: list, size?: int, query?: record}
export def "plugins-ml-predict mlpredict0" [
  algorithm_name: string
  model_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --input-query: record # shape: {_source?: list, size?: int, query?: record}
  --input-index: list # The input index.
  --text-docs: list # The text documents.
  --return-number: oneof<nothing, bool> # Whether to return bytes in model output.
  --target-response: list # The target response.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/_predict/($algorithm_name)/($model_id)" $qp)
  let body = {input_query: $input_query, input_index: $input_index, text_docs: $text_docs, return_number: $return_number, target_response: $target_response} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Trains a model and predicts against the same training dataset.
#
# POST /_plugins/_ml/_train_predict/{algorithm_name}
# operationId: ml.train_predict.0
# --parameters shape: {centroids?: int, iterations?: int, distance_type?: "COSINE"|"EUCLIDEAN"|"L1"}
# --input_query shape: {_source?: list, size?: int, query?: record}
# --input_data shape: {column_metas?: list, rows?: list}
export def "plugins-ml-train-predict predict0" [
  algorithm_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --parameters: record # shape: {centroids?: int, iterations?: int, distance_type?: "COSINE"|"EUCLIDEAN"|"L1"}
  --input-query: record # shape: {_source?: list, size?: int, query?: record}
  --input-index: list # The input index.
  --input-data: record # shape: {column_metas?: list, rows?: list}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/_train_predict/($algorithm_name)" $qp)
  let body = {parameters: $parameters, input_query: $input_query, input_index: $input_index, input_data: $input_data} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Trains a model synchronously.
#
# POST /_plugins/_ml/_train/{algorithm_name}
# operationId: ml.train.0
# --parameters shape: {centroids?: int, iterations?: int, distance_type?: "COSINE"|"EUCLIDEAN"|"L1"}
# --input_query shape: {_source?: list, size?: int, query?: record}
export def "plugins-ml-train mltrain0" [
  algorithm_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --parameters: record # shape: {centroids?: int, iterations?: int, distance_type?: "COSINE"|"EUCLIDEAN"|"L1"}
  --input-query: record # shape: {_source?: list, size?: int, query?: record}
  --input-index: list # The input index.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/_train/($algorithm_name)" $qp)
  let body = {parameters: $parameters, input_query: $input_query, input_index: $input_index} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get stats.
#
# GET /_plugins/_ml/{node_id}/stats
# operationId: ml.get_stats.2
export def "plugins-ml-stats stats2" [
  node_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/($node_id)/stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get stats.
#
# GET /_plugins/_ml/{node_id}/stats/{stat}
# operationId: ml.get_stats.3
export def "plugins-ml-stats stats3" [
  node_id: string
  stat: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/($node_id)/stats/($stat)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Register an agent.
#
# POST /_plugins/_ml/agents/_register
# operationId: ml.register_agents.0
# --tools item shape: {name?: string, type?: string, description?: string, parameters?: record, include_output_in_agent_response?: bool, attributes?: record}
# --memory shape: {type?: string, memory_id?: string, create_time?: string, updated_time?: string, name?: string, user?: string, additional_info?: record}
# --llm shape: {model_id?: string, parameters?: record}
export def "plugins-ml-agents-register agents0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  name: string
  type: string
  --description: string
  --tools: list # item shape: {name?: string, type?: string, description?: string, parameters?: record, include_output_in_agent_response?: bool, attributes?: record}
  --parameters: record
  --app-type: string
  --memory: record # shape: {type?: string, memory_id?: string, create_time?: string, updated_time?: string, name?: string, user?: string, additional_info?: record}
  --llm: record # shape: {model_id?: string, parameters?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/agents/_register" $qp)
  let body = {name: $name, type: $type, description: $description, tools: $tools, parameters: $parameters, app_type: $app_type, memory: $memory, llm: $llm} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Search agents.
#
# GET /_plugins/_ml/agents/_search
# operationId: ml.search_agents.0
# --query shape: {bool?: record, match?: record, match_all?: record, term?: record}
# --sort item shape: {created_time?: record, is_hidden?: record, last_updated_time?: record, parameters?: record, tools?: record, type?: record, _id?: record, _index?: record, _score?: record, _seq_no?: record}
export def "plugins-ml-agents-search agents0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # The query. — shape: {bool?: record, match?: record, match_all?: record, term?: record}
  --size: int # The number of agents to return.
  --body-sort: list # The sort order. — item shape: {created_time?: record, is_hidden?: record, last_updated_time?: record, parameters?: record, tools?: record, type?: record, _id?: record, _index?: record, _score?: record, _seq_no?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/agents/_search" $qp)
  let body = {query: $body_query, size: $size, sort: $body_sort} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Search agents.
#
# POST /_plugins/_ml/agents/_search
# operationId: ml.search_agents.1
# --query shape: {bool?: record, match?: record, match_all?: record, term?: record}
# --sort item shape: {created_time?: record, is_hidden?: record, last_updated_time?: record, parameters?: record, tools?: record, type?: record, _id?: record, _index?: record, _score?: record, _seq_no?: record}
export def "plugins-ml-agents-search agents1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # The query. — shape: {bool?: record, match?: record, match_all?: record, term?: record}
  --size: int # The number of agents to return.
  --body-sort: list # The sort order. — item shape: {created_time?: record, is_hidden?: record, last_updated_time?: record, parameters?: record, tools?: record, type?: record, _id?: record, _index?: record, _score?: record, _seq_no?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/agents/_search" $qp)
  let body = {query: $body_query, size: $size, sort: $body_sort} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete an agent.
#
# DELETE /_plugins/_ml/agents/{agent_id}
# operationId: ml.delete_agent.0
export def "plugins-ml-agents agent0-by-agent_id" [
  agent_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/agents/($agent_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get an agent.
#
# GET /_plugins/_ml/agents/{agent_id}
# operationId: ml.get_agent.0
export def "plugins-ml-agents agent0-by-agent_id-1" [
  agent_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/agents/($agent_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Execute an agent.
#
# POST /_plugins/_ml/agents/{agent_id}/_execute
# operationId: ml.execute_agent.0
export def "plugins-ml-agents-execute agent0" [
  agent_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  parameters: record
  --parametersverbose: oneof<nothing, bool> # Whether to provide verbose output.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/agents/($agent_id)/_execute" $qp)
  let body = {parameters: $parameters, parameters.verbose: $parametersverbose} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Creates a standalone connector.
#
# POST /_plugins/_ml/connectors/_create
# operationId: ml.create_connector.0
# --credential shape: {access_key?: string, secret_key?: string, session_token?: string}
# --client_config shape: {max_connection?: int, connection_timeout?: int, read_timeout?: int, retry_backoff_policy?: string, max_retry_times?: int, retry_backoff_millis?: int, retry_timeout_seconds?: int}
# --actions item shape: {action_type?: string, method?: string, headers?: record, url?: string, request_body?: string, pre_process_function?: string, post_process_function?: string}
export def "plugins-ml-connectors-create connector0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  name: string
  description: string
  version: int
  protocol: string@protocol-completer
  credential: record # shape: {access_key?: string, secret_key?: string, session_token?: string}
  parameters: record
  --client-config: record # shape: {max_connection?: int, connection_timeout?: int, read_timeout?: int, retry_backoff_policy?: string, max_retry_times?: int, retry_backoff_millis?: int, retry_timeout_seconds?: int}
  actions: list # item shape: {action_type?: string, method?: string, headers?: record, url?: string, request_body?: string, pre_process_function?: string, post_process_function?: string}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/connectors/_create" $qp)
  let body = {name: $name, description: $description, version: $version, protocol: $protocol, credential: $credential, parameters: $parameters, client_config: $client_config, actions: $actions} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Searches for standalone connectors.
#
# GET /_plugins/_ml/connectors/_search
# operationId: ml.search_connectors.0
# --query shape: {bool?: record, match?: record, match_all?: record, term?: record}
export def "plugins-ml-connectors-search connectors0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # The query. — shape: {bool?: record, match?: record, match_all?: record, term?: record}
  --size: int # The number of connectors to return.
  --body-sort: list # The sort order.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/connectors/_search" $qp)
  let body = {query: $body_query, size: $size, sort: $body_sort} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Searches for standalone connectors.
#
# POST /_plugins/_ml/connectors/_search
# operationId: ml.search_connectors.1
# --query shape: {bool?: record, match?: record, match_all?: record, term?: record}
export def "plugins-ml-connectors-search connectors1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # The query. — shape: {bool?: record, match?: record, match_all?: record, term?: record}
  --size: int # The number of connectors to return.
  --body-sort: list # The sort order.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/connectors/_search" $qp)
  let body = {query: $body_query, size: $size, sort: $body_sort} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes a standalone connector.
#
# DELETE /_plugins/_ml/connectors/{connector_id}
# operationId: ml.delete_connector.0
export def "plugins-ml-connectors connector0-by-connector_id" [
  connector_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/connectors/($connector_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves a standalone connector.
#
# GET /_plugins/_ml/connectors/{connector_id}
# operationId: ml.get_connector.0
export def "plugins-ml-connectors connector0-by-connector_id-1" [
  connector_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/connectors/($connector_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Updates a standalone connector.
#
# PUT /_plugins/_ml/connectors/{connector_id}
# operationId: ml.update_connector.0
# --credential shape: {access_key?: string, secret_key?: string, session_token?: string}
# --actions item shape: {action_type?: string, method?: string, headers?: record, url?: string, request_body?: string, pre_process_function?: string, post_process_function?: string}
export def "plugins-ml-connectors connector0-by-connector_id-2" [
  connector_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --name: string # The name of a resource or configuration element.
  --description: string # The connector description.
  --version: int # format: int64
  --protocol: string@protocol-completer # The connector protocol.
  --parameters: record
  --credential: record # shape: {access_key?: string, secret_key?: string, session_token?: string}
  --actions: list # item shape: {action_type?: string, method?: string, headers?: record, url?: string, request_body?: string, pre_process_function?: string, post_process_function?: string}
  --backend-roles: list # The backend roles.
  --access-mode: string@access-mode-completer # The model group access mode.
  --parametersskip-validating-missing-parameters: oneof<nothing, bool> # Whether to skip validating missing parameters.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/connectors/($connector_id)" $qp)
  let body = {name: $name, description: $description, version: $version, protocol: $protocol, parameters: $parameters, credential: $credential, actions: $actions, backend_roles: $backend_roles, access_mode: $access_mode, parameters.skip_validating_missing_parameters: $parametersskip_validating_missing_parameters} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes a controller.
#
# DELETE /_plugins/_ml/controllers/{model_id}
# operationId: ml.delete_controller.0
export def "plugins-ml-controllers controller0-by-model_id" [
  model_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/controllers/($model_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves a controller.
#
# GET /_plugins/_ml/controllers/{model_id}
# operationId: ml.get_controller.0
export def "plugins-ml-controllers controller0-by-model_id-1" [
  model_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/controllers/($model_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates a controller.
#
# POST /_plugins/_ml/controllers/{model_id}
# operationId: ml.create_controller.0
export def "plugins-ml-controllers controller0-by-model_id-2" [
  model_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --user-rate-limiter: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/controllers/($model_id)" $qp)
  let body = {user_rate_limiter: $user_rate_limiter} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Updates a controller.
#
# PUT /_plugins/_ml/controllers/{model_id}
# operationId: ml.update_controller.0
export def "plugins-ml-controllers controller0-by-model_id-3" [
  model_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --user-rate-limiter: record
  --body-model-id: string # The name of a resource or configuration element.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/controllers/($model_id)" $qp)
  let body = {user_rate_limiter: $user_rate_limiter, model_id: $body_model_id} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get all memories.
#
# GET /_plugins/_ml/memory
# operationId: ml.get_all_memories.0
export def "plugins-ml-memory memories0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --max-results: int # format: int32
  --next-token: int # format: int32
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "max_results" $max_results "scalar") (serialize-qp "next_token" $next_token "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/memory" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create a memory.
#
# POST /_plugins/_ml/memory
# operationId: ml.create_memory.0
export def "plugins-ml-memory memory0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --name: string # The name of a resource or configuration element.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/memory" $qp)
  let body = {name: $name} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Search memory.
#
# GET /_plugins/_ml/memory/_search
# operationId: ml.search_memory.0
# --query shape: {bool?: record, match?: record, match_all?: record, term?: record}
# --sort item shape: {additional_info?: record, application_time?: record, create_time?: record, updated_time?: record, user?: record, _id?: record, _index?: record, _score?: record, _seq_no?: record}
export def "plugins-ml-memory-search memory0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # The query. — shape: {bool?: record, match?: record, match_all?: record, term?: record}
  --size: int # The number of memories to return. (format: int64)
  --body-sort: list # The sort order. — item shape: {additional_info?: record, application_time?: record, create_time?: record, updated_time?: record, user?: record, _id?: record, _index?: record, _score?: record, _seq_no?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/memory/_search" $qp)
  let body = {query: $body_query, size: $size, sort: $body_sort} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Search memory.
#
# POST /_plugins/_ml/memory/_search
# operationId: ml.search_memory.1
# --query shape: {bool?: record, match?: record, match_all?: record, term?: record}
# --sort item shape: {additional_info?: record, application_time?: record, create_time?: record, updated_time?: record, user?: record, _id?: record, _index?: record, _score?: record, _seq_no?: record}
export def "plugins-ml-memory-search memory1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # The query. — shape: {bool?: record, match?: record, match_all?: record, term?: record}
  --size: int # The number of memories to return. (format: int64)
  --body-sort: list # The sort order. — item shape: {additional_info?: record, application_time?: record, create_time?: record, updated_time?: record, user?: record, _id?: record, _index?: record, _score?: record, _seq_no?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/memory/_search" $qp)
  let body = {query: $body_query, size: $size, sort: $body_sort} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete a memory.
#
# DELETE /_plugins/_ml/memory/{memory_id}
# operationId: ml.delete_memory.0
export def "plugins-ml-memory memory0-by-memory_id" [
  memory_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/memory/($memory_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get a memory.
#
# GET /_plugins/_ml/memory/{memory_id}
# operationId: ml.get_memory.0
export def "plugins-ml-memory memory0-by-memory_id-1" [
  memory_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/memory/($memory_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update a memory.
#
# PUT /_plugins/_ml/memory/{memory_id}
# operationId: ml.update_memory.0
export def "plugins-ml-memory memory0-by-memory_id-2" [
  memory_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --name: string # The name of a resource or configuration element.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/memory/($memory_id)" $qp)
  let body = {name: $name} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Search messages.
#
# GET /_plugins/_ml/memory/{memory_id}/_search
# operationId: ml.search_message.0
# --query shape: {bool?: record, match?: record, match_all?: record, term?: record}
# --sort item shape: {additional_info?: record, create_time?: record, memory_id?: record, origin?: record, parent_message_id?: record, trace_number?: record, _id?: record, _index?: record, _score?: record, _seq_no?: record}
export def "plugins-ml-memory-search message0" [
  memory_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # The query. — shape: {bool?: record, match?: record, match_all?: record, term?: record}
  --size: int # The number of messages to return. (format: int64)
  --body-sort: list # The sort order. — item shape: {additional_info?: record, create_time?: record, memory_id?: record, origin?: record, parent_message_id?: record, trace_number?: record, _id?: record, _index?: record, _score?: record, _seq_no?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/memory/($memory_id)/_search" $qp)
  let body = {query: $body_query, size: $size, sort: $body_sort} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Search messages.
#
# POST /_plugins/_ml/memory/{memory_id}/_search
# operationId: ml.search_message.1
# --query shape: {bool?: record, match?: record, match_all?: record, term?: record}
# --sort item shape: {additional_info?: record, create_time?: record, memory_id?: record, origin?: record, parent_message_id?: record, trace_number?: record, _id?: record, _index?: record, _score?: record, _seq_no?: record}
export def "plugins-ml-memory-search message1" [
  memory_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # The query. — shape: {bool?: record, match?: record, match_all?: record, term?: record}
  --size: int # The number of messages to return. (format: int64)
  --body-sort: list # The sort order. — item shape: {additional_info?: record, create_time?: record, memory_id?: record, origin?: record, parent_message_id?: record, trace_number?: record, _id?: record, _index?: record, _score?: record, _seq_no?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/memory/($memory_id)/_search" $qp)
  let body = {query: $body_query, size: $size, sort: $body_sort} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get all messages in a memory.
#
# GET /_plugins/_ml/memory/{memory_id}/messages
# operationId: ml.get_all_messages.0
export def "plugins-ml-memory-messages messages0" [
  memory_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --max-results: int # format: int32
  --next-token: int # format: int32
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "max_results" $max_results "scalar") (serialize-qp "next_token" $next_token "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/memory/($memory_id)/messages" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create a message.
#
# POST /_plugins/_ml/memory/{memory_id}/messages
# operationId: ml.create_message.0
export def "plugins-ml-memory-messages message0" [
  memory_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --input: string # The question in the message.
  --prompt-template: string # The prompt template.
  --response: string # The answer to the question.
  --origin: string # The system name that generated the response.
  --additional-info: record # Additional information query.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/memory/($memory_id)/messages" $qp)
  let body = {input: $input, prompt_template: $prompt_template, response: $response, origin: $origin, additional_info: $additional_info} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get a message.
#
# GET /_plugins/_ml/memory/message/{message_id}
# operationId: ml.get_message.0
export def "plugins-ml-memory-message message0-by-message_id" [
  message_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/memory/message/($message_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update a message.
#
# PUT /_plugins/_ml/memory/message/{message_id}
# operationId: ml.update_message.0
export def "plugins-ml-memory-message message0-by-message_id-1" [
  message_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --input: string # The question in the message.
  --prompt-template: string # The prompt template.
  --response: string # The answer to the question.
  --origin: string # The system name that generated the response.
  --additional-info: record # Additional information query.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/memory/message/($message_id)" $qp)
  let body = {input: $input, prompt_template: $prompt_template, response: $response, origin: $origin, additional_info: $additional_info} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get a message traces.
#
# GET /_plugins/_ml/memory/message/{message_id}/traces
# operationId: ml.get_message_traces.0
export def "plugins-ml-memory-message-traces traces0" [
  message_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --max-results: int # format: int32
  --next-token: int # format: int32
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "max_results" $max_results "scalar") (serialize-qp "next_token" $next_token "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/memory/message/($message_id)/traces" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Registers a model group.
#
# POST /_plugins/_ml/model_groups/_register
# operationId: ml.register_model_group.0
export def "plugins-ml-model-groups-register group0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  name: string # The model group name.
  --description: string # The model group description.
  --access-mode: string@access-mode-completer # The model group access mode.
  --backend-roles: list # The backend roles.
  --add-all-backend-roles: oneof<nothing, bool> # The add all backend roles.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/model_groups/_register" $qp)
  let body = {name: $name, description: $description, access_mode: $access_mode, backend_roles: $backend_roles, add_all_backend_roles: $add_all_backend_roles} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Searches for model groups.
#
# GET /_plugins/_ml/model_groups/_search
# operationId: ml.search_model_group.0
# --query shape: {bool?: record, match?: record, match_all?: record, term?: record}
# --sort item shape: {algorithm?: record, auto_redeploy_retry_times?: record, chunk_number?: record, created_time?: record, current_worker_node_count?: record, deploy_to_all_nodes?: record, is_hidden?: record, last_registered_time?: record, last_updated_time?: record, model_content_hash_value?: record, model_content_size_in_bytes?: record, model_format?: record, model_group_id?: record, model_state?: record, model_version?: record, planning_worker_nodes?: record, planning_worker_node_count?: record, total_chunks?: record, _index?: record, _id?: record, _seq_no?: record, _score?: record}
export def "plugins-ml-model-groups-search group0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # The query. — shape: {bool?: record, match?: record, match_all?: record, term?: record}
  --size: int # The number of model groups to return. (format: int64)
  --body-sort: list # The sort order. — item shape: {algorithm?: record, auto_redeploy_retry_times?: record, chunk_number?: record, created_time?: record, current_worker_node_count?: record, deploy_to_all_nodes?: record, is_hidden?: record, last_registered_time?: record, last_updated_time?: record, model_content_hash_value?: record, model_content_size_in_bytes?: record, model_format?: record, model_group_id?: record, model_state?: record, model_version?: record, planning_worker_nodes?: record, planning_worker_node_count?: record, total_chunks?: record, _index?: record, _id?: record, _seq_no?: record, _score?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/model_groups/_search" $qp)
  let body = {query: $body_query, size: $size, sort: $body_sort} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Searches for model groups.
#
# POST /_plugins/_ml/model_groups/_search
# operationId: ml.search_model_group.1
# --query shape: {bool?: record, match?: record, match_all?: record, term?: record}
# --sort item shape: {algorithm?: record, auto_redeploy_retry_times?: record, chunk_number?: record, created_time?: record, current_worker_node_count?: record, deploy_to_all_nodes?: record, is_hidden?: record, last_registered_time?: record, last_updated_time?: record, model_content_hash_value?: record, model_content_size_in_bytes?: record, model_format?: record, model_group_id?: record, model_state?: record, model_version?: record, planning_worker_nodes?: record, planning_worker_node_count?: record, total_chunks?: record, _index?: record, _id?: record, _seq_no?: record, _score?: record}
export def "plugins-ml-model-groups-search group1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # The query. — shape: {bool?: record, match?: record, match_all?: record, term?: record}
  --size: int # The number of model groups to return. (format: int64)
  --body-sort: list # The sort order. — item shape: {algorithm?: record, auto_redeploy_retry_times?: record, chunk_number?: record, created_time?: record, current_worker_node_count?: record, deploy_to_all_nodes?: record, is_hidden?: record, last_registered_time?: record, last_updated_time?: record, model_content_hash_value?: record, model_content_size_in_bytes?: record, model_format?: record, model_group_id?: record, model_state?: record, model_version?: record, planning_worker_nodes?: record, planning_worker_node_count?: record, total_chunks?: record, _index?: record, _id?: record, _seq_no?: record, _score?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/model_groups/_search" $qp)
  let body = {query: $body_query, size: $size, sort: $body_sort} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes a model group.
#
# DELETE /_plugins/_ml/model_groups/{model_group_id}
# operationId: ml.delete_model_group.0
export def "plugins-ml-model-groups group0-by-model_group_id" [
  model_group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/model_groups/($model_group_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves a model group.
#
# GET /_plugins/_ml/model_groups/{model_group_id}
# operationId: ml.get_model_group.0
export def "plugins-ml-model-groups group0-by-model_group_id-1" [
  model_group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/model_groups/($model_group_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Updates a model group.
#
# PUT /_plugins/_ml/model_groups/{model_group_id}
# operationId: ml.update_model_group.0
export def "plugins-ml-model-groups group0-by-model_group_id-2" [
  model_group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --name: string # The model group name.
  --description: string # The model group description.
  --add-all-backend-roles: oneof<nothing, bool> # Whether to include all backend roles.
  --model-access-name: string # The model access name
  --backend-roles: list # The backend roles.
  --add-backend-roles: list # The backend roles to add.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/model_groups/($model_group_id)" $qp)
  let body = {name: $name, description: $description, add_all_backend_roles: $add_all_backend_roles, model_access_name: $model_access_name, backend_roles: $backend_roles, add_backend_roles: $add_backend_roles} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Registers a model.
#
# POST /_plugins/_ml/models/_register
# operationId: ml.register_model.0
export def "plugins-ml-models-register model0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  name: string # The model name.
  --version: string
  --model-format: string@model-format-completer
  --description: string # The model description.
  --model-group-id: string # The unique identifier for a resource.
  --function-name: string # The function name.
  --connector-id: string # The connector ID.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/models/_register" $qp)
  let body = {name: $name, version: $version, model_format: $model_format, description: $description, model_group_id: $model_group_id, function_name: $function_name, connector_id: $connector_id} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Registers model metadata.
#
# POST /_plugins/_ml/models/_register_meta
# operationId: ml.register_model_meta.0
# --model_config shape: {all_config?: string, model_type?: string, embedding_dimension?: int, framework_type?: string}
export def "plugins-ml-models-register-meta meta0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  name: string # The model name.
  version: string
  model_format: string@model-format-completer
  --model-group-id: string # The unique identifier for a resource.
  model_content_hash_value: string # The model content hash value.
  model_config: record # shape: {all_config?: string, model_type?: string, embedding_dimension?: int, framework_type?: string}
  total_chunks: int # Number of chunks the model is split into. (format: int64)
  --body-url: string # The model URL.
  --description: string # The model description.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/models/_register_meta" $qp)
  let body = {name: $name, version: $version, model_format: $model_format, model_group_id: $model_group_id, model_content_hash_value: $model_content_hash_value, model_config: $model_config, total_chunks: $total_chunks, url: $body_url, description: $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Searches for models.
#
# GET /_plugins/_ml/models/_search
# operationId: ml.search_models.0
# --query shape: {bool?: record, match?: record, match_all?: record, term?: record}
# --sort item shape: {algorithm?: record, auto_redeploy_retry_times?: record, chunk_number?: record, created_time?: record, current_worker_node_count?: record, deploy_to_all_nodes?: record, is_hidden?: record, last_registered_time?: record, last_updated_time?: record, model_content_hash_value?: record, model_content_size_in_bytes?: record, model_format?: record, model_group_id?: record, model_state?: record, model_version?: record, planning_worker_nodes?: record, planning_worker_node_count?: record, total_chunks?: record, _index?: record, _id?: record, _seq_no?: record, _score?: record}
export def "plugins-ml-models-search models0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # The query. — shape: {bool?: record, match?: record, match_all?: record, term?: record}
  --size: int # The number of models to return. (format: int64)
  --body-sort: list # The sort order. — item shape: {algorithm?: record, auto_redeploy_retry_times?: record, chunk_number?: record, created_time?: record, current_worker_node_count?: record, deploy_to_all_nodes?: record, is_hidden?: record, last_registered_time?: record, last_updated_time?: record, model_content_hash_value?: record, model_content_size_in_bytes?: record, model_format?: record, model_group_id?: record, model_state?: record, model_version?: record, planning_worker_nodes?: record, planning_worker_node_count?: record, total_chunks?: record, _index?: record, _id?: record, _seq_no?: record, _score?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/models/_search" $qp)
  let body = {query: $body_query, size: $size, sort: $body_sort} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Searches for models.
#
# POST /_plugins/_ml/models/_search
# operationId: ml.search_models.1
# --query shape: {bool?: record, match?: record, match_all?: record, term?: record}
# --sort item shape: {algorithm?: record, auto_redeploy_retry_times?: record, chunk_number?: record, created_time?: record, current_worker_node_count?: record, deploy_to_all_nodes?: record, is_hidden?: record, last_registered_time?: record, last_updated_time?: record, model_content_hash_value?: record, model_content_size_in_bytes?: record, model_format?: record, model_group_id?: record, model_state?: record, model_version?: record, planning_worker_nodes?: record, planning_worker_node_count?: record, total_chunks?: record, _index?: record, _id?: record, _seq_no?: record, _score?: record}
export def "plugins-ml-models-search models1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # The query. — shape: {bool?: record, match?: record, match_all?: record, term?: record}
  --size: int # The number of models to return. (format: int64)
  --body-sort: list # The sort order. — item shape: {algorithm?: record, auto_redeploy_retry_times?: record, chunk_number?: record, created_time?: record, current_worker_node_count?: record, deploy_to_all_nodes?: record, is_hidden?: record, last_registered_time?: record, last_updated_time?: record, model_content_hash_value?: record, model_content_size_in_bytes?: record, model_format?: record, model_group_id?: record, model_state?: record, model_version?: record, planning_worker_nodes?: record, planning_worker_node_count?: record, total_chunks?: record, _index?: record, _id?: record, _seq_no?: record, _score?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/models/_search" $qp)
  let body = {query: $body_query, size: $size, sort: $body_sort} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Undeploys a model.
#
# POST /_plugins/_ml/models/_undeploy
# operationId: ml.undeploy_model.1
export def "plugins-ml-models-undeploy model1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --node-ids: list
  --model-ids: list
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/models/_undeploy" $qp)
  let body = {node_ids: $node_ids, model_ids: $model_ids} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Unloads a model.
#
# POST /_plugins/_ml/models/_unload
# DEPRECATED
# operationId: ml.unload_model.1
@deprecated
export def "plugins-ml-models-unload model1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --node-ids: list
  --model-ids: list
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/models/_unload" $qp)
  let body = {node_ids: $node_ids, model_ids: $model_ids} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Registers a model.
#
# POST /_plugins/_ml/models/_upload
# DEPRECATED
# operationId: ml.upload_model.0
@deprecated
export def "plugins-ml-models-upload model0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  name: string # The model name.
  version: string
  --model-format: string@model-format-completer
  --description: string # The model description.
  --model-group-id: string # The unique identifier for a resource.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/models/_upload" $qp)
  let body = {name: $name, version: $version, model_format: $model_format, description: $description, model_group_id: $model_group_id} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes a model.
#
# DELETE /_plugins/_ml/models/{model_id}
# operationId: ml.delete_model.0
export def "plugins-ml-models model0-by-model_id" [
  model_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/models/($model_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves a model.
#
# GET /_plugins/_ml/models/{model_id}
# operationId: ml.get_model.0
export def "plugins-ml-models model0-by-model_id-1" [
  model_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/models/($model_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Updates a model.
#
# PUT /_plugins/_ml/models/{model_id}
# operationId: ml.update_model.0
# --model_config shape: {all_config?: string, model_type?: string, embedding_dimension?: int, framework_type?: string}
# --rate_limiter shape: {limit: any, unit: "DAYS"|"HOURS"|"MICROSECONDS"|"MILLISECONDS"|"MINUTES"|"NANOSECONDS"|"SECONDS"}
# --guardrails shape: {type?: "local_regex"|"model", input_guardrail?: record, output_guardrail?: record, stop_words?: any, index_name?: string, source_fields?: any, regex?: record, model_id?: string, response_filter?: string, response_validation_regex?: string}
export def "plugins-ml-models model0-by-model_id-2" [
  model_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --connector: record # The connector to use for the model.
  --connector-id: string # The connector ID.
  --is-enabled: oneof<nothing, bool> # Whether the model is enabled.
  --description: string # The model description.
  --model-config: record # shape: {all_config?: string, model_type?: string, embedding_dimension?: int, framework_type?: string}
  --name: string # The model name.
  --rate-limiter: record # shape: {limit: any, unit: "DAYS"|"HOURS"|"MICROSECONDS"|"MILLISECONDS"|"MINUTES"|"NANOSECONDS"|"SECONDS"}
  --guardrails: record # shape: {type?: "local_regex"|"model", input_guardrail?: record, output_guardrail?: record, stop_words?: any, index_name?: string, source_fields?: any, regex?: record, model_id?: string, response_filter?: string, response_validation_regex?: string}
  --interface: record # The model interface.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/models/($model_id)" $qp)
  let body = {connector: $connector, connector_id: $connector_id, is_enabled: $is_enabled, description: $description, model_config: $model_config, name: $name, rate_limiter: $rate_limiter, guardrails: $guardrails, interface: $interface} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deploys a model.
#
# POST /_plugins/_ml/models/{model_id}/_deploy
# operationId: ml.deploy_model.0
export def "plugins-ml-models-deploy model0" [
  model_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/models/($model_id)/_deploy" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Deploys a model.
#
# POST /_plugins/_ml/models/{model_id}/_load
# DEPRECATED
# operationId: ml.load_model.0
@deprecated
export def "plugins-ml-models-load model0" [
  model_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/models/($model_id)/_load" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Predicts a model.
#
# POST /_plugins/_ml/models/{model_id}/_predict
# operationId: ml.predict_model.0
export def "plugins-ml-models-predict model0" [
  model_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --query-text: string # The query text.
  text_docs: list # The text documents.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/models/($model_id)/_predict" $qp)
  let body = {query_text: $query_text, text_docs: $text_docs} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Undeploys a model.
#
# POST /_plugins/_ml/models/{model_id}/_undeploy
# operationId: ml.undeploy_model.0
export def "plugins-ml-models-undeploy model0" [
  model_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --node-ids: list
  --model-ids: list
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/models/($model_id)/_undeploy" $qp)
  let body = {node_ids: $node_ids, model_ids: $model_ids} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Unloads a model.
#
# POST /_plugins/_ml/models/{model_id}/_unload
# DEPRECATED
# operationId: ml.unload_model.0
@deprecated
export def "plugins-ml-models-unload model0" [
  model_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --node-ids: list
  --model-ids: list
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/models/($model_id)/_unload" $qp)
  let body = {node_ids: $node_ids, model_ids: $model_ids} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Uploads model chunk.
#
# POST /_plugins/_ml/models/{model_id}/chunk/{chunk_number}
# DEPRECATED
# operationId: ml.chunk_model.0
@deprecated
export def "plugins-ml-models-chunk model0" [
  chunk_number: int
  model_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  chunk: string # The model chunk.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/models/($model_id)/chunk/($chunk_number)" $qp)
  let body = {chunk: $chunk} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Uploads model chunk.
#
# POST /_plugins/_ml/models/{model_id}/upload_chunk/{chunk_number}
# operationId: ml.upload_chunk.0
export def "plugins-ml-models-upload-chunk chunk0" [
  chunk_number: int
  model_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  chunk: string # The model chunk.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/models/($model_id)/upload_chunk/($chunk_number)" $qp)
  let body = {chunk: $chunk} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Registers model metadata.
#
# POST /_plugins/_ml/models/meta
# DEPRECATED
# operationId: ml.create_model_meta.0
# --model_config shape: {all_config?: string, model_type?: string, embedding_dimension?: int, framework_type?: string}
@deprecated
export def "plugins-ml-models-meta meta0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  name: string # The model name.
  version: string
  model_format: string@model-format-completer
  --model-group-id: string # The unique identifier for a resource.
  model_content_hash_value: string # The model content hash value.
  model_config: record # shape: {all_config?: string, model_type?: string, embedding_dimension?: int, framework_type?: string}
  total_chunks: int # Number of chunks the model is split into. (format: int64)
  --body-url: string # The model URL.
  --description: string # The model description.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/models/meta" $qp)
  let body = {name: $name, version: $version, model_format: $model_format, model_group_id: $model_group_id, model_content_hash_value: $model_content_hash_value, model_config: $model_config, total_chunks: $total_chunks, url: $body_url, description: $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get a profile.
#
# GET /_plugins/_ml/profile
# operationId: ml.get_profile.0
export def "plugins-ml-profile profile0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --node-ids: list
  --model-ids: list
  --task-ids: list
  --return-all-tasks: oneof<nothing, bool> # Whether to return all tasks.
  --return-all-models: oneof<nothing, bool> # Whether to return all models.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/profile" $qp)
  let body = {node_ids: $node_ids, model_ids: $model_ids, task_ids: $task_ids, return_all_tasks: $return_all_tasks, return_all_models: $return_all_models} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get a profile models.
#
# GET /_plugins/_ml/profile/models
# operationId: ml.get_profile_models.0
export def "plugins-ml-profile-models models0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --node-ids: list
  --model-ids: list
  --task-ids: list
  --return-all-tasks: oneof<nothing, bool> # Whether to return all tasks.
  --return-all-models: oneof<nothing, bool> # Whether to return all models.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/profile/models" $qp)
  let body = {node_ids: $node_ids, model_ids: $model_ids, task_ids: $task_ids, return_all_tasks: $return_all_tasks, return_all_models: $return_all_models} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get a profile models.
#
# GET /_plugins/_ml/profile/models/{model_id}
# operationId: ml.get_profile_models.1
export def "plugins-ml-profile-models models1" [
  model_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --node-ids: list
  --model-ids: list
  --task-ids: list
  --return-all-tasks: oneof<nothing, bool> # Whether to return all tasks.
  --return-all-models: oneof<nothing, bool> # Whether to return all models.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/profile/models/($model_id)" $qp)
  let body = {node_ids: $node_ids, model_ids: $model_ids, task_ids: $task_ids, return_all_tasks: $return_all_tasks, return_all_models: $return_all_models} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get a profile tasks.
#
# GET /_plugins/_ml/profile/tasks
# operationId: ml.get_profile_tasks.0
export def "plugins-ml-profile-tasks tasks0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --node-ids: list
  --model-ids: list
  --task-ids: list
  --return-all-tasks: oneof<nothing, bool> # Whether to return all tasks.
  --return-all-models: oneof<nothing, bool> # Whether to return all models.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/profile/tasks" $qp)
  let body = {node_ids: $node_ids, model_ids: $model_ids, task_ids: $task_ids, return_all_tasks: $return_all_tasks, return_all_models: $return_all_models} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get a profile tasks.
#
# GET /_plugins/_ml/profile/tasks/{task_id}
# operationId: ml.get_profile_tasks.1
export def "plugins-ml-profile-tasks tasks1" [
  task_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --node-ids: list
  --model-ids: list
  --task-ids: list
  --return-all-tasks: oneof<nothing, bool> # Whether to return all tasks.
  --return-all-models: oneof<nothing, bool> # Whether to return all models.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/profile/tasks/($task_id)" $qp)
  let body = {node_ids: $node_ids, model_ids: $model_ids, task_ids: $task_ids, return_all_tasks: $return_all_tasks, return_all_models: $return_all_models} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get stats.
#
# GET /_plugins/_ml/stats
# operationId: ml.get_stats.0
export def "plugins-ml-stats stats0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get stats.
#
# GET /_plugins/_ml/stats/{stat}
# operationId: ml.get_stats.1
export def "plugins-ml-stats stats1" [
  stat: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/stats/($stat)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Searches for tasks.
#
# GET /_plugins/_ml/tasks/_search
# operationId: ml.search_tasks.0
# --query shape: {bool?: record, match?: record, match_all?: record, term?: record}
# --sort item shape: {algorithm?: record, auto_redeploy_retry_times?: record, chunk_number?: record, created_time?: record, current_worker_node_count?: record, deploy_to_all_nodes?: record, is_hidden?: record, last_registered_time?: record, last_updated_time?: record, model_content_hash_value?: record, model_content_size_in_bytes?: record, model_format?: record, model_group_id?: record, model_state?: record, model_version?: record, planning_worker_nodes?: record, planning_worker_node_count?: record, total_chunks?: record, _index?: record, _id?: record, _seq_no?: record, _score?: record}
export def "plugins-ml-tasks-search tasks0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # The query. — shape: {bool?: record, match?: record, match_all?: record, term?: record}
  --size: int # The number of tasks to return. (format: int64)
  --body-sort: list # The sort order. — item shape: {algorithm?: record, auto_redeploy_retry_times?: record, chunk_number?: record, created_time?: record, current_worker_node_count?: record, deploy_to_all_nodes?: record, is_hidden?: record, last_registered_time?: record, last_updated_time?: record, model_content_hash_value?: record, model_content_size_in_bytes?: record, model_format?: record, model_group_id?: record, model_state?: record, model_version?: record, planning_worker_nodes?: record, planning_worker_node_count?: record, total_chunks?: record, _index?: record, _id?: record, _seq_no?: record, _score?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/tasks/_search" $qp)
  let body = {query: $body_query, size: $size, sort: $body_sort} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Searches for tasks.
#
# POST /_plugins/_ml/tasks/_search
# operationId: ml.search_tasks.1
# --query shape: {bool?: record, match?: record, match_all?: record, term?: record}
# --sort item shape: {algorithm?: record, auto_redeploy_retry_times?: record, chunk_number?: record, created_time?: record, current_worker_node_count?: record, deploy_to_all_nodes?: record, is_hidden?: record, last_registered_time?: record, last_updated_time?: record, model_content_hash_value?: record, model_content_size_in_bytes?: record, model_format?: record, model_group_id?: record, model_state?: record, model_version?: record, planning_worker_nodes?: record, planning_worker_node_count?: record, total_chunks?: record, _index?: record, _id?: record, _seq_no?: record, _score?: record}
export def "plugins-ml-tasks-search tasks1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # The query. — shape: {bool?: record, match?: record, match_all?: record, term?: record}
  --size: int # The number of tasks to return. (format: int64)
  --body-sort: list # The sort order. — item shape: {algorithm?: record, auto_redeploy_retry_times?: record, chunk_number?: record, created_time?: record, current_worker_node_count?: record, deploy_to_all_nodes?: record, is_hidden?: record, last_registered_time?: record, last_updated_time?: record, model_content_hash_value?: record, model_content_size_in_bytes?: record, model_format?: record, model_group_id?: record, model_state?: record, model_version?: record, planning_worker_nodes?: record, planning_worker_node_count?: record, total_chunks?: record, _index?: record, _id?: record, _seq_no?: record, _score?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/tasks/_search" $qp)
  let body = {query: $body_query, size: $size, sort: $body_sort} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes a task.
#
# DELETE /_plugins/_ml/tasks/{task_id}
# operationId: ml.delete_task.0
export def "plugins-ml-tasks task0-by-task_id" [
  task_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/tasks/($task_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves a task.
#
# GET /_plugins/_ml/tasks/{task_id}
# operationId: ml.get_task.0
export def "plugins-ml-tasks task0-by-task_id-1" [
  task_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/tasks/($task_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get tools.
#
# GET /_plugins/_ml/tools
# operationId: ml.get_all_tools.0
export def "plugins-ml-tools tools0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ml/tools" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get tools.
#
# GET /_plugins/_ml/tools/{tool_name}
# operationId: ml.get_tool.0
export def "plugins-ml-tools tool0" [
  tool_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_ml/tools/($tool_name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Provides information about the current status of the neural-search plugin.
#
# GET /_plugins/_neural/{node_id}/stats
# operationId: neural.stats.2
export def "plugins-neural-stats neuralstats2" [
  node_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --flat-stat-paths: oneof<nothing, bool> # Whether to return stats in the flat form, which can improve readability, especially for heavily nested stats. For example, the flat form of `"processors": { "ingest": { "text_embedding_executions": 20181212 } }` is  `"processors.ingest.text_embedding_executions": "20181212"`. (default: false)
  --include-metadata: oneof<nothing, bool> # Whether to return stat metadata instead of the raw stat value, includes additional information about the stat. These can include things like type hints, time since last stats being recorded, or recent rolling interval values (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "flat_stat_paths" $flat_stat_paths "scalar") (serialize-qp "include_metadata" $include_metadata "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_neural/($node_id)/stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Provides information about the current status of the neural-search plugin.
#
# GET /_plugins/_neural/{node_id}/stats/{stat}
# operationId: neural.stats.3
export def "plugins-neural-stats neuralstats3" [
  node_id: string
  stat: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --flat-stat-paths: oneof<nothing, bool> # Whether to return stats in the flat form, which can improve readability, especially for heavily nested stats. For example, the flat form of `"processors": { "ingest": { "text_embedding_executions": 20181212 } }` is  `"processors.ingest.text_embedding_executions": "20181212"`. (default: false)
  --include-metadata: oneof<nothing, bool> # Whether to return stat metadata instead of the raw stat value, includes additional information about the stat. These can include things like type hints, time since last stats being recorded, or recent rolling interval values (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "flat_stat_paths" $flat_stat_paths "scalar") (serialize-qp "include_metadata" $include_metadata "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_neural/($node_id)/stats/($stat)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Provides information about the current status of the neural-search plugin.
#
# GET /_plugins/_neural/stats
# operationId: neural.stats.0
export def "plugins-neural-stats neuralstats0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --flat-stat-paths: oneof<nothing, bool> # Whether to return stats in the flat form, which can improve readability, especially for heavily nested stats. For example, the flat form of `"processors": { "ingest": { "text_embedding_executions": 20181212 } }` is  `"processors.ingest.text_embedding_executions": "20181212"`. (default: false)
  --include-metadata: oneof<nothing, bool> # Whether to return stat metadata instead of the raw stat value, includes additional information about the stat. These can include things like type hints, time since last stats being recorded, or recent rolling interval values (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "flat_stat_paths" $flat_stat_paths "scalar") (serialize-qp "include_metadata" $include_metadata "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_neural/stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Provides information about the current status of the neural-search plugin.
#
# GET /_plugins/_neural/stats/{stat}
# operationId: neural.stats.1
export def "plugins-neural-stats neuralstats1" [
  stat: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --flat-stat-paths: oneof<nothing, bool> # Whether to return stats in the flat form, which can improve readability, especially for heavily nested stats. For example, the flat form of `"processors": { "ingest": { "text_embedding_executions": 20181212 } }` is  `"processors.ingest.text_embedding_executions": "20181212"`. (default: false)
  --include-metadata: oneof<nothing, bool> # Whether to return stat metadata instead of the raw stat value, includes additional information about the stat. These can include things like type hints, time since last stats being recorded, or recent rolling interval values (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "flat_stat_paths" $flat_stat_paths "scalar") (serialize-qp "include_metadata" $include_metadata "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_neural/stats/($stat)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List created notification channels.
#
# GET /_plugins/_notifications/channels
# Docs: https://opensearch.org/docs/latest/observing-your-data/notifications/api/#list-all-notification-channels
# operationId: notifications.list_channels.0
export def "plugins-notifications-channels channels0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_notifications/channels" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Delete multiple channel configurations.
#
# DELETE /_plugins/_notifications/configs
# Docs: https://opensearch.org/docs/latest/observing-your-data/notifications/api/#delete-channel-configuration
# operationId: notifications.delete_configs.0
export def "plugins-notifications-configs configs0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --config-id: string # The ID of the channel configuration to delete.
  --config-id-list: string # A comma-separated list of channel IDs to delete.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "config_id" $config_id "scalar") (serialize-qp "config_id_list" $config_id_list "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_notifications/configs" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get multiple channel configurations with filtering.
#
# GET /_plugins/_notifications/configs
# Docs: https://opensearch.org/docs/latest/observing-your-data/notifications/api/#list-all-notification-configurations
# operationId: notifications.get_configs.0
export def "plugins-notifications-configs configs0-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --chimeurl: string
  --chimeurlkeyword: string
  --config-id: string # Notification configuration ID.
  --config-id-list: list # Notification configuration IDs.
  --config-type: string@config-type-completer # Type of notification configuration.
  --created-time-ms: int # format: int64
  --description: string
  --descriptionkeyword: string
  --emailemail-account-id: string
  --emailemail-group-id-list: string
  --emailrecipient-listrecipient: string
  --emailrecipient-listrecipientkeyword: string
  --email-grouprecipient-listrecipient: string
  --email-grouprecipient-listrecipientkeyword: string
  --is-enabled: oneof<nothing, bool>
  --last-updated-time-ms: int # format: int64
  --microsoft-teamsurl: string
  --microsoft-teamsurlkeyword: string
  --name: string
  --namekeyword: string
  --qp-query: string
  --ses-accountfrom-address: string
  --ses-accountfrom-addresskeyword: string
  --ses-accountregion: string
  --ses-accountrole-arn: string
  --ses-accountrole-arnkeyword: string
  --slackurl: string
  --slackurlkeyword: string
  --smtp-accountfrom-address: string
  --smtp-accountfrom-addresskeyword: string
  --smtp-accounthost: string
  --smtp-accounthostkeyword: string
  --smtp-accountmethod: string
  --snsrole-arn: string
  --snsrole-arnkeyword: string
  --snstopic-arn: string
  --snstopic-arnkeyword: string
  --text-query: string
  --webhookurl: string
  --webhookurlkeyword: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --config-id-list: list
  --sort-field: string
  --sort-order: string
  --from-index: int # format: int32
  --max-items: int # format: int32
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "chime.url" $chimeurl "scalar") (serialize-qp "chime.url.keyword" $chimeurlkeyword "scalar") (serialize-qp "config_id" $config_id "scalar") (serialize-qp "config_id_list" $config_id_list "multi") (serialize-qp "config_type" $config_type "scalar") (serialize-qp "created_time_ms" $created_time_ms "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "description.keyword" $descriptionkeyword "scalar") (serialize-qp "email.email_account_id" $emailemail_account_id "scalar") (serialize-qp "email.email_group_id_list" $emailemail_group_id_list "scalar") (serialize-qp "email.recipient_list.recipient" $emailrecipient_listrecipient "scalar") (serialize-qp "email.recipient_list.recipient.keyword" $emailrecipient_listrecipientkeyword "scalar") (serialize-qp "email_group.recipient_list.recipient" $email_grouprecipient_listrecipient "scalar") (serialize-qp "email_group.recipient_list.recipient.keyword" $email_grouprecipient_listrecipientkeyword "scalar") (serialize-qp "is_enabled" $is_enabled "scalar") (serialize-qp "last_updated_time_ms" $last_updated_time_ms "scalar") (serialize-qp "microsoft_teams.url" $microsoft_teamsurl "scalar") (serialize-qp "microsoft_teams.url.keyword" $microsoft_teamsurlkeyword "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "name.keyword" $namekeyword "scalar") (serialize-qp "query" $qp_query "scalar") (serialize-qp "ses_account.from_address" $ses_accountfrom_address "scalar") (serialize-qp "ses_account.from_address.keyword" $ses_accountfrom_addresskeyword "scalar") (serialize-qp "ses_account.region" $ses_accountregion "scalar") (serialize-qp "ses_account.role_arn" $ses_accountrole_arn "scalar") (serialize-qp "ses_account.role_arn.keyword" $ses_accountrole_arnkeyword "scalar") (serialize-qp "slack.url" $slackurl "scalar") (serialize-qp "slack.url.keyword" $slackurlkeyword "scalar") (serialize-qp "smtp_account.from_address" $smtp_accountfrom_address "scalar") (serialize-qp "smtp_account.from_address.keyword" $smtp_accountfrom_addresskeyword "scalar") (serialize-qp "smtp_account.host" $smtp_accounthost "scalar") (serialize-qp "smtp_account.host.keyword" $smtp_accounthostkeyword "scalar") (serialize-qp "smtp_account.method" $smtp_accountmethod "scalar") (serialize-qp "sns.role_arn" $snsrole_arn "scalar") (serialize-qp "sns.role_arn.keyword" $snsrole_arnkeyword "scalar") (serialize-qp "sns.topic_arn" $snstopic_arn "scalar") (serialize-qp "sns.topic_arn.keyword" $snstopic_arnkeyword "scalar") (serialize-qp "text_query" $text_query "scalar") (serialize-qp "webhook.url" $webhookurl "scalar") (serialize-qp "webhook.url.keyword" $webhookurlkeyword "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_notifications/configs" $qp)
  let body = {config_id_list: $config_id_list, sort_field: $sort_field, sort_order: $sort_order, from_index: $from_index, max_items: $max_items} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Create channel configuration.
#
# POST /_plugins/_notifications/configs
# Docs: https://opensearch.org/docs/latest/observing-your-data/notifications/api/#create-channel-configuration
# operationId: notifications.create_config.0
# --config shape: {name: string, description?: string, config_type: "chime"|"email"|"email_group"|"microsoft_teams"|"ses_account"|"slack"|"smtp_account"|"sns"|"webhook", is_enabled?: bool, sns?: record, slack?: record, chime?: record, webhook?: record, smtp_account?: record, ses_account?: record, email_group?: record, email?: record, microsoft_teams?: record}
export def "plugins-notifications-configs config0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --config-id: string
  config: record # shape: {name: string, description?: string, config_type: "chime"|"email"|"email_group"|"microsoft_teams"|"ses_account"|"slack"|"smtp_account"|"sns"|"webhook", is_enabled?: bool, sns?: record, slack?: record, chime?: record, webhook?: record, smtp_account?: record, ses_account?: record, email_group?: record, email?: record, microsoft_teams?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_notifications/configs" $qp)
  let body = {config_id: $config_id, config: $config} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete a channel configuration.
#
# DELETE /_plugins/_notifications/configs/{config_id}
# Docs: https://opensearch.org/docs/latest/observing-your-data/notifications/api/#delete-channel-configuration
# operationId: notifications.delete_config.0
export def "plugins-notifications-configs config0-by-config_id" [
  config_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_notifications/configs/($config_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get a specific channel configuration.
#
# GET /_plugins/_notifications/configs/{config_id}
# operationId: notifications.get_config.0
export def "plugins-notifications-configs config0-by-config_id-1" [
  config_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_notifications/configs/($config_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update channel configuration.
#
# PUT /_plugins/_notifications/configs/{config_id}
# Docs: https://opensearch.org/docs/latest/observing-your-data/notifications/api/#update-channel-configuration
# operationId: notifications.update_config.0
# --config shape: {name: string, description?: string, config_type: "chime"|"email"|"email_group"|"microsoft_teams"|"ses_account"|"slack"|"smtp_account"|"sns"|"webhook", is_enabled?: bool, sns?: record, slack?: record, chime?: record, webhook?: record, smtp_account?: record, ses_account?: record, email_group?: record, email?: record, microsoft_teams?: record}
export def "plugins-notifications-configs config0-by-config_id-2" [
  config_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-config-id: string
  config: record # shape: {name: string, description?: string, config_type: "chime"|"email"|"email_group"|"microsoft_teams"|"ses_account"|"slack"|"smtp_account"|"sns"|"webhook", is_enabled?: bool, sns?: record, slack?: record, chime?: record, webhook?: record, smtp_account?: record, ses_account?: record, email_group?: record, email?: record, microsoft_teams?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_notifications/configs/($config_id)" $qp)
  let body = {config_id: $body_config_id, config: $config} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Send a test notification.
#
# GET /_plugins/_notifications/feature/test/{config_id}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/observing-your-data/notifications/api/#send-test-notification
# operationId: notifications.send_test.0
@deprecated
export def "plugins-notifications-feature-test test0" [
  config_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_notifications/feature/test/($config_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Send a test notification.
#
# POST /_plugins/_notifications/feature/test/{config_id}
# Docs: https://opensearch.org/docs/latest/observing-your-data/notifications/api/#send-test-notification
# operationId: notifications.send_test.1
export def "plugins-notifications-feature-test test1" [
  config_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_notifications/feature/test/($config_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List supported channel configurations.
#
# GET /_plugins/_notifications/features
# Docs: https://opensearch.org/docs/latest/observing-your-data/notifications/api/#list-supported-channel-configurations
# operationId: notifications.list_features.0
export def "plugins-notifications-features features0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_notifications/features" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves local stats of all observability objects.
#
# GET /_plugins/_observability/_local/stats
# operationId: observability.get_localstats.0
export def "plugins-observability-local-stats localstats0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_observability/_local/stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Deletes specific observability objects specified by ID or a list of IDs.
#
# DELETE /_plugins/_observability/object
# operationId: observability.delete_objects.0
export def "plugins-observability-object objects0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --objectId: string # The ID of a single observability object to delete.
  --objectIdList: string # A comma-separated list of observability object IDs to delete.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "objectId" $objectId "scalar") (serialize-qp "objectIdList" $objectIdList "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_observability/object" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves list of all observability objects.
#
# GET /_plugins/_observability/object
# operationId: observability.list_objects.0
export def "plugins-observability-object objects0-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_observability/object" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates a new observability object.
#
# POST /_plugins/_observability/object
# operationId: observability.create_object.0
# --operationalPanel shape: {name: string, visualizations: list, timeRange: record, queryFilter: record, applicationId: string}
# --savedVisualization shape: {name: string, description: string, query: string, type: string, selected_date_range: record, selected_timestamp: record, selected_fields: record}
# --savedQuery shape: {name: string, description: string, query: string, selected_date_range: record, selected_timestamp: record, selected_fields: record}
export def "plugins-observability-object object0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  objectId: string # The unique identifier of the observability object.
  --lastUpdatedTimeMs: int # When the object was last updated in milliseconds.
  --createdTimeMs: int # When the object was created in milliseconds.
  --tenant: string # The tenant associated with the observability object.
  --operationalPanel: record # The panel configuration for operational visualizations. — shape: {name: string, visualizations: list, timeRange: record, queryFilter: record, applicationId: string}
  --savedVisualization: record # The configuration for a saved visualization. — shape: {name: string, description: string, query: string, type: string, selected_date_range: record, selected_timestamp: record, selected_fields: record}
  --savedQuery: record # The configuration for a saved query. — shape: {name: string, description: string, query: string, selected_date_range: record, selected_timestamp: record, selected_fields: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_observability/object" $qp)
  let body = {objectId: $objectId, lastUpdatedTimeMs: $lastUpdatedTimeMs, createdTimeMs: $createdTimeMs, tenant: $tenant, operationalPanel: $operationalPanel, savedVisualization: $savedVisualization, savedQuery: $savedQuery} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes specific observability object specified by ID.
#
# DELETE /_plugins/_observability/object/{object_id}
# operationId: observability.delete_object.0
export def "plugins-observability-object object0-by-object_id" [
  object_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_observability/object/($object_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves specific observability object specified by ID.
#
# GET /_plugins/_observability/object/{object_id}
# operationId: observability.get_object.0
export def "plugins-observability-object object0-by-object_id-1" [
  object_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_observability/object/($object_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Updates an existing observability object.
#
# PUT /_plugins/_observability/object/{object_id}
# operationId: observability.update_object.0
# --operationalPanel shape: {name: string, visualizations: list, timeRange: record, queryFilter: record, applicationId: string}
# --savedVisualization shape: {name: string, description: string, query: string, type: string, selected_date_range: record, selected_timestamp: record, selected_fields: record}
# --savedQuery shape: {name: string, description: string, query: string, selected_date_range: record, selected_timestamp: record, selected_fields: record}
export def "plugins-observability-object object0-by-object_id-2" [
  object_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  objectId: string # The unique identifier of the observability object.
  --lastUpdatedTimeMs: int # When the object was last updated in milliseconds.
  --createdTimeMs: int # When the object was created in milliseconds.
  --tenant: string # The tenant associated with the observability object.
  --operationalPanel: record # The panel configuration for operational visualizations. — shape: {name: string, visualizations: list, timeRange: record, queryFilter: record, applicationId: string}
  --savedVisualization: record # The configuration for a saved visualization. — shape: {name: string, description: string, query: string, type: string, selected_date_range: record, selected_timestamp: record, selected_fields: record}
  --savedQuery: record # The configuration for a saved query. — shape: {name: string, description: string, query: string, selected_date_range: record, selected_timestamp: record, selected_fields: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_observability/object/($object_id)" $qp)
  let body = {objectId: $objectId, lastUpdatedTimeMs: $lastUpdatedTimeMs, createdTimeMs: $createdTimeMs, tenant: $tenant, operationalPanel: $operationalPanel, savedVisualization: $savedVisualization, savedQuery: $savedQuery} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Executes a PPL query against OpenSearch indexes.
#
# POST /_plugins/_ppl
# Docs: https://opensearch.org/docs/latest/search-plugins/sql/sql-ppl-api/
# operationId: ppl.query.0
export def "plugins-ppl pplquery0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --format: string # Specifies the response format (JSON OR YAML).
  --sanitize: oneof<nothing, bool> # Whether to sanitize special characters in the results. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: string # The SQL query string to execute.
  --filter: record # The filter to apply to query results.
  --fetch-size: int # The number of results to return in each response.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "format" $format "scalar") (serialize-qp "sanitize" $sanitize "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ppl" $qp)
  let body = {query: $body_query, filter: $filter, fetch_size: $fetch_size} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns the execution plan for a PPL query.
#
# POST /_plugins/_ppl/_explain
# Docs: https://opensearch.org/docs/latest/search-plugins/sql/sql-ppl-api/
# operationId: ppl.explain.0
export def "plugins-ppl-explain pplexplain0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --format: string # Specifies the response format (JSON, YAML).
  --sanitize: oneof<nothing, bool> # Whether to escape special characters in the results. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: string # The SQL query to explain.
  --filter: record # The filter to apply when explaining the query.
  --fetch-size: int # The number of results to analyze.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "format" $format "scalar") (serialize-qp "sanitize" $sanitize "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ppl/_explain" $qp)
  let body = {query: $body_query, filter: $filter, fetch_size: $fetch_size} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves performance metrics for the PPL plugin.
#
# GET /_plugins/_ppl/stats
# Docs: https://opensearch.org/docs/latest/search-plugins/sql/monitoring/
# operationId: ppl.get_stats.0
export def "plugins-ppl-stats stats0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --format: string # Specifies the response format (JSON, YAML).
  --sanitize: oneof<nothing, bool> # Whether to escape special characters in the results. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "format" $format "scalar") (serialize-qp "sanitize" $sanitize "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ppl/stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves filtered performance metrics for the PPL plugin.
#
# POST /_plugins/_ppl/stats
# Docs: https://opensearch.org/docs/latest/search-plugins/sql/monitoring/
# operationId: ppl.post_stats.1
export def "plugins-ppl-stats stats1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --format: string # Specifies the response format (JSON, YAML).
  --sanitize: oneof<nothing, bool> # Whether to escape special characters in the results. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --start-time: string # When the query execution started.
  --end-time: record # When the query execution ended.
  --cluster-name: record # The name of the cluster executing the query.
  --index: record # The index information for the query.
  --body-query: record # The query information.
  --user: record # The user information.
  --execution-time: record # The time taken to execute the query.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "format" $format "scalar") (serialize-qp "sanitize" $sanitize "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_ppl/stats" $qp)
  let body = {start_time: $start_time, end_time: $end_time, cluster_name: $cluster_name, index: $index, query: $body_query, user: $user, execution_time: $execution_time} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves a list of all available data sources.
#
# GET /_plugins/_query/_datasources
# operationId: query.datasources_list.0
export def "plugins-query-datasources list0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_query/_datasources" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates a new query data source.
#
# POST /_plugins/_query/_datasources
# operationId: query.datasources_create.0
# --configuration shape: {endpoint: string, credentials: record}
export def "plugins-query-datasources create0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  name: string # The name of the data source.
  --description: string # The description of the data source.
  connector: string # The connector type for the data source.
  --allowedRoles: list
  properties: record # The configuration properties for the data source.
  resultIndex: string # The index where query results are stored.
  status: string # The current status of the data source.
  --configuration: record # shape: {endpoint: string, credentials: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_query/_datasources" $qp)
  let body = {name: $name, description: $description, connector: $connector, allowedRoles: $allowedRoles, properties: $properties, resultIndex: $resultIndex, status: $status, configuration: $configuration} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Updates an existing query data source.
#
# PUT /_plugins/_query/_datasources
# operationId: query.datasources_update.0
# --configuration shape: {endpoint: string, credentials: record}
export def "plugins-query-datasources update0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  name: string # The name of the data source.
  --description: string # The description of the data source.
  connector: string # The connector type for the data source.
  --allowedRoles: list
  properties: record # The configuration properties for the data source.
  resultIndex: string # The index where query results are stored.
  status: string # The current status of the data source.
  --configuration: record # shape: {endpoint: string, credentials: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_query/_datasources" $qp)
  let body = {name: $name, description: $description, connector: $connector, allowedRoles: $allowedRoles, properties: $properties, resultIndex: $resultIndex, status: $status, configuration: $configuration} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes a specific data source by name.
#
# DELETE /_plugins/_query/_datasources/{datasource_name}
# operationId: query.datasource_delete.0
export def "plugins-query-datasources delete0" [
  datasource_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_query/_datasources/($datasource_name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves a specific data source by name.
#
# GET /_plugins/_query/_datasources/{datasource_name}
# operationId: query.datasource_retrieve.0
export def "plugins-query-datasources retrieve0" [
  datasource_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_query/_datasources/($datasource_name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Updates SQL plugin settings in the OpenSearch cluster configuration.
#
# PUT /_plugins/_query/settings
# Docs: https://opensearch.org/docs/latest/search-plugins/sql/settings/
# operationId: sql.settings.0
# --transient shape: {plugins.sql.enabled?: bool, plugins.ppl.enabled?: bool, plugins.sql.slowlog?: int, plugins.sql.cursor.keep_alive?: string, plugins.query.memory_limit?: string, plugins.query.size_limit?: int}
export def "plugins-query-settings sqlsettings0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --format: string # Specifies the response format (JSON or YAML).
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --transient: record # shape: {plugins.sql.enabled?: bool, plugins.ppl.enabled?: bool, plugins.sql.slowlog?: int, plugins.sql.cursor.keep_alive?: string, plugins.query.memory_limit?: string, plugins.query.size_limit?: int}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "format" $format "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_query/settings" $qp)
  let body = {transient: $transient} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Refreshes search analyzers in real time.
#
# POST /_plugins/_refresh_search_analyzers/{index}
# Docs: https://opensearch.org/docs/latest/im-plugin/refresh-analyzer/
# operationId: ism.refresh_search_analyzers.0
export def "plugins-refresh-search-analyzers analyzers0" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_refresh_search_analyzers/($index)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Deletes the specified replication rule.
#
# DELETE /_plugins/_replication/_autofollow
# Docs: https://opensearch.org/docs/latest/tuning-your-cluster/replication-plugin/api/#delete-replication-rule
# operationId: replication.delete_replication_rule.0
export def "plugins-replication-autofollow rule0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --leader-alias: string # The name of an index alias.
  --name: string # The name of the replication rule to delete.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_replication/_autofollow" $qp)
  let body = {leader_alias: $leader_alias, name: $name} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Automatically starts the replication on indexes matching a specified pattern.
#
# POST /_plugins/_replication/_autofollow
# Docs: https://opensearch.org/docs/latest/tuning-your-cluster/replication-plugin/api/#create-replication-rule
# operationId: replication.create_replication_rule.0
# --use_roles shape: {leader_cluster_role?: string, follower_cluster_role?: string}
export def "plugins-replication-autofollow rule0-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --leader-alias: string # The name of an index alias.
  --name: string # The name of the replication rule.
  --pattern: string # The pattern used to match indices for replication.
  --use-roles: record # shape: {leader_cluster_role?: string, follower_cluster_role?: string}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_replication/_autofollow" $qp)
  let body = {leader_alias: $leader_alias, name: $name, pattern: $pattern, use_roles: $use_roles} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Pauses the replication of the leader index.
#
# POST /_plugins/_replication/{index}/_pause
# Docs: https://opensearch.org/docs/latest/tuning-your-cluster/replication-plugin/api/#pause-replication
# operationId: replication.pause.0
export def "plugins-replication-pause replicationpause0" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_replication/($index)/_pause" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Resumes replication of the leader index.
#
# POST /_plugins/_replication/{index}/_resume
# Docs: https://opensearch.org/docs/latest/tuning-your-cluster/replication-plugin/api/#resume-replication
# operationId: replication.resume.0
export def "plugins-replication-resume replicationresume0" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_replication/($index)/_resume" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Initiates the replication of an index from the leader cluster to the follower cluster.
#
# PUT /_plugins/_replication/{index}/_start
# Docs: https://opensearch.org/docs/latest/tuning-your-cluster/replication-plugin/api/#start-replication
# operationId: replication.start.0
# --use_roles shape: {leader_cluster_role?: string, follower_cluster_role?: string}
export def "plugins-replication-start replicationstart0" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --leader-alias: string # The name of an index alias.
  --leader-index: string
  --use-roles: record # shape: {leader_cluster_role?: string, follower_cluster_role?: string}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_replication/($index)/_start" $qp)
  let body = {leader_alias: $leader_alias, leader_index: $leader_index, use_roles: $use_roles} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves the the status of an index replication.
#
# GET /_plugins/_replication/{index}/_status
# Docs: https://opensearch.org/docs/latest/tuning-your-cluster/replication-plugin/api/#get-replication-status
# operationId: replication.status.0
export def "plugins-replication-status replicationstatus0" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_replication/($index)/_status" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Terminates the replication and converts the follower index to a standard index.
#
# POST /_plugins/_replication/{index}/_stop
# Docs: https://opensearch.org/docs/latest/tuning-your-cluster/replication-plugin/api/#stop-replication
# operationId: replication.stop.0
export def "plugins-replication-stop replicationstop0" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_replication/($index)/_stop" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Updates any settings on the follower index.
#
# PUT /_plugins/_replication/{index}/_update
# Docs: https://opensearch.org/docs/latest/tuning-your-cluster/replication-plugin/api/#update-settings
# operationId: replication.update_settings.0
export def "plugins-replication-update settings0" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --settings: any
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_replication/($index)/_update" $qp)
  let body = {settings: $settings} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves information about any auto-follow activity and any replication rules configured on the specified cluster.
#
# GET /_plugins/_replication/autofollow_stats
# Docs: https://opensearch.org/docs/latest/tuning-your-cluster/replication-plugin/api/#get-auto-follow-stats
# operationId: replication.autofollow_stats.0
export def "plugins-replication-autofollow-stats stats0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_replication/autofollow_stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves information about any follower (syncing) indexes on a specified cluster.
#
# GET /_plugins/_replication/follower_stats
# Docs: https://opensearch.org/docs/latest/tuning-your-cluster/replication-plugin/api/#get-follower-cluster-stats
# operationId: replication.follower_stats.0
export def "plugins-replication-follower-stats stats0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_replication/follower_stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves information about any replicated leader indexes on a specified cluster.
#
# GET /_plugins/_replication/leader_stats
# Docs: https://opensearch.org/docs/latest/tuning-your-cluster/replication-plugin/api/#get-leader-cluster-stats
# operationId: replication.leader_stats.0
export def "plugins-replication-leader-stats stats0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_replication/leader_stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Deletes an index rollup job configuration.
#
# DELETE /_plugins/_rollup/jobs/{id}
# Docs: https://opensearch.org/docs/latest/im-plugin/index-rollups/rollup-api/#delete-an-index-rollup-job
# operationId: rollups.delete.0
export def "plugins-rollup-jobs rollupsdelete0" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_rollup/jobs/($id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves an index rollup job configuration by ID.
#
# GET /_plugins/_rollup/jobs/{id}
# Docs: https://opensearch.org/docs/latest/im-plugin/index-rollups/rollup-api/#get-an-index-rollup-job
# operationId: rollups.get.0
export def "plugins-rollup-jobs rollupsget0" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_rollup/jobs/($id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or updates an index rollup job configuration.
#
# PUT /_plugins/_rollup/jobs/{id}
# Docs: https://opensearch.org/docs/latest/im-plugin/index-rollups/rollup-api/#create-or-update-an-index-rollup-job
# operationId: rollups.put.0
# --rollup shape: {rollup_id?: string, enabled?: bool, schedule?: record, last_updated_time?: float, enabled_time?: float, description?: string, schema_version?: float, source_index?: string, target_index?: string, metadata_id?: string, page_size?: float, delay?: float, continuous?: bool, dimensions?: list, metrics?: list, error_notification?: string}
export def "plugins-rollup-jobs rollupsput0" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --if-primary-term: float # Only performs the operation if the document has the specified primary term.
  --if-seq-no: int # Only performs the operation if the document has the specified sequence number. (format: int64)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-id: string # The unique identifier for a resource.
  --seq-no: int # The sequence number of the document. (format: int64)
  --primary-term: int # The primary term of the document. (format: int64)
  --version: int # format: int64
  --rollup: record # Defines the configuration for a rollup job. — shape: {rollup_id?: string, enabled?: bool, schedule?: record, last_updated_time?: float, enabled_time?: float, description?: string, schema_version?: float, source_index?: string, target_index?: string, metadata_id?: string, page_size?: float, delay?: float, continuous?: bool, dimensions?: list, metrics?: list, error_notification?: string}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "if_primary_term" $if_primary_term "scalar") (serialize-qp "if_seq_no" $if_seq_no "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_rollup/jobs/($id)" $qp)
  let body = {_id: $body_id, _seq_no: $seq_no, _primary_term: $primary_term, _version: $version, rollup: $rollup} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves the execution status information for an index rollup job.
#
# GET /_plugins/_rollup/jobs/{id}/_explain
# Docs: https://opensearch.org/docs/latest/im-plugin/index-rollups/rollup-api/#explain-an-index-rollup-job
# operationId: rollups.explain.0
export def "plugins-rollup-jobs-explain rollupsexplain0" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_rollup/jobs/($id)/_explain" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Starts the execution of an index rollup job.
#
# POST /_plugins/_rollup/jobs/{id}/_start
# Docs: https://opensearch.org/docs/latest/im-plugin/index-rollups/rollup-api/#start-or-stop-an-index-rollup-job
# operationId: rollups.start.0
export def "plugins-rollup-jobs-start rollupsstart0" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_rollup/jobs/($id)/_start" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Stops the execution of an index rollup job.
#
# POST /_plugins/_rollup/jobs/{id}/_stop
# Docs: https://opensearch.org/docs/latest/im-plugin/index-rollups/rollup-api/#start-or-stop-an-index-rollup-job
# operationId: rollups.stop.0
export def "plugins-rollup-jobs-stop rollupsstop0" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_rollup/jobs/($id)/_stop" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Checks whether or not an upgrade can be performed and which security resources can be updated.
#
# GET /_plugins/_security/api/_upgrade_check
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#configuration-upgrade-check
# operationId: security.config_upgrade_check.0
export def "plugins-security-upgrade-check check0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/_upgrade_check" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Assists the cluster operator with upgrading missing default values and stale default definitions.
#
# POST /_plugins/_security/api/_upgrade_perform
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#configuration-upgrade
# operationId: security.config_upgrade_perform.0
export def "plugins-security-upgrade-perform perform0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --config: list # A list of configurations to upgrade.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/_upgrade_perform" $qp)
  let body = {config: $config} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns account information for the current user.
#
# GET /_plugins/_security/api/account
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#get-account-details
# operationId: security.get_account_details.0
export def "plugins-security-account details0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/account" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Changes the password for the current user.
#
# PUT /_plugins/_security/api/account
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#change-password
# operationId: security.change_password.0
export def "plugins-security-account password0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  current_password: string # The current password.
  password: string # The new password to set.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/account" $qp)
  let body = {current_password: $current_password, password: $password} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves all action groups.
#
# GET /_plugins/_security/api/actiongroups
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#get-action-groups
# operationId: security.get_action_groups.0
export def "plugins-security-actiongroups groups0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/actiongroups" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates, updates, or deletes multiple action groups in a single request.
#
# PATCH /_plugins/_security/api/actiongroups
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#patch-action-groups
# operationId: security.patch_action_groups.1
export def "plugins-security-actiongroups groups1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/actiongroups" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes the specified action group.
#
# DELETE /_plugins/_security/api/actiongroups/{action_group}
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#delete-action-group
# operationId: security.delete_action_group.0
export def "plugins-security-actiongroups group0-by-action_group" [
  action_group: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/actiongroups/($action_group)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves one action group.
#
# GET /_plugins/_security/api/actiongroups/{action_group}
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#get-action-group
# operationId: security.get_action_group.0
export def "plugins-security-actiongroups group0-by-action_group-1" [
  action_group: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/actiongroups/($action_group)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Updates the individual attributes of an action group.
#
# PATCH /_plugins/_security/api/actiongroups/{action_group}
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#patch-action-group
# operationId: security.patch_action_group.0
export def "plugins-security-actiongroups group0-by-action_group-2" [
  action_group: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/actiongroups/($action_group)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Creates or replaces the specified action group.
#
# PUT /_plugins/_security/api/actiongroups/{action_group}
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#create-action-group
# operationId: security.create_action_group.0
export def "plugins-security-actiongroups group0-by-action_group-3" [
  action_group: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --reserved: oneof<nothing, bool>
  --hidden: oneof<nothing, bool>
  --allowed-actions: list
  --type: string
  --description: string
  --static: oneof<nothing, bool>
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/actiongroups/($action_group)" $qp)
  let body = {reserved: $reserved, hidden: $hidden, allowed_actions: $allowed_actions, type: $type, description: $description, static: $static} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves the current list of allowed APIs accessible to a normal user.
#
# GET /_plugins/_security/api/allowlist
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#access-control-for-the-api
# operationId: security.get_allowlist.0
export def "plugins-security-allowlist allowlist0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/allowlist" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Updates the current list of APIs accessible for users on the allow list.
#
# PATCH /_plugins/_security/api/allowlist
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#access-control-for-the-api
# operationId: security.patch_allowlist.0
export def "plugins-security-allowlist allowlist0-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/allowlist" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Creates or replaces APIs permitted for users on the allow list. Requires a super admin certificate or REST API permissions.
#
# PUT /_plugins/_security/api/allowlist
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#access-control-for-the-api
# operationId: security.create_allowlist.0
export def "plugins-security-allowlist allowlist0-2" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --enabled: oneof<nothing, bool>
  --requests: record # An object with APIs as key and array of HTTP methods as values.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/allowlist" $qp)
  let body = {enabled: $enabled, requests: $requests} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves the audit configuration.
#
# GET /_plugins/_security/api/audit
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#audit-logs
# operationId: security.get_audit_configuration.0
export def "plugins-security-audit configuration0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/audit" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Updates the specified fields in the audit configuration.
#
# PATCH /_plugins/_security/api/audit
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#audit-logs
# operationId: security.patch_audit_configuration.0
export def "plugins-security-audit configuration0-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/audit" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Updates the audit configuration.
#
# PUT /_plugins/_security/api/audit/config
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#audit-logs
# operationId: security.update_audit_configuration.0
# --compliance shape: {enabled?: bool, write_log_diffs?: bool, read_watched_fields?: any, read_ignore_users?: list, write_watched_indices?: list, write_ignore_users?: list, read_metadata_only?: bool, write_metadata_only?: bool, external_config?: bool, internal_config?: bool}
# --audit shape: {ignore_users?: list, ignore_requests?: list, ignore_headers?: list, ignore_url_params?: list, disabled_rest_categories?: list, disabled_transport_categories?: list, log_request_body?: bool, resolve_indices?: bool, resolve_bulk_requests?: bool, exclude_sensitive_headers?: bool, enable_transport?: bool, enable_rest?: bool}
export def "plugins-security-audit-config configuration0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --compliance: record # shape: {enabled?: bool, write_log_diffs?: bool, read_watched_fields?: any, read_ignore_users?: list, write_watched_indices?: list, write_ignore_users?: list, read_metadata_only?: bool, write_metadata_only?: bool, external_config?: bool, internal_config?: bool}
  --enabled: oneof<nothing, bool>
  --audit: record # shape: {ignore_users?: list, ignore_requests?: list, ignore_headers?: list, ignore_url_params?: list, disabled_rest_categories?: list, disabled_transport_categories?: list, log_request_body?: bool, resolve_indices?: bool, resolve_bulk_requests?: bool, exclude_sensitive_headers?: bool, enable_transport?: bool, enable_rest?: bool}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/audit/config" $qp)
  let body = {compliance: $compliance, enabled: $enabled, audit: $audit} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns the authorization token for the current user.
#
# POST /_plugins/_security/api/authtoken
# operationId: security.authtoken.0
export def "plugins-security-authtoken securityauthtoken0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/authtoken" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Flushes the Security plugin's user, authentication, and authorization cache.
#
# DELETE /_plugins/_security/api/cache
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#flush-cache
# operationId: security.flush_cache.0
export def "plugins-security-cache cache0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/cache" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Not supported for the Cache API.
#
# GET /_plugins/_security/api/cache
# operationId: security.cache.1
export def "plugins-security-cache securitycache1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/cache" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Not supported for the Cache API.
#
# POST /_plugins/_security/api/cache
# operationId: security.cache.2
export def "plugins-security-cache securitycache2" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/cache" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Not supported for the Cache API.
#
# PUT /_plugins/_security/api/cache
# operationId: security.cache.3
export def "plugins-security-cache securitycache3" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/cache" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves the cluster security certificates.
#
# GET /_plugins/_security/api/certificates
# operationId: security.get_all_certificates.0
export def "plugins-security-certificates list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cert-type: string # The type of certificates (`HTTP`, `TRANSPORT`, or `ALL`) to retrieve from all nodes.
  --timeout: string # The maximum duration, in seconds, to spend retrieving certificates from all nodes before a timeout.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cert_type" $cert_type "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/certificates" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves the specified node's security certificates.
#
# GET /_plugins/_security/api/certificates/{node_id}
# operationId: security.get_node_certificates.0
export def "plugins-security-certificates certificates0" [
  node_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cert-type: string # The type of certificates (`HTTP`, `TRANSPORT`, or `ALL`) to retrieve from a node.
  --timeout: string # The maximum duration, in seconds, to spend retrieving certificates from all nodes before a timeout.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cert_type" $cert_type "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/certificates/($node_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Generates a `On-Behalf-Of` token for the current user.
#
# POST /_plugins/_security/api/generateonbehalfoftoken
# Docs: https://opensearch.org/docs/latest/security/access-control/authentication-tokens/#api-endpoint
# operationId: security.generate_obo_token.0
export def "plugins-security-generateonbehalfoftoken token0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  description: string # The description supplied by the user to describe the token.
  --service: string # The name of the service when generating a token for that service.
  --duration: string # A duration in seconds.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/generateonbehalfoftoken" $qp)
  let body = {description: $description, service: $service, duration: $duration} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieve all internal users.
#
# GET /_plugins/_security/api/internalusers
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#get-users
# operationId: security.get_users.0
export def "plugins-security-internalusers users0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/internalusers" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates, updates, or deletes multiple internal users in a single request.
#
# PATCH /_plugins/_security/api/internalusers
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#patch-users
# operationId: security.patch_users.0
export def "plugins-security-internalusers users0-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/internalusers" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes the specified internal user.
#
# DELETE /_plugins/_security/api/internalusers/{username}
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#delete-user
# operationId: security.delete_user.0
export def "plugins-security-internalusers user0-by-username" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/internalusers/($username)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieve information about the specified internal user.
#
# GET /_plugins/_security/api/internalusers/{username}
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#get-user
# operationId: security.get_user.0
export def "plugins-security-internalusers user0-by-username-1" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/internalusers/($username)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Updates individual attributes for an internal user.
#
# PATCH /_plugins/_security/api/internalusers/{username}
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#patch-user
# operationId: security.patch_user.0
export def "plugins-security-internalusers user0-by-username-2" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/internalusers/($username)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Creates or replaces the specified user.
#
# PUT /_plugins/_security/api/internalusers/{username}
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#create-user
# operationId: security.create_user.0
export def "plugins-security-internalusers user0-by-username-3" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --password: string
  --hash: string
  --reserved: oneof<nothing, bool>
  --hidden: oneof<nothing, bool>
  --backend-roles: list
  --attributes: record
  --description: string
  --opendistro-security-roles: list
  --static: oneof<nothing, bool>
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/internalusers/($username)" $qp)
  let body = {password: $password, hash: $hash, reserved: $reserved, hidden: $hidden, backend_roles: $backend_roles, attributes: $attributes, description: $description, opendistro_security_roles: $opendistro_security_roles, static: $static} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Generates an authorization token for the specified user.
#
# POST /_plugins/_security/api/internalusers/{username}/authtoken
# operationId: security.generate_user_token.0
export def "plugins-security-internalusers-authtoken token0" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/internalusers/($username)/authtoken" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Migrates the security configuration from v6 to v7.
#
# POST /_plugins/_security/api/migrate
# operationId: security.migrate.0
export def "plugins-security-migrate securitymigrate0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/migrate" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves all node distinguished names. Requires super admin or REST API permissions.
#
# GET /_plugins/_security/api/nodesdn
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#get-distinguished-names
# operationId: security.get_distinguished_names.0
export def "plugins-security-nodesdn names0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --show-all: oneof<nothing, bool> # Whether to include or exclude any static node's DN settings from the final result.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "show_all" $show_all "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/nodesdn" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Bulk updates specified node distinguished names. Requires super admin or REST API permissions.
#
# PATCH /_plugins/_security/api/nodesdn
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#update-all-distinguished-names
# operationId: security.patch_distinguished_names.0
export def "plugins-security-nodesdn names0-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/nodesdn" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes all distinguished names in the specified cluster or node allowlist. Requires super admin or REST API permissions.
#
# DELETE /_plugins/_security/api/nodesdn/{cluster_name}
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#delete-distinguished-names
# operationId: security.delete_distinguished_name.0
export def "plugins-security-nodesdn name0-by-cluster_name" [
  cluster_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/nodesdn/($cluster_name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves all node distinguished names. Requires super admin or REST API permissions.
#
# GET /_plugins/_security/api/nodesdn/{cluster_name}
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#get-distinguished-names
# operationId: security.get_distinguished_name.0
export def "plugins-security-nodesdn name0-by-cluster_name-1" [
  cluster_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --show-all: oneof<nothing, bool> # Whether to include or exclude any static node's DN settings from the final result.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "show_all" $show_all "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/nodesdn/($cluster_name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Updates the distinguished cluster name for the specified cluster. Requires super admin or REST API permissions.
#
# PATCH /_plugins/_security/api/nodesdn/{cluster_name}
# operationId: security.patch_distinguished_name.0
export def "plugins-security-nodesdn name0-by-cluster_name-2" [
  cluster_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/nodesdn/($cluster_name)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Adds or updates the specified distinguished names in the cluster or node allowlist. Requires super admin or REST API permissions.
#
# PUT /_plugins/_security/api/nodesdn/{cluster_name}
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#update-distinguished-names
# operationId: security.update_distinguished_name.0
export def "plugins-security-nodesdn name0-by-cluster_name-3" [
  cluster_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --nodes-dn: list
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/nodesdn/($cluster_name)" $qp)
  let body = {nodes_dn: $nodes_dn} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves the evaluated REST API permissions for the currently logged in user.
#
# GET /_plugins/_security/api/permissionsinfo
# operationId: security.get_permissions_info.0
export def "plugins-security-permissionsinfo info0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/permissionsinfo" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves all roles.
#
# GET /_plugins/_security/api/roles
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#get-roles
# operationId: security.get_roles.0
export def "plugins-security-roles roles0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/roles" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates, updates, or deletes multiple roles in a single call.
#
# PATCH /_plugins/_security/api/roles
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#patch-roles
# operationId: security.patch_roles.0
export def "plugins-security-roles roles0-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/roles" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes the specified role.
#
# DELETE /_plugins/_security/api/roles/{role}
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#delete-role
# operationId: security.delete_role.0
export def "plugins-security-roles role0-by-role" [
  role: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/roles/($role)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves one role.
#
# GET /_plugins/_security/api/roles/{role}
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#get-role
# operationId: security.get_role.0
export def "plugins-security-roles role0-by-role-1" [
  role: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/roles/($role)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Updates the individual attributes of a role.
#
# PATCH /_plugins/_security/api/roles/{role}
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#patch-role
# operationId: security.patch_role.0
export def "plugins-security-roles role0-by-role-2" [
  role: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/roles/($role)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Creates or replaces the specified role.
#
# PUT /_plugins/_security/api/roles/{role}
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#create-role
# operationId: security.create_role.0
# --index_permissions item shape: {index_patterns?: list, dls?: string, fls?: list, masked_fields?: list, allowed_actions?: list}
# --tenant_permissions item shape: {tenant_patterns?: list, allowed_actions?: list}
export def "plugins-security-roles role0-by-role-3" [
  role: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --reserved: oneof<nothing, bool>
  --hidden: oneof<nothing, bool>
  --description: string
  --cluster-permissions: list
  --index-permissions: list # item shape: {index_patterns?: list, dls?: string, fls?: list, masked_fields?: list, allowed_actions?: list}
  --tenant-permissions: list # item shape: {tenant_patterns?: list, allowed_actions?: list}
  --static: oneof<nothing, bool>
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/roles/($role)" $qp)
  let body = {reserved: $reserved, hidden: $hidden, description: $description, cluster_permissions: $cluster_permissions, index_permissions: $index_permissions, tenant_permissions: $tenant_permissions, static: $static} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves all role mappings.
#
# GET /_plugins/_security/api/rolesmapping
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#get-role-mappings
# operationId: security.get_role_mappings.0
export def "plugins-security-rolesmapping mappings0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/rolesmapping" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or updates multiple role mappings in a single request.
#
# PATCH /_plugins/_security/api/rolesmapping
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#patch-role-mappings
# operationId: security.patch_role_mappings.0
export def "plugins-security-rolesmapping mappings0-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/rolesmapping" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes the specified role mapping.
#
# DELETE /_plugins/_security/api/rolesmapping/{role}
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#delete-role-mapping
# operationId: security.delete_role_mapping.0
export def "plugins-security-rolesmapping mapping0-by-role" [
  role: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/rolesmapping/($role)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves the specified role mapping.
#
# GET /_plugins/_security/api/rolesmapping/{role}
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#get-role-mapping
# operationId: security.get_role_mapping.0
export def "plugins-security-rolesmapping mapping0-by-role-1" [
  role: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/rolesmapping/($role)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Updates the individual attributes of a role mapping.
#
# PATCH /_plugins/_security/api/rolesmapping/{role}
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#patch-role-mapping
# operationId: security.patch_role_mapping.0
export def "plugins-security-rolesmapping mapping0-by-role-2" [
  role: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/rolesmapping/($role)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Creates or replaces the specified role mapping.
#
# PUT /_plugins/_security/api/rolesmapping/{role}
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#create-role-mapping
# operationId: security.create_role_mapping.0
export def "plugins-security-rolesmapping mapping0-by-role-3" [
  role: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --hosts: list
  --users: list
  --reserved: oneof<nothing, bool>
  --hidden: oneof<nothing, bool>
  --backend-roles: list
  --and-backend-roles: list
  --description: string
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/rolesmapping/($role)" $qp)
  let body = {hosts: $hosts, users: $users, reserved: $reserved, hidden: $hidden, backend_roles: $backend_roles, and_backend_roles: $and_backend_roles, description: $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns the current Security plugin configuration in a JSON format.
#
# GET /_plugins/_security/api/securityconfig
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#get-configuration
# operationId: security.get_configuration.0
export def "plugins-security-securityconfig configuration0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/securityconfig" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Updates the existing security configuration using the REST API. Requires super admin or REST API permissions.
#
# PATCH /_plugins/_security/api/securityconfig
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#patch-configuration
# operationId: security.patch_configuration.0
export def "plugins-security-securityconfig configuration0-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/securityconfig" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Updates the settings for an existing security configuration. Requires super admin or REST API permissions.
#
# PUT /_plugins/_security/api/securityconfig/config
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#update-configuration
# operationId: security.update_configuration.0
# --dynamic shape: {filtered_alias_mode?: string, disable_rest_auth?: bool, disable_intertransport_auth?: bool, respect_request_indices_options?: bool, opensearch-dashboards?: record, kibana?: record, http?: record, authc?: record, authz?: record, auth_failure_listeners?: record, do_not_fail_on_forbidden?: bool, multi_rolespan_enabled?: bool, hosts_resolver_mode?: string, do_not_fail_on_forbidden_empty?: bool, on_behalf_of?: record}
export def "plugins-security-securityconfig-config configuration0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --dynamic: record # shape: {filtered_alias_mode?: string, disable_rest_auth?: bool, disable_intertransport_auth?: bool, respect_request_indices_options?: bool, opensearch-dashboards?: record, kibana?: record, http?: record, authc?: record, authz?: record, auth_failure_listeners?: record, do_not_fail_on_forbidden?: bool, multi_rolespan_enabled?: bool, hosts_resolver_mode?: string, do_not_fail_on_forbidden_empty?: bool, on_behalf_of?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/securityconfig/config" $qp)
  let body = {dynamic: $dynamic} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves the cluster security certificates.
#
# GET /_plugins/_security/api/ssl/certs
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#get-certificates
# operationId: security.get_certificates.0
export def "plugins-security-ssl-certs certificates0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/ssl/certs" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Reloads the HTTP communication certificates.
#
# PUT /_plugins/_security/api/ssl/http/reloadcerts
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#reload-http-certificates
# operationId: security.reload_http_certificates.0
export def "plugins-security-ssl-http-reloadcerts certificates0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/ssl/http/reloadcerts" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Reloads the transport communication certificates.
#
# PUT /_plugins/_security/api/ssl/transport/reloadcerts
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#reload-transport-certificates
# operationId: security.reload_transport_certificates.0
export def "plugins-security-ssl-transport-reloadcerts certificates0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/ssl/transport/reloadcerts" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves the multi-tenancy configuration. Requires super admin or REST API permissions.
#
# GET /_plugins/_security/api/tenancy/config
# Docs: https://opensearch.org/docs/latest/security/multi-tenancy/dynamic-config/#configuring-multi-tenancy-with-the-rest-api
# operationId: security.get_tenancy_config.0
export def "plugins-security-tenancy-config config0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/tenancy/config" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or replaces the multi-tenancy configuration. Requires super admin or REST API permissions.
#
# PUT /_plugins/_security/api/tenancy/config
# Docs: https://opensearch.org/docs/latest/security/multi-tenancy/dynamic-config/#configuring-multi-tenancy-with-the-rest-api
# operationId: security.create_update_tenancy_config.0
export def "plugins-security-tenancy-config config0-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --default-tenant: string
  --private-tenant-enabled: oneof<nothing, bool>
  --multitenancy-enabled: oneof<nothing, bool>
  --sign-in-options: list
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/tenancy/config" $qp)
  let body = {default_tenant: $default_tenant, private_tenant_enabled: $private_tenant_enabled, multitenancy_enabled: $multitenancy_enabled, sign_in_options: $sign_in_options} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves all tenants.
#
# GET /_plugins/_security/api/tenants
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#get-tenants
# operationId: security.get_tenants.0
export def "plugins-security-tenants tenants0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/tenants" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Adds, deletes, or modifies multiple tenants in a single request.
#
# PATCH /_plugins/_security/api/tenants
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#patch-tenants
# operationId: security.patch_tenants.0
export def "plugins-security-tenants tenants0-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/tenants" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes the specified tenant.
#
# DELETE /_plugins/_security/api/tenants/{tenant}
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#delete-action-group
# operationId: security.delete_tenant.0
export def "plugins-security-tenants tenant0-by-tenant" [
  tenant: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/tenants/($tenant)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves the specified tenant.
#
# GET /_plugins/_security/api/tenants/{tenant}
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#get-tenant
# operationId: security.get_tenant.0
export def "plugins-security-tenants tenant0-by-tenant-1" [
  tenant: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/tenants/($tenant)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Adds, deletes, or modifies a single tenant.
#
# PATCH /_plugins/_security/api/tenants/{tenant}
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#patch-tenant
# operationId: security.patch_tenant.0
export def "plugins-security-tenants tenant0-by-tenant-2" [
  tenant: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/tenants/($tenant)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Creates or replaces the specified tenant.
#
# PUT /_plugins/_security/api/tenants/{tenant}
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#create-tenant
# operationId: security.create_tenant.0
export def "plugins-security-tenants tenant0-by-tenant-3" [
  tenant: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --description: string
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/tenants/($tenant)" $qp)
  let body = {description: $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieve all internal users. Legacy API.
#
# GET /_plugins/_security/api/user
# operationId: security.get_users_legacy.0
export def "plugins-security-user legacy0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/user" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Delete the specified user. Legacy API.
#
# DELETE /_plugins/_security/api/user/{username}
# operationId: security.delete_user_legacy.0
export def "plugins-security-user legacy0-by-username" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/user/($username)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieve one user. Legacy API.
#
# GET /_plugins/_security/api/user/{username}
# operationId: security.get_user_legacy.0
export def "plugins-security-user legacy0-by-username-1" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/user/($username)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or replaces the specified user. Legacy API.
#
# PUT /_plugins/_security/api/user/{username}
# operationId: security.create_user_legacy.0
export def "plugins-security-user legacy0-by-username-2" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --password: string
  --hash: string
  --reserved: oneof<nothing, bool>
  --hidden: oneof<nothing, bool>
  --backend-roles: list
  --attributes: record
  --description: string
  --opendistro-security-roles: list
  --static: oneof<nothing, bool>
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/user/($username)" $qp)
  let body = {password: $password, hash: $hash, reserved: $reserved, hidden: $hidden, backend_roles: $backend_roles, attributes: $attributes, description: $description, opendistro_security_roles: $opendistro_security_roles, static: $static} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Generates authorization token for the given user. Legacy API. Not Implemented.
#
# POST /_plugins/_security/api/user/{username}/authtoken
# operationId: security.generate_user_token_legacy.0
export def "plugins-security-user-authtoken legacy0" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_security/api/user/($username)/authtoken" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Checks whether the v6 security configuration is valid and ready to be migrated to v7.
#
# GET /_plugins/_security/api/validate
# operationId: security.validate.0
export def "plugins-security-validate securityvalidate0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --accept-invalid: oneof<nothing, bool> # Whether an invalid v6 configuration should be allowed.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "accept_invalid" $accept_invalid "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/api/validate" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns or updates authentication information for the currently authenticated user.
#
# GET /_plugins/_security/authinfo
# operationId: security.authinfo.0
export def "plugins-security-authinfo securityauthinfo0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --auth-type: string # The type of the current authentication request.
  --verbose: oneof<nothing, bool> # Whether to return a verbose response.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "auth_type" $auth_type "scalar") (serialize-qp "verbose" $verbose "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/authinfo" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns or updates authentication information for the currently authenticated user.
#
# POST /_plugins/_security/authinfo
# operationId: security.authinfo.1
export def "plugins-security-authinfo securityauthinfo1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --auth-type: string # The type of the current authentication request.
  --verbose: oneof<nothing, bool> # Whether to return a verbose response.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "auth_type" $auth_type "scalar") (serialize-qp "verbose" $verbose "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/authinfo" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves the current values for dynamic security settings for OpenSearch Dashboards.
#
# GET /_plugins/_security/dashboardsinfo
# operationId: security.get_dashboards_info.0
export def "plugins-security-dashboardsinfo info0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/dashboardsinfo" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves the current values for dynamic security settings for OpenSearch Dashboards.
#
# POST /_plugins/_security/dashboardsinfo
# operationId: security.post_dashboards_info.1
export def "plugins-security-dashboardsinfo info1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/dashboardsinfo" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Checks to see if the Security plugin is running.
#
# GET /_plugins/_security/health
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#health-check
# operationId: security.health.0
export def "plugins-security-health securityhealth0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --mode: string # A flag that determines whether to consider the security status before returning a response for a health query response. For example, `strict` mode indicates service should check the Security plugin status.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "mode" $mode "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/health" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Checks to see if the Security plugin is running.
#
# POST /_plugins/_security/health
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#health-check
# operationId: security.health.1
export def "plugins-security-health securityhealth1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --mode: string # A flag that determines whether to consider the security status before returning a response for a health query response. For example, `strict` mode indicates service should check the Security plugin status.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "mode" $mode "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/health" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves the names of current tenants. Requires super admin or `kibanaserver` permissions.
#
# GET /_plugins/_security/tenantinfo
# operationId: security.tenant_info.0
export def "plugins-security-tenantinfo info0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/tenantinfo" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves the names of current tenants. Requires super admin or `kibanaserver` permissions.
#
# POST /_plugins/_security/tenantinfo
# operationId: security.tenant_info.1
export def "plugins-security-tenantinfo info1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/tenantinfo" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Gets the identity information for the user currently logged in.
#
# GET /_plugins/_security/whoami
# operationId: security.who_am_i.0
export def "plugins-security-whoami i0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/whoami" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Gets the identity information for the user currently logged in.
#
# POST /_plugins/_security/whoami
# operationId: security.who_am_i.1
export def "plugins-security-whoami i1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/whoami" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Gets the identity information for the user currently logged in. To use this operation, you must have access to this endpoint when authorization at REST layer is enabled.
#
# GET /_plugins/_security/whoamiprotected
# operationId: security.who_am_i_protected.0
export def "plugins-security-whoamiprotected protected0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_security/whoamiprotected" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves all snapshot management policies with optional pagination and filtering.
#
# GET /_plugins/_sm/policies
# operationId: sm.get_policies.0
export def "plugins-sm-policies policies0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-from: int # The starting index from which to retrieve snapshot management policies. (default: 0)
  --queryString: string # The query string to filter the returned snapshot management policies.
  --size: int # The number of snapshot management policies to return.
  --sortField: string # The name of the field to sort the snapshot management policies by.
  --sortOrder: string@sortOrder-completer # The order to sort the snapshot management policies. (default: asc)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "from" $qp_from "scalar") (serialize-qp "queryString" $queryString "scalar") (serialize-qp "size" $size "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_sm/policies" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Deletes a snapshot management policy.
#
# DELETE /_plugins/_sm/policies/{policy_name}
# operationId: sm.delete_policy.0
export def "plugins-sm-policies policy0-by-policy_name" [
  policy_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_sm/policies/($policy_name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves a specific snapshot management policy by name.
#
# GET /_plugins/_sm/policies/{policy_name}
# operationId: sm.get_policy.0
export def "plugins-sm-policies policy0-by-policy_name-1" [
  policy_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_sm/policies/($policy_name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates a snapshot management policy.
#
# POST /_plugins/_sm/policies/{policy_name}
# operationId: sm.create_policy.0
# --creation shape: {schedule: record, time_limit?: string}
# --deletion shape: {schedule?: record, condition?: record, time_limit?: string}
# --snapshot_config shape: {date_format?: string, timezone?: string, indices?: string, repository: string, ignore_unavailable?: bool, include_global_state?: bool, partial?: bool, metadata?: record}
# --notification shape: {channel?: record, conditions?: record}
export def "plugins-sm-policies policy0-by-policy_name-2" [
  policy_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --description: string # A user-provided description of the snapshot policy.
  creation: record # shape: {schedule: record, time_limit?: string}
  --deletion: record # shape: {schedule?: record, condition?: record, time_limit?: string}
  snapshot_config: record # shape: {date_format?: string, timezone?: string, indices?: string, repository: string, ignore_unavailable?: bool, include_global_state?: bool, partial?: bool, metadata?: record}
  --notification: record # shape: {channel?: record, conditions?: record}
  --enabled: oneof<nothing, bool> # Whether the policy should be enabled at creation. (default: true)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_sm/policies/($policy_name)" $qp)
  let body = {description: $description, creation: $creation, deletion: $deletion, snapshot_config: $snapshot_config, notification: $notification, enabled: $enabled} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Updates an existing snapshot management policy. Requires `if_seq_no` and `if_primary_term`.
#
# PUT /_plugins/_sm/policies/{policy_name}
# operationId: sm.update_policy.0
# --creation shape: {schedule: record, time_limit?: string}
# --deletion shape: {schedule?: record, condition?: record, time_limit?: string}
# --snapshot_config shape: {date_format?: string, timezone?: string, indices?: string, repository: string, ignore_unavailable?: bool, include_global_state?: bool, partial?: bool, metadata?: record}
# --notification shape: {channel?: record, conditions?: record}
export def "plugins-sm-policies policy0-by-policy_name-3" [
  policy_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --if-primary-term: int # The primary term of the policy to update.
  --if-seq-no: int # The sequence number of the policy to update.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --description: string # A user-provided description of the snapshot policy.
  creation: record # shape: {schedule: record, time_limit?: string}
  --deletion: record # shape: {schedule?: record, condition?: record, time_limit?: string}
  snapshot_config: record # shape: {date_format?: string, timezone?: string, indices?: string, repository: string, ignore_unavailable?: bool, include_global_state?: bool, partial?: bool, metadata?: record}
  --notification: record # shape: {channel?: record, conditions?: record}
  --enabled: oneof<nothing, bool> # Whether the policy should be enabled at creation. (default: true)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "if_primary_term" $if_primary_term "scalar") (serialize-qp "if_seq_no" $if_seq_no "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_sm/policies/($policy_name)" $qp)
  let body = {description: $description, creation: $creation, deletion: $deletion, snapshot_config: $snapshot_config, notification: $notification, enabled: $enabled} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Explains the state of the snapshot management policy.
#
# GET /_plugins/_sm/policies/{policy_name}/_explain
# operationId: sm.explain_policy.0
export def "plugins-sm-policies-explain policy0" [
  policy_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_sm/policies/($policy_name)/_explain" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Starts a snapshot management policy.
#
# POST /_plugins/_sm/policies/{policy_name}/_start
# operationId: sm.start_policy.0
export def "plugins-sm-policies-start policy0" [
  policy_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_sm/policies/($policy_name)/_start" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Stops a snapshot management policy.
#
# POST /_plugins/_sm/policies/{policy_name}/_stop
# operationId: sm.stop_policy.0
export def "plugins-sm-policies-stop policy0" [
  policy_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_sm/policies/($policy_name)/_stop" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Executes SQL or PPL queries against OpenSearch indexes.
#
# POST /_plugins/_sql
# Docs: https://opensearch.org/docs/latest/search-plugins/sql/sql-ppl-api/
# operationId: sql.query.0
export def "plugins-sql sqlquery0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --format: string # Specifies the response format (JSON or YAML).
  --sanitize: oneof<nothing, bool> # Whether to escape special characters in the results. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: string # The SQL query string to execute.
  --filter: record # The filter to apply to query results.
  --fetch-size: int # The number of results to return in each response.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "format" $format "scalar") (serialize-qp "sanitize" $sanitize "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_sql" $qp)
  let body = {query: $body_query, filter: $filter, fetch_size: $fetch_size} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns the execution plan for a SQL or PPL query.
#
# POST /_plugins/_sql/_explain
# Docs: https://opensearch.org/docs/latest/search-plugins/sql/sql-ppl-api/
# operationId: sql.explain.0
export def "plugins-sql-explain sqlexplain0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --format: string # Specifies the response format (JSON or YAML).
  --sanitize: oneof<nothing, bool> # Whether to escape special characters in the results. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: string # The SQL query to explain.
  --filter: record # The filter to apply when explaining the query.
  --fetch-size: int # The number of results to analyze.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "format" $format "scalar") (serialize-qp "sanitize" $sanitize "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_sql/_explain" $qp)
  let body = {query: $body_query, filter: $filter, fetch_size: $fetch_size} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Closes an open cursor to free server-side resources.
#
# POST /_plugins/_sql/close
# Docs: https://opensearch.org/docs/latest/search-plugins/sql/sql-ppl-api/
# operationId: sql.close.0
export def "plugins-sql-close sqlclose0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --format: string # Specifies the response format (JSON or YAML).
  --sanitize: oneof<nothing, bool> # Whether to escape special characters in the results. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --cursor: string # The cursor identifier to close.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "format" $format "scalar") (serialize-qp "sanitize" $sanitize "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_sql/close" $qp)
  let body = {cursor: $cursor} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves performance metrics for the SQL plugin.
#
# GET /_plugins/_sql/stats
# Docs: https://opensearch.org/docs/latest/search-plugins/sql/monitoring/
# operationId: sql.get_stats.0
export def "plugins-sql-stats stats0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --format: string # Specifies the response format (JSON or YAML).
  --sanitize: oneof<nothing, bool> # Whether to escape special characters in the results. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "format" $format "scalar") (serialize-qp "sanitize" $sanitize "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_sql/stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves filtered performance metrics for the SQL plugin.
#
# POST /_plugins/_sql/stats
# Docs: https://opensearch.org/docs/latest/search-plugins/sql/monitoring/
# operationId: sql.post_stats.1
export def "plugins-sql-stats stats1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --format: string # Specifies the response format (JSON or YAML).
  --sanitize: oneof<nothing, bool> # Whether to escape special characters in the results. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --start-time: string # When the query execution started.
  --end-time: record # When the query execution ended.
  --cluster-name: record # The name of the cluster executing the query.
  --index: record # The index information for the query.
  --body-query: record # The query information.
  --user: record # The user information.
  --execution-time: record # The time taken to execute the query.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "format" $format "scalar") (serialize-qp "sanitize" $sanitize "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_sql/stats" $qp)
  let body = {start_time: $start_time, end_time: $end_time, cluster_name: $cluster_name, index: $index, query: $body_query, user: $user, execution_time: $execution_time} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns the details of all transform jobs.
#
# GET /_plugins/_transform
# Docs: https://opensearch.org/docs/latest/im-plugin/index-transforms/transforms-apis/#get-a-transform-jobs-details
# operationId: transforms.search.0
export def "plugins-transform transformssearch0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-from: float # The starting transform to return. Default is `0`.
  --search: string # The search term to use to filter results.
  --size: float # Specifies the number of transforms to return. Default is `10`.
  --sortDirection: string # Specifies the direction to sort results in. Can be `ASC` or `DESC`. Default is `ASC`.
  --sortField: string # The field to sort results with.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "from" $qp_from "scalar") (serialize-qp "search" $search "scalar") (serialize-qp "size" $size "scalar") (serialize-qp "sortDirection" $sortDirection "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_transform" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns a preview of what a transformed index would look like.
#
# POST /_plugins/_transform/_preview
# Docs: https://opensearch.org/docs/latest/im-plugin/index-transforms/transforms-apis/#preview-a-transform-jobs-results
# operationId: transforms.preview.0
# --transform shape: {transform_id?: string, schema_version?: float, continuous?: bool, schedule?: record, metadata_id?: string, updated_at?: float, enabled?: bool, enabled_at?: float, description?: string, source_index?: string, data_selection_query?: record, target_index?: string, roles?: list, page_size?: float, groups?: list, aggregations?: record}
export def "plugins-transform-preview transformspreview0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  transform: record # shape: {transform_id?: string, schema_version?: float, continuous?: bool, schedule?: record, metadata_id?: string, updated_at?: float, enabled?: bool, enabled_at?: float, description?: string, source_index?: string, data_selection_query?: record, target_index?: string, roles?: list, page_size?: float, groups?: list, aggregations?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/_transform/_preview" $qp)
  let body = {transform: $transform} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete an index transform.
#
# DELETE /_plugins/_transform/{id}
# Docs: https://opensearch.org/docs/latest/im-plugin/index-transforms/transforms-apis/#delete-a-transform-job
# operationId: transforms.delete.0
export def "plugins-transform transformsdelete0" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_transform/($id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns the status and metadata of a transform job.
#
# GET /_plugins/_transform/{id}
# Docs: https://opensearch.org/docs/latest/im-plugin/index-transforms/transforms-apis/#get-a-transform-jobs-details
# operationId: transforms.get.0
export def "plugins-transform transformsget0" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_transform/($id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create an index transform, or update a transform if `if_seq_no` and `if_primary_term` are provided.
#
# PUT /_plugins/_transform/{id}
# Docs: https://opensearch.org/docs/latest/im-plugin/index-transforms/transforms-apis/#create-a-transform-job
# operationId: transforms.put.0
# --transform shape: {transform_id?: string, schema_version?: float, continuous?: bool, schedule?: record, metadata_id?: string, updated_at?: float, enabled?: bool, enabled_at?: float, description?: string, source_index?: string, data_selection_query?: record, target_index?: string, roles?: list, page_size?: float, groups?: list, aggregations?: record}
export def "plugins-transform transformsput0" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --if-primary-term: float # Only perform the operation if the document has this primary term.
  --if-seq-no: int # Only perform the operation if the document has this sequence number. (format: int64)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  transform: record # shape: {transform_id?: string, schema_version?: float, continuous?: bool, schedule?: record, metadata_id?: string, updated_at?: float, enabled?: bool, enabled_at?: float, description?: string, source_index?: string, data_selection_query?: record, target_index?: string, roles?: list, page_size?: float, groups?: list, aggregations?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "if_primary_term" $if_primary_term "scalar") (serialize-qp "if_seq_no" $if_seq_no "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_transform/($id)" $qp)
  let body = {transform: $transform} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns the status and metadata of a transform job.
#
# GET /_plugins/_transform/{id}/_explain
# Docs: https://opensearch.org/docs/latest/im-plugin/index-transforms/transforms-apis/#get-the-status-of-a-transform-job
# operationId: transforms.explain.0
export def "plugins-transform-explain transformsexplain0" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_transform/($id)/_explain" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Start transform.
#
# POST /_plugins/_transform/{id}/_start
# Docs: https://opensearch.org/docs/latest/im-plugin/index-transforms/transforms-apis/#start-a-transform-job
# operationId: transforms.start.0
export def "plugins-transform-start transformsstart0" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_transform/($id)/_start" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Stop transform.
#
# POST /_plugins/_transform/{id}/_stop
# Docs: https://opensearch.org/docs/latest/im-plugin/index-transforms/transforms-apis/#stop-a-transform-job
# operationId: transforms.stop.0
export def "plugins-transform-stop transformsstop0" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/_transform/($id)/_stop" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves statistics for all geospatial uploads.
#
# GET /_plugins/geospatial/_upload/stats
# operationId: geospatial.get_upload_stats.0
export def "plugins-geospatial-upload-stats stats0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/geospatial/_upload/stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Use an OpenSearch query to upload `GeoJSON`, operation will fail if index exists. - When type is `geo_point`, only Point geometry is allowed - When type is `geo_shape`, all geometry types are allowed (Point, MultiPoint, LineString, MultiLineString, Polygon, MultiPolygon, GeometryCollection, Envelope).
#
# POST /_plugins/geospatial/geojson/_upload
# operationId: geospatial.geojson_upload_post.0
# --data item shape: {type: "Feature"|"FeatureCollection", geometry: any, properties?: record}
export def "plugins-geospatial-geojson-upload post0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  index: string
  --field: string # Field name for the geospatial data. (default: location)
  type: string@type-completer-1 # Field type for the geospatial data.
  data: list # Array of GeoJSON features. — item shape: {type: "Feature"|"FeatureCollection", geometry: any, properties?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/geospatial/geojson/_upload" $qp)
  let body = {index: $index, field: $field, type: $type, data: $data} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Use an OpenSearch query to upload `GeoJSON` regardless if index exists. - When type is `geo_point`, only Point geometry is allowed - When type is `geo_shape`, all geometry types are allowed (Point, MultiPoint, LineString, MultiLineString, Polygon, MultiPolygon, GeometryCollection, Envelope).
#
# PUT /_plugins/geospatial/geojson/_upload
# operationId: geospatial.geojson_upload_put.0
# --data item shape: {type: "Feature"|"FeatureCollection", geometry: any, properties?: record}
export def "plugins-geospatial-geojson-upload put0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  index: string
  --field: string # Field name for the geospatial data. (default: location)
  type: string@type-completer-1 # Field type for the geospatial data.
  data: list # Array of GeoJSON features. — item shape: {type: "Feature"|"FeatureCollection", geometry: any, properties?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/geospatial/geojson/_upload" $qp)
  let body = {index: $index, field: $field, type: $type, data: $data} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get one or more IP2Geo data sources, defaulting to returning all if no names specified.
#
# GET /_plugins/geospatial/ip2geo/datasource
# Docs: https://docs.opensearch.org/docs/latest/ingest-pipelines/processors/ip2geo/#sending-a-get-request
# operationId: geospatial.get_ip2geo_datasource.0
export def "plugins-geospatial-ip2geo-datasource datasource0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_plugins/geospatial/ip2geo/datasource" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Delete a specific IP2Geo data source.
#
# DELETE /_plugins/geospatial/ip2geo/datasource/{name}
# Docs: https://docs.opensearch.org/docs/latest/ingest-pipelines/processors/ip2geo/#deleting-the-ip2geo-data-source
# operationId: geospatial.delete_ip2geo_datasource.0
export def "plugins-geospatial-ip2geo-datasource datasource0-by-name" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/geospatial/ip2geo/datasource/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get one or more IP2Geo data sources, defaulting to returning all if no names specified.
#
# GET /_plugins/geospatial/ip2geo/datasource/{name}
# Docs: https://docs.opensearch.org/docs/latest/ingest-pipelines/processors/ip2geo/#sending-a-get-request
# operationId: geospatial.get_ip2geo_datasource.1
export def "plugins-geospatial-ip2geo-datasource datasource1" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/geospatial/ip2geo/datasource/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create a specific IP2Geo data source. Default values:   - `endpoint`: `"https://geoip.maps.opensearch.org/v1/geolite2-city/manifest.json"`   - `update_interval_in_days`: 3.
#
# PUT /_plugins/geospatial/ip2geo/datasource/{name}
# Docs: https://docs.opensearch.org/docs/latest/ingest-pipelines/processors/ip2geo/#data-source-options
# operationId: geospatial.put_ip2geo_datasource.0
export def "plugins-geospatial-ip2geo-datasource datasource0-by-name-1" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --endpoint: string # URL endpoint for the data source. (default: https://geoip.maps.opensearch.org/v1/geolite2-city/manifest.json)
  --update-interval-in-days: int # Update interval in days. (default: 3)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/geospatial/ip2geo/datasource/($name)" $qp)
  let body = {endpoint: $endpoint, update_interval_in_days: $update_interval_in_days} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Update a specific IP2Geo data source.
#
# PUT /_plugins/geospatial/ip2geo/datasource/{name}/_settings
# Docs: https://docs.opensearch.org/docs/latest/ingest-pipelines/processors/ip2geo/#updating-an-ip2geo-data-source
# operationId: geospatial.put_ip2geo_datasource_settings.0
export def "plugins-geospatial-ip2geo-datasource-settings settings0" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --endpoint: string # URL endpoint for the data source. (default: https://geoip.maps.opensearch.org/v1/geolite2-city/manifest.json)
  --update-interval-in-days: int # Update interval in days. (default: 3)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_plugins/geospatial/ip2geo/datasource/($name)/_settings" $qp)
  let body = {endpoint: $endpoint, update_interval_in_days: $update_interval_in_days} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Allows to evaluate the quality of ranked search results over a set of typical search queries.
#
# GET /_rank_eval
# Docs: https://opensearch.org/docs/latest/api-reference/rank-eval/
# operationId: rank_eval.0
# --requests item shape: {id: string, request?: record, ratings: list, template_id?: string, params?: record}
# --metric shape: {precision?: any, recall?: any, mean_reciprocal_rank?: any, dcg?: any, expected_reciprocal_rank?: any}
export def "rank-eval eval0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with `foo` but no index starts with `bar`.
  --expand-wildcards: string # Whether to expand wildcard expression to concrete indexes that are open, closed or both.
  --ignore-unavailable: oneof<nothing, bool> # If `true`, missing or closed indexes are not included in the response.
  --search-type: string # Search operation type
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  requests: list # A set of typical search requests, together with their provided ratings. — item shape: {id: string, request?: record, ratings: list, template_id?: string, params?: record}
  --metric: record # shape: {precision?: any, recall?: any, mean_reciprocal_rank?: any, dcg?: any, expected_reciprocal_rank?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "search_type" $search_type "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_rank_eval" $qp)
  let body = {requests: $requests, metric: $metric} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Allows to evaluate the quality of ranked search results over a set of typical search queries.
#
# POST /_rank_eval
# Docs: https://opensearch.org/docs/latest/api-reference/rank-eval/
# operationId: rank_eval.1
# --requests item shape: {id: string, request?: record, ratings: list, template_id?: string, params?: record}
# --metric shape: {precision?: any, recall?: any, mean_reciprocal_rank?: any, dcg?: any, expected_reciprocal_rank?: any}
export def "rank-eval eval1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with `foo` but no index starts with `bar`.
  --expand-wildcards: string # Whether to expand wildcard expression to concrete indexes that are open, closed or both.
  --ignore-unavailable: oneof<nothing, bool> # If `true`, missing or closed indexes are not included in the response.
  --search-type: string # Search operation type
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  requests: list # A set of typical search requests, together with their provided ratings. — item shape: {id: string, request?: record, ratings: list, template_id?: string, params?: record}
  --metric: record # shape: {precision?: any, recall?: any, mean_reciprocal_rank?: any, dcg?: any, expected_reciprocal_rank?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "search_type" $search_type "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_rank_eval" $qp)
  let body = {requests: $requests, metric: $metric} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns information about ongoing index shard recoveries.
#
# GET /_recovery
# Docs: https://opensearch.org/docs/latest
# operationId: indices.recovery.0
export def "recovery indicesrecovery0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --active-only: oneof<nothing, bool> # If `true`, the response only includes ongoing shard recoveries. (default: false)
  --detailed: oneof<nothing, bool> # If `true`, the response includes detailed information about shard recoveries. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "active_only" $active_only "scalar") (serialize-qp "detailed" $detailed "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_recovery" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Performs the refresh operation in one or more indexes.
#
# GET /_refresh
# Docs: https://opensearch.org/docs/latest/tuning-your-cluster/availability-and-recovery/remote-store/index/#refresh-level-and-request-level-durability
# operationId: indices.refresh.0
export def "refresh indicesrefresh0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_refresh" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Performs the refresh operation in one or more indexes.
#
# POST /_refresh
# Docs: https://opensearch.org/docs/latest/tuning-your-cluster/availability-and-recovery/remote-store/index/#refresh-level-and-request-level-durability
# operationId: indices.refresh.1
export def "refresh indicesrefresh1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_refresh" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Allows to copy documents from one index to another, optionally filtering the source documents by a query, changing the destination index settings, or fetching the documents from a remote cluster.
#
# POST /_reindex
# Docs: https://opensearch.org/docs/latest/im-plugin/reindex-data/
# operationId: reindex.0
# --dest shape: {index: string, op_type?: any, pipeline?: string, routing?: string, version_type?: any}
# --source shape: {index: any, query?: record, remote?: record, size?: int, slice?: record, sort?: any, _source?: any}
export def "reindex reindex0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --max-docs: int # Maximum number of documents to process. By default, all documents. (format: int32)
  --refresh: string # If `true`, the request refreshes affected shards to make this operation visible to search.
  --requests-per-second: float # The throttle for this request in sub-requests per second. Defaults to no throttle. (format: float, default: 0)
  --require-alias: oneof<nothing, bool>
  --scroll: string # Specifies how long a consistent view of the index should be maintained for scrolled search.
  --slices: string # The number of slices this task should be divided into. Defaults to 1 slice, meaning the task isn't sliced into subtasks.
  --timeout: string # Period each indexing waits for automatic index creation, dynamic mapping updates, and waiting for active shards.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to `all` or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --wait-for-completion: oneof<nothing, bool> # If `true`, the request blocks until the operation is complete. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests. — shape: {index: any, query?: record, remote?: record, size?: int, slice?: record, sort?: any, _source?: any}
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --conflicts: any
  dest: record # shape: {index: string, op_type?: any, pipeline?: string, routing?: string, version_type?: any}
  --max-docs: int # The maximum number of documents to reindex. (format: int32)
  --script: any
  --size: int # format: int32
  --body-source: record # shape: {index: any, query?: record, remote?: record, size?: int, slice?: record, sort?: any, _source?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "max_docs" $max_docs "scalar") (serialize-qp "refresh" $refresh "scalar") (serialize-qp "requests_per_second" $requests_per_second "scalar") (serialize-qp "require_alias" $require_alias "scalar") (serialize-qp "scroll" $scroll "scalar") (serialize-qp "slices" $slices "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "wait_for_completion" $wait_for_completion "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_reindex" $qp)
  let body = {conflicts: $conflicts, dest: $dest, max_docs: $max_docs, script: $script, size: $size, source: $body_source} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Changes the number of requests per second for a particular reindex operation.
#
# POST /_reindex/{task_id}/_rethrottle
# Docs: https://opensearch.org/docs/latest
# operationId: reindex_rethrottle.0
export def "reindex-rethrottle rethrottle0" [
  task_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --requests-per-second: float # The throttle for this request in sub-requests per second. (format: float)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "requests_per_second" $requests_per_second "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_reindex/($task_id)/_rethrottle" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns the information about configured remote clusters.
#
# GET /_remote/info
# Docs: https://opensearch.org/docs/latest/api-reference/remote-info/
# operationId: cluster.remote_info.0
export def "remote-info info0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_remote/info" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Restores from remote store.
#
# POST /_remotestore/_restore
# Docs: https://opensearch.org/docs/latest/opensearch/remote/#restoring-from-a-backup
# operationId: remote_store.restore.0
export def "remotestore-restore storerestore0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --wait-for-completion: oneof<nothing, bool> # Should this request wait until the operation has completed before returning. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  indices: list
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "wait_for_completion" $wait_for_completion "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_remotestore/_restore" $qp)
  let body = {indices: $indices} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Allows to use the Mustache language to pre-render a search definition.
#
# GET /_render/template
# Docs: https://opensearch.org/docs/latest/search-plugins/search-template/
# operationId: render_search_template.0
export def "render-template template0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --id: string # The unique identifier for a resource.
  --params: record # Key-value pairs used to replace Mustache variables in the template. The key is the variable name. The value is the variable value.
  --body-source: string # An inline search template. Supports the same parameters as the search API request body. These parameters also support Mustache variables. If no `id` or `<templated-id>` is specified, this parameter is required.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_render/template" $qp)
  let body = {id: $id, params: $params, source: $body_source} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Allows to use the Mustache language to pre-render a search definition.
#
# POST /_render/template
# Docs: https://opensearch.org/docs/latest/search-plugins/search-template/
# operationId: render_search_template.1
export def "render-template template1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --id: string # The unique identifier for a resource.
  --params: record # Key-value pairs used to replace Mustache variables in the template. The key is the variable name. The value is the variable value.
  --body-source: string # An inline search template. Supports the same parameters as the search API request body. These parameters also support Mustache variables. If no `id` or `<templated-id>` is specified, this parameter is required.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_render/template" $qp)
  let body = {id: $id, params: $params, source: $body_source} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Allows to use the Mustache language to pre-render a search definition.
#
# GET /_render/template/{id}
# Docs: https://opensearch.org/docs/latest/search-plugins/search-template/
# operationId: render_search_template.2
export def "render-template template2" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-id: string # The unique identifier for a resource.
  --params: record # Key-value pairs used to replace Mustache variables in the template. The key is the variable name. The value is the variable value.
  --body-source: string # An inline search template. Supports the same parameters as the search API request body. These parameters also support Mustache variables. If no `id` or `<templated-id>` is specified, this parameter is required.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_render/template/($id)" $qp)
  let body = {id: $body_id, params: $params, source: $body_source} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Allows to use the Mustache language to pre-render a search definition.
#
# POST /_render/template/{id}
# Docs: https://opensearch.org/docs/latest/search-plugins/search-template/
# operationId: render_search_template.3
export def "render-template template3" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-id: string # The unique identifier for a resource.
  --params: record # Key-value pairs used to replace Mustache variables in the template. The key is the variable name. The value is the variable value.
  --body-source: string # An inline search template. Supports the same parameters as the search API request body. These parameters also support Mustache variables. If no `id` or `<templated-id>` is specified, this parameter is required.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_render/template/($id)" $qp)
  let body = {id: $body_id, params: $params, source: $body_source} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns information about any matching indexes, aliases, and data streams.
#
# GET /_resolve/index/{name}
# Docs: https://opensearch.org/docs/latest
# operationId: indices.resolve_index.0
export def "resolve-index index0" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_resolve/index/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns all script contexts.
#
# GET /_script_context
# Docs: https://opensearch.org/docs/latest/api-reference/script-apis/get-script-contexts/
# operationId: get_script_context.0
export def "script-context context0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_script_context" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns available script types, languages and contexts.
#
# GET /_script_language
# Docs: https://opensearch.org/docs/latest/api-reference/script-apis/get-script-language/
# operationId: get_script_languages.0
export def "script-language languages0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_script_language" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Deletes a script.
#
# DELETE /_scripts/{id}
# Docs: https://opensearch.org/docs/latest/api-reference/script-apis/delete-script/
# operationId: delete_script.0
@deprecated --flag master-timeout
export def "scripts script0-by-id" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_scripts/($id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns a script.
#
# GET /_scripts/{id}
# Docs: https://opensearch.org/docs/latest/api-reference/script-apis/get-stored-script/
# operationId: get_script.0
@deprecated --flag master-timeout
export def "scripts script0-by-id-1" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --master-timeout: string # Specify timeout for connection to master (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_scripts/($id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or updates a script.
#
# POST /_scripts/{id}
# Docs: https://opensearch.org/docs/latest/api-reference/script-apis/create-stored-script/
# operationId: put_script.0
# --script shape: {lang: any, options?: record, source: string}
@deprecated --flag master-timeout
export def "scripts script0-by-id-2" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --context: string # Context in which the script or search template should run. To prevent errors, the API immediately compiles the script or template in this context.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  script: record # The definition of a stored script. — shape: {lang: any, options?: record, source: string}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "context" $context "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_scripts/($id)" $qp)
  let body = {script: $script} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Creates or updates a script.
#
# PUT /_scripts/{id}
# Docs: https://opensearch.org/docs/latest/api-reference/script-apis/create-stored-script/
# operationId: put_script.1
# --script shape: {lang: any, options?: record, source: string}
@deprecated --flag master-timeout
export def "scripts script1" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --context: string # Context in which the script or search template should run. To prevent errors, the API immediately compiles the script or template in this context.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  script: record # The definition of a stored script. — shape: {lang: any, options?: record, source: string}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "context" $context "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_scripts/($id)" $qp)
  let body = {script: $script} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Creates or updates a script.
#
# POST /_scripts/{id}/{context}
# Docs: https://opensearch.org/docs/latest/api-reference/script-apis/create-stored-script/
# operationId: put_script.2
# --script shape: {lang: any, options?: record, source: string}
@deprecated --flag master-timeout
export def "scripts script2" [
  context: string
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --context: string # Context in which the script or search template should run. To prevent errors, the API immediately compiles the script or template in this context.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  script: record # The definition of a stored script. — shape: {lang: any, options?: record, source: string}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "context" $context "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_scripts/($id)/($context)" $qp)
  let body = {script: $script} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Creates or updates a script.
#
# PUT /_scripts/{id}/{context}
# Docs: https://opensearch.org/docs/latest/api-reference/script-apis/create-stored-script/
# operationId: put_script.3
# --script shape: {lang: any, options?: record, source: string}
@deprecated --flag master-timeout
export def "scripts script3" [
  context: string
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --context: string # Context in which the script or search template should run. To prevent errors, the API immediately compiles the script or template in this context.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  script: record # The definition of a stored script. — shape: {lang: any, options?: record, source: string}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "context" $context "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_scripts/($id)/($context)" $qp)
  let body = {script: $script} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Allows an arbitrary script to be executed and a result to be returned.
#
# GET /_scripts/painless/_execute
# Docs: https://opensearch.org/docs/latest/api-reference/script-apis/exec-script/
# operationId: scripts_painless_execute.0
# --context_setup shape: {document: record, index: string, query?: record}
export def "scripts-painless-execute execute0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --context: string # The context that the script should run in.
  --context-setup: record # shape: {document: record, index: string, query?: record}
  --script: any
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_scripts/painless/_execute" $qp)
  let body = {context: $context, context_setup: $context_setup, script: $script} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Allows an arbitrary script to be executed and a result to be returned.
#
# POST /_scripts/painless/_execute
# Docs: https://opensearch.org/docs/latest/api-reference/script-apis/exec-script/
# operationId: scripts_painless_execute.1
# --context_setup shape: {document: record, index: string, query?: record}
export def "scripts-painless-execute execute1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --context: string # The context that the script should run in.
  --context-setup: record # shape: {document: record, index: string, query?: record}
  --script: any
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_scripts/painless/_execute" $qp)
  let body = {context: $context, context_setup: $context_setup, script: $script} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns results matching a query.
#
# GET /_search
# Docs: https://opensearch.org/docs/latest/api-reference/search/
# operationId: search.0
# --collapse shape: {field: string, inner_hits?: any, max_concurrent_group_searches?: int}
# --rank shape: {rrf?: any}
# --post_filter shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
# --query shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
# --slice shape: {field?: string, id: int, max: int}
# --suggest shape: {text?: string}
# --pit shape: {id: string, keep_alive?: string}
export def "search search0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: string # Indicates which source fields are returned for matching documents. These fields are returned in the `hits._source` property of the search response. Valid values are: `true` to return the entire document source; `false` to not return the document source; `<string>` to return the source fields that are specified as a comma-separated list (supports wildcard (`*`) patterns).
  --source-excludes: string # A comma-separated list of source fields to exclude from the response. You can also use this parameter to exclude fields from the subset specified in `_source_includes` query parameter. If the `_source` parameter is `false`, this parameter is ignored.
  --source-includes: string # A comma-separated list of source fields to include in the response. If this parameter is specified, only these source fields are returned. You can exclude fields from this subset using the `_source_excludes` query parameter. If the `_source` parameter is `false`, this parameter is ignored.
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with `foo` but no index starts with `bar`.
  --allow-partial-search-results: oneof<nothing, bool> # If `true`, returns partial results if there are shard request timeouts or shard failures. If `false`, returns an error with no partial results. (default: true)
  --analyze-wildcard: oneof<nothing, bool> # If `true`, wildcard and prefix queries are analyzed. This parameter can only be used when the q query string parameter is specified. (default: false)
  --analyzer: string # Analyzer to use for the query string. This parameter can only be used when the q query string parameter is specified.
  --batched-reduce-size: int # The number of shard results that should be reduced at once on the coordinating node. This value should be used as a protection mechanism to reduce the memory overhead per search request if the potential number of shards in the request can be large. (format: int32, default: 512)
  --cancel-after-time-interval: string # The time after which the search request will be canceled. Request-level parameter takes precedence over `cancel_after_time_interval` cluster setting.
  --ccs-minimize-roundtrips: oneof<nothing, bool> # If `true`, network round-trips between the coordinating node and the remote clusters are minimized when executing cross-cluster search (CCS) requests. (default: true)
  --default-operator: string@default-operator-completer # The default operator for query string query: AND or OR. This parameter can only be used when the `q` query string parameter is specified.
  --df: string # Field to use as default where no field prefix is given in the query string. This parameter can only be used when the q query string parameter is specified.
  --docvalue-fields: string # A comma-separated list of fields to return as the docvalue representation for each hit.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`.
  --explain: oneof<nothing, bool> # If `true`, returns detailed information about score computation as part of a hit.
  --qp-from: int # Starting document offset. Needs to be non-negative. By default, you cannot page through more than 10,000 hits using the `from` and `size` parameters. To page through more hits, use the `search_after` parameter. (format: int32, default: 0)
  --ignore-throttled: oneof<nothing, bool> # If `true`, concrete, expanded or aliased indexes will be ignored when frozen.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --include-named-queries-score: oneof<nothing, bool> # Indicates whether `hit.matched_queries` should be rendered as a map that includes the name of the matched query associated with its score (true) or as an array containing the name of the matched queries (false) (default: false)
  --lenient: oneof<nothing, bool> # If `true`, format-based query failures (such as providing text to a numeric field) in the query string will be ignored. This parameter can only be used when the `q` query string parameter is specified.
  --max-concurrent-shard-requests: int # Defines the number of concurrent shard requests per node this search executes concurrently. This value should be used to limit the impact of the search on the cluster in order to limit the number of concurrent shard requests. (format: int32, default: 5)
  --phase-took: oneof<nothing, bool> # Indicates whether to return phase-level `took` time values in the response. (default: false)
  --pre-filter-shard-size: int # Defines a threshold that enforces a pre-filter roundtrip to prefilter search shards based on query rewriting if the number of shards the search request expands to exceeds the threshold. This filter roundtrip can limit the number of shards significantly if for instance a shard can not match any documents based on its rewrite method (if date filters are mandatory to match but the shard bounds and the query are disjoint). When unspecified, the pre-filter phase is executed if any of these conditions is met: the request targets more than 128 shards; the request targets one or more read-only index; the primary sort of the query targets an indexed field. (format: int32)
  --preference: string # Nodes and shards used for the search. By default, OpenSearch selects from eligible nodes and shards using adaptive replica selection, accounting for allocation awareness. Valid values are: `_only_local` to run the search only on shards on the local node; `_local` to, if possible, run the search on shards on the local node, or if not, select shards using the default method; `_only_nodes:<node-id>,<node-id>` to run the search on only the specified nodes IDs, where, if suitable shards exist on more than one selected node, use shards on those nodes using the default method, or if none of the specified nodes are available, select shards from any available node using the default method; `_prefer_nodes:<node-id>,<node-id>` to if possible, run the search on the specified nodes IDs, or if not, select shards using the default method; `_shards:<shard>,<shard>` to run the search only on the specified shards; `<custom-string>` (any string that does not start with `_`) to route searches with the same `<custom-string>` to the same shards in the same order. (default: random)
  --q: string # Query in the Lucene query string syntax using query parameter search. Query parameter searches do not support the full OpenSearch Query DSL but are handy for testing.
  --request-cache: oneof<nothing, bool> # If `true`, the caching of search results is enabled for requests where `size` is `0`. Defaults to index level settings.
  --rest-total-hits-as-int: oneof<nothing, bool> # Indicates whether `hits.total` should be rendered as an integer or an object in the rest search response. (default: false)
  --routing: string # Custom value used to route operations to a specific shard.
  --scroll: string # Period to retain the search context for scrolling. See Scroll search results. By default, this value cannot exceed `1d` (24 hours). You can change this limit using the `search.max_keep_alive` cluster-level setting.
  --search-pipeline: string # Customizable sequence of processing stages applied to search queries.
  --search-type: string # How distributed term frequencies are calculated for relevance scoring.
  --seq-no-primary-term: oneof<nothing, bool> # If `true`, returns sequence number and primary term of the last modification of each hit.
  --size: int # Defines the number of hits to return. By default, you cannot page through more than 10,000 hits using the `from` and `size` parameters. To page through more hits, use the `search_after` parameter. (format: int32, default: 10)
  --qp-sort: string # A comma-separated list of <field>:<direction> pairs.
  --stats: list # Specific `tag` of the request for logging and statistical purposes.
  --stored-fields: string # A comma-separated list of stored fields to return as part of a hit. If no fields are specified, no stored fields are included in the response. If this field is specified, the `_source` parameter defaults to `false`. You can pass `_source: true` to return both source fields and stored fields in the search response.
  --suggest-field: string # Specifies which field to use for suggestions.
  --suggest-mode: string@suggest-mode-completer # Specifies the suggest mode. This parameter can only be used when the `suggest_field` and `suggest_text` query string parameters are specified.
  --suggest-size: int # Number of suggestions to return. This parameter can only be used when the `suggest_field` and `suggest_text` query string parameters are specified. (format: int32)
  --suggest-text: string # The source text for which the suggestions should be returned. This parameter can only be used when the `suggest_field` and `suggest_text` query string parameters are specified.
  --terminate-after: int # Maximum number of documents to collect for each shard. If a query reaches this limit, OpenSearch terminates the query early. OpenSearch collects documents before sorting. Use with caution. OpenSearch applies this parameter to each shard handling the request. When possible, let OpenSearch perform early termination automatically. Avoid specifying this parameter for requests that target data streams with backing indexes across multiple data tiers. If set to `0` (default), the query does not terminate early. (format: int32)
  --timeout: string # Specifies the period of time to wait for a response from each shard. If no response is received before the timeout expires, the request fails and returns an error.
  --track-scores: oneof<nothing, bool> # If `true`, calculate and return document scores, even if the scores are not used for sorting.
  --track-total-hits: string # Number of hits matching the query to count accurately. If `true`, the exact number of hits is returned at the cost of some performance. If `false`, the response does not include the total number of hits matching the query.
  --typed-keys: oneof<nothing, bool> # If `true`, aggregation and suggester names are be prefixed by their respective types in the response.
  --verbose-pipeline: oneof<nothing, bool> # Enables or disables verbose mode for the search pipeline. When verbose mode is enabled, detailed information about each processor in the search pipeline is included in the search response. This includes the processor name, execution status, input, output, and time taken for processing. This parameter is primarily intended for debugging purposes, allowing users to track how data flows and transforms through the search pipeline.
  --version: oneof<nothing, bool> # If `true`, returns document version as part of a hit.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --aggregations: record # Defines the aggregations that are run as part of the search request.
  --aggs: record # Defines the aggregations that are run as part of the search request.
  --collapse: record # shape: {field: string, inner_hits?: any, max_concurrent_group_searches?: int}
  --explain: oneof<nothing, bool> # If `true`, returns detailed information about score computation as part of a hit.
  --ext: record # Configuration of search extensions defined by OpenSearch plugins.
  --body-from: int # Starting document offset. Needs to be non-negative. By default, you cannot page through more than 10,000 hits using the `from` and `size` parameters. To page through more hits, use the `search_after` parameter. (format: int32)
  --highlight: any
  --track-total-hits: any # The number of hits matching the query. When `true`, the exact number of hits is returned at the cost of some performance. When `false`, the response does not include the total number of hits matching the query. Default is `10,000` hits.
  --indices-boost: list # Boosts the `_score` of documents from specified indexes.
  --docvalue-fields: list # Array of wildcard (`*`) patterns. The request returns doc values for field names matching these patterns in the `hits.fields` property of the response.
  --rank: record # The container for ranking configuration. — shape: {rrf?: any}
  --min-score: float # Minimum `_score` for matching documents. Documents with a lower `_score` are not included in the search results. (format: float)
  --post-filter: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
  --profile: oneof<nothing, bool> # Set to `true` to return detailed timing information about the execution of individual components in a search request. NOTE: This is a debugging tool and adds significant overhead to search execution.
  --body-query: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
  --rescore: any # Can be used to improve precision by reordering just the top (for example 100 - 500) documents returned by the `query` and `post_filter` phases.
  --script-fields: record # Retrieve a script evaluation (based on different fields) for each hit.
  --search-after: list
  --size: int # The number of hits to return. By default, you cannot page through more than 10,000 hits using the `from` and `size` parameters. To page through more hits, use the `search_after` parameter. (format: int32)
  --slice: record # The configuration for a sliced scroll request. — shape: {field?: string, id: int, max: int}
  --body-sort: any
  --body-source: any # Defines how to fetch a source. Fetching can be disabled entirely, or the source can be filtered.
  --body-fields: list # Array of wildcard (`*`) patterns. The request returns values for field names matching these patterns in the `hits.fields` property of the response.
  --suggest: record # shape: {text?: string}
  --terminate-after: int # Maximum number of documents to collect for each shard. If a query reaches this limit, OpenSearch terminates the query early. OpenSearch collects documents before sorting. Use with caution. OpenSearch applies this parameter to each shard handling the request. When possible, let OpenSearch perform early termination automatically. Avoid specifying this parameter for requests that target data streams with backing indexes across multiple data tiers. If set to `0` (default), the query does not terminate early. (format: int32)
  --timeout: string # Specifies the period of time to wait for a response from each shard. If no response is received before the timeout expires, the request fails and returns an error. Defaults to no timeout.
  --track-scores: oneof<nothing, bool> # If `true`, calculate and return document scores, even if the scores are not used for sorting.
  --version: oneof<nothing, bool> # If `true`, returns document version as part of a hit.
  --seq-no-primary-term: oneof<nothing, bool> # If `true`, returns sequence number and primary term of the last modification of each hit.
  --stored-fields: any
  --pit: record # shape: {id: string, keep_alive?: string}
  --stats: list # Stats groups to associate with the search. Each group maintains a statistics aggregation for its associated searches. You can retrieve these stats using the indexes stats API.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "scalar") (serialize-qp "_source_excludes" $source_excludes "scalar") (serialize-qp "_source_includes" $source_includes "scalar") (serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "allow_partial_search_results" $allow_partial_search_results "scalar") (serialize-qp "analyze_wildcard" $analyze_wildcard "scalar") (serialize-qp "analyzer" $analyzer "scalar") (serialize-qp "batched_reduce_size" $batched_reduce_size "scalar") (serialize-qp "cancel_after_time_interval" $cancel_after_time_interval "scalar") (serialize-qp "ccs_minimize_roundtrips" $ccs_minimize_roundtrips "scalar") (serialize-qp "default_operator" $default_operator "scalar") (serialize-qp "df" $df "scalar") (serialize-qp "docvalue_fields" $docvalue_fields "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "explain" $explain "scalar") (serialize-qp "from" $qp_from "scalar") (serialize-qp "ignore_throttled" $ignore_throttled "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "include_named_queries_score" $include_named_queries_score "scalar") (serialize-qp "lenient" $lenient "scalar") (serialize-qp "max_concurrent_shard_requests" $max_concurrent_shard_requests "scalar") (serialize-qp "phase_took" $phase_took "scalar") (serialize-qp "pre_filter_shard_size" $pre_filter_shard_size "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "request_cache" $request_cache "scalar") (serialize-qp "rest_total_hits_as_int" $rest_total_hits_as_int "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "scroll" $scroll "scalar") (serialize-qp "search_pipeline" $search_pipeline "scalar") (serialize-qp "search_type" $search_type "scalar") (serialize-qp "seq_no_primary_term" $seq_no_primary_term "scalar") (serialize-qp "size" $size "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "stats" $stats "multi") (serialize-qp "stored_fields" $stored_fields "scalar") (serialize-qp "suggest_field" $suggest_field "scalar") (serialize-qp "suggest_mode" $suggest_mode "scalar") (serialize-qp "suggest_size" $suggest_size "scalar") (serialize-qp "suggest_text" $suggest_text "scalar") (serialize-qp "terminate_after" $terminate_after "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "track_scores" $track_scores "scalar") (serialize-qp "track_total_hits" $track_total_hits "scalar") (serialize-qp "typed_keys" $typed_keys "scalar") (serialize-qp "verbose_pipeline" $verbose_pipeline "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_search" $qp)
  let body = {aggregations: $aggregations, aggs: $aggs, collapse: $collapse, explain: $explain, ext: $ext, from: $body_from, highlight: $highlight, track_total_hits: $track_total_hits, indices_boost: $indices_boost, docvalue_fields: $docvalue_fields, rank: $rank, min_score: $min_score, post_filter: $post_filter, profile: $profile, query: $body_query, rescore: $rescore, script_fields: $script_fields, search_after: $search_after, size: $size, slice: $slice, sort: $body_sort, _source: $body_source, fields: $body_fields, suggest: $suggest, terminate_after: $terminate_after, timeout: $timeout, track_scores: $track_scores, version: $version, seq_no_primary_term: $seq_no_primary_term, stored_fields: $stored_fields, pit: $pit, stats: $stats} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns results matching a query.
#
# POST /_search
# Docs: https://opensearch.org/docs/latest/api-reference/search/
# operationId: search.1
# --collapse shape: {field: string, inner_hits?: any, max_concurrent_group_searches?: int}
# --rank shape: {rrf?: any}
# --post_filter shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
# --query shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
# --slice shape: {field?: string, id: int, max: int}
# --suggest shape: {text?: string}
# --pit shape: {id: string, keep_alive?: string}
export def "search search1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: string # Indicates which source fields are returned for matching documents. These fields are returned in the `hits._source` property of the search response. Valid values are: `true` to return the entire document source; `false` to not return the document source; `<string>` to return the source fields that are specified as a comma-separated list (supports wildcard (`*`) patterns).
  --source-excludes: string # A comma-separated list of source fields to exclude from the response. You can also use this parameter to exclude fields from the subset specified in `_source_includes` query parameter. If the `_source` parameter is `false`, this parameter is ignored.
  --source-includes: string # A comma-separated list of source fields to include in the response. If this parameter is specified, only these source fields are returned. You can exclude fields from this subset using the `_source_excludes` query parameter. If the `_source` parameter is `false`, this parameter is ignored.
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with `foo` but no index starts with `bar`.
  --allow-partial-search-results: oneof<nothing, bool> # If `true`, returns partial results if there are shard request timeouts or shard failures. If `false`, returns an error with no partial results. (default: true)
  --analyze-wildcard: oneof<nothing, bool> # If `true`, wildcard and prefix queries are analyzed. This parameter can only be used when the q query string parameter is specified. (default: false)
  --analyzer: string # Analyzer to use for the query string. This parameter can only be used when the q query string parameter is specified.
  --batched-reduce-size: int # The number of shard results that should be reduced at once on the coordinating node. This value should be used as a protection mechanism to reduce the memory overhead per search request if the potential number of shards in the request can be large. (format: int32, default: 512)
  --cancel-after-time-interval: string # The time after which the search request will be canceled. Request-level parameter takes precedence over `cancel_after_time_interval` cluster setting.
  --ccs-minimize-roundtrips: oneof<nothing, bool> # If `true`, network round-trips between the coordinating node and the remote clusters are minimized when executing cross-cluster search (CCS) requests. (default: true)
  --default-operator: string@default-operator-completer # The default operator for query string query: AND or OR. This parameter can only be used when the `q` query string parameter is specified.
  --df: string # Field to use as default where no field prefix is given in the query string. This parameter can only be used when the q query string parameter is specified.
  --docvalue-fields: string # A comma-separated list of fields to return as the docvalue representation for each hit.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`.
  --explain: oneof<nothing, bool> # If `true`, returns detailed information about score computation as part of a hit.
  --qp-from: int # Starting document offset. Needs to be non-negative. By default, you cannot page through more than 10,000 hits using the `from` and `size` parameters. To page through more hits, use the `search_after` parameter. (format: int32, default: 0)
  --ignore-throttled: oneof<nothing, bool> # If `true`, concrete, expanded or aliased indexes will be ignored when frozen.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --include-named-queries-score: oneof<nothing, bool> # Indicates whether `hit.matched_queries` should be rendered as a map that includes the name of the matched query associated with its score (true) or as an array containing the name of the matched queries (false) (default: false)
  --lenient: oneof<nothing, bool> # If `true`, format-based query failures (such as providing text to a numeric field) in the query string will be ignored. This parameter can only be used when the `q` query string parameter is specified.
  --max-concurrent-shard-requests: int # Defines the number of concurrent shard requests per node this search executes concurrently. This value should be used to limit the impact of the search on the cluster in order to limit the number of concurrent shard requests. (format: int32, default: 5)
  --phase-took: oneof<nothing, bool> # Indicates whether to return phase-level `took` time values in the response. (default: false)
  --pre-filter-shard-size: int # Defines a threshold that enforces a pre-filter roundtrip to prefilter search shards based on query rewriting if the number of shards the search request expands to exceeds the threshold. This filter roundtrip can limit the number of shards significantly if for instance a shard can not match any documents based on its rewrite method (if date filters are mandatory to match but the shard bounds and the query are disjoint). When unspecified, the pre-filter phase is executed if any of these conditions is met: the request targets more than 128 shards; the request targets one or more read-only index; the primary sort of the query targets an indexed field. (format: int32)
  --preference: string # Nodes and shards used for the search. By default, OpenSearch selects from eligible nodes and shards using adaptive replica selection, accounting for allocation awareness. Valid values are: `_only_local` to run the search only on shards on the local node; `_local` to, if possible, run the search on shards on the local node, or if not, select shards using the default method; `_only_nodes:<node-id>,<node-id>` to run the search on only the specified nodes IDs, where, if suitable shards exist on more than one selected node, use shards on those nodes using the default method, or if none of the specified nodes are available, select shards from any available node using the default method; `_prefer_nodes:<node-id>,<node-id>` to if possible, run the search on the specified nodes IDs, or if not, select shards using the default method; `_shards:<shard>,<shard>` to run the search only on the specified shards; `<custom-string>` (any string that does not start with `_`) to route searches with the same `<custom-string>` to the same shards in the same order. (default: random)
  --q: string # Query in the Lucene query string syntax using query parameter search. Query parameter searches do not support the full OpenSearch Query DSL but are handy for testing.
  --request-cache: oneof<nothing, bool> # If `true`, the caching of search results is enabled for requests where `size` is `0`. Defaults to index level settings.
  --rest-total-hits-as-int: oneof<nothing, bool> # Indicates whether `hits.total` should be rendered as an integer or an object in the rest search response. (default: false)
  --routing: string # Custom value used to route operations to a specific shard.
  --scroll: string # Period to retain the search context for scrolling. See Scroll search results. By default, this value cannot exceed `1d` (24 hours). You can change this limit using the `search.max_keep_alive` cluster-level setting.
  --search-pipeline: string # Customizable sequence of processing stages applied to search queries.
  --search-type: string # How distributed term frequencies are calculated for relevance scoring.
  --seq-no-primary-term: oneof<nothing, bool> # If `true`, returns sequence number and primary term of the last modification of each hit.
  --size: int # Defines the number of hits to return. By default, you cannot page through more than 10,000 hits using the `from` and `size` parameters. To page through more hits, use the `search_after` parameter. (format: int32, default: 10)
  --qp-sort: string # A comma-separated list of <field>:<direction> pairs.
  --stats: list # Specific `tag` of the request for logging and statistical purposes.
  --stored-fields: string # A comma-separated list of stored fields to return as part of a hit. If no fields are specified, no stored fields are included in the response. If this field is specified, the `_source` parameter defaults to `false`. You can pass `_source: true` to return both source fields and stored fields in the search response.
  --suggest-field: string # Specifies which field to use for suggestions.
  --suggest-mode: string@suggest-mode-completer # Specifies the suggest mode. This parameter can only be used when the `suggest_field` and `suggest_text` query string parameters are specified.
  --suggest-size: int # Number of suggestions to return. This parameter can only be used when the `suggest_field` and `suggest_text` query string parameters are specified. (format: int32)
  --suggest-text: string # The source text for which the suggestions should be returned. This parameter can only be used when the `suggest_field` and `suggest_text` query string parameters are specified.
  --terminate-after: int # Maximum number of documents to collect for each shard. If a query reaches this limit, OpenSearch terminates the query early. OpenSearch collects documents before sorting. Use with caution. OpenSearch applies this parameter to each shard handling the request. When possible, let OpenSearch perform early termination automatically. Avoid specifying this parameter for requests that target data streams with backing indexes across multiple data tiers. If set to `0` (default), the query does not terminate early. (format: int32)
  --timeout: string # Specifies the period of time to wait for a response from each shard. If no response is received before the timeout expires, the request fails and returns an error.
  --track-scores: oneof<nothing, bool> # If `true`, calculate and return document scores, even if the scores are not used for sorting.
  --track-total-hits: string # Number of hits matching the query to count accurately. If `true`, the exact number of hits is returned at the cost of some performance. If `false`, the response does not include the total number of hits matching the query.
  --typed-keys: oneof<nothing, bool> # If `true`, aggregation and suggester names are be prefixed by their respective types in the response.
  --verbose-pipeline: oneof<nothing, bool> # Enables or disables verbose mode for the search pipeline. When verbose mode is enabled, detailed information about each processor in the search pipeline is included in the search response. This includes the processor name, execution status, input, output, and time taken for processing. This parameter is primarily intended for debugging purposes, allowing users to track how data flows and transforms through the search pipeline.
  --version: oneof<nothing, bool> # If `true`, returns document version as part of a hit.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --aggregations: record # Defines the aggregations that are run as part of the search request.
  --aggs: record # Defines the aggregations that are run as part of the search request.
  --collapse: record # shape: {field: string, inner_hits?: any, max_concurrent_group_searches?: int}
  --explain: oneof<nothing, bool> # If `true`, returns detailed information about score computation as part of a hit.
  --ext: record # Configuration of search extensions defined by OpenSearch plugins.
  --body-from: int # Starting document offset. Needs to be non-negative. By default, you cannot page through more than 10,000 hits using the `from` and `size` parameters. To page through more hits, use the `search_after` parameter. (format: int32)
  --highlight: any
  --track-total-hits: any # The number of hits matching the query. When `true`, the exact number of hits is returned at the cost of some performance. When `false`, the response does not include the total number of hits matching the query. Default is `10,000` hits.
  --indices-boost: list # Boosts the `_score` of documents from specified indexes.
  --docvalue-fields: list # Array of wildcard (`*`) patterns. The request returns doc values for field names matching these patterns in the `hits.fields` property of the response.
  --rank: record # The container for ranking configuration. — shape: {rrf?: any}
  --min-score: float # Minimum `_score` for matching documents. Documents with a lower `_score` are not included in the search results. (format: float)
  --post-filter: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
  --profile: oneof<nothing, bool> # Set to `true` to return detailed timing information about the execution of individual components in a search request. NOTE: This is a debugging tool and adds significant overhead to search execution.
  --body-query: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
  --rescore: any # Can be used to improve precision by reordering just the top (for example 100 - 500) documents returned by the `query` and `post_filter` phases.
  --script-fields: record # Retrieve a script evaluation (based on different fields) for each hit.
  --search-after: list
  --size: int # The number of hits to return. By default, you cannot page through more than 10,000 hits using the `from` and `size` parameters. To page through more hits, use the `search_after` parameter. (format: int32)
  --slice: record # The configuration for a sliced scroll request. — shape: {field?: string, id: int, max: int}
  --body-sort: any
  --body-source: any # Defines how to fetch a source. Fetching can be disabled entirely, or the source can be filtered.
  --body-fields: list # Array of wildcard (`*`) patterns. The request returns values for field names matching these patterns in the `hits.fields` property of the response.
  --suggest: record # shape: {text?: string}
  --terminate-after: int # Maximum number of documents to collect for each shard. If a query reaches this limit, OpenSearch terminates the query early. OpenSearch collects documents before sorting. Use with caution. OpenSearch applies this parameter to each shard handling the request. When possible, let OpenSearch perform early termination automatically. Avoid specifying this parameter for requests that target data streams with backing indexes across multiple data tiers. If set to `0` (default), the query does not terminate early. (format: int32)
  --timeout: string # Specifies the period of time to wait for a response from each shard. If no response is received before the timeout expires, the request fails and returns an error. Defaults to no timeout.
  --track-scores: oneof<nothing, bool> # If `true`, calculate and return document scores, even if the scores are not used for sorting.
  --version: oneof<nothing, bool> # If `true`, returns document version as part of a hit.
  --seq-no-primary-term: oneof<nothing, bool> # If `true`, returns sequence number and primary term of the last modification of each hit.
  --stored-fields: any
  --pit: record # shape: {id: string, keep_alive?: string}
  --stats: list # Stats groups to associate with the search. Each group maintains a statistics aggregation for its associated searches. You can retrieve these stats using the indexes stats API.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "scalar") (serialize-qp "_source_excludes" $source_excludes "scalar") (serialize-qp "_source_includes" $source_includes "scalar") (serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "allow_partial_search_results" $allow_partial_search_results "scalar") (serialize-qp "analyze_wildcard" $analyze_wildcard "scalar") (serialize-qp "analyzer" $analyzer "scalar") (serialize-qp "batched_reduce_size" $batched_reduce_size "scalar") (serialize-qp "cancel_after_time_interval" $cancel_after_time_interval "scalar") (serialize-qp "ccs_minimize_roundtrips" $ccs_minimize_roundtrips "scalar") (serialize-qp "default_operator" $default_operator "scalar") (serialize-qp "df" $df "scalar") (serialize-qp "docvalue_fields" $docvalue_fields "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "explain" $explain "scalar") (serialize-qp "from" $qp_from "scalar") (serialize-qp "ignore_throttled" $ignore_throttled "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "include_named_queries_score" $include_named_queries_score "scalar") (serialize-qp "lenient" $lenient "scalar") (serialize-qp "max_concurrent_shard_requests" $max_concurrent_shard_requests "scalar") (serialize-qp "phase_took" $phase_took "scalar") (serialize-qp "pre_filter_shard_size" $pre_filter_shard_size "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "request_cache" $request_cache "scalar") (serialize-qp "rest_total_hits_as_int" $rest_total_hits_as_int "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "scroll" $scroll "scalar") (serialize-qp "search_pipeline" $search_pipeline "scalar") (serialize-qp "search_type" $search_type "scalar") (serialize-qp "seq_no_primary_term" $seq_no_primary_term "scalar") (serialize-qp "size" $size "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "stats" $stats "multi") (serialize-qp "stored_fields" $stored_fields "scalar") (serialize-qp "suggest_field" $suggest_field "scalar") (serialize-qp "suggest_mode" $suggest_mode "scalar") (serialize-qp "suggest_size" $suggest_size "scalar") (serialize-qp "suggest_text" $suggest_text "scalar") (serialize-qp "terminate_after" $terminate_after "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "track_scores" $track_scores "scalar") (serialize-qp "track_total_hits" $track_total_hits "scalar") (serialize-qp "typed_keys" $typed_keys "scalar") (serialize-qp "verbose_pipeline" $verbose_pipeline "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_search" $qp)
  let body = {aggregations: $aggregations, aggs: $aggs, collapse: $collapse, explain: $explain, ext: $ext, from: $body_from, highlight: $highlight, track_total_hits: $track_total_hits, indices_boost: $indices_boost, docvalue_fields: $docvalue_fields, rank: $rank, min_score: $min_score, post_filter: $post_filter, profile: $profile, query: $body_query, rescore: $rescore, script_fields: $script_fields, search_after: $search_after, size: $size, slice: $slice, sort: $body_sort, _source: $body_source, fields: $body_fields, suggest: $suggest, terminate_after: $terminate_after, timeout: $timeout, track_scores: $track_scores, version: $version, seq_no_primary_term: $seq_no_primary_term, stored_fields: $stored_fields, pit: $pit, stats: $stats} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns information about the indexes and shards that a search request would be executed against.
#
# GET /_search_shards
# Docs: https://opensearch.org/docs/latest
# operationId: search_shards.0
export def "search-shards shards0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with `foo` but no index starts with `bar`.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --local: oneof<nothing, bool> # If `true`, the request retrieves information from the local node only. (default: false)
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --routing: string # Custom value used to route operations to a specific shard.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_search_shards" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about the indexes and shards that a search request would be executed against.
#
# POST /_search_shards
# Docs: https://opensearch.org/docs/latest
# operationId: search_shards.1
export def "search-shards shards1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with `foo` but no index starts with `bar`.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --local: oneof<nothing, bool> # If `true`, the request retrieves information from the local node only. (default: false)
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --routing: string # Custom value used to route operations to a specific shard.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_search_shards" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves information about a specified search pipeline.
#
# GET /_search/pipeline
# operationId: search_pipeline.get.0
export def "search-pipeline pipelineget0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # operation timeout for connection to cluster-manager node.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_search/pipeline" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Deletes the specified search pipeline.
#
# DELETE /_search/pipeline/{id}
# operationId: search_pipeline.delete.0
export def "search-pipeline pipelinedelete0" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --timeout: string # Operation timeout.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_search/pipeline/($id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves information about a specified search pipeline.
#
# GET /_search/pipeline/{id}
# operationId: search_pipeline.get.1
export def "search-pipeline pipelineget1" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # operation timeout for connection to cluster-manager node.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_search/pipeline/($id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or replaces the specified search pipeline.
#
# PUT /_search/pipeline/{id}
# Docs: https://opensearch.org/docs/latest/search-plugins/search-pipelines/creating-search-pipeline/
# operationId: search_pipeline.put.0
export def "search-pipeline pipelineput0" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # operation timeout for connection to cluster-manager node.
  --timeout: string # Operation timeout.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --description: string
  --version: int # format: int32
  --request-processors: list
  --response-processors: list
  --phase-results-processors: list
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_search/pipeline/($id)" $qp)
  let body = {description: $description, version: $version, request_processors: $request_processors, response_processors: $response_processors, phase_results_processors: $phase_results_processors} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes one or more point in time searches based on the IDs passed.
#
# DELETE /_search/point_in_time
# Docs: https://opensearch.org/docs/latest/search-plugins/point-in-time-api/#delete-pits
# operationId: delete_pit.0
export def "search-point-in-time pit0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  pit_id: list
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_search/point_in_time" $qp)
  let body = {pit_id: $pit_id} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes all active point in time searches.
#
# DELETE /_search/point_in_time/_all
# Docs: https://opensearch.org/docs/latest/search-plugins/point-in-time-api/#delete-pits
# operationId: delete_all_pits.0
export def "search-point-in-time-all pits0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_search/point_in_time/_all" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Lists all active point in time searches.
#
# GET /_search/point_in_time/_all
# Docs: https://opensearch.org/docs/latest/search-plugins/point-in-time-api/#list-all-pits
# operationId: get_all_pits.0
export def "search-point-in-time-all pits0-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_search/point_in_time/_all" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Explicitly clears the search context for a scroll.
#
# DELETE /_search/scroll
# Docs: https://opensearch.org/docs/latest/api-reference/scroll/
# operationId: clear_scroll.0
export def "search-scroll scroll0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --scroll-id: any
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_search/scroll" $qp)
  let body = {scroll_id: $scroll_id} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Allows to retrieve a large numbers of results from a single search request.
#
# GET /_search/scroll
# Docs: https://opensearch.org/docs/latest/api-reference/scroll/#path-and-http-methods
# operationId: scroll.0
@deprecated --flag scroll-id
export def "search-scroll scroll0-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --rest-total-hits-as-int: oneof<nothing, bool> # If `true`, the API response's `hit.total` property is returned as an integer. If `false`, the API response's `hit.total` property is returned as an object. (default: false)
  --scroll: string # Period to retain the search context for scrolling.
  --scroll-id: string # The scroll ID for scrolled search (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --scroll: string # A duration. Units can be `nanos`, `micros`, `ms` (milliseconds), `s` (seconds), `m` (minutes), `h` (hours) and `d` (days). Also accepts `0` without a unit and `-1` to indicate an unspecified value.
  --scroll-id: string
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "rest_total_hits_as_int" $rest_total_hits_as_int "scalar") (serialize-qp "scroll" $scroll "scalar") (serialize-qp "scroll_id" $scroll_id "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_search/scroll" $qp)
  let body = {scroll: $scroll, scroll_id: $scroll_id} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Allows to retrieve a large numbers of results from a single search request.
#
# POST /_search/scroll
# Docs: https://opensearch.org/docs/latest/api-reference/scroll/#path-and-http-methods
# operationId: scroll.1
@deprecated --flag scroll-id
export def "search-scroll scroll1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --rest-total-hits-as-int: oneof<nothing, bool> # If `true`, the API response's `hit.total` property is returned as an integer. If `false`, the API response's `hit.total` property is returned as an object. (default: false)
  --scroll: string # Period to retain the search context for scrolling.
  --scroll-id: string # The scroll ID for scrolled search (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --scroll: string # A duration. Units can be `nanos`, `micros`, `ms` (milliseconds), `s` (seconds), `m` (minutes), `h` (hours) and `d` (days). Also accepts `0` without a unit and `-1` to indicate an unspecified value.
  --scroll-id: string
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "rest_total_hits_as_int" $rest_total_hits_as_int "scalar") (serialize-qp "scroll" $scroll "scalar") (serialize-qp "scroll_id" $scroll_id "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_search/scroll" $qp)
  let body = {scroll: $scroll, scroll_id: $scroll_id} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Explicitly clears the search context for a scroll.
#
# DELETE /_search/scroll/{scroll_id}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/api-reference/scroll/
# operationId: clear_scroll.1
@deprecated
export def "search-scroll scroll1-by-scroll_id" [
  scroll_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-scroll-id: any
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_search/scroll/($scroll_id)" $qp)
  let body = {scroll_id: $body_scroll_id} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Allows to retrieve a large numbers of results from a single search request.
#
# GET /_search/scroll/{scroll_id}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/api-reference/scroll/#path-and-http-methods
# operationId: scroll.2
@deprecated
@deprecated --flag scroll-id
export def "search-scroll scroll2" [
  scroll_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --rest-total-hits-as-int: oneof<nothing, bool> # If `true`, the API response's `hit.total` property is returned as an integer. If `false`, the API response's `hit.total` property is returned as an object. (default: false)
  --scroll: string # Period to retain the search context for scrolling.
  --scroll-id: string # The scroll ID for scrolled search (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --scroll: string # A duration. Units can be `nanos`, `micros`, `ms` (milliseconds), `s` (seconds), `m` (minutes), `h` (hours) and `d` (days). Also accepts `0` without a unit and `-1` to indicate an unspecified value.
  --body-scroll-id: string
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "rest_total_hits_as_int" $rest_total_hits_as_int "scalar") (serialize-qp "scroll" $scroll "scalar") (serialize-qp "scroll_id" $scroll_id "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_search/scroll/($scroll_id)" $qp)
  let body = {scroll: $scroll, scroll_id: $body_scroll_id} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Allows to retrieve a large numbers of results from a single search request.
#
# POST /_search/scroll/{scroll_id}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/api-reference/scroll/#path-and-http-methods
# operationId: scroll.3
@deprecated
@deprecated --flag scroll-id
export def "search-scroll scroll3" [
  scroll_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --rest-total-hits-as-int: oneof<nothing, bool> # If `true`, the API response's `hit.total` property is returned as an integer. If `false`, the API response's `hit.total` property is returned as an object. (default: false)
  --scroll: string # Period to retain the search context for scrolling.
  --scroll-id: string # The scroll ID for scrolled search (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --scroll: string # A duration. Units can be `nanos`, `micros`, `ms` (milliseconds), `s` (seconds), `m` (minutes), `h` (hours) and `d` (days). Also accepts `0` without a unit and `-1` to indicate an unspecified value.
  --body-scroll-id: string
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "rest_total_hits_as_int" $rest_total_hits_as_int "scalar") (serialize-qp "scroll" $scroll "scalar") (serialize-qp "scroll_id" $scroll_id "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_search/scroll/($scroll_id)" $qp)
  let body = {scroll: $scroll, scroll_id: $body_scroll_id} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Allows to use the Mustache language to pre-render a search definition.
#
# GET /_search/template
# Docs: https://opensearch.org/docs/latest/search-plugins/search-template/
# operationId: search_template.0
export def "search-template template0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with `foo` but no index starts with `bar`.
  --ccs-minimize-roundtrips: oneof<nothing, bool> # If `true`, network round-trips are minimized for cross-cluster search requests. (default: true)
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --explain: oneof<nothing, bool> # If `true`, the response includes additional details about score computation as part of a hit.
  --ignore-throttled: oneof<nothing, bool> # If `true`, specified concrete, expanded, or aliased indexes are not included in the response when throttled.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --profile: oneof<nothing, bool> # If `true`, the query execution is profiled.
  --rest-total-hits-as-int: oneof<nothing, bool> # If `true`, `hits.total` are rendered as an integer in the response. (default: false)
  --routing: string # Custom value used to route operations to a specific shard.
  --scroll: string # Specifies how long a consistent view of the index should be maintained for scrolled search.
  --search-type: string # The type of the search operation.
  --typed-keys: oneof<nothing, bool> # If `true`, the response prefixes aggregation and suggester names with their respective types.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --explain: oneof<nothing, bool> # If `true`, returns detailed information about score calculation as part of each hit.
  --id: string # The unique identifier for a resource.
  --params: record # Key-value pairs used to replace Mustache variables in the template. The key is the variable name. The value is the variable value.
  --profile: oneof<nothing, bool> # If `true`, the query execution is profiled.
  --body-source: string # An inline search template. Supports the same parameters as the search API request body. Also supports Mustache variables. If no id is specified, this parameter is required.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "ccs_minimize_roundtrips" $ccs_minimize_roundtrips "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "explain" $explain "scalar") (serialize-qp "ignore_throttled" $ignore_throttled "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "profile" $profile "scalar") (serialize-qp "rest_total_hits_as_int" $rest_total_hits_as_int "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "scroll" $scroll "scalar") (serialize-qp "search_type" $search_type "scalar") (serialize-qp "typed_keys" $typed_keys "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_search/template" $qp)
  let body = {explain: $explain, id: $id, params: $params, profile: $profile, source: $body_source} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Allows to use the Mustache language to pre-render a search definition.
#
# POST /_search/template
# Docs: https://opensearch.org/docs/latest/search-plugins/search-template/
# operationId: search_template.1
export def "search-template template1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with `foo` but no index starts with `bar`.
  --ccs-minimize-roundtrips: oneof<nothing, bool> # If `true`, network round-trips are minimized for cross-cluster search requests. (default: true)
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --explain: oneof<nothing, bool> # If `true`, the response includes additional details about score computation as part of a hit.
  --ignore-throttled: oneof<nothing, bool> # If `true`, specified concrete, expanded, or aliased indexes are not included in the response when throttled.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --profile: oneof<nothing, bool> # If `true`, the query execution is profiled.
  --rest-total-hits-as-int: oneof<nothing, bool> # If `true`, `hits.total` are rendered as an integer in the response. (default: false)
  --routing: string # Custom value used to route operations to a specific shard.
  --scroll: string # Specifies how long a consistent view of the index should be maintained for scrolled search.
  --search-type: string # The type of the search operation.
  --typed-keys: oneof<nothing, bool> # If `true`, the response prefixes aggregation and suggester names with their respective types.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --explain: oneof<nothing, bool> # If `true`, returns detailed information about score calculation as part of each hit.
  --id: string # The unique identifier for a resource.
  --params: record # Key-value pairs used to replace Mustache variables in the template. The key is the variable name. The value is the variable value.
  --profile: oneof<nothing, bool> # If `true`, the query execution is profiled.
  --body-source: string # An inline search template. Supports the same parameters as the search API request body. Also supports Mustache variables. If no id is specified, this parameter is required.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "ccs_minimize_roundtrips" $ccs_minimize_roundtrips "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "explain" $explain "scalar") (serialize-qp "ignore_throttled" $ignore_throttled "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "profile" $profile "scalar") (serialize-qp "rest_total_hits_as_int" $rest_total_hits_as_int "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "scroll" $scroll "scalar") (serialize-qp "search_type" $search_type "scalar") (serialize-qp "typed_keys" $typed_keys "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_search/template" $qp)
  let body = {explain: $explain, id: $id, params: $params, profile: $profile, source: $body_source} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Provides low-level information about segments in a Lucene index.
#
# GET /_segments
# Docs: https://opensearch.org/docs/latest
# operationId: indices.segments.0
export def "segments indicessegments0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --verbose: oneof<nothing, bool> # If `true`, the request returns a verbose response. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "verbose" $verbose "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_segments" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns settings for one or more indexes.
#
# GET /_settings
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/get-settings/
# operationId: indices.get_settings.0
@deprecated --flag master-timeout
export def "settings settings0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with foo but no index starts with `bar`.
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`.
  --flat-settings: oneof<nothing, bool> # If `true`, returns settings in flat format. (default: false)
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --include-defaults: oneof<nothing, bool> # If `true`, return all default settings in the response. (default: false)
  --local: oneof<nothing, bool> # If `true`, the request retrieves information from the local node only. If `false`, information is retrieved from the cluster-manager node. (default: false)
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "include_defaults" $include_defaults "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_settings" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Updates the index settings.
#
# PUT /_settings
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/update-settings/
# operationId: indices.put_settings.0
# --index shape: {index?: record, mode?: string, routing_path?: any, soft_deletes?: record, soft_deletes.retention_lease.period?: string, sort?: record, number_of_shards?: any, number_of_replicas?: any, number_of_routing_shards?: any, check_on_startup?: "checksum"|"false"|"true", codec?: string, routing_partition_size?: any, load_fixed_bitset_filters_eagerly?: any, hidden?: any, auto_expand_replicas?: string, merge?: record, merge.scheduler.max_thread_count?: any, search?: record, search.idle.after?: string, refresh_interval?: string, max_result_window?: any, max_inner_result_window?: any, max_rescore_window?: any, max_docvalue_fields_search?: any, max_script_fields?: any, max_ngram_diff?: any, max_shingle_diff?: any, blocks?: record, blocks.read_only?: any, blocks.read_only_allow_delete?: any, blocks.read?: any, blocks.write?: any, blocks.metadata?: any, max_refresh_listeners?: any, analyze?: record, analyze.max_token_count?: any, highlight?: record, highlight.max_analyzed_offset?: any, max_terms_count?: any, max_regex_length?: any, routing?: record, gc_deletes?: string, default_pipeline?: string, final_pipeline?: string, lifecycle?: record, lifecycle.name?: string, provided_name?: string, creation_date?: any, creation_date_string?: any, uuid?: string, version?: record, verified_before_close?: any, format?: any, max_slices_per_scroll?: any, translog?: record, translog.durability?: any, translog.flush_threshold_size?: string, query_string?: record, query_string.lenient?: any, priority?: any, top_metrics_max_size?: any, analysis?: record, settings?: record, queries?: record, similarity?: record, mapping?: record, indexing?: record, indexing_pressure?: record, store?: record, knn?: any, knn.algo_param.ef_search?: any, composite_index.star_tree?: record}
# --soft_deletes shape: {enabled?: any, retention?: record, retention_lease?: record}
# --sort shape: {field?: any, order?: any, mode?: any, missing?: any}
# --merge shape: {log_byte_size_policy?: record, policy?: any, policy.deletes_pct_allowed?: any, policy.expunge_deletes_allowed?: any, policy.floor_segment?: string, policy.max_merge_at_once?: any, policy.max_merged_segment?: string, policy.reclaim_deletes_weight?: any, policy.segments_per_tier?: any, scheduler?: record}
# --search shape: {concurrent?: record, concurrent_segment_search?: record, default_pipeline?: string, idle?: record, slowlog?: record, throttled?: any}
# --blocks shape: {read_only?: any, read_only_allow_delete?: any, read?: any, write?: any, metadata?: any, search_only?: any}
# --analyze shape: {max_token_count?: any}
# --highlight shape: {max_analyzed_offset?: any}
# --routing shape: {allocation?: record, rebalance?: record}
# --lifecycle shape: {name: string, indexing_complete?: any, origination_date?: any, parse_origination_date?: bool, step?: record, rollover_alias?: string}
# --version shape: {created?: string, created_string?: string}
# --translog shape: {sync_interval?: string, durability?: any, flush_threshold_size?: string, generation_threshold_size?: string, retention?: record}
# --query_string shape: {lenient?: any}
# --analysis shape: {analyzer?: record, char_filter?: record, filter?: record, normalizer?: record, tokenizer?: record}
# --settings shape: {index?: record, mode?: string, routing_path?: any, soft_deletes?: record, soft_deletes.retention_lease.period?: string, sort?: record, number_of_shards?: any, number_of_replicas?: any, number_of_routing_shards?: any, check_on_startup?: "checksum"|"false"|"true", codec?: string, routing_partition_size?: any, load_fixed_bitset_filters_eagerly?: any, hidden?: any, auto_expand_replicas?: string, merge?: record, merge.scheduler.max_thread_count?: any, search?: record, search.idle.after?: string, refresh_interval?: string, max_result_window?: any, max_inner_result_window?: any, max_rescore_window?: any, max_docvalue_fields_search?: any, max_script_fields?: any, max_ngram_diff?: any, max_shingle_diff?: any, blocks?: record, blocks.read_only?: any, blocks.read_only_allow_delete?: any, blocks.read?: any, blocks.write?: any, blocks.metadata?: any, max_refresh_listeners?: any, analyze?: record, analyze.max_token_count?: any, highlight?: record, highlight.max_analyzed_offset?: any, max_terms_count?: any, max_regex_length?: any, routing?: record, gc_deletes?: string, default_pipeline?: string, final_pipeline?: string, lifecycle?: record, lifecycle.name?: string, provided_name?: string, creation_date?: any, creation_date_string?: any, uuid?: string, version?: record, verified_before_close?: any, format?: any, max_slices_per_scroll?: any, translog?: record, translog.durability?: any, translog.flush_threshold_size?: string, query_string?: record, query_string.lenient?: any, priority?: any, top_metrics_max_size?: any, analysis?: record, settings?: record, queries?: record, similarity?: record, mapping?: record, indexing?: record, indexing_pressure?: record, store?: record, knn?: any, knn.algo_param.ef_search?: any, composite_index.star_tree?: record}
# --queries shape: {cache?: record}
# --similarity shape: {bm25?: record, dfi?: record, dfr?: record, ib?: record, lmd?: record, lmj?: record, scripted_tfidf?: record}
# --mapping shape: {coerce?: any, total_fields?: record, depth?: record, nested_fields?: record, nested_objects?: record, field_name_length?: record, dimension_fields?: record, ignore_malformed?: any}
# --indexing shape: {slowlog?: record}
# --indexing_pressure shape: {memory: record}
# --store shape: {type: any, allow_mmap?: any, fs?: record, hybrid?: record, preload?: list, stats_refresh_interval?: string}
# --composite_index.star_tree shape: {default?: record, field?: record, max_fields?: any}
@deprecated --flag master-timeout
export def "settings settings0-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with `foo` but no index starts with `bar`.
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`.
  --flat-settings: oneof<nothing, bool> # If `true`, returns settings in flat format. (default: false)
  --ignore-unavailable: oneof<nothing, bool> # Whether specified concrete indexes should be ignored when unavailable (missing or closed).
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --preserve-existing: oneof<nothing, bool> # If `true`, existing index settings remain unchanged. (default: false)
  --timeout: string # Period to wait for a response. If no response is received before the  timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --index: record # The configuration settings for an index. — shape: {index?: record, mode?: string, routing_path?: any, soft_deletes?: record, soft_deletes.retention_lease.period?: string, sort?: record, number_of_shards?: any, number_of_replicas?: any, number_of_routing_shards?: any, check_on_startup?: "checksum"|"false"|"true", codec?: string, routing_partition_size?: any, load_fixed_bitset_filters_eagerly?: any, hidden?: any, auto_expand_replicas?: string, merge?: record, merge.scheduler.max_thread_count?: any, search?: record, search.idle.after?: string, refresh_interval?: string, max_result_window?: any, max_inner_result_window?: any, max_rescore_window?: any, max_docvalue_fields_search?: any, max_script_fields?: any, max_ngram_diff?: any, max_shingle_diff?: any, blocks?: record, blocks.read_only?: any, blocks.read_only_allow_delete?: any, blocks.read?: any, blocks.write?: any, blocks.metadata?: any, max_refresh_listeners?: any, analyze?: record, analyze.max_token_count?: any, highlight?: record, highlight.max_analyzed_offset?: any, max_terms_count?: any, max_regex_length?: any, routing?: record, gc_deletes?: string, default_pipeline?: string, final_pipeline?: string, lifecycle?: record, lifecycle.name?: string, provided_name?: string, creation_date?: any, creation_date_string?: any, uuid?: string, version?: record, verified_before_close?: any, format?: any, max_slices_per_scroll?: any, translog?: record, translog.durability?: any, translog.flush_threshold_size?: string, query_string?: record, query_string.lenient?: any, priority?: any, top_metrics_max_size?: any, analysis?: record, settings?: record, queries?: record, similarity?: record, mapping?: record, indexing?: record, indexing_pressure?: record, store?: record, knn?: any, knn.algo_param.ef_search?: any, composite_index.star_tree?: record}
  --mode: string
  --routing-path: any
  --soft-deletes: record # The configuration for soft deletes. — shape: {enabled?: any, retention?: record, retention_lease?: record}
  --soft-deletesretention-leaseperiod: string # A duration. Units can be `nanos`, `micros`, `ms` (milliseconds), `s` (seconds), `m` (minutes), `h` (hours) and `d` (days). Also accepts `0` without a unit and `-1` to indicate an unspecified value.
  --body-sort: record # The configuration for segment sorting. — shape: {field?: any, order?: any, mode?: any, missing?: any}
  --number-of-shards: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --number-of-replicas: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --number-of-routing-shards: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --check-on-startup: string@check-on-startup-completer # The type of check to perform when starting an index.
  --codec: string # The compression type for stored data.
  --routing-partition-size: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --load-fixed-bitset-filters-eagerly: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --hidden: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --auto-expand-replicas: string # The range of replicas to maintain.
  --merge: record # The configuration for segment merging. — shape: {log_byte_size_policy?: record, policy?: any, policy.deletes_pct_allowed?: any, policy.expunge_deletes_allowed?: any, policy.floor_segment?: string, policy.max_merge_at_once?: any, policy.max_merged_segment?: string, policy.reclaim_deletes_weight?: any, policy.segments_per_tier?: any, scheduler?: record}
  --mergeschedulermax-thread-count: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --search: record # The configuration for search operations. — shape: {concurrent?: record, concurrent_segment_search?: record, default_pipeline?: string, idle?: record, slowlog?: record, throttled?: any}
  --searchidleafter: string # A duration. Units can be `nanos`, `micros`, `ms` (milliseconds), `s` (seconds), `m` (minutes), `h` (hours) and `d` (days). Also accepts `0` without a unit and `-1` to indicate an unspecified value.
  --refresh-interval: string # A duration. Units can be `nanos`, `micros`, `ms` (milliseconds), `s` (seconds), `m` (minutes), `h` (hours) and `d` (days). Also accepts `0` without a unit and `-1` to indicate an unspecified value.
  --max-result-window: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --max-inner-result-window: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --max-rescore-window: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --max-docvalue-fields-search: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --max-script-fields: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --max-ngram-diff: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --max-shingle-diff: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --blocks: record # The block settings that control index operations. — shape: {read_only?: any, read_only_allow_delete?: any, read?: any, write?: any, metadata?: any, search_only?: any}
  --blocksread-only: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --blocksread-only-allow-delete: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --blocksread: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --blockswrite: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --blocksmetadata: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --max-refresh-listeners: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --analyze: record # The configuration for text analysis. — shape: {max_token_count?: any}
  --analyzemax-token-count: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --highlight: record # The configuration for highlighting. — shape: {max_analyzed_offset?: any}
  --highlightmax-analyzed-offset: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --max-terms-count: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --max-regex-length: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --routing: record # The routing configuration for index operations. — shape: {allocation?: record, rebalance?: record}
  --gc-deletes: string # A duration. Units can be `nanos`, `micros`, `ms` (milliseconds), `s` (seconds), `m` (minutes), `h` (hours) and `d` (days). Also accepts `0` without a unit and `-1` to indicate an unspecified value.
  --default-pipeline: string
  --final-pipeline: string
  --lifecycle: record # The index lifecycle management configuration. — shape: {name: string, indexing_complete?: any, origination_date?: any, parse_origination_date?: bool, step?: record, rollover_alias?: string}
  --lifecyclename: string # The name of a resource or configuration element.
  --provided-name: string # The name of a resource or configuration element.
  --creation-date: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --creation-date-string: any # A date and time, either as a string whose format depends on the context (defaulting to ISO_8601) or the number of milliseconds since the epoch. OpenSearch accepts both as an input but will generally output a string. representation.
  --uuid: string # The universally unique identifier.
  --version: record # The versioning information for the index. — shape: {created?: string, created_string?: string}
  --verified-before-close: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --format: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --max-slices-per-scroll: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --translog: record # The translog configuration. — shape: {sync_interval?: string, durability?: any, flush_threshold_size?: string, generation_threshold_size?: string, retention?: record}
  --translogdurability: any # The durability settings for the translog.
  --translogflush-threshold-size: string # The unique identifier of a node.
  --query-string: record # The configuration for query string parsing. — shape: {lenient?: any}
  --query-stringlenient: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --priority: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --top-metrics-max-size: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --analysis: record # The text analysis configuration. — shape: {analyzer?: record, char_filter?: record, filter?: record, normalizer?: record, tokenizer?: record}
  --settings: record # The configuration settings for an index. — shape: {index?: record, mode?: string, routing_path?: any, soft_deletes?: record, soft_deletes.retention_lease.period?: string, sort?: record, number_of_shards?: any, number_of_replicas?: any, number_of_routing_shards?: any, check_on_startup?: "checksum"|"false"|"true", codec?: string, routing_partition_size?: any, load_fixed_bitset_filters_eagerly?: any, hidden?: any, auto_expand_replicas?: string, merge?: record, merge.scheduler.max_thread_count?: any, search?: record, search.idle.after?: string, refresh_interval?: string, max_result_window?: any, max_inner_result_window?: any, max_rescore_window?: any, max_docvalue_fields_search?: any, max_script_fields?: any, max_ngram_diff?: any, max_shingle_diff?: any, blocks?: record, blocks.read_only?: any, blocks.read_only_allow_delete?: any, blocks.read?: any, blocks.write?: any, blocks.metadata?: any, max_refresh_listeners?: any, analyze?: record, analyze.max_token_count?: any, highlight?: record, highlight.max_analyzed_offset?: any, max_terms_count?: any, max_regex_length?: any, routing?: record, gc_deletes?: string, default_pipeline?: string, final_pipeline?: string, lifecycle?: record, lifecycle.name?: string, provided_name?: string, creation_date?: any, creation_date_string?: any, uuid?: string, version?: record, verified_before_close?: any, format?: any, max_slices_per_scroll?: any, translog?: record, translog.durability?: any, translog.flush_threshold_size?: string, query_string?: record, query_string.lenient?: any, priority?: any, top_metrics_max_size?: any, analysis?: record, settings?: record, queries?: record, similarity?: record, mapping?: record, indexing?: record, indexing_pressure?: record, store?: record, knn?: any, knn.algo_param.ef_search?: any, composite_index.star_tree?: record}
  --queries: record # The configuration for query caching. — shape: {cache?: record}
  --similarity: record # The configuration for similarity algorithms. — shape: {bm25?: record, dfi?: record, dfr?: record, ib?: record, lmd?: record, lmj?: record, scripted_tfidf?: record}
  --mapping: record # The mapping configuration for the index. — shape: {coerce?: any, total_fields?: record, depth?: record, nested_fields?: record, nested_objects?: record, field_name_length?: record, dimension_fields?: record, ignore_malformed?: any}
  --indexing: record # The indexing-related settings for an index. — shape: {slowlog?: record}
  --indexing-pressure: record # The configuration for indexing backpressure. — shape: {memory: record}
  --store: record # shape: {type: any, allow_mmap?: any, fs?: record, hybrid?: record, preload?: list, stats_refresh_interval?: string}
  --knn: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --knnalgo-paramef-search: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --composite-indexstar-tree: record # The star tree indexing configuration. — shape: {default?: record, field?: record, max_fields?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "preserve_existing" $preserve_existing "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_settings" $qp)
  let body = {index: $index, mode: $mode, routing_path: $routing_path, soft_deletes: $soft_deletes, soft_deletes.retention_lease.period: $soft_deletesretention_leaseperiod, sort: $body_sort, number_of_shards: $number_of_shards, number_of_replicas: $number_of_replicas, number_of_routing_shards: $number_of_routing_shards, check_on_startup: $check_on_startup, codec: $codec, routing_partition_size: $routing_partition_size, load_fixed_bitset_filters_eagerly: $load_fixed_bitset_filters_eagerly, hidden: $hidden, auto_expand_replicas: $auto_expand_replicas, merge: $merge, merge.scheduler.max_thread_count: $mergeschedulermax_thread_count, search: $search, search.idle.after: $searchidleafter, refresh_interval: $refresh_interval, max_result_window: $max_result_window, max_inner_result_window: $max_inner_result_window, max_rescore_window: $max_rescore_window, max_docvalue_fields_search: $max_docvalue_fields_search, max_script_fields: $max_script_fields, max_ngram_diff: $max_ngram_diff, max_shingle_diff: $max_shingle_diff, blocks: $blocks, blocks.read_only: $blocksread_only, blocks.read_only_allow_delete: $blocksread_only_allow_delete, blocks.read: $blocksread, blocks.write: $blockswrite, blocks.metadata: $blocksmetadata, max_refresh_listeners: $max_refresh_listeners, analyze: $analyze, analyze.max_token_count: $analyzemax_token_count, highlight: $highlight, highlight.max_analyzed_offset: $highlightmax_analyzed_offset, max_terms_count: $max_terms_count, max_regex_length: $max_regex_length, routing: $routing, gc_deletes: $gc_deletes, default_pipeline: $default_pipeline, final_pipeline: $final_pipeline, lifecycle: $lifecycle, lifecycle.name: $lifecyclename, provided_name: $provided_name, creation_date: $creation_date, creation_date_string: $creation_date_string, uuid: $uuid, version: $version, verified_before_close: $verified_before_close, format: $format, max_slices_per_scroll: $max_slices_per_scroll, translog: $translog, translog.durability: $translogdurability, translog.flush_threshold_size: $translogflush_threshold_size, query_string: $query_string, query_string.lenient: $query_stringlenient, priority: $priority, top_metrics_max_size: $top_metrics_max_size, analysis: $analysis, settings: $settings, queries: $queries, similarity: $similarity, mapping: $mapping, indexing: $indexing, indexing_pressure: $indexing_pressure, store: $store, knn: $knn, knn.algo_param.ef_search: $knnalgo_paramef_search, composite_index.star_tree: $composite_indexstar_tree} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns settings for one or more indexes.
#
# GET /_settings/{name}
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/get-settings/
# operationId: indices.get_settings.1
@deprecated --flag master-timeout
export def "settings settings1-by-name" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with foo but no index starts with `bar`.
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`.
  --flat-settings: oneof<nothing, bool> # If `true`, returns settings in flat format. (default: false)
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --include-defaults: oneof<nothing, bool> # If `true`, return all default settings in the response. (default: false)
  --local: oneof<nothing, bool> # If `true`, the request retrieves information from the local node only. If `false`, information is retrieved from the cluster-manager node. (default: false)
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "include_defaults" $include_defaults "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_settings/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Provides store information for shard copies of indexes.
#
# GET /_shard_stores
# Docs: https://opensearch.org/docs/latest
# operationId: indices.shard_stores.0
export def "shard-stores stores0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams.
  --ignore-unavailable: oneof<nothing, bool> # If `true`, missing or closed indexes are not included in the response.
  --status: string # List of shard health statuses used to limit the request.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "status" $status "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_shard_stores" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about a snapshot repository.
#
# GET /_snapshot
# Docs: https://opensearch.org/docs/latest/api-reference/snapshots/get-snapshot-repository/
# operationId: snapshot.get_repository.0
@deprecated --flag master-timeout
export def "snapshot repository0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --local: oneof<nothing, bool> # Whether to get information from the local node. (default: false)
  --master-timeout: string # Explicit operation timeout for connection to cluster-manager node (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_snapshot" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about the status of a snapshot.
#
# GET /_snapshot/_status
# Docs: https://opensearch.org/docs/latest/api-reference/snapshots/get-snapshot-status/
# operationId: snapshot.status.0
@deprecated --flag master-timeout
export def "snapshot-status snapshotstatus0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --ignore-unavailable: oneof<nothing, bool> # Whether to ignore any unavailable snapshots, When `false`, a `SnapshotMissingException` is thrown. (default: false)
  --master-timeout: string # Explicit operation timeout for connection to cluster-manager node (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_snapshot/_status" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Deletes a snapshot repository.
#
# DELETE /_snapshot/{repository}
# Docs: https://opensearch.org/docs/latest/api-reference/snapshots/delete-snapshot-repository/
# operationId: snapshot.delete_repository.0
@deprecated --flag master-timeout
export def "snapshot repository0-by-repository" [
  repository: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --master-timeout: string # Explicit operation timeout for connection to cluster-manager node (DEPRECATED)
  --timeout: string # The amount of time to wait for a response.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_snapshot/($repository)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about a snapshot repository.
#
# GET /_snapshot/{repository}
# Docs: https://opensearch.org/docs/latest/api-reference/snapshots/get-snapshot-repository/
# operationId: snapshot.get_repository.1
@deprecated --flag master-timeout
export def "snapshot repository1-by-repository" [
  repository: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --local: oneof<nothing, bool> # Whether to get information from the local node. (default: false)
  --master-timeout: string # Explicit operation timeout for connection to cluster-manager node (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_snapshot/($repository)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates a snapshot repository.
#
# POST /_snapshot/{repository}
# Docs: https://opensearch.org/docs/latest/api-reference/snapshots/create-repository/
# operationId: snapshot.create_repository.0
# --repository shape: {type?: string, uuid?: string, settings?: record}
# --settings shape: {chunk_size?: string, compress?: any, concurrent_streams?: any, location?: string, read_only?: any}
@deprecated --flag master-timeout
export def "snapshot repository0-by-repository-1" [
  repository: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --master-timeout: string # Explicit operation timeout for connection to cluster-manager node (DEPRECATED)
  --timeout: string # The amount of time to wait for a response.
  --verify: oneof<nothing, bool> # When `true`, verifies the creation of the snapshot repository.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-repository: record # The name of the repository to store the snapshot. — shape: {type?: string, uuid?: string, settings?: record}
  type: string
  settings: record # The settings for the snapshot repository. — shape: {chunk_size?: string, compress?: any, concurrent_streams?: any, location?: string, read_only?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "verify" $verify "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_snapshot/($repository)" $qp)
  let body = {repository: $body_repository, type: $type, settings: $settings} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Creates a snapshot repository.
#
# PUT /_snapshot/{repository}
# Docs: https://opensearch.org/docs/latest/api-reference/snapshots/create-repository/
# operationId: snapshot.create_repository.1
# --repository shape: {type?: string, uuid?: string, settings?: record}
# --settings shape: {chunk_size?: string, compress?: any, concurrent_streams?: any, location?: string, read_only?: any}
@deprecated --flag master-timeout
export def "snapshot repository1-by-repository-1" [
  repository: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --master-timeout: string # Explicit operation timeout for connection to cluster-manager node (DEPRECATED)
  --timeout: string # The amount of time to wait for a response.
  --verify: oneof<nothing, bool> # When `true`, verifies the creation of the snapshot repository.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-repository: record # The name of the repository to store the snapshot. — shape: {type?: string, uuid?: string, settings?: record}
  type: string
  settings: record # The settings for the snapshot repository. — shape: {chunk_size?: string, compress?: any, concurrent_streams?: any, location?: string, read_only?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "verify" $verify "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_snapshot/($repository)" $qp)
  let body = {repository: $body_repository, type: $type, settings: $settings} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Removes any stale data from a snapshot repository.
#
# POST /_snapshot/{repository}/_cleanup
# Docs: https://opensearch.org/docs/latest
# operationId: snapshot.cleanup_repository.0
@deprecated --flag master-timeout
export def "snapshot-cleanup repository0" [
  repository: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. (DEPRECATED)
  --timeout: string # The amount of time to wait for a response.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_snapshot/($repository)/_cleanup" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about the status of a snapshot.
#
# GET /_snapshot/{repository}/_status
# Docs: https://opensearch.org/docs/latest/api-reference/snapshots/get-snapshot-status/
# operationId: snapshot.status.1
@deprecated --flag master-timeout
export def "snapshot-status snapshotstatus1" [
  repository: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --ignore-unavailable: oneof<nothing, bool> # Whether to ignore any unavailable snapshots, When `false`, a `SnapshotMissingException` is thrown. (default: false)
  --master-timeout: string # Explicit operation timeout for connection to cluster-manager node (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_snapshot/($repository)/_status" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Verifies a repository.
#
# POST /_snapshot/{repository}/_verify
# Docs: https://opensearch.org/docs/latest/api-reference/snapshots/verify-snapshot-repository/
# operationId: snapshot.verify_repository.0
@deprecated --flag master-timeout
export def "snapshot-verify repository0" [
  repository: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --master-timeout: string # Explicit operation timeout for connection to cluster-manager node (DEPRECATED)
  --timeout: string # The amount of time to wait for a response.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_snapshot/($repository)/_verify" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Deletes a snapshot.
#
# DELETE /_snapshot/{repository}/{snapshot}
# Docs: https://opensearch.org/docs/latest/api-reference/snapshots/delete-snapshot/
# operationId: snapshot.delete.0
@deprecated --flag master-timeout
export def "snapshot snapshotdelete0" [
  repository: string
  snapshot: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --master-timeout: string # Explicit operation timeout for connection to cluster-manager node (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_snapshot/($repository)/($snapshot)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about a snapshot.
#
# GET /_snapshot/{repository}/{snapshot}
# Docs: https://opensearch.org/docs/latest
# operationId: snapshot.get.0
@deprecated --flag master-timeout
export def "snapshot snapshotget0" [
  repository: string
  snapshot: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --ignore-unavailable: oneof<nothing, bool> # When `false`, the request returns an error for any snapshots that are unavailable. (default: false)
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --verbose: oneof<nothing, bool> # When `true`, returns additional information about each snapshot such as the version of OpenSearch which took the snapshot, the start and end times of the snapshot, and the number of shards contained in the snapshot.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "verbose" $verbose "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_snapshot/($repository)/($snapshot)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates a snapshot within an existing repository.
#
# POST /_snapshot/{repository}/{snapshot}
# Docs: https://opensearch.org/docs/latest/api-reference/snapshots/create-snapshot/
# operationId: snapshot.create.0
@deprecated --flag master-timeout
export def "snapshot snapshotcreate0" [
  repository: string
  snapshot: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --wait-for-completion: oneof<nothing, bool> # When `true`, the request returns a response when the snapshot is complete. When `false`, the request returns a response when the snapshot initializes. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --ignore-unavailable: oneof<nothing, bool> # When `true`, the request ignores any data streams and indexes that are missing or closed.  When `false`, the request returns an error for any data streams or indexes that is missing or closed.
  --include-global-state: oneof<nothing, bool> # When `true`, includes the current cluster state in the snapshot.  The cluster state includes persistent cluster settings, composable index templates, legacy index templates, ingest pipelines, and ILM policies.  The cluster state also includes data stored in system indexes, such as watches and task records (configurable with `feature_states`). (default: true)
  --indices: any # A comma-separated list of data streams, indexes, and aliases used to limit the request. Supports wildcards (`*`). To target all data streams and indexes, omit this parameter or use `*` or `_all`.
  --feature-states: list # A list of feature states to include in the snapshot. Each feature state includes one or more system indexes which contain related data. You can view a list of features using the Get Feature API.   If `include_global_state` is `true`, all current feature states are included by default. If `include_global_state` is `false`, no feature states are included by default.
  --metadata: record # The custom metadata attached to a resource.
  --partial: oneof<nothing, bool> # When `true`, enables the restoration of a partial snapshot from indexes with unavailable shards.  Only shards that were successfully included in the snapshot will be restored. All missing shards will be recreated as empty.  When `false`, the entire restore operation will fail if one or more indexes included in the snapshot do not have all primary shards available. (default: false)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "wait_for_completion" $wait_for_completion "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_snapshot/($repository)/($snapshot)" $qp)
  let body = {ignore_unavailable: $ignore_unavailable, include_global_state: $include_global_state, indices: $indices, feature_states: $feature_states, metadata: $metadata, partial: $partial} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Creates a snapshot within an existing repository.
#
# PUT /_snapshot/{repository}/{snapshot}
# Docs: https://opensearch.org/docs/latest/api-reference/snapshots/create-snapshot/
# operationId: snapshot.create.1
@deprecated --flag master-timeout
export def "snapshot snapshotcreate1" [
  repository: string
  snapshot: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --wait-for-completion: oneof<nothing, bool> # When `true`, the request returns a response when the snapshot is complete. When `false`, the request returns a response when the snapshot initializes. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --ignore-unavailable: oneof<nothing, bool> # When `true`, the request ignores any data streams and indexes that are missing or closed.  When `false`, the request returns an error for any data streams or indexes that is missing or closed.
  --include-global-state: oneof<nothing, bool> # When `true`, includes the current cluster state in the snapshot.  The cluster state includes persistent cluster settings, composable index templates, legacy index templates, ingest pipelines, and ILM policies.  The cluster state also includes data stored in system indexes, such as watches and task records (configurable with `feature_states`). (default: true)
  --indices: any # A comma-separated list of data streams, indexes, and aliases used to limit the request. Supports wildcards (`*`). To target all data streams and indexes, omit this parameter or use `*` or `_all`.
  --feature-states: list # A list of feature states to include in the snapshot. Each feature state includes one or more system indexes which contain related data. You can view a list of features using the Get Feature API.   If `include_global_state` is `true`, all current feature states are included by default. If `include_global_state` is `false`, no feature states are included by default.
  --metadata: record # The custom metadata attached to a resource.
  --partial: oneof<nothing, bool> # When `true`, enables the restoration of a partial snapshot from indexes with unavailable shards.  Only shards that were successfully included in the snapshot will be restored. All missing shards will be recreated as empty.  When `false`, the entire restore operation will fail if one or more indexes included in the snapshot do not have all primary shards available. (default: false)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "wait_for_completion" $wait_for_completion "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_snapshot/($repository)/($snapshot)" $qp)
  let body = {ignore_unavailable: $ignore_unavailable, include_global_state: $include_global_state, indices: $indices, feature_states: $feature_states, metadata: $metadata, partial: $partial} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Creates a clone of all or part of a snapshot in the same repository as the original snapshot.
#
# PUT /_snapshot/{repository}/{snapshot}/_clone/{target_snapshot}
# Docs: https://opensearch.org/docs/latest
# operationId: snapshot.clone.0
@deprecated --flag master-timeout
export def "snapshot-clone snapshotclone0" [
  repository: string
  snapshot: string
  target_snapshot: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --master-timeout: string # Explicit operation timeout for connection to cluster-manager node (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  indices: string
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_snapshot/($repository)/($snapshot)/_clone/($target_snapshot)" $qp)
  let body = {indices: $indices} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Restores a snapshot.
#
# POST /_snapshot/{repository}/{snapshot}/_restore
# Docs: https://opensearch.org/docs/latest/api-reference/snapshots/restore-snapshot/
# operationId: snapshot.restore.0
# --index_settings shape: {index?: record, mode?: string, routing_path?: any, soft_deletes?: record, soft_deletes.retention_lease.period?: string, sort?: record, number_of_shards?: any, number_of_replicas?: any, number_of_routing_shards?: any, check_on_startup?: "checksum"|"false"|"true", codec?: string, routing_partition_size?: any, load_fixed_bitset_filters_eagerly?: any, hidden?: any, auto_expand_replicas?: string, merge?: record, merge.scheduler.max_thread_count?: any, search?: record, search.idle.after?: string, refresh_interval?: string, max_result_window?: any, max_inner_result_window?: any, max_rescore_window?: any, max_docvalue_fields_search?: any, max_script_fields?: any, max_ngram_diff?: any, max_shingle_diff?: any, blocks?: record, blocks.read_only?: any, blocks.read_only_allow_delete?: any, blocks.read?: any, blocks.write?: any, blocks.metadata?: any, max_refresh_listeners?: any, analyze?: record, analyze.max_token_count?: any, highlight?: record, highlight.max_analyzed_offset?: any, max_terms_count?: any, max_regex_length?: any, routing?: record, gc_deletes?: string, default_pipeline?: string, final_pipeline?: string, lifecycle?: record, lifecycle.name?: string, provided_name?: string, creation_date?: any, creation_date_string?: any, uuid?: string, version?: record, verified_before_close?: any, format?: any, max_slices_per_scroll?: any, translog?: record, translog.durability?: any, translog.flush_threshold_size?: string, query_string?: record, query_string.lenient?: any, priority?: any, top_metrics_max_size?: any, analysis?: record, settings?: record, queries?: record, similarity?: record, mapping?: record, indexing?: record, indexing_pressure?: record, store?: record, knn?: any, knn.algo_param.ef_search?: any, composite_index.star_tree?: record}
@deprecated --flag master-timeout
export def "snapshot-restore snapshotrestore0" [
  repository: string
  snapshot: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --master-timeout: string # Explicit operation timeout for connection to cluster-manager node (DEPRECATED)
  --wait-for-completion: oneof<nothing, bool> # -| Whether to return a response after the restore operation has completed. When `false`, the request returns a response when the restore operation initializes. When `true`, the request returns a response when the restore operation completes. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --ignore-index-settings: list # A comma-delimited list of index settings to ignore when restoring indexes from a snapshot.
  --ignore-unavailable: oneof<nothing, bool> # How to handle data streams or indexes that are missing or closed.  When `false`, the request returns an error for any data stream or index that is missing or closed.  When `true`, the request ignores data streams and indexes in indexes that are missing or closed.  (default: false)
  --include-aliases: oneof<nothing, bool> # How to handle index aliases from the original snapshot.  When `true`, index aliases from the original snapshot are restored.  When `false`, aliases along with associated indexes are not restored.  (default: true)
  --include-global-state: oneof<nothing, bool> # Whether to restore the current cluster state. When `false`, the cluster state is not restored.  When `true`, the current cluster state is restored.  (default: false)
  --index-settings: record # The configuration settings for an index. — shape: {index?: record, mode?: string, routing_path?: any, soft_deletes?: record, soft_deletes.retention_lease.period?: string, sort?: record, number_of_shards?: any, number_of_replicas?: any, number_of_routing_shards?: any, check_on_startup?: "checksum"|"false"|"true", codec?: string, routing_partition_size?: any, load_fixed_bitset_filters_eagerly?: any, hidden?: any, auto_expand_replicas?: string, merge?: record, merge.scheduler.max_thread_count?: any, search?: record, search.idle.after?: string, refresh_interval?: string, max_result_window?: any, max_inner_result_window?: any, max_rescore_window?: any, max_docvalue_fields_search?: any, max_script_fields?: any, max_ngram_diff?: any, max_shingle_diff?: any, blocks?: record, blocks.read_only?: any, blocks.read_only_allow_delete?: any, blocks.read?: any, blocks.write?: any, blocks.metadata?: any, max_refresh_listeners?: any, analyze?: record, analyze.max_token_count?: any, highlight?: record, highlight.max_analyzed_offset?: any, max_terms_count?: any, max_regex_length?: any, routing?: record, gc_deletes?: string, default_pipeline?: string, final_pipeline?: string, lifecycle?: record, lifecycle.name?: string, provided_name?: string, creation_date?: any, creation_date_string?: any, uuid?: string, version?: record, verified_before_close?: any, format?: any, max_slices_per_scroll?: any, translog?: record, translog.durability?: any, translog.flush_threshold_size?: string, query_string?: record, query_string.lenient?: any, priority?: any, top_metrics_max_size?: any, analysis?: record, settings?: record, queries?: record, similarity?: record, mapping?: record, indexing?: record, indexing_pressure?: record, store?: record, knn?: any, knn.algo_param.ef_search?: any, composite_index.star_tree?: record}
  --indices: any # A comma-separated list of data streams, indexes, and aliases used to limit the request. Supports wildcards (`*`). To target all data streams and indexes, omit this parameter or use `*` or `_all`.
  --partial: oneof<nothing, bool> # How the restore operation will behave if indexes in the snapshot do not have all primary shards available. When `false`, the entire restore operation fails if any indexes in the snapshot do not have all primary shards available. When `true`, allows the restoration of a partial snapshot of indexes with unavailable shards. Only shards that were successfully included in the snapshot are restored.  All missing shards are recreated as empty. By default, the entire restore operation fails if one or more indexes included in the snapshot do not have all primary shards available. To change this behavior, set `partial` to `true`. (default: false)
  --rename-pattern: string # The pattern to apply to the restored data streams and indexes. Data streams and indexes matching the rename pattern will be renamed according to the `rename_replacement` setting. The rename pattern is applied as defined by the regular expression that supports referencing the original text. The request fails if two or more data streams or indexes are renamed into the same name. If you rename a restored data stream, its backing indexes are also renamed. For example, if you rename the logs data stream to `recovered-logs`, the backing index `.ds-logs-1` is renamed to `.ds-recovered-logs-1`. If you rename a restored stream, ensure an index template matches the new stream name. If there are no matching index template names, the stream cannot roll over and new backing indexes are not created.
  --rename-replacement: string # The rename replacement string.
  --rename-alias-pattern: string # The pattern to apply to the restored aliases. Aliases matching the rename pattern will be renamed according to the `rename_alias_replacement` setting. The rename pattern is applied as defined by the regular expression that supports referencing the original text. If two or more aliases are renamed into the same name, these aliases will be merged into one.
  --rename-alias-replacement: string # The rename replacement string for aliases.
  --source-remote-store-repository: string # The name of the remote store repository of the source index being restored. If not provided, the Snapshot Restore API will use the repository that was registered when the snapshot was created.
  --storage-type: string # Where will be the authoritative store of the restored indexes' data. A value of `local` indicates that all snapshot metadata and index data will be downloaded to local storage. A value of `remote_snapshot` indicates that snapshot metadata will be downloaded to the cluster, but the remote repository will remain the authoritative store of the index data. Data will be downloaded and cached as necessary to service queries. At least one node in the cluster must be configured with the search role in order to restore a snapshot using the type `remote_snapshot`. (default: local)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "wait_for_completion" $wait_for_completion "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_snapshot/($repository)/($snapshot)/_restore" $qp)
  let body = {ignore_index_settings: $ignore_index_settings, ignore_unavailable: $ignore_unavailable, include_aliases: $include_aliases, include_global_state: $include_global_state, index_settings: $index_settings, indices: $indices, partial: $partial, rename_pattern: $rename_pattern, rename_replacement: $rename_replacement, rename_alias_pattern: $rename_alias_pattern, rename_alias_replacement: $rename_alias_replacement, source_remote_store_repository: $source_remote_store_repository, storage_type: $storage_type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns information about the status of a snapshot.
#
# GET /_snapshot/{repository}/{snapshot}/_status
# Docs: https://opensearch.org/docs/latest/api-reference/snapshots/get-snapshot-status/
# operationId: snapshot.status.2
@deprecated --flag master-timeout
export def "snapshot-status snapshotstatus2" [
  repository: string
  snapshot: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # The amount of time to wait for a response from the cluster manager node. For more information about supported time units, see [Common parameters](https://opensearch.org/docs/latest/api-reference/common-parameters/#time-units).
  --ignore-unavailable: oneof<nothing, bool> # Whether to ignore any unavailable snapshots, When `false`, a `SnapshotMissingException` is thrown. (default: false)
  --master-timeout: string # Explicit operation timeout for connection to cluster-manager node (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_snapshot/($repository)/($snapshot)/_status" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Provides statistics on operations happening in an index.
#
# GET /_stats
# Docs: https://opensearch.org/docs/latest
# operationId: indices.stats.0
export def "stats indicesstats0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --completion-fields: string # Comma-separated list or wildcard expressions of fields to include in field data and suggest statistics.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`.
  --fielddata-fields: string # Comma-separated list or wildcard expressions of fields to include in field data statistics.
  --qp-fields: string # Comma-separated list or wildcard expressions of fields to include in the statistics.
  --forbid-closed-indices: oneof<nothing, bool> # If `true`, statistics are not collected from closed indexes. (default: true)
  --groups: string # Comma-separated list of search groups to include in the search statistics.
  --include-segment-file-sizes: oneof<nothing, bool> # If `true`, the call reports the aggregated disk usage of each one of the Lucene index files (only applies if segment stats are requested). (default: false)
  --include-unloaded-segments: oneof<nothing, bool> # If `true`, the response includes information from segments that are not loaded into memory. (default: false)
  --level: string@level-completer-1 # Indicates whether statistics are aggregated at the cluster, index, or shard level.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "completion_fields" $completion_fields "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "fielddata_fields" $fielddata_fields "scalar") (serialize-qp "fields" $qp_fields "scalar") (serialize-qp "forbid_closed_indices" $forbid_closed_indices "scalar") (serialize-qp "groups" $groups "scalar") (serialize-qp "include_segment_file_sizes" $include_segment_file_sizes "scalar") (serialize-qp "include_unloaded_segments" $include_unloaded_segments "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Provides statistics on operations happening in an index.
#
# GET /_stats/{metric}
# Docs: https://opensearch.org/docs/latest
# operationId: indices.stats.1
export def "stats indicesstats1" [
  metric: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --completion-fields: string # Comma-separated list or wildcard expressions of fields to include in field data and suggest statistics.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`.
  --fielddata-fields: string # Comma-separated list or wildcard expressions of fields to include in field data statistics.
  --qp-fields: string # Comma-separated list or wildcard expressions of fields to include in the statistics.
  --forbid-closed-indices: oneof<nothing, bool> # If `true`, statistics are not collected from closed indexes. (default: true)
  --groups: string # Comma-separated list of search groups to include in the search statistics.
  --include-segment-file-sizes: oneof<nothing, bool> # If `true`, the call reports the aggregated disk usage of each one of the Lucene index files (only applies if segment stats are requested). (default: false)
  --include-unloaded-segments: oneof<nothing, bool> # If `true`, the response includes information from segments that are not loaded into memory. (default: false)
  --level: string@level-completer-1 # Indicates whether statistics are aggregated at the cluster, index, or shard level.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "completion_fields" $completion_fields "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "fielddata_fields" $fielddata_fields "scalar") (serialize-qp "fields" $qp_fields "scalar") (serialize-qp "forbid_closed_indices" $forbid_closed_indices "scalar") (serialize-qp "groups" $groups "scalar") (serialize-qp "include_segment_file_sizes" $include_segment_file_sizes "scalar") (serialize-qp "include_unloaded_segments" $include_unloaded_segments "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_stats/($metric)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns a list of tasks.
#
# GET /_tasks
# Docs: https://opensearch.org/docs/latest/api-reference/tasks/
# operationId: tasks.list.0
export def "tasks taskslist0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --actions: string # A comma-separated list of actions that should be returned. Keep empty to return all.
  --detailed: oneof<nothing, bool> # When `true`, the response includes detailed information about shard recoveries. (default: false)
  --group-by: string@group-by-completer # Groups tasks by parent/child relationships or nodes.
  --nodes: list # A comma-separated list of node IDs or names used to limit the returned information. Use `_local` to return information from the node you're connecting to, specify the node name to get information from a specific node, or keep the parameter empty to get information from all nodes.
  --parent-task-id: string # Returns tasks with a specified parent task ID (`node_id:task_number`). Keep empty or set to -1 to return all.
  --timeout: string # The amount of time to wait for a response.
  --wait-for-completion: oneof<nothing, bool> # Waits for the matching task to complete. When `true`, the request is blocked until the task has completed. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "actions" $actions "scalar") (serialize-qp "detailed" $detailed "scalar") (serialize-qp "group_by" $group_by "scalar") (serialize-qp "nodes" $nodes "multi") (serialize-qp "parent_task_id" $parent_task_id "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "wait_for_completion" $wait_for_completion "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_tasks" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Cancels a task, if it can be cancelled through an API.
#
# POST /_tasks/_cancel
# Docs: https://opensearch.org/docs/latest/api-reference/tasks/#task-canceling
# operationId: tasks.cancel.0
export def "tasks-cancel taskscancel0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --actions: string # A comma-separated list of actions that should be returned. Keep empty to return all.
  --nodes: list # A comma-separated list of node IDs or names used to limit the returned information. Use `_local` to return information from the node you're connecting to, specify the node name to get information from a specific node, or keep the parameter empty to get information from all nodes.
  --parent-task-id: string # Returns tasks with a specified parent task ID (`node_id:task_number`). Keep empty or set to -1 to return all.
  --wait-for-completion: oneof<nothing, bool> # Waits for the matching task to complete. When `true`, the request is blocked until the task has completed. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "actions" $actions "scalar") (serialize-qp "nodes" $nodes "multi") (serialize-qp "parent_task_id" $parent_task_id "scalar") (serialize-qp "wait_for_completion" $wait_for_completion "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_tasks/_cancel" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about a task.
#
# GET /_tasks/{task_id}
# Docs: https://opensearch.org/docs/latest/api-reference/tasks/
# operationId: tasks.get.0
export def "tasks tasksget0" [
  task_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --timeout: string # The amount of time to wait for a response.
  --wait-for-completion: oneof<nothing, bool> # Waits for the matching task to complete. When `true`, the request is blocked until the task has completed. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "timeout" $timeout "scalar") (serialize-qp "wait_for_completion" $wait_for_completion "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_tasks/($task_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Cancels a task, if it can be cancelled through an API.
#
# POST /_tasks/{task_id}/_cancel
# Docs: https://opensearch.org/docs/latest/api-reference/tasks/#task-canceling
# operationId: tasks.cancel.1
export def "tasks-cancel taskscancel1" [
  task_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --actions: string # A comma-separated list of actions that should be returned. Keep empty to return all.
  --nodes: list # A comma-separated list of node IDs or names used to limit the returned information. Use `_local` to return information from the node you're connecting to, specify the node name to get information from a specific node, or keep the parameter empty to get information from all nodes.
  --parent-task-id: string # Returns tasks with a specified parent task ID (`node_id:task_number`). Keep empty or set to -1 to return all.
  --wait-for-completion: oneof<nothing, bool> # Waits for the matching task to complete. When `true`, the request is blocked until the task has completed. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "actions" $actions "scalar") (serialize-qp "nodes" $nodes "multi") (serialize-qp "parent_task_id" $parent_task_id "scalar") (serialize-qp "wait_for_completion" $wait_for_completion "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_tasks/($task_id)/_cancel" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns an index template.
#
# GET /_template
# Docs: https://opensearch.org/docs/latest
# operationId: indices.get_template.0
@deprecated --flag master-timeout
export def "template template0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --flat-settings: oneof<nothing, bool> # If `true`, returns settings in flat format. (default: false)
  --local: oneof<nothing, bool> # If `true`, the request retrieves information from the local node only. (default: false)
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_template" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Deletes an index template.
#
# DELETE /_template/{name}
# Docs: https://opensearch.org/docs/latest
# operationId: indices.delete_template.0
@deprecated --flag master-timeout
export def "template template0-by-name" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_template/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns an index template.
#
# GET /_template/{name}
# Docs: https://opensearch.org/docs/latest
# operationId: indices.get_template.1
@deprecated --flag master-timeout
export def "template template1-by-name" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --flat-settings: oneof<nothing, bool> # If `true`, returns settings in flat format. (default: false)
  --local: oneof<nothing, bool> # If `true`, the request retrieves information from the local node only. (default: false)
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_template/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about whether a particular index template exists.
#
# HEAD /_template/{name}
# Docs: https://opensearch.org/docs/latest
# operationId: indices.exists_template.0
@deprecated --flag master-timeout
export def "template template0-by-name-1" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --flat-settings: oneof<nothing, bool> # Return settings in flat format. (default: false)
  --local: oneof<nothing, bool> # Return local information, do not retrieve the state from cluster-manager node. (default: false)
  --master-timeout: string # Explicit operation timeout for connection to cluster-manager node (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_template/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or updates an index template.
#
# POST /_template/{name}
# Docs: https://opensearch.org/docs/latest/im-plugin/index-templates/
# operationId: indices.put_template.0
# --mappings shape: {all_field?: record, date_detection?: bool, dynamic?: any, dynamic_date_formats?: list, dynamic_templates?: list, _field_names?: record, index_field?: record, _meta?: record, numeric_detection?: bool, properties?: record, _routing?: record, _size?: record, _source?: record, enabled?: bool, _data_stream_timestamp?: record}
@deprecated --flag master-timeout
export def "template template0-by-name-2" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --create: oneof<nothing, bool> # If `true`, this request cannot replace or update existing index templates. (default: false)
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --order: int # Order in which OpenSearch applies this template if index matches multiple templates.  Templates with lower 'order' values are merged first. Templates with higher 'order' values are merged later, overriding templates with lower values. (format: int32)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --aliases: record # Aliases for the index.
  --index-patterns: any
  --mappings: record # shape: {all_field?: record, date_detection?: bool, dynamic?: any, dynamic_date_formats?: list, dynamic_templates?: list, _field_names?: record, index_field?: record, _meta?: record, numeric_detection?: bool, properties?: record, _routing?: record, _size?: record, _source?: record, enabled?: bool, _data_stream_timestamp?: record}
  --order: int # Order in which OpenSearch applies this template if index matches multiple templates.  Templates with lower 'order' values are merged first. Templates with higher 'order' values are merged later, overriding templates with lower values. (format: int32)
  --settings: record # Configuration options for the index.
  --version: int # format: int64
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "create" $create "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "order" $order "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_template/($name)" $qp)
  let body = {aliases: $aliases, index_patterns: $index_patterns, mappings: $mappings, order: $order, settings: $settings, version: $version} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Creates or updates an index template.
#
# PUT /_template/{name}
# Docs: https://opensearch.org/docs/latest/im-plugin/index-templates/
# operationId: indices.put_template.1
# --mappings shape: {all_field?: record, date_detection?: bool, dynamic?: any, dynamic_date_formats?: list, dynamic_templates?: list, _field_names?: record, index_field?: record, _meta?: record, numeric_detection?: bool, properties?: record, _routing?: record, _size?: record, _source?: record, enabled?: bool, _data_stream_timestamp?: record}
@deprecated --flag master-timeout
export def "template template1-by-name-1" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --create: oneof<nothing, bool> # If `true`, this request cannot replace or update existing index templates. (default: false)
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --order: int # Order in which OpenSearch applies this template if index matches multiple templates.  Templates with lower 'order' values are merged first. Templates with higher 'order' values are merged later, overriding templates with lower values. (format: int32)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --aliases: record # Aliases for the index.
  --index-patterns: any
  --mappings: record # shape: {all_field?: record, date_detection?: bool, dynamic?: any, dynamic_date_formats?: list, dynamic_templates?: list, _field_names?: record, index_field?: record, _meta?: record, numeric_detection?: bool, properties?: record, _routing?: record, _size?: record, _source?: record, enabled?: bool, _data_stream_timestamp?: record}
  --order: int # Order in which OpenSearch applies this template if index matches multiple templates.  Templates with lower 'order' values are merged first. Templates with higher 'order' values are merged later, overriding templates with lower values. (format: int32)
  --settings: record # Configuration options for the index.
  --version: int # format: int64
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "create" $create "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "order" $order "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_template/($name)" $qp)
  let body = {aliases: $aliases, index_patterns: $index_patterns, mappings: $mappings, order: $order, settings: $settings, version: $version} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Changes the number of requests per second for a particular Update By Query operation.
#
# POST /_update_by_query/{task_id}/_rethrottle
# Docs: https://opensearch.org/docs/latest
# operationId: update_by_query_rethrottle.0
export def "update-by-query-rethrottle rethrottle0" [
  task_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --requests-per-second: float # The throttle for this request in sub-requests per second. (format: float)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "requests_per_second" $requests_per_second "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_update_by_query/($task_id)/_rethrottle" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# The `_upgrade` API is no longer useful and will be removed.
#
# GET /_upgrade
# Docs: https://opensearch.org/docs/latest
# operationId: indices.get_upgrade.0
export def "upgrade upgrade0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # Whether to ignore if a wildcard indexes expression resolves into no concrete indexes. (This includes `_all` string or when no indexes have been specified).
  --expand-wildcards: string # Whether to expand wildcard expression to concrete indexes that are open, closed or both.
  --ignore-unavailable: oneof<nothing, bool> # Whether specified concrete indexes should be ignored when unavailable (missing or closed).
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_upgrade" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# The `_upgrade` API is no longer useful and will be removed.
#
# POST /_upgrade
# Docs: https://opensearch.org/docs/latest
# operationId: indices.upgrade.0
export def "upgrade indicesupgrade0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # Whether to ignore if a wildcard indexes expression resolves into no concrete indexes. (This includes `_all` string or when no indexes have been specified).
  --expand-wildcards: string # Whether to expand wildcard expression to concrete indexes that are open, closed or both.
  --ignore-unavailable: oneof<nothing, bool> # Whether specified concrete indexes should be ignored when unavailable (missing or closed).
  --only-ancient-segments: oneof<nothing, bool> # If `true`, only ancient (an older Lucene major release) segments will be upgraded.
  --wait-for-completion: oneof<nothing, bool> # Should this request wait until the operation has completed before returning. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "only_ancient_segments" $only_ancient_segments "scalar") (serialize-qp "wait_for_completion" $wait_for_completion "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_upgrade" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Allows a user to validate a potentially expensive query without executing it.
#
# GET /_validate/query
# Docs: https://opensearch.org/docs/latest
# operationId: indices.validate_query.0
# --query shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
export def "validate-query query0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --all-shards: oneof<nothing, bool> # If `true`, the validation is executed on all shards instead of one random shard per index.
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --analyze-wildcard: oneof<nothing, bool> # If `true`, wildcard and prefix queries are analyzed. (default: false)
  --analyzer: string # Analyzer to use for the query string. This parameter can only be used when the `q` query string parameter is specified.
  --default-operator: string@default-operator-completer # The default operator for query string query: `AND` or `OR`.
  --df: string # Field to use as default where no field prefix is given in the query string. This parameter can only be used when the `q` query string parameter is specified.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --explain: oneof<nothing, bool> # If `true`, the response returns detailed information if an error has occurred.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --lenient: oneof<nothing, bool> # If `true`, format-based query failures (such as providing text to a numeric field) in the query string will be ignored.
  --q: string # Query in the Lucene query string syntax.
  --rewrite: oneof<nothing, bool> # If `true`, returns a more detailed explanation showing the actual Lucene query that will be executed.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "all_shards" $all_shards "scalar") (serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "analyze_wildcard" $analyze_wildcard "scalar") (serialize-qp "analyzer" $analyzer "scalar") (serialize-qp "default_operator" $default_operator "scalar") (serialize-qp "df" $df "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "explain" $explain "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "lenient" $lenient "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "rewrite" $rewrite "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_validate/query" $qp)
  let body = {query: $body_query} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Allows a user to validate a potentially expensive query without executing it.
#
# POST /_validate/query
# Docs: https://opensearch.org/docs/latest
# operationId: indices.validate_query.1
# --query shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
export def "validate-query query1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --all-shards: oneof<nothing, bool> # If `true`, the validation is executed on all shards instead of one random shard per index.
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --analyze-wildcard: oneof<nothing, bool> # If `true`, wildcard and prefix queries are analyzed. (default: false)
  --analyzer: string # Analyzer to use for the query string. This parameter can only be used when the `q` query string parameter is specified.
  --default-operator: string@default-operator-completer # The default operator for query string query: `AND` or `OR`.
  --df: string # Field to use as default where no field prefix is given in the query string. This parameter can only be used when the `q` query string parameter is specified.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --explain: oneof<nothing, bool> # If `true`, the response returns detailed information if an error has occurred.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --lenient: oneof<nothing, bool> # If `true`, format-based query failures (such as providing text to a numeric field) in the query string will be ignored.
  --q: string # Query in the Lucene query string syntax.
  --rewrite: oneof<nothing, bool> # If `true`, returns a more detailed explanation showing the actual Lucene query that will be executed.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "all_shards" $all_shards "scalar") (serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "analyze_wildcard" $analyze_wildcard "scalar") (serialize-qp "analyzer" $analyzer "scalar") (serialize-qp "default_operator" $default_operator "scalar") (serialize-qp "df" $df "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "explain" $explain "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "lenient" $lenient "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "rewrite" $rewrite "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_validate/query" $qp)
  let body = {query: $body_query} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves the specified query group. If no query group is specified, all query groups in the cluster are retrieved.
#
# GET /_wlm/query_group
# operationId: wlm.get_query_group.0
export def "wlm-query-group group0" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_wlm/query_group" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates a new query group and sets the resource limits for the new query group.
#
# PUT /_wlm/query_group
# operationId: wlm.create_query_group.0
export def "wlm-query-group group0-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  name: string
  resiliency_mode: string@resiliency-mode-completer
  resource_limits: any
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_wlm/query_group" $qp)
  let body = {name: $name, resiliency_mode: $resiliency_mode, resource_limits: $resource_limits} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes the specified query group.
#
# DELETE /_wlm/query_group/{name}
# operationId: wlm.delete_query_group.0
export def "wlm-query-group group0-by-name" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_wlm/query_group/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves the specified query group. If no query group is specified, all query groups in the cluster are retrieved.
#
# GET /_wlm/query_group/{name}
# operationId: wlm.get_query_group.1
export def "wlm-query-group group1" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_wlm/query_group/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Updates the specified query group.
#
# PUT /_wlm/query_group/{name}
# operationId: wlm.update_query_group.0
# --resource_limits shape: {memory?: float, cpu?: float}
export def "wlm-query-group group0-by-name-1" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --resiliency-mode: string@resiliency-mode-completer
  --resource-limits: record # shape: {memory?: float, cpu?: float}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_wlm/query_group/($name)" $qp)
  let body = {resiliency_mode: $resiliency_mode, resource_limits: $resource_limits} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Updates an alias to point to a new index when the existing index is considered to be too large or too old.
#
# POST /{alias}/_rollover
# Docs: https://opensearch.org/docs/latest/dashboards/im-dashboards/rollover/
# operationId: indices.rollover.0
# --conditions shape: {min_age?: string, max_age?: string, max_age_millis?: int, min_docs?: int, max_docs?: int, max_size?: string, max_size_bytes?: int, min_size?: string, min_size_bytes?: int, max_primary_shard_size?: string, max_primary_shard_size_bytes?: int, min_primary_shard_size?: string, min_primary_shard_size_bytes?: int, max_primary_shard_docs?: int, min_primary_shard_docs?: int}
# --mappings shape: {all_field?: record, date_detection?: bool, dynamic?: any, dynamic_date_formats?: list, dynamic_templates?: list, _field_names?: record, index_field?: record, _meta?: record, numeric_detection?: bool, properties?: record, _routing?: record, _size?: record, _source?: record, enabled?: bool, _data_stream_timestamp?: record}
@deprecated --flag master-timeout
export def "rollover indicesrollover0" [
  alias: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --dry-run: oneof<nothing, bool> # If `true`, checks whether the current index satisfies the specified conditions but does not perform a rollover. (default: false)
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to all or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --aliases: record # Aliases for the target index. Data streams do not support this parameter.
  --conditions: record # The conditions that trigger an index rollover. — shape: {min_age?: string, max_age?: string, max_age_millis?: int, min_docs?: int, max_docs?: int, max_size?: string, max_size_bytes?: int, min_size?: string, min_size_bytes?: int, max_primary_shard_size?: string, max_primary_shard_size_bytes?: int, min_primary_shard_size?: string, min_primary_shard_size_bytes?: int, max_primary_shard_docs?: int, min_primary_shard_docs?: int}
  --mappings: record # shape: {all_field?: record, date_detection?: bool, dynamic?: any, dynamic_date_formats?: list, dynamic_templates?: list, _field_names?: record, index_field?: record, _meta?: record, numeric_detection?: bool, properties?: record, _routing?: record, _size?: record, _source?: record, enabled?: bool, _data_stream_timestamp?: record}
  --settings: record # Configuration options for the index. Data streams do not support this parameter.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "dry_run" $dry_run "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($alias)/_rollover" $qp)
  let body = {aliases: $aliases, conditions: $conditions, mappings: $mappings, settings: $settings} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Updates an alias to point to a new index when the existing index is considered to be too large or too old.
#
# POST /{alias}/_rollover/{new_index}
# Docs: https://opensearch.org/docs/latest/dashboards/im-dashboards/rollover/
# operationId: indices.rollover.1
# --conditions shape: {min_age?: string, max_age?: string, max_age_millis?: int, min_docs?: int, max_docs?: int, max_size?: string, max_size_bytes?: int, min_size?: string, min_size_bytes?: int, max_primary_shard_size?: string, max_primary_shard_size_bytes?: int, min_primary_shard_size?: string, min_primary_shard_size_bytes?: int, max_primary_shard_docs?: int, min_primary_shard_docs?: int}
# --mappings shape: {all_field?: record, date_detection?: bool, dynamic?: any, dynamic_date_formats?: list, dynamic_templates?: list, _field_names?: record, index_field?: record, _meta?: record, numeric_detection?: bool, properties?: record, _routing?: record, _size?: record, _source?: record, enabled?: bool, _data_stream_timestamp?: record}
@deprecated --flag master-timeout
export def "rollover indicesrollover1" [
  alias: string
  new_index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --dry-run: oneof<nothing, bool> # If `true`, checks whether the current index satisfies the specified conditions but does not perform a rollover. (default: false)
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to all or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --aliases: record # Aliases for the target index. Data streams do not support this parameter.
  --conditions: record # The conditions that trigger an index rollover. — shape: {min_age?: string, max_age?: string, max_age_millis?: int, min_docs?: int, max_docs?: int, max_size?: string, max_size_bytes?: int, min_size?: string, min_size_bytes?: int, max_primary_shard_size?: string, max_primary_shard_size_bytes?: int, min_primary_shard_size?: string, min_primary_shard_size_bytes?: int, max_primary_shard_docs?: int, min_primary_shard_docs?: int}
  --mappings: record # shape: {all_field?: record, date_detection?: bool, dynamic?: any, dynamic_date_formats?: list, dynamic_templates?: list, _field_names?: record, index_field?: record, _meta?: record, numeric_detection?: bool, properties?: record, _routing?: record, _size?: record, _source?: record, enabled?: bool, _data_stream_timestamp?: record}
  --settings: record # Configuration options for the index. Data streams do not support this parameter.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "dry_run" $dry_run "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($alias)/_rollover/($new_index)" $qp)
  let body = {aliases: $aliases, conditions: $conditions, mappings: $mappings, settings: $settings} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes an index.
#
# DELETE /{index}
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/delete-index/
# operationId: indices.delete.0
@deprecated --flag master-timeout
export def "api indicesdelete0" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. (default: false)
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index. (default: false)
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about one or more indexes.
#
# GET /{index}
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/get-index/
# operationId: indices.get.0
@deprecated --flag master-timeout
export def "api indicesget0" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting foo*,bar* returns an error if an index starts with foo but no index starts with bar. (default: false)
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --expand-wildcards: string # Type of index that wildcard expressions can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`.
  --flat-settings: oneof<nothing, bool> # If `true`, returns settings in flat format. (default: false)
  --ignore-unavailable: oneof<nothing, bool> # If `false`, requests that target a missing index return an error. (default: false)
  --include-defaults: oneof<nothing, bool> # If `true`, return all default settings in the response. (default: false)
  --local: oneof<nothing, bool> # If `true`, the request retrieves information from the local node only. Defaults to false, which means information is retrieved from the cluster-manager node. (default: false)
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "include_defaults" $include_defaults "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about whether a particular index exists.
#
# HEAD /{index}
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/exists/
# operationId: indices.exists.0
export def "api indicesexists0" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. (default: false)
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --flat-settings: oneof<nothing, bool> # If `true`, returns settings in flat format. (default: false)
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index. (default: false)
  --include-defaults: oneof<nothing, bool> # If `true`, return all default settings in the response. (default: false)
  --local: oneof<nothing, bool> # If `true`, the request retrieves information from the local node only. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "include_defaults" $include_defaults "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates an index with optional settings and mappings.
#
# PUT /{index}
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/create-index/
# operationId: indices.create.0
# --mappings shape: {all_field?: record, date_detection?: bool, dynamic?: any, dynamic_date_formats?: list, dynamic_templates?: list, _field_names?: record, index_field?: record, _meta?: record, numeric_detection?: bool, properties?: record, _routing?: record, _size?: record, _source?: record, enabled?: bool, _data_stream_timestamp?: record}
# --settings shape: {index?: record, mode?: string, routing_path?: any, soft_deletes?: record, soft_deletes.retention_lease.period?: string, sort?: record, number_of_shards?: any, number_of_replicas?: any, number_of_routing_shards?: any, check_on_startup?: "checksum"|"false"|"true", codec?: string, routing_partition_size?: any, load_fixed_bitset_filters_eagerly?: any, hidden?: any, auto_expand_replicas?: string, merge?: record, merge.scheduler.max_thread_count?: any, search?: record, search.idle.after?: string, refresh_interval?: string, max_result_window?: any, max_inner_result_window?: any, max_rescore_window?: any, max_docvalue_fields_search?: any, max_script_fields?: any, max_ngram_diff?: any, max_shingle_diff?: any, blocks?: record, blocks.read_only?: any, blocks.read_only_allow_delete?: any, blocks.read?: any, blocks.write?: any, blocks.metadata?: any, max_refresh_listeners?: any, analyze?: record, analyze.max_token_count?: any, highlight?: record, highlight.max_analyzed_offset?: any, max_terms_count?: any, max_regex_length?: any, routing?: record, gc_deletes?: string, default_pipeline?: string, final_pipeline?: string, lifecycle?: record, lifecycle.name?: string, provided_name?: string, creation_date?: any, creation_date_string?: any, uuid?: string, version?: record, verified_before_close?: any, format?: any, max_slices_per_scroll?: any, translog?: record, translog.durability?: any, translog.flush_threshold_size?: string, query_string?: record, query_string.lenient?: any, priority?: any, top_metrics_max_size?: any, analysis?: record, settings?: record, queries?: record, similarity?: record, mapping?: record, indexing?: record, indexing_pressure?: record, store?: record, knn?: any, knn.algo_param.ef_search?: any, composite_index.star_tree?: record}
@deprecated --flag master-timeout
export def "api indicescreate0" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation.  Set to `all` or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --aliases: record # Aliases for the index.
  --mappings: record # shape: {all_field?: record, date_detection?: bool, dynamic?: any, dynamic_date_formats?: list, dynamic_templates?: list, _field_names?: record, index_field?: record, _meta?: record, numeric_detection?: bool, properties?: record, _routing?: record, _size?: record, _source?: record, enabled?: bool, _data_stream_timestamp?: record}
  --settings: record # The configuration settings for an index. — shape: {index?: record, mode?: string, routing_path?: any, soft_deletes?: record, soft_deletes.retention_lease.period?: string, sort?: record, number_of_shards?: any, number_of_replicas?: any, number_of_routing_shards?: any, check_on_startup?: "checksum"|"false"|"true", codec?: string, routing_partition_size?: any, load_fixed_bitset_filters_eagerly?: any, hidden?: any, auto_expand_replicas?: string, merge?: record, merge.scheduler.max_thread_count?: any, search?: record, search.idle.after?: string, refresh_interval?: string, max_result_window?: any, max_inner_result_window?: any, max_rescore_window?: any, max_docvalue_fields_search?: any, max_script_fields?: any, max_ngram_diff?: any, max_shingle_diff?: any, blocks?: record, blocks.read_only?: any, blocks.read_only_allow_delete?: any, blocks.read?: any, blocks.write?: any, blocks.metadata?: any, max_refresh_listeners?: any, analyze?: record, analyze.max_token_count?: any, highlight?: record, highlight.max_analyzed_offset?: any, max_terms_count?: any, max_regex_length?: any, routing?: record, gc_deletes?: string, default_pipeline?: string, final_pipeline?: string, lifecycle?: record, lifecycle.name?: string, provided_name?: string, creation_date?: any, creation_date_string?: any, uuid?: string, version?: record, verified_before_close?: any, format?: any, max_slices_per_scroll?: any, translog?: record, translog.durability?: any, translog.flush_threshold_size?: string, query_string?: record, query_string.lenient?: any, priority?: any, top_metrics_max_size?: any, analysis?: record, settings?: record, queries?: record, similarity?: record, mapping?: record, indexing?: record, indexing_pressure?: record, store?: record, knn?: any, knn.algo_param.ef_search?: any, composite_index.star_tree?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)" $qp)
  let body = {aliases: $aliases, mappings: $mappings, settings: $settings} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns an alias.
#
# GET /{index}/_alias
# Docs: https://opensearch.org/docs/latest/im-plugin/index-alias/
# operationId: indices.get_alias.2
export def "alias alias2-by-index" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --local: oneof<nothing, bool> # If `true`, the request retrieves information from the local node only. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_alias" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or updates an alias.
#
# PUT /{index}/_alias
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/update-alias/
# operationId: indices.put_alias.5
# --filter shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
@deprecated --flag master-timeout
export def "alias alias5" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --alias: string
  --filter: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
  --body-index: string
  --index-routing: string # The routing value for the document.
  --is-write-index: oneof<nothing, bool> # If `true`, sets the write index or data stream for the alias. If an alias points to multiple indexes or data streams and `is_write_index` isn't set, the alias rejects write requests. If an index alias points to one index and `is_write_index` isn't set, the index automatically acts as the write index. Data stream aliases don't automatically set a write data stream, even if the alias points to one data stream.
  --routing: string # The routing value for the document.
  --search-routing: string # The routing value for the document.
  --is-hidden: oneof<nothing, bool> # If `true`, the alias will be hidden`. (default: false)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_alias" $qp)
  let body = {alias: $alias, filter: $filter, index: $body_index, index_routing: $index_routing, is_write_index: $is_write_index, routing: $routing, search_routing: $search_routing, is_hidden: $is_hidden} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes an alias.
#
# DELETE /{index}/_alias/{name}
# Docs: https://opensearch.org/docs/latest/im-plugin/index-alias/#delete-aliases
# operationId: indices.delete_alias.0
@deprecated --flag master-timeout
export def "alias alias0-by-index-name" [
  index: string
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_alias/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns an alias.
#
# GET /{index}/_alias/{name}
# Docs: https://opensearch.org/docs/latest/im-plugin/index-alias/
# operationId: indices.get_alias.3
export def "alias alias3" [
  index: string
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --local: oneof<nothing, bool> # If `true`, the request retrieves information from the local node only. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_alias/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about whether a particular alias exists.
#
# HEAD /{index}/_alias/{name}
# Docs: https://opensearch.org/docs/latest
# operationId: indices.exists_alias.1
export def "alias alias1-by-index-name" [
  index: string
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, requests that include a missing data stream or index in the target indexes or data streams return an error.
  --local: oneof<nothing, bool> # If `true`, the request retrieves information from the local node only. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_alias/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or updates an alias.
#
# POST /{index}/_alias/{name}
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/update-alias/
# operationId: indices.put_alias.6
# --filter shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
@deprecated --flag master-timeout
export def "alias alias6" [
  index: string
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --alias: string
  --filter: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
  --body-index: string
  --index-routing: string # The routing value for the document.
  --is-write-index: oneof<nothing, bool> # If `true`, sets the write index or data stream for the alias. If an alias points to multiple indexes or data streams and `is_write_index` isn't set, the alias rejects write requests. If an index alias points to one index and `is_write_index` isn't set, the index automatically acts as the write index. Data stream aliases don't automatically set a write data stream, even if the alias points to one data stream.
  --routing: string # The routing value for the document.
  --search-routing: string # The routing value for the document.
  --is-hidden: oneof<nothing, bool> # If `true`, the alias will be hidden`. (default: false)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_alias/($name)" $qp)
  let body = {alias: $alias, filter: $filter, index: $body_index, index_routing: $index_routing, is_write_index: $is_write_index, routing: $routing, search_routing: $search_routing, is_hidden: $is_hidden} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Creates or updates an alias.
#
# PUT /{index}/_alias/{name}
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/update-alias/
# operationId: indices.put_alias.7
# --filter shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
@deprecated --flag master-timeout
export def "alias alias7" [
  index: string
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --alias: string
  --filter: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
  --body-index: string
  --index-routing: string # The routing value for the document.
  --is-write-index: oneof<nothing, bool> # If `true`, sets the write index or data stream for the alias. If an alias points to multiple indexes or data streams and `is_write_index` isn't set, the alias rejects write requests. If an index alias points to one index and `is_write_index` isn't set, the index automatically acts as the write index. Data stream aliases don't automatically set a write data stream, even if the alias points to one data stream.
  --routing: string # The routing value for the document.
  --search-routing: string # The routing value for the document.
  --is-hidden: oneof<nothing, bool> # If `true`, the alias will be hidden`. (default: false)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_alias/($name)" $qp)
  let body = {alias: $alias, filter: $filter, index: $body_index, index_routing: $index_routing, is_write_index: $is_write_index, routing: $routing, search_routing: $search_routing, is_hidden: $is_hidden} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Creates or updates an alias.
#
# PUT /{index}/_aliases
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/update-alias/
# operationId: indices.put_alias.8
# --filter shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
@deprecated --flag master-timeout
export def "aliases alias8" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --alias: string
  --filter: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
  --body-index: string
  --index-routing: string # The routing value for the document.
  --is-write-index: oneof<nothing, bool> # If `true`, sets the write index or data stream for the alias. If an alias points to multiple indexes or data streams and `is_write_index` isn't set, the alias rejects write requests. If an index alias points to one index and `is_write_index` isn't set, the index automatically acts as the write index. Data stream aliases don't automatically set a write data stream, even if the alias points to one data stream.
  --routing: string # The routing value for the document.
  --search-routing: string # The routing value for the document.
  --is-hidden: oneof<nothing, bool> # If `true`, the alias will be hidden`. (default: false)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_aliases" $qp)
  let body = {alias: $alias, filter: $filter, index: $body_index, index_routing: $index_routing, is_write_index: $is_write_index, routing: $routing, search_routing: $search_routing, is_hidden: $is_hidden} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes an alias.
#
# DELETE /{index}/_aliases/{name}
# Docs: https://opensearch.org/docs/latest/im-plugin/index-alias/#delete-aliases
# operationId: indices.delete_alias.1
@deprecated --flag master-timeout
export def "aliases alias1" [
  index: string
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_aliases/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or updates an alias.
#
# POST /{index}/_aliases/{name}
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/update-alias/
# operationId: indices.put_alias.9
# --filter shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
@deprecated --flag master-timeout
export def "aliases alias9" [
  index: string
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --alias: string
  --filter: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
  --body-index: string
  --index-routing: string # The routing value for the document.
  --is-write-index: oneof<nothing, bool> # If `true`, sets the write index or data stream for the alias. If an alias points to multiple indexes or data streams and `is_write_index` isn't set, the alias rejects write requests. If an index alias points to one index and `is_write_index` isn't set, the index automatically acts as the write index. Data stream aliases don't automatically set a write data stream, even if the alias points to one data stream.
  --routing: string # The routing value for the document.
  --search-routing: string # The routing value for the document.
  --is-hidden: oneof<nothing, bool> # If `true`, the alias will be hidden`. (default: false)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_aliases/($name)" $qp)
  let body = {alias: $alias, filter: $filter, index: $body_index, index_routing: $index_routing, is_write_index: $is_write_index, routing: $routing, search_routing: $search_routing, is_hidden: $is_hidden} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Creates or updates an alias.
#
# PUT /{index}/_aliases/{name}
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/update-alias/
# operationId: indices.put_alias.10
# --filter shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
@deprecated --flag master-timeout
export def "aliases alias10" [
  index: string
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --alias: string
  --filter: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
  --body-index: string
  --index-routing: string # The routing value for the document.
  --is-write-index: oneof<nothing, bool> # If `true`, sets the write index or data stream for the alias. If an alias points to multiple indexes or data streams and `is_write_index` isn't set, the alias rejects write requests. If an index alias points to one index and `is_write_index` isn't set, the index automatically acts as the write index. Data stream aliases don't automatically set a write data stream, even if the alias points to one data stream.
  --routing: string # The routing value for the document.
  --search-routing: string # The routing value for the document.
  --is-hidden: oneof<nothing, bool> # If `true`, the alias will be hidden`. (default: false)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_aliases/($name)" $qp)
  let body = {alias: $alias, filter: $filter, index: $body_index, index_routing: $index_routing, is_write_index: $is_write_index, routing: $routing, search_routing: $search_routing, is_hidden: $is_hidden} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Performs the analysis process on a text and return the tokens breakdown of the text.
#
# GET /{index}/_analyze
# Docs: https://opensearch.org/docs/latest/api-reference/analyze-apis/perform-text-analysis/
# operationId: indices.analyze.2
export def "analyze indicesanalyze2" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --index: string # The name of the index to scope the operation.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --analyzer: string # The name of the analyzer that should be applied to the provided `text`. This could be a built-in analyzer, or an analyzer that's been configured in the index.
  --attributes: list # Array of token attributes used to filter the output of the `explain` parameter.
  --char-filter: list # Array of character filters used to preprocess characters before the tokenizer.
  --explain: oneof<nothing, bool> # If `true`, the response includes token attributes and additional details.
  --field: string # The path to a field or an array of paths. Some APIs support wildcards in the path, which allows you to select multiple fields.
  --filter: list # Array of token filters used to apply after the tokenizer.
  --normalizer: string # Normalizer to use to convert text into a single token.
  --text: any
  --tokenizer: any
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "index" $index "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_analyze" $qp)
  let body = {analyzer: $analyzer, attributes: $attributes, char_filter: $char_filter, explain: $explain, field: $field, filter: $filter, normalizer: $normalizer, text: $text, tokenizer: $tokenizer} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Performs the analysis process on a text and return the tokens breakdown of the text.
#
# POST /{index}/_analyze
# Docs: https://opensearch.org/docs/latest/api-reference/analyze-apis/perform-text-analysis/
# operationId: indices.analyze.3
export def "analyze indicesanalyze3" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --index: string # The name of the index to scope the operation.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --analyzer: string # The name of the analyzer that should be applied to the provided `text`. This could be a built-in analyzer, or an analyzer that's been configured in the index.
  --attributes: list # Array of token attributes used to filter the output of the `explain` parameter.
  --char-filter: list # Array of character filters used to preprocess characters before the tokenizer.
  --explain: oneof<nothing, bool> # If `true`, the response includes token attributes and additional details.
  --field: string # The path to a field or an array of paths. Some APIs support wildcards in the path, which allows you to select multiple fields.
  --filter: list # Array of token filters used to apply after the tokenizer.
  --normalizer: string # Normalizer to use to convert text into a single token.
  --text: any
  --tokenizer: any
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "index" $index "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_analyze" $qp)
  let body = {analyzer: $analyzer, attributes: $attributes, char_filter: $char_filter, explain: $explain, field: $field, filter: $filter, normalizer: $normalizer, text: $text, tokenizer: $tokenizer} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Adds a block to an index.
#
# PUT /{index}/_block/{block}
# Docs: https://opensearch.org/docs/latest
# operationId: indices.add_block.0
@deprecated --flag master-timeout
export def "block block0" [
  block: string
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # Whether to ignore if a wildcard indexes expression resolves into no concrete indexes. (This includes `_all` string or when no indexes have been specified).
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --expand-wildcards: string # Whether to expand wildcard expression to concrete indexes that are open, closed or both.
  --ignore-unavailable: oneof<nothing, bool> # Whether specified concrete indexes should be ignored when unavailable (missing or closed).
  --master-timeout: string # Specify timeout for connection to cluster manager. (DEPRECATED)
  --timeout: string # Explicit operation timeout
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_block/($block)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Allows to perform multiple index/update/delete operations in a single request.
#
# POST /{index}/_bulk
# Docs: https://opensearch.org/docs/latest/api-reference/document-apis/bulk/
# operationId: bulk.2
export def "bulk bulk2" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: string # `true` or `false` to return the `_source` field or not, or a list of fields to return.
  --source-excludes: string # A comma-separated list of source fields to exclude from the response.
  --source-includes: string # A comma-separated list of source fields to include in the response.
  --pipeline: string # ID of the pipeline to use to preprocess incoming documents. If the index has a default ingest pipeline specified, then setting the value to `_none` disables the default ingest pipeline for this request. If a final pipeline is configured it will always run, regardless of the value of this parameter.
  --refresh: string # If `true`, OpenSearch refreshes the affected shards to make this operation visible to search, if `wait_for` then wait for a refresh to make this operation visible to search, if `false` do nothing with refreshes. Valid values: `true`, `false`, `wait_for`.
  --require-alias: oneof<nothing, bool> # If `true`, the request's actions must target an index alias. (default: false)
  --routing: string # Custom value used to route operations to a specific shard.
  --timeout: string # Period each action waits for the following operations: automatic index creation, dynamic mapping updates, waiting for active shards.
  --type: string # Default document type for items which don't provide one.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to all or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "scalar") (serialize-qp "_source_excludes" $source_excludes "scalar") (serialize-qp "_source_includes" $source_includes "scalar") (serialize-qp "pipeline" $pipeline "scalar") (serialize-qp "refresh" $refresh "scalar") (serialize-qp "require_alias" $require_alias "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_bulk" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/x-ndjson" $body
}

# Allows to perform multiple index/update/delete operations in a single request.
#
# PUT /{index}/_bulk
# Docs: https://opensearch.org/docs/latest/api-reference/document-apis/bulk/
# operationId: bulk.3
export def "bulk bulk3" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: string # `true` or `false` to return the `_source` field or not, or a list of fields to return.
  --source-excludes: string # A comma-separated list of source fields to exclude from the response.
  --source-includes: string # A comma-separated list of source fields to include in the response.
  --pipeline: string # ID of the pipeline to use to preprocess incoming documents. If the index has a default ingest pipeline specified, then setting the value to `_none` disables the default ingest pipeline for this request. If a final pipeline is configured it will always run, regardless of the value of this parameter.
  --refresh: string # If `true`, OpenSearch refreshes the affected shards to make this operation visible to search, if `wait_for` then wait for a refresh to make this operation visible to search, if `false` do nothing with refreshes. Valid values: `true`, `false`, `wait_for`.
  --require-alias: oneof<nothing, bool> # If `true`, the request's actions must target an index alias. (default: false)
  --routing: string # Custom value used to route operations to a specific shard.
  --timeout: string # Period each action waits for the following operations: automatic index creation, dynamic mapping updates, waiting for active shards.
  --type: string # Default document type for items which don't provide one.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to all or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "scalar") (serialize-qp "_source_excludes" $source_excludes "scalar") (serialize-qp "_source_includes" $source_includes "scalar") (serialize-qp "pipeline" $pipeline "scalar") (serialize-qp "refresh" $refresh "scalar") (serialize-qp "require_alias" $require_alias "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_bulk" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/x-ndjson" $body
}

# Allows to perform multiple index/update/delete operations using request response streaming.
#
# POST /{index}/_bulk/stream
# Docs: https://opensearch.org/docs/latest/api-reference/document-apis/bulk-streaming/
# operationId: bulk_stream.2
export def "bulk-stream stream2" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: string # `true` or `false` to return the `_source` field or not, or a list of fields to return.
  --source-excludes: string # A comma-separated list of source fields to exclude from the response.
  --source-includes: string # A comma-separated list of source fields to include in the response.
  --batch-interval: string # Specifies for how long bulk operations should be accumulated into a batch before sending the batch to data nodes.
  --batch-size: int # Specifies how many bulk operations should be accumulated into a batch before sending the batch to data nodes. (format: int64)
  --pipeline: string # ID of the pipeline to use to preprocess incoming documents. If the index has a default ingest pipeline specified, then setting the value to `_none` disables the default ingest pipeline for this request. If a final pipeline is configured it will always run, regardless of the value of this parameter.
  --refresh: string # If `true`, OpenSearch refreshes the affected shards to make this operation visible to search, if `wait_for` then wait for a refresh to make this operation visible to search, if `false` do nothing with refreshes. Valid values: `true`, `false`, `wait_for`.
  --require-alias: oneof<nothing, bool> # If `true`, the request's actions must target an index alias. (default: false)
  --routing: string # Custom value used to route operations to a specific shard.
  --timeout: string # Period each action waits for the following operations: automatic index creation, dynamic mapping updates, waiting for active shards.
  --type: string # Default document type for items which don't provide one.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to all or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "scalar") (serialize-qp "_source_excludes" $source_excludes "scalar") (serialize-qp "_source_includes" $source_includes "scalar") (serialize-qp "batch_interval" $batch_interval "scalar") (serialize-qp "batch_size" $batch_size "scalar") (serialize-qp "pipeline" $pipeline "scalar") (serialize-qp "refresh" $refresh "scalar") (serialize-qp "require_alias" $require_alias "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_bulk/stream" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/x-ndjson" $body
}

# Allows to perform multiple index/update/delete operations using request response streaming.
#
# PUT /{index}/_bulk/stream
# Docs: https://opensearch.org/docs/latest/api-reference/document-apis/bulk-streaming/
# operationId: bulk_stream.3
export def "bulk-stream stream3" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: string # `true` or `false` to return the `_source` field or not, or a list of fields to return.
  --source-excludes: string # A comma-separated list of source fields to exclude from the response.
  --source-includes: string # A comma-separated list of source fields to include in the response.
  --batch-interval: string # Specifies for how long bulk operations should be accumulated into a batch before sending the batch to data nodes.
  --batch-size: int # Specifies how many bulk operations should be accumulated into a batch before sending the batch to data nodes. (format: int64)
  --pipeline: string # ID of the pipeline to use to preprocess incoming documents. If the index has a default ingest pipeline specified, then setting the value to `_none` disables the default ingest pipeline for this request. If a final pipeline is configured it will always run, regardless of the value of this parameter.
  --refresh: string # If `true`, OpenSearch refreshes the affected shards to make this operation visible to search, if `wait_for` then wait for a refresh to make this operation visible to search, if `false` do nothing with refreshes. Valid values: `true`, `false`, `wait_for`.
  --require-alias: oneof<nothing, bool> # If `true`, the request's actions must target an index alias. (default: false)
  --routing: string # Custom value used to route operations to a specific shard.
  --timeout: string # Period each action waits for the following operations: automatic index creation, dynamic mapping updates, waiting for active shards.
  --type: string # Default document type for items which don't provide one.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to all or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "scalar") (serialize-qp "_source_excludes" $source_excludes "scalar") (serialize-qp "_source_includes" $source_includes "scalar") (serialize-qp "batch_interval" $batch_interval "scalar") (serialize-qp "batch_size" $batch_size "scalar") (serialize-qp "pipeline" $pipeline "scalar") (serialize-qp "refresh" $refresh "scalar") (serialize-qp "require_alias" $require_alias "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_bulk/stream" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/x-ndjson" $body
}

# Clears all or specific caches for one or more indexes.
#
# POST /{index}/_cache/clear
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/clear-index-cache/
# operationId: indices.clear_cache.1
export def "cache-clear cache1" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --fielddata: oneof<nothing, bool> # If `true`, clears the fields cache. Use the `fields` parameter to clear the cache of specific fields only.
  --qp-fields: string # Comma-separated list of field names used to limit the `fielddata` parameter.
  --file: oneof<nothing, bool> # If `true`, clears the unused entries from the file cache on nodes with the Search role. (default: false)
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --index: list # Comma-separated list of indexes; use `_all` or empty string to perform the operation on all indexes.
  --qp-query: oneof<nothing, bool> # If `true`, clears the query cache.
  --request: oneof<nothing, bool> # If `true`, clears the request cache.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "fielddata" $fielddata "scalar") (serialize-qp "fields" $qp_fields "scalar") (serialize-qp "file" $file "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "index" $index "multi") (serialize-qp "query" $qp_query "scalar") (serialize-qp "request" $request "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_cache/clear" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Clones an index.
#
# POST /{index}/_clone/{target}
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/clone/
# operationId: indices.clone.0
@deprecated --flag master-timeout
export def "clone indicesclone0" [
  index: string
  target: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --task-execution-timeout: string # Explicit task execution timeout, only useful when `wait_for_completion` is false, defaults to `1h`.
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to `all` or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --wait-for-completion: oneof<nothing, bool> # Should this request wait until the operation has completed before returning. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --aliases: record # Aliases for the resulting index.
  --settings: record # Configuration options for the target index.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "task_execution_timeout" $task_execution_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "wait_for_completion" $wait_for_completion "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_clone/($target)" $qp)
  let body = {aliases: $aliases, settings: $settings} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Clones an index.
#
# PUT /{index}/_clone/{target}
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/clone/
# operationId: indices.clone.1
@deprecated --flag master-timeout
export def "clone indicesclone1" [
  index: string
  target: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --task-execution-timeout: string # Explicit task execution timeout, only useful when `wait_for_completion` is false, defaults to `1h`.
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to `all` or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --wait-for-completion: oneof<nothing, bool> # Should this request wait until the operation has completed before returning. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --aliases: record # Aliases for the resulting index.
  --settings: record # Configuration options for the target index.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "task_execution_timeout" $task_execution_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "wait_for_completion" $wait_for_completion "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_clone/($target)" $qp)
  let body = {aliases: $aliases, settings: $settings} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Closes an index.
#
# POST /{index}/_close
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/close-index/
# operationId: indices.close.0
@deprecated --flag master-timeout
export def "close indicesclose0" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to `all` or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_close" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns number of documents matching a query.
#
# GET /{index}/_count
# Docs: https://opensearch.org/docs/latest/api-reference/count/
# operationId: count.2
# --query shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
export def "count count2" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --analyze-wildcard: oneof<nothing, bool> # If `true`, wildcard and prefix queries are analyzed. This parameter can only be used when the `q` query string parameter is specified. (default: false)
  --analyzer: string # Analyzer to use for the query string. This parameter can only be used when the `q` query string parameter is specified.
  --default-operator: string@default-operator-completer # The default operator for query string query: `AND` or `OR`. This parameter can only be used when the `q` query string parameter is specified.
  --df: string # Field to use as default where no field prefix is given in the query string. This parameter can only be used when the `q` query string parameter is specified.
  --expand-wildcards: string
  --ignore-throttled: oneof<nothing, bool> # If `true`, concrete, expanded or aliased indexes are ignored when frozen.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --lenient: oneof<nothing, bool> # If `true`, format-based query failures (such as providing text to a numeric field) in the query string will be ignored.
  --min-score: float # Sets the minimum `_score` value that documents must have to be included in the result. (format: float)
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --q: string # Query in the Lucene query string syntax.
  --routing: string # Custom value used to route operations to a specific shard.
  --terminate-after: int # Maximum number of documents to collect for each shard. If a query reaches this limit, OpenSearch terminates the query early. OpenSearch collects documents before sorting. (format: int32)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "analyze_wildcard" $analyze_wildcard "scalar") (serialize-qp "analyzer" $analyzer "scalar") (serialize-qp "default_operator" $default_operator "scalar") (serialize-qp "df" $df "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_throttled" $ignore_throttled "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "lenient" $lenient "scalar") (serialize-qp "min_score" $min_score "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "terminate_after" $terminate_after "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_count" $qp)
  let body = {query: $body_query} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns number of documents matching a query.
#
# POST /{index}/_count
# Docs: https://opensearch.org/docs/latest/api-reference/count/
# operationId: count.3
# --query shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
export def "count count3" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --analyze-wildcard: oneof<nothing, bool> # If `true`, wildcard and prefix queries are analyzed. This parameter can only be used when the `q` query string parameter is specified. (default: false)
  --analyzer: string # Analyzer to use for the query string. This parameter can only be used when the `q` query string parameter is specified.
  --default-operator: string@default-operator-completer # The default operator for query string query: `AND` or `OR`. This parameter can only be used when the `q` query string parameter is specified.
  --df: string # Field to use as default where no field prefix is given in the query string. This parameter can only be used when the `q` query string parameter is specified.
  --expand-wildcards: string
  --ignore-throttled: oneof<nothing, bool> # If `true`, concrete, expanded or aliased indexes are ignored when frozen.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --lenient: oneof<nothing, bool> # If `true`, format-based query failures (such as providing text to a numeric field) in the query string will be ignored.
  --min-score: float # Sets the minimum `_score` value that documents must have to be included in the result. (format: float)
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --q: string # Query in the Lucene query string syntax.
  --routing: string # Custom value used to route operations to a specific shard.
  --terminate-after: int # Maximum number of documents to collect for each shard. If a query reaches this limit, OpenSearch terminates the query early. OpenSearch collects documents before sorting. (format: int32)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "analyze_wildcard" $analyze_wildcard "scalar") (serialize-qp "analyzer" $analyzer "scalar") (serialize-qp "default_operator" $default_operator "scalar") (serialize-qp "df" $df "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_throttled" $ignore_throttled "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "lenient" $lenient "scalar") (serialize-qp "min_score" $min_score "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "terminate_after" $terminate_after "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_count" $qp)
  let body = {query: $body_query} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Creates a new document in the index.  Returns a 409 response when a document with a same ID already exists in the index.
#
# POST /{index}/_create/{id}
# Docs: https://opensearch.org/docs/latest/api-reference/document-apis/index-document/
# operationId: create.0
export def "create create0" [
  id: string
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pipeline: string # ID of the pipeline to use to preprocess incoming documents. If the index has a default ingest pipeline specified, then setting the value to `_none` disables the default ingest pipeline for this request. If a final pipeline is configured it will always run, regardless of the value of this parameter.
  --refresh: string # If `true`, OpenSearch refreshes the affected shards to make this operation visible to search, if `wait_for` then wait for a refresh to make this operation visible to search, if `false` do nothing with refreshes. Valid values: `true`, `false`, `wait_for`.
  --routing: string # Custom value used to route operations to a specific shard.
  --timeout: string # Period the request waits for the following operations: automatic index creation, dynamic mapping updates, waiting for active shards.
  --version: int # Explicit version number for concurrency control. The specified version must match the current version of the document for the request to succeed. (format: int64)
  --version-type: string # Specific version type: `external`, `external_gte`.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to `all` or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pipeline" $pipeline "scalar") (serialize-qp "refresh" $refresh "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "version_type" $version_type "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_create/($id)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Creates a new document in the index.  Returns a 409 response when a document with a same ID already exists in the index.
#
# PUT /{index}/_create/{id}
# Docs: https://opensearch.org/docs/latest/api-reference/document-apis/index-document/
# operationId: create.1
export def "create create1" [
  id: string
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pipeline: string # ID of the pipeline to use to preprocess incoming documents. If the index has a default ingest pipeline specified, then setting the value to `_none` disables the default ingest pipeline for this request. If a final pipeline is configured it will always run, regardless of the value of this parameter.
  --refresh: string # If `true`, OpenSearch refreshes the affected shards to make this operation visible to search, if `wait_for` then wait for a refresh to make this operation visible to search, if `false` do nothing with refreshes. Valid values: `true`, `false`, `wait_for`.
  --routing: string # Custom value used to route operations to a specific shard.
  --timeout: string # Period the request waits for the following operations: automatic index creation, dynamic mapping updates, waiting for active shards.
  --version: int # Explicit version number for concurrency control. The specified version must match the current version of the document for the request to succeed. (format: int64)
  --version-type: string # Specific version type: `external`, `external_gte`.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to `all` or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pipeline" $pipeline "scalar") (serialize-qp "refresh" $refresh "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "version_type" $version_type "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_create/($id)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes documents matching the provided query.
#
# POST /{index}/_delete_by_query
# Docs: https://opensearch.org/docs/latest/api-reference/document-apis/delete-by-query/
# operationId: delete_by_query.0
# --query shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
# --slice shape: {field?: string, id: int, max: int}
export def "delete-by-query query0" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: string # Set to `true` or `false` to return the `_source` field or not, or a list of fields to return.
  --source-excludes: list # List of fields to exclude from the returned `_source` field.
  --source-includes: list # List of fields to extract and return from the `_source` field.
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with `foo` but no index starts with `bar`.
  --analyze-wildcard: oneof<nothing, bool> # If `true`, wildcard and prefix queries are analyzed. (default: false)
  --analyzer: string # Analyzer to use for the query string.
  --conflicts: string # What to do if delete by query hits version conflicts: `abort` or `proceed`.
  --default-operator: string@default-operator-completer # The default operator for query string query: `AND` or `OR`.
  --df: string # Field to use as default where no field prefix is given in the query string.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --qp-from: int # Starting offset. (format: int32, default: 0)
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --lenient: oneof<nothing, bool> # If `true`, format-based query failures (such as providing text to a numeric field) in the query string will be ignored.
  --max-docs: int # Maximum number of documents to process. Defaults to all documents. (format: int32)
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --q: string # Query in the Lucene query string syntax.
  --refresh: string # If `true`, OpenSearch refreshes all shards involved in the delete by query after the request completes.
  --request-cache: oneof<nothing, bool> # If `true`, the request cache is used for this request. Defaults to the index-level setting.
  --requests-per-second: float # The throttle for this request in sub-requests per second. (format: float, default: 0)
  --routing: string # Custom value used to route operations to a specific shard.
  --scroll: string # Period to retain the search context for scrolling.
  --scroll-size: int # Size of the scroll request that powers the operation. (format: int32, default: 100)
  --search-timeout: string # Explicit timeout for each search request. Defaults to no timeout.
  --search-type: string # The type of the search operation. Available options: `query_then_fetch`, `dfs_query_then_fetch`.
  --size: int # Deprecated, use `max_docs` instead. (format: int32)
  --slices: string # The number of slices this task should be divided into.
  --qp-sort: list # A comma-separated list of <field>:<direction> pairs.
  --stats: list # Specific `tag` of the request for logging and statistical purposes.
  --terminate-after: int # Maximum number of documents to collect for each shard. If a query reaches this limit, OpenSearch terminates the query early. OpenSearch collects documents before sorting. Use with caution. OpenSearch applies this parameter to each shard handling the request. When possible, let OpenSearch perform early termination automatically. Avoid specifying this parameter for requests that target data streams with backing indexes across multiple data tiers. (format: int32)
  --timeout: string # Period each deletion request waits for active shards.
  --version: oneof<nothing, bool> # If `true`, returns the document version as part of a hit.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to all or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --wait-for-completion: oneof<nothing, bool> # If `true`, the request blocks until the operation is complete. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --max-docs: int # The maximum number of documents to delete. (format: int32)
  --body-query: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
  --slice: record # The configuration for a sliced scroll request. — shape: {field?: string, id: int, max: int}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "scalar") (serialize-qp "_source_excludes" $source_excludes "multi") (serialize-qp "_source_includes" $source_includes "multi") (serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "analyze_wildcard" $analyze_wildcard "scalar") (serialize-qp "analyzer" $analyzer "scalar") (serialize-qp "conflicts" $conflicts "scalar") (serialize-qp "default_operator" $default_operator "scalar") (serialize-qp "df" $df "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "from" $qp_from "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "lenient" $lenient "scalar") (serialize-qp "max_docs" $max_docs "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "refresh" $refresh "scalar") (serialize-qp "request_cache" $request_cache "scalar") (serialize-qp "requests_per_second" $requests_per_second "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "scroll" $scroll "scalar") (serialize-qp "scroll_size" $scroll_size "scalar") (serialize-qp "search_timeout" $search_timeout "scalar") (serialize-qp "search_type" $search_type "scalar") (serialize-qp "size" $size "scalar") (serialize-qp "slices" $slices "scalar") (serialize-qp "sort" $qp_sort "multi") (serialize-qp "stats" $stats "multi") (serialize-qp "terminate_after" $terminate_after "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "wait_for_completion" $wait_for_completion "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_delete_by_query" $qp)
  let body = {max_docs: $max_docs, query: $body_query, slice: $slice} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Creates or updates a document in an index.
#
# POST /{index}/_doc
# Docs: https://opensearch.org/docs/latest/api-reference/document-apis/index-document/
# operationId: index.0
export def "doc index0" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --if-primary-term: int # Only perform the operation if the document has this primary term. (format: int64)
  --if-seq-no: int # Only perform the operation if the document has this sequence number. (format: int64)
  --op-type: string # Set to create to only index the document if it does not already exist (put if absent). If a document with the specified `_id` already exists, the indexing operation will fail. Same as using the `<index>/_create` endpoint. Valid values: `index`, `create`. If document id is specified, it defaults to `index`. Otherwise, it defaults to `create`.
  --pipeline: string # ID of the pipeline to use to preprocess incoming documents. If the index has a default ingest pipeline specified, then setting the value to `_none` disables the default ingest pipeline for this request. If a final pipeline is configured it will always run, regardless of the value of this parameter.
  --refresh: string # If `true`, OpenSearch refreshes the affected shards to make this operation visible to search, if `wait_for` then wait for a refresh to make this operation visible to search, if `false` do nothing with refreshes. Valid values: `true`, `false`, `wait_for`.
  --require-alias: oneof<nothing, bool> # If `true`, the destination must be an index alias. (default: false)
  --routing: string # Custom value used to route operations to a specific shard.
  --timeout: string # Period the request waits for the following operations: automatic index creation, dynamic mapping updates, waiting for active shards.
  --version: int # Explicit version number for concurrency control. The specified version must match the current version of the document for the request to succeed. (format: int64)
  --version-type: string # Specific version type: `external`, `external_gte`.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to all or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "if_primary_term" $if_primary_term "scalar") (serialize-qp "if_seq_no" $if_seq_no "scalar") (serialize-qp "op_type" $op_type "scalar") (serialize-qp "pipeline" $pipeline "scalar") (serialize-qp "refresh" $refresh "scalar") (serialize-qp "require_alias" $require_alias "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "version_type" $version_type "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_doc" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Removes a document from the index.
#
# DELETE /{index}/_doc/{id}
# Docs: https://opensearch.org/docs/latest/api-reference/document-apis/delete-document/
# operationId: delete.0
export def "doc delete0" [
  id: string
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --if-primary-term: int # Only perform the operation if the document has this primary term. (format: int64)
  --if-seq-no: int # Only perform the operation if the document has this sequence number. (format: int64)
  --refresh: string # If `true`, OpenSearch refreshes the affected shards to make this operation visible to search, if `wait_for` then wait for a refresh to make this operation visible to search, if `false` do nothing with refreshes. Valid values: `true`, `false`, `wait_for`.
  --routing: string # Custom value used to route operations to a specific shard.
  --timeout: string # Period to wait for active shards.
  --version: int # Explicit version number for concurrency control. The specified version must match the current version of the document for the request to succeed. (format: int64)
  --version-type: string # Specific version type: `external`, `external_gte`.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to `all` or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "if_primary_term" $if_primary_term "scalar") (serialize-qp "if_seq_no" $if_seq_no "scalar") (serialize-qp "refresh" $refresh "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "version_type" $version_type "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_doc/($id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns a document.
#
# GET /{index}/_doc/{id}
# Docs: https://opensearch.org/docs/latest/api-reference/document-apis/get-documents/
# operationId: get.0
export def "doc get0" [
  id: string
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: string # Set to `true` or `false` to return the `_source` field or not, or a list of fields to return.
  --source-excludes: string # A comma-separated list of source fields to exclude in the response.
  --source-includes: string # A comma-separated list of source fields to include in the response.
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --realtime: oneof<nothing, bool> # If `true`, the request is real-time as opposed to near-real-time.
  --refresh: string # If `true`, OpenSearch refreshes the affected shards to make this operation visible to search. If `false`, do nothing with refreshes.
  --routing: string # Target the specified primary shard.
  --stored-fields: string # List of stored fields to return as part of a hit. If no fields are specified, no stored fields are included in the response. If this field is specified, the `_source` parameter defaults to false.
  --version: int # Explicit version number for concurrency control. The specified version must match the current version of the document for the request to succeed. (format: int64)
  --version-type: string # Specific version type: `internal`, `external`, `external_gte`.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "scalar") (serialize-qp "_source_excludes" $source_excludes "scalar") (serialize-qp "_source_includes" $source_includes "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "realtime" $realtime "scalar") (serialize-qp "refresh" $refresh "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "stored_fields" $stored_fields "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "version_type" $version_type "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_doc/($id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about whether a document exists in an index.
#
# HEAD /{index}/_doc/{id}
# Docs: https://opensearch.org/docs/latest/api-reference/document-apis/get-documents/
# operationId: exists.0
export def "doc exists0" [
  id: string
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: string # `true` or `false` to return the `_source` field or not, or a list of fields to return.
  --source-excludes: string # A comma-separated list of source fields to exclude in the response.
  --source-includes: string # A comma-separated list of source fields to include in the response.
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --realtime: oneof<nothing, bool> # If `true`, the request is real-time as opposed to near-real-time.
  --refresh: string # If `true`, OpenSearch refreshes all shards involved in the delete by query after the request completes.
  --routing: string # Target the specified primary shard.
  --stored-fields: string # List of stored fields to return as part of a hit. If no fields are specified, no stored fields are included in the response. If this field is specified, the `_source` parameter defaults to false.
  --version: int # Explicit version number for concurrency control. The specified version must match the current version of the document for the request to succeed. (format: int64)
  --version-type: string # Specific version type: `external`, `external_gte`.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "scalar") (serialize-qp "_source_excludes" $source_excludes "scalar") (serialize-qp "_source_includes" $source_includes "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "realtime" $realtime "scalar") (serialize-qp "refresh" $refresh "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "stored_fields" $stored_fields "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "version_type" $version_type "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_doc/($id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or updates a document in an index.
#
# POST /{index}/_doc/{id}
# Docs: https://opensearch.org/docs/latest/api-reference/document-apis/index-document/
# operationId: index.1
export def "doc index1" [
  id: string
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --if-primary-term: int # Only perform the operation if the document has this primary term. (format: int64)
  --if-seq-no: int # Only perform the operation if the document has this sequence number. (format: int64)
  --op-type: string # Set to create to only index the document if it does not already exist (put if absent). If a document with the specified `_id` already exists, the indexing operation will fail. Same as using the `<index>/_create` endpoint. Valid values: `index`, `create`. If document id is specified, it defaults to `index`. Otherwise, it defaults to `create`.
  --pipeline: string # ID of the pipeline to use to preprocess incoming documents. If the index has a default ingest pipeline specified, then setting the value to `_none` disables the default ingest pipeline for this request. If a final pipeline is configured it will always run, regardless of the value of this parameter.
  --refresh: string # If `true`, OpenSearch refreshes the affected shards to make this operation visible to search, if `wait_for` then wait for a refresh to make this operation visible to search, if `false` do nothing with refreshes. Valid values: `true`, `false`, `wait_for`.
  --require-alias: oneof<nothing, bool> # If `true`, the destination must be an index alias. (default: false)
  --routing: string # Custom value used to route operations to a specific shard.
  --timeout: string # Period the request waits for the following operations: automatic index creation, dynamic mapping updates, waiting for active shards.
  --version: int # Explicit version number for concurrency control. The specified version must match the current version of the document for the request to succeed. (format: int64)
  --version-type: string # Specific version type: `external`, `external_gte`.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to all or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "if_primary_term" $if_primary_term "scalar") (serialize-qp "if_seq_no" $if_seq_no "scalar") (serialize-qp "op_type" $op_type "scalar") (serialize-qp "pipeline" $pipeline "scalar") (serialize-qp "refresh" $refresh "scalar") (serialize-qp "require_alias" $require_alias "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "version_type" $version_type "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_doc/($id)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Creates or updates a document in an index.
#
# PUT /{index}/_doc/{id}
# Docs: https://opensearch.org/docs/latest/api-reference/document-apis/index-document/
# operationId: index.2
export def "doc index2" [
  id: string
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --if-primary-term: int # Only perform the operation if the document has this primary term. (format: int64)
  --if-seq-no: int # Only perform the operation if the document has this sequence number. (format: int64)
  --op-type: string # Set to create to only index the document if it does not already exist (put if absent). If a document with the specified `_id` already exists, the indexing operation will fail. Same as using the `<index>/_create` endpoint. Valid values: `index`, `create`. If document id is specified, it defaults to `index`. Otherwise, it defaults to `create`.
  --pipeline: string # ID of the pipeline to use to preprocess incoming documents. If the index has a default ingest pipeline specified, then setting the value to `_none` disables the default ingest pipeline for this request. If a final pipeline is configured it will always run, regardless of the value of this parameter.
  --refresh: string # If `true`, OpenSearch refreshes the affected shards to make this operation visible to search, if `wait_for` then wait for a refresh to make this operation visible to search, if `false` do nothing with refreshes. Valid values: `true`, `false`, `wait_for`.
  --require-alias: oneof<nothing, bool> # If `true`, the destination must be an index alias. (default: false)
  --routing: string # Custom value used to route operations to a specific shard.
  --timeout: string # Period the request waits for the following operations: automatic index creation, dynamic mapping updates, waiting for active shards.
  --version: int # Explicit version number for concurrency control. The specified version must match the current version of the document for the request to succeed. (format: int64)
  --version-type: string # Specific version type: `external`, `external_gte`.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to all or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "if_primary_term" $if_primary_term "scalar") (serialize-qp "if_seq_no" $if_seq_no "scalar") (serialize-qp "op_type" $op_type "scalar") (serialize-qp "pipeline" $pipeline "scalar") (serialize-qp "refresh" $refresh "scalar") (serialize-qp "require_alias" $require_alias "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "version_type" $version_type "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_doc/($id)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns information about why a specific matches (or doesn't match) a query.
#
# GET /{index}/_explain/{id}
# Docs: https://opensearch.org/docs/latest/api-reference/explain/
# operationId: explain.0
# --query shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
export def "explain explain0" [
  id: string
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: string # Set to `true` or `false` to return the `_source` field or not, or a list of fields to return.
  --source-excludes: string # A comma-separated list of source fields to exclude from the response.
  --source-includes: string # A comma-separated list of source fields to include in the response.
  --analyze-wildcard: oneof<nothing, bool> # If `true`, wildcard and prefix queries are analyzed. (default: false)
  --analyzer: string # Analyzer to use for the query string. This parameter can only be used when the `q` query string parameter is specified.
  --default-operator: string@default-operator-completer # The default operator for query string query: `AND` or `OR`.
  --df: string # Field to use as default where no field prefix is given in the query string. (default: _all)
  --lenient: oneof<nothing, bool> # If `true`, format-based query failures (such as providing text to a numeric field) in the query string will be ignored.
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --q: string # Query in the Lucene query string syntax.
  --routing: string # Custom value used to route operations to a specific shard.
  --stored-fields: string # A comma-separated list of stored fields to return in the response.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "scalar") (serialize-qp "_source_excludes" $source_excludes "scalar") (serialize-qp "_source_includes" $source_includes "scalar") (serialize-qp "analyze_wildcard" $analyze_wildcard "scalar") (serialize-qp "analyzer" $analyzer "scalar") (serialize-qp "default_operator" $default_operator "scalar") (serialize-qp "df" $df "scalar") (serialize-qp "lenient" $lenient "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "stored_fields" $stored_fields "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_explain/($id)" $qp)
  let body = {query: $body_query} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns information about why a specific matches (or doesn't match) a query.
#
# POST /{index}/_explain/{id}
# Docs: https://opensearch.org/docs/latest/api-reference/explain/
# operationId: explain.1
# --query shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
export def "explain explain1" [
  id: string
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: string # Set to `true` or `false` to return the `_source` field or not, or a list of fields to return.
  --source-excludes: string # A comma-separated list of source fields to exclude from the response.
  --source-includes: string # A comma-separated list of source fields to include in the response.
  --analyze-wildcard: oneof<nothing, bool> # If `true`, wildcard and prefix queries are analyzed. (default: false)
  --analyzer: string # Analyzer to use for the query string. This parameter can only be used when the `q` query string parameter is specified.
  --default-operator: string@default-operator-completer # The default operator for query string query: `AND` or `OR`.
  --df: string # Field to use as default where no field prefix is given in the query string. (default: _all)
  --lenient: oneof<nothing, bool> # If `true`, format-based query failures (such as providing text to a numeric field) in the query string will be ignored.
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --q: string # Query in the Lucene query string syntax.
  --routing: string # Custom value used to route operations to a specific shard.
  --stored-fields: string # A comma-separated list of stored fields to return in the response.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "scalar") (serialize-qp "_source_excludes" $source_excludes "scalar") (serialize-qp "_source_includes" $source_includes "scalar") (serialize-qp "analyze_wildcard" $analyze_wildcard "scalar") (serialize-qp "analyzer" $analyzer "scalar") (serialize-qp "default_operator" $default_operator "scalar") (serialize-qp "df" $df "scalar") (serialize-qp "lenient" $lenient "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "stored_fields" $stored_fields "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_explain/($id)" $qp)
  let body = {query: $body_query} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns the information about the capabilities of fields among multiple indexes.
#
# GET /{index}/_field_caps
# Docs: https://opensearch.org/docs/latest/field-types/supported-field-types/alias/#using-aliases-in-field-capabilities-api-operations
# operationId: field_caps.2
# --index_filter shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
export def "field-caps caps2" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with foo but no index starts with bar.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`.
  --qp-fields: string # Comma-separated list of fields to retrieve capabilities for. Wildcard (`*`) expressions are supported.
  --ignore-unavailable: oneof<nothing, bool> # If `true`, missing or closed indexes are not included in the response.
  --include-unmapped: oneof<nothing, bool> # If `true`, unmapped fields are included in the response. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-fields: any
  --index-filter: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "fields" $qp_fields "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "include_unmapped" $include_unmapped "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_field_caps" $qp)
  let body = {fields: $body_fields, index_filter: $index_filter} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns the information about the capabilities of fields among multiple indexes.
#
# POST /{index}/_field_caps
# Docs: https://opensearch.org/docs/latest/field-types/supported-field-types/alias/#using-aliases-in-field-capabilities-api-operations
# operationId: field_caps.3
# --index_filter shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
export def "field-caps caps3" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with foo but no index starts with bar.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`.
  --qp-fields: string # Comma-separated list of fields to retrieve capabilities for. Wildcard (`*`) expressions are supported.
  --ignore-unavailable: oneof<nothing, bool> # If `true`, missing or closed indexes are not included in the response.
  --include-unmapped: oneof<nothing, bool> # If `true`, unmapped fields are included in the response. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-fields: any
  --index-filter: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "fields" $qp_fields "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "include_unmapped" $include_unmapped "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_field_caps" $qp)
  let body = {fields: $body_fields, index_filter: $index_filter} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Performs the flush operation on one or more indexes.
#
# GET /{index}/_flush
# Docs: https://opensearch.org/docs/latest
# operationId: indices.flush.2
export def "flush indicesflush2" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --force: oneof<nothing, bool> # If `true`, the request forces a flush even if there are no changes to commit to the index.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --wait-if-ongoing: oneof<nothing, bool> # If `true`, the flush operation blocks until execution when another flush operation is running. If `false`, OpenSearch returns an error if you request a flush when another flush operation is running. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "force" $force "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "wait_if_ongoing" $wait_if_ongoing "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_flush" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Performs the flush operation on one or more indexes.
#
# POST /{index}/_flush
# Docs: https://opensearch.org/docs/latest
# operationId: indices.flush.3
export def "flush indicesflush3" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --force: oneof<nothing, bool> # If `true`, the request forces a flush even if there are no changes to commit to the index.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --wait-if-ongoing: oneof<nothing, bool> # If `true`, the flush operation blocks until execution when another flush operation is running. If `false`, OpenSearch returns an error if you request a flush when another flush operation is running. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "force" $force "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "wait_if_ongoing" $wait_if_ongoing "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_flush" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Performs the force merge operation on one or more indexes.
#
# POST /{index}/_forcemerge
# Docs: https://opensearch.org/docs/latest
# operationId: indices.forcemerge.1
export def "forcemerge indicesforcemerge1" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # Whether to ignore if a wildcard indexes expression resolves into no concrete indexes. (This includes `_all` string or when no indexes have been specified)
  --expand-wildcards: string # Whether to expand wildcard expression to concrete indexes that are open, closed or both.
  --flush: oneof<nothing, bool> # Specify whether the index should be flushed after performing the operation. (default: true)
  --ignore-unavailable: oneof<nothing, bool> # Whether specified concrete indexes should be ignored when unavailable (missing or closed)
  --max-num-segments: int # The number of larger segments into which smaller segments are merged. Set this parameter to 1 to merge all segments into one segment. The default behavior is to perform the merge as necessary. (format: int64)
  --only-expunge-deletes: oneof<nothing, bool> # Specify whether the operation should only expunge deleted documents
  --primary-only: oneof<nothing, bool> # Specify whether the operation should only perform on primary shards. Defaults to false. (default: false)
  --wait-for-completion: oneof<nothing, bool> # Should the request wait until the force merge is completed. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "flush" $flush "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "max_num_segments" $max_num_segments "scalar") (serialize-qp "only_expunge_deletes" $only_expunge_deletes "scalar") (serialize-qp "primary_only" $primary_only "scalar") (serialize-qp "wait_for_completion" $wait_for_completion "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_forcemerge" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns mappings for one or more indexes.
#
# GET /{index}/_mapping
# Docs: https://opensearch.org/docs/latest/field-types/index/#get-a-mapping
# operationId: indices.get_mapping.1
@deprecated --flag master-timeout
export def "mapping mapping1-by-index" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --index: string # Comma-separated list of data streams, indexes, and aliases used to limit the request. Supports wildcards (`*`). To target all data streams and indexes, omit this parameter or use `*` or `_all`.
  --local: oneof<nothing, bool> # If `true`, the request retrieves information from the local node only. (default: false)
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "index" $index "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_mapping" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Updates the index mappings.
#
# POST /{index}/_mapping
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/put-mapping/
# operationId: indices.put_mapping.0
# --_field_names shape: {enabled: bool}
# --_routing shape: {required: bool}
# --_source shape: {compress?: bool, compress_threshold?: string, enabled?: bool, excludes?: list, includes?: list}
@deprecated --flag master-timeout
export def "mapping mapping0-by-index" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --write-index-only: oneof<nothing, bool> # If `true`, the mappings are applied only to the current write index for the target. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --date-detection: oneof<nothing, bool> # Controls whether dynamic date detection is enabled.
  --dynamic: any
  --dynamic-date-formats: list # If date detection is enabled then new string fields are checked against `dynamic_date_formats` and if the value matches then a new date field is added instead of string.
  --dynamic-templates: any # Specify dynamic templates for the mapping.
  --field-names: record # shape: {enabled: bool}
  --meta: record # The custom metadata attached to a resource.
  --numeric-detection: oneof<nothing, bool> # Automatically map strings into numeric data types for all fields.
  --properties: record # Mapping for a field. For new fields, this mapping can include:  - Field name - Field data type - Mapping parameters
  --routing: record # shape: {required: bool}
  --body-source: record # shape: {compress?: bool, compress_threshold?: string, enabled?: bool, excludes?: list, includes?: list}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "write_index_only" $write_index_only "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_mapping" $qp)
  let body = {date_detection: $date_detection, dynamic: $dynamic, dynamic_date_formats: $dynamic_date_formats, dynamic_templates: $dynamic_templates, _field_names: $field_names, _meta: $meta, numeric_detection: $numeric_detection, properties: $properties, _routing: $routing, _source: $body_source} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Updates the index mappings.
#
# PUT /{index}/_mapping
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/put-mapping/
# operationId: indices.put_mapping.1
# --_field_names shape: {enabled: bool}
# --_routing shape: {required: bool}
# --_source shape: {compress?: bool, compress_threshold?: string, enabled?: bool, excludes?: list, includes?: list}
@deprecated --flag master-timeout
export def "mapping mapping1-by-index-1" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --write-index-only: oneof<nothing, bool> # If `true`, the mappings are applied only to the current write index for the target. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --date-detection: oneof<nothing, bool> # Controls whether dynamic date detection is enabled.
  --dynamic: any
  --dynamic-date-formats: list # If date detection is enabled then new string fields are checked against `dynamic_date_formats` and if the value matches then a new date field is added instead of string.
  --dynamic-templates: any # Specify dynamic templates for the mapping.
  --field-names: record # shape: {enabled: bool}
  --meta: record # The custom metadata attached to a resource.
  --numeric-detection: oneof<nothing, bool> # Automatically map strings into numeric data types for all fields.
  --properties: record # Mapping for a field. For new fields, this mapping can include:  - Field name - Field data type - Mapping parameters
  --routing: record # shape: {required: bool}
  --body-source: record # shape: {compress?: bool, compress_threshold?: string, enabled?: bool, excludes?: list, includes?: list}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "write_index_only" $write_index_only "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_mapping" $qp)
  let body = {date_detection: $date_detection, dynamic: $dynamic, dynamic_date_formats: $dynamic_date_formats, dynamic_templates: $dynamic_templates, _field_names: $field_names, _meta: $meta, numeric_detection: $numeric_detection, properties: $properties, _routing: $routing, _source: $body_source} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns mapping for one or more fields.
#
# GET /{index}/_mapping/field/{fields}
# Docs: https://opensearch.org/docs/latest/field-types/index/
# operationId: indices.get_field_mapping.1
export def "mapping-field mapping1" [
  fields: string
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --include-defaults: oneof<nothing, bool> # If `true`, return all default settings in the response.
  --local: oneof<nothing, bool> # If `true`, the request retrieves information from the local node only. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "include_defaults" $include_defaults "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_mapping/field/($fields)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Allows to get multiple documents in one request.
#
# GET /{index}/_mget
# Docs: https://opensearch.org/docs/latest/api-reference/document-apis/multi-get/
# operationId: mget.2
# --docs item shape: {_id: string, _index?: string, routing?: string, _source?: any, stored_fields?: any, version?: int, version_type?: any}
export def "mget mget2" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: string # Set to `true` or `false` to return the `_source` field or not, or a list of fields to return.
  --source-excludes: string # A comma-separated list of source fields to exclude from the response. You can also use this parameter to exclude fields from the subset specified in `_source_includes` query parameter.
  --source-includes: string # A comma-separated list of source fields to include in the response. If this parameter is specified, only these source fields are returned. You can exclude fields from this subset using the `_source_excludes` query parameter. If the `_source` parameter is `false`, this parameter is ignored.
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --realtime: oneof<nothing, bool> # If `true`, the request is real-time as opposed to near-real-time.
  --refresh: string # If `true`, the request refreshes relevant shards before retrieving documents.
  --routing: string # Custom value used to route operations to a specific shard.
  --stored-fields: string # If `true`, retrieves the document fields stored in the index rather than the document `_source`.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --docs: list # The documents you want to retrieve. Required if no index is specified in the request URI. — item shape: {_id: string, _index?: string, routing?: string, _source?: any, stored_fields?: any, version?: int, version_type?: any}
  --ids: any
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "scalar") (serialize-qp "_source_excludes" $source_excludes "scalar") (serialize-qp "_source_includes" $source_includes "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "realtime" $realtime "scalar") (serialize-qp "refresh" $refresh "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "stored_fields" $stored_fields "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_mget" $qp)
  let body = {docs: $docs, ids: $ids} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Allows to get multiple documents in one request.
#
# POST /{index}/_mget
# Docs: https://opensearch.org/docs/latest/api-reference/document-apis/multi-get/
# operationId: mget.3
# --docs item shape: {_id: string, _index?: string, routing?: string, _source?: any, stored_fields?: any, version?: int, version_type?: any}
export def "mget mget3" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: string # Set to `true` or `false` to return the `_source` field or not, or a list of fields to return.
  --source-excludes: string # A comma-separated list of source fields to exclude from the response. You can also use this parameter to exclude fields from the subset specified in `_source_includes` query parameter.
  --source-includes: string # A comma-separated list of source fields to include in the response. If this parameter is specified, only these source fields are returned. You can exclude fields from this subset using the `_source_excludes` query parameter. If the `_source` parameter is `false`, this parameter is ignored.
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --realtime: oneof<nothing, bool> # If `true`, the request is real-time as opposed to near-real-time.
  --refresh: string # If `true`, the request refreshes relevant shards before retrieving documents.
  --routing: string # Custom value used to route operations to a specific shard.
  --stored-fields: string # If `true`, retrieves the document fields stored in the index rather than the document `_source`.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --docs: list # The documents you want to retrieve. Required if no index is specified in the request URI. — item shape: {_id: string, _index?: string, routing?: string, _source?: any, stored_fields?: any, version?: int, version_type?: any}
  --ids: any
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "scalar") (serialize-qp "_source_excludes" $source_excludes "scalar") (serialize-qp "_source_includes" $source_includes "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "realtime" $realtime "scalar") (serialize-qp "refresh" $refresh "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "stored_fields" $stored_fields "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_mget" $qp)
  let body = {docs: $docs, ids: $ids} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Allows to execute several search operations in one request.
#
# GET /{index}/_msearch
# Docs: https://opensearch.org/docs/latest/api-reference/multi-search/
# operationId: msearch.2
export def "msearch msearch2" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --ccs-minimize-roundtrips: oneof<nothing, bool> # If `true`, network round-trips between the coordinating node and remote clusters are minimized for cross-cluster search requests. (default: true)
  --max-concurrent-searches: int # Maximum number of concurrent searches the multi search API can execute. (format: int32)
  --max-concurrent-shard-requests: int # Maximum number of concurrent shard requests that each sub-search request executes per node. (format: int32, default: 5)
  --pre-filter-shard-size: int # Defines a threshold that enforces a pre-filter roundtrip to prefilter search shards based on query rewriting if the number of shards the search request expands to exceeds the threshold. This filter roundtrip can limit the number of shards significantly if for instance a shard can not match any documents based on its rewrite method i.e., if date filters are mandatory to match but the shard bounds and the query are disjoint. (format: int32)
  --rest-total-hits-as-int: oneof<nothing, bool> # If `true`, `hits.total` are returned as an integer in the response. Defaults to false, which returns an object. (default: false)
  --search-type: string # Indicates whether global term and document frequencies should be used when scoring returned documents.
  --typed-keys: oneof<nothing, bool> # Specifies whether aggregation and suggester names should be prefixed by their respective types in the response.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "ccs_minimize_roundtrips" $ccs_minimize_roundtrips "scalar") (serialize-qp "max_concurrent_searches" $max_concurrent_searches "scalar") (serialize-qp "max_concurrent_shard_requests" $max_concurrent_shard_requests "scalar") (serialize-qp "pre_filter_shard_size" $pre_filter_shard_size "scalar") (serialize-qp "rest_total_hits_as_int" $rest_total_hits_as_int "scalar") (serialize-qp "search_type" $search_type "scalar") (serialize-qp "typed_keys" $typed_keys "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_msearch" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/x-ndjson" $body
}

# Allows to execute several search operations in one request.
#
# POST /{index}/_msearch
# Docs: https://opensearch.org/docs/latest/api-reference/multi-search/
# operationId: msearch.3
export def "msearch msearch3" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --ccs-minimize-roundtrips: oneof<nothing, bool> # If `true`, network round-trips between the coordinating node and remote clusters are minimized for cross-cluster search requests. (default: true)
  --max-concurrent-searches: int # Maximum number of concurrent searches the multi search API can execute. (format: int32)
  --max-concurrent-shard-requests: int # Maximum number of concurrent shard requests that each sub-search request executes per node. (format: int32, default: 5)
  --pre-filter-shard-size: int # Defines a threshold that enforces a pre-filter roundtrip to prefilter search shards based on query rewriting if the number of shards the search request expands to exceeds the threshold. This filter roundtrip can limit the number of shards significantly if for instance a shard can not match any documents based on its rewrite method i.e., if date filters are mandatory to match but the shard bounds and the query are disjoint. (format: int32)
  --rest-total-hits-as-int: oneof<nothing, bool> # If `true`, `hits.total` are returned as an integer in the response. Defaults to false, which returns an object. (default: false)
  --search-type: string # Indicates whether global term and document frequencies should be used when scoring returned documents.
  --typed-keys: oneof<nothing, bool> # Specifies whether aggregation and suggester names should be prefixed by their respective types in the response.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "ccs_minimize_roundtrips" $ccs_minimize_roundtrips "scalar") (serialize-qp "max_concurrent_searches" $max_concurrent_searches "scalar") (serialize-qp "max_concurrent_shard_requests" $max_concurrent_shard_requests "scalar") (serialize-qp "pre_filter_shard_size" $pre_filter_shard_size "scalar") (serialize-qp "rest_total_hits_as_int" $rest_total_hits_as_int "scalar") (serialize-qp "search_type" $search_type "scalar") (serialize-qp "typed_keys" $typed_keys "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_msearch" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/x-ndjson" $body
}

# Allows to execute several search template operations in one request.
#
# GET /{index}/_msearch/template
# Docs: https://opensearch.org/docs/latest/search-plugins/search-template/
# operationId: msearch_template.2
export def "msearch-template template2" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --ccs-minimize-roundtrips: oneof<nothing, bool> # If `true`, network round-trips are minimized for cross-cluster search requests. (default: true)
  --max-concurrent-searches: int # Maximum number of concurrent searches the API can run. (format: int32)
  --rest-total-hits-as-int: oneof<nothing, bool> # If `true`, the response returns `hits.total` as an integer. If `false`, it returns `hits.total` as an object. (default: false)
  --search-type: string # The type of the search operation. Available options: `query_then_fetch`, `dfs_query_then_fetch`.
  --typed-keys: oneof<nothing, bool> # If `true`, the response prefixes aggregation and suggester names with their respective types.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "ccs_minimize_roundtrips" $ccs_minimize_roundtrips "scalar") (serialize-qp "max_concurrent_searches" $max_concurrent_searches "scalar") (serialize-qp "rest_total_hits_as_int" $rest_total_hits_as_int "scalar") (serialize-qp "search_type" $search_type "scalar") (serialize-qp "typed_keys" $typed_keys "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_msearch/template" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/x-ndjson" $body
}

# Allows to execute several search template operations in one request.
#
# POST /{index}/_msearch/template
# Docs: https://opensearch.org/docs/latest/search-plugins/search-template/
# operationId: msearch_template.3
export def "msearch-template template3" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --ccs-minimize-roundtrips: oneof<nothing, bool> # If `true`, network round-trips are minimized for cross-cluster search requests. (default: true)
  --max-concurrent-searches: int # Maximum number of concurrent searches the API can run. (format: int32)
  --rest-total-hits-as-int: oneof<nothing, bool> # If `true`, the response returns `hits.total` as an integer. If `false`, it returns `hits.total` as an object. (default: false)
  --search-type: string # The type of the search operation. Available options: `query_then_fetch`, `dfs_query_then_fetch`.
  --typed-keys: oneof<nothing, bool> # If `true`, the response prefixes aggregation and suggester names with their respective types.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "ccs_minimize_roundtrips" $ccs_minimize_roundtrips "scalar") (serialize-qp "max_concurrent_searches" $max_concurrent_searches "scalar") (serialize-qp "rest_total_hits_as_int" $rest_total_hits_as_int "scalar") (serialize-qp "search_type" $search_type "scalar") (serialize-qp "typed_keys" $typed_keys "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_msearch/template" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/x-ndjson" $body
}

# Returns multiple termvectors in one request.
#
# GET /{index}/_mtermvectors
# Docs: https://opensearch.org/docs/latest
# operationId: mtermvectors.2
# --docs item shape: {_id: string, _index?: string, doc?: any, fields?: any, field_statistics?: bool, filter?: record, offsets?: bool, payloads?: bool, positions?: bool, routing?: string, term_statistics?: bool, version?: int, version_type?: any}
export def "mtermvectors mtermvectors2" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --field-statistics: oneof<nothing, bool> # If `true`, the response includes the document count, sum of document frequencies, and sum of total term frequencies. (default: true)
  --qp-fields: string # Comma-separated list or wildcard expressions of fields to include in the statistics. Used as the default list unless a specific field list is provided in the `completion_fields` or `fielddata_fields` parameters.
  --ids: list # A comma-separated list of documents ids. You must define ids as parameter or set "ids" or "docs" in the request body
  --offsets: oneof<nothing, bool> # If `true`, the response includes term offsets. (default: true)
  --payloads: oneof<nothing, bool> # If `true`, the response includes term payloads. (default: true)
  --positions: oneof<nothing, bool> # If `true`, the response includes term positions. (default: true)
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --realtime: oneof<nothing, bool> # If `true`, the request is real-time as opposed to near-real-time. (default: true)
  --routing: string # Custom value used to route operations to a specific shard.
  --term-statistics: oneof<nothing, bool> # If `true`, the response includes term frequency and document frequency. (default: false)
  --version: int # If `true`, returns the document version as part of a hit. (format: int64)
  --version-type: string # Specific version type.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --docs: list # Array of existing or artificial documents. — item shape: {_id: string, _index?: string, doc?: any, fields?: any, field_statistics?: bool, filter?: record, offsets?: bool, payloads?: bool, positions?: bool, routing?: string, term_statistics?: bool, version?: int, version_type?: any}
  --ids: list # Simplified syntax to specify documents by their ID if they're in the same index.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "field_statistics" $field_statistics "scalar") (serialize-qp "fields" $qp_fields "scalar") (serialize-qp "ids" $ids "multi") (serialize-qp "offsets" $offsets "scalar") (serialize-qp "payloads" $payloads "scalar") (serialize-qp "positions" $positions "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "realtime" $realtime "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "term_statistics" $term_statistics "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "version_type" $version_type "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_mtermvectors" $qp)
  let body = {docs: $docs, ids: $ids} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns multiple termvectors in one request.
#
# POST /{index}/_mtermvectors
# Docs: https://opensearch.org/docs/latest
# operationId: mtermvectors.3
# --docs item shape: {_id: string, _index?: string, doc?: any, fields?: any, field_statistics?: bool, filter?: record, offsets?: bool, payloads?: bool, positions?: bool, routing?: string, term_statistics?: bool, version?: int, version_type?: any}
export def "mtermvectors mtermvectors3" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --field-statistics: oneof<nothing, bool> # If `true`, the response includes the document count, sum of document frequencies, and sum of total term frequencies. (default: true)
  --qp-fields: string # Comma-separated list or wildcard expressions of fields to include in the statistics. Used as the default list unless a specific field list is provided in the `completion_fields` or `fielddata_fields` parameters.
  --ids: list # A comma-separated list of documents ids. You must define ids as parameter or set "ids" or "docs" in the request body
  --offsets: oneof<nothing, bool> # If `true`, the response includes term offsets. (default: true)
  --payloads: oneof<nothing, bool> # If `true`, the response includes term payloads. (default: true)
  --positions: oneof<nothing, bool> # If `true`, the response includes term positions. (default: true)
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --realtime: oneof<nothing, bool> # If `true`, the request is real-time as opposed to near-real-time. (default: true)
  --routing: string # Custom value used to route operations to a specific shard.
  --term-statistics: oneof<nothing, bool> # If `true`, the response includes term frequency and document frequency. (default: false)
  --version: int # If `true`, returns the document version as part of a hit. (format: int64)
  --version-type: string # Specific version type.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --docs: list # Array of existing or artificial documents. — item shape: {_id: string, _index?: string, doc?: any, fields?: any, field_statistics?: bool, filter?: record, offsets?: bool, payloads?: bool, positions?: bool, routing?: string, term_statistics?: bool, version?: int, version_type?: any}
  --ids: list # Simplified syntax to specify documents by their ID if they're in the same index.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "field_statistics" $field_statistics "scalar") (serialize-qp "fields" $qp_fields "scalar") (serialize-qp "ids" $ids "multi") (serialize-qp "offsets" $offsets "scalar") (serialize-qp "payloads" $payloads "scalar") (serialize-qp "positions" $positions "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "realtime" $realtime "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "term_statistics" $term_statistics "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "version_type" $version_type "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_mtermvectors" $qp)
  let body = {docs: $docs, ids: $ids} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Opens an index.
#
# POST /{index}/_open
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/open-index/
# operationId: indices.open.0
@deprecated --flag master-timeout
export def "open indicesopen0" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --task-execution-timeout: string # Explicit task execution timeout, only useful when `wait_for_completion` is false, defaults to `1h`.
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to `all` or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --wait-for-completion: oneof<nothing, bool> # Should this request wait until the operation has completed before returning. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "task_execution_timeout" $task_execution_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "wait_for_completion" $wait_for_completion "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_open" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Allows to evaluate the quality of ranked search results over a set of typical search queries.
#
# GET /{index}/_rank_eval
# Docs: https://opensearch.org/docs/latest/api-reference/rank-eval/
# operationId: rank_eval.2
# --requests item shape: {id: string, request?: record, ratings: list, template_id?: string, params?: record}
# --metric shape: {precision?: any, recall?: any, mean_reciprocal_rank?: any, dcg?: any, expected_reciprocal_rank?: any}
export def "rank-eval eval2" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with `foo` but no index starts with `bar`.
  --expand-wildcards: string # Whether to expand wildcard expression to concrete indexes that are open, closed or both.
  --ignore-unavailable: oneof<nothing, bool> # If `true`, missing or closed indexes are not included in the response.
  --search-type: string # Search operation type
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  requests: list # A set of typical search requests, together with their provided ratings. — item shape: {id: string, request?: record, ratings: list, template_id?: string, params?: record}
  --metric: record # shape: {precision?: any, recall?: any, mean_reciprocal_rank?: any, dcg?: any, expected_reciprocal_rank?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "search_type" $search_type "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_rank_eval" $qp)
  let body = {requests: $requests, metric: $metric} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Allows to evaluate the quality of ranked search results over a set of typical search queries.
#
# POST /{index}/_rank_eval
# Docs: https://opensearch.org/docs/latest/api-reference/rank-eval/
# operationId: rank_eval.3
# --requests item shape: {id: string, request?: record, ratings: list, template_id?: string, params?: record}
# --metric shape: {precision?: any, recall?: any, mean_reciprocal_rank?: any, dcg?: any, expected_reciprocal_rank?: any}
export def "rank-eval eval3" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with `foo` but no index starts with `bar`.
  --expand-wildcards: string # Whether to expand wildcard expression to concrete indexes that are open, closed or both.
  --ignore-unavailable: oneof<nothing, bool> # If `true`, missing or closed indexes are not included in the response.
  --search-type: string # Search operation type
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  requests: list # A set of typical search requests, together with their provided ratings. — item shape: {id: string, request?: record, ratings: list, template_id?: string, params?: record}
  --metric: record # shape: {precision?: any, recall?: any, mean_reciprocal_rank?: any, dcg?: any, expected_reciprocal_rank?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "search_type" $search_type "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_rank_eval" $qp)
  let body = {requests: $requests, metric: $metric} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns information about ongoing index shard recoveries.
#
# GET /{index}/_recovery
# Docs: https://opensearch.org/docs/latest
# operationId: indices.recovery.1
export def "recovery indicesrecovery1" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --active-only: oneof<nothing, bool> # If `true`, the response only includes ongoing shard recoveries. (default: false)
  --detailed: oneof<nothing, bool> # If `true`, the response includes detailed information about shard recoveries. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "active_only" $active_only "scalar") (serialize-qp "detailed" $detailed "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_recovery" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Performs the refresh operation in one or more indexes.
#
# GET /{index}/_refresh
# Docs: https://opensearch.org/docs/latest/tuning-your-cluster/availability-and-recovery/remote-store/index/#refresh-level-and-request-level-durability
# operationId: indices.refresh.2
export def "refresh indicesrefresh2" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_refresh" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Performs the refresh operation in one or more indexes.
#
# POST /{index}/_refresh
# Docs: https://opensearch.org/docs/latest/tuning-your-cluster/availability-and-recovery/remote-store/index/#refresh-level-and-request-level-durability
# operationId: indices.refresh.3
export def "refresh indicesrefresh3" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_refresh" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns results matching a query.
#
# GET /{index}/_search
# Docs: https://opensearch.org/docs/latest/api-reference/search/
# operationId: search.2
# --collapse shape: {field: string, inner_hits?: any, max_concurrent_group_searches?: int}
# --rank shape: {rrf?: any}
# --post_filter shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
# --query shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
# --slice shape: {field?: string, id: int, max: int}
# --suggest shape: {text?: string}
# --pit shape: {id: string, keep_alive?: string}
export def "search search2" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: string # Indicates which source fields are returned for matching documents. These fields are returned in the `hits._source` property of the search response. Valid values are: `true` to return the entire document source; `false` to not return the document source; `<string>` to return the source fields that are specified as a comma-separated list (supports wildcard (`*`) patterns).
  --source-excludes: string # A comma-separated list of source fields to exclude from the response. You can also use this parameter to exclude fields from the subset specified in `_source_includes` query parameter. If the `_source` parameter is `false`, this parameter is ignored.
  --source-includes: string # A comma-separated list of source fields to include in the response. If this parameter is specified, only these source fields are returned. You can exclude fields from this subset using the `_source_excludes` query parameter. If the `_source` parameter is `false`, this parameter is ignored.
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with `foo` but no index starts with `bar`.
  --allow-partial-search-results: oneof<nothing, bool> # If `true`, returns partial results if there are shard request timeouts or shard failures. If `false`, returns an error with no partial results. (default: true)
  --analyze-wildcard: oneof<nothing, bool> # If `true`, wildcard and prefix queries are analyzed. This parameter can only be used when the q query string parameter is specified. (default: false)
  --analyzer: string # Analyzer to use for the query string. This parameter can only be used when the q query string parameter is specified.
  --batched-reduce-size: int # The number of shard results that should be reduced at once on the coordinating node. This value should be used as a protection mechanism to reduce the memory overhead per search request if the potential number of shards in the request can be large. (format: int32, default: 512)
  --cancel-after-time-interval: string # The time after which the search request will be canceled. Request-level parameter takes precedence over `cancel_after_time_interval` cluster setting.
  --ccs-minimize-roundtrips: oneof<nothing, bool> # If `true`, network round-trips between the coordinating node and the remote clusters are minimized when executing cross-cluster search (CCS) requests. (default: true)
  --default-operator: string@default-operator-completer # The default operator for query string query: AND or OR. This parameter can only be used when the `q` query string parameter is specified.
  --df: string # Field to use as default where no field prefix is given in the query string. This parameter can only be used when the q query string parameter is specified.
  --docvalue-fields: string # A comma-separated list of fields to return as the docvalue representation for each hit.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`.
  --explain: oneof<nothing, bool> # If `true`, returns detailed information about score computation as part of a hit.
  --qp-from: int # Starting document offset. Needs to be non-negative. By default, you cannot page through more than 10,000 hits using the `from` and `size` parameters. To page through more hits, use the `search_after` parameter. (format: int32, default: 0)
  --ignore-throttled: oneof<nothing, bool> # If `true`, concrete, expanded or aliased indexes will be ignored when frozen.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --include-named-queries-score: oneof<nothing, bool> # Indicates whether `hit.matched_queries` should be rendered as a map that includes the name of the matched query associated with its score (true) or as an array containing the name of the matched queries (false) (default: false)
  --lenient: oneof<nothing, bool> # If `true`, format-based query failures (such as providing text to a numeric field) in the query string will be ignored. This parameter can only be used when the `q` query string parameter is specified.
  --max-concurrent-shard-requests: int # Defines the number of concurrent shard requests per node this search executes concurrently. This value should be used to limit the impact of the search on the cluster in order to limit the number of concurrent shard requests. (format: int32, default: 5)
  --phase-took: oneof<nothing, bool> # Indicates whether to return phase-level `took` time values in the response. (default: false)
  --pre-filter-shard-size: int # Defines a threshold that enforces a pre-filter roundtrip to prefilter search shards based on query rewriting if the number of shards the search request expands to exceeds the threshold. This filter roundtrip can limit the number of shards significantly if for instance a shard can not match any documents based on its rewrite method (if date filters are mandatory to match but the shard bounds and the query are disjoint). When unspecified, the pre-filter phase is executed if any of these conditions is met: the request targets more than 128 shards; the request targets one or more read-only index; the primary sort of the query targets an indexed field. (format: int32)
  --preference: string # Nodes and shards used for the search. By default, OpenSearch selects from eligible nodes and shards using adaptive replica selection, accounting for allocation awareness. Valid values are: `_only_local` to run the search only on shards on the local node; `_local` to, if possible, run the search on shards on the local node, or if not, select shards using the default method; `_only_nodes:<node-id>,<node-id>` to run the search on only the specified nodes IDs, where, if suitable shards exist on more than one selected node, use shards on those nodes using the default method, or if none of the specified nodes are available, select shards from any available node using the default method; `_prefer_nodes:<node-id>,<node-id>` to if possible, run the search on the specified nodes IDs, or if not, select shards using the default method; `_shards:<shard>,<shard>` to run the search only on the specified shards; `<custom-string>` (any string that does not start with `_`) to route searches with the same `<custom-string>` to the same shards in the same order. (default: random)
  --q: string # Query in the Lucene query string syntax using query parameter search. Query parameter searches do not support the full OpenSearch Query DSL but are handy for testing.
  --request-cache: oneof<nothing, bool> # If `true`, the caching of search results is enabled for requests where `size` is `0`. Defaults to index level settings.
  --rest-total-hits-as-int: oneof<nothing, bool> # Indicates whether `hits.total` should be rendered as an integer or an object in the rest search response. (default: false)
  --routing: string # Custom value used to route operations to a specific shard.
  --scroll: string # Period to retain the search context for scrolling. See Scroll search results. By default, this value cannot exceed `1d` (24 hours). You can change this limit using the `search.max_keep_alive` cluster-level setting.
  --search-pipeline: string # Customizable sequence of processing stages applied to search queries.
  --search-type: string # How distributed term frequencies are calculated for relevance scoring.
  --seq-no-primary-term: oneof<nothing, bool> # If `true`, returns sequence number and primary term of the last modification of each hit.
  --size: int # Defines the number of hits to return. By default, you cannot page through more than 10,000 hits using the `from` and `size` parameters. To page through more hits, use the `search_after` parameter. (format: int32, default: 10)
  --qp-sort: string # A comma-separated list of <field>:<direction> pairs.
  --stats: list # Specific `tag` of the request for logging and statistical purposes.
  --stored-fields: string # A comma-separated list of stored fields to return as part of a hit. If no fields are specified, no stored fields are included in the response. If this field is specified, the `_source` parameter defaults to `false`. You can pass `_source: true` to return both source fields and stored fields in the search response.
  --suggest-field: string # Specifies which field to use for suggestions.
  --suggest-mode: string@suggest-mode-completer # Specifies the suggest mode. This parameter can only be used when the `suggest_field` and `suggest_text` query string parameters are specified.
  --suggest-size: int # Number of suggestions to return. This parameter can only be used when the `suggest_field` and `suggest_text` query string parameters are specified. (format: int32)
  --suggest-text: string # The source text for which the suggestions should be returned. This parameter can only be used when the `suggest_field` and `suggest_text` query string parameters are specified.
  --terminate-after: int # Maximum number of documents to collect for each shard. If a query reaches this limit, OpenSearch terminates the query early. OpenSearch collects documents before sorting. Use with caution. OpenSearch applies this parameter to each shard handling the request. When possible, let OpenSearch perform early termination automatically. Avoid specifying this parameter for requests that target data streams with backing indexes across multiple data tiers. If set to `0` (default), the query does not terminate early. (format: int32)
  --timeout: string # Specifies the period of time to wait for a response from each shard. If no response is received before the timeout expires, the request fails and returns an error.
  --track-scores: oneof<nothing, bool> # If `true`, calculate and return document scores, even if the scores are not used for sorting.
  --track-total-hits: string # Number of hits matching the query to count accurately. If `true`, the exact number of hits is returned at the cost of some performance. If `false`, the response does not include the total number of hits matching the query.
  --typed-keys: oneof<nothing, bool> # If `true`, aggregation and suggester names are be prefixed by their respective types in the response.
  --verbose-pipeline: oneof<nothing, bool> # Enables or disables verbose mode for the search pipeline. When verbose mode is enabled, detailed information about each processor in the search pipeline is included in the search response. This includes the processor name, execution status, input, output, and time taken for processing. This parameter is primarily intended for debugging purposes, allowing users to track how data flows and transforms through the search pipeline.
  --version: oneof<nothing, bool> # If `true`, returns document version as part of a hit.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --aggregations: record # Defines the aggregations that are run as part of the search request.
  --aggs: record # Defines the aggregations that are run as part of the search request.
  --collapse: record # shape: {field: string, inner_hits?: any, max_concurrent_group_searches?: int}
  --explain: oneof<nothing, bool> # If `true`, returns detailed information about score computation as part of a hit.
  --ext: record # Configuration of search extensions defined by OpenSearch plugins.
  --body-from: int # Starting document offset. Needs to be non-negative. By default, you cannot page through more than 10,000 hits using the `from` and `size` parameters. To page through more hits, use the `search_after` parameter. (format: int32)
  --highlight: any
  --track-total-hits: any # The number of hits matching the query. When `true`, the exact number of hits is returned at the cost of some performance. When `false`, the response does not include the total number of hits matching the query. Default is `10,000` hits.
  --indices-boost: list # Boosts the `_score` of documents from specified indexes.
  --docvalue-fields: list # Array of wildcard (`*`) patterns. The request returns doc values for field names matching these patterns in the `hits.fields` property of the response.
  --rank: record # The container for ranking configuration. — shape: {rrf?: any}
  --min-score: float # Minimum `_score` for matching documents. Documents with a lower `_score` are not included in the search results. (format: float)
  --post-filter: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
  --profile: oneof<nothing, bool> # Set to `true` to return detailed timing information about the execution of individual components in a search request. NOTE: This is a debugging tool and adds significant overhead to search execution.
  --body-query: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
  --rescore: any # Can be used to improve precision by reordering just the top (for example 100 - 500) documents returned by the `query` and `post_filter` phases.
  --script-fields: record # Retrieve a script evaluation (based on different fields) for each hit.
  --search-after: list
  --size: int # The number of hits to return. By default, you cannot page through more than 10,000 hits using the `from` and `size` parameters. To page through more hits, use the `search_after` parameter. (format: int32)
  --slice: record # The configuration for a sliced scroll request. — shape: {field?: string, id: int, max: int}
  --body-sort: any
  --body-source: any # Defines how to fetch a source. Fetching can be disabled entirely, or the source can be filtered.
  --body-fields: list # Array of wildcard (`*`) patterns. The request returns values for field names matching these patterns in the `hits.fields` property of the response.
  --suggest: record # shape: {text?: string}
  --terminate-after: int # Maximum number of documents to collect for each shard. If a query reaches this limit, OpenSearch terminates the query early. OpenSearch collects documents before sorting. Use with caution. OpenSearch applies this parameter to each shard handling the request. When possible, let OpenSearch perform early termination automatically. Avoid specifying this parameter for requests that target data streams with backing indexes across multiple data tiers. If set to `0` (default), the query does not terminate early. (format: int32)
  --timeout: string # Specifies the period of time to wait for a response from each shard. If no response is received before the timeout expires, the request fails and returns an error. Defaults to no timeout.
  --track-scores: oneof<nothing, bool> # If `true`, calculate and return document scores, even if the scores are not used for sorting.
  --version: oneof<nothing, bool> # If `true`, returns document version as part of a hit.
  --seq-no-primary-term: oneof<nothing, bool> # If `true`, returns sequence number and primary term of the last modification of each hit.
  --stored-fields: any
  --pit: record # shape: {id: string, keep_alive?: string}
  --stats: list # Stats groups to associate with the search. Each group maintains a statistics aggregation for its associated searches. You can retrieve these stats using the indexes stats API.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "scalar") (serialize-qp "_source_excludes" $source_excludes "scalar") (serialize-qp "_source_includes" $source_includes "scalar") (serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "allow_partial_search_results" $allow_partial_search_results "scalar") (serialize-qp "analyze_wildcard" $analyze_wildcard "scalar") (serialize-qp "analyzer" $analyzer "scalar") (serialize-qp "batched_reduce_size" $batched_reduce_size "scalar") (serialize-qp "cancel_after_time_interval" $cancel_after_time_interval "scalar") (serialize-qp "ccs_minimize_roundtrips" $ccs_minimize_roundtrips "scalar") (serialize-qp "default_operator" $default_operator "scalar") (serialize-qp "df" $df "scalar") (serialize-qp "docvalue_fields" $docvalue_fields "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "explain" $explain "scalar") (serialize-qp "from" $qp_from "scalar") (serialize-qp "ignore_throttled" $ignore_throttled "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "include_named_queries_score" $include_named_queries_score "scalar") (serialize-qp "lenient" $lenient "scalar") (serialize-qp "max_concurrent_shard_requests" $max_concurrent_shard_requests "scalar") (serialize-qp "phase_took" $phase_took "scalar") (serialize-qp "pre_filter_shard_size" $pre_filter_shard_size "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "request_cache" $request_cache "scalar") (serialize-qp "rest_total_hits_as_int" $rest_total_hits_as_int "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "scroll" $scroll "scalar") (serialize-qp "search_pipeline" $search_pipeline "scalar") (serialize-qp "search_type" $search_type "scalar") (serialize-qp "seq_no_primary_term" $seq_no_primary_term "scalar") (serialize-qp "size" $size "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "stats" $stats "multi") (serialize-qp "stored_fields" $stored_fields "scalar") (serialize-qp "suggest_field" $suggest_field "scalar") (serialize-qp "suggest_mode" $suggest_mode "scalar") (serialize-qp "suggest_size" $suggest_size "scalar") (serialize-qp "suggest_text" $suggest_text "scalar") (serialize-qp "terminate_after" $terminate_after "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "track_scores" $track_scores "scalar") (serialize-qp "track_total_hits" $track_total_hits "scalar") (serialize-qp "typed_keys" $typed_keys "scalar") (serialize-qp "verbose_pipeline" $verbose_pipeline "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_search" $qp)
  let body = {aggregations: $aggregations, aggs: $aggs, collapse: $collapse, explain: $explain, ext: $ext, from: $body_from, highlight: $highlight, track_total_hits: $track_total_hits, indices_boost: $indices_boost, docvalue_fields: $docvalue_fields, rank: $rank, min_score: $min_score, post_filter: $post_filter, profile: $profile, query: $body_query, rescore: $rescore, script_fields: $script_fields, search_after: $search_after, size: $size, slice: $slice, sort: $body_sort, _source: $body_source, fields: $body_fields, suggest: $suggest, terminate_after: $terminate_after, timeout: $timeout, track_scores: $track_scores, version: $version, seq_no_primary_term: $seq_no_primary_term, stored_fields: $stored_fields, pit: $pit, stats: $stats} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns results matching a query.
#
# POST /{index}/_search
# Docs: https://opensearch.org/docs/latest/api-reference/search/
# operationId: search.3
# --collapse shape: {field: string, inner_hits?: any, max_concurrent_group_searches?: int}
# --rank shape: {rrf?: any}
# --post_filter shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
# --query shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
# --slice shape: {field?: string, id: int, max: int}
# --suggest shape: {text?: string}
# --pit shape: {id: string, keep_alive?: string}
export def "search search3" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: string # Indicates which source fields are returned for matching documents. These fields are returned in the `hits._source` property of the search response. Valid values are: `true` to return the entire document source; `false` to not return the document source; `<string>` to return the source fields that are specified as a comma-separated list (supports wildcard (`*`) patterns).
  --source-excludes: string # A comma-separated list of source fields to exclude from the response. You can also use this parameter to exclude fields from the subset specified in `_source_includes` query parameter. If the `_source` parameter is `false`, this parameter is ignored.
  --source-includes: string # A comma-separated list of source fields to include in the response. If this parameter is specified, only these source fields are returned. You can exclude fields from this subset using the `_source_excludes` query parameter. If the `_source` parameter is `false`, this parameter is ignored.
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with `foo` but no index starts with `bar`.
  --allow-partial-search-results: oneof<nothing, bool> # If `true`, returns partial results if there are shard request timeouts or shard failures. If `false`, returns an error with no partial results. (default: true)
  --analyze-wildcard: oneof<nothing, bool> # If `true`, wildcard and prefix queries are analyzed. This parameter can only be used when the q query string parameter is specified. (default: false)
  --analyzer: string # Analyzer to use for the query string. This parameter can only be used when the q query string parameter is specified.
  --batched-reduce-size: int # The number of shard results that should be reduced at once on the coordinating node. This value should be used as a protection mechanism to reduce the memory overhead per search request if the potential number of shards in the request can be large. (format: int32, default: 512)
  --cancel-after-time-interval: string # The time after which the search request will be canceled. Request-level parameter takes precedence over `cancel_after_time_interval` cluster setting.
  --ccs-minimize-roundtrips: oneof<nothing, bool> # If `true`, network round-trips between the coordinating node and the remote clusters are minimized when executing cross-cluster search (CCS) requests. (default: true)
  --default-operator: string@default-operator-completer # The default operator for query string query: AND or OR. This parameter can only be used when the `q` query string parameter is specified.
  --df: string # Field to use as default where no field prefix is given in the query string. This parameter can only be used when the q query string parameter is specified.
  --docvalue-fields: string # A comma-separated list of fields to return as the docvalue representation for each hit.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`.
  --explain: oneof<nothing, bool> # If `true`, returns detailed information about score computation as part of a hit.
  --qp-from: int # Starting document offset. Needs to be non-negative. By default, you cannot page through more than 10,000 hits using the `from` and `size` parameters. To page through more hits, use the `search_after` parameter. (format: int32, default: 0)
  --ignore-throttled: oneof<nothing, bool> # If `true`, concrete, expanded or aliased indexes will be ignored when frozen.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --include-named-queries-score: oneof<nothing, bool> # Indicates whether `hit.matched_queries` should be rendered as a map that includes the name of the matched query associated with its score (true) or as an array containing the name of the matched queries (false) (default: false)
  --lenient: oneof<nothing, bool> # If `true`, format-based query failures (such as providing text to a numeric field) in the query string will be ignored. This parameter can only be used when the `q` query string parameter is specified.
  --max-concurrent-shard-requests: int # Defines the number of concurrent shard requests per node this search executes concurrently. This value should be used to limit the impact of the search on the cluster in order to limit the number of concurrent shard requests. (format: int32, default: 5)
  --phase-took: oneof<nothing, bool> # Indicates whether to return phase-level `took` time values in the response. (default: false)
  --pre-filter-shard-size: int # Defines a threshold that enforces a pre-filter roundtrip to prefilter search shards based on query rewriting if the number of shards the search request expands to exceeds the threshold. This filter roundtrip can limit the number of shards significantly if for instance a shard can not match any documents based on its rewrite method (if date filters are mandatory to match but the shard bounds and the query are disjoint). When unspecified, the pre-filter phase is executed if any of these conditions is met: the request targets more than 128 shards; the request targets one or more read-only index; the primary sort of the query targets an indexed field. (format: int32)
  --preference: string # Nodes and shards used for the search. By default, OpenSearch selects from eligible nodes and shards using adaptive replica selection, accounting for allocation awareness. Valid values are: `_only_local` to run the search only on shards on the local node; `_local` to, if possible, run the search on shards on the local node, or if not, select shards using the default method; `_only_nodes:<node-id>,<node-id>` to run the search on only the specified nodes IDs, where, if suitable shards exist on more than one selected node, use shards on those nodes using the default method, or if none of the specified nodes are available, select shards from any available node using the default method; `_prefer_nodes:<node-id>,<node-id>` to if possible, run the search on the specified nodes IDs, or if not, select shards using the default method; `_shards:<shard>,<shard>` to run the search only on the specified shards; `<custom-string>` (any string that does not start with `_`) to route searches with the same `<custom-string>` to the same shards in the same order. (default: random)
  --q: string # Query in the Lucene query string syntax using query parameter search. Query parameter searches do not support the full OpenSearch Query DSL but are handy for testing.
  --request-cache: oneof<nothing, bool> # If `true`, the caching of search results is enabled for requests where `size` is `0`. Defaults to index level settings.
  --rest-total-hits-as-int: oneof<nothing, bool> # Indicates whether `hits.total` should be rendered as an integer or an object in the rest search response. (default: false)
  --routing: string # Custom value used to route operations to a specific shard.
  --scroll: string # Period to retain the search context for scrolling. See Scroll search results. By default, this value cannot exceed `1d` (24 hours). You can change this limit using the `search.max_keep_alive` cluster-level setting.
  --search-pipeline: string # Customizable sequence of processing stages applied to search queries.
  --search-type: string # How distributed term frequencies are calculated for relevance scoring.
  --seq-no-primary-term: oneof<nothing, bool> # If `true`, returns sequence number and primary term of the last modification of each hit.
  --size: int # Defines the number of hits to return. By default, you cannot page through more than 10,000 hits using the `from` and `size` parameters. To page through more hits, use the `search_after` parameter. (format: int32, default: 10)
  --qp-sort: string # A comma-separated list of <field>:<direction> pairs.
  --stats: list # Specific `tag` of the request for logging and statistical purposes.
  --stored-fields: string # A comma-separated list of stored fields to return as part of a hit. If no fields are specified, no stored fields are included in the response. If this field is specified, the `_source` parameter defaults to `false`. You can pass `_source: true` to return both source fields and stored fields in the search response.
  --suggest-field: string # Specifies which field to use for suggestions.
  --suggest-mode: string@suggest-mode-completer # Specifies the suggest mode. This parameter can only be used when the `suggest_field` and `suggest_text` query string parameters are specified.
  --suggest-size: int # Number of suggestions to return. This parameter can only be used when the `suggest_field` and `suggest_text` query string parameters are specified. (format: int32)
  --suggest-text: string # The source text for which the suggestions should be returned. This parameter can only be used when the `suggest_field` and `suggest_text` query string parameters are specified.
  --terminate-after: int # Maximum number of documents to collect for each shard. If a query reaches this limit, OpenSearch terminates the query early. OpenSearch collects documents before sorting. Use with caution. OpenSearch applies this parameter to each shard handling the request. When possible, let OpenSearch perform early termination automatically. Avoid specifying this parameter for requests that target data streams with backing indexes across multiple data tiers. If set to `0` (default), the query does not terminate early. (format: int32)
  --timeout: string # Specifies the period of time to wait for a response from each shard. If no response is received before the timeout expires, the request fails and returns an error.
  --track-scores: oneof<nothing, bool> # If `true`, calculate and return document scores, even if the scores are not used for sorting.
  --track-total-hits: string # Number of hits matching the query to count accurately. If `true`, the exact number of hits is returned at the cost of some performance. If `false`, the response does not include the total number of hits matching the query.
  --typed-keys: oneof<nothing, bool> # If `true`, aggregation and suggester names are be prefixed by their respective types in the response.
  --verbose-pipeline: oneof<nothing, bool> # Enables or disables verbose mode for the search pipeline. When verbose mode is enabled, detailed information about each processor in the search pipeline is included in the search response. This includes the processor name, execution status, input, output, and time taken for processing. This parameter is primarily intended for debugging purposes, allowing users to track how data flows and transforms through the search pipeline.
  --version: oneof<nothing, bool> # If `true`, returns document version as part of a hit.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --aggregations: record # Defines the aggregations that are run as part of the search request.
  --aggs: record # Defines the aggregations that are run as part of the search request.
  --collapse: record # shape: {field: string, inner_hits?: any, max_concurrent_group_searches?: int}
  --explain: oneof<nothing, bool> # If `true`, returns detailed information about score computation as part of a hit.
  --ext: record # Configuration of search extensions defined by OpenSearch plugins.
  --body-from: int # Starting document offset. Needs to be non-negative. By default, you cannot page through more than 10,000 hits using the `from` and `size` parameters. To page through more hits, use the `search_after` parameter. (format: int32)
  --highlight: any
  --track-total-hits: any # The number of hits matching the query. When `true`, the exact number of hits is returned at the cost of some performance. When `false`, the response does not include the total number of hits matching the query. Default is `10,000` hits.
  --indices-boost: list # Boosts the `_score` of documents from specified indexes.
  --docvalue-fields: list # Array of wildcard (`*`) patterns. The request returns doc values for field names matching these patterns in the `hits.fields` property of the response.
  --rank: record # The container for ranking configuration. — shape: {rrf?: any}
  --min-score: float # Minimum `_score` for matching documents. Documents with a lower `_score` are not included in the search results. (format: float)
  --post-filter: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
  --profile: oneof<nothing, bool> # Set to `true` to return detailed timing information about the execution of individual components in a search request. NOTE: This is a debugging tool and adds significant overhead to search execution.
  --body-query: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
  --rescore: any # Can be used to improve precision by reordering just the top (for example 100 - 500) documents returned by the `query` and `post_filter` phases.
  --script-fields: record # Retrieve a script evaluation (based on different fields) for each hit.
  --search-after: list
  --size: int # The number of hits to return. By default, you cannot page through more than 10,000 hits using the `from` and `size` parameters. To page through more hits, use the `search_after` parameter. (format: int32)
  --slice: record # The configuration for a sliced scroll request. — shape: {field?: string, id: int, max: int}
  --body-sort: any
  --body-source: any # Defines how to fetch a source. Fetching can be disabled entirely, or the source can be filtered.
  --body-fields: list # Array of wildcard (`*`) patterns. The request returns values for field names matching these patterns in the `hits.fields` property of the response.
  --suggest: record # shape: {text?: string}
  --terminate-after: int # Maximum number of documents to collect for each shard. If a query reaches this limit, OpenSearch terminates the query early. OpenSearch collects documents before sorting. Use with caution. OpenSearch applies this parameter to each shard handling the request. When possible, let OpenSearch perform early termination automatically. Avoid specifying this parameter for requests that target data streams with backing indexes across multiple data tiers. If set to `0` (default), the query does not terminate early. (format: int32)
  --timeout: string # Specifies the period of time to wait for a response from each shard. If no response is received before the timeout expires, the request fails and returns an error. Defaults to no timeout.
  --track-scores: oneof<nothing, bool> # If `true`, calculate and return document scores, even if the scores are not used for sorting.
  --version: oneof<nothing, bool> # If `true`, returns document version as part of a hit.
  --seq-no-primary-term: oneof<nothing, bool> # If `true`, returns sequence number and primary term of the last modification of each hit.
  --stored-fields: any
  --pit: record # shape: {id: string, keep_alive?: string}
  --stats: list # Stats groups to associate with the search. Each group maintains a statistics aggregation for its associated searches. You can retrieve these stats using the indexes stats API.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "scalar") (serialize-qp "_source_excludes" $source_excludes "scalar") (serialize-qp "_source_includes" $source_includes "scalar") (serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "allow_partial_search_results" $allow_partial_search_results "scalar") (serialize-qp "analyze_wildcard" $analyze_wildcard "scalar") (serialize-qp "analyzer" $analyzer "scalar") (serialize-qp "batched_reduce_size" $batched_reduce_size "scalar") (serialize-qp "cancel_after_time_interval" $cancel_after_time_interval "scalar") (serialize-qp "ccs_minimize_roundtrips" $ccs_minimize_roundtrips "scalar") (serialize-qp "default_operator" $default_operator "scalar") (serialize-qp "df" $df "scalar") (serialize-qp "docvalue_fields" $docvalue_fields "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "explain" $explain "scalar") (serialize-qp "from" $qp_from "scalar") (serialize-qp "ignore_throttled" $ignore_throttled "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "include_named_queries_score" $include_named_queries_score "scalar") (serialize-qp "lenient" $lenient "scalar") (serialize-qp "max_concurrent_shard_requests" $max_concurrent_shard_requests "scalar") (serialize-qp "phase_took" $phase_took "scalar") (serialize-qp "pre_filter_shard_size" $pre_filter_shard_size "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "request_cache" $request_cache "scalar") (serialize-qp "rest_total_hits_as_int" $rest_total_hits_as_int "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "scroll" $scroll "scalar") (serialize-qp "search_pipeline" $search_pipeline "scalar") (serialize-qp "search_type" $search_type "scalar") (serialize-qp "seq_no_primary_term" $seq_no_primary_term "scalar") (serialize-qp "size" $size "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "stats" $stats "multi") (serialize-qp "stored_fields" $stored_fields "scalar") (serialize-qp "suggest_field" $suggest_field "scalar") (serialize-qp "suggest_mode" $suggest_mode "scalar") (serialize-qp "suggest_size" $suggest_size "scalar") (serialize-qp "suggest_text" $suggest_text "scalar") (serialize-qp "terminate_after" $terminate_after "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "track_scores" $track_scores "scalar") (serialize-qp "track_total_hits" $track_total_hits "scalar") (serialize-qp "typed_keys" $typed_keys "scalar") (serialize-qp "verbose_pipeline" $verbose_pipeline "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_search" $qp)
  let body = {aggregations: $aggregations, aggs: $aggs, collapse: $collapse, explain: $explain, ext: $ext, from: $body_from, highlight: $highlight, track_total_hits: $track_total_hits, indices_boost: $indices_boost, docvalue_fields: $docvalue_fields, rank: $rank, min_score: $min_score, post_filter: $post_filter, profile: $profile, query: $body_query, rescore: $rescore, script_fields: $script_fields, search_after: $search_after, size: $size, slice: $slice, sort: $body_sort, _source: $body_source, fields: $body_fields, suggest: $suggest, terminate_after: $terminate_after, timeout: $timeout, track_scores: $track_scores, version: $version, seq_no_primary_term: $seq_no_primary_term, stored_fields: $stored_fields, pit: $pit, stats: $stats} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns information about the indexes and shards that a search request would be executed against.
#
# GET /{index}/_search_shards
# Docs: https://opensearch.org/docs/latest
# operationId: search_shards.2
export def "search-shards shards2" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with `foo` but no index starts with `bar`.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --local: oneof<nothing, bool> # If `true`, the request retrieves information from the local node only. (default: false)
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --routing: string # Custom value used to route operations to a specific shard.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_search_shards" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about the indexes and shards that a search request would be executed against.
#
# POST /{index}/_search_shards
# Docs: https://opensearch.org/docs/latest
# operationId: search_shards.3
export def "search-shards shards3" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with `foo` but no index starts with `bar`.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --local: oneof<nothing, bool> # If `true`, the request retrieves information from the local node only. (default: false)
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --routing: string # Custom value used to route operations to a specific shard.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_search_shards" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates point in time context.
#
# POST /{index}/_search/point_in_time
# Docs: https://opensearch.org/docs/latest/search-plugins/point-in-time-api/#create-a-pit
# operationId: create_pit.0
export def "search-point-in-time pit0-by-index" [
  index: list
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-partial-pit-creation: oneof<nothing, bool> # Allow if point in time can be created with partial failures.
  --expand-wildcards: string # Whether to expand wildcard expression to concrete indexes that are open, closed or both.
  --keep-alive: string # Specify the keep alive for point in time.
  --preference: string # Specify the node or shard the operation should be performed on. (default: random)
  --routing: string # Comma-separated list of specific routing values.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_partial_pit_creation" $allow_partial_pit_creation "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "keep_alive" $keep_alive "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_search/point_in_time" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Allows to use the Mustache language to pre-render a search definition.
#
# GET /{index}/_search/template
# Docs: https://opensearch.org/docs/latest/search-plugins/search-template/
# operationId: search_template.2
export def "search-template template2" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with `foo` but no index starts with `bar`.
  --ccs-minimize-roundtrips: oneof<nothing, bool> # If `true`, network round-trips are minimized for cross-cluster search requests. (default: true)
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --explain: oneof<nothing, bool> # If `true`, the response includes additional details about score computation as part of a hit.
  --ignore-throttled: oneof<nothing, bool> # If `true`, specified concrete, expanded, or aliased indexes are not included in the response when throttled.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --profile: oneof<nothing, bool> # If `true`, the query execution is profiled.
  --rest-total-hits-as-int: oneof<nothing, bool> # If `true`, `hits.total` are rendered as an integer in the response. (default: false)
  --routing: string # Custom value used to route operations to a specific shard.
  --scroll: string # Specifies how long a consistent view of the index should be maintained for scrolled search.
  --search-type: string # The type of the search operation.
  --typed-keys: oneof<nothing, bool> # If `true`, the response prefixes aggregation and suggester names with their respective types.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --explain: oneof<nothing, bool> # If `true`, returns detailed information about score calculation as part of each hit.
  --id: string # The unique identifier for a resource.
  --params: record # Key-value pairs used to replace Mustache variables in the template. The key is the variable name. The value is the variable value.
  --profile: oneof<nothing, bool> # If `true`, the query execution is profiled.
  --body-source: string # An inline search template. Supports the same parameters as the search API request body. Also supports Mustache variables. If no id is specified, this parameter is required.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "ccs_minimize_roundtrips" $ccs_minimize_roundtrips "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "explain" $explain "scalar") (serialize-qp "ignore_throttled" $ignore_throttled "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "profile" $profile "scalar") (serialize-qp "rest_total_hits_as_int" $rest_total_hits_as_int "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "scroll" $scroll "scalar") (serialize-qp "search_type" $search_type "scalar") (serialize-qp "typed_keys" $typed_keys "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_search/template" $qp)
  let body = {explain: $explain, id: $id, params: $params, profile: $profile, source: $body_source} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Allows to use the Mustache language to pre-render a search definition.
#
# POST /{index}/_search/template
# Docs: https://opensearch.org/docs/latest/search-plugins/search-template/
# operationId: search_template.3
export def "search-template template3" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with `foo` but no index starts with `bar`.
  --ccs-minimize-roundtrips: oneof<nothing, bool> # If `true`, network round-trips are minimized for cross-cluster search requests. (default: true)
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --explain: oneof<nothing, bool> # If `true`, the response includes additional details about score computation as part of a hit.
  --ignore-throttled: oneof<nothing, bool> # If `true`, specified concrete, expanded, or aliased indexes are not included in the response when throttled.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --profile: oneof<nothing, bool> # If `true`, the query execution is profiled.
  --rest-total-hits-as-int: oneof<nothing, bool> # If `true`, `hits.total` are rendered as an integer in the response. (default: false)
  --routing: string # Custom value used to route operations to a specific shard.
  --scroll: string # Specifies how long a consistent view of the index should be maintained for scrolled search.
  --search-type: string # The type of the search operation.
  --typed-keys: oneof<nothing, bool> # If `true`, the response prefixes aggregation and suggester names with their respective types.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --explain: oneof<nothing, bool> # If `true`, returns detailed information about score calculation as part of each hit.
  --id: string # The unique identifier for a resource.
  --params: record # Key-value pairs used to replace Mustache variables in the template. The key is the variable name. The value is the variable value.
  --profile: oneof<nothing, bool> # If `true`, the query execution is profiled.
  --body-source: string # An inline search template. Supports the same parameters as the search API request body. Also supports Mustache variables. If no id is specified, this parameter is required.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "ccs_minimize_roundtrips" $ccs_minimize_roundtrips "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "explain" $explain "scalar") (serialize-qp "ignore_throttled" $ignore_throttled "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "profile" $profile "scalar") (serialize-qp "rest_total_hits_as_int" $rest_total_hits_as_int "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "scroll" $scroll "scalar") (serialize-qp "search_type" $search_type "scalar") (serialize-qp "typed_keys" $typed_keys "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_search/template" $qp)
  let body = {explain: $explain, id: $id, params: $params, profile: $profile, source: $body_source} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Provides low-level information about segments in a Lucene index.
#
# GET /{index}/_segments
# Docs: https://opensearch.org/docs/latest
# operationId: indices.segments.1
export def "segments indicessegments1" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --verbose: oneof<nothing, bool> # If `true`, the request returns a verbose response. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "verbose" $verbose "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_segments" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns settings for one or more indexes.
#
# GET /{index}/_settings
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/get-settings/
# operationId: indices.get_settings.2
@deprecated --flag master-timeout
export def "settings settings2" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with foo but no index starts with `bar`.
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`.
  --flat-settings: oneof<nothing, bool> # If `true`, returns settings in flat format. (default: false)
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --include-defaults: oneof<nothing, bool> # If `true`, return all default settings in the response. (default: false)
  --local: oneof<nothing, bool> # If `true`, the request retrieves information from the local node only. If `false`, information is retrieved from the cluster-manager node. (default: false)
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "include_defaults" $include_defaults "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_settings" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Updates the index settings.
#
# PUT /{index}/_settings
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/update-settings/
# operationId: indices.put_settings.1
# --index shape: {index?: record, mode?: string, routing_path?: any, soft_deletes?: record, soft_deletes.retention_lease.period?: string, sort?: record, number_of_shards?: any, number_of_replicas?: any, number_of_routing_shards?: any, check_on_startup?: "checksum"|"false"|"true", codec?: string, routing_partition_size?: any, load_fixed_bitset_filters_eagerly?: any, hidden?: any, auto_expand_replicas?: string, merge?: record, merge.scheduler.max_thread_count?: any, search?: record, search.idle.after?: string, refresh_interval?: string, max_result_window?: any, max_inner_result_window?: any, max_rescore_window?: any, max_docvalue_fields_search?: any, max_script_fields?: any, max_ngram_diff?: any, max_shingle_diff?: any, blocks?: record, blocks.read_only?: any, blocks.read_only_allow_delete?: any, blocks.read?: any, blocks.write?: any, blocks.metadata?: any, max_refresh_listeners?: any, analyze?: record, analyze.max_token_count?: any, highlight?: record, highlight.max_analyzed_offset?: any, max_terms_count?: any, max_regex_length?: any, routing?: record, gc_deletes?: string, default_pipeline?: string, final_pipeline?: string, lifecycle?: record, lifecycle.name?: string, provided_name?: string, creation_date?: any, creation_date_string?: any, uuid?: string, version?: record, verified_before_close?: any, format?: any, max_slices_per_scroll?: any, translog?: record, translog.durability?: any, translog.flush_threshold_size?: string, query_string?: record, query_string.lenient?: any, priority?: any, top_metrics_max_size?: any, analysis?: record, settings?: record, queries?: record, similarity?: record, mapping?: record, indexing?: record, indexing_pressure?: record, store?: record, knn?: any, knn.algo_param.ef_search?: any, composite_index.star_tree?: record}
# --soft_deletes shape: {enabled?: any, retention?: record, retention_lease?: record}
# --sort shape: {field?: any, order?: any, mode?: any, missing?: any}
# --merge shape: {log_byte_size_policy?: record, policy?: any, policy.deletes_pct_allowed?: any, policy.expunge_deletes_allowed?: any, policy.floor_segment?: string, policy.max_merge_at_once?: any, policy.max_merged_segment?: string, policy.reclaim_deletes_weight?: any, policy.segments_per_tier?: any, scheduler?: record}
# --search shape: {concurrent?: record, concurrent_segment_search?: record, default_pipeline?: string, idle?: record, slowlog?: record, throttled?: any}
# --blocks shape: {read_only?: any, read_only_allow_delete?: any, read?: any, write?: any, metadata?: any, search_only?: any}
# --analyze shape: {max_token_count?: any}
# --highlight shape: {max_analyzed_offset?: any}
# --routing shape: {allocation?: record, rebalance?: record}
# --lifecycle shape: {name: string, indexing_complete?: any, origination_date?: any, parse_origination_date?: bool, step?: record, rollover_alias?: string}
# --version shape: {created?: string, created_string?: string}
# --translog shape: {sync_interval?: string, durability?: any, flush_threshold_size?: string, generation_threshold_size?: string, retention?: record}
# --query_string shape: {lenient?: any}
# --analysis shape: {analyzer?: record, char_filter?: record, filter?: record, normalizer?: record, tokenizer?: record}
# --settings shape: {index?: record, mode?: string, routing_path?: any, soft_deletes?: record, soft_deletes.retention_lease.period?: string, sort?: record, number_of_shards?: any, number_of_replicas?: any, number_of_routing_shards?: any, check_on_startup?: "checksum"|"false"|"true", codec?: string, routing_partition_size?: any, load_fixed_bitset_filters_eagerly?: any, hidden?: any, auto_expand_replicas?: string, merge?: record, merge.scheduler.max_thread_count?: any, search?: record, search.idle.after?: string, refresh_interval?: string, max_result_window?: any, max_inner_result_window?: any, max_rescore_window?: any, max_docvalue_fields_search?: any, max_script_fields?: any, max_ngram_diff?: any, max_shingle_diff?: any, blocks?: record, blocks.read_only?: any, blocks.read_only_allow_delete?: any, blocks.read?: any, blocks.write?: any, blocks.metadata?: any, max_refresh_listeners?: any, analyze?: record, analyze.max_token_count?: any, highlight?: record, highlight.max_analyzed_offset?: any, max_terms_count?: any, max_regex_length?: any, routing?: record, gc_deletes?: string, default_pipeline?: string, final_pipeline?: string, lifecycle?: record, lifecycle.name?: string, provided_name?: string, creation_date?: any, creation_date_string?: any, uuid?: string, version?: record, verified_before_close?: any, format?: any, max_slices_per_scroll?: any, translog?: record, translog.durability?: any, translog.flush_threshold_size?: string, query_string?: record, query_string.lenient?: any, priority?: any, top_metrics_max_size?: any, analysis?: record, settings?: record, queries?: record, similarity?: record, mapping?: record, indexing?: record, indexing_pressure?: record, store?: record, knn?: any, knn.algo_param.ef_search?: any, composite_index.star_tree?: record}
# --queries shape: {cache?: record}
# --similarity shape: {bm25?: record, dfi?: record, dfr?: record, ib?: record, lmd?: record, lmj?: record, scripted_tfidf?: record}
# --mapping shape: {coerce?: any, total_fields?: record, depth?: record, nested_fields?: record, nested_objects?: record, field_name_length?: record, dimension_fields?: record, ignore_malformed?: any}
# --indexing shape: {slowlog?: record}
# --indexing_pressure shape: {memory: record}
# --store shape: {type: any, allow_mmap?: any, fs?: record, hybrid?: record, preload?: list, stats_refresh_interval?: string}
# --composite_index.star_tree shape: {default?: record, field?: record, max_fields?: any}
@deprecated --flag master-timeout
export def "settings settings1-by-index" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with `foo` but no index starts with `bar`.
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`.
  --flat-settings: oneof<nothing, bool> # If `true`, returns settings in flat format. (default: false)
  --ignore-unavailable: oneof<nothing, bool> # Whether specified concrete indexes should be ignored when unavailable (missing or closed).
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --preserve-existing: oneof<nothing, bool> # If `true`, existing index settings remain unchanged. (default: false)
  --timeout: string # Period to wait for a response. If no response is received before the  timeout expires, the request fails and returns an error.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-index: record # The configuration settings for an index. — shape: {index?: record, mode?: string, routing_path?: any, soft_deletes?: record, soft_deletes.retention_lease.period?: string, sort?: record, number_of_shards?: any, number_of_replicas?: any, number_of_routing_shards?: any, check_on_startup?: "checksum"|"false"|"true", codec?: string, routing_partition_size?: any, load_fixed_bitset_filters_eagerly?: any, hidden?: any, auto_expand_replicas?: string, merge?: record, merge.scheduler.max_thread_count?: any, search?: record, search.idle.after?: string, refresh_interval?: string, max_result_window?: any, max_inner_result_window?: any, max_rescore_window?: any, max_docvalue_fields_search?: any, max_script_fields?: any, max_ngram_diff?: any, max_shingle_diff?: any, blocks?: record, blocks.read_only?: any, blocks.read_only_allow_delete?: any, blocks.read?: any, blocks.write?: any, blocks.metadata?: any, max_refresh_listeners?: any, analyze?: record, analyze.max_token_count?: any, highlight?: record, highlight.max_analyzed_offset?: any, max_terms_count?: any, max_regex_length?: any, routing?: record, gc_deletes?: string, default_pipeline?: string, final_pipeline?: string, lifecycle?: record, lifecycle.name?: string, provided_name?: string, creation_date?: any, creation_date_string?: any, uuid?: string, version?: record, verified_before_close?: any, format?: any, max_slices_per_scroll?: any, translog?: record, translog.durability?: any, translog.flush_threshold_size?: string, query_string?: record, query_string.lenient?: any, priority?: any, top_metrics_max_size?: any, analysis?: record, settings?: record, queries?: record, similarity?: record, mapping?: record, indexing?: record, indexing_pressure?: record, store?: record, knn?: any, knn.algo_param.ef_search?: any, composite_index.star_tree?: record}
  --mode: string
  --routing-path: any
  --soft-deletes: record # The configuration for soft deletes. — shape: {enabled?: any, retention?: record, retention_lease?: record}
  --soft-deletesretention-leaseperiod: string # A duration. Units can be `nanos`, `micros`, `ms` (milliseconds), `s` (seconds), `m` (minutes), `h` (hours) and `d` (days). Also accepts `0` without a unit and `-1` to indicate an unspecified value.
  --body-sort: record # The configuration for segment sorting. — shape: {field?: any, order?: any, mode?: any, missing?: any}
  --number-of-shards: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --number-of-replicas: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --number-of-routing-shards: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --check-on-startup: string@check-on-startup-completer # The type of check to perform when starting an index.
  --codec: string # The compression type for stored data.
  --routing-partition-size: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --load-fixed-bitset-filters-eagerly: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --hidden: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --auto-expand-replicas: string # The range of replicas to maintain.
  --merge: record # The configuration for segment merging. — shape: {log_byte_size_policy?: record, policy?: any, policy.deletes_pct_allowed?: any, policy.expunge_deletes_allowed?: any, policy.floor_segment?: string, policy.max_merge_at_once?: any, policy.max_merged_segment?: string, policy.reclaim_deletes_weight?: any, policy.segments_per_tier?: any, scheduler?: record}
  --mergeschedulermax-thread-count: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --search: record # The configuration for search operations. — shape: {concurrent?: record, concurrent_segment_search?: record, default_pipeline?: string, idle?: record, slowlog?: record, throttled?: any}
  --searchidleafter: string # A duration. Units can be `nanos`, `micros`, `ms` (milliseconds), `s` (seconds), `m` (minutes), `h` (hours) and `d` (days). Also accepts `0` without a unit and `-1` to indicate an unspecified value.
  --refresh-interval: string # A duration. Units can be `nanos`, `micros`, `ms` (milliseconds), `s` (seconds), `m` (minutes), `h` (hours) and `d` (days). Also accepts `0` without a unit and `-1` to indicate an unspecified value.
  --max-result-window: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --max-inner-result-window: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --max-rescore-window: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --max-docvalue-fields-search: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --max-script-fields: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --max-ngram-diff: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --max-shingle-diff: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --blocks: record # The block settings that control index operations. — shape: {read_only?: any, read_only_allow_delete?: any, read?: any, write?: any, metadata?: any, search_only?: any}
  --blocksread-only: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --blocksread-only-allow-delete: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --blocksread: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --blockswrite: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --blocksmetadata: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --max-refresh-listeners: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --analyze: record # The configuration for text analysis. — shape: {max_token_count?: any}
  --analyzemax-token-count: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --highlight: record # The configuration for highlighting. — shape: {max_analyzed_offset?: any}
  --highlightmax-analyzed-offset: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --max-terms-count: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --max-regex-length: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --routing: record # The routing configuration for index operations. — shape: {allocation?: record, rebalance?: record}
  --gc-deletes: string # A duration. Units can be `nanos`, `micros`, `ms` (milliseconds), `s` (seconds), `m` (minutes), `h` (hours) and `d` (days). Also accepts `0` without a unit and `-1` to indicate an unspecified value.
  --default-pipeline: string
  --final-pipeline: string
  --lifecycle: record # The index lifecycle management configuration. — shape: {name: string, indexing_complete?: any, origination_date?: any, parse_origination_date?: bool, step?: record, rollover_alias?: string}
  --lifecyclename: string # The name of a resource or configuration element.
  --provided-name: string # The name of a resource or configuration element.
  --creation-date: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --creation-date-string: any # A date and time, either as a string whose format depends on the context (defaulting to ISO_8601) or the number of milliseconds since the epoch. OpenSearch accepts both as an input but will generally output a string. representation.
  --uuid: string # The universally unique identifier.
  --version: record # The versioning information for the index. — shape: {created?: string, created_string?: string}
  --verified-before-close: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --format: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --max-slices-per-scroll: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --translog: record # The translog configuration. — shape: {sync_interval?: string, durability?: any, flush_threshold_size?: string, generation_threshold_size?: string, retention?: record}
  --translogdurability: any # The durability settings for the translog.
  --translogflush-threshold-size: string # The unique identifier of a node.
  --query-string: record # The configuration for query string parsing. — shape: {lenient?: any}
  --query-stringlenient: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --priority: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --top-metrics-max-size: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --analysis: record # The text analysis configuration. — shape: {analyzer?: record, char_filter?: record, filter?: record, normalizer?: record, tokenizer?: record}
  --settings: record # The configuration settings for an index. — shape: {index?: record, mode?: string, routing_path?: any, soft_deletes?: record, soft_deletes.retention_lease.period?: string, sort?: record, number_of_shards?: any, number_of_replicas?: any, number_of_routing_shards?: any, check_on_startup?: "checksum"|"false"|"true", codec?: string, routing_partition_size?: any, load_fixed_bitset_filters_eagerly?: any, hidden?: any, auto_expand_replicas?: string, merge?: record, merge.scheduler.max_thread_count?: any, search?: record, search.idle.after?: string, refresh_interval?: string, max_result_window?: any, max_inner_result_window?: any, max_rescore_window?: any, max_docvalue_fields_search?: any, max_script_fields?: any, max_ngram_diff?: any, max_shingle_diff?: any, blocks?: record, blocks.read_only?: any, blocks.read_only_allow_delete?: any, blocks.read?: any, blocks.write?: any, blocks.metadata?: any, max_refresh_listeners?: any, analyze?: record, analyze.max_token_count?: any, highlight?: record, highlight.max_analyzed_offset?: any, max_terms_count?: any, max_regex_length?: any, routing?: record, gc_deletes?: string, default_pipeline?: string, final_pipeline?: string, lifecycle?: record, lifecycle.name?: string, provided_name?: string, creation_date?: any, creation_date_string?: any, uuid?: string, version?: record, verified_before_close?: any, format?: any, max_slices_per_scroll?: any, translog?: record, translog.durability?: any, translog.flush_threshold_size?: string, query_string?: record, query_string.lenient?: any, priority?: any, top_metrics_max_size?: any, analysis?: record, settings?: record, queries?: record, similarity?: record, mapping?: record, indexing?: record, indexing_pressure?: record, store?: record, knn?: any, knn.algo_param.ef_search?: any, composite_index.star_tree?: record}
  --queries: record # The configuration for query caching. — shape: {cache?: record}
  --similarity: record # The configuration for similarity algorithms. — shape: {bm25?: record, dfi?: record, dfr?: record, ib?: record, lmd?: record, lmj?: record, scripted_tfidf?: record}
  --mapping: record # The mapping configuration for the index. — shape: {coerce?: any, total_fields?: record, depth?: record, nested_fields?: record, nested_objects?: record, field_name_length?: record, dimension_fields?: record, ignore_malformed?: any}
  --indexing: record # The indexing-related settings for an index. — shape: {slowlog?: record}
  --indexing-pressure: record # The configuration for indexing backpressure. — shape: {memory: record}
  --store: record # shape: {type: any, allow_mmap?: any, fs?: record, hybrid?: record, preload?: list, stats_refresh_interval?: string}
  --knn: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --knnalgo-paramef-search: any # Certain APIs may return values, including numbers such as epoch timestamps, as strings. This setting captures this behavior while keeping the semantics of the field type.  Depending on the target language, code generators can keep the union or remove it and leniently parse strings to the target type.
  --composite-indexstar-tree: record # The star tree indexing configuration. — shape: {default?: record, field?: record, max_fields?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "preserve_existing" $preserve_existing "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_settings" $qp)
  let body = {index: $body_index, mode: $mode, routing_path: $routing_path, soft_deletes: $soft_deletes, soft_deletes.retention_lease.period: $soft_deletesretention_leaseperiod, sort: $body_sort, number_of_shards: $number_of_shards, number_of_replicas: $number_of_replicas, number_of_routing_shards: $number_of_routing_shards, check_on_startup: $check_on_startup, codec: $codec, routing_partition_size: $routing_partition_size, load_fixed_bitset_filters_eagerly: $load_fixed_bitset_filters_eagerly, hidden: $hidden, auto_expand_replicas: $auto_expand_replicas, merge: $merge, merge.scheduler.max_thread_count: $mergeschedulermax_thread_count, search: $search, search.idle.after: $searchidleafter, refresh_interval: $refresh_interval, max_result_window: $max_result_window, max_inner_result_window: $max_inner_result_window, max_rescore_window: $max_rescore_window, max_docvalue_fields_search: $max_docvalue_fields_search, max_script_fields: $max_script_fields, max_ngram_diff: $max_ngram_diff, max_shingle_diff: $max_shingle_diff, blocks: $blocks, blocks.read_only: $blocksread_only, blocks.read_only_allow_delete: $blocksread_only_allow_delete, blocks.read: $blocksread, blocks.write: $blockswrite, blocks.metadata: $blocksmetadata, max_refresh_listeners: $max_refresh_listeners, analyze: $analyze, analyze.max_token_count: $analyzemax_token_count, highlight: $highlight, highlight.max_analyzed_offset: $highlightmax_analyzed_offset, max_terms_count: $max_terms_count, max_regex_length: $max_regex_length, routing: $routing, gc_deletes: $gc_deletes, default_pipeline: $default_pipeline, final_pipeline: $final_pipeline, lifecycle: $lifecycle, lifecycle.name: $lifecyclename, provided_name: $provided_name, creation_date: $creation_date, creation_date_string: $creation_date_string, uuid: $uuid, version: $version, verified_before_close: $verified_before_close, format: $format, max_slices_per_scroll: $max_slices_per_scroll, translog: $translog, translog.durability: $translogdurability, translog.flush_threshold_size: $translogflush_threshold_size, query_string: $query_string, query_string.lenient: $query_stringlenient, priority: $priority, top_metrics_max_size: $top_metrics_max_size, analysis: $analysis, settings: $settings, queries: $queries, similarity: $similarity, mapping: $mapping, indexing: $indexing, indexing_pressure: $indexing_pressure, store: $store, knn: $knn, knn.algo_param.ef_search: $knnalgo_paramef_search, composite_index.star_tree: $composite_indexstar_tree} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns settings for one or more indexes.
#
# GET /{index}/_settings/{name}
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/get-settings/
# operationId: indices.get_settings.3
@deprecated --flag master-timeout
export def "settings settings3" [
  index: string
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with foo but no index starts with `bar`.
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`.
  --flat-settings: oneof<nothing, bool> # If `true`, returns settings in flat format. (default: false)
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --include-defaults: oneof<nothing, bool> # If `true`, return all default settings in the response. (default: false)
  --local: oneof<nothing, bool> # If `true`, the request retrieves information from the local node only. If `false`, information is retrieved from the cluster-manager node. (default: false)
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "flat_settings" $flat_settings "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "include_defaults" $include_defaults "scalar") (serialize-qp "local" $local "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_settings/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Provides store information for shard copies of indexes.
#
# GET /{index}/_shard_stores
# Docs: https://opensearch.org/docs/latest
# operationId: indices.shard_stores.1
export def "shard-stores stores1" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams.
  --ignore-unavailable: oneof<nothing, bool> # If `true`, missing or closed indexes are not included in the response.
  --status: string # List of shard health statuses used to limit the request.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "status" $status "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_shard_stores" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Allow to shrink an existing index into a new index with fewer primary shards.
#
# POST /{index}/_shrink/{target}
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/shrink-index/
# operationId: indices.shrink.0
@deprecated --flag master-timeout
export def "shrink indicesshrink0" [
  index: string
  target: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --copy-settings: oneof<nothing, bool> # whether or not to copy settings from the source index. (default: false)
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --task-execution-timeout: string # Explicit task execution timeout, only useful when `wait_for_completion` is false, defaults to `1h`.
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to `all` or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --wait-for-completion: oneof<nothing, bool> # Should this request wait until the operation has completed before returning. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --aliases: record # The key is the alias name. Index alias names support date math.
  --settings: record # Configuration options for the target index.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "copy_settings" $copy_settings "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "task_execution_timeout" $task_execution_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "wait_for_completion" $wait_for_completion "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_shrink/($target)" $qp)
  let body = {aliases: $aliases, settings: $settings} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Allow to shrink an existing index into a new index with fewer primary shards.
#
# PUT /{index}/_shrink/{target}
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/shrink-index/
# operationId: indices.shrink.1
@deprecated --flag master-timeout
export def "shrink indicesshrink1" [
  index: string
  target: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --copy-settings: oneof<nothing, bool> # whether or not to copy settings from the source index. (default: false)
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --task-execution-timeout: string # Explicit task execution timeout, only useful when `wait_for_completion` is false, defaults to `1h`.
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to `all` or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --wait-for-completion: oneof<nothing, bool> # Should this request wait until the operation has completed before returning. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --aliases: record # The key is the alias name. Index alias names support date math.
  --settings: record # Configuration options for the target index.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "copy_settings" $copy_settings "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "task_execution_timeout" $task_execution_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "wait_for_completion" $wait_for_completion "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_shrink/($target)" $qp)
  let body = {aliases: $aliases, settings: $settings} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns the source of a document.
#
# GET /{index}/_source/{id}
# Docs: https://opensearch.org/docs/latest/api-reference/document-apis/get-documents/
# operationId: get_source.0
export def "source source0-by-id-index" [
  id: string
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: string # Set to `true` or `false` to return the `_source` field or not, or a list of fields to return.
  --source-excludes: string # A comma-separated list of source fields to exclude in the response.
  --source-includes: string # A comma-separated list of source fields to include in the response.
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --realtime: oneof<nothing, bool> # Boolean) If `true`, the request is real-time as opposed to near-real-time.
  --refresh: string # If `true`, OpenSearch refreshes the affected shards to make this operation visible to search. If `false`, do nothing with refreshes.
  --routing: string # Target the specified primary shard.
  --version: int # Explicit version number for concurrency control. The specified version must match the current version of the document for the request to succeed. (format: int64)
  --version-type: string # Specific version type. One of `internal`, `external`, `external_gte`.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "scalar") (serialize-qp "_source_excludes" $source_excludes "scalar") (serialize-qp "_source_includes" $source_includes "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "realtime" $realtime "scalar") (serialize-qp "refresh" $refresh "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "version_type" $version_type "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_source/($id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about whether a document source exists in an index.
#
# HEAD /{index}/_source/{id}
# Docs: https://opensearch.org/docs/latest/api-reference/document-apis/get-documents/
# operationId: exists_source.0
export def "source source0-by-id-index-1" [
  id: string
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: string # `true` or `false` to return the `_source` field or not, or a list of fields to return.
  --source-excludes: string # A comma-separated list of source fields to exclude in the response.
  --source-includes: string # A comma-separated list of source fields to include in the response.
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --realtime: oneof<nothing, bool> # If `true`, the request is real-time as opposed to near-real-time.
  --refresh: string # If `true`, OpenSearch refreshes all shards involved in the delete by query after the request completes.
  --routing: string # Target the specified primary shard.
  --version: int # Explicit version number for concurrency control. The specified version must match the current version of the document for the request to succeed. (format: int64)
  --version-type: string # Specific version type: `external`, `external_gte`.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "scalar") (serialize-qp "_source_excludes" $source_excludes "scalar") (serialize-qp "_source_includes" $source_includes "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "realtime" $realtime "scalar") (serialize-qp "refresh" $refresh "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "version_type" $version_type "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_source/($id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Allows you to split an existing index into a new index with more primary shards.
#
# POST /{index}/_split/{target}
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/split/
# operationId: indices.split.0
@deprecated --flag master-timeout
export def "split indicessplit0" [
  index: string
  target: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --copy-settings: oneof<nothing, bool> # whether or not to copy settings from the source index. (default: false)
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --task-execution-timeout: string # Explicit task execution timeout, only useful when `wait_for_completion` is false, defaults to `1h`.
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to `all` or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --wait-for-completion: oneof<nothing, bool> # Should this request wait until the operation has completed before returning. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --aliases: record # Aliases for the resulting index.
  --settings: record # Configuration options for the target index.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "copy_settings" $copy_settings "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "task_execution_timeout" $task_execution_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "wait_for_completion" $wait_for_completion "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_split/($target)" $qp)
  let body = {aliases: $aliases, settings: $settings} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Allows you to split an existing index into a new index with more primary shards.
#
# PUT /{index}/_split/{target}
# Docs: https://opensearch.org/docs/latest/api-reference/index-apis/split/
# operationId: indices.split.1
@deprecated --flag master-timeout
export def "split indicessplit1" [
  index: string
  target: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --cluster-manager-timeout: string # Operation timeout for connection to cluster-manager node.
  --copy-settings: oneof<nothing, bool> # whether or not to copy settings from the source index. (default: false)
  --master-timeout: string # Period to wait for a connection to the cluster-manager node. If no response is received before the timeout expires, the request fails and returns an error. (DEPRECATED)
  --task-execution-timeout: string # Explicit task execution timeout, only useful when `wait_for_completion` is false, defaults to `1h`.
  --timeout: string # Period to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to `all` or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --wait-for-completion: oneof<nothing, bool> # Should this request wait until the operation has completed before returning. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --aliases: record # Aliases for the resulting index.
  --settings: record # Configuration options for the target index.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "cluster_manager_timeout" $cluster_manager_timeout "scalar") (serialize-qp "copy_settings" $copy_settings "scalar") (serialize-qp "master_timeout" $master_timeout "scalar") (serialize-qp "task_execution_timeout" $task_execution_timeout "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "wait_for_completion" $wait_for_completion "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_split/($target)" $qp)
  let body = {aliases: $aliases, settings: $settings} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Provides statistics on operations happening in an index.
#
# GET /{index}/_stats
# Docs: https://opensearch.org/docs/latest
# operationId: indices.stats.2
export def "stats indicesstats2" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --completion-fields: string # Comma-separated list or wildcard expressions of fields to include in field data and suggest statistics.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`.
  --fielddata-fields: string # Comma-separated list or wildcard expressions of fields to include in field data statistics.
  --qp-fields: string # Comma-separated list or wildcard expressions of fields to include in the statistics.
  --forbid-closed-indices: oneof<nothing, bool> # If `true`, statistics are not collected from closed indexes. (default: true)
  --groups: string # Comma-separated list of search groups to include in the search statistics.
  --include-segment-file-sizes: oneof<nothing, bool> # If `true`, the call reports the aggregated disk usage of each one of the Lucene index files (only applies if segment stats are requested). (default: false)
  --include-unloaded-segments: oneof<nothing, bool> # If `true`, the response includes information from segments that are not loaded into memory. (default: false)
  --level: string@level-completer-1 # Indicates whether statistics are aggregated at the cluster, index, or shard level.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "completion_fields" $completion_fields "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "fielddata_fields" $fielddata_fields "scalar") (serialize-qp "fields" $qp_fields "scalar") (serialize-qp "forbid_closed_indices" $forbid_closed_indices "scalar") (serialize-qp "groups" $groups "scalar") (serialize-qp "include_segment_file_sizes" $include_segment_file_sizes "scalar") (serialize-qp "include_unloaded_segments" $include_unloaded_segments "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Provides statistics on operations happening in an index.
#
# GET /{index}/_stats/{metric}
# Docs: https://opensearch.org/docs/latest
# operationId: indices.stats.3
export def "stats indicesstats3" [
  index: string
  metric: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --completion-fields: string # Comma-separated list or wildcard expressions of fields to include in field data and suggest statistics.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`.
  --fielddata-fields: string # Comma-separated list or wildcard expressions of fields to include in field data statistics.
  --qp-fields: string # Comma-separated list or wildcard expressions of fields to include in the statistics.
  --forbid-closed-indices: oneof<nothing, bool> # If `true`, statistics are not collected from closed indexes. (default: true)
  --groups: string # Comma-separated list of search groups to include in the search statistics.
  --include-segment-file-sizes: oneof<nothing, bool> # If `true`, the call reports the aggregated disk usage of each one of the Lucene index files (only applies if segment stats are requested). (default: false)
  --include-unloaded-segments: oneof<nothing, bool> # If `true`, the response includes information from segments that are not loaded into memory. (default: false)
  --level: string@level-completer-1 # Indicates whether statistics are aggregated at the cluster, index, or shard level.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "completion_fields" $completion_fields "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "fielddata_fields" $fielddata_fields "scalar") (serialize-qp "fields" $qp_fields "scalar") (serialize-qp "forbid_closed_indices" $forbid_closed_indices "scalar") (serialize-qp "groups" $groups "scalar") (serialize-qp "include_segment_file_sizes" $include_segment_file_sizes "scalar") (serialize-qp "include_unloaded_segments" $include_unloaded_segments "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_stats/($metric)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information and statistics about terms in the fields of a particular document.
#
# GET /{index}/_termvectors
# Docs: https://opensearch.org/docs/latest
# operationId: termvectors.0
# --filter shape: {max_doc_freq?: int, max_num_terms?: int, max_term_freq?: int, max_word_length?: int, min_doc_freq?: int, min_term_freq?: int, min_word_length?: int}
export def "termvectors termvectors0" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --field-statistics: oneof<nothing, bool> # If `true`, the response includes the document count, sum of document frequencies, and sum of total term frequencies. (default: true)
  --qp-fields: string # Comma-separated list or wildcard expressions of fields to include in the statistics. Used as the default list unless a specific field list is provided in the `completion_fields` or `fielddata_fields` parameters.
  --offsets: oneof<nothing, bool> # If `true`, the response includes term offsets. (default: true)
  --payloads: oneof<nothing, bool> # If `true`, the response includes term payloads. (default: true)
  --positions: oneof<nothing, bool> # If `true`, the response includes term positions. (default: true)
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --realtime: oneof<nothing, bool> # If `true`, the request is real-time as opposed to near-real-time. (default: true)
  --routing: string # Custom value used to route operations to a specific shard.
  --term-statistics: oneof<nothing, bool> # If `true`, the response includes term frequency and document frequency. (default: false)
  --version: int # If `true`, returns the document version as part of a hit. (format: int64)
  --version-type: string # Specific version type.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --doc: record # An artificial document (a document not present in the index) for which you want to retrieve term vectors.
  --filter: record # shape: {max_doc_freq?: int, max_num_terms?: int, max_term_freq?: int, max_word_length?: int, min_doc_freq?: int, min_term_freq?: int, min_word_length?: int}
  --per-field-analyzer: record # Overrides the default per-field analyzer.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "field_statistics" $field_statistics "scalar") (serialize-qp "fields" $qp_fields "scalar") (serialize-qp "offsets" $offsets "scalar") (serialize-qp "payloads" $payloads "scalar") (serialize-qp "positions" $positions "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "realtime" $realtime "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "term_statistics" $term_statistics "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "version_type" $version_type "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_termvectors" $qp)
  let body = {doc: $doc, filter: $filter, per_field_analyzer: $per_field_analyzer} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns information and statistics about terms in the fields of a particular document.
#
# POST /{index}/_termvectors
# Docs: https://opensearch.org/docs/latest
# operationId: termvectors.1
# --filter shape: {max_doc_freq?: int, max_num_terms?: int, max_term_freq?: int, max_word_length?: int, min_doc_freq?: int, min_term_freq?: int, min_word_length?: int}
export def "termvectors termvectors1" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --field-statistics: oneof<nothing, bool> # If `true`, the response includes the document count, sum of document frequencies, and sum of total term frequencies. (default: true)
  --qp-fields: string # Comma-separated list or wildcard expressions of fields to include in the statistics. Used as the default list unless a specific field list is provided in the `completion_fields` or `fielddata_fields` parameters.
  --offsets: oneof<nothing, bool> # If `true`, the response includes term offsets. (default: true)
  --payloads: oneof<nothing, bool> # If `true`, the response includes term payloads. (default: true)
  --positions: oneof<nothing, bool> # If `true`, the response includes term positions. (default: true)
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --realtime: oneof<nothing, bool> # If `true`, the request is real-time as opposed to near-real-time. (default: true)
  --routing: string # Custom value used to route operations to a specific shard.
  --term-statistics: oneof<nothing, bool> # If `true`, the response includes term frequency and document frequency. (default: false)
  --version: int # If `true`, returns the document version as part of a hit. (format: int64)
  --version-type: string # Specific version type.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --doc: record # An artificial document (a document not present in the index) for which you want to retrieve term vectors.
  --filter: record # shape: {max_doc_freq?: int, max_num_terms?: int, max_term_freq?: int, max_word_length?: int, min_doc_freq?: int, min_term_freq?: int, min_word_length?: int}
  --per-field-analyzer: record # Overrides the default per-field analyzer.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "field_statistics" $field_statistics "scalar") (serialize-qp "fields" $qp_fields "scalar") (serialize-qp "offsets" $offsets "scalar") (serialize-qp "payloads" $payloads "scalar") (serialize-qp "positions" $positions "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "realtime" $realtime "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "term_statistics" $term_statistics "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "version_type" $version_type "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_termvectors" $qp)
  let body = {doc: $doc, filter: $filter, per_field_analyzer: $per_field_analyzer} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns information and statistics about terms in the fields of a particular document.
#
# GET /{index}/_termvectors/{id}
# Docs: https://opensearch.org/docs/latest
# operationId: termvectors.2
# --filter shape: {max_doc_freq?: int, max_num_terms?: int, max_term_freq?: int, max_word_length?: int, min_doc_freq?: int, min_term_freq?: int, min_word_length?: int}
export def "termvectors termvectors2" [
  id: string
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --field-statistics: oneof<nothing, bool> # If `true`, the response includes the document count, sum of document frequencies, and sum of total term frequencies. (default: true)
  --qp-fields: string # Comma-separated list or wildcard expressions of fields to include in the statistics. Used as the default list unless a specific field list is provided in the `completion_fields` or `fielddata_fields` parameters.
  --offsets: oneof<nothing, bool> # If `true`, the response includes term offsets. (default: true)
  --payloads: oneof<nothing, bool> # If `true`, the response includes term payloads. (default: true)
  --positions: oneof<nothing, bool> # If `true`, the response includes term positions. (default: true)
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --realtime: oneof<nothing, bool> # If `true`, the request is real-time as opposed to near-real-time. (default: true)
  --routing: string # Custom value used to route operations to a specific shard.
  --term-statistics: oneof<nothing, bool> # If `true`, the response includes term frequency and document frequency. (default: false)
  --version: int # If `true`, returns the document version as part of a hit. (format: int64)
  --version-type: string # Specific version type.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --doc: record # An artificial document (a document not present in the index) for which you want to retrieve term vectors.
  --filter: record # shape: {max_doc_freq?: int, max_num_terms?: int, max_term_freq?: int, max_word_length?: int, min_doc_freq?: int, min_term_freq?: int, min_word_length?: int}
  --per-field-analyzer: record # Overrides the default per-field analyzer.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "field_statistics" $field_statistics "scalar") (serialize-qp "fields" $qp_fields "scalar") (serialize-qp "offsets" $offsets "scalar") (serialize-qp "payloads" $payloads "scalar") (serialize-qp "positions" $positions "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "realtime" $realtime "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "term_statistics" $term_statistics "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "version_type" $version_type "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_termvectors/($id)" $qp)
  let body = {doc: $doc, filter: $filter, per_field_analyzer: $per_field_analyzer} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns information and statistics about terms in the fields of a particular document.
#
# POST /{index}/_termvectors/{id}
# Docs: https://opensearch.org/docs/latest
# operationId: termvectors.3
# --filter shape: {max_doc_freq?: int, max_num_terms?: int, max_term_freq?: int, max_word_length?: int, min_doc_freq?: int, min_term_freq?: int, min_word_length?: int}
export def "termvectors termvectors3" [
  id: string
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --field-statistics: oneof<nothing, bool> # If `true`, the response includes the document count, sum of document frequencies, and sum of total term frequencies. (default: true)
  --qp-fields: string # Comma-separated list or wildcard expressions of fields to include in the statistics. Used as the default list unless a specific field list is provided in the `completion_fields` or `fielddata_fields` parameters.
  --offsets: oneof<nothing, bool> # If `true`, the response includes term offsets. (default: true)
  --payloads: oneof<nothing, bool> # If `true`, the response includes term payloads. (default: true)
  --positions: oneof<nothing, bool> # If `true`, the response includes term positions. (default: true)
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --realtime: oneof<nothing, bool> # If `true`, the request is real-time as opposed to near-real-time. (default: true)
  --routing: string # Custom value used to route operations to a specific shard.
  --term-statistics: oneof<nothing, bool> # If `true`, the response includes term frequency and document frequency. (default: false)
  --version: int # If `true`, returns the document version as part of a hit. (format: int64)
  --version-type: string # Specific version type.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --doc: record # An artificial document (a document not present in the index) for which you want to retrieve term vectors.
  --filter: record # shape: {max_doc_freq?: int, max_num_terms?: int, max_term_freq?: int, max_word_length?: int, min_doc_freq?: int, min_term_freq?: int, min_word_length?: int}
  --per-field-analyzer: record # Overrides the default per-field analyzer.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "field_statistics" $field_statistics "scalar") (serialize-qp "fields" $qp_fields "scalar") (serialize-qp "offsets" $offsets "scalar") (serialize-qp "payloads" $payloads "scalar") (serialize-qp "positions" $positions "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "realtime" $realtime "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "term_statistics" $term_statistics "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "version_type" $version_type "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_termvectors/($id)" $qp)
  let body = {doc: $doc, filter: $filter, per_field_analyzer: $per_field_analyzer} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Performs an update on every document in the index without changing the source, for example to pick up a mapping change.
#
# POST /{index}/_update_by_query
# Docs: https://opensearch.org/docs/latest/api-reference/document-apis/update-by-query/
# operationId: update_by_query.0
# --query shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
# --slice shape: {field?: string, id: int, max: int}
export def "update-by-query query0" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: string # Set to `true` or `false` to return the `_source` field or not, or a list of fields to return.
  --source-excludes: list # List of fields to exclude from the returned `_source` field.
  --source-includes: list # List of fields to extract and return from the `_source` field.
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes. For example, a request targeting `foo*,bar*` returns an error if an index starts with `foo` but no index starts with `bar`.
  --analyze-wildcard: oneof<nothing, bool> # If `true`, wildcard and prefix queries are analyzed. (default: false)
  --analyzer: string # Analyzer to use for the query string.
  --conflicts: string # What to do if update by query hits version conflicts: `abort` or `proceed`.
  --default-operator: string@default-operator-completer # The default operator for query string query: `AND` or `OR`.
  --df: string # Field to use as default where no field prefix is given in the query string.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --qp-from: int # Starting offset. (format: int32, default: 0)
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --lenient: oneof<nothing, bool> # If `true`, format-based query failures (such as providing text to a numeric field) in the query string will be ignored.
  --max-docs: int # Maximum number of documents to process. Defaults to all documents. (format: int32)
  --pipeline: string # ID of the pipeline to use to preprocess incoming documents. If the index has a default ingest pipeline specified, then setting the value to `_none` disables the default ingest pipeline for this request. If a final pipeline is configured it will always run, regardless of the value of this parameter.
  --preference: string # Specifies the node or shard the operation should be performed on. Random by default. (default: random)
  --q: string # Query in the Lucene query string syntax.
  --refresh: string # If `true`, OpenSearch refreshes affected shards to make the operation visible to search.
  --request-cache: oneof<nothing, bool> # If `true`, the request cache is used for this request.
  --requests-per-second: float # The throttle for this request in sub-requests per second. (format: float, default: 0)
  --routing: string # Custom value used to route operations to a specific shard.
  --scroll: string # Period to retain the search context for scrolling.
  --scroll-size: int # Size of the scroll request that powers the operation. (format: int32, default: 100)
  --search-timeout: string # Explicit timeout for each search request.
  --search-type: string # The type of the search operation. Available options: `query_then_fetch`, `dfs_query_then_fetch`.
  --size: int # Deprecated, use `max_docs` instead. (format: int32)
  --slices: string # The number of slices this task should be divided into.
  --qp-sort: list # A comma-separated list of <field>:<direction> pairs.
  --stats: list # Specific `tag` of the request for logging and statistical purposes.
  --terminate-after: int # Maximum number of documents to collect for each shard. If a query reaches this limit, OpenSearch terminates the query early. OpenSearch collects documents before sorting. Use with caution. OpenSearch applies this parameter to each shard handling the request. When possible, let OpenSearch perform early termination automatically. Avoid specifying this parameter for requests that target data streams with backing indexes across multiple data tiers. (format: int32)
  --timeout: string # Period each update request waits for the following operations: dynamic mapping updates, waiting for active shards.
  --version: oneof<nothing, bool> # If `true`, returns the document version as part of a hit.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operation. Set to `all` or any positive integer up to the total number of shards in the index (`number_of_replicas+1`).
  --wait-for-completion: oneof<nothing, bool> # If `true`, the request blocks until the operation is complete. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --max-docs: int # The maximum number of documents to update. (format: int32)
  --body-query: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
  --script: any
  --slice: record # The configuration for a sliced scroll request. — shape: {field?: string, id: int, max: int}
  --conflicts: any
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "scalar") (serialize-qp "_source_excludes" $source_excludes "multi") (serialize-qp "_source_includes" $source_includes "multi") (serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "analyze_wildcard" $analyze_wildcard "scalar") (serialize-qp "analyzer" $analyzer "scalar") (serialize-qp "conflicts" $conflicts "scalar") (serialize-qp "default_operator" $default_operator "scalar") (serialize-qp "df" $df "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "from" $qp_from "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "lenient" $lenient "scalar") (serialize-qp "max_docs" $max_docs "scalar") (serialize-qp "pipeline" $pipeline "scalar") (serialize-qp "preference" $preference "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "refresh" $refresh "scalar") (serialize-qp "request_cache" $request_cache "scalar") (serialize-qp "requests_per_second" $requests_per_second "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "scroll" $scroll "scalar") (serialize-qp "scroll_size" $scroll_size "scalar") (serialize-qp "search_timeout" $search_timeout "scalar") (serialize-qp "search_type" $search_type "scalar") (serialize-qp "size" $size "scalar") (serialize-qp "slices" $slices "scalar") (serialize-qp "sort" $qp_sort "multi") (serialize-qp "stats" $stats "multi") (serialize-qp "terminate_after" $terminate_after "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "wait_for_completion" $wait_for_completion "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_update_by_query" $qp)
  let body = {max_docs: $max_docs, query: $body_query, script: $script, slice: $slice, conflicts: $conflicts} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Updates a document with a script or partial document.
#
# POST /{index}/_update/{id}
# Docs: https://opensearch.org/docs/latest/api-reference/document-apis/update-document/
# operationId: update.0
export def "update update0" [
  id: string
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-source: string # Set to `false` to disable source retrieval. You can also specify a comma-separated list of the fields you want to retrieve.
  --source-excludes: string # Specify the source fields you want to exclude.
  --source-includes: string # Specify the source fields you want to retrieve.
  --if-primary-term: int # Only perform the operation if the document has this primary term. (format: int64)
  --if-seq-no: int # Only perform the operation if the document has this sequence number. (format: int64)
  --lang: string # The script language. (default: painless)
  --refresh: string # If 'true', OpenSearch refreshes the affected shards to make this operation visible to search, if `wait_for` then wait for a refresh to make this operation visible to search, if `false` do nothing with refreshes.
  --require-alias: oneof<nothing, bool> # If `true`, the destination must be an index alias. (default: false)
  --retry-on-conflict: int # Specify how many times should the operation be retried when a conflict occurs. (format: int32, default: 0)
  --routing: string # Custom value used to route operations to a specific shard.
  --timeout: string # Period to wait for dynamic mapping updates and active shards. This guarantees OpenSearch waits for at least the timeout before failing. The actual wait time could be longer, particularly when multiple waits occur.
  --wait-for-active-shards: string # The number of shard copies that must be active before proceeding with the operations. Set to 'all' or any positive integer up to the total number of shards in the index (number_of_replicas+1). Defaults to 1 meaning the primary shard.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --detect-noop: oneof<nothing, bool> # Set to `false` to disable setting `result` in the response to `noop` if no change to the document occurred.
  --doc: record # A partial update to an existing document.
  --doc-as-upsert: oneof<nothing, bool> # Set to `true` to use the contents of 'doc' as the value of 'upsert'
  --script: any
  --scripted-upsert: oneof<nothing, bool> # Set to `true` to execute the script whether or not the document exists.
  --body-source: any # Defines how to fetch a source. Fetching can be disabled entirely, or the source can be filtered.
  --upsert: record # If the document does not already exist, the contents of 'upsert' are inserted as a new document. If the document exists, the 'script' is executed.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "_source" $qp_source "scalar") (serialize-qp "_source_excludes" $source_excludes "scalar") (serialize-qp "_source_includes" $source_includes "scalar") (serialize-qp "if_primary_term" $if_primary_term "scalar") (serialize-qp "if_seq_no" $if_seq_no "scalar") (serialize-qp "lang" $lang "scalar") (serialize-qp "refresh" $refresh "scalar") (serialize-qp "require_alias" $require_alias "scalar") (serialize-qp "retry_on_conflict" $retry_on_conflict "scalar") (serialize-qp "routing" $routing "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "wait_for_active_shards" $wait_for_active_shards "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_update/($id)" $qp)
  let body = {detect_noop: $detect_noop, doc: $doc, doc_as_upsert: $doc_as_upsert, script: $script, scripted_upsert: $scripted_upsert, _source: $body_source, upsert: $upsert} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# The `_upgrade` API is no longer useful and will be removed.
#
# GET /{index}/_upgrade
# Docs: https://opensearch.org/docs/latest
# operationId: indices.get_upgrade.1
export def "upgrade upgrade1" [
  index: list
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # Whether to ignore if a wildcard indexes expression resolves into no concrete indexes. (This includes `_all` string or when no indexes have been specified).
  --expand-wildcards: string # Whether to expand wildcard expression to concrete indexes that are open, closed or both.
  --ignore-unavailable: oneof<nothing, bool> # Whether specified concrete indexes should be ignored when unavailable (missing or closed).
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_upgrade" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# The `_upgrade` API is no longer useful and will be removed.
#
# POST /{index}/_upgrade
# Docs: https://opensearch.org/docs/latest
# operationId: indices.upgrade.1
export def "upgrade indicesupgrade1" [
  index: list
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allow-no-indices: oneof<nothing, bool> # Whether to ignore if a wildcard indexes expression resolves into no concrete indexes. (This includes `_all` string or when no indexes have been specified).
  --expand-wildcards: string # Whether to expand wildcard expression to concrete indexes that are open, closed or both.
  --ignore-unavailable: oneof<nothing, bool> # Whether specified concrete indexes should be ignored when unavailable (missing or closed).
  --only-ancient-segments: oneof<nothing, bool> # If `true`, only ancient (an older Lucene major release) segments will be upgraded.
  --wait-for-completion: oneof<nothing, bool> # Should this request wait until the operation has completed before returning. (default: false)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "only_ancient_segments" $only_ancient_segments "scalar") (serialize-qp "wait_for_completion" $wait_for_completion "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_upgrade" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Allows a user to validate a potentially expensive query without executing it.
#
# GET /{index}/_validate/query
# Docs: https://opensearch.org/docs/latest
# operationId: indices.validate_query.2
# --query shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
export def "validate-query query2" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --all-shards: oneof<nothing, bool> # If `true`, the validation is executed on all shards instead of one random shard per index.
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --analyze-wildcard: oneof<nothing, bool> # If `true`, wildcard and prefix queries are analyzed. (default: false)
  --analyzer: string # Analyzer to use for the query string. This parameter can only be used when the `q` query string parameter is specified.
  --default-operator: string@default-operator-completer # The default operator for query string query: `AND` or `OR`.
  --df: string # Field to use as default where no field prefix is given in the query string. This parameter can only be used when the `q` query string parameter is specified.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --explain: oneof<nothing, bool> # If `true`, the response returns detailed information if an error has occurred.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --lenient: oneof<nothing, bool> # If `true`, format-based query failures (such as providing text to a numeric field) in the query string will be ignored.
  --q: string # Query in the Lucene query string syntax.
  --rewrite: oneof<nothing, bool> # If `true`, returns a more detailed explanation showing the actual Lucene query that will be executed.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "all_shards" $all_shards "scalar") (serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "analyze_wildcard" $analyze_wildcard "scalar") (serialize-qp "analyzer" $analyzer "scalar") (serialize-qp "default_operator" $default_operator "scalar") (serialize-qp "df" $df "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "explain" $explain "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "lenient" $lenient "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "rewrite" $rewrite "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_validate/query" $qp)
  let body = {query: $body_query} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Allows a user to validate a potentially expensive query without executing it.
#
# POST /{index}/_validate/query
# Docs: https://opensearch.org/docs/latest
# operationId: indices.validate_query.3
# --query shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
export def "validate-query query3" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --all-shards: oneof<nothing, bool> # If `true`, the validation is executed on all shards instead of one random shard per index.
  --allow-no-indices: oneof<nothing, bool> # If `false`, the request returns an error if any wildcard expression, index alias, or `_all` value targets only missing or closed indexes. This behavior applies even if the request targets other open indexes.
  --analyze-wildcard: oneof<nothing, bool> # If `true`, wildcard and prefix queries are analyzed. (default: false)
  --analyzer: string # Analyzer to use for the query string. This parameter can only be used when the `q` query string parameter is specified.
  --default-operator: string@default-operator-completer # The default operator for query string query: `AND` or `OR`.
  --df: string # Field to use as default where no field prefix is given in the query string. This parameter can only be used when the `q` query string parameter is specified.
  --expand-wildcards: string # Type of index that wildcard patterns can match. If the request can target data streams, this argument determines whether wildcard expressions match hidden data streams. Supports comma-separated values, such as `open,hidden`. Valid values are: `all`, `open`, `closed`, `hidden`, `none`.
  --explain: oneof<nothing, bool> # If `true`, the response returns detailed information if an error has occurred.
  --ignore-unavailable: oneof<nothing, bool> # If `false`, the request returns an error if it targets a missing or closed index.
  --lenient: oneof<nothing, bool> # If `true`, format-based query failures (such as providing text to a numeric field) in the query string will be ignored.
  --q: string # Query in the Lucene query string syntax.
  --rewrite: oneof<nothing, bool> # If `true`, returns a more detailed explanation showing the actual Lucene query that will be executed.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "all_shards" $all_shards "scalar") (serialize-qp "allow_no_indices" $allow_no_indices "scalar") (serialize-qp "analyze_wildcard" $analyze_wildcard "scalar") (serialize-qp "analyzer" $analyzer "scalar") (serialize-qp "default_operator" $default_operator "scalar") (serialize-qp "df" $df "scalar") (serialize-qp "expand_wildcards" $expand_wildcards "scalar") (serialize-qp "explain" $explain "scalar") (serialize-qp "ignore_unavailable" $ignore_unavailable "scalar") (serialize-qp "lenient" $lenient "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "rewrite" $rewrite "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/($index)/_validate/query" $qp)
  let body = {query: $body_query} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Performs an asynchronous search.
#
# POST /_opendistro/_asynchronous_search
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/search-plugins/async/index/#rest-api
# operationId: asynchronous_search.search.0_superseded
# --collapse shape: {field: string, inner_hits?: any, max_concurrent_group_searches?: int}
# --rank shape: {rrf?: any}
# --post_filter shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
# --query shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
# --slice shape: {field?: string, id: int, max: int}
# --suggest shape: {text?: string}
# --pit shape: {id: string, keep_alive?: string}
@deprecated
export def "opendistro-asynchronous-search superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --index: string # The name of the index to be searched. Can be an individual name, a comma-separated list of indexes, or a wildcard expression of index names.
  --keep-alive: string # The amount of time that the result is saved in the cluster. For example, `2d` means that the results are stored in the cluster for 48 hours.  The saved search results are deleted after this period or if the search is canceled. Note that this includes the query execution time.  If the query exceeds this amount of time, the process cancels this query automatically.
  --keep-on-completion: oneof<nothing, bool> # Whether to save the results in the cluster after the search is complete. You can examine the stored results at a later time.
  --wait-for-completion-timeout: string # The amount of time to wait for the results. You can poll the remaining results based on an ID. The maximum value is 300 seconds. Default is `1s`.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --aggregations: record # Defines the aggregations that are run as part of the search request.
  --collapse: record # shape: {field: string, inner_hits?: any, max_concurrent_group_searches?: int}
  --explain: oneof<nothing, bool> # If `true`, returns detailed information about score computation as part of a hit.
  --ext: record # Configuration of search extensions defined by OpenSearch plugins.
  --body-from: float # Starting document offset. Needs to be non-negative. By default, you cannot page through more than 10,000 hits using the `from` and `size` parameters. To page through more hits, use the `search_after` parameter.
  --highlight: any
  --track-total-hits: any # The number of hits matching the query. When `true`, the exact number of hits is returned at the cost of some performance. When `false`, the response does not include the total number of hits matching the query. Default is `10,000` hits.
  --indices-boost: list # Boosts the `_score` of documents from specified indexes.
  --docvalue-fields: list # Array of wildcard (`*`) patterns. The request returns doc values for field names matching these patterns in the `hits.fields` property of the response.
  --rank: record # The container for ranking configuration. — shape: {rrf?: any}
  --min-score: float # Minimum `_score` for matching documents. Documents with a lower `_score` are not included in the search results.
  --post-filter: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
  --profile: oneof<nothing, bool> # Set to `true` to return detailed timing information about the execution of individual components in a search request. NOTE: This is a debugging tool and adds significant overhead to search execution.
  --body-query: record # shape: {bool?: any, boosting?: any, common?: record, combined_fields?: any, constant_score?: any, dis_max?: any, distance_feature?: any, exists?: any, function_score?: any, fuzzy?: record, geo_bounding_box?: any, geo_distance?: any, geo_polygon?: any, geo_shape?: any, has_child?: any, has_parent?: any, hybrid?: any, ids?: any, intervals?: record, knn?: record, match?: record, match_all?: any, match_bool_prefix?: record, match_none?: any, match_phrase?: record, match_phrase_prefix?: record, more_like_this?: any, multi_match?: any, nested?: any, neural?: record, parent_id?: any, percolate?: any, pinned?: any, prefix?: record, query_string?: any, range?: record, rank_feature?: any, regexp?: record, script?: any, script_score?: any, simple_query_string?: any, span_containing?: any, field_masking_span?: any, span_first?: any, span_multi?: any, span_near?: any, span_not?: any, span_or?: any, span_term?: record, span_within?: any, term?: record, terms?: any, terms_set?: record, type?: any, wildcard?: record, wrapper?: any, xy_shape?: any}
  --script-fields: record # Retrieve a script evaluation (based on different fields) for each hit.
  --search-after: list
  --size: float # The number of hits to return. By default, you cannot page through more than 10,000 hits using the `from` and `size` parameters. To page through more hits, use the `search_after` parameter.
  --slice: record # The configuration for a sliced scroll request. — shape: {field?: string, id: int, max: int}
  --body-sort: any
  --body-source: any # Defines how to fetch a source. Fetching can be disabled entirely, or the source can be filtered.
  --body-fields: list # Array of wildcard (`*`) patterns. The request returns values for field names matching these patterns in the `hits.fields` property of the response.
  --suggest: record # shape: {text?: string}
  --terminate-after: int # Maximum number of documents to collect for each shard. If a query reaches this limit, OpenSearch terminates the query early. OpenSearch collects documents before sorting. Use with caution. OpenSearch applies this parameter to each shard handling the request. When possible, let OpenSearch perform early termination automatically. Avoid specifying this parameter for requests that target data streams with backing indexes across multiple data tiers. If set to `0` (default), the query does not terminate early. (format: int32)
  --timeout: string # Specifies the period of time to wait for a response from each shard. If no response is received before the timeout expires, the request fails and returns an error. Defaults to no timeout.
  --track-scores: oneof<nothing, bool> # If `true`, calculate and return document scores, even if the scores are not used for sorting.
  --version: oneof<nothing, bool> # If `true`, returns document version as part of a hit.
  --seq-no-primary-term: oneof<nothing, bool> # If `true`, returns sequence number and primary term of the last modification of each hit.
  --stored-fields: any
  --pit: record # shape: {id: string, keep_alive?: string}
  --stats: list # Stats groups to associate with the search. Each group maintains a statistics aggregation for its associated searches. You can retrieve these stats using the indexes stats API.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "index" $index "scalar") (serialize-qp "keep_alive" $keep_alive "scalar") (serialize-qp "keep_on_completion" $keep_on_completion "scalar") (serialize-qp "wait_for_completion_timeout" $wait_for_completion_timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_asynchronous_search" $qp)
  let body = {aggregations: $aggregations, collapse: $collapse, explain: $explain, ext: $ext, from: $body_from, highlight: $highlight, track_total_hits: $track_total_hits, indices_boost: $indices_boost, docvalue_fields: $docvalue_fields, rank: $rank, min_score: $min_score, post_filter: $post_filter, profile: $profile, query: $body_query, script_fields: $script_fields, search_after: $search_after, size: $size, slice: $slice, sort: $body_sort, _source: $body_source, fields: $body_fields, suggest: $suggest, terminate_after: $terminate_after, timeout: $timeout, track_scores: $track_scores, version: $version, seq_no_primary_term: $seq_no_primary_term, stored_fields: $stored_fields, pit: $pit, stats: $stats} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Monitors any asynchronous searches that are `running`, `completed`, or `persisted`.
#
# GET /_opendistro/_asynchronous_search/stats
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/search-plugins/async/index/#monitor-stats
# operationId: asynchronous_search.stats.0_superseded
@deprecated
export def "opendistro-asynchronous-search-stats superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_asynchronous_search/stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Gets partial responses from an asynchronous search.
#
# GET /_opendistro/_asynchronous_search/{id}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/search-plugins/async/index/#get-partial-results
# operationId: asynchronous_search.get.0_superseded
@deprecated
export def "opendistro-asynchronous-search superseded-by-id" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_asynchronous_search/($id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Deletes any responses from an asynchronous search.
#
# DELETE /_opendistro/_asynchronous_search/{id}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/search-plugins/async/index/#delete-searches-and-results
# operationId: asynchronous_search.delete.0_superseded
@deprecated
export def "opendistro-asynchronous-search superseded-by-id-1" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_asynchronous_search/($id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Adds a policy to an index.
#
# POST /_opendistro/_ism/add
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#add-policy
# operationId: ism.add_policy.0_superseded
@deprecated
export def "opendistro-ism-add superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --index: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  policy_id: string # The ID of the policy to add.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "index" $index "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_ism/add" $qp)
  let body = {policy_id: $policy_id} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Adds a policy to an index.
#
# POST /_opendistro/_ism/add/{index}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#add-policy
# operationId: ism.add_policy.1_superseded
@deprecated
export def "opendistro-ism-add superseded-by-index" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --index: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  policy_id: string # The ID of the policy to add.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "index" $index "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_ism/add/($index)" $qp)
  let body = {policy_id: $policy_id} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Updates the managed index policy to a new policy.
#
# POST /_opendistro/_ism/change_policy
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#update-managed-index-policy
# operationId: ism.change_policy.0_superseded
# --include item shape: {state?: string}
@deprecated
export def "opendistro-ism-change-policy superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --index: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  policy_id: string # The ID of the new policy.
  --state: string # The state to transition to.
  --include: list # The states to include in the policy change. — item shape: {state?: string}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "index" $index "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_ism/change_policy" $qp)
  let body = {policy_id: $policy_id, state: $state, include: $include} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Updates the managed index policy to a new policy.
#
# POST /_opendistro/_ism/change_policy/{index}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#update-managed-index-policy
# operationId: ism.change_policy.1_superseded
# --include item shape: {state?: string}
@deprecated
export def "opendistro-ism-change-policy superseded-by-index" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --index: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  policy_id: string # The ID of the new policy.
  --state: string # The state to transition to.
  --include: list # The states to include in the policy change. — item shape: {state?: string}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "index" $index "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_ism/change_policy/($index)" $qp)
  let body = {policy_id: $policy_id, state: $state, include: $include} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves the currently applied policy on the specified indexes.
#
# GET /_opendistro/_ism/explain
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#explain-index
# operationId: ism.explain_policy.0_superseded
@deprecated
export def "opendistro-ism-explain superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_ism/explain" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves the currently applied policy on the specified indexes.
#
# POST /_opendistro/_ism/explain
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#explain-index
# operationId: ism.explain_policy.1_superseded
@deprecated
export def "opendistro-ism-explain superseded-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_ism/explain" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves the currently applied policy on the specified indexes.
#
# GET /_opendistro/_ism/explain/{index}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#explain-index
# operationId: ism.explain_policy.3_superseded
@deprecated
export def "opendistro-ism-explain superseded-by-index" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_ism/explain/($index)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves the currently applied policy on the specified indexes.
#
# POST /_opendistro/_ism/explain/{index}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#explain-index
# operationId: ism.explain_policy.4_superseded
@deprecated
export def "opendistro-ism-explain superseded-by-index-1" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_ism/explain/($index)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves the policies.
#
# GET /_opendistro/_ism/policies
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#get-policy
# operationId: ism.get_policies.0_superseded
@deprecated
export def "opendistro-ism-policies superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_ism/policies" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or updates policies.
#
# PUT /_opendistro/_ism/policies
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#create-policy
# operationId: ism.put_policies.0_superseded
# --policy shape: {policy_id?: string, description?: string, last_updated_time?: int, schema_version?: float, error_notification?: any, default_state?: string, states?: list, ism_template?: any}
@deprecated
export def "opendistro-ism-policies superseded-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --if-primary-term: float # Only perform the operation if the document has this primary term.
  --if-seq-no: int # Only perform the operation if the document has this sequence number. (format: int64)
  --policyID: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --policy: record # The policy defines how an index is managed throughout its lifetime. — shape: {policy_id?: string, description?: string, last_updated_time?: int, schema_version?: float, error_notification?: any, default_state?: string, states?: list, ism_template?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "if_primary_term" $if_primary_term "scalar") (serialize-qp "if_seq_no" $if_seq_no "scalar") (serialize-qp "policyID" $policyID "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_ism/policies" $qp)
  let body = {policy: $policy} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves a specific policy.
#
# GET /_opendistro/_ism/policies/{policy_id}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#put-policy
# operationId: ism.get_policy.0_superseded
@deprecated
export def "opendistro-ism-policies superseded-by-policy_id" [
  policy_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_ism/policies/($policy_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Checks for the existence of a policy.
#
# HEAD /_opendistro/_ism/policies/{policy_id}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#get-policy
# operationId: ism.exists_policy.0_superseded
@deprecated
export def "opendistro-ism-policies superseded-by-policy_id-1" [
  policy_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_ism/policies/($policy_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or updates a policy.
#
# PUT /_opendistro/_ism/policies/{policy_id}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#create-policy
# operationId: ism.put_policy.0_superseded
# --policy shape: {policy_id?: string, description?: string, last_updated_time?: int, schema_version?: float, error_notification?: any, default_state?: string, states?: list, ism_template?: any}
@deprecated
export def "opendistro-ism-policies superseded-by-policy_id-2" [
  policy_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --if-primary-term: float # Only perform the operation if the document has this primary term.
  --if-seq-no: int # Only perform the operation if the document has this sequence number. (format: int64)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --policy: record # The policy defines how an index is managed throughout its lifetime. — shape: {policy_id?: string, description?: string, last_updated_time?: int, schema_version?: float, error_notification?: any, default_state?: string, states?: list, ism_template?: any}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "if_primary_term" $if_primary_term "scalar") (serialize-qp "if_seq_no" $if_seq_no "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_ism/policies/($policy_id)" $qp)
  let body = {policy: $policy} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes a policy.
#
# DELETE /_opendistro/_ism/policies/{policy_id}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#delete-policy
# operationId: ism.delete_policy.0_superseded
@deprecated
export def "opendistro-ism-policies superseded-by-policy_id-3" [
  policy_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_ism/policies/($policy_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Removes a policy from an index.
#
# POST /_opendistro/_ism/remove
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#remove-policy
# operationId: ism.remove_policy.0_superseded
@deprecated
export def "opendistro-ism-remove superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --index: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "index" $index "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_ism/remove" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Removes a policy from an index.
#
# POST /_opendistro/_ism/remove/{index}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#remove-policy
# operationId: ism.remove_policy.1_superseded
@deprecated
export def "opendistro-ism-remove superseded-by-index" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --index: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "index" $index "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_ism/remove/($index)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retries the failed action for an index.
#
# POST /_opendistro/_ism/retry
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#retry-failed-index
# operationId: ism.retry_index.0_superseded
@deprecated
export def "opendistro-ism-retry superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --index: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  state: string # The state to retry.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "index" $index "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_ism/retry" $qp)
  let body = {state: $state} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retries the failed action for an index.
#
# POST /_opendistro/_ism/retry/{index}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/im-plugin/ism/api/#retry-failed-index
# operationId: ism.retry_index.1_superseded
@deprecated
export def "opendistro-ism-retry superseded-by-index" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --index: string
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  state: string # The state to retry.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "index" $index "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_ism/retry/($index)" $qp)
  let body = {state: $state} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Provides information about the current status of the k-NN plugin.
#
# GET /_opendistro/_knn/stats/{stat}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/search-plugins/knn/api/#stats
# operationId: knn.stats.1_superseded
@deprecated
export def "opendistro-knn-stats list" [
  stat: list
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --timeout: string # Operation timeout.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_knn/stats/($stat)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Preloads native library files into memory, reducing initial search latency for specified indexes.
#
# GET /_opendistro/_knn/warmup/{index}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/search-plugins/knn/api/#warmup-operation
# operationId: knn.warmup.0_superseded
@deprecated
export def "opendistro-knn-warmup superseded" [
  index: list
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_knn/warmup/($index)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Provides information about the current status of the k-NN plugin.
#
# GET /_opendistro/_knn/{node_id}/stats/{stat}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/search-plugins/knn/api/#stats
# operationId: knn.stats.3_superseded
@deprecated
export def "opendistro-knn-stats superseded" [
  node_id: list
  stat: list
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --timeout: string # Operation timeout.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "timeout" $timeout "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_knn/($node_id)/stats/($stat)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Executes a PPL query against OpenSearch indexes.
#
# POST /_opendistro/_ppl
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/search-plugins/sql/sql-ppl-api/
# operationId: ppl.query.0_superseded
@deprecated
export def "opendistro-ppl superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --format: string # Specifies the response format (JSON OR YAML).
  --sanitize: oneof<nothing, bool> # Whether to sanitize special characters in the results. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: string # The SQL query string to execute.
  --filter: record # The filter to apply to query results.
  --fetch-size: int # The number of results to return in each response.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "format" $format "scalar") (serialize-qp "sanitize" $sanitize "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_ppl" $qp)
  let body = {query: $body_query, filter: $filter, fetch_size: $fetch_size} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns the execution plan for a PPL query.
#
# POST /_opendistro/_ppl/_explain
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/search-plugins/sql/sql-ppl-api/
# operationId: ppl.explain.0_superseded
@deprecated
export def "opendistro-ppl-explain superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --format: string # Specifies the response format (JSON, YAML).
  --sanitize: oneof<nothing, bool> # Whether to escape special characters in the results. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: string # The SQL query to explain.
  --filter: record # The filter to apply when explaining the query.
  --fetch-size: int # The number of results to analyze.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "format" $format "scalar") (serialize-qp "sanitize" $sanitize "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_ppl/_explain" $qp)
  let body = {query: $body_query, filter: $filter, fetch_size: $fetch_size} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves performance metrics for the PPL plugin.
#
# GET /_opendistro/_ppl/stats
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/search-plugins/sql/monitoring/
# operationId: ppl.get_stats.0_superseded
@deprecated
export def "opendistro-ppl-stats superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --format: string # Specifies the response format (JSON, YAML).
  --sanitize: oneof<nothing, bool> # Whether to escape special characters in the results. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "format" $format "scalar") (serialize-qp "sanitize" $sanitize "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_ppl/stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves filtered performance metrics for the PPL plugin.
#
# POST /_opendistro/_ppl/stats
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/search-plugins/sql/monitoring/
# operationId: ppl.post_stats.1_superseded
@deprecated
export def "opendistro-ppl-stats superseded-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --format: string # Specifies the response format (JSON, YAML).
  --sanitize: oneof<nothing, bool> # Whether to escape special characters in the results. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --start-time: string # When the query execution started.
  --end-time: record # When the query execution ended.
  --cluster-name: record # The name of the cluster executing the query.
  --index: record # The index information for the query.
  --body-query: record # The query information.
  --user: record # The user information.
  --execution-time: record # The time taken to execute the query.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "format" $format "scalar") (serialize-qp "sanitize" $sanitize "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_ppl/stats" $qp)
  let body = {start_time: $start_time, end_time: $end_time, cluster_name: $cluster_name, index: $index, query: $body_query, user: $user, execution_time: $execution_time} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Refreshes search analyzers in real time.
#
# POST /_opendistro/_refresh_search_analyzers/{index}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/im-plugin/refresh-analyzer/
# operationId: ism.refresh_search_analyzers.0_superseded
@deprecated
export def "opendistro-refresh-search-analyzers superseded" [
  index: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_refresh_search_analyzers/($index)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves an index rollup job configuration by ID.
#
# GET /_opendistro/_rollup/jobs/{id}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/im-plugin/index-rollups/rollup-api/#get-an-index-rollup-job
# operationId: rollups.get.0_superseded
@deprecated
export def "opendistro-rollup-jobs superseded-by-id" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_rollup/jobs/($id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or updates an index rollup job configuration.
#
# PUT /_opendistro/_rollup/jobs/{id}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/im-plugin/index-rollups/rollup-api/#create-or-update-an-index-rollup-job
# operationId: rollups.put.0_superseded
# --rollup shape: {rollup_id?: string, enabled?: bool, schedule?: record, last_updated_time?: float, enabled_time?: float, description?: string, schema_version?: float, source_index?: string, target_index?: string, metadata_id?: string, page_size?: float, delay?: float, continuous?: bool, dimensions?: list, metrics?: list, error_notification?: string}
@deprecated
export def "opendistro-rollup-jobs superseded-by-id-1" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --if-primary-term: float # Only performs the operation if the document has the specified primary term.
  --if-seq-no: int # Only performs the operation if the document has the specified sequence number. (format: int64)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-id: string # The unique identifier for a resource.
  --seq-no: int # The sequence number of the document. (format: int64)
  --primary-term: int # The primary term of the document. (format: int64)
  --version: int # format: int64
  --rollup: record # Defines the configuration for a rollup job. — shape: {rollup_id?: string, enabled?: bool, schedule?: record, last_updated_time?: float, enabled_time?: float, description?: string, schema_version?: float, source_index?: string, target_index?: string, metadata_id?: string, page_size?: float, delay?: float, continuous?: bool, dimensions?: list, metrics?: list, error_notification?: string}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "if_primary_term" $if_primary_term "scalar") (serialize-qp "if_seq_no" $if_seq_no "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_rollup/jobs/($id)" $qp)
  let body = {_id: $body_id, _seq_no: $seq_no, _primary_term: $primary_term, _version: $version, rollup: $rollup} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes an index rollup job configuration.
#
# DELETE /_opendistro/_rollup/jobs/{id}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/im-plugin/index-rollups/rollup-api/#delete-an-index-rollup-job
# operationId: rollups.delete.0_superseded
@deprecated
export def "opendistro-rollup-jobs superseded-by-id-2" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_rollup/jobs/($id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves the execution status information for an index rollup job.
#
# GET /_opendistro/_rollup/jobs/{id}/_explain
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/im-plugin/index-rollups/rollup-api/#explain-an-index-rollup-job
# operationId: rollups.explain.0_superseded
@deprecated
export def "opendistro-rollup-jobs-explain superseded" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_rollup/jobs/($id)/_explain" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Starts the execution of an index rollup job.
#
# POST /_opendistro/_rollup/jobs/{id}/_start
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/im-plugin/index-rollups/rollup-api/#start-or-stop-an-index-rollup-job
# operationId: rollups.start.0_superseded
@deprecated
export def "opendistro-rollup-jobs-start superseded" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_rollup/jobs/($id)/_start" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Stops the execution of an index rollup job.
#
# POST /_opendistro/_rollup/jobs/{id}/_stop
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/im-plugin/index-rollups/rollup-api/#start-or-stop-an-index-rollup-job
# operationId: rollups.stop.0_superseded
@deprecated
export def "opendistro-rollup-jobs-stop superseded" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_rollup/jobs/($id)/_stop" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns account information for the current user.
#
# GET /_opendistro/_security/api/account
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#get-account-details
# operationId: security.get_account_details.0_superseded
@deprecated
export def "opendistro-security-account superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/api/account" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Changes the password for the current user.
#
# PUT /_opendistro/_security/api/account
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#change-password
# operationId: security.change_password.0_superseded
@deprecated
export def "opendistro-security-account superseded-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  current_password: string # The current password.
  password: string # The new password to set.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/api/account" $qp)
  let body = {current_password: $current_password, password: $password} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves one action group.
#
# GET /_opendistro/_security/api/actiongroups/{action_group}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#get-action-group
# operationId: security.get_action_group.0_superseded
@deprecated
export def "opendistro-security-actiongroups superseded-by-action_group" [
  action_group: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/actiongroups/($action_group)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or replaces the specified action group.
#
# PUT /_opendistro/_security/api/actiongroups/{action_group}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#create-action-group
# operationId: security.create_action_group.0_superseded
@deprecated
export def "opendistro-security-actiongroups superseded-by-action_group-1" [
  action_group: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --reserved: oneof<nothing, bool>
  --hidden: oneof<nothing, bool>
  --allowed-actions: list
  --type: string
  --description: string
  --static: oneof<nothing, bool>
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/actiongroups/($action_group)" $qp)
  let body = {reserved: $reserved, hidden: $hidden, allowed_actions: $allowed_actions, type: $type, description: $description, static: $static} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Updates the individual attributes of an action group.
#
# PATCH /_opendistro/_security/api/actiongroups/{action_group}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#patch-action-group
# operationId: security.patch_action_group.0_superseded
@deprecated
export def "opendistro-security-actiongroups superseded-by-action_group-2" [
  action_group: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/actiongroups/($action_group)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes the specified action group.
#
# DELETE /_opendistro/_security/api/actiongroups/{action_group}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#delete-action-group
# operationId: security.delete_action_group.0_superseded
@deprecated
export def "opendistro-security-actiongroups superseded-by-action_group-3" [
  action_group: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/actiongroups/($action_group)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Updates the audit configuration.
#
# PUT /_opendistro/_security/api/audit/config
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#audit-logs
# operationId: security.update_audit_configuration.0_superseded
# --compliance shape: {enabled?: bool, write_log_diffs?: bool, read_watched_fields?: any, read_ignore_users?: list, write_watched_indices?: list, write_ignore_users?: list, read_metadata_only?: bool, write_metadata_only?: bool, external_config?: bool, internal_config?: bool}
# --audit shape: {ignore_users?: list, ignore_requests?: list, ignore_headers?: list, ignore_url_params?: list, disabled_rest_categories?: list, disabled_transport_categories?: list, log_request_body?: bool, resolve_indices?: bool, resolve_bulk_requests?: bool, exclude_sensitive_headers?: bool, enable_transport?: bool, enable_rest?: bool}
@deprecated
export def "opendistro-security-audit-config superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --compliance: record # shape: {enabled?: bool, write_log_diffs?: bool, read_watched_fields?: any, read_ignore_users?: list, write_watched_indices?: list, write_ignore_users?: list, read_metadata_only?: bool, write_metadata_only?: bool, external_config?: bool, internal_config?: bool}
  --enabled: oneof<nothing, bool>
  --audit: record # shape: {ignore_users?: list, ignore_requests?: list, ignore_headers?: list, ignore_url_params?: list, disabled_rest_categories?: list, disabled_transport_categories?: list, log_request_body?: bool, resolve_indices?: bool, resolve_bulk_requests?: bool, exclude_sensitive_headers?: bool, enable_transport?: bool, enable_rest?: bool}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/api/audit/config" $qp)
  let body = {compliance: $compliance, enabled: $enabled, audit: $audit} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns the authorization token for the current user.
#
# POST /_opendistro/_security/api/authtoken
# DEPRECATED
# operationId: security.authtoken.0_superseded
@deprecated
export def "opendistro-security-authtoken superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/api/authtoken" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Not supported for the Cache API.
#
# GET /_opendistro/_security/api/cache
# DEPRECATED
# operationId: security.cache.1_superseded
@deprecated
export def "opendistro-security-cache superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/api/cache" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Not supported for the Cache API.
#
# POST /_opendistro/_security/api/cache
# DEPRECATED
# operationId: security.cache.2_superseded
@deprecated
export def "opendistro-security-cache superseded-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/api/cache" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Not supported for the Cache API.
#
# PUT /_opendistro/_security/api/cache
# DEPRECATED
# operationId: security.cache.3_superseded
@deprecated
export def "opendistro-security-cache superseded-2" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/api/cache" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Flushes the Security plugin's user, authentication, and authorization cache.
#
# DELETE /_opendistro/_security/api/cache
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#flush-cache
# operationId: security.flush_cache.0_superseded
@deprecated
export def "opendistro-security-cache superseded-3" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/api/cache" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieve information about the specified internal user.
#
# GET /_opendistro/_security/api/internalusers/{username}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#get-user
# operationId: security.get_user.0_superseded
@deprecated
export def "opendistro-security-internalusers superseded-by-username" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/internalusers/($username)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or replaces the specified user.
#
# PUT /_opendistro/_security/api/internalusers/{username}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#create-user
# operationId: security.create_user.0_superseded
@deprecated
export def "opendistro-security-internalusers superseded-by-username-1" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --password: string
  --hash: string
  --reserved: oneof<nothing, bool>
  --hidden: oneof<nothing, bool>
  --backend-roles: list
  --attributes: record
  --description: string
  --opendistro-security-roles: list
  --static: oneof<nothing, bool>
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/internalusers/($username)" $qp)
  let body = {password: $password, hash: $hash, reserved: $reserved, hidden: $hidden, backend_roles: $backend_roles, attributes: $attributes, description: $description, opendistro_security_roles: $opendistro_security_roles, static: $static} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Updates individual attributes for an internal user.
#
# PATCH /_opendistro/_security/api/internalusers/{username}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#patch-user
# operationId: security.patch_user.0_superseded
@deprecated
export def "opendistro-security-internalusers superseded-by-username-2" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/internalusers/($username)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes the specified internal user.
#
# DELETE /_opendistro/_security/api/internalusers/{username}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#delete-user
# operationId: security.delete_user.0_superseded
@deprecated
export def "opendistro-security-internalusers superseded-by-username-3" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/internalusers/($username)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Generates an authorization token for the specified user.
#
# POST /_opendistro/_security/api/internalusers/{username}/authtoken
# DEPRECATED
# operationId: security.generate_user_token.0_superseded
@deprecated
export def "opendistro-security-internalusers-authtoken superseded" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/internalusers/($username)/authtoken" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Migrates the security configuration from v6 to v7.
#
# POST /_opendistro/_security/api/migrate
# DEPRECATED
# operationId: security.migrate.0_superseded
@deprecated
export def "opendistro-security-migrate superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/api/migrate" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves the evaluated REST API permissions for the currently logged in user.
#
# GET /_opendistro/_security/api/permissionsinfo
# DEPRECATED
# operationId: security.get_permissions_info.0_superseded
@deprecated
export def "opendistro-security-permissionsinfo superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/api/permissionsinfo" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves one role.
#
# GET /_opendistro/_security/api/roles/{role}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#get-role
# operationId: security.get_role.0_superseded
@deprecated
export def "opendistro-security-roles superseded-by-role" [
  role: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/roles/($role)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or replaces the specified role.
#
# PUT /_opendistro/_security/api/roles/{role}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#create-role
# operationId: security.create_role.0_superseded
# --index_permissions item shape: {index_patterns?: list, dls?: string, fls?: list, masked_fields?: list, allowed_actions?: list}
# --tenant_permissions item shape: {tenant_patterns?: list, allowed_actions?: list}
@deprecated
export def "opendistro-security-roles superseded-by-role-1" [
  role: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --reserved: oneof<nothing, bool>
  --hidden: oneof<nothing, bool>
  --description: string
  --cluster-permissions: list
  --index-permissions: list # item shape: {index_patterns?: list, dls?: string, fls?: list, masked_fields?: list, allowed_actions?: list}
  --tenant-permissions: list # item shape: {tenant_patterns?: list, allowed_actions?: list}
  --static: oneof<nothing, bool>
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/roles/($role)" $qp)
  let body = {reserved: $reserved, hidden: $hidden, description: $description, cluster_permissions: $cluster_permissions, index_permissions: $index_permissions, tenant_permissions: $tenant_permissions, static: $static} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Updates the individual attributes of a role.
#
# PATCH /_opendistro/_security/api/roles/{role}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#patch-role
# operationId: security.patch_role.0_superseded
@deprecated
export def "opendistro-security-roles superseded-by-role-2" [
  role: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/roles/($role)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes the specified role.
#
# DELETE /_opendistro/_security/api/roles/{role}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#delete-role
# operationId: security.delete_role.0_superseded
@deprecated
export def "opendistro-security-roles superseded-by-role-3" [
  role: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/roles/($role)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves the specified role mapping.
#
# GET /_opendistro/_security/api/rolesmapping/{role}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#get-role-mapping
# operationId: security.get_role_mapping.0_superseded
@deprecated
export def "opendistro-security-rolesmapping superseded-by-role" [
  role: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/rolesmapping/($role)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or replaces the specified role mapping.
#
# PUT /_opendistro/_security/api/rolesmapping/{role}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#create-role-mapping
# operationId: security.create_role_mapping.0_superseded
@deprecated
export def "opendistro-security-rolesmapping superseded-by-role-1" [
  role: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --hosts: list
  --users: list
  --reserved: oneof<nothing, bool>
  --hidden: oneof<nothing, bool>
  --backend-roles: list
  --and-backend-roles: list
  --description: string
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/rolesmapping/($role)" $qp)
  let body = {hosts: $hosts, users: $users, reserved: $reserved, hidden: $hidden, backend_roles: $backend_roles, and_backend_roles: $and_backend_roles, description: $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Updates the individual attributes of a role mapping.
#
# PATCH /_opendistro/_security/api/rolesmapping/{role}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#patch-role-mapping
# operationId: security.patch_role_mapping.0_superseded
@deprecated
export def "opendistro-security-rolesmapping superseded-by-role-2" [
  role: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/rolesmapping/($role)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes the specified role mapping.
#
# DELETE /_opendistro/_security/api/rolesmapping/{role}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#delete-role-mapping
# operationId: security.delete_role_mapping.0_superseded
@deprecated
export def "opendistro-security-rolesmapping superseded-by-role-3" [
  role: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/rolesmapping/($role)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns the current Security plugin configuration in a JSON format.
#
# GET /_opendistro/_security/api/securityconfig
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#get-configuration
# operationId: security.get_configuration.0_superseded
@deprecated
export def "opendistro-security-securityconfig superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/api/securityconfig" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Updates the existing security configuration using the REST API. Requires super admin or REST API permissions.
#
# PATCH /_opendistro/_security/api/securityconfig
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#patch-configuration
# operationId: security.patch_configuration.0_superseded
@deprecated
export def "opendistro-security-securityconfig superseded-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/api/securityconfig" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Updates the settings for an existing security configuration. Requires super admin or REST API permissions.
#
# PUT /_opendistro/_security/api/securityconfig/config
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#update-configuration
# operationId: security.update_configuration.0_superseded
# --dynamic shape: {filtered_alias_mode?: string, disable_rest_auth?: bool, disable_intertransport_auth?: bool, respect_request_indices_options?: bool, opensearch-dashboards?: record, kibana?: record, http?: record, authc?: record, authz?: record, auth_failure_listeners?: record, do_not_fail_on_forbidden?: bool, multi_rolespan_enabled?: bool, hosts_resolver_mode?: string, do_not_fail_on_forbidden_empty?: bool, on_behalf_of?: record}
@deprecated
export def "opendistro-security-securityconfig-config superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --dynamic: record # shape: {filtered_alias_mode?: string, disable_rest_auth?: bool, disable_intertransport_auth?: bool, respect_request_indices_options?: bool, opensearch-dashboards?: record, kibana?: record, http?: record, authc?: record, authz?: record, auth_failure_listeners?: record, do_not_fail_on_forbidden?: bool, multi_rolespan_enabled?: bool, hosts_resolver_mode?: string, do_not_fail_on_forbidden_empty?: bool, on_behalf_of?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/api/securityconfig/config" $qp)
  let body = {dynamic: $dynamic} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves the cluster security certificates.
#
# GET /_opendistro/_security/api/ssl/certs
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#get-certificates
# operationId: security.get_certificates.0_superseded
@deprecated
export def "opendistro-security-ssl-certs superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/api/ssl/certs" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Reloads the transport communication certificates.
#
# PUT /_opendistro/_security/api/ssl/transport/reloadcerts
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#reload-transport-certificates
# operationId: security.reload_transport_certificates.0_superseded
@deprecated
export def "opendistro-security-ssl-transport-reloadcerts superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/api/ssl/transport/reloadcerts" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Reloads the HTTP communication certificates.
#
# PUT /_opendistro/_security/api/ssl/http/reloadcerts
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#reload-http-certificates
# operationId: security.reload_http_certificates.0_superseded
@deprecated
export def "opendistro-security-ssl-http-reloadcerts superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/api/ssl/http/reloadcerts" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves all node distinguished names. Requires super admin or REST API permissions.
#
# GET /_opendistro/_security/api/nodesdn/{cluster_name}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#get-distinguished-names
# operationId: security.get_distinguished_name.0_superseded
@deprecated
export def "opendistro-security-nodesdn superseded-by-cluster_name" [
  cluster_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --show-all: oneof<nothing, bool> # Whether to include or exclude any static node's DN settings from the final result.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "show_all" $show_all "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/nodesdn/($cluster_name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Adds or updates the specified distinguished names in the cluster or node allowlist. Requires super admin or REST API permissions.
#
# PUT /_opendistro/_security/api/nodesdn/{cluster_name}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#update-distinguished-names
# operationId: security.update_distinguished_name.0_superseded
@deprecated
export def "opendistro-security-nodesdn superseded-by-cluster_name-1" [
  cluster_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --nodes-dn: list
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/nodesdn/($cluster_name)" $qp)
  let body = {nodes_dn: $nodes_dn} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Updates the distinguished cluster name for the specified cluster. Requires super admin or REST API permissions.
#
# PATCH /_opendistro/_security/api/nodesdn/{cluster_name}
# DEPRECATED
# operationId: security.patch_distinguished_name.0_superseded
@deprecated
export def "opendistro-security-nodesdn superseded-by-cluster_name-2" [
  cluster_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/nodesdn/($cluster_name)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes all distinguished names in the specified cluster or node allowlist. Requires super admin or REST API permissions.
#
# DELETE /_opendistro/_security/api/nodesdn/{cluster_name}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#delete-distinguished-names
# operationId: security.delete_distinguished_name.0_superseded
@deprecated
export def "opendistro-security-nodesdn superseded-by-cluster_name-3" [
  cluster_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/nodesdn/($cluster_name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves the multi-tenancy configuration. Requires super admin or REST API permissions.
#
# GET /_opendistro/_security/api/tenancy/config
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/multi-tenancy/dynamic-config/#configuring-multi-tenancy-with-the-rest-api
# operationId: security.get_tenancy_config.0_superseded
@deprecated
export def "opendistro-security-tenancy-config superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/api/tenancy/config" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or replaces the multi-tenancy configuration. Requires super admin or REST API permissions.
#
# PUT /_opendistro/_security/api/tenancy/config
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/multi-tenancy/dynamic-config/#configuring-multi-tenancy-with-the-rest-api
# operationId: security.create_update_tenancy_config.0_superseded
@deprecated
export def "opendistro-security-tenancy-config superseded-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --default-tenant: string
  --private-tenant-enabled: oneof<nothing, bool>
  --multitenancy-enabled: oneof<nothing, bool>
  --sign-in-options: list
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/api/tenancy/config" $qp)
  let body = {default_tenant: $default_tenant, private_tenant_enabled: $private_tenant_enabled, multitenancy_enabled: $multitenancy_enabled, sign_in_options: $sign_in_options} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves all tenants.
#
# GET /_opendistro/_security/api/tenants
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#get-tenants
# operationId: security.get_tenants.0_superseded
@deprecated
export def "opendistro-security-tenants superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/api/tenants" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Adds, deletes, or modifies multiple tenants in a single request.
#
# PATCH /_opendistro/_security/api/tenants
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#patch-tenants
# operationId: security.patch_tenants.0_superseded
@deprecated
export def "opendistro-security-tenants superseded-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/api/tenants" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves the specified tenant.
#
# GET /_opendistro/_security/api/tenants/{tenant}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#get-tenant
# operationId: security.get_tenant.0_superseded
@deprecated
export def "opendistro-security-tenants superseded-by-tenant" [
  tenant: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/tenants/($tenant)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or replaces the specified tenant.
#
# PUT /_opendistro/_security/api/tenants/{tenant}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#create-tenant
# operationId: security.create_tenant.0_superseded
@deprecated
export def "opendistro-security-tenants superseded-by-tenant-1" [
  tenant: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --description: string
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/tenants/($tenant)" $qp)
  let body = {description: $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Adds, deletes, or modifies a single tenant.
#
# PATCH /_opendistro/_security/api/tenants/{tenant}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#patch-tenant
# operationId: security.patch_tenant.0_superseded
@deprecated
export def "opendistro-security-tenants superseded-by-tenant-2" [
  tenant: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/tenants/($tenant)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes the specified tenant.
#
# DELETE /_opendistro/_security/api/tenants/{tenant}
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#delete-action-group
# operationId: security.delete_tenant.0_superseded
@deprecated
export def "opendistro-security-tenants superseded-by-tenant-3" [
  tenant: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/tenants/($tenant)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieve all internal users. Legacy API.
#
# GET /_opendistro/_security/api/user
# DEPRECATED
# operationId: security.get_users_legacy.0_superseded
@deprecated
export def "opendistro-security-user superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/api/user" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieve one user. Legacy API.
#
# GET /_opendistro/_security/api/user/{username}
# DEPRECATED
# operationId: security.get_user_legacy.0_superseded
@deprecated
export def "opendistro-security-user superseded-by-username" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/user/($username)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or replaces the specified user. Legacy API.
#
# PUT /_opendistro/_security/api/user/{username}
# DEPRECATED
# operationId: security.create_user_legacy.0_superseded
@deprecated
export def "opendistro-security-user superseded-by-username-1" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --password: string
  --hash: string
  --reserved: oneof<nothing, bool>
  --hidden: oneof<nothing, bool>
  --backend-roles: list
  --attributes: record
  --description: string
  --opendistro-security-roles: list
  --static: oneof<nothing, bool>
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/user/($username)" $qp)
  let body = {password: $password, hash: $hash, reserved: $reserved, hidden: $hidden, backend_roles: $backend_roles, attributes: $attributes, description: $description, opendistro_security_roles: $opendistro_security_roles, static: $static} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete the specified user. Legacy API.
#
# DELETE /_opendistro/_security/api/user/{username}
# DEPRECATED
# operationId: security.delete_user_legacy.0_superseded
@deprecated
export def "opendistro-security-user superseded-by-username-2" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/user/($username)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Generates authorization token for the given user. Legacy API. Not Implemented.
#
# POST /_opendistro/_security/api/user/{username}/authtoken
# DEPRECATED
# operationId: security.generate_user_token_legacy.0_superseded
@deprecated
export def "opendistro-security-user-authtoken superseded" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_opendistro/_security/api/user/($username)/authtoken" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Checks whether the v6 security configuration is valid and ready to be migrated to v7.
#
# GET /_opendistro/_security/api/validate
# DEPRECATED
# operationId: security.validate.0_superseded
@deprecated
export def "opendistro-security-validate superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --accept-invalid: oneof<nothing, bool> # Whether an invalid v6 configuration should be allowed.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "accept_invalid" $accept_invalid "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/api/validate" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves the current list of allowed APIs accessible to a normal user.
#
# GET /_opendistro/_security/api/allowlist
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#access-control-for-the-api
# operationId: security.get_allowlist.0_superseded
@deprecated
export def "opendistro-security-allowlist superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/api/allowlist" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates or replaces APIs permitted for users on the allow list. Requires a super admin certificate or REST API permissions.
#
# PUT /_opendistro/_security/api/allowlist
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#access-control-for-the-api
# operationId: security.create_allowlist.0_superseded
@deprecated
export def "opendistro-security-allowlist superseded-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --enabled: oneof<nothing, bool>
  --requests: record # An object with APIs as key and array of HTTP methods as values.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/api/allowlist" $qp)
  let body = {enabled: $enabled, requests: $requests} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Updates the current list of APIs accessible for users on the allow list.
#
# PATCH /_opendistro/_security/api/allowlist
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#access-control-for-the-api
# operationId: security.patch_allowlist.0_superseded
@deprecated
export def "opendistro-security-allowlist superseded-2" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/api/allowlist" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns or updates authentication information for the currently authenticated user.
#
# GET /_opendistro/_security/authinfo
# DEPRECATED
# operationId: security.authinfo.0_superseded
@deprecated
export def "opendistro-security-authinfo superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --auth-type: string # The type of the current authentication request.
  --verbose: oneof<nothing, bool> # Whether to return a verbose response.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "auth_type" $auth_type "scalar") (serialize-qp "verbose" $verbose "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/authinfo" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns or updates authentication information for the currently authenticated user.
#
# POST /_opendistro/_security/authinfo
# DEPRECATED
# operationId: security.authinfo.1_superseded
@deprecated
export def "opendistro-security-authinfo superseded-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --auth-type: string # The type of the current authentication request.
  --verbose: oneof<nothing, bool> # Whether to return a verbose response.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "auth_type" $auth_type "scalar") (serialize-qp "verbose" $verbose "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/authinfo" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Checks to see if the Security plugin is running.
#
# GET /_opendistro/_security/health
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#health-check
# operationId: security.health.0_superseded
@deprecated
export def "opendistro-security-health superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --mode: string # A flag that determines whether to consider the security status before returning a response for a health query response. For example, `strict` mode indicates service should check the Security plugin status.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "mode" $mode "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/health" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Checks to see if the Security plugin is running.
#
# POST /_opendistro/_security/health
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/security/access-control/api/#health-check
# operationId: security.health.1_superseded
@deprecated
export def "opendistro-security-health superseded-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --mode: string # A flag that determines whether to consider the security status before returning a response for a health query response. For example, `strict` mode indicates service should check the Security plugin status.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "mode" $mode "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/health" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves the current values for dynamic security settings for OpenSearch Dashboards.
#
# GET /_opendistro/_security/kibanainfo
# DEPRECATED
# operationId: security.get_dashboards_info.0_superseded
@deprecated
export def "opendistro-security-kibanainfo superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/kibanainfo" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves the current values for dynamic security settings for OpenSearch Dashboards.
#
# POST /_opendistro/_security/kibanainfo
# DEPRECATED
# operationId: security.post_dashboards_info.1_superseded
@deprecated
export def "opendistro-security-kibanainfo superseded-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/kibanainfo" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves the names of current tenants. Requires super admin or `kibanaserver` permissions.
#
# GET /_opendistro/_security/tenantinfo
# DEPRECATED
# operationId: security.tenant_info.0_superseded
@deprecated
export def "opendistro-security-tenantinfo superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/tenantinfo" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves the names of current tenants. Requires super admin or `kibanaserver` permissions.
#
# POST /_opendistro/_security/tenantinfo
# DEPRECATED
# operationId: security.tenant_info.1_superseded
@deprecated
export def "opendistro-security-tenantinfo superseded-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_security/tenantinfo" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Executes SQL or PPL queries against OpenSearch indexes.
#
# POST /_opendistro/_sql
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/search-plugins/sql/sql-ppl-api/
# operationId: sql.query.0_superseded
@deprecated
export def "opendistro-sql superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --format: string # Specifies the response format (JSON or YAML).
  --sanitize: oneof<nothing, bool> # Whether to escape special characters in the results. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: string # The SQL query string to execute.
  --filter: record # The filter to apply to query results.
  --fetch-size: int # The number of results to return in each response.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "format" $format "scalar") (serialize-qp "sanitize" $sanitize "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_sql" $qp)
  let body = {query: $body_query, filter: $filter, fetch_size: $fetch_size} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns the execution plan for a SQL or PPL query.
#
# POST /_opendistro/_sql/_explain
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/search-plugins/sql/sql-ppl-api/
# operationId: sql.explain.0_superseded
@deprecated
export def "opendistro-sql-explain superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --format: string # Specifies the response format (JSON or YAML).
  --sanitize: oneof<nothing, bool> # Whether to escape special characters in the results. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --body-query: string # The SQL query to explain.
  --filter: record # The filter to apply when explaining the query.
  --fetch-size: int # The number of results to analyze.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "format" $format "scalar") (serialize-qp "sanitize" $sanitize "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_sql/_explain" $qp)
  let body = {query: $body_query, filter: $filter, fetch_size: $fetch_size} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Closes an open cursor to free server-side resources.
#
# POST /_opendistro/_sql/close
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/search-plugins/sql/sql-ppl-api/
# operationId: sql.close.0_superseded
@deprecated
export def "opendistro-sql-close superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --format: string # Specifies the response format (JSON or YAML).
  --sanitize: oneof<nothing, bool> # Whether to escape special characters in the results. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --cursor: string # The cursor identifier to close.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "format" $format "scalar") (serialize-qp "sanitize" $sanitize "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_sql/close" $qp)
  let body = {cursor: $cursor} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Updates SQL plugin settings in the OpenSearch cluster configuration.
#
# PUT /_opendistro/_sql/settings
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/search-plugins/sql/settings/
# operationId: sql.settings.0_superseded
# --transient shape: {plugins.sql.enabled?: bool, plugins.ppl.enabled?: bool, plugins.sql.slowlog?: int, plugins.sql.cursor.keep_alive?: string, plugins.query.memory_limit?: string, plugins.query.size_limit?: int}
@deprecated
export def "opendistro-sql-settings superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --format: string # Specifies the response format (JSON or YAML).
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --transient: record # shape: {plugins.sql.enabled?: bool, plugins.ppl.enabled?: bool, plugins.sql.slowlog?: int, plugins.sql.cursor.keep_alive?: string, plugins.query.memory_limit?: string, plugins.query.size_limit?: int}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "format" $format "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_sql/settings" $qp)
  let body = {transient: $transient} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieves performance metrics for the SQL plugin.
#
# GET /_opendistro/_sql/stats
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/search-plugins/sql/monitoring/
# operationId: sql.get_stats.0_superseded
@deprecated
export def "opendistro-sql-stats superseded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --format: string # Specifies the response format (JSON or YAML).
  --sanitize: oneof<nothing, bool> # Whether to escape special characters in the results. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "format" $format "scalar") (serialize-qp "sanitize" $sanitize "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_sql/stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieves filtered performance metrics for the SQL plugin.
#
# POST /_opendistro/_sql/stats
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/search-plugins/sql/monitoring/
# operationId: sql.post_stats.1_superseded
@deprecated
export def "opendistro-sql-stats superseded-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --format: string # Specifies the response format (JSON or YAML).
  --sanitize: oneof<nothing, bool> # Whether to escape special characters in the results. (default: true)
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
  --start-time: string # When the query execution started.
  --end-time: record # When the query execution ended.
  --cluster-name: record # The name of the cluster executing the query.
  --index: record # The index information for the query.
  --body-query: record # The query information.
  --user: record # The user information.
  --execution-time: record # The time taken to execute the query.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "format" $format "scalar") (serialize-qp "sanitize" $sanitize "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_opendistro/_sql/stats" $qp)
  let body = {start_time: $start_time, end_time: $end_time, cluster_name: $cluster_name, index: $index, query: $body_query, user: $user, execution_time: $execution_time} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Returns information about hot threads on each node in the cluster.
#
# GET /_cluster/nodes/hotthreads
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/api-reference/nodes-apis/nodes-hot-threads/
# operationId: nodes.hot_threads.0_superseded
@deprecated
export def "cluster-nodes-hotthreads list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --ignore-idle-threads: oneof<nothing, bool> # Whether to show threads that are in known-idle places, such as waiting on a socket select or pulling from an empty task queue. (default: true)
  --interval: string # The time interval between thread stack trace samples.
  --snapshots: int # The number of thread stack trace samples to collect. (format: int32, default: 10)
  --threads: int # The number of threads to provide information for. (format: int32, default: 3)
  --timeout: string # The amount of time to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --type: string # The type to sample.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "ignore_idle_threads" $ignore_idle_threads "scalar") (serialize-qp "interval" $interval "scalar") (serialize-qp "snapshots" $snapshots "scalar") (serialize-qp "threads" $threads "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_cluster/nodes/hotthreads" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about hot threads on each node in the cluster.
#
# GET /_cluster/nodes/{node_id}/hotthreads
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/api-reference/nodes-apis/nodes-hot-threads/
# operationId: nodes.hot_threads.2_superseded
@deprecated
export def "cluster-nodes-hotthreads superseded" [
  node_id: list
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --ignore-idle-threads: oneof<nothing, bool> # Whether to show threads that are in known-idle places, such as waiting on a socket select or pulling from an empty task queue. (default: true)
  --interval: string # The time interval between thread stack trace samples.
  --snapshots: int # The number of thread stack trace samples to collect. (format: int32, default: 10)
  --threads: int # The number of threads to provide information for. (format: int32, default: 3)
  --timeout: string # The amount of time to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --type: string # The type to sample.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "ignore_idle_threads" $ignore_idle_threads "scalar") (serialize-qp "interval" $interval "scalar") (serialize-qp "snapshots" $snapshots "scalar") (serialize-qp "threads" $threads "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_cluster/nodes/($node_id)/hotthreads" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about hot threads on each node in the cluster.
#
# GET /_nodes/{node_id}/hotthreads
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/api-reference/nodes-apis/nodes-hot-threads/
# operationId: nodes.hot_threads.6_superseded
@deprecated
export def "nodes-hotthreads superseded" [
  node_id: list
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --ignore-idle-threads: oneof<nothing, bool> # Whether to show threads that are in known-idle places, such as waiting on a socket select or pulling from an empty task queue. (default: true)
  --interval: string # The time interval between thread stack trace samples.
  --snapshots: int # The number of thread stack trace samples to collect. (format: int32, default: 10)
  --threads: int # The number of threads to provide information for. (format: int32, default: 3)
  --timeout: string # The amount of time to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --type: string # The type to sample.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "ignore_idle_threads" $ignore_idle_threads "scalar") (serialize-qp "interval" $interval "scalar") (serialize-qp "snapshots" $snapshots "scalar") (serialize-qp "threads" $threads "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/_nodes/($node_id)/hotthreads" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Returns information about hot threads on each node in the cluster.
#
# GET /_nodes/hotthreads
# DEPRECATED
# Docs: https://opensearch.org/docs/latest/api-reference/nodes-apis/nodes-hot-threads/
# operationId: nodes.hot_threads.4_superseded
@deprecated
export def "nodes-hotthreads list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --ignore-idle-threads: oneof<nothing, bool> # Whether to show threads that are in known-idle places, such as waiting on a socket select or pulling from an empty task queue. (default: true)
  --interval: string # The time interval between thread stack trace samples.
  --snapshots: int # The number of thread stack trace samples to collect. (format: int32, default: 10)
  --threads: int # The number of threads to provide information for. (format: int32, default: 3)
  --timeout: string # The amount of time to wait for a response. If no response is received before the timeout expires, the request fails and returns an error.
  --type: string # The type to sample.
  --pretty: oneof<nothing, bool> # Whether to pretty format the returned JSON response. (default: false)
  --human: oneof<nothing, bool> # Whether to return human readable values for statistics. (default: true)
  --error-trace: oneof<nothing, bool> # Whether to include the stack trace of returned errors. (default: false)
  --qp-source: string # The URL-encoded request definition. Useful for libraries that do not accept a request body for non-POST requests.
  --filter-path: string # Used to reduce the response. This parameter takes a comma-separated list of filters. It supports using wildcards to match any field or part of a field’s name. You can also exclude fields with "-".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "ignore_idle_threads" $ignore_idle_threads "scalar") (serialize-qp "interval" $interval "scalar") (serialize-qp "snapshots" $snapshots "scalar") (serialize-qp "threads" $threads "scalar") (serialize-qp "timeout" $timeout "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "pretty" $pretty "scalar") (serialize-qp "human" $human "scalar") (serialize-qp "error_trace" $error_trace "scalar") (serialize-qp "source" $qp_source "scalar") (serialize-qp "filter_path" $filter_path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/_nodes/hotthreads" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}
