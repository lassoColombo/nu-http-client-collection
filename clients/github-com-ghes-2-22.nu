# Auto-generated client for GitHub v3 REST API v1.1.4
# Source: https://api.apis.guru/v2/specs/github.com/ghes-2.22/1.1.4/openapi.json
# Auth: --token flag or $env.GITHUB_V3_REST_API_TOKEN

const BASE_URL = "http://HOSTNAME/api/v3"
const DEFAULT_AUTH = "bearer"

# Build auth: returns {headers: record, query: string}
def build-auth [token?: string, auth_scheme?: string]: nothing -> record {
  let token_val = if ($token != null) and ($token | is-not-empty) { $token } else { $env | get -o GITHUB_V3_REST_API_TOKEN | default "" }
  let scheme = ($auth_scheme | default "bearer")
  if ($scheme == "none") or ($token_val | is-empty) { return {headers: {}, query: ""} }
  match $scheme {
    "none" => { {headers: {}, query: ""} }
    _ => { {headers: {Authorization: $"Bearer ($token_val)"}, query: ""} }
  }
}

# Serialize a single query parameter based on collection style
def serialize-qp [name: string, value: any, style: string]: nothing -> list<string> {
  if ($value == null) { return [] }
  let n = ($name | url encode)
  let is_list = ($value | describe | str starts-with "list")
  if ($value | describe | str starts-with "record") { return ($value | transpose k v | each { $"($n)[($in.k | into string | url encode)]=($in.v | into string | url encode)" }) }
  if not $is_list { return [$"($n)=($value | into string | url encode)"] }
  match $style {
    "multi" => { $value | each {|v| $"($n)=($v | into string | url encode)" } }
    "csv" => { let joined = ($value | each { $in | into string | url encode } | str join ","); [$"($n)=($joined)"] }
    "ssv" => { let joined = ($value | each { $in | into string | url encode } | str join "%20"); [$"($n)=($joined)"] }
    "tsv" => { let joined = ($value | each { $in | into string | url encode } | str join "%09"); [$"($n)=($joined)"] }
    "pipes" => { let joined = ($value | each { $in | into string | url encode } | str join "|"); [$"($n)=($joined)"] }
    "deepObject" => { $value | each {|v| $"($n)[]=($v | into string | url encode)" } }
    _ => { $value | each {|v| $"($n)=($v | into string | url encode)" } }
  }
}

# Percent-encode a path-segment value per RFC 3986.
# Unreserved chars ([A-Za-z0-9-._~]) stay literal; everything else gets %XX.
# Trick: `url encode --all` over-encodes, then we decode the four unreserved
# punctuation chars back. Pre-existing %XX sequences in the input survive
# because `url encode --all` first turns their % into %25.
def encode-path-segment [v: any]: nothing -> string {
  $v | into string | url encode --all | str replace --all "%2D" "-" | str replace --all "%2E" "." | str replace --all "%5F" "_" | str replace --all "%7E" "~"
}

# Build URL from base, path, and optional query string
def build-url [base: string, path: string, query?: string]: nothing -> string {
  let parsed = ($base | url parse | reject params)
  let full_path = if ($path | is-empty) { $parsed.path } else { [$parsed.path $path] | str join "/" | str replace --all --regex '/+' '/' }
  let result = ($parsed | upsert path $full_path)
  if ($query != null) and ($query | is-not-empty) { $result | upsert query $query | url join } else { $result | url join }
}

# Execute HTTP request with method dispatch
def do-request [method: string, url: string, auth: record, insecure: bool, raw: bool, dry_run: bool, max_time?: duration, allow_errors?: bool, content_type?: string, body?: any]: nothing -> any {
  let req_url = if ($auth.query | is-not-empty) { if ($url | str contains "?") { $"($url)&($auth.query)" } else { $"($url)?($auth.query)" } } else { $url }
  let timeout = ($max_time | default 30min)
  let ct = ($content_type | default "application/json")
  if $dry_run { return {method: $method, url: $req_url, headers: $auth.headers, query_string: $auth.query, content_type: $ct, timeout: $timeout, body: $body} }
  let resp = match $method {
    "get" => { http get --headers $auth.headers --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url }
    "head" => { http head --headers $auth.headers --max-time $timeout --insecure=$insecure $req_url }
    "options" => { http options --headers $auth.headers --max-time $timeout --insecure=$insecure $req_url }
    "post" => { http post --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "put" => { http put --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "patch" => { http patch --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "delete" => { if ($body | is-empty) { http delete --headers $auth.headers --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url } else { http delete --headers $auth.headers --content-type $ct --data $body --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url } }
  }
  if ($method in ["head" "options"]) { return $resp }
  if $allow_errors { $resp } else if $resp.status == 204 { null } else if $resp.status >= 400 { error make --unspanned { msg: $"HTTP ($resp.status): ($resp.body)" } } else { $resp.body }
}

def base-url-completer [] { ["http://HOSTNAME/api/v3" "https://uploads.github.com" "{origin}"] }
def auth-scheme-completer [] { ["bearer"] }

# Completers for enum parameters
def direction-completer [] { ["asc" "desc"] }
def sort-completer [] { ["accessed" "created" "updated"] }
def sort-completer-1 [] { ["created" "name" "updated"] }
def visibility-completer [] { ["all" "selected"] }
def filter-completer [] { ["all" "assigned" "created" "mentioned" "repos" "subscribed"] }
def state-completer [] { ["all" "closed" "open"] }
def sort-completer-2 [] { ["comments" "created" "updated"] }
def mode-completer [] { ["gfm" "markdown"] }
def default-repository-permission-completer [] { ["admin" "none" "read" "write"] }
def members-allowed-repository-creation-type-completer [] { ["all" "none" "private"] }
def visibility-completer-1 [] { ["all" "private" "selected"] }
def filter-completer-1 [] { ["2fa_disabled" "all"] }
def role-completer [] { ["admin" "all" "member"] }
def role-completer-1 [] { ["admin" "member"] }
def type-completer [] { ["all" "forks" "internal" "member" "private" "public" "sources"] }
def sort-completer-3 [] { ["created" "full_name" "pushed" "updated"] }
def visibility-completer-2 [] { ["internal" "private" "public"] }
def permission-completer [] { ["admin" "pull" "push"] }
def privacy-completer [] { ["closed" "secret"] }
def content-completer [] { ["+1" "-1" "confused" "eyes" "heart" "hooray" "laugh" "rocket"] }
def role-completer-2 [] { ["all" "maintainer" "member"] }
def role-completer-3 [] { ["maintainer" "member"] }
def permission-completer-1 [] { ["admin" "read" "write"] }
def permission-completer-2 [] { ["admin" "maintain" "pull" "push" "triage"] }
def archived-state-completer [] { ["all" "archived" "not_archived"] }
def organization-permission-completer [] { ["admin" "none" "read" "write"] }
def affiliation-completer [] { ["all" "direct" "outside"] }
def status-completer [] { ["action_required" "cancelled" "completed" "failure" "in_progress" "neutral" "queued" "requested" "skipped" "stale" "success" "timed_out" "waiting"] }
def filter-completer-2 [] { ["all" "latest"] }
def conclusion-completer [] { ["action_required" "cancelled" "failure" "neutral" "skipped" "stale" "success" "timed_out"] }
def status-completer-1 [] { ["completed" "in_progress" "queued"] }
def state-completer-1 [] { ["closed" "dismissed" "fixed" "open"] }
def dismissed-reason-completer [] { ["" "false positive" "used in tests" "won't fix"] }
def state-completer-2 [] { ["dismissed" "open"] }
def accept-completer [] { ["application/json" "application/vnd.github.v3.object"] }
def environment-completer [] { ["production" "qa" "staging"] }
def state-completer-3 [] { ["error" "failure" "in_progress" "inactive" "pending" "queued" "success"] }
def sort-completer-4 [] { ["newest" "oldest" "stargazers" "watchers"] }
def type-completer-1 [] { ["blob" "commit" "tree"] }
def permissions-completer [] { ["admin" "maintain" "read" "triage" "write"] }
def sort-completer-5 [] { ["created" "updated"] }
def state-completer-4 [] { ["closed" "open"] }
def lock-reason-completer [] { ["off-topic" "resolved" "spam" "too heated"] }
def sort-completer-6 [] { ["completeness" "due_on"] }
def enforcement-completer [] { ["disabled" "enabled" "testing"] }
def sort-completer-7 [] { ["created" "long-running" "popularity" "updated"] }
def sort-completer-8 [] { ["created" "created_at" "updated"] }
def side-completer [] { ["LEFT" "RIGHT"] }
def start-side-completer [] { ["LEFT" "RIGHT" "side"] }
def merge-method-completer [] { ["merge" "rebase" "squash"] }
def event-completer [] { ["APPROVE" "COMMENT" "REQUEST_CHANGES"] }
def state-completer-5 [] { ["error" "failure" "pending" "success"] }
def visibility-completer-3 [] { ["all" "public"] }
def sort-completer-9 [] { ["indexed"] }
def order-completer [] { ["asc" "desc"] }
def sort-completer-10 [] { ["author-date" "committer-date"] }
def sort-completer-11 [] { ["comments" "created" "interactions" "reactions" "reactions-+1" "reactions--1" "reactions-heart" "reactions-smile" "reactions-tada" "reactions-thinking_face" "updated"] }
def sort-completer-12 [] { ["forks" "help-wanted-issues" "stars" "updated"] }
def sort-completer-13 [] { ["followers" "joined" "repositories"] }
def state-completer-6 [] { ["active" "pending"] }
def state-completer-7 [] { ["active"] }
def visibility-completer-4 [] { ["all" "private" "public"] }
def type-completer-2 [] { ["all" "member" "owner" "private" "public"] }
def accept-completer-1 [] { ["application/json" "application/vnd.github.v3.star+json"] }
def subject-type-completer [] { ["issue" "organization" "pull_request" "repository"] }
def type-completer-3 [] { ["all" "member" "owner"] }

# List all available API commands with their parameters
export def commands []: nothing -> table {
  let builtin_flags = ["base-url" "token" "auth-scheme" "insecure" "max-time" "raw" "allow-errors" "dry-run" "accept" "help"]
  let mod_name = (scope modules | where { $in.commands | any { $in.name == "meta get-root" } } | get name | first)
  let mod_cmds = (scope modules | where name == $mod_name | get commands | first)
  let cmd_ids = ($mod_cmds | where name not-in [$mod_name "commands"] | get decl_id)
  scope commands | where decl_id in $cmd_ids | each {|cmd|
    let sig = $cmd.signatures | values | first
    let params = $sig
      | where parameter_type not-in ["input" "output"]
      | where parameter_name not-in $builtin_flags
      | select parameter_name parameter_type syntax_shape is_optional description
    let return_type = ($sig | where parameter_type == "output" | get -o syntax_shape | first | default "any")
    {
      name: ($cmd.name | str replace $"($mod_name) " "")
      description: $cmd.description
      extra_description: $cmd.extra_description
      return_type: $return_type
      params: $params
    }
  }
}

# GitHub API Root
#
# GET /
# Docs: https://docs.github.com/enterprise-server@2.22/rest/overview/resources-in-the-rest-api#root-endpoint — API method documentation
# operationId: meta/root
export def "meta get-root" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<authorizations_url: string, code_search_url: string, commit_search_url: string, current_user_authorizations_html_url: string, current_user_repositories_url: string, current_user_url: string, emails_url: string, emojis_url: string, events_url: string, feeds_url: string, followers_url: string, following_url: string, gists_url: string, hub_url: string, issue_search_url: string, issues_url: string, keys_url: string, label_search_url: string, notifications_url: string, organization_repositories_url: string, organization_teams_url: string, organization_url: string, public_gists_url: string, rate_limit_url: string, repository_search_url: string, repository_url: string, starred_gists_url: string, starred_url: string, topic_search_url: string, user_organizations_url: string, user_repositories_url: string, user_search_url: string, user_url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List global webhooks
#
# GET /admin/hooks
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#list-global-webhooks — API method documentation
# operationId: enterprise-admin/list-global-webhooks
export def "admin-hooks list-enterprise-global-webhooks" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
  --hdr-accept: string # This API is under preview and subject to change.
]: nothing -> table<active: bool, config: record<content_type: string, insecure_ssl: string, secret: string, url: string>, created_at: string, events: list<string>, id: int, name: string, ping_url: string, type: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/admin/hooks" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  let extra_headers = {"accept": $hdr_accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a global webhook
#
# POST /admin/hooks
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#create-a-global-webhook — API method documentation
# operationId: enterprise-admin/create-global-webhook
# --config shape: {content_type?: string, insecure_ssl?: string, secret?: string, url: string}
export def "admin-hooks create-enterprise-global-webhook" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --hdr-accept: string # This API is under preview and subject to change.
  --active: oneof<nothing, bool> # Determines if notifications are sent when the webhook is triggered. Set to `true` to send notifications. (default: true)
  config: record # Key/value pairs to provide settings for this webhook. — shape: {content_type?: string, insecure_ssl?: string, secret?: string, url: string}
  --events: list<string> # The [events](https://docs.github.com/enterprise-server@2.22/webhooks/event-payloads) that trigger this webhook. A global webhook can be triggered by `user` and `organization` events. Default: `user` and `organization`.
  name: string # Must be passed as "web".
]: any -> record<active: bool, config: record<content_type: string, insecure_ssl: string, secret: string, url: string>, created_at: string, events: list<string>, id: int, name: string, ping_url: string, type: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/admin/hooks")
  let req_body = {"active": $active, "config": $config, "events": $events, "name": $name} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  let extra_headers = {"accept": $hdr_accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a global webhook
#
# DELETE /admin/hooks/{hook_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#delete-a-global-webhook — API method documentation
# operationId: enterprise-admin/delete-global-webhook
export def "admin-hooks delete-enterprise-global-webhook" [
  hook_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --hdr-accept: string # This API is under preview and subject to change.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({hook_id: (encode-path-segment $hook_id)} | format pattern "/admin/hooks/{hook_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  let extra_headers = {"accept": $hdr_accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a global webhook
#
# GET /admin/hooks/{hook_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#get-a-global-webhook — API method documentation
# operationId: enterprise-admin/get-global-webhook
export def "admin-hooks get-enterprise-global-webhook" [
  hook_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --hdr-accept: string # This API is under preview and subject to change.
]: nothing -> record<active: bool, config: record<content_type: string, insecure_ssl: string, secret: string, url: string>, created_at: string, events: list<string>, id: int, name: string, ping_url: string, type: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({hook_id: (encode-path-segment $hook_id)} | format pattern "/admin/hooks/{hook_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  let extra_headers = {"accept": $hdr_accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a global webhook
#
# PATCH /admin/hooks/{hook_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#update-a-global-webhook — API method documentation
# operationId: enterprise-admin/update-global-webhook
# --config shape: {content_type?: string, insecure_ssl?: string, secret?: string, url: string}
export def "admin-hooks update-enterprise-global-webhook" [
  hook_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --hdr-accept: string # This API is under preview and subject to change.
  --active: oneof<nothing, bool> # Determines if notifications are sent when the webhook is triggered. Set to `true` to send notifications. (default: true)
  --config: record # Key/value pairs to provide settings for this webhook. — shape: {content_type?: string, insecure_ssl?: string, secret?: string, url: string}
  --events: list<string> # The [events](https://docs.github.com/enterprise-server@2.22/webhooks/event-payloads) that trigger this webhook. A global webhook can be triggered by `user` and `organization` events. Default: `user` and `organization`.
]: any -> record<active: bool, config: record<content_type: string, insecure_ssl: string, url: string>, created_at: string, events: list<string>, id: int, name: string, ping_url: string, type: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({hook_id: (encode-path-segment $hook_id)} | format pattern "/admin/hooks/{hook_id}"))
  let req_body = {"active": $active, "config": $config, "events": $events} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  let extra_headers = {"accept": $hdr_accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Ping a global webhook
#
# POST /admin/hooks/{hook_id}/pings
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#ping-a-global-webhook — API method documentation
# operationId: enterprise-admin/ping-global-webhook
export def "admin-hooks-pings ping-enterprise-global-webhook" [
  hook_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --hdr-accept: string # This API is under preview and subject to change.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({hook_id: (encode-path-segment $hook_id)} | format pattern "/admin/hooks/{hook_id}/pings"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  let extra_headers = {"accept": $hdr_accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List public keys
#
# GET /admin/keys
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#list-public-keys — API method documentation
# operationId: enterprise-admin/list-public-keys
export def "admin-keys list-enterprise-public" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
  --direction: string@direction-completer # One of `asc` (ascending) or `desc` (descending). (default: desc)
  --qp-sort: string@sort-completer # default: created
  --since: string # Only show public keys accessed after the given time.
]: nothing -> table<created_at: string, id: int, key: string, last_used: string, read_only: bool, repository_id: int, title: string, url: string, user_id: int, verified: bool> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar") (serialize-qp "direction" $direction "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "since" $since "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/admin/keys" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a public key
#
# DELETE /admin/keys/{key_ids}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#delete-a-public-key — API method documentation
# operationId: enterprise-admin/delete-public-key
export def "admin-keys delete-enterprise-public" [
  key_ids: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({key_ids: (encode-path-segment $key_ids)} | format pattern "/admin/keys/{key_ids}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update LDAP mapping for a team
#
# PATCH /admin/ldap/teams/{team_id}/mapping
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#update-ldap-mapping-for-a-team — API method documentation
# operationId: enterprise-admin/update-ldap-mapping-for-team
export def "admin-ldap-teams-mapping update-enterprise" [
  team_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  ldap_dn: string # The [distinguished name](https://www.ldap.com/ldap-dns-and-rdns) (DN) of the LDAP entry to map to a team.
]: any -> record<description: string, html_url: string, id: int, ldap_dn: string, members_url: string, name: string, node_id: string, parent: any, permission: string, privacy: string, repositories_url: string, slug: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id)} | format pattern "/admin/ldap/teams/{team_id}/mapping"))
  let req_body = {"ldap_dn": $ldap_dn} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Sync LDAP mapping for a team
#
# POST /admin/ldap/teams/{team_id}/sync
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#sync-ldap-mapping-for-a-team — API method documentation
# operationId: enterprise-admin/sync-ldap-mapping-for-team
export def "admin-ldap-teams-sync sync-enterprise-mapping" [
  team_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<status: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id)} | format pattern "/admin/ldap/teams/{team_id}/sync"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update LDAP mapping for a user
#
# PATCH /admin/ldap/users/{username}/mapping
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#update-ldap-mapping-for-a-user — API method documentation
# operationId: enterprise-admin/update-ldap-mapping-for-user
export def "admin-ldap-users-mapping update-enterprise" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  ldap_dn: string # The [distinguished name](https://www.ldap.com/ldap-dns-and-rdns) (DN) of the LDAP entry to map to a team.
]: any -> record<avatar_url: string, bio: string, blog: string, business_plus: bool, collaborators: int, company: string, created_at: string, disk_usage: int, email: string, events_url: string, followers: int, followers_url: string, following: int, following_url: string, gists_url: string, gravatar_id: string, hireable: bool, html_url: string, id: int, ldap_dn: string, location: string, login: string, name: string, node_id: string, organizations_url: string, owned_private_repos: int, plan: record<collaborators: int, name: string, private_repos: int, space: int>, private_gists: int, public_gists: int, public_repos: int, received_events_url: string, repos_url: string, site_admin: bool, starred_url: string, subscriptions_url: string, suspended_at: string, total_private_repos: int, twitter_username: string, two_factor_authentication: bool, type: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/admin/ldap/users/{username}/mapping"))
  let req_body = {"ldap_dn": $ldap_dn} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Sync LDAP mapping for a user
#
# POST /admin/ldap/users/{username}/sync
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#sync-ldap-mapping-for-a-user — API method documentation
# operationId: enterprise-admin/sync-ldap-mapping-for-user
export def "admin-ldap-users-sync sync-enterprise-mapping" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<status: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/admin/ldap/users/{username}/sync"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an organization
#
# POST /admin/organizations
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#create-an-organization — API method documentation
# operationId: enterprise-admin/create-org
export def "admin-organizations create-enterprise-org" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  admin: string # The login of the user who will manage this organization.
  login: string # The organization's username.
  --profile-name: string # The organization's display name.
]: any -> record<avatar_url: string, description: string, events_url: string, hooks_url: string, id: int, issues_url: string, login: string, members_url: string, node_id: string, public_members_url: string, repos_url: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/admin/organizations")
  let req_body = {"admin": $admin, "login": $login, "profile_name": $profile_name} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Update an organization name
#
# PATCH /admin/organizations/{org}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#update-an-organization-name — API method documentation
# operationId: enterprise-admin/update-org-name
export def "admin-organizations update-enterprise-name" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  login: string # The organization's new name.
]: any -> record<message: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/admin/organizations/{org}"))
  let req_body = {"login": $login} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List pre-receive environments
#
# GET /admin/pre-receive-environments
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#list-pre-receive-environments — API method documentation
# operationId: enterprise-admin/list-pre-receive-environments
export def "admin-pre-receive-environments list-enterprise" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
  --direction: string@direction-completer # One of `asc` (ascending) or `desc` (descending). (default: desc)
  --qp-sort: string@sort-completer-1 # default: created
]: nothing -> table<created_at: string, default_environment: bool, download: record<downloaded_at: string, message: string, state: string, url: string>, hooks_count: int, html_url: string, id: int, image_url: string, name: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar") (serialize-qp "direction" $direction "scalar") (serialize-qp "sort" $qp_sort "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/admin/pre-receive-environments" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a pre-receive environment
#
# POST /admin/pre-receive-environments
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#create-a-pre-receive-environment — API method documentation
# operationId: enterprise-admin/create-pre-receive-environment
export def "admin-pre-receive-environments create-enterprise" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  image_url: string # URL from which to download a tarball of this environment.
  name: string # The new pre-receive environment's name.
]: any -> record<created_at: string, default_environment: bool, download: record<downloaded_at: string, message: string, state: string, url: string>, hooks_count: int, html_url: string, id: int, image_url: string, name: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/admin/pre-receive-environments")
  let req_body = {"image_url": $image_url, "name": $name} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a pre-receive environment
#
# DELETE /admin/pre-receive-environments/{pre_receive_environment_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#delete-a-pre-receive-environment — API method documentation
# operationId: enterprise-admin/delete-pre-receive-environment
export def "admin-pre-receive-environments delete-enterprise" [
  pre_receive_environment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({pre_receive_environment_id: (encode-path-segment $pre_receive_environment_id)} | format pattern "/admin/pre-receive-environments/{pre_receive_environment_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a pre-receive environment
#
# GET /admin/pre-receive-environments/{pre_receive_environment_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#get-a-pre-receive-environment — API method documentation
# operationId: enterprise-admin/get-pre-receive-environment
export def "admin-pre-receive-environments get-enterprise" [
  pre_receive_environment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created_at: string, default_environment: bool, download: record<downloaded_at: string, message: string, state: string, url: string>, hooks_count: int, html_url: string, id: int, image_url: string, name: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({pre_receive_environment_id: (encode-path-segment $pre_receive_environment_id)} | format pattern "/admin/pre-receive-environments/{pre_receive_environment_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a pre-receive environment
#
# PATCH /admin/pre-receive-environments/{pre_receive_environment_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#update-a-pre-receive-environment — API method documentation
# operationId: enterprise-admin/update-pre-receive-environment
export def "admin-pre-receive-environments update-enterprise" [
  pre_receive_environment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --image-url: string # URL from which to download a tarball of this environment.
  --name: string # This pre-receive environment's new name.
]: any -> record<created_at: string, default_environment: bool, download: record<downloaded_at: string, message: string, state: string, url: string>, hooks_count: int, html_url: string, id: int, image_url: string, name: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({pre_receive_environment_id: (encode-path-segment $pre_receive_environment_id)} | format pattern "/admin/pre-receive-environments/{pre_receive_environment_id}"))
  let req_body = {"image_url": $image_url, "name": $name} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Start a pre-receive environment download
#
# POST /admin/pre-receive-environments/{pre_receive_environment_id}/downloads
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#start-a-pre-receive-environment-download — API method documentation
# operationId: enterprise-admin/start-pre-receive-environment-download
export def "admin-pre-receive-environments-downloads start-enterprise" [
  pre_receive_environment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<downloaded_at: string, message: string, state: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({pre_receive_environment_id: (encode-path-segment $pre_receive_environment_id)} | format pattern "/admin/pre-receive-environments/{pre_receive_environment_id}/downloads"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the download status for a pre-receive environment
#
# GET /admin/pre-receive-environments/{pre_receive_environment_id}/downloads/latest
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#get-the-download-status-for-a-pre-receive-environment — API method documentation
# operationId: enterprise-admin/get-download-status-for-pre-receive-environment
export def "admin-pre-receive-environments-downloads-latest get-enterprise-status" [
  pre_receive_environment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<downloaded_at: string, message: string, state: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({pre_receive_environment_id: (encode-path-segment $pre_receive_environment_id)} | format pattern "/admin/pre-receive-environments/{pre_receive_environment_id}/downloads/latest"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List pre-receive hooks
#
# GET /admin/pre-receive-hooks
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#list-pre-receive-hooks — API method documentation
# operationId: enterprise-admin/list-pre-receive-hooks
export def "admin-pre-receive-hooks list-enterprise" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
  --direction: string@direction-completer # One of `asc` (ascending) or `desc` (descending). (default: desc)
  --qp-sort: string@sort-completer-1 # One of `created` (when the repository was starred) or `updated` (when it was last pushed to) or `name`. (default: created)
]: nothing -> table<allow_downstream_configuration: bool, enforcement: string, environment: record<created_at: string, default_environment: bool, download: record, hooks_count: int, html_url: string, id: int, image_url: string, name: string, url: string>, id: int, name: string, script: string, script_repository: record<full_name: string, html_url: string, id: int, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar") (serialize-qp "direction" $direction "scalar") (serialize-qp "sort" $qp_sort "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/admin/pre-receive-hooks" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a pre-receive hook
#
# POST /admin/pre-receive-hooks
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#create-a-pre-receive-hook — API method documentation
# operationId: enterprise-admin/create-pre-receive-hook
export def "admin-pre-receive-hooks create-enterprise" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --allow-downstream-configuration: oneof<nothing, bool> # Whether enforcement can be overridden at the org or repo level. default: `false`
  --enforcement: string # The state of enforcement for this hook. default: `disabled`
  environment: record # The pre-receive environment where the script is executed.
  name: string # The name of the hook.
  script: string # The script that the hook runs.
  script_repository: record # The GitHub repository where the script is kept.
]: any -> record<allow_downstream_configuration: bool, enforcement: string, environment: record<created_at: string, default_environment: bool, download: record<downloaded_at: string, message: string, state: string, url: string>, hooks_count: int, html_url: string, id: int, image_url: string, name: string, url: string>, id: int, name: string, script: string, script_repository: record<full_name: string, html_url: string, id: int, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/admin/pre-receive-hooks")
  let req_body = {"allow_downstream_configuration": $allow_downstream_configuration, "enforcement": $enforcement, "environment": $environment, "name": $name, "script": $script, "script_repository": $script_repository} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a pre-receive hook
#
# DELETE /admin/pre-receive-hooks/{pre_receive_hook_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#delete-a-pre-receive-hook — API method documentation
# operationId: enterprise-admin/delete-pre-receive-hook
export def "admin-pre-receive-hooks delete-enterprise" [
  pre_receive_hook_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({pre_receive_hook_id: (encode-path-segment $pre_receive_hook_id)} | format pattern "/admin/pre-receive-hooks/{pre_receive_hook_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a pre-receive hook
#
# GET /admin/pre-receive-hooks/{pre_receive_hook_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#get-a-pre-receive-hook — API method documentation
# operationId: enterprise-admin/get-pre-receive-hook
export def "admin-pre-receive-hooks get-enterprise" [
  pre_receive_hook_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<allow_downstream_configuration: bool, enforcement: string, environment: record<created_at: string, default_environment: bool, download: record<downloaded_at: string, message: string, state: string, url: string>, hooks_count: int, html_url: string, id: int, image_url: string, name: string, url: string>, id: int, name: string, script: string, script_repository: record<full_name: string, html_url: string, id: int, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({pre_receive_hook_id: (encode-path-segment $pre_receive_hook_id)} | format pattern "/admin/pre-receive-hooks/{pre_receive_hook_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a pre-receive hook
#
# PATCH /admin/pre-receive-hooks/{pre_receive_hook_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#update-a-pre-receive-hook — API method documentation
# operationId: enterprise-admin/update-pre-receive-hook
export def "admin-pre-receive-hooks update-enterprise" [
  pre_receive_hook_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --allow-downstream-configuration: oneof<nothing, bool> # Whether enforcement can be overridden at the org or repo level.
  --enforcement: string # The state of enforcement for this hook.
  --environment: record # The pre-receive environment where the script is executed.
  --name: string # The name of the hook.
  --script: string # The script that the hook runs.
  --script-repository: record # The GitHub repository where the script is kept.
]: any -> record<allow_downstream_configuration: bool, enforcement: string, environment: record<created_at: string, default_environment: bool, download: record<downloaded_at: string, message: string, state: string, url: string>, hooks_count: int, html_url: string, id: int, image_url: string, name: string, url: string>, id: int, name: string, script: string, script_repository: record<full_name: string, html_url: string, id: int, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({pre_receive_hook_id: (encode-path-segment $pre_receive_hook_id)} | format pattern "/admin/pre-receive-hooks/{pre_receive_hook_id}"))
  let req_body = {"allow_downstream_configuration": $allow_downstream_configuration, "enforcement": $enforcement, "environment": $environment, "name": $name, "script": $script, "script_repository": $script_repository} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List personal access tokens
#
# GET /admin/tokens
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#list-personal-access-tokens — API method documentation
# operationId: enterprise-admin/list-personal-access-tokens
export def "admin-tokens list-enterprise-personal-access" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<app: record<client_id: string, name: string, url: string>, created_at: string, fingerprint: string, hashed_token: string, id: int, installation: record<account: record, has_multiple_single_files: bool, permissions: record, repositories_url: string, repository_selection: string, single_file_name: string, single_file_paths: list>, note: string, note_url: string, scopes: list<string>, token: string, token_last_eight: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/admin/tokens" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a personal access token
#
# DELETE /admin/tokens/{token_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#delete-a-personal-access-token — API method documentation
# operationId: enterprise-admin/delete-personal-access-token
export def "admin-tokens delete-enterprise-personal-access" [
  token_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({token_id: (encode-path-segment $token_id)} | format pattern "/admin/tokens/{token_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a user
#
# POST /admin/users
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#create-a-user — API method documentation
# operationId: enterprise-admin/create-user
export def "admin-users create-enterprise" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --email: string # **Required for built-in authentication.** The user's email address. This parameter can be omitted when using CAS, LDAP, or SAML. For details on built-in and centrally-managed authentication, see the the [GitHub authentication guide](https://help.github.com/enterprise/2.18/admin/guides/user-management/authenticating-users-for-your-github-enterprise-server-instance/).
  login: string # The user's username.
]: any -> record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/admin/users")
  let req_body = {"email": $email, "login": $login} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a user
#
# DELETE /admin/users/{username}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#delete-a-user — API method documentation
# operationId: enterprise-admin/delete-user
export def "admin-users delete-enterprise" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/admin/users/{username}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update the username for a user
#
# PATCH /admin/users/{username}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#update-the-username-for-a-user — API method documentation
# operationId: enterprise-admin/update-username-for-user
export def "admin-users update-enterprise" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  login: string # The user's new username.
]: any -> record<message: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/admin/users/{username}"))
  let req_body = {"login": $login} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete an impersonation OAuth token
#
# DELETE /admin/users/{username}/authorizations
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#delete-an-impersonation-oauth-token — API method documentation
# operationId: enterprise-admin/delete-impersonation-o-auth-token
export def "admin-users-authorizations delete-enterprise-impersonation-o-auth-token" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/admin/users/{username}/authorizations"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an impersonation OAuth token
#
# POST /admin/users/{username}/authorizations
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#create-an-impersonation-oauth-token — API method documentation
# operationId: enterprise-admin/create-impersonation-o-auth-token
export def "admin-users-authorizations create-enterprise-impersonation-o-auth-token" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --scopes: list<string> # A list of [scopes](https://docs.github.com/enterprise-server@2.22/apps/building-oauth-apps/understanding-scopes-for-oauth-apps/).
]: any -> record<app: record<client_id: string, name: string, url: string>, created_at: string, fingerprint: string, hashed_token: string, id: int, installation: record<account: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, has_multiple_single_files: bool, permissions: record<actions: string, administration: string, checks: string, content_references: string, contents: string, deployments: string, environments: string, issues: string, members: string, metadata: string, organization_administration: string, organization_hooks: string, organization_packages: string, organization_plan: string, organization_projects: string, organization_secrets: string, organization_self_hosted_runners: string, organization_user_blocking: string, packages: string, pages: string, pull_requests: string, repository_hooks: string, repository_projects: string, secret_scanning_alerts: string, secrets: string, security_events: string, single_file: string, statuses: string, team_discussions: string, vulnerability_alerts: string, workflows: string>, repositories_url: string, repository_selection: string, single_file_name: string, single_file_paths: list<string>>, note: string, note_url: string, scopes: list<string>, token: string, token_last_eight: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/admin/users/{username}/authorizations"))
  let req_body = {"scopes": $scopes} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Get the authenticated app
#
# GET /app
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps#get-the-authenticated-app — API method documentation
# operationId: apps/get-authenticated
export def "app get-authenticated" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<client_id: string, client_secret: string, created_at: string, description: string, events: list<string>, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, pem: string, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string>, slug: string, updated_at: string, webhook_secret: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/app")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a GitHub App from a manifest
#
# POST /app-manifests/{code}/conversions
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps#create-a-github-app-from-a-manifest — API method documentation
# operationId: apps/create-from-manifest
export def "app-manifests-conversions create" [
  code: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --body: record
]: any -> record<client_id: string, client_secret: string, created_at: string, description: string, events: list<string>, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, pem: string, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string>, slug: string, updated_at: string, webhook_secret: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({code: (encode-path-segment $code)} | format pattern "/app-manifests/{code}/conversions"))
  let req_body = $body
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List installations for the authenticated app
#
# GET /app/installations
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps#list-installations-for-the-authenticated-app — API method documentation
# operationId: apps/list-installations
export def "app-installations list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
  --since: string # Only show notifications updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --outdated: string
]: nothing -> table<access_tokens_url: string, account: any, app_id: int, app_slug: string, contact_email: string, created_at: string, events: list<string>, html_url: string, id: int, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string, organization_administration: string, pull_requests: string, statuses: string>, repositories_url: string, repository_selection: string, single_file_name: string, suspended_at: string, suspended_by: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, target_id: int, target_type: string, updated_at: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar") (serialize-qp "since" $since "scalar") (serialize-qp "outdated" $outdated "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/app/installations" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an installation for the authenticated app
#
# DELETE /app/installations/{installation_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps#delete-an-installation-for-the-authenticated-app — API method documentation
# operationId: apps/delete-installation
export def "app-installations delete" [
  installation_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({installation_id: (encode-path-segment $installation_id)} | format pattern "/app/installations/{installation_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get an installation for the authenticated app
#
# GET /app/installations/{installation_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps#get-an-installation-for-the-authenticated-app — API method documentation
# operationId: apps/get-installation
export def "app-installations get" [
  installation_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<access_tokens_url: string, account: any, app_id: int, app_slug: string, contact_email: string, created_at: string, events: list<string>, html_url: string, id: int, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string, organization_administration: string, pull_requests: string, statuses: string>, repositories_url: string, repository_selection: string, single_file_name: string, suspended_at: string, suspended_by: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, target_id: int, target_type: string, updated_at: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({installation_id: (encode-path-segment $installation_id)} | format pattern "/app/installations/{installation_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an installation access token for an app
#
# POST /app/installations/{installation_id}/access_tokens
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps/#create-an-installation-access-token-for-an-app — API method documentation
# operationId: apps/create-installation-access-token
# --permissions shape: {actions?: "read"|"write", administration?: "read"|"write", checks?: "read"|"write", content_references?: "read"|"write", contents?: "read"|"write", deployments?: "read"|"write", environments?: "read"|"write", issues?: "read"|"write", members?: "read"|"write", metadata?: "read"|"write", organization_administration?: "read"|"write", organization_hooks?: "read"|"write", organization_packages?: "read"|"write", organization_plan?: "read", organization_projects?: "read"|"write"|"admin", ... (16 more fields)}
export def "app-installations-access-tokens create" [
  installation_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --permissions: record # The permissions granted to the user-to-server access token. (e.g. {contents: read, deployments: write, issues: read, single_file: read}) — shape: {actions?: "read"|"write", administration?: "read"|"write", checks?: "read"|"write", content_references?: "read"|"write", contents?: "read"|"write", deployments?: "read"|"write", environments?: "read"|"write", issues?: "read"|"write", members?: "read"|"write", metadata?: "read"|"write", organization_administration?: "read"|"write", organization_hooks?: "read"|"write", organization_packages?: "read"|"write", organization_plan?: "read", organization_projects?: "read"|"write"|"admin", ... (16 more fields)}
  --repositories: list<string> # List of repository names that the token should have access to
  --repository-ids: list<int> # List of repository IDs that the token should have access to (e.g. [1])
]: any -> record<expires_at: string, has_multiple_single_files: bool, permissions: record<actions: string, administration: string, checks: string, content_references: string, contents: string, deployments: string, environments: string, issues: string, members: string, metadata: string, organization_administration: string, organization_hooks: string, organization_packages: string, organization_plan: string, organization_projects: string, organization_secrets: string, organization_self_hosted_runners: string, organization_user_blocking: string, packages: string, pages: string, pull_requests: string, repository_hooks: string, repository_projects: string, secret_scanning_alerts: string, secrets: string, security_events: string, single_file: string, statuses: string, team_discussions: string, vulnerability_alerts: string, workflows: string>, repositories: table<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, repository_selection: string, single_file: string, single_file_paths: list<string>, token: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({installation_id: (encode-path-segment $installation_id)} | format pattern "/app/installations/{installation_id}/access_tokens"))
  let req_body = {"permissions": $permissions, "repositories": $repositories, "repository_ids": $repository_ids} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Unsuspend an app installation
#
# DELETE /app/installations/{installation_id}/suspended
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps#unsuspend-an-app-installation — API method documentation
# operationId: apps/unsuspend-installation
export def "app-installations-suspended delete-unsuspend" [
  installation_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({installation_id: (encode-path-segment $installation_id)} | format pattern "/app/installations/{installation_id}/suspended"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Suspend an app installation
#
# PUT /app/installations/{installation_id}/suspended
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps#suspend-an-app-installation — API method documentation
# operationId: apps/suspend-installation
export def "app-installations-suspended update-suspend" [
  installation_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({installation_id: (encode-path-segment $installation_id)} | format pattern "/app/installations/{installation_id}/suspended"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List your grants
#
# GET /applications/grants
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/oauth-authorizations#list-your-grants — API method documentation
# operationId: oauth-authorizations/list-grants
@deprecated
export def "applications-grants list-oauth-authorizations" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
  --client-id: string # The client ID of your GitHub app.
]: nothing -> table<app: record<client_id: string, name: string, url: string>, created_at: string, id: int, scopes: list<string>, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar") (serialize-qp "client_id" $client_id "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/applications/grants" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a grant
#
# DELETE /applications/grants/{grant_id}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/oauth-authorizations#delete-a-grant — API method documentation
# operationId: oauth-authorizations/delete-grant
@deprecated
export def "applications-grants delete-oauth-authorizations" [
  grant_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({grant_id: (encode-path-segment $grant_id)} | format pattern "/applications/grants/{grant_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a single grant
#
# GET /applications/grants/{grant_id}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/oauth-authorizations#get-a-single-grant — API method documentation
# operationId: oauth-authorizations/get-grant
@deprecated
export def "applications-grants get-oauth-authorizations" [
  grant_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<app: record<client_id: string, name: string, url: string>, created_at: string, id: int, scopes: list<string>, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({grant_id: (encode-path-segment $grant_id)} | format pattern "/applications/grants/{grant_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an app authorization
#
# DELETE /applications/{client_id}/grant
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps#delete-an-app-authorization — API method documentation
# operationId: apps/delete-authorization
export def "applications-grant delete-apps-authorization" [
  client_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  access_token: string # The OAuth access token used to authenticate to the GitHub API.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({client_id: (encode-path-segment $client_id)} | format pattern "/applications/{client_id}/grant"))
  let req_body = {"access_token": $access_token} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Revoke a grant for an application
#
# DELETE /applications/{client_id}/grants/{access_token}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps#revoke-a-grant-for-an-application — API method documentation
# operationId: apps/revoke-grant-for-application
@deprecated
export def "applications-grants delete-apps" [
  client_id: string
  access_token: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({client_id: (encode-path-segment $client_id), access_token: (encode-path-segment $access_token)} | format pattern "/applications/{client_id}/grants/{access_token}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an app token
#
# DELETE /applications/{client_id}/token
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps#delete-an-app-token — API method documentation
# operationId: apps/delete-token
export def "applications-token delete-apps" [
  client_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  access_token: string # The OAuth access token used to authenticate to the GitHub API.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({client_id: (encode-path-segment $client_id)} | format pattern "/applications/{client_id}/token"))
  let req_body = {"access_token": $access_token} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Reset a token
#
# PATCH /applications/{client_id}/token
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps#reset-a-token — API method documentation
# operationId: apps/reset-token
export def "applications-token reset-apps" [
  client_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  access_token: string # The access_token of the OAuth application.
]: any -> record<app: record<client_id: string, name: string, url: string>, created_at: string, fingerprint: string, hashed_token: string, id: int, installation: record<account: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, has_multiple_single_files: bool, permissions: record<actions: string, administration: string, checks: string, content_references: string, contents: string, deployments: string, environments: string, issues: string, members: string, metadata: string, organization_administration: string, organization_hooks: string, organization_packages: string, organization_plan: string, organization_projects: string, organization_secrets: string, organization_self_hosted_runners: string, organization_user_blocking: string, packages: string, pages: string, pull_requests: string, repository_hooks: string, repository_projects: string, secret_scanning_alerts: string, secrets: string, security_events: string, single_file: string, statuses: string, team_discussions: string, vulnerability_alerts: string, workflows: string>, repositories_url: string, repository_selection: string, single_file_name: string, single_file_paths: list<string>>, note: string, note_url: string, scopes: list<string>, token: string, token_last_eight: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({client_id: (encode-path-segment $client_id)} | format pattern "/applications/{client_id}/token"))
  let req_body = {"access_token": $access_token} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Check a token
#
# POST /applications/{client_id}/token
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps#check-a-token — API method documentation
# operationId: apps/check-token
export def "applications-token check-apps" [
  client_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  access_token: string # The access_token of the OAuth application.
]: any -> record<app: record<client_id: string, name: string, url: string>, created_at: string, fingerprint: string, hashed_token: string, id: int, installation: record<account: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, has_multiple_single_files: bool, permissions: record<actions: string, administration: string, checks: string, content_references: string, contents: string, deployments: string, environments: string, issues: string, members: string, metadata: string, organization_administration: string, organization_hooks: string, organization_packages: string, organization_plan: string, organization_projects: string, organization_secrets: string, organization_self_hosted_runners: string, organization_user_blocking: string, packages: string, pages: string, pull_requests: string, repository_hooks: string, repository_projects: string, secret_scanning_alerts: string, secrets: string, security_events: string, single_file: string, statuses: string, team_discussions: string, vulnerability_alerts: string, workflows: string>, repositories_url: string, repository_selection: string, single_file_name: string, single_file_paths: list<string>>, note: string, note_url: string, scopes: list<string>, token: string, token_last_eight: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({client_id: (encode-path-segment $client_id)} | format pattern "/applications/{client_id}/token"))
  let req_body = {"access_token": $access_token} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Revoke an authorization for an application
#
# DELETE /applications/{client_id}/tokens/{access_token}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps#revoke-an-authorization-for-an-application — API method documentation
# operationId: apps/revoke-authorization-for-application
@deprecated
export def "applications-tokens delete-apps-authorization" [
  client_id: string
  access_token: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({client_id: (encode-path-segment $client_id), access_token: (encode-path-segment $access_token)} | format pattern "/applications/{client_id}/tokens/{access_token}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Check an authorization
#
# GET /applications/{client_id}/tokens/{access_token}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps#check-an-authorization — API method documentation
# operationId: apps/check-authorization
@deprecated
export def "applications-tokens check-apps-authorization" [
  client_id: string
  access_token: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<app: record<client_id: string, name: string, url: string>, created_at: string, fingerprint: string, hashed_token: string, id: int, installation: record<account: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, has_multiple_single_files: bool, permissions: record<actions: string, administration: string, checks: string, content_references: string, contents: string, deployments: string, environments: string, issues: string, members: string, metadata: string, organization_administration: string, organization_hooks: string, organization_packages: string, organization_plan: string, organization_projects: string, organization_secrets: string, organization_self_hosted_runners: string, organization_user_blocking: string, packages: string, pages: string, pull_requests: string, repository_hooks: string, repository_projects: string, secret_scanning_alerts: string, secrets: string, security_events: string, single_file: string, statuses: string, team_discussions: string, vulnerability_alerts: string, workflows: string>, repositories_url: string, repository_selection: string, single_file_name: string, single_file_paths: list<string>>, note: string, note_url: string, scopes: list<string>, token: string, token_last_eight: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({client_id: (encode-path-segment $client_id), access_token: (encode-path-segment $access_token)} | format pattern "/applications/{client_id}/tokens/{access_token}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Reset an authorization
#
# POST /applications/{client_id}/tokens/{access_token}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps#reset-an-authorization — API method documentation
# operationId: apps/reset-authorization
@deprecated
export def "applications-tokens reset-apps-authorization" [
  client_id: string
  access_token: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<app: record<client_id: string, name: string, url: string>, created_at: string, fingerprint: string, hashed_token: string, id: int, installation: record<account: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, has_multiple_single_files: bool, permissions: record<actions: string, administration: string, checks: string, content_references: string, contents: string, deployments: string, environments: string, issues: string, members: string, metadata: string, organization_administration: string, organization_hooks: string, organization_packages: string, organization_plan: string, organization_projects: string, organization_secrets: string, organization_self_hosted_runners: string, organization_user_blocking: string, packages: string, pages: string, pull_requests: string, repository_hooks: string, repository_projects: string, secret_scanning_alerts: string, secrets: string, security_events: string, single_file: string, statuses: string, team_discussions: string, vulnerability_alerts: string, workflows: string>, repositories_url: string, repository_selection: string, single_file_name: string, single_file_paths: list<string>>, note: string, note_url: string, scopes: list<string>, token: string, token_last_eight: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({client_id: (encode-path-segment $client_id), access_token: (encode-path-segment $access_token)} | format pattern "/applications/{client_id}/tokens/{access_token}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get an app
#
# GET /apps/{app_slug}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps/#get-an-app — API method documentation
# operationId: apps/get-by-slug
export def "apps get" [
  app_slug: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<client_id: string, client_secret: string, created_at: string, description: string, events: list<string>, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, pem: string, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string>, slug: string, updated_at: string, webhook_secret: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({app_slug: (encode-path-segment $app_slug)} | format pattern "/apps/{app_slug}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List your authorizations
#
# GET /authorizations
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/oauth-authorizations#list-your-authorizations — API method documentation
# operationId: oauth-authorizations/list-authorizations
@deprecated
export def "authorizations list-oauth" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
  --client-id: string # The client ID of your GitHub app.
]: nothing -> table<app: record<client_id: string, name: string, url: string>, created_at: string, fingerprint: string, hashed_token: string, id: int, installation: record<account: record, has_multiple_single_files: bool, permissions: record, repositories_url: string, repository_selection: string, single_file_name: string, single_file_paths: list>, note: string, note_url: string, scopes: list<string>, token: string, token_last_eight: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar") (serialize-qp "client_id" $client_id "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/authorizations" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a new authorization
#
# POST /authorizations
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/oauth-authorizations#create-a-new-authorization — API method documentation
# operationId: oauth-authorizations/create-authorization
@deprecated
export def "authorizations create-oauth" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --client-id: string # The OAuth app client key for which to create the token.
  --client-secret: string # The OAuth app client secret for which to create the token.
  --fingerprint: string # A unique string to distinguish an authorization from others created for the same client ID and user.
  --note: string # A note to remind you what the OAuth token is for. (e.g. Update all gems)
  --note-url: string # A URL to remind you what app the OAuth token is for.
  --scopes: list<string> # A list of scopes that this authorization is in. (nullable, e.g. [public_repo, user])
]: any -> record<app: record<client_id: string, name: string, url: string>, created_at: string, fingerprint: string, hashed_token: string, id: int, installation: record<account: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, has_multiple_single_files: bool, permissions: record<actions: string, administration: string, checks: string, content_references: string, contents: string, deployments: string, environments: string, issues: string, members: string, metadata: string, organization_administration: string, organization_hooks: string, organization_packages: string, organization_plan: string, organization_projects: string, organization_secrets: string, organization_self_hosted_runners: string, organization_user_blocking: string, packages: string, pages: string, pull_requests: string, repository_hooks: string, repository_projects: string, secret_scanning_alerts: string, secrets: string, security_events: string, single_file: string, statuses: string, team_discussions: string, vulnerability_alerts: string, workflows: string>, repositories_url: string, repository_selection: string, single_file_name: string, single_file_paths: list<string>>, note: string, note_url: string, scopes: list<string>, token: string, token_last_eight: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/authorizations")
  let req_body = {"client_id": $client_id, "client_secret": $client_secret, "fingerprint": $fingerprint, "note": $note, "note_url": $note_url, "scopes": $scopes} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Get-or-create an authorization for a specific app
#
# PUT /authorizations/clients/{client_id}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/oauth-authorizations#get-or-create-an-authorization-for-a-specific-app — API method documentation
# operationId: oauth-authorizations/get-or-create-authorization-for-app
@deprecated
export def "authorizations-clients get-oauth-or-create-for-app" [
  client_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  client_secret: string # The OAuth app client secret for which to create the token.
  --fingerprint: string # A unique string to distinguish an authorization from others created for the same client ID and user.
  --note: string # A note to remind you what the OAuth token is for. (e.g. Update all gems)
  --note-url: string # A URL to remind you what app the OAuth token is for.
  --scopes: list<string> # A list of scopes that this authorization is in. (nullable, e.g. [public_repo, user])
]: any -> record<app: record<client_id: string, name: string, url: string>, created_at: string, fingerprint: string, hashed_token: string, id: int, installation: record<account: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, has_multiple_single_files: bool, permissions: record<actions: string, administration: string, checks: string, content_references: string, contents: string, deployments: string, environments: string, issues: string, members: string, metadata: string, organization_administration: string, organization_hooks: string, organization_packages: string, organization_plan: string, organization_projects: string, organization_secrets: string, organization_self_hosted_runners: string, organization_user_blocking: string, packages: string, pages: string, pull_requests: string, repository_hooks: string, repository_projects: string, secret_scanning_alerts: string, secrets: string, security_events: string, single_file: string, statuses: string, team_discussions: string, vulnerability_alerts: string, workflows: string>, repositories_url: string, repository_selection: string, single_file_name: string, single_file_paths: list<string>>, note: string, note_url: string, scopes: list<string>, token: string, token_last_eight: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({client_id: (encode-path-segment $client_id)} | format pattern "/authorizations/clients/{client_id}"))
  let req_body = {"client_secret": $client_secret, "fingerprint": $fingerprint, "note": $note, "note_url": $note_url, "scopes": $scopes} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Get-or-create an authorization for a specific app and fingerprint
#
# PUT /authorizations/clients/{client_id}/{fingerprint}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/oauth-authorizations#get-or-create-an-authorization-for-a-specific-app-and-fingerprint — API method documentation
# operationId: oauth-authorizations/get-or-create-authorization-for-app-and-fingerprint
@deprecated
export def "authorizations-clients get-oauth-or-create-for-app-and" [
  client_id: string
  fingerprint: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  client_secret: string # The OAuth app client secret for which to create the token.
  --note: string # A note to remind you what the OAuth token is for. (e.g. Update all gems)
  --note-url: string # A URL to remind you what app the OAuth token is for.
  --scopes: list<string> # A list of scopes that this authorization is in. (nullable, e.g. [public_repo, user])
]: any -> record<app: record<client_id: string, name: string, url: string>, created_at: string, fingerprint: string, hashed_token: string, id: int, installation: record<account: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, has_multiple_single_files: bool, permissions: record<actions: string, administration: string, checks: string, content_references: string, contents: string, deployments: string, environments: string, issues: string, members: string, metadata: string, organization_administration: string, organization_hooks: string, organization_packages: string, organization_plan: string, organization_projects: string, organization_secrets: string, organization_self_hosted_runners: string, organization_user_blocking: string, packages: string, pages: string, pull_requests: string, repository_hooks: string, repository_projects: string, secret_scanning_alerts: string, secrets: string, security_events: string, single_file: string, statuses: string, team_discussions: string, vulnerability_alerts: string, workflows: string>, repositories_url: string, repository_selection: string, single_file_name: string, single_file_paths: list<string>>, note: string, note_url: string, scopes: list<string>, token: string, token_last_eight: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({client_id: (encode-path-segment $client_id), fingerprint: (encode-path-segment $fingerprint)} | format pattern "/authorizations/clients/{client_id}/{fingerprint}"))
  let req_body = {"client_secret": $client_secret, "note": $note, "note_url": $note_url, "scopes": $scopes} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete an authorization
#
# DELETE /authorizations/{authorization_id}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/oauth-authorizations#delete-an-authorization — API method documentation
# operationId: oauth-authorizations/delete-authorization
@deprecated
export def "authorizations delete-oauth" [
  authorization_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({authorization_id: (encode-path-segment $authorization_id)} | format pattern "/authorizations/{authorization_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a single authorization
#
# GET /authorizations/{authorization_id}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/oauth-authorizations#get-a-single-authorization — API method documentation
# operationId: oauth-authorizations/get-authorization
@deprecated
export def "authorizations get-oauth" [
  authorization_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<app: record<client_id: string, name: string, url: string>, created_at: string, fingerprint: string, hashed_token: string, id: int, installation: record<account: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, has_multiple_single_files: bool, permissions: record<actions: string, administration: string, checks: string, content_references: string, contents: string, deployments: string, environments: string, issues: string, members: string, metadata: string, organization_administration: string, organization_hooks: string, organization_packages: string, organization_plan: string, organization_projects: string, organization_secrets: string, organization_self_hosted_runners: string, organization_user_blocking: string, packages: string, pages: string, pull_requests: string, repository_hooks: string, repository_projects: string, secret_scanning_alerts: string, secrets: string, security_events: string, single_file: string, statuses: string, team_discussions: string, vulnerability_alerts: string, workflows: string>, repositories_url: string, repository_selection: string, single_file_name: string, single_file_paths: list<string>>, note: string, note_url: string, scopes: list<string>, token: string, token_last_eight: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({authorization_id: (encode-path-segment $authorization_id)} | format pattern "/authorizations/{authorization_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update an existing authorization
#
# PATCH /authorizations/{authorization_id}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/oauth-authorizations#update-an-existing-authorization — API method documentation
# operationId: oauth-authorizations/update-authorization
@deprecated
export def "authorizations update-oauth" [
  authorization_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --add-scopes: list<string> # A list of scopes to add to this authorization.
  --fingerprint: string # A unique string to distinguish an authorization from others created for the same client ID and user.
  --note: string # A note to remind you what the OAuth token is for. (e.g. Update all gems)
  --note-url: string # A URL to remind you what app the OAuth token is for.
  --remove-scopes: list<string> # A list of scopes to remove from this authorization.
  --scopes: list<string> # A list of scopes that this authorization is in. (nullable, e.g. [public_repo, user])
]: any -> record<app: record<client_id: string, name: string, url: string>, created_at: string, fingerprint: string, hashed_token: string, id: int, installation: record<account: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, has_multiple_single_files: bool, permissions: record<actions: string, administration: string, checks: string, content_references: string, contents: string, deployments: string, environments: string, issues: string, members: string, metadata: string, organization_administration: string, organization_hooks: string, organization_packages: string, organization_plan: string, organization_projects: string, organization_secrets: string, organization_self_hosted_runners: string, organization_user_blocking: string, packages: string, pages: string, pull_requests: string, repository_hooks: string, repository_projects: string, secret_scanning_alerts: string, secrets: string, security_events: string, single_file: string, statuses: string, team_discussions: string, vulnerability_alerts: string, workflows: string>, repositories_url: string, repository_selection: string, single_file_name: string, single_file_paths: list<string>>, note: string, note_url: string, scopes: list<string>, token: string, token_last_eight: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({authorization_id: (encode-path-segment $authorization_id)} | format pattern "/authorizations/{authorization_id}"))
  let req_body = {"add_scopes": $add_scopes, "fingerprint": $fingerprint, "note": $note, "note_url": $note_url, "remove_scopes": $remove_scopes, "scopes": $scopes} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Get all codes of conduct
#
# GET /codes_of_conduct
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/codes-of-conduct#get-all-codes-of-conduct — API method documentation
# operationId: codes-of-conduct/get-all-codes-of-conduct
export def "codes-of-conduct get-list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> table<body: string, html_url: string, key: string, name: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/codes_of_conduct")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a code of conduct
#
# GET /codes_of_conduct/{key}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/codes-of-conduct#get-a-code-of-conduct — API method documentation
# operationId: codes-of-conduct/get-conduct-code
export def "codes-of-conduct get" [
  key: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<body: string, html_url: string, key: string, name: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({key: (encode-path-segment $key)} | format pattern "/codes_of_conduct/{key}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get emojis
#
# GET /emojis
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/emojis#get-emojis — API method documentation
# operationId: emojis/get
export def "emojis get" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/emojis")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get license information
#
# GET /enterprise/settings/license
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#get-license-information — API method documentation
# operationId: enterprise-admin/get-license-information
export def "enterprise-settings-license get-admin-information" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<days_until_expiration: int, expire_at: string, kind: string, seats: any, seats_available: any, seats_used: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/enterprise/settings/license")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get all statistics
#
# GET /enterprise/stats/all
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#get-statistics — API method documentation
# operationId: enterprise-admin/get-all-stats
export def "enterprise-stats-all get-admin" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<comments: record<total_commit_comments: int, total_gist_comments: int, total_issue_comments: int, total_pull_request_comments: int>, gists: record<private_gists: int, public_gists: int, total_gists: int>, hooks: record<active_hooks: int, inactive_hooks: int, total_hooks: int>, issues: record<closed_issues: int, open_issues: int, total_issues: int>, milestones: record<closed_milestones: int, open_milestones: int, total_milestones: int>, orgs: record<disabled_orgs: int, total_orgs: int, total_team_members: int, total_teams: int>, pages: record<total_pages: int>, pulls: record<mergeable_pulls: int, merged_pulls: int, total_pulls: int, unmergeable_pulls: int>, repos: record<fork_repos: int, org_repos: int, root_repos: int, total_pushes: int, total_repos: int, total_wikis: int>, users: record<admin_users: int, suspended_users: int, total_users: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/enterprise/stats/all")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get comment statistics
#
# GET /enterprise/stats/comments
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#get-comment-statistics — API method documentation
# operationId: enterprise-admin/get-comment-stats
export def "enterprise-stats-comments get-admin" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<total_commit_comments: int, total_gist_comments: int, total_issue_comments: int, total_pull_request_comments: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/enterprise/stats/comments")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get gist statistics
#
# GET /enterprise/stats/gists
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#get-gist-statistics — API method documentation
export def "enterprise-stats-gists get" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<private_gists: int, public_gists: int, total_gists: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/enterprise/stats/gists")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get hooks statistics
#
# GET /enterprise/stats/hooks
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#get-hooks-statistics — API method documentation
# operationId: enterprise-admin/get-hooks-stats
export def "enterprise-stats-hooks get-admin" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<active_hooks: int, inactive_hooks: int, total_hooks: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/enterprise/stats/hooks")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get issue statistics
#
# GET /enterprise/stats/issues
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#get-issues-statistics — API method documentation
# operationId: enterprise-admin/get-issue-stats
export def "enterprise-stats-issues get-admin" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<closed_issues: int, open_issues: int, total_issues: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/enterprise/stats/issues")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get milestone statistics
#
# GET /enterprise/stats/milestones
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#get-milestone-statistics — API method documentation
# operationId: enterprise-admin/get-milestone-stats
export def "enterprise-stats-milestones get-admin" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<closed_milestones: int, open_milestones: int, total_milestones: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/enterprise/stats/milestones")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get organization statistics
#
# GET /enterprise/stats/orgs
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#get-organization-statistics — API method documentation
# operationId: enterprise-admin/get-org-stats
export def "enterprise-stats-orgs get-admin" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<disabled_orgs: int, total_orgs: int, total_team_members: int, total_teams: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/enterprise/stats/orgs")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get pages statistics
#
# GET /enterprise/stats/pages
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#get-pages-statistics — API method documentation
# operationId: enterprise-admin/get-pages-stats
export def "enterprise-stats-pages get-admin" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<total_pages: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/enterprise/stats/pages")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get pull request statistics
#
# GET /enterprise/stats/pulls
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#get-pull-requests-statistics — API method documentation
# operationId: enterprise-admin/get-pull-request-stats
export def "enterprise-stats-pulls get-admin-request" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<mergeable_pulls: int, merged_pulls: int, total_pulls: int, unmergeable_pulls: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/enterprise/stats/pulls")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get repository statistics
#
# GET /enterprise/stats/repos
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#get-repository-statistics — API method documentation
# operationId: enterprise-admin/get-repo-stats
export def "enterprise-stats-repos get-admin" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<fork_repos: int, org_repos: int, root_repos: int, total_pushes: int, total_repos: int, total_wikis: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/enterprise/stats/repos")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get users statistics
#
# GET /enterprise/stats/users
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#get-users-statistics — API method documentation
# operationId: enterprise-admin/get-user-stats
export def "enterprise-stats-users get-admin" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<admin_users: int, suspended_users: int, total_users: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/enterprise/stats/users")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List self-hosted runner groups for an enterprise
#
# GET /enterprises/{enterprise}/actions/runner-groups
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#list-self-hosted-runner-groups-for-an-enterprise — API method documentation
# operationId: enterprise-admin/list-self-hosted-runner-groups-for-enterprise
export def "enterprises-actions-runner-groups list-admin-self-hosted" [
  enterprise: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<runner_groups: table<allows_public_repositories: bool, default: bool, id: float, name: string, runners_url: string, selected_organizations_url: string, visibility: string>, total_count: float> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({enterprise: (encode-path-segment $enterprise)} | format pattern "/enterprises/{enterprise}/actions/runner-groups") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a self-hosted runner group for an enterprise
#
# POST /enterprises/{enterprise}/actions/runner-groups
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#create-self-hosted-runner-group-for-an-enterprise — API method documentation
# operationId: enterprise-admin/create-self-hosted-runner-group-for-enterprise
export def "enterprises-actions-runner-groups create-admin-self-hosted" [
  enterprise: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  name: string # Name of the runner group.
  --runners: list<int> # List of runner IDs to add to the runner group.
  --selected-organization-ids: list<int> # List of organization IDs that can access the runner group.
  --visibility: string@visibility-completer # Visibility of a runner group. You can select all organizations or select individual organization. Can be one of: `all` or `selected`
]: any -> record<allows_public_repositories: bool, default: bool, id: float, name: string, runners_url: string, selected_organizations_url: string, visibility: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({enterprise: (encode-path-segment $enterprise)} | format pattern "/enterprises/{enterprise}/actions/runner-groups"))
  let req_body = {"name": $name, "runners": $runners, "selected_organization_ids": $selected_organization_ids, "visibility": $visibility} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a self-hosted runner group from an enterprise
#
# DELETE /enterprises/{enterprise}/actions/runner-groups/{runner_group_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#delete-a-self-hosted-runner-group-from-an-enterprise — API method documentation
# operationId: enterprise-admin/delete-self-hosted-runner-group-from-enterprise
export def "enterprises-actions-runner-groups delete-admin-self-hosted" [
  enterprise: string
  runner_group_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({enterprise: (encode-path-segment $enterprise), runner_group_id: (encode-path-segment $runner_group_id)} | format pattern "/enterprises/{enterprise}/actions/runner-groups/{runner_group_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a self-hosted runner group for an enterprise
#
# GET /enterprises/{enterprise}/actions/runner-groups/{runner_group_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#get-a-self-hosted-runner-group-for-an-enterprise — API method documentation
# operationId: enterprise-admin/get-self-hosted-runner-group-for-enterprise
export def "enterprises-actions-runner-groups get-admin-self-hosted" [
  enterprise: string
  runner_group_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<allows_public_repositories: bool, default: bool, id: float, name: string, runners_url: string, selected_organizations_url: string, visibility: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({enterprise: (encode-path-segment $enterprise), runner_group_id: (encode-path-segment $runner_group_id)} | format pattern "/enterprises/{enterprise}/actions/runner-groups/{runner_group_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a self-hosted runner group for an enterprise
#
# PATCH /enterprises/{enterprise}/actions/runner-groups/{runner_group_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#update-a-self-hosted-runner-group-for-an-enterprise — API method documentation
# operationId: enterprise-admin/update-self-hosted-runner-group-for-enterprise
export def "enterprises-actions-runner-groups update-admin-self-hosted" [
  enterprise: string
  runner_group_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --name: string # Name of the runner group.
  --visibility: string@visibility-completer # Visibility of a runner group. You can select all organizations or select individual organizations. Can be one of: `all` or `selected` (default: all)
]: any -> record<allows_public_repositories: bool, default: bool, id: float, name: string, runners_url: string, selected_organizations_url: string, visibility: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({enterprise: (encode-path-segment $enterprise), runner_group_id: (encode-path-segment $runner_group_id)} | format pattern "/enterprises/{enterprise}/actions/runner-groups/{runner_group_id}"))
  let req_body = {"name": $name, "visibility": $visibility} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List organization access to a self-hosted runner group in an enterprise
#
# GET /enterprises/{enterprise}/actions/runner-groups/{runner_group_id}/organizations
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#list-organization-access-to-a-self-hosted-runner-group-in-a-enterprise — API method documentation
# operationId: enterprise-admin/list-org-access-to-self-hosted-runner-group-in-enterprise
export def "enterprises-actions-runner-groups-organizations list-admin-org-access-to-self-hosted" [
  enterprise: string
  runner_group_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<organizations: table<avatar_url: string, description: string, events_url: string, hooks_url: string, id: int, issues_url: string, login: string, members_url: string, node_id: string, public_members_url: string, repos_url: string, url: string>, total_count: float> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({enterprise: (encode-path-segment $enterprise), runner_group_id: (encode-path-segment $runner_group_id)} | format pattern "/enterprises/{enterprise}/actions/runner-groups/{runner_group_id}/organizations") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Set organization access for a self-hosted runner group in an enterprise
#
# PUT /enterprises/{enterprise}/actions/runner-groups/{runner_group_id}/organizations
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#set-organization-access-to-a-self-hosted-runner-group-in-an-enterprise — API method documentation
# operationId: enterprise-admin/set-org-access-to-self-hosted-runner-group-in-enterprise
export def "enterprises-actions-runner-groups-organizations update-admin-org-access-to-self-hosted" [
  enterprise: string
  runner_group_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  selected_organization_ids: list<int> # List of organization IDs that can access the runner group.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({enterprise: (encode-path-segment $enterprise), runner_group_id: (encode-path-segment $runner_group_id)} | format pattern "/enterprises/{enterprise}/actions/runner-groups/{runner_group_id}/organizations"))
  let req_body = {"selected_organization_ids": $selected_organization_ids} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Remove organization access to a self-hosted runner group in an enterprise
#
# DELETE /enterprises/{enterprise}/actions/runner-groups/{runner_group_id}/organizations/{org_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#remove-organization-access-to-a-self-hosted-runner-group-in-an-enterprise — API method documentation
# operationId: enterprise-admin/remove-org-access-to-self-hosted-runner-group-in-enterprise
export def "enterprises-actions-runner-groups-organizations delete-admin-access-to-self-hosted" [
  enterprise: string
  runner_group_id: int
  org_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({enterprise: (encode-path-segment $enterprise), runner_group_id: (encode-path-segment $runner_group_id), org_id: (encode-path-segment $org_id)} | format pattern "/enterprises/{enterprise}/actions/runner-groups/{runner_group_id}/organizations/{org_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add organization access to a self-hosted runner group in an enterprise
#
# PUT /enterprises/{enterprise}/actions/runner-groups/{runner_group_id}/organizations/{org_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#add-organization-access-to-a-self-hosted-runner-group-in-an-enterprise — API method documentation
# operationId: enterprise-admin/add-org-access-to-self-hosted-runner-group-in-enterprise
export def "enterprises-actions-runner-groups-organizations create-admin-access-to-self-hosted" [
  enterprise: string
  runner_group_id: int
  org_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({enterprise: (encode-path-segment $enterprise), runner_group_id: (encode-path-segment $runner_group_id), org_id: (encode-path-segment $org_id)} | format pattern "/enterprises/{enterprise}/actions/runner-groups/{runner_group_id}/organizations/{org_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List self-hosted runners in a group for an enterprise
#
# GET /enterprises/{enterprise}/actions/runner-groups/{runner_group_id}/runners
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#list-self-hosted-runners-in-a-group-for-an-enterprise — API method documentation
# operationId: enterprise-admin/list-self-hosted-runners-in-group-for-enterprise
export def "enterprises-actions-runner-groups-runners list-admin-self-hosted-in" [
  enterprise: string
  runner_group_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<runners: table<busy: bool, id: float, name: string, os: string, status: string>, total_count: float> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({enterprise: (encode-path-segment $enterprise), runner_group_id: (encode-path-segment $runner_group_id)} | format pattern "/enterprises/{enterprise}/actions/runner-groups/{runner_group_id}/runners") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Set self-hosted runners in a group for an enterprise
#
# PUT /enterprises/{enterprise}/actions/runner-groups/{runner_group_id}/runners
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#set-self-hosted-runners-in-a-group-for-an-enterprise — API method documentation
# operationId: enterprise-admin/set-self-hosted-runners-in-group-for-enterprise
export def "enterprises-actions-runner-groups-runners update-admin-self-hosted-in" [
  enterprise: string
  runner_group_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  runners: list<int> # List of runner IDs to add to the runner group.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({enterprise: (encode-path-segment $enterprise), runner_group_id: (encode-path-segment $runner_group_id)} | format pattern "/enterprises/{enterprise}/actions/runner-groups/{runner_group_id}/runners"))
  let req_body = {"runners": $runners} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Remove a self-hosted runner from a group for an enterprise
#
# DELETE /enterprises/{enterprise}/actions/runner-groups/{runner_group_id}/runners/{runner_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#remove-a-self-hosted-runner-from-a-group-for-an-enterprise — API method documentation
# operationId: enterprise-admin/remove-self-hosted-runner-from-group-for-enterprise
export def "enterprises-actions-runner-groups-runners delete-admin-self-hosted-from" [
  enterprise: string
  runner_group_id: int
  runner_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({enterprise: (encode-path-segment $enterprise), runner_group_id: (encode-path-segment $runner_group_id), runner_id: (encode-path-segment $runner_id)} | format pattern "/enterprises/{enterprise}/actions/runner-groups/{runner_group_id}/runners/{runner_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add a self-hosted runner to a group for an enterprise
#
# PUT /enterprises/{enterprise}/actions/runner-groups/{runner_group_id}/runners/{runner_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#add-a-self-hosted-runner-to-a-group-for-an-enterprise — API method documentation
# operationId: enterprise-admin/add-self-hosted-runner-to-group-for-enterprise
export def "enterprises-actions-runner-groups-runners create-admin-self-hosted-to" [
  enterprise: string
  runner_group_id: int
  runner_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({enterprise: (encode-path-segment $enterprise), runner_group_id: (encode-path-segment $runner_group_id), runner_id: (encode-path-segment $runner_id)} | format pattern "/enterprises/{enterprise}/actions/runner-groups/{runner_group_id}/runners/{runner_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List self-hosted runners for an enterprise
#
# GET /enterprises/{enterprise}/actions/runners
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#list-self-hosted-runners-for-an-enterprise — API method documentation
# operationId: enterprise-admin/list-self-hosted-runners-for-enterprise
export def "enterprises-actions-runners list-admin-self-hosted" [
  enterprise: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<runners: table<busy: bool, id: float, name: string, os: string, status: string>, total_count: float> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({enterprise: (encode-path-segment $enterprise)} | format pattern "/enterprises/{enterprise}/actions/runners") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List runner applications for an enterprise
#
# GET /enterprises/{enterprise}/actions/runners/downloads
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#list-runner-applications-for-an-enterprise — API method documentation
# operationId: enterprise-admin/list-runner-applications-for-enterprise
export def "enterprises-actions-runners-downloads list-admin-applications" [
  enterprise: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> table<architecture: string, download_url: string, filename: string, os: string, sha256_checksum: string, temp_download_token: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({enterprise: (encode-path-segment $enterprise)} | format pattern "/enterprises/{enterprise}/actions/runners/downloads"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a registration token for an enterprise
#
# POST /enterprises/{enterprise}/actions/runners/registration-token
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#create-a-registration-token-for-an-enterprise — API method documentation
# operationId: enterprise-admin/create-registration-token-for-enterprise
export def "enterprises-actions-runners-registration-token create-admin" [
  enterprise: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<expires_at: string, permissions: record, repositories: table<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, repository_selection: string, single_file: string, token: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({enterprise: (encode-path-segment $enterprise)} | format pattern "/enterprises/{enterprise}/actions/runners/registration-token"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a remove token for an enterprise
#
# POST /enterprises/{enterprise}/actions/runners/remove-token
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#create-a-remove-token-for-an-enterprise — API method documentation
# operationId: enterprise-admin/create-remove-token-for-enterprise
export def "enterprises-actions-runners-remove-token create-admin" [
  enterprise: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<expires_at: string, permissions: record, repositories: table<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, repository_selection: string, single_file: string, token: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({enterprise: (encode-path-segment $enterprise)} | format pattern "/enterprises/{enterprise}/actions/runners/remove-token"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a self-hosted runner from an enterprise
#
# DELETE /enterprises/{enterprise}/actions/runners/{runner_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#delete-self-hosted-runner-from-an-enterprise — API method documentation
# operationId: enterprise-admin/delete-self-hosted-runner-from-enterprise
export def "enterprises-actions-runners delete-admin-self-hosted" [
  enterprise: string
  runner_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({enterprise: (encode-path-segment $enterprise), runner_id: (encode-path-segment $runner_id)} | format pattern "/enterprises/{enterprise}/actions/runners/{runner_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a self-hosted runner for an enterprise
#
# GET /enterprises/{enterprise}/actions/runners/{runner_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#get-a-self-hosted-runner-for-an-enterprise — API method documentation
# operationId: enterprise-admin/get-self-hosted-runner-for-enterprise
export def "enterprises-actions-runners get-admin-self-hosted" [
  enterprise: string
  runner_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<busy: bool, id: float, name: string, os: string, status: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({enterprise: (encode-path-segment $enterprise), runner_id: (encode-path-segment $runner_id)} | format pattern "/enterprises/{enterprise}/actions/runners/{runner_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List public events
#
# GET /events
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#list-public-events — API method documentation
# operationId: activity/list-public-events
export def "events list-activity-public" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<actor: record<avatar_url: string, display_login: string, gravatar_id: string, id: int, login: string, url: string>, created_at: string, id: string, org: record<avatar_url: string, display_login: string, gravatar_id: string, id: int, login: string, url: string>, payload: record<action: string, comment: record, issue: record, pages: list>, public: bool, repo: record<id: int, name: string, url: string>, type: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/events" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get feeds
#
# GET /feeds
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#get-feeds — API method documentation
# operationId: activity/get-feeds
export def "feeds get-activity" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_links: record<current_user: record<href: string, type: string>, current_user_actor: record<href: string, type: string>, current_user_organization: record<href: string, type: string>, current_user_organizations: list<record>, current_user_public: record<href: string, type: string>, security_advisories: record<href: string, type: string>, timeline: record<href: string, type: string>, user: record<href: string, type: string>>, current_user_actor_url: string, current_user_organization_url: string, current_user_organization_urls: list<string>, current_user_public_url: string, current_user_url: string, timeline_url: string, user_url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/feeds")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List gists for the authenticated user
#
# GET /gists
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/gists#list-gists-for-the-authenticated-user — API method documentation
# operationId: gists/list
export def "gists list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --since: string # Only show notifications updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<comments: int, comments_url: string, commits_url: string, created_at: string, description: string, files: record, forks: list<any>, forks_url: string, git_pull_url: string, git_push_url: string, history: list<any>, html_url: string, id: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, public: bool, truncated: bool, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "since" $since "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/gists" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a gist
#
# POST /gists
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/gists#create-a-gist — API method documentation
# operationId: gists/create
export def "gists create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string # Description of the gist (e.g. Example Ruby script)
  files: record # Names and content for the files that make up the gist (e.g. {hello.rb: {content: puts "Hello, World!"}})
  --public: any
]: any -> record<comments: int, comments_url: string, commits_url: string, created_at: string, description: string, files: record, fork_of: record<comments: int, comments_url: string, commits_url: string, created_at: string, description: string, files: record, forks: list<any>, forks_url: string, git_pull_url: string, git_push_url: string, history: list<any>, html_url: string, id: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, public: bool, truncated: bool, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>>, forks: table<created_at: string, id: string, updated_at: string, url: string, user: record>, forks_url: string, git_pull_url: string, git_push_url: string, history: table<change_status: record, committed_at: string, url: string, user: record, version: string>, html_url: string, id: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, public: bool, truncated: bool, updated_at: string, url: string, user: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/gists")
  let req_body = {"description": $description, "files": $files, "public": $public} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List public gists
#
# GET /gists/public
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/gists#list-public-gists — API method documentation
# operationId: gists/list-public
export def "gists-public list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --since: string # Only show notifications updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<comments: int, comments_url: string, commits_url: string, created_at: string, description: string, files: record, forks: list<any>, forks_url: string, git_pull_url: string, git_push_url: string, history: list<any>, html_url: string, id: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, public: bool, truncated: bool, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "since" $since "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/gists/public" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List starred gists
#
# GET /gists/starred
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/gists#list-starred-gists — API method documentation
# operationId: gists/list-starred
export def "gists-starred list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --since: string # Only show notifications updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<comments: int, comments_url: string, commits_url: string, created_at: string, description: string, files: record, forks: list<any>, forks_url: string, git_pull_url: string, git_push_url: string, history: list<any>, html_url: string, id: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, public: bool, truncated: bool, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "since" $since "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/gists/starred" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a gist
#
# DELETE /gists/{gist_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/gists#delete-a-gist — API method documentation
# operationId: gists/delete
export def "gists delete" [
  gist_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({gist_id: (encode-path-segment $gist_id)} | format pattern "/gists/{gist_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a gist
#
# GET /gists/{gist_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/gists#get-a-gist — API method documentation
# operationId: gists/get
export def "gists get" [
  gist_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<comments: int, comments_url: string, commits_url: string, created_at: string, description: string, files: record, fork_of: record<comments: int, comments_url: string, commits_url: string, created_at: string, description: string, files: record, forks: list<any>, forks_url: string, git_pull_url: string, git_push_url: string, history: list<any>, html_url: string, id: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, public: bool, truncated: bool, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>>, forks: table<created_at: string, id: string, updated_at: string, url: string, user: record>, forks_url: string, git_pull_url: string, git_push_url: string, history: table<change_status: record, committed_at: string, url: string, user: record, version: string>, html_url: string, id: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, public: bool, truncated: bool, updated_at: string, url: string, user: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({gist_id: (encode-path-segment $gist_id)} | format pattern "/gists/{gist_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a gist
#
# PATCH /gists/{gist_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/gists/#update-a-gist — API method documentation
# operationId: gists/update
export def "gists update" [
  gist_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string # Description of the gist (e.g. Example Ruby script)
  --files: record # Names of files to be updated (e.g. {hello.rb: {content: blah, filename: goodbye.rb}})
]: any -> record<comments: int, comments_url: string, commits_url: string, created_at: string, description: string, files: record, fork_of: record<comments: int, comments_url: string, commits_url: string, created_at: string, description: string, files: record, forks: list<any>, forks_url: string, git_pull_url: string, git_push_url: string, history: list<any>, html_url: string, id: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, public: bool, truncated: bool, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>>, forks: table<created_at: string, id: string, updated_at: string, url: string, user: record>, forks_url: string, git_pull_url: string, git_push_url: string, history: table<change_status: record, committed_at: string, url: string, user: record, version: string>, html_url: string, id: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, public: bool, truncated: bool, updated_at: string, url: string, user: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({gist_id: (encode-path-segment $gist_id)} | format pattern "/gists/{gist_id}"))
  let req_body = {"description": $description, "files": $files} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List gist comments
#
# GET /gists/{gist_id}/comments
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/gists#list-gist-comments — API method documentation
# operationId: gists/list-comments
export def "gists-comments list" [
  gist_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<author_association: string, body: string, created_at: string, id: int, node_id: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({gist_id: (encode-path-segment $gist_id)} | format pattern "/gists/{gist_id}/comments") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a gist comment
#
# POST /gists/{gist_id}/comments
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/gists#create-a-gist-comment — API method documentation
# operationId: gists/create-comment
export def "gists-comments create" [
  gist_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  body: string # The comment text. (e.g. Body of the attachment)
]: any -> record<author_association: string, body: string, created_at: string, id: int, node_id: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({gist_id: (encode-path-segment $gist_id)} | format pattern "/gists/{gist_id}/comments"))
  let req_body = {"body": $body} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a gist comment
#
# DELETE /gists/{gist_id}/comments/{comment_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/gists#delete-a-gist-comment — API method documentation
# operationId: gists/delete-comment
export def "gists-comments delete" [
  gist_id: string
  comment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({gist_id: (encode-path-segment $gist_id), comment_id: (encode-path-segment $comment_id)} | format pattern "/gists/{gist_id}/comments/{comment_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a gist comment
#
# GET /gists/{gist_id}/comments/{comment_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/gists#get-a-gist-comment — API method documentation
# operationId: gists/get-comment
export def "gists-comments get" [
  gist_id: string
  comment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<author_association: string, body: string, created_at: string, id: int, node_id: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({gist_id: (encode-path-segment $gist_id), comment_id: (encode-path-segment $comment_id)} | format pattern "/gists/{gist_id}/comments/{comment_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a gist comment
#
# PATCH /gists/{gist_id}/comments/{comment_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/gists#update-a-gist-comment — API method documentation
# operationId: gists/update-comment
export def "gists-comments update" [
  gist_id: string
  comment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  body: string # The comment text. (e.g. Body of the attachment)
]: any -> record<author_association: string, body: string, created_at: string, id: int, node_id: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({gist_id: (encode-path-segment $gist_id), comment_id: (encode-path-segment $comment_id)} | format pattern "/gists/{gist_id}/comments/{comment_id}"))
  let req_body = {"body": $body} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List gist commits
#
# GET /gists/{gist_id}/commits
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/gists#list-gist-commits — API method documentation
# operationId: gists/list-commits
export def "gists-commits list" [
  gist_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<change_status: record<additions: int, deletions: int, total: int>, committed_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, version: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({gist_id: (encode-path-segment $gist_id)} | format pattern "/gists/{gist_id}/commits") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List gist forks
#
# GET /gists/{gist_id}/forks
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/gists#list-gist-forks — API method documentation
# operationId: gists/list-forks
export def "gists-forks list" [
  gist_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<comments: int, comments_url: string, commits_url: string, created_at: string, description: string, files: record, fork_of: record<comments: int, comments_url: string, commits_url: string, created_at: string, description: string, files: record, forks: list, forks_url: string, git_pull_url: string, git_push_url: string, history: list, html_url: string, id: string, node_id: string, owner: record, public: bool, truncated: bool, updated_at: string, url: string, user: record>, forks: list<record>, forks_url: string, git_pull_url: string, git_push_url: string, history: list<record>, html_url: string, id: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, public: bool, truncated: bool, updated_at: string, url: string, user: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({gist_id: (encode-path-segment $gist_id)} | format pattern "/gists/{gist_id}/forks") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Fork a gist
#
# POST /gists/{gist_id}/forks
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/gists#fork-a-gist — API method documentation
# operationId: gists/fork
export def "gists-forks create" [
  gist_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<comments: int, comments_url: string, commits_url: string, created_at: string, description: string, files: record, forks: list<any>, forks_url: string, git_pull_url: string, git_push_url: string, history: list<any>, html_url: string, id: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, public: bool, truncated: bool, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({gist_id: (encode-path-segment $gist_id)} | format pattern "/gists/{gist_id}/forks"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Unstar a gist
#
# DELETE /gists/{gist_id}/star
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/gists#unstar-a-gist — API method documentation
# operationId: gists/unstar
export def "gists-star delete-unstar" [
  gist_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({gist_id: (encode-path-segment $gist_id)} | format pattern "/gists/{gist_id}/star"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Check if a gist is starred
#
# GET /gists/{gist_id}/star
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/gists#check-if-a-gist-is-starred — API method documentation
# operationId: gists/check-is-starred
export def "gists-star check-is-starred" [
  gist_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({gist_id: (encode-path-segment $gist_id)} | format pattern "/gists/{gist_id}/star"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Star a gist
#
# PUT /gists/{gist_id}/star
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/gists#star-a-gist — API method documentation
# operationId: gists/star
export def "gists-star update" [
  gist_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({gist_id: (encode-path-segment $gist_id)} | format pattern "/gists/{gist_id}/star"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a gist revision
#
# GET /gists/{gist_id}/{sha}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/gists#get-a-gist-revision — API method documentation
# operationId: gists/get-revision
export def "gists get-revision" [
  gist_id: string
  sha: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<comments: int, comments_url: string, commits_url: string, created_at: string, description: string, files: record, fork_of: record<comments: int, comments_url: string, commits_url: string, created_at: string, description: string, files: record, forks: list<any>, forks_url: string, git_pull_url: string, git_push_url: string, history: list<any>, html_url: string, id: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, public: bool, truncated: bool, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>>, forks: table<created_at: string, id: string, updated_at: string, url: string, user: record>, forks_url: string, git_pull_url: string, git_push_url: string, history: table<change_status: record, committed_at: string, url: string, user: record, version: string>, html_url: string, id: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, public: bool, truncated: bool, updated_at: string, url: string, user: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({gist_id: (encode-path-segment $gist_id), sha: (encode-path-segment $sha)} | format pattern "/gists/{gist_id}/{sha}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get all gitignore templates
#
# GET /gitignore/templates
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/gitignore#get-all-gitignore-templates — API method documentation
# operationId: gitignore/get-all-templates
export def "gitignore-templates get-list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> list<string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/gitignore/templates")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a gitignore template
#
# GET /gitignore/templates/{name}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/gitignore#get-a-gitignore-template — API method documentation
# operationId: gitignore/get-template
export def "gitignore-templates get" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<name: string, source: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({name: (encode-path-segment $name)} | format pattern "/gitignore/templates/{name}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List repositories accessible to the app installation
#
# GET /installation/repositories
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps#list-repositories-accessible-to-the-app-installation — API method documentation
# operationId: apps/list-repos-accessible-to-installation
export def "installation-repositories list-apps-repos-accessible" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<repositories: table<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, repository_selection: string, total_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/installation/repositories" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Revoke an installation access token
#
# DELETE /installation/token
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps#revoke-an-installation-access-token — API method documentation
# operationId: apps/revoke-installation-access-token
export def "installation-token delete-apps-access" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/installation/token")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List issues assigned to the authenticated user
#
# GET /issues
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#list-issues-assigned-to-the-authenticated-user — API method documentation
# operationId: issues/list
export def "issues list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string@filter-completer # Indicates which sorts of issues to return. Can be one of: \* `assigned`: Issues assigned to you \* `created`: Issues created by you \* `mentioned`: Issues mentioning you \* `subscribed`: Issues you're subscribed to updates for \* `all` or `repos`: All issues the authenticated user can see, regardless of participation or creation (default: assigned)
  --state: string@state-completer # Indicates the state of the issues to return. Can be either `open`, `closed`, or `all`. (default: open)
  --labels: string # A list of comma separated label names. Example: `bug,ui,@high`
  --qp-sort: string@sort-completer-2 # What to sort results by. Can be either `created`, `updated`, `comments`. (default: created)
  --direction: string@direction-completer # One of `asc` (ascending) or `desc` (descending). (default: desc)
  --since: string # Only show notifications updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --collab: oneof<nothing, bool>
  --orgs: oneof<nothing, bool>
  --owned: oneof<nothing, bool>
  --pulls: oneof<nothing, bool>
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<active_lock_reason: string, assignee: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, assignees: list<record>, author_association: string, body: string, body_html: string, body_text: string, closed_at: string, closed_by: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, comments: int, comments_url: string, created_at: string, events_url: string, html_url: string, id: int, labels: list<any>, labels_url: string, locked: bool, milestone: record<closed_at: string, closed_issues: int, created_at: string, creator: record, description: string, due_on: string, html_url: string, id: int, labels_url: string, node_id: string, number: int, open_issues: int, state: string, title: string, updated_at: string, url: string>, node_id: string, number: int, performed_via_github_app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record, pem: string, permissions: record, slug: string, updated_at: string, webhook_secret: string>, pull_request: record<diff_url: string, html_url: string, merged_at: string, patch_url: string, url: string>, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, repository_url: string, state: string, timeline_url: string, title: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "state" $state "scalar") (serialize-qp "labels" $labels "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "direction" $direction "scalar") (serialize-qp "since" $since "scalar") (serialize-qp "collab" $collab "scalar") (serialize-qp "orgs" $orgs "scalar") (serialize-qp "owned" $owned "scalar") (serialize-qp "pulls" $pulls "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/issues" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get all commonly used licenses
#
# GET /licenses
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/licenses#get-all-commonly-used-licenses — API method documentation
# operationId: licenses/get-all-commonly-used
export def "licenses get-list-commonly-used" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --featured: oneof<nothing, bool>
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "featured" $featured "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/licenses" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a license
#
# GET /licenses/{license}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/licenses#get-a-license — API method documentation
# operationId: licenses/get
export def "licenses get" [
  license: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<body: string, conditions: list<string>, description: string, featured: bool, html_url: string, implementation: string, key: string, limitations: list<string>, name: string, node_id: string, permissions: list<string>, spdx_id: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({license: (encode-path-segment $license)} | format pattern "/licenses/{license}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Render a Markdown document
#
# POST /markdown
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/markdown#render-a-markdown-document — API method documentation
# operationId: markdown/render
export def "markdown create-render" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --context: string # The repository context to use when creating references in `gfm` mode.
  --mode: string@mode-completer # The rendering mode. (default: markdown, e.g. markdown)
  text: string # The Markdown text to render in HTML.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/markdown")
  let req_body = {"context": $context, "mode": $mode, "text": $text} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "text/html"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Render a Markdown document in raw mode
#
# POST /markdown/raw
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/markdown#render-a-markdown-document-in-raw-mode — API method documentation
# operationId: markdown/render-raw
export def "markdown-raw create-render" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/markdown/raw")
  let req_body = $body
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "text/html"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "text/plain" $req_body
}

# Get GitHub Enterprise Server meta information
#
# GET /meta
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/meta#get-github-meta-information — API method documentation
# operationId: meta/get
export def "meta get" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<dependabot: list<string>, installed_version: string, packages: list<string>, verifiable_password_authentication: bool> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/meta")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List public events for a network of repositories
#
# GET /networks/{owner}/{repo}/events
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#list-public-events-for-a-network-of-repositories — API method documentation
# operationId: activity/list-public-events-for-repo-network
export def "networks-events list-activity-public" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<actor: record<avatar_url: string, display_login: string, gravatar_id: string, id: int, login: string, url: string>, created_at: string, id: string, org: record<avatar_url: string, display_login: string, gravatar_id: string, id: int, login: string, url: string>, payload: record<action: string, comment: record, issue: record, pages: list>, public: bool, repo: record<id: int, name: string, url: string>, type: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/networks/{owner}/{repo}/events") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List notifications for the authenticated user
#
# GET /notifications
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#list-notifications-for-the-authenticated-user — API method documentation
# operationId: activity/list-notifications-for-authenticated-user
export def "notifications list-activity-for-authenticated-user" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --all: oneof<nothing, bool> # If `true`, show notifications marked as read. (default: false)
  --participating: oneof<nothing, bool> # If `true`, only shows notifications in which the user is directly participating or mentioned. (default: false)
  --since: string # Only show notifications updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --before: string # Only show notifications updated before the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<id: string, last_read_at: string, reason: string, repository: record<allow_forking: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, subject: record<latest_comment_url: string, title: string, type: string, url: string>, subscription_url: string, unread: bool, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "all" $all "scalar") (serialize-qp "participating" $participating "scalar") (serialize-qp "since" $since "scalar") (serialize-qp "before" $before "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/notifications" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Mark notifications as read
#
# PUT /notifications
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#mark-notifications-as-read — API method documentation
# operationId: activity/mark-notifications-as-read
export def "notifications get-activity-mark-as" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --last-read-at: string # Describes the last point that notifications were checked. (format: date-time)
  --read: oneof<nothing, bool> # Whether the notification has been read.
]: any -> record<message: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/notifications")
  let req_body = {"last_read_at": $last_read_at, "read": $read} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Get a thread
#
# GET /notifications/threads/{thread_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#get-a-thread — API method documentation
# operationId: activity/get-thread
export def "notifications-threads get-activity" [
  thread_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<id: string, last_read_at: string, reason: string, repository: record<allow_forking: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record<body: string, html_url: string, key: string, name: string, url: string>, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<key: string, name: string, node_id: string, spdx_id: string, url: string>, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, subject: record<latest_comment_url: string, title: string, type: string, url: string>, subscription_url: string, unread: bool, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({thread_id: (encode-path-segment $thread_id)} | format pattern "/notifications/threads/{thread_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Mark a thread as read
#
# PATCH /notifications/threads/{thread_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#mark-a-thread-as-read — API method documentation
# operationId: activity/mark-thread-as-read
export def "notifications-threads get-activity-mark-as" [
  thread_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({thread_id: (encode-path-segment $thread_id)} | format pattern "/notifications/threads/{thread_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a thread subscription
#
# DELETE /notifications/threads/{thread_id}/subscription
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#delete-a-thread-subscription — API method documentation
# operationId: activity/delete-thread-subscription
export def "notifications-threads-subscription delete-activity" [
  thread_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({thread_id: (encode-path-segment $thread_id)} | format pattern "/notifications/threads/{thread_id}/subscription"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a thread subscription for the authenticated user
#
# GET /notifications/threads/{thread_id}/subscription
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#get-a-thread-subscription-for-the-authenticated-user — API method documentation
# operationId: activity/get-thread-subscription-for-authenticated-user
export def "notifications-threads-subscription get-activity-for-authenticated-user" [
  thread_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created_at: string, ignored: bool, reason: string, repository_url: string, subscribed: bool, thread_url: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({thread_id: (encode-path-segment $thread_id)} | format pattern "/notifications/threads/{thread_id}/subscription"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Set a thread subscription
#
# PUT /notifications/threads/{thread_id}/subscription
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#set-a-thread-subscription — API method documentation
# operationId: activity/set-thread-subscription
export def "notifications-threads-subscription update-activity" [
  thread_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --ignored: oneof<nothing, bool> # Whether to block all notifications from a thread. (default: false)
]: any -> record<created_at: string, ignored: bool, reason: string, repository_url: string, subscribed: bool, thread_url: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({thread_id: (encode-path-segment $thread_id)} | format pattern "/notifications/threads/{thread_id}/subscription"))
  let req_body = {"ignored": $ignored} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Get Octocat
#
# GET /octocat
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/meta#get-octocat — API method documentation
# operationId: meta/get-octocat
export def "octocat get-meta" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --s: string # The words to show in Octocat's speech bubble
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "s" $s "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/octocat" $qp)
  let accept_val = "application/octocat-stream"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List organizations
#
# GET /organizations
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#list-organizations — API method documentation
# operationId: orgs/list
export def "organizations list-orgs" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --since: int # An organization ID. Only return organizations with an ID greater than this ID.
  --per-page: int # Results per page (max 100) (default: 30)
]: nothing -> table<avatar_url: string, description: string, events_url: string, hooks_url: string, id: int, issues_url: string, login: string, members_url: string, node_id: string, public_members_url: string, repos_url: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "since" $since "scalar") (serialize-qp "per_page" $per_page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/organizations" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get an organization
#
# GET /orgs/{org}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#get-an-organization — API method documentation
# operationId: orgs/get
export def "orgs get" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<avatar_url: string, billing_email: string, blog: string, collaborators: int, company: string, created_at: string, default_repository_permission: string, description: string, disk_usage: int, email: string, events_url: string, followers: int, following: int, has_organization_projects: bool, has_repository_projects: bool, hooks_url: string, html_url: string, id: int, is_verified: bool, issues_url: string, location: string, login: string, members_allowed_repository_creation_type: string, members_can_create_internal_repositories: bool, members_can_create_pages: bool, members_can_create_private_pages: bool, members_can_create_private_repositories: bool, members_can_create_public_pages: bool, members_can_create_public_repositories: bool, members_can_create_repositories: bool, members_url: string, name: string, node_id: string, owned_private_repos: int, plan: record<filled_seats: int, name: string, private_repos: int, seats: int, space: int>, private_gists: int, public_gists: int, public_members_url: string, public_repos: int, repos_url: string, total_private_repos: int, twitter_username: string, two_factor_requirement_enabled: bool, type: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/orgs/{org}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update an organization
#
# PATCH /orgs/{org}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs/#update-an-organization — API method documentation
# operationId: orgs/update
export def "orgs update" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --billing-email: string # Billing email address. This address is not publicized.
  --blog: string # e.g. "http://github.blog"
  --company: string # The company name.
  --default-repository-permission: string@default-repository-permission-completer # Default permission level members have for organization repositories: \* `read` - can pull, but not push to or administer this repository. \* `write` - can pull and push, but not administer this repository. \* `admin` - can pull, push, and administer this repository. \* `none` - no permissions granted by default. (default: read)
  --description: string # The description of the company.
  --email: string # The publicly visible email address.
  --has-organization-projects: oneof<nothing, bool> # Toggles whether an organization can use organization projects.
  --has-repository-projects: oneof<nothing, bool> # Toggles whether repositories that belong to the organization can use repository projects.
  --location: string # The location.
  --members-allowed-repository-creation-type: string@members-allowed-repository-creation-type-completer # Specifies which types of repositories non-admin organization members can create. Can be one of: \* `all` - all organization members can create public and private repositories. \* `private` - members can create private repositories. This option is only available to repositories that are part of an organization on GitHub Enterprise Cloud. \* `none` - only admin members can create repositories. **Note:** This parameter is deprecated and will be removed in the future. Its return value ignores internal repositories. Using this parameter overrides values set in `members_can_create_repositories`. See the parameter deprecation notice in the operation description for details.
  --members-can-create-internal-repositories: oneof<nothing, bool> # Toggles whether organization members can create internal repositories, which are visible to all enterprise members. You can only allow members to create internal repositories if your organization is associated with an enterprise account using GitHub Enterprise Cloud or GitHub Enterprise Server 2.20+. Can be one of: \* `true` - all organization members can create internal repositories. \* `false` - only organization owners can create internal repositories. Default: `true`. For more information, see "[Restricting repository creation in your organization](https://help.github.com/github/setting-up-and-managing-organizations-and-teams/restricting-repository-creation-in-your-organization)" in the GitHub Help documentation.
  --members-can-create-private-repositories: oneof<nothing, bool> # Toggles whether organization members can create private repositories, which are visible to organization members with permission. Can be one of: \* `true` - all organization members can create private repositories. \* `false` - only organization owners can create private repositories. Default: `true`. For more information, see "[Restricting repository creation in your organization](https://help.github.com/github/setting-up-and-managing-organizations-and-teams/restricting-repository-creation-in-your-organization)" in the GitHub Help documentation.
  --members-can-create-public-repositories: oneof<nothing, bool> # Toggles whether organization members can create public repositories, which are visible to anyone. Can be one of: \* `true` - all organization members can create public repositories. \* `false` - only organization owners can create public repositories. Default: `true`. For more information, see "[Restricting repository creation in your organization](https://help.github.com/github/setting-up-and-managing-organizations-and-teams/restricting-repository-creation-in-your-organization)" in the GitHub Help documentation.
  --members-can-create-repositories: oneof<nothing, bool> # Toggles the ability of non-admin organization members to create repositories. Can be one of: \* `true` - all organization members can create repositories. \* `false` - only organization owners can create repositories. Default: `true` **Note:** A parameter can override this parameter. See `members_allowed_repository_creation_type` in this table for details. **Note:** A parameter can override this parameter. See `members_allowed_repository_creation_type` in this table for details. (default: true)
  --name: string # The shorthand name of the company.
  --twitter-username: string # The Twitter username of the company.
]: any -> record<avatar_url: string, billing_email: string, blog: string, collaborators: int, company: string, created_at: string, default_repository_permission: string, description: string, disk_usage: int, email: string, events_url: string, followers: int, following: int, has_organization_projects: bool, has_repository_projects: bool, hooks_url: string, html_url: string, id: int, is_verified: bool, issues_url: string, location: string, login: string, members_allowed_repository_creation_type: string, members_can_create_internal_repositories: bool, members_can_create_pages: bool, members_can_create_private_pages: bool, members_can_create_private_repositories: bool, members_can_create_public_pages: bool, members_can_create_public_repositories: bool, members_can_create_repositories: bool, members_url: string, name: string, node_id: string, owned_private_repos: int, plan: record<filled_seats: int, name: string, private_repos: int, seats: int, space: int>, private_gists: int, public_gists: int, public_members_url: string, public_repos: int, repos_url: string, total_private_repos: int, twitter_username: string, two_factor_requirement_enabled: bool, type: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/orgs/{org}"))
  let req_body = {"billing_email": $billing_email, "blog": $blog, "company": $company, "default_repository_permission": $default_repository_permission, "description": $description, "email": $email, "has_organization_projects": $has_organization_projects, "has_repository_projects": $has_repository_projects, "location": $location, "members_allowed_repository_creation_type": $members_allowed_repository_creation_type, "members_can_create_internal_repositories": $members_can_create_internal_repositories, "members_can_create_private_repositories": $members_can_create_private_repositories, "members_can_create_public_repositories": $members_can_create_public_repositories, "members_can_create_repositories": $members_can_create_repositories, "name": $name, "twitter_username": $twitter_username} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List self-hosted runner groups for an organization
#
# GET /orgs/{org}/actions/runner-groups
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#list-self-hosted-runner-groups-for-an-organization — API method documentation
# operationId: actions/list-self-hosted-runner-groups-for-org
export def "orgs-actions-runner-groups list-self-hosted" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<runner_groups: table<allows_public_repositories: bool, default: bool, id: float, inherited: bool, inherited_allows_public_repositories: bool, name: string, runners_url: string, selected_repositories_url: string, visibility: string>, total_count: float> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/orgs/{org}/actions/runner-groups") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a self-hosted runner group for an organization
#
# POST /orgs/{org}/actions/runner-groups
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#create-a-self-hosted-runner-group-for-an-organization — API method documentation
# operationId: actions/create-self-hosted-runner-group-for-org
export def "orgs-actions-runner-groups create-self-hosted" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  name: string # Name of the runner group.
  --runners: list<int> # List of runner IDs to add to the runner group.
  --selected-repository-ids: list<int> # List of repository IDs that can access the runner group.
  --visibility: string@visibility-completer-1 # Visibility of a runner group. You can select all repositories, select individual repositories, or limit access to private repositories. Can be one of: `all`, `selected`, or `private`. (default: all)
]: any -> record<allows_public_repositories: bool, default: bool, id: float, inherited: bool, inherited_allows_public_repositories: bool, name: string, runners_url: string, selected_repositories_url: string, visibility: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/orgs/{org}/actions/runner-groups"))
  let req_body = {"name": $name, "runners": $runners, "selected_repository_ids": $selected_repository_ids, "visibility": $visibility} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a self-hosted runner group from an organization
#
# DELETE /orgs/{org}/actions/runner-groups/{runner_group_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#delete-a-self-hosted-runner-group-from-an-organization — API method documentation
# operationId: actions/delete-self-hosted-runner-group-from-org
export def "orgs-actions-runner-groups delete-self-hosted" [
  org: string
  runner_group_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), runner_group_id: (encode-path-segment $runner_group_id)} | format pattern "/orgs/{org}/actions/runner-groups/{runner_group_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a self-hosted runner group for an organization
#
# GET /orgs/{org}/actions/runner-groups/{runner_group_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#get-a-self-hosted-runner-group-for-an-organization — API method documentation
# operationId: actions/get-self-hosted-runner-group-for-org
export def "orgs-actions-runner-groups get-self-hosted" [
  org: string
  runner_group_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<allows_public_repositories: bool, default: bool, id: float, inherited: bool, inherited_allows_public_repositories: bool, name: string, runners_url: string, selected_repositories_url: string, visibility: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), runner_group_id: (encode-path-segment $runner_group_id)} | format pattern "/orgs/{org}/actions/runner-groups/{runner_group_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a self-hosted runner group for an organization
#
# PATCH /orgs/{org}/actions/runner-groups/{runner_group_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#update-a-self-hosted-runner-group-for-an-organization — API method documentation
# operationId: actions/update-self-hosted-runner-group-for-org
export def "orgs-actions-runner-groups update-self-hosted" [
  org: string
  runner_group_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  name: string # Name of the runner group.
  --visibility: string@visibility-completer-1 # Visibility of a runner group. You can select all repositories, select individual repositories, or all private repositories. Can be one of: `all`, `selected`, or `private`.
]: any -> record<allows_public_repositories: bool, default: bool, id: float, inherited: bool, inherited_allows_public_repositories: bool, name: string, runners_url: string, selected_repositories_url: string, visibility: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), runner_group_id: (encode-path-segment $runner_group_id)} | format pattern "/orgs/{org}/actions/runner-groups/{runner_group_id}"))
  let req_body = {"name": $name, "visibility": $visibility} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List repository access to a self-hosted runner group in an organization
#
# GET /orgs/{org}/actions/runner-groups/{runner_group_id}/repositories
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#list-repository-access-to-a-self-hosted-runner-group-in-an-organization — API method documentation
# operationId: actions/list-repo-access-to-self-hosted-runner-group-in-org
export def "orgs-actions-runner-groups-repositories list-repo-access-to-self-hosted" [
  org: string
  runner_group_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --page: int # Page number of the results to fetch. (default: 1)
  --per-page: int # Results per page (max 100) (default: 30)
]: nothing -> record<repositories: table<allow_forking: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, total_count: float> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "page" $page "scalar") (serialize-qp "per_page" $per_page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({org: (encode-path-segment $org), runner_group_id: (encode-path-segment $runner_group_id)} | format pattern "/orgs/{org}/actions/runner-groups/{runner_group_id}/repositories") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Set repository access for a self-hosted runner group in an organization
#
# PUT /orgs/{org}/actions/runner-groups/{runner_group_id}/repositories
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#set-repository-access-to-a-self-hosted-runner-group-in-an-organization — API method documentation
# operationId: actions/set-repo-access-to-self-hosted-runner-group-in-org
export def "orgs-actions-runner-groups-repositories update-repo-access-to-self-hosted" [
  org: string
  runner_group_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  selected_repository_ids: list<int> # List of repository IDs that can access the runner group.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), runner_group_id: (encode-path-segment $runner_group_id)} | format pattern "/orgs/{org}/actions/runner-groups/{runner_group_id}/repositories"))
  let req_body = {"selected_repository_ids": $selected_repository_ids} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Remove repository access to a self-hosted runner group in an organization
#
# DELETE /orgs/{org}/actions/runner-groups/{runner_group_id}/repositories/{repository_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#remove-repository-access-to-a-self-hosted-runner-group-in-an-organization — API method documentation
# operationId: actions/remove-repo-access-to-self-hosted-runner-group-in-org
export def "orgs-actions-runner-groups-repositories delete-repo-access-to-self-hosted" [
  org: string
  runner_group_id: int
  repository_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), runner_group_id: (encode-path-segment $runner_group_id), repository_id: (encode-path-segment $repository_id)} | format pattern "/orgs/{org}/actions/runner-groups/{runner_group_id}/repositories/{repository_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add repository access to a self-hosted runner group in an organization
#
# PUT /orgs/{org}/actions/runner-groups/{runner_group_id}/repositories/{repository_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#add-repository-acess-to-a-self-hosted-runner-group-in-an-organization — API method documentation
# operationId: actions/add-repo-access-to-self-hosted-runner-group-in-org
export def "orgs-actions-runner-groups-repositories create-repo-access-to-self-hosted" [
  org: string
  runner_group_id: int
  repository_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), runner_group_id: (encode-path-segment $runner_group_id), repository_id: (encode-path-segment $repository_id)} | format pattern "/orgs/{org}/actions/runner-groups/{runner_group_id}/repositories/{repository_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List self-hosted runners in a group for an organization
#
# GET /orgs/{org}/actions/runner-groups/{runner_group_id}/runners
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#list-self-hosted-runners-in-a-group-for-an-organization — API method documentation
# operationId: actions/list-self-hosted-runners-in-group-for-org
export def "orgs-actions-runner-groups-runners list-self-hosted-in" [
  org: string
  runner_group_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<runners: table<busy: bool, id: float, name: string, os: string, status: string>, total_count: float> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({org: (encode-path-segment $org), runner_group_id: (encode-path-segment $runner_group_id)} | format pattern "/orgs/{org}/actions/runner-groups/{runner_group_id}/runners") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Set self-hosted runners in a group for an organization
#
# PUT /orgs/{org}/actions/runner-groups/{runner_group_id}/runners
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#set-self-hosted-runners-in-a-group-for-an-organization — API method documentation
# operationId: actions/set-self-hosted-runners-in-group-for-org
export def "orgs-actions-runner-groups-runners update-self-hosted-in" [
  org: string
  runner_group_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  runners: list<int> # List of runner IDs to add to the runner group.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), runner_group_id: (encode-path-segment $runner_group_id)} | format pattern "/orgs/{org}/actions/runner-groups/{runner_group_id}/runners"))
  let req_body = {"runners": $runners} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Remove a self-hosted runner from a group for an organization
#
# DELETE /orgs/{org}/actions/runner-groups/{runner_group_id}/runners/{runner_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#remove-a-self-hosted-runner-from-a-group-for-an-organization — API method documentation
# operationId: actions/remove-self-hosted-runner-from-group-for-org
export def "orgs-actions-runner-groups-runners delete-self-hosted-from" [
  org: string
  runner_group_id: int
  runner_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), runner_group_id: (encode-path-segment $runner_group_id), runner_id: (encode-path-segment $runner_id)} | format pattern "/orgs/{org}/actions/runner-groups/{runner_group_id}/runners/{runner_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add a self-hosted runner to a group for an organization
#
# PUT /orgs/{org}/actions/runner-groups/{runner_group_id}/runners/{runner_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#add-a-self-hosted-runner-to-a-group-for-an-organization — API method documentation
# operationId: actions/add-self-hosted-runner-to-group-for-org
export def "orgs-actions-runner-groups-runners create-self-hosted-to" [
  org: string
  runner_group_id: int
  runner_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), runner_group_id: (encode-path-segment $runner_group_id), runner_id: (encode-path-segment $runner_id)} | format pattern "/orgs/{org}/actions/runner-groups/{runner_group_id}/runners/{runner_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List self-hosted runners for an organization
#
# GET /orgs/{org}/actions/runners
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#list-self-hosted-runners-for-an-organization — API method documentation
# operationId: actions/list-self-hosted-runners-for-org
export def "orgs-actions-runners list-self-hosted" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<runners: table<busy: bool, id: float, name: string, os: string, status: string>, total_count: float> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/orgs/{org}/actions/runners") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List runner applications for an organization
#
# GET /orgs/{org}/actions/runners/downloads
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#list-runner-applications-for-an-organization — API method documentation
# operationId: actions/list-runner-applications-for-org
export def "orgs-actions-runners-downloads list-applications" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> table<architecture: string, download_url: string, filename: string, os: string, sha256_checksum: string, temp_download_token: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/orgs/{org}/actions/runners/downloads"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a registration token for an organization
#
# POST /orgs/{org}/actions/runners/registration-token
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#create-a-registration-token-for-an-organization — API method documentation
# operationId: actions/create-registration-token-for-org
export def "orgs-actions-runners-registration-token create" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<expires_at: string, permissions: record, repositories: table<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, repository_selection: string, single_file: string, token: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/orgs/{org}/actions/runners/registration-token"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a remove token for an organization
#
# POST /orgs/{org}/actions/runners/remove-token
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#create-a-remove-token-for-an-organization — API method documentation
# operationId: actions/create-remove-token-for-org
export def "orgs-actions-runners-remove-token create" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<expires_at: string, permissions: record, repositories: table<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, repository_selection: string, single_file: string, token: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/orgs/{org}/actions/runners/remove-token"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a self-hosted runner from an organization
#
# DELETE /orgs/{org}/actions/runners/{runner_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#delete-a-self-hosted-runner-from-an-organization — API method documentation
# operationId: actions/delete-self-hosted-runner-from-org
export def "orgs-actions-runners delete-self-hosted" [
  org: string
  runner_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), runner_id: (encode-path-segment $runner_id)} | format pattern "/orgs/{org}/actions/runners/{runner_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a self-hosted runner for an organization
#
# GET /orgs/{org}/actions/runners/{runner_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#get-a-self-hosted-runner-for-an-organization — API method documentation
# operationId: actions/get-self-hosted-runner-for-org
export def "orgs-actions-runners get-self-hosted" [
  org: string
  runner_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<busy: bool, id: float, name: string, os: string, status: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), runner_id: (encode-path-segment $runner_id)} | format pattern "/orgs/{org}/actions/runners/{runner_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List organization secrets
#
# GET /orgs/{org}/actions/secrets
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#list-organization-secrets — API method documentation
# operationId: actions/list-org-secrets
export def "orgs-actions-secrets list" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<secrets: table<created_at: string, name: string, selected_repositories_url: string, updated_at: string, visibility: string>, total_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/orgs/{org}/actions/secrets") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get an organization public key
#
# GET /orgs/{org}/actions/secrets/public-key
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#get-an-organization-public-key — API method documentation
# operationId: actions/get-org-public-key
export def "orgs-actions-secrets-public-key get" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created_at: string, id: int, key: string, key_id: string, title: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/orgs/{org}/actions/secrets/public-key"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an organization secret
#
# DELETE /orgs/{org}/actions/secrets/{secret_name}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#delete-an-organization-secret — API method documentation
# operationId: actions/delete-org-secret
export def "orgs-actions-secrets delete" [
  org: string
  secret_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), secret_name: (encode-path-segment $secret_name)} | format pattern "/orgs/{org}/actions/secrets/{secret_name}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get an organization secret
#
# GET /orgs/{org}/actions/secrets/{secret_name}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#get-an-organization-secret — API method documentation
# operationId: actions/get-org-secret
export def "orgs-actions-secrets get" [
  org: string
  secret_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created_at: string, name: string, selected_repositories_url: string, updated_at: string, visibility: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), secret_name: (encode-path-segment $secret_name)} | format pattern "/orgs/{org}/actions/secrets/{secret_name}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create or update an organization secret
#
# PUT /orgs/{org}/actions/secrets/{secret_name}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#create-or-update-an-organization-secret — API method documentation
# operationId: actions/create-or-update-org-secret
export def "orgs-actions-secrets create-or-update" [
  org: string
  secret_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --encrypted-value: string # Value for your secret, encrypted with [LibSodium](https://libsodium.gitbook.io/doc/bindings_for_other_languages) using the public key retrieved from the [Get an organization public key](https://docs.github.com/enterprise-server@2.22/rest/reference/actions#get-an-organization-public-key) endpoint.
  --key-id: string # ID of the key you used to encrypt the secret.
  --selected-repository-ids: list<string> # An array of repository ids that can access the organization secret. You can only provide a list of repository ids when the `visibility` is set to `selected`. You can manage the list of selected repositories using the [List selected repositories for an organization secret](https://docs.github.com/enterprise-server@2.22/rest/reference/actions#list-selected-repositories-for-an-organization-secret), [Set selected repositories for an organization secret](https://docs.github.com/enterprise-server@2.22/rest/reference/actions#set-selected-repositories-for-an-organization-secret), and [Remove selected repository from an organization secret](https://docs.github.com/enterprise-server@2.22/rest/reference/actions#remove-selected-repository-from-an-organization-secret) endpoints.
  visibility: string@visibility-completer-1 # Configures the access that repositories have to the organization secret. Can be one of: \- `all` - All repositories in an organization can access the secret. \- `private` - Private repositories in an organization can access the secret. \- `selected` - Only specific repositories can access the secret.
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), secret_name: (encode-path-segment $secret_name)} | format pattern "/orgs/{org}/actions/secrets/{secret_name}"))
  let req_body = {"encrypted_value": $encrypted_value, "key_id": $key_id, "selected_repository_ids": $selected_repository_ids, "visibility": $visibility} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List selected repositories for an organization secret
#
# GET /orgs/{org}/actions/secrets/{secret_name}/repositories
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#list-selected-repositories-for-an-organization-secret — API method documentation
# operationId: actions/list-selected-repos-for-org-secret
export def "orgs-actions-secrets-repositories list-selected-repos" [
  org: string
  secret_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --page: int # Page number of the results to fetch. (default: 1)
  --per-page: int # Results per page (max 100) (default: 30)
]: nothing -> record<repositories: table<allow_forking: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, total_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "page" $page "scalar") (serialize-qp "per_page" $per_page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({org: (encode-path-segment $org), secret_name: (encode-path-segment $secret_name)} | format pattern "/orgs/{org}/actions/secrets/{secret_name}/repositories") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Set selected repositories for an organization secret
#
# PUT /orgs/{org}/actions/secrets/{secret_name}/repositories
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#set-selected-repositories-for-an-organization-secret — API method documentation
# operationId: actions/set-selected-repos-for-org-secret
export def "orgs-actions-secrets-repositories update-selected-repos" [
  org: string
  secret_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  selected_repository_ids: list<int> # An array of repository ids that can access the organization secret. You can only provide a list of repository ids when the `visibility` is set to `selected`. You can add and remove individual repositories using the [Set selected repositories for an organization secret](https://docs.github.com/enterprise-server@2.22/rest/reference/actions#set-selected-repositories-for-an-organization-secret) and [Remove selected repository from an organization secret](https://docs.github.com/enterprise-server@2.22/rest/reference/actions#remove-selected-repository-from-an-organization-secret) endpoints.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), secret_name: (encode-path-segment $secret_name)} | format pattern "/orgs/{org}/actions/secrets/{secret_name}/repositories"))
  let req_body = {"selected_repository_ids": $selected_repository_ids} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Remove selected repository from an organization secret
#
# DELETE /orgs/{org}/actions/secrets/{secret_name}/repositories/{repository_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#remove-selected-repository-from-an-organization-secret — API method documentation
# operationId: actions/remove-selected-repo-from-org-secret
export def "orgs-actions-secrets-repositories delete-selected-repo" [
  org: string
  secret_name: string
  repository_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), secret_name: (encode-path-segment $secret_name), repository_id: (encode-path-segment $repository_id)} | format pattern "/orgs/{org}/actions/secrets/{secret_name}/repositories/{repository_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add selected repository to an organization secret
#
# PUT /orgs/{org}/actions/secrets/{secret_name}/repositories/{repository_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#add-selected-repository-to-an-organization-secret — API method documentation
# operationId: actions/add-selected-repo-to-org-secret
export def "orgs-actions-secrets-repositories create-selected-repo" [
  org: string
  secret_name: string
  repository_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), secret_name: (encode-path-segment $secret_name), repository_id: (encode-path-segment $repository_id)} | format pattern "/orgs/{org}/actions/secrets/{secret_name}/repositories/{repository_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List public organization events
#
# GET /orgs/{org}/events
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#list-public-organization-events — API method documentation
# operationId: activity/list-public-org-events
export def "orgs-events list-activity-public" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<actor: record<avatar_url: string, display_login: string, gravatar_id: string, id: int, login: string, url: string>, created_at: string, id: string, org: record<avatar_url: string, display_login: string, gravatar_id: string, id: int, login: string, url: string>, payload: record<action: string, comment: record, issue: record, pages: list>, public: bool, repo: record<id: int, name: string, url: string>, type: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/orgs/{org}/events") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List organization webhooks
#
# GET /orgs/{org}/hooks
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#list-organization-webhooks — API method documentation
# operationId: orgs/list-webhooks
export def "orgs-hooks list-webhooks" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<active: bool, config: record<content_type: string, insecure_ssl: string, secret: string, url: string>, created_at: string, events: list<string>, id: int, name: string, ping_url: string, type: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/orgs/{org}/hooks") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an organization webhook
#
# POST /orgs/{org}/hooks
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#create-an-organization-webhook — API method documentation
# operationId: orgs/create-webhook
# --config shape: {content_type?: string, insecure_ssl?: any, password?: string, secret?: string, url: string, username?: string}
export def "orgs-hooks create-webhook" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --active: oneof<nothing, bool> # Determines if notifications are sent when the webhook is triggered. Set to `true` to send notifications. (default: true)
  config: record # Key/value pairs to provide settings for this webhook. [These are defined below](https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#create-hook-config-params). — shape: {content_type?: string, insecure_ssl?: any, password?: string, secret?: string, url: string, username?: string}
  --events: list<string> # Determines what [events](https://docs.github.com/enterprise-server@2.22/webhooks/event-payloads) the hook is triggered for. (default: [push])
  name: string # Must be passed as "web".
]: any -> record<active: bool, config: record<content_type: string, insecure_ssl: string, secret: string, url: string>, created_at: string, events: list<string>, id: int, name: string, ping_url: string, type: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/orgs/{org}/hooks"))
  let req_body = {"active": $active, "config": $config, "events": $events, "name": $name} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete an organization webhook
#
# DELETE /orgs/{org}/hooks/{hook_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#delete-an-organization-webhook — API method documentation
# operationId: orgs/delete-webhook
export def "orgs-hooks delete-webhook" [
  org: string
  hook_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), hook_id: (encode-path-segment $hook_id)} | format pattern "/orgs/{org}/hooks/{hook_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get an organization webhook
#
# GET /orgs/{org}/hooks/{hook_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#get-an-organization-webhook — API method documentation
# operationId: orgs/get-webhook
export def "orgs-hooks get-webhook" [
  org: string
  hook_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<active: bool, config: record<content_type: string, insecure_ssl: string, secret: string, url: string>, created_at: string, events: list<string>, id: int, name: string, ping_url: string, type: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), hook_id: (encode-path-segment $hook_id)} | format pattern "/orgs/{org}/hooks/{hook_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update an organization webhook
#
# PATCH /orgs/{org}/hooks/{hook_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#update-an-organization-webhook — API method documentation
# operationId: orgs/update-webhook
# --config shape: {content_type?: string, insecure_ssl?: any, secret?: string, url: string}
export def "orgs-hooks update-webhook" [
  org: string
  hook_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --active: oneof<nothing, bool> # Determines if notifications are sent when the webhook is triggered. Set to `true` to send notifications. (default: true)
  --config: record # Key/value pairs to provide settings for this webhook. [These are defined below](https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#update-hook-config-params). — shape: {content_type?: string, insecure_ssl?: any, secret?: string, url: string}
  --events: list<string> # Determines what [events](https://docs.github.com/enterprise-server@2.22/webhooks/event-payloads) the hook is triggered for. (default: [push])
  --name: string # e.g. "web"
]: any -> record<active: bool, config: record<content_type: string, insecure_ssl: string, secret: string, url: string>, created_at: string, events: list<string>, id: int, name: string, ping_url: string, type: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), hook_id: (encode-path-segment $hook_id)} | format pattern "/orgs/{org}/hooks/{hook_id}"))
  let req_body = {"active": $active, "config": $config, "events": $events, "name": $name} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Ping an organization webhook
#
# POST /orgs/{org}/hooks/{hook_id}/pings
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#ping-an-organization-webhook — API method documentation
# operationId: orgs/ping-webhook
export def "orgs-hooks-pings ping-webhook" [
  org: string
  hook_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), hook_id: (encode-path-segment $hook_id)} | format pattern "/orgs/{org}/hooks/{hook_id}/pings"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get an organization installation for the authenticated app
#
# GET /orgs/{org}/installation
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps#get-an-organization-installation-for-the-authenticated-app — API method documentation
# operationId: apps/get-org-installation
export def "orgs-installation get-apps" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<access_tokens_url: string, account: any, app_id: int, app_slug: string, contact_email: string, created_at: string, events: list<string>, html_url: string, id: int, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string, organization_administration: string, pull_requests: string, statuses: string>, repositories_url: string, repository_selection: string, single_file_name: string, suspended_at: string, suspended_by: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, target_id: int, target_type: string, updated_at: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/orgs/{org}/installation"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List app installations for an organization
#
# GET /orgs/{org}/installations
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#list-app-installations-for-an-organization — API method documentation
# operationId: orgs/list-app-installations
export def "orgs-installations list-app" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<installations: table<access_tokens_url: string, account: any, app_id: int, app_slug: string, contact_email: string, created_at: string, events: list, html_url: string, id: int, permissions: record, repositories_url: string, repository_selection: string, single_file_name: string, suspended_at: string, suspended_by: record, target_id: int, target_type: string, updated_at: string>, total_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/orgs/{org}/installations") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List organization issues assigned to the authenticated user
#
# GET /orgs/{org}/issues
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#list-organization-issues-assigned-to-the-authenticated-user — API method documentation
# operationId: issues/list-for-org
export def "orgs-issues list" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string@filter-completer # Indicates which sorts of issues to return. Can be one of: \* `assigned`: Issues assigned to you \* `created`: Issues created by you \* `mentioned`: Issues mentioning you \* `subscribed`: Issues you're subscribed to updates for \* `all` or `repos`: All issues the authenticated user can see, regardless of participation or creation (default: assigned)
  --state: string@state-completer # Indicates the state of the issues to return. Can be either `open`, `closed`, or `all`. (default: open)
  --labels: string # A list of comma separated label names. Example: `bug,ui,@high`
  --qp-sort: string@sort-completer-2 # What to sort results by. Can be either `created`, `updated`, `comments`. (default: created)
  --direction: string@direction-completer # One of `asc` (ascending) or `desc` (descending). (default: desc)
  --since: string # Only show notifications updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<active_lock_reason: string, assignee: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, assignees: list<record>, author_association: string, body: string, body_html: string, body_text: string, closed_at: string, closed_by: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, comments: int, comments_url: string, created_at: string, events_url: string, html_url: string, id: int, labels: list<any>, labels_url: string, locked: bool, milestone: record<closed_at: string, closed_issues: int, created_at: string, creator: record, description: string, due_on: string, html_url: string, id: int, labels_url: string, node_id: string, number: int, open_issues: int, state: string, title: string, updated_at: string, url: string>, node_id: string, number: int, performed_via_github_app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record, pem: string, permissions: record, slug: string, updated_at: string, webhook_secret: string>, pull_request: record<diff_url: string, html_url: string, merged_at: string, patch_url: string, url: string>, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, repository_url: string, state: string, timeline_url: string, title: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "state" $state "scalar") (serialize-qp "labels" $labels "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "direction" $direction "scalar") (serialize-qp "since" $since "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/orgs/{org}/issues") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List organization members
#
# GET /orgs/{org}/members
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#list-organization-members — API method documentation
# operationId: orgs/list-members
export def "orgs-members list" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string@filter-completer-1 # Filter members returned in the list. Can be one of: \* `2fa_disabled` - Members without [two-factor authentication](https://github.com/blog/1614-two-factor-authentication) enabled. Available for organization owners. \* `all` - All members the authenticated user can see. (default: all)
  --role: string@role-completer # Filter members returned by their role. Can be one of: \* `all` - All members of the organization, regardless of role. \* `admin` - Organization owners. \* `member` - Non-owner organization members. (default: all)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "role" $role "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/orgs/{org}/members") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove an organization member
#
# DELETE /orgs/{org}/members/{username}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#remove-an-organization-member — API method documentation
# operationId: orgs/remove-member
export def "orgs-members delete" [
  org: string
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), username: (encode-path-segment $username)} | format pattern "/orgs/{org}/members/{username}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Check organization membership for a user
#
# GET /orgs/{org}/members/{username}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#check-organization-membership-for-a-user — API method documentation
# operationId: orgs/check-membership-for-user
export def "orgs-members check-membership-for-user" [
  org: string
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), username: (encode-path-segment $username)} | format pattern "/orgs/{org}/members/{username}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove organization membership for a user
#
# DELETE /orgs/{org}/memberships/{username}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#remove-organization-membership-for-a-user — API method documentation
# operationId: orgs/remove-membership-for-user
export def "orgs-memberships delete-for-user" [
  org: string
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), username: (encode-path-segment $username)} | format pattern "/orgs/{org}/memberships/{username}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get organization membership for a user
#
# GET /orgs/{org}/memberships/{username}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#get-organization-membership-for-a-user — API method documentation
# operationId: orgs/get-membership-for-user
export def "orgs-memberships get-for-user" [
  org: string
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<organization: record<avatar_url: string, description: string, events_url: string, hooks_url: string, id: int, issues_url: string, login: string, members_url: string, node_id: string, public_members_url: string, repos_url: string, url: string>, organization_url: string, permissions: record<can_create_repository: bool>, role: string, state: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), username: (encode-path-segment $username)} | format pattern "/orgs/{org}/memberships/{username}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Set organization membership for a user
#
# PUT /orgs/{org}/memberships/{username}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#set-organization-membership-for-a-user — API method documentation
# operationId: orgs/set-membership-for-user
export def "orgs-memberships update-for-user" [
  org: string
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --role: string@role-completer-1 # The role to give the user in the organization. Can be one of: \* `admin` - The user will become an owner of the organization. \* `member` - The user will become a non-owner member of the organization. (default: member)
]: any -> record<organization: record<avatar_url: string, description: string, events_url: string, hooks_url: string, id: int, issues_url: string, login: string, members_url: string, node_id: string, public_members_url: string, repos_url: string, url: string>, organization_url: string, permissions: record<can_create_repository: bool>, role: string, state: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), username: (encode-path-segment $username)} | format pattern "/orgs/{org}/memberships/{username}"))
  let req_body = {"role": $role} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List outside collaborators for an organization
#
# GET /orgs/{org}/outside_collaborators
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#list-outside-collaborators-for-an-organization — API method documentation
# operationId: orgs/list-outside-collaborators
export def "orgs-outside-collaborators list" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string@filter-completer-1 # Filter the list of outside collaborators. Can be one of: \* `2fa_disabled`: Outside collaborators without [two-factor authentication](https://github.com/blog/1614-two-factor-authentication) enabled. \* `all`: All outside collaborators. (default: all)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/orgs/{org}/outside_collaborators") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove outside collaborator from an organization
#
# DELETE /orgs/{org}/outside_collaborators/{username}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#remove-outside-collaborator-from-an-organization — API method documentation
# operationId: orgs/remove-outside-collaborator
export def "orgs-outside-collaborators delete" [
  org: string
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), username: (encode-path-segment $username)} | format pattern "/orgs/{org}/outside_collaborators/{username}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Convert an organization member to outside collaborator
#
# PUT /orgs/{org}/outside_collaborators/{username}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#convert-an-organization-member-to-outside-collaborator — API method documentation
# operationId: orgs/convert-member-to-outside-collaborator
export def "orgs-outside-collaborators update-convert-member" [
  org: string
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), username: (encode-path-segment $username)} | format pattern "/orgs/{org}/outside_collaborators/{username}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List pre-receive hooks for an organization
#
# GET /orgs/{org}/pre-receive-hooks
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#list-pre-receive-hooks-for-an-organization — API method documentation
# operationId: enterprise-admin/list-pre-receive-hooks-for-org
export def "orgs-pre-receive-hooks list-enterprise-admin" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
  --direction: string@direction-completer # One of `asc` (ascending) or `desc` (descending). (default: desc)
  --qp-sort: string@sort-completer-1 # The sort order for the response collection. (default: created)
]: nothing -> table<allow_downstream_configuration: bool, configuration_url: string, enforcement: string, id: int, name: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar") (serialize-qp "direction" $direction "scalar") (serialize-qp "sort" $qp_sort "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/orgs/{org}/pre-receive-hooks") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove pre-receive hook enforcement for an organization
#
# DELETE /orgs/{org}/pre-receive-hooks/{pre_receive_hook_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#remove-pre-receive-hook-enforcement-for-an-organization — API method documentation
# operationId: enterprise-admin/remove-pre-receive-hook-enforcement-for-org
export def "orgs-pre-receive-hooks delete-enterprise-admin-enforcement" [
  org: string
  pre_receive_hook_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<allow_downstream_configuration: bool, configuration_url: string, enforcement: string, id: int, name: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), pre_receive_hook_id: (encode-path-segment $pre_receive_hook_id)} | format pattern "/orgs/{org}/pre-receive-hooks/{pre_receive_hook_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a pre-receive hook for an organization
#
# GET /orgs/{org}/pre-receive-hooks/{pre_receive_hook_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#get-a-pre-receive-hook-for-an-organization — API method documentation
# operationId: enterprise-admin/get-pre-receive-hook-for-org
export def "orgs-pre-receive-hooks get-enterprise-admin" [
  org: string
  pre_receive_hook_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<allow_downstream_configuration: bool, configuration_url: string, enforcement: string, id: int, name: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), pre_receive_hook_id: (encode-path-segment $pre_receive_hook_id)} | format pattern "/orgs/{org}/pre-receive-hooks/{pre_receive_hook_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update pre-receive hook enforcement for an organization
#
# PATCH /orgs/{org}/pre-receive-hooks/{pre_receive_hook_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#update-pre-receive-hook-enforcement-for-an-organization — API method documentation
# operationId: enterprise-admin/update-pre-receive-hook-enforcement-for-org
export def "orgs-pre-receive-hooks update-enterprise-admin-enforcement" [
  org: string
  pre_receive_hook_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --allow-downstream-configuration: oneof<nothing, bool> # Whether repositories can override enforcement.
  --enforcement: string # The state of enforcement for the hook on this repository.
]: any -> record<allow_downstream_configuration: bool, configuration_url: string, enforcement: string, id: int, name: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), pre_receive_hook_id: (encode-path-segment $pre_receive_hook_id)} | format pattern "/orgs/{org}/pre-receive-hooks/{pre_receive_hook_id}"))
  let req_body = {"allow_downstream_configuration": $allow_downstream_configuration, "enforcement": $enforcement} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List organization projects
#
# GET /orgs/{org}/projects
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/projects#list-organization-projects — API method documentation
# operationId: projects/list-for-org
export def "orgs-projects list" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --state: string@state-completer # Indicates the state of the projects to return. Can be either `open`, `closed`, or `all`. (default: open)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<body: string, columns_url: string, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, html_url: string, id: int, name: string, node_id: string, number: int, organization_permission: string, owner_url: string, private: bool, state: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "state" $state "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/orgs/{org}/projects") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an organization project
#
# POST /orgs/{org}/projects
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/projects#create-an-organization-project — API method documentation
# operationId: projects/create-for-org
export def "orgs-projects create" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --body: string # The description of the project.
  name: string # The name of the project.
]: any -> record<body: string, columns_url: string, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, html_url: string, id: int, name: string, node_id: string, number: int, organization_permission: string, owner_url: string, private: bool, state: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/orgs/{org}/projects"))
  let req_body = {"body": $body, "name": $name} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List public organization members
#
# GET /orgs/{org}/public_members
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#list-public-organization-members — API method documentation
# operationId: orgs/list-public-members
export def "orgs-public-members list" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/orgs/{org}/public_members") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove public organization membership for the authenticated user
#
# DELETE /orgs/{org}/public_members/{username}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#remove-public-organization-membership-for-the-authenticated-user — API method documentation
# operationId: orgs/remove-public-membership-for-authenticated-user
export def "orgs-public-members delete-membership-for-authenticated-user" [
  org: string
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), username: (encode-path-segment $username)} | format pattern "/orgs/{org}/public_members/{username}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Check public organization membership for a user
#
# GET /orgs/{org}/public_members/{username}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#check-public-organization-membership-for-a-user — API method documentation
# operationId: orgs/check-public-membership-for-user
export def "orgs-public-members check-membership-for-user" [
  org: string
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), username: (encode-path-segment $username)} | format pattern "/orgs/{org}/public_members/{username}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Set public organization membership for the authenticated user
#
# PUT /orgs/{org}/public_members/{username}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#set-public-organization-membership-for-the-authenticated-user — API method documentation
# operationId: orgs/set-public-membership-for-authenticated-user
export def "orgs-public-members update-membership-for-authenticated-user" [
  org: string
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), username: (encode-path-segment $username)} | format pattern "/orgs/{org}/public_members/{username}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List organization repositories
#
# GET /orgs/{org}/repos
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-organization-repositories — API method documentation
# operationId: repos/list-for-org
export def "orgs-repos list" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --type: string@type-completer # Specifies the types of repositories you want returned. Can be one of `all`, `public`, `private`, `forks`, `sources`, `member`, `internal`. Note: For GitHub AE, can be one of `all`, `private`, `forks`, `sources`, `member`, `internal`. Default: `all`. If your organization is associated with an enterprise account using GitHub Enterprise Cloud or GitHub Enterprise Server 2.20+, `type` can also be `internal`. However, the `internal` value is not yet supported when a GitHub App calls this API with an installation access token.
  --qp-sort: string@sort-completer-3 # Can be one of `created`, `updated`, `pushed`, `full_name`. (default: created)
  --direction: string@direction-completer # Can be one of `asc` or `desc`. Default: when using `full_name`: `asc`, otherwise `desc`
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<allow_forking: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record<body: string, html_url: string, key: string, name: string, url: string>, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<key: string, name: string, node_id: string, spdx_id: string, url: string>, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "type" $type "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "direction" $direction "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/orgs/{org}/repos") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an organization repository
#
# POST /orgs/{org}/repos
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#create-an-organization-repository — API method documentation
# operationId: repos/create-in-org
export def "orgs-repos create" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --allow-merge-commit: oneof<nothing, bool> # Either `true` to allow merging pull requests with a merge commit, or `false` to prevent merging pull requests with merge commits. (default: true)
  --allow-rebase-merge: oneof<nothing, bool> # Either `true` to allow rebase-merging pull requests, or `false` to prevent rebase-merging. (default: true)
  --allow-squash-merge: oneof<nothing, bool> # Either `true` to allow squash-merging pull requests, or `false` to prevent squash-merging. (default: true)
  --auto-init: oneof<nothing, bool> # Pass `true` to create an initial commit with empty README. (default: false)
  --delete-branch-on-merge: oneof<nothing, bool> # Either `true` to allow automatically deleting head branches when pull requests are merged, or `false` to prevent automatic deletion. (default: false)
  --description: string # A short description of the repository.
  --gitignore-template: string # Desired language or platform [.gitignore template](https://github.com/github/gitignore) to apply. Use the name of the template without the extension. For example, "Haskell".
  --has-issues: oneof<nothing, bool> # Either `true` to enable issues for this repository or `false` to disable them. (default: true)
  --has-projects: oneof<nothing, bool> # Either `true` to enable projects for this repository or `false` to disable them. **Note:** If you're creating a repository in an organization that has disabled repository projects, the default is `false`, and if you pass `true`, the API returns an error. (default: true)
  --has-wiki: oneof<nothing, bool> # Either `true` to enable the wiki for this repository or `false` to disable it. (default: true)
  --homepage: string # A URL with more information about the repository.
  --is-template: oneof<nothing, bool> # Either `true` to make this repo available as a template repository or `false` to prevent it. (default: false)
  --license-template: string # Choose an [open source license template](https://choosealicense.com/) that best suits your needs, and then use the [license keyword](https://help.github.com/articles/licensing-a-repository/#searching-github-by-license-type) as the `license_template` string. For example, "mit" or "mpl-2.0".
  name: string # The name of the repository.
  --private: oneof<nothing, bool> # Whether the repository is private. (default: false)
  --team-id: int # The id of the team that will be granted access to this repository. This is only valid when creating a repository in an organization.
  --visibility: string@visibility-completer-2 # Can be `public` or `private`. If your organization is associated with an enterprise account using GitHub Enterprise Cloud or GitHub Enterprise Server 2.20+, `visibility` can also be `internal`. Note: For GitHub Enterprise Server and GitHub AE, this endpoint will only list repositories available to all users on the enterprise. For more information, see "[Creating an internal repository](https://help.github.com/en/github/creating-cloning-and-archiving-repositories/about-repository-visibility#about-internal-repositories)" in the GitHub Help documentation. The `visibility` parameter overrides the `private` parameter when you use both parameters with the `nebula-preview` preview header.
]: any -> record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues_count: int, owner: record<avatar_url: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/orgs/{org}/repos"))
  let req_body = {"allow_merge_commit": $allow_merge_commit, "allow_rebase_merge": $allow_rebase_merge, "allow_squash_merge": $allow_squash_merge, "auto_init": $auto_init, "delete_branch_on_merge": $delete_branch_on_merge, "description": $description, "gitignore_template": $gitignore_template, "has_issues": $has_issues, "has_projects": $has_projects, "has_wiki": $has_wiki, "homepage": $homepage, "is_template": $is_template, "license_template": $license_template, "name": $name, "private": $private, "team_id": $team_id, "visibility": $visibility} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List teams
#
# GET /orgs/{org}/teams
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#list-teams — API method documentation
# operationId: teams/list
export def "orgs-teams list" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<description: string, html_url: string, id: int, members_url: string, name: string, node_id: string, parent: record<description: string, html_url: string, id: int, ldap_dn: string, members_url: string, name: string, node_id: string, permission: string, privacy: string, repositories_url: string, slug: string, url: string>, permission: string, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, privacy: string, repositories_url: string, slug: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/orgs/{org}/teams") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a team
#
# POST /orgs/{org}/teams
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#create-a-team — API method documentation
# operationId: teams/create
export def "orgs-teams create" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string # The description of the team.
  --maintainers: list<string> # List GitHub IDs for organization members who will become team maintainers.
  name: string # The name of the team.
  --parent-team-id: int # The ID of a team to set as the parent team.
  --permission: string@permission-completer # **Deprecated**. The permission that new repositories will be added to the team with when none is specified. Can be one of: \* `pull` - team members can pull, but not push to or administer newly-added repositories. \* `push` - team members can pull and push, but not administer newly-added repositories. \* `admin` - team members can pull, push and administer newly-added repositories. (default: pull)
  --privacy: string@privacy-completer # The level of privacy this team should have. The options are: **For a non-nested team:** \* `secret` - only visible to organization owners and members of this team. \* `closed` - visible to all members of this organization. Default: `secret` **For a parent or child team:** \* `closed` - visible to all members of this organization. Default for child team: `closed`
  --repo-names: list<string> # The full name (e.g., "organization-name/repository-name") of repositories to add the team to.
]: any -> record<created_at: string, description: string, html_url: string, id: int, ldap_dn: string, members_count: int, members_url: string, name: string, node_id: string, organization: record<avatar_url: string, billing_email: string, blog: string, collaborators: int, company: string, created_at: string, default_repository_permission: string, description: string, disk_usage: int, email: string, events_url: string, followers: int, following: int, has_organization_projects: bool, has_repository_projects: bool, hooks_url: string, html_url: string, id: int, is_verified: bool, issues_url: string, location: string, login: string, members_allowed_repository_creation_type: string, members_can_create_internal_repositories: bool, members_can_create_pages: bool, members_can_create_private_pages: bool, members_can_create_private_repositories: bool, members_can_create_public_pages: bool, members_can_create_public_repositories: bool, members_can_create_repositories: bool, members_url: string, name: string, node_id: string, owned_private_repos: int, plan: record<filled_seats: int, name: string, private_repos: int, seats: int, space: int>, private_gists: int, public_gists: int, public_members_url: string, public_repos: int, repos_url: string, total_private_repos: int, twitter_username: string, two_factor_requirement_enabled: bool, type: string, updated_at: string, url: string>, parent: record<description: string, html_url: string, id: int, ldap_dn: string, members_url: string, name: string, node_id: string, permission: string, privacy: string, repositories_url: string, slug: string, url: string>, permission: string, privacy: string, repos_count: int, repositories_url: string, slug: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/orgs/{org}/teams"))
  let req_body = {"description": $description, "maintainers": $maintainers, "name": $name, "parent_team_id": $parent_team_id, "permission": $permission, "privacy": $privacy, "repo_names": $repo_names} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a team
#
# DELETE /orgs/{org}/teams/{team_slug}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#delete-a-team — API method documentation
# operationId: teams/delete-in-org
export def "orgs-teams delete" [
  org: string
  team_slug: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug)} | format pattern "/orgs/{org}/teams/{team_slug}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a team by name
#
# GET /orgs/{org}/teams/{team_slug}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#get-a-team-by-name — API method documentation
# operationId: teams/get-by-name
export def "orgs-teams get-by-name" [
  org: string
  team_slug: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created_at: string, description: string, html_url: string, id: int, ldap_dn: string, members_count: int, members_url: string, name: string, node_id: string, organization: record<avatar_url: string, billing_email: string, blog: string, collaborators: int, company: string, created_at: string, default_repository_permission: string, description: string, disk_usage: int, email: string, events_url: string, followers: int, following: int, has_organization_projects: bool, has_repository_projects: bool, hooks_url: string, html_url: string, id: int, is_verified: bool, issues_url: string, location: string, login: string, members_allowed_repository_creation_type: string, members_can_create_internal_repositories: bool, members_can_create_pages: bool, members_can_create_private_pages: bool, members_can_create_private_repositories: bool, members_can_create_public_pages: bool, members_can_create_public_repositories: bool, members_can_create_repositories: bool, members_url: string, name: string, node_id: string, owned_private_repos: int, plan: record<filled_seats: int, name: string, private_repos: int, seats: int, space: int>, private_gists: int, public_gists: int, public_members_url: string, public_repos: int, repos_url: string, total_private_repos: int, twitter_username: string, two_factor_requirement_enabled: bool, type: string, updated_at: string, url: string>, parent: record<description: string, html_url: string, id: int, ldap_dn: string, members_url: string, name: string, node_id: string, permission: string, privacy: string, repositories_url: string, slug: string, url: string>, permission: string, privacy: string, repos_count: int, repositories_url: string, slug: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug)} | format pattern "/orgs/{org}/teams/{team_slug}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a team
#
# PATCH /orgs/{org}/teams/{team_slug}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#update-a-team — API method documentation
# operationId: teams/update-in-org
export def "orgs-teams update" [
  org: string
  team_slug: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string # The description of the team.
  --name: string # The name of the team.
  --parent-team-id: int # The ID of a team to set as the parent team. (nullable)
  --permission: string@permission-completer # **Deprecated**. The permission that new repositories will be added to the team with when none is specified. Can be one of: \* `pull` - team members can pull, but not push to or administer newly-added repositories. \* `push` - team members can pull and push, but not administer newly-added repositories. \* `admin` - team members can pull, push and administer newly-added repositories. (default: pull)
  --privacy: string@privacy-completer # The level of privacy this team should have. Editing teams without specifying this parameter leaves `privacy` intact. When a team is nested, the `privacy` for parent teams cannot be `secret`. The options are: **For a non-nested team:** \* `secret` - only visible to organization owners and members of this team. \* `closed` - visible to all members of this organization. **For a parent or child team:** \* `closed` - visible to all members of this organization.
]: any -> record<created_at: string, description: string, html_url: string, id: int, ldap_dn: string, members_count: int, members_url: string, name: string, node_id: string, organization: record<avatar_url: string, billing_email: string, blog: string, collaborators: int, company: string, created_at: string, default_repository_permission: string, description: string, disk_usage: int, email: string, events_url: string, followers: int, following: int, has_organization_projects: bool, has_repository_projects: bool, hooks_url: string, html_url: string, id: int, is_verified: bool, issues_url: string, location: string, login: string, members_allowed_repository_creation_type: string, members_can_create_internal_repositories: bool, members_can_create_pages: bool, members_can_create_private_pages: bool, members_can_create_private_repositories: bool, members_can_create_public_pages: bool, members_can_create_public_repositories: bool, members_can_create_repositories: bool, members_url: string, name: string, node_id: string, owned_private_repos: int, plan: record<filled_seats: int, name: string, private_repos: int, seats: int, space: int>, private_gists: int, public_gists: int, public_members_url: string, public_repos: int, repos_url: string, total_private_repos: int, twitter_username: string, two_factor_requirement_enabled: bool, type: string, updated_at: string, url: string>, parent: record<description: string, html_url: string, id: int, ldap_dn: string, members_url: string, name: string, node_id: string, permission: string, privacy: string, repositories_url: string, slug: string, url: string>, permission: string, privacy: string, repos_count: int, repositories_url: string, slug: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug)} | format pattern "/orgs/{org}/teams/{team_slug}"))
  let req_body = {"description": $description, "name": $name, "parent_team_id": $parent_team_id, "permission": $permission, "privacy": $privacy} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List discussions
#
# GET /orgs/{org}/teams/{team_slug}/discussions
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#list-discussions — API method documentation
# operationId: teams/list-discussions-in-org
export def "orgs-teams-discussions list" [
  org: string
  team_slug: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --direction: string@direction-completer # One of `asc` (ascending) or `desc` (descending). (default: desc)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
  --pinned: string # Pinned discussions only filter
]: nothing -> table<author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, body: string, body_html: string, body_version: string, comments_count: int, comments_url: string, created_at: string, html_url: string, last_edited_at: string, node_id: string, number: int, pinned: bool, private: bool, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, team_url: string, title: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "direction" $direction "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar") (serialize-qp "pinned" $pinned "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug)} | format pattern "/orgs/{org}/teams/{team_slug}/discussions") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a discussion
#
# POST /orgs/{org}/teams/{team_slug}/discussions
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#create-a-discussion — API method documentation
# operationId: teams/create-discussion-in-org
export def "orgs-teams-discussions create" [
  org: string
  team_slug: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  body: string # The discussion post's body text.
  --private: oneof<nothing, bool> # Private posts are only visible to team members, organization owners, and team maintainers. Public posts are visible to all members of the organization. Set to `true` to create a private post. (default: false)
  title: string # The discussion post's title.
]: any -> record<author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, body: string, body_html: string, body_version: string, comments_count: int, comments_url: string, created_at: string, html_url: string, last_edited_at: string, node_id: string, number: int, pinned: bool, private: bool, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, team_url: string, title: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug)} | format pattern "/orgs/{org}/teams/{team_slug}/discussions"))
  let req_body = {"body": $body, "private": $private, "title": $title} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a discussion
#
# DELETE /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#delete-a-discussion — API method documentation
# operationId: teams/delete-discussion-in-org
export def "orgs-teams-discussions delete" [
  org: string
  team_slug: string
  discussion_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug), discussion_number: (encode-path-segment $discussion_number)} | format pattern "/orgs/{org}/teams/{team_slug}/discussions/{discussion_number}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a discussion
#
# GET /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#get-a-discussion — API method documentation
# operationId: teams/get-discussion-in-org
export def "orgs-teams-discussions get" [
  org: string
  team_slug: string
  discussion_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, body: string, body_html: string, body_version: string, comments_count: int, comments_url: string, created_at: string, html_url: string, last_edited_at: string, node_id: string, number: int, pinned: bool, private: bool, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, team_url: string, title: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug), discussion_number: (encode-path-segment $discussion_number)} | format pattern "/orgs/{org}/teams/{team_slug}/discussions/{discussion_number}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a discussion
#
# PATCH /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#update-a-discussion — API method documentation
# operationId: teams/update-discussion-in-org
export def "orgs-teams-discussions update" [
  org: string
  team_slug: string
  discussion_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --body: string # The discussion post's body text.
  --title: string # The discussion post's title.
]: any -> record<author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, body: string, body_html: string, body_version: string, comments_count: int, comments_url: string, created_at: string, html_url: string, last_edited_at: string, node_id: string, number: int, pinned: bool, private: bool, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, team_url: string, title: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug), discussion_number: (encode-path-segment $discussion_number)} | format pattern "/orgs/{org}/teams/{team_slug}/discussions/{discussion_number}"))
  let req_body = {"body": $body, "title": $title} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List discussion comments
#
# GET /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/comments
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#list-discussion-comments — API method documentation
# operationId: teams/list-discussion-comments-in-org
export def "orgs-teams-discussions-comments list" [
  org: string
  team_slug: string
  discussion_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --direction: string@direction-completer # One of `asc` (ascending) or `desc` (descending). (default: desc)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, body: string, body_html: string, body_version: string, created_at: string, discussion_url: string, html_url: string, last_edited_at: string, node_id: string, number: int, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "direction" $direction "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug), discussion_number: (encode-path-segment $discussion_number)} | format pattern "/orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/comments") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a discussion comment
#
# POST /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/comments
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#create-a-discussion-comment — API method documentation
# operationId: teams/create-discussion-comment-in-org
export def "orgs-teams-discussions-comments create" [
  org: string
  team_slug: string
  discussion_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  body: string # The discussion comment's body text.
]: any -> record<author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, body: string, body_html: string, body_version: string, created_at: string, discussion_url: string, html_url: string, last_edited_at: string, node_id: string, number: int, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug), discussion_number: (encode-path-segment $discussion_number)} | format pattern "/orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/comments"))
  let req_body = {"body": $body} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a discussion comment
#
# DELETE /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/comments/{comment_number}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#delete-a-discussion-comment — API method documentation
# operationId: teams/delete-discussion-comment-in-org
export def "orgs-teams-discussions-comments delete" [
  org: string
  team_slug: string
  discussion_number: int
  comment_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug), discussion_number: (encode-path-segment $discussion_number), comment_number: (encode-path-segment $comment_number)} | format pattern "/orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/comments/{comment_number}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a discussion comment
#
# GET /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/comments/{comment_number}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#get-a-discussion-comment — API method documentation
# operationId: teams/get-discussion-comment-in-org
export def "orgs-teams-discussions-comments get" [
  org: string
  team_slug: string
  discussion_number: int
  comment_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, body: string, body_html: string, body_version: string, created_at: string, discussion_url: string, html_url: string, last_edited_at: string, node_id: string, number: int, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug), discussion_number: (encode-path-segment $discussion_number), comment_number: (encode-path-segment $comment_number)} | format pattern "/orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/comments/{comment_number}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a discussion comment
#
# PATCH /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/comments/{comment_number}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#update-a-discussion-comment — API method documentation
# operationId: teams/update-discussion-comment-in-org
export def "orgs-teams-discussions-comments update" [
  org: string
  team_slug: string
  discussion_number: int
  comment_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  body: string # The discussion comment's body text.
]: any -> record<author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, body: string, body_html: string, body_version: string, created_at: string, discussion_url: string, html_url: string, last_edited_at: string, node_id: string, number: int, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug), discussion_number: (encode-path-segment $discussion_number), comment_number: (encode-path-segment $comment_number)} | format pattern "/orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/comments/{comment_number}"))
  let req_body = {"body": $body} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List reactions for a team discussion comment
#
# GET /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/comments/{comment_number}/reactions
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#list-reactions-for-a-team-discussion-comment — API method documentation
# operationId: reactions/list-for-team-discussion-comment-in-org
export def "orgs-teams-discussions-comments-reactions list-for" [
  org: string
  team_slug: string
  discussion_number: int
  comment_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --content: string@content-completer # Returns a single [reaction type](https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#reaction-types). Omit this parameter to list all reactions to a team discussion comment.
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<content: string, created_at: string, id: int, node_id: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "content" $content "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug), discussion_number: (encode-path-segment $discussion_number), comment_number: (encode-path-segment $comment_number)} | format pattern "/orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/comments/{comment_number}/reactions") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create reaction for a team discussion comment
#
# POST /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/comments/{comment_number}/reactions
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#create-reaction-for-a-team-discussion-comment — API method documentation
# operationId: reactions/create-for-team-discussion-comment-in-org
export def "orgs-teams-discussions-comments-reactions create-for" [
  org: string
  team_slug: string
  discussion_number: int
  comment_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  content: string@content-completer # The [reaction type](https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#reaction-types) to add to the team discussion comment.
]: any -> record<content: string, created_at: string, id: int, node_id: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug), discussion_number: (encode-path-segment $discussion_number), comment_number: (encode-path-segment $comment_number)} | format pattern "/orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/comments/{comment_number}/reactions"))
  let req_body = {"content": $content} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete team discussion comment reaction
#
# DELETE /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/comments/{comment_number}/reactions/{reaction_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#delete-team-discussion-comment-reaction — API method documentation
# operationId: reactions/delete-for-team-discussion-comment
export def "orgs-teams-discussions-comments-reactions delete" [
  org: string
  team_slug: string
  discussion_number: int
  comment_number: int
  reaction_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug), discussion_number: (encode-path-segment $discussion_number), comment_number: (encode-path-segment $comment_number), reaction_id: (encode-path-segment $reaction_id)} | format pattern "/orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/comments/{comment_number}/reactions/{reaction_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List reactions for a team discussion
#
# GET /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/reactions
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#list-reactions-for-a-team-discussion — API method documentation
# operationId: reactions/list-for-team-discussion-in-org
export def "orgs-teams-discussions-reactions list-for" [
  org: string
  team_slug: string
  discussion_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --content: string@content-completer # Returns a single [reaction type](https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#reaction-types). Omit this parameter to list all reactions to a team discussion.
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<content: string, created_at: string, id: int, node_id: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "content" $content "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug), discussion_number: (encode-path-segment $discussion_number)} | format pattern "/orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/reactions") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create reaction for a team discussion
#
# POST /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/reactions
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#create-reaction-for-a-team-discussion — API method documentation
# operationId: reactions/create-for-team-discussion-in-org
export def "orgs-teams-discussions-reactions create-for" [
  org: string
  team_slug: string
  discussion_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  content: string@content-completer # The [reaction type](https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#reaction-types) to add to the team discussion.
]: any -> record<content: string, created_at: string, id: int, node_id: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug), discussion_number: (encode-path-segment $discussion_number)} | format pattern "/orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/reactions"))
  let req_body = {"content": $content} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete team discussion reaction
#
# DELETE /orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/reactions/{reaction_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#delete-team-discussion-reaction — API method documentation
# operationId: reactions/delete-for-team-discussion
export def "orgs-teams-discussions-reactions delete" [
  org: string
  team_slug: string
  discussion_number: int
  reaction_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug), discussion_number: (encode-path-segment $discussion_number), reaction_id: (encode-path-segment $reaction_id)} | format pattern "/orgs/{org}/teams/{team_slug}/discussions/{discussion_number}/reactions/{reaction_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List team members
#
# GET /orgs/{org}/teams/{team_slug}/members
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#list-team-members — API method documentation
# operationId: teams/list-members-in-org
export def "orgs-teams-members list" [
  org: string
  team_slug: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --role: string@role-completer-2 # Filters members returned by their role in the team. Can be one of: \* `member` - normal members of the team. \* `maintainer` - team maintainers. \* `all` - all members of the team. (default: all)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "role" $role "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug)} | format pattern "/orgs/{org}/teams/{team_slug}/members") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove team membership for a user
#
# DELETE /orgs/{org}/teams/{team_slug}/memberships/{username}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#remove-team-membership-for-a-user — API method documentation
# operationId: teams/remove-membership-for-user-in-org
export def "orgs-teams-memberships delete-for-user" [
  org: string
  team_slug: string
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug), username: (encode-path-segment $username)} | format pattern "/orgs/{org}/teams/{team_slug}/memberships/{username}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get team membership for a user
#
# GET /orgs/{org}/teams/{team_slug}/memberships/{username}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#get-team-membership-for-a-user — API method documentation
# operationId: teams/get-membership-for-user-in-org
export def "orgs-teams-memberships get-for-user" [
  org: string
  team_slug: string
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<role: string, state: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug), username: (encode-path-segment $username)} | format pattern "/orgs/{org}/teams/{team_slug}/memberships/{username}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add or update team membership for a user
#
# PUT /orgs/{org}/teams/{team_slug}/memberships/{username}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#add-or-update-team-membership-for-a-user — API method documentation
# operationId: teams/add-or-update-membership-for-user-in-org
export def "orgs-teams-memberships create-or-update-for-user" [
  org: string
  team_slug: string
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --role: string@role-completer-3 # The role that this user should have in the team. Can be one of: \* `member` - a normal member of the team. \* `maintainer` - a team maintainer. Able to add/remove other team members, promote other team members to team maintainer, and edit the team's name and description. (default: member)
]: any -> record<role: string, state: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug), username: (encode-path-segment $username)} | format pattern "/orgs/{org}/teams/{team_slug}/memberships/{username}"))
  let req_body = {"role": $role} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List team projects
#
# GET /orgs/{org}/teams/{team_slug}/projects
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#list-team-projects — API method documentation
# operationId: teams/list-projects-in-org
export def "orgs-teams-projects list" [
  org: string
  team_slug: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<body: string, columns_url: string, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, html_url: string, id: int, name: string, node_id: string, number: int, organization_permission: string, owner_url: string, permissions: record<admin: bool, read: bool, write: bool>, private: bool, state: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug)} | format pattern "/orgs/{org}/teams/{team_slug}/projects") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove a project from a team
#
# DELETE /orgs/{org}/teams/{team_slug}/projects/{project_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#remove-a-project-from-a-team — API method documentation
# operationId: teams/remove-project-in-org
export def "orgs-teams-projects delete" [
  org: string
  team_slug: string
  project_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug), project_id: (encode-path-segment $project_id)} | format pattern "/orgs/{org}/teams/{team_slug}/projects/{project_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Check team permissions for a project
#
# GET /orgs/{org}/teams/{team_slug}/projects/{project_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#check-team-permissions-for-a-project — API method documentation
# operationId: teams/check-permissions-for-project-in-org
export def "orgs-teams-projects check-permissions-for" [
  org: string
  team_slug: string
  project_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<body: string, columns_url: string, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, html_url: string, id: int, name: string, node_id: string, number: int, organization_permission: string, owner_url: string, permissions: record<admin: bool, read: bool, write: bool>, private: bool, state: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug), project_id: (encode-path-segment $project_id)} | format pattern "/orgs/{org}/teams/{team_slug}/projects/{project_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add or update team project permissions
#
# PUT /orgs/{org}/teams/{team_slug}/projects/{project_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#add-or-update-team-project-permissions — API method documentation
# operationId: teams/add-or-update-project-permissions-in-org
export def "orgs-teams-projects create-or-update-permissions" [
  org: string
  team_slug: string
  project_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --permission: string@permission-completer-1 # The permission to grant to the team for this project. Can be one of: \* `read` - team members can read, but not write to or administer this project. \* `write` - team members can read and write, but not administer this project. \* `admin` - team members can read, write and administer this project. Default: the team's `permission` attribute will be used to determine what permission to grant the team on this project. Note that, if you choose not to pass any parameters, you'll need to set `Content-Length` to zero when calling out to this endpoint. For more information, see "[HTTP verbs](https://docs.github.com/enterprise-server@2.22/rest/overview/resources-in-the-rest-api#http-verbs)."
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug), project_id: (encode-path-segment $project_id)} | format pattern "/orgs/{org}/teams/{team_slug}/projects/{project_id}"))
  let req_body = {"permission": $permission} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List team repositories
#
# GET /orgs/{org}/teams/{team_slug}/repos
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#list-team-repositories — API method documentation
# operationId: teams/list-repos-in-org
export def "orgs-teams-repos list" [
  org: string
  team_slug: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<allow_forking: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record<body: string, html_url: string, key: string, name: string, url: string>, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<key: string, name: string, node_id: string, spdx_id: string, url: string>, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug)} | format pattern "/orgs/{org}/teams/{team_slug}/repos") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove a repository from a team
#
# DELETE /orgs/{org}/teams/{team_slug}/repos/{owner}/{repo}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams/#remove-a-repository-from-a-team — API method documentation
# operationId: teams/remove-repo-in-org
export def "orgs-teams-repos delete" [
  org: string
  team_slug: string
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug), owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/orgs/{org}/teams/{team_slug}/repos/{owner}/{repo}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Check team permissions for a repository
#
# GET /orgs/{org}/teams/{team_slug}/repos/{owner}/{repo}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams/#check-team-permissions-for-a-repository — API method documentation
# operationId: teams/check-permissions-for-repo-in-org
export def "orgs-teams-repos check-permissions-for" [
  org: string
  team_slug: string
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug), owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/orgs/{org}/teams/{team_slug}/repos/{owner}/{repo}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add or update team repository permissions
#
# PUT /orgs/{org}/teams/{team_slug}/repos/{owner}/{repo}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams/#add-or-update-team-repository-permissions — API method documentation
# operationId: teams/add-or-update-repo-permissions-in-org
export def "orgs-teams-repos create-or-update-permissions" [
  org: string
  team_slug: string
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --permission: string@permission-completer-2 # The permission to grant the team on this repository. Can be one of: \* `pull` - team members can pull, but not push to or administer this repository. \* `push` - team members can pull and push, but not administer this repository. \* `admin` - team members can pull, push and administer this repository. \* `maintain` - team members can manage the repository without access to sensitive or destructive actions. Recommended for project managers. Only applies to repositories owned by organizations. \* `triage` - team members can proactively manage issues and pull requests without write access. Recommended for contributors who triage a repository. Only applies to repositories owned by organizations. If no permission is specified, the team's `permission` attribute will be used to determine what permission to grant the team on this repository.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug), owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/orgs/{org}/teams/{team_slug}/repos/{owner}/{repo}"))
  let req_body = {"permission": $permission} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List child teams
#
# GET /orgs/{org}/teams/{team_slug}/teams
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#list-child-teams — API method documentation
# operationId: teams/list-child-in-org
export def "orgs-teams-teams list-child" [
  org: string
  team_slug: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<description: string, html_url: string, id: int, members_url: string, name: string, node_id: string, parent: record<description: string, html_url: string, id: int, ldap_dn: string, members_url: string, name: string, node_id: string, permission: string, privacy: string, repositories_url: string, slug: string, url: string>, permission: string, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, privacy: string, repositories_url: string, slug: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({org: (encode-path-segment $org), team_slug: (encode-path-segment $team_slug)} | format pattern "/orgs/{org}/teams/{team_slug}/teams") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a project card
#
# DELETE /projects/columns/cards/{card_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/projects#delete-a-project-card — API method documentation
# operationId: projects/delete-card
export def "projects-columns-cards delete" [
  card_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({card_id: (encode-path-segment $card_id)} | format pattern "/projects/columns/cards/{card_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a project card
#
# GET /projects/columns/cards/{card_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/projects#get-a-project-card — API method documentation
# operationId: projects/get-card
export def "projects-columns-cards get" [
  card_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<archived: bool, column_name: string, column_url: string, content_url: string, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, id: int, node_id: string, note: string, project_id: string, project_url: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({card_id: (encode-path-segment $card_id)} | format pattern "/projects/columns/cards/{card_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update an existing project card
#
# PATCH /projects/columns/cards/{card_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/projects#update-a-project-card — API method documentation
# operationId: projects/update-card
export def "projects-columns-cards update" [
  card_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --archived: oneof<nothing, bool> # Whether or not the card is archived (e.g. false)
  --note: string # The project card's note (nullable, e.g. Update all gems)
]: any -> record<archived: bool, column_name: string, column_url: string, content_url: string, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, id: int, node_id: string, note: string, project_id: string, project_url: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({card_id: (encode-path-segment $card_id)} | format pattern "/projects/columns/cards/{card_id}"))
  let req_body = {"archived": $archived, "note": $note} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Move a project card
#
# POST /projects/columns/cards/{card_id}/moves
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/projects#move-a-project-card — API method documentation
# operationId: projects/move-card
export def "projects-columns-cards-moves move" [
  card_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --column-id: int # The unique identifier of the column the card should be moved to (e.g. 42)
  position: string # The position of the card in a column. Can be one of: `top`, `bottom`, or `after:<card_id>` to place after the specified card. (e.g. bottom)
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({card_id: (encode-path-segment $card_id)} | format pattern "/projects/columns/cards/{card_id}/moves"))
  let req_body = {"column_id": $column_id, "position": $position} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a project column
#
# DELETE /projects/columns/{column_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/projects#delete-a-project-column — API method documentation
# operationId: projects/delete-column
export def "projects-columns delete" [
  column_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({column_id: (encode-path-segment $column_id)} | format pattern "/projects/columns/{column_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a project column
#
# GET /projects/columns/{column_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/projects#get-a-project-column — API method documentation
# operationId: projects/get-column
export def "projects-columns get" [
  column_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<cards_url: string, created_at: string, id: int, name: string, node_id: string, project_url: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({column_id: (encode-path-segment $column_id)} | format pattern "/projects/columns/{column_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update an existing project column
#
# PATCH /projects/columns/{column_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/projects#update-a-project-column — API method documentation
# operationId: projects/update-column
export def "projects-columns update" [
  column_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  name: string # Name of the project column (e.g. Remaining tasks)
]: any -> record<cards_url: string, created_at: string, id: int, name: string, node_id: string, project_url: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({column_id: (encode-path-segment $column_id)} | format pattern "/projects/columns/{column_id}"))
  let req_body = {"name": $name} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List project cards
#
# GET /projects/columns/{column_id}/cards
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/projects#list-project-cards — API method documentation
# operationId: projects/list-cards
export def "projects-columns-cards list" [
  column_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --archived-state: string@archived-state-completer # Filters the project cards that are returned by the card's state. Can be one of `all`,`archived`, or `not_archived`. (default: not_archived)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<archived: bool, column_name: string, column_url: string, content_url: string, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, id: int, node_id: string, note: string, project_id: string, project_url: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "archived_state" $archived_state "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({column_id: (encode-path-segment $column_id)} | format pattern "/projects/columns/{column_id}/cards") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a project card
#
# POST /projects/columns/{column_id}/cards
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/projects#create-a-project-card — API method documentation
# operationId: projects/create-card
export def "projects-columns-cards create" [
  column_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --note: string # The project card's note (nullable, e.g. Update all gems)
  --content-id: int # The unique identifier of the content associated with the card (e.g. 42)
  --content-type: string # The piece of content associated with the card (e.g. PullRequest)
]: any -> record<archived: bool, column_name: string, column_url: string, content_url: string, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, id: int, node_id: string, note: string, project_id: string, project_url: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({column_id: (encode-path-segment $column_id)} | format pattern "/projects/columns/{column_id}/cards"))
  let req_body = {"note": $note, "content_id": $content_id, "content_type": $content_type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Move a project column
#
# POST /projects/columns/{column_id}/moves
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/projects#move-a-project-column — API method documentation
# operationId: projects/move-column
export def "projects-columns-moves move" [
  column_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  position: string # The position of the column in a project. Can be one of: `first`, `last`, or `after:<column_id>` to place after the specified column. (e.g. last)
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({column_id: (encode-path-segment $column_id)} | format pattern "/projects/columns/{column_id}/moves"))
  let req_body = {"position": $position} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a project
#
# DELETE /projects/{project_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/projects#delete-a-project — API method documentation
# operationId: projects/delete
export def "projects delete" [
  project_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({project_id: (encode-path-segment $project_id)} | format pattern "/projects/{project_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a project
#
# GET /projects/{project_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/projects#get-a-project — API method documentation
# operationId: projects/get
export def "projects get" [
  project_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<body: string, columns_url: string, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, html_url: string, id: int, name: string, node_id: string, number: int, organization_permission: string, owner_url: string, private: bool, state: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({project_id: (encode-path-segment $project_id)} | format pattern "/projects/{project_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a project
#
# PATCH /projects/{project_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/projects#update-a-project — API method documentation
# operationId: projects/update
export def "projects update" [
  project_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --body: string # Body of the project (nullable, e.g. This project represents the sprint of the first week in January)
  --name: string # Name of the project (e.g. Week One Sprint)
  --organization-permission: string@organization-permission-completer # The baseline permission that all organization members have on this project
  --private: oneof<nothing, bool> # Whether or not this project can be seen by everyone.
  --state: string # State of the project; either 'open' or 'closed' (e.g. open)
]: any -> record<body: string, columns_url: string, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, html_url: string, id: int, name: string, node_id: string, number: int, organization_permission: string, owner_url: string, private: bool, state: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({project_id: (encode-path-segment $project_id)} | format pattern "/projects/{project_id}"))
  let req_body = {"body": $body, "name": $name, "organization_permission": $organization_permission, "private": $private, "state": $state} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List project collaborators
#
# GET /projects/{project_id}/collaborators
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/projects#list-project-collaborators — API method documentation
# operationId: projects/list-collaborators
export def "projects-collaborators list" [
  project_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --affiliation: string@affiliation-completer # Filters the collaborators by their affiliation. Can be one of: \* `outside`: Outside collaborators of a project that are not a member of the project's organization. \* `direct`: Collaborators with permissions to a project, regardless of organization membership status. \* `all`: All collaborators the authenticated user can see. (default: all)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "affiliation" $affiliation "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({project_id: (encode-path-segment $project_id)} | format pattern "/projects/{project_id}/collaborators") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove user as a collaborator
#
# DELETE /projects/{project_id}/collaborators/{username}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/projects#remove-project-collaborator — API method documentation
# operationId: projects/remove-collaborator
export def "projects-collaborators delete" [
  project_id: int
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({project_id: (encode-path-segment $project_id), username: (encode-path-segment $username)} | format pattern "/projects/{project_id}/collaborators/{username}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add project collaborator
#
# PUT /projects/{project_id}/collaborators/{username}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/projects#add-project-collaborator — API method documentation
# operationId: projects/add-collaborator
export def "projects-collaborators create" [
  project_id: int
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --permission: string@permission-completer-1 # The permission to grant the collaborator. (default: write, e.g. write)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({project_id: (encode-path-segment $project_id), username: (encode-path-segment $username)} | format pattern "/projects/{project_id}/collaborators/{username}"))
  let req_body = {"permission": $permission} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Get project permission for a user
#
# GET /projects/{project_id}/collaborators/{username}/permission
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/projects#get-project-permission-for-a-user — API method documentation
# operationId: projects/get-permission-for-user
export def "projects-collaborators-permission get-for-user" [
  project_id: int
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<permission: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({project_id: (encode-path-segment $project_id), username: (encode-path-segment $username)} | format pattern "/projects/{project_id}/collaborators/{username}/permission"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List project columns
#
# GET /projects/{project_id}/columns
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/projects#list-project-columns — API method documentation
# operationId: projects/list-columns
export def "projects-columns list" [
  project_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<cards_url: string, created_at: string, id: int, name: string, node_id: string, project_url: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({project_id: (encode-path-segment $project_id)} | format pattern "/projects/{project_id}/columns") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a project column
#
# POST /projects/{project_id}/columns
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/projects#create-a-project-column — API method documentation
# operationId: projects/create-column
export def "projects-columns create" [
  project_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  name: string # Name of the project column (e.g. Remaining tasks)
]: any -> record<cards_url: string, created_at: string, id: int, name: string, node_id: string, project_url: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({project_id: (encode-path-segment $project_id)} | format pattern "/projects/{project_id}/columns"))
  let req_body = {"name": $name} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Get rate limit status for the authenticated user
#
# GET /rate_limit
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/rate-limit#get-rate-limit-status-for-the-authenticated-user — API method documentation
# operationId: rate-limit/get
export def "rate-limit get" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<rate: record<limit: int, remaining: int, reset: int, used: int>, resources: record<actions_runner_registration: record<limit: int, remaining: int, reset: int, used: int>, code_scanning_upload: record<limit: int, remaining: int, reset: int, used: int>, core: record<limit: int, remaining: int, reset: int, used: int>, graphql: record<limit: int, remaining: int, reset: int, used: int>, integration_manifest: record<limit: int, remaining: int, reset: int, used: int>, search: record<limit: int, remaining: int, reset: int, used: int>, source_import: record<limit: int, remaining: int, reset: int, used: int>>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/rate_limit")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a reaction (Legacy)
#
# DELETE /reactions/{reaction_id}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/reactions/#delete-a-reaction-legacy — API method documentation
# operationId: reactions/delete-legacy
@deprecated
export def "reactions delete-legacy" [
  reaction_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({reaction_id: (encode-path-segment $reaction_id)} | format pattern "/reactions/{reaction_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a repository
#
# DELETE /repos/{owner}/{repo}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#delete-a-repository — API method documentation
# operationId: repos/delete
export def "repos delete" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a repository
#
# GET /repos/{owner}/{repo}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-a-repository — API method documentation
# operationId: repos/get
export def "repos get" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, anonymous_access_enabled: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record<html_url: string, key: string, name: string, url: string>, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, parent: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, source: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a repository
#
# PATCH /repos/{owner}/{repo}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos/#update-a-repository — API method documentation
# operationId: repos/update
export def "repos update" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --allow-forking: oneof<nothing, bool> # Either `true` to allow private forks, or `false` to prevent private forks. (default: false)
  --allow-merge-commit: oneof<nothing, bool> # Either `true` to allow merging pull requests with a merge commit, or `false` to prevent merging pull requests with merge commits. (default: true)
  --allow-rebase-merge: oneof<nothing, bool> # Either `true` to allow rebase-merging pull requests, or `false` to prevent rebase-merging. (default: true)
  --allow-squash-merge: oneof<nothing, bool> # Either `true` to allow squash-merging pull requests, or `false` to prevent squash-merging. (default: true)
  --archived: oneof<nothing, bool> # `true` to archive this repository. **Note**: You cannot unarchive repositories through the API. (default: false)
  --default-branch: string # Updates the default branch for this repository.
  --delete-branch-on-merge: oneof<nothing, bool> # Either `true` to allow automatically deleting head branches when pull requests are merged, or `false` to prevent automatic deletion. (default: false)
  --description: string # A short description of the repository.
  --has-issues: oneof<nothing, bool> # Either `true` to enable issues for this repository or `false` to disable them. (default: true)
  --has-projects: oneof<nothing, bool> # Either `true` to enable projects for this repository or `false` to disable them. **Note:** If you're creating a repository in an organization that has disabled repository projects, the default is `false`, and if you pass `true`, the API returns an error. (default: true)
  --has-wiki: oneof<nothing, bool> # Either `true` to enable the wiki for this repository or `false` to disable it. (default: true)
  --homepage: string # A URL with more information about the repository.
  --is-template: oneof<nothing, bool> # Either `true` to make this repo available as a template repository or `false` to prevent it. (default: false)
  --name: string # The name of the repository.
  --private: oneof<nothing, bool> # Either `true` to make the repository private or `false` to make it public. Default: `false`. **Note**: You will get a `422` error if the organization restricts [changing repository visibility](https://help.github.com/articles/repository-permission-levels-for-an-organization#changing-the-visibility-of-repositories) to organization owners and a non-owner tries to change the value of private. **Note**: You will get a `422` error if the organization restricts [changing repository visibility](https://help.github.com/articles/repository-permission-levels-for-an-organization#changing-the-visibility-of-repositories) to organization owners and a non-owner tries to change the value of private. (default: false)
  --visibility: string@visibility-completer-2 # Can be `public` or `private`. If your organization is associated with an enterprise account using GitHub Enterprise Cloud or GitHub Enterprise Server 2.20+, `visibility` can also be `internal`."
]: any -> record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, anonymous_access_enabled: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record<html_url: string, key: string, name: string, url: string>, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, parent: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, source: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}"))
  let req_body = {"allow_forking": $allow_forking, "allow_merge_commit": $allow_merge_commit, "allow_rebase_merge": $allow_rebase_merge, "allow_squash_merge": $allow_squash_merge, "archived": $archived, "default_branch": $default_branch, "delete_branch_on_merge": $delete_branch_on_merge, "description": $description, "has_issues": $has_issues, "has_projects": $has_projects, "has_wiki": $has_wiki, "homepage": $homepage, "is_template": $is_template, "name": $name, "private": $private, "visibility": $visibility} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List artifacts for a repository
#
# GET /repos/{owner}/{repo}/actions/artifacts
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#list-artifacts-for-a-repository — API method documentation
# operationId: actions/list-artifacts-for-repo
export def "repos-actions-artifacts list" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<artifacts: table<archive_download_url: string, created_at: string, expired: bool, expires_at: string, id: int, name: string, node_id: string, size_in_bytes: int, updated_at: string, url: string>, total_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/actions/artifacts") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an artifact
#
# DELETE /repos/{owner}/{repo}/actions/artifacts/{artifact_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#delete-an-artifact — API method documentation
# operationId: actions/delete-artifact
export def "repos-actions-artifacts delete" [
  owner: string
  repo: string
  artifact_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), artifact_id: (encode-path-segment $artifact_id)} | format pattern "/repos/{owner}/{repo}/actions/artifacts/{artifact_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get an artifact
#
# GET /repos/{owner}/{repo}/actions/artifacts/{artifact_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#get-an-artifact — API method documentation
# operationId: actions/get-artifact
export def "repos-actions-artifacts get" [
  owner: string
  repo: string
  artifact_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<archive_download_url: string, created_at: string, expired: bool, expires_at: string, id: int, name: string, node_id: string, size_in_bytes: int, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), artifact_id: (encode-path-segment $artifact_id)} | format pattern "/repos/{owner}/{repo}/actions/artifacts/{artifact_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download an artifact
#
# GET /repos/{owner}/{repo}/actions/artifacts/{artifact_id}/{archive_format}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#download-an-artifact — API method documentation
# operationId: actions/download-artifact
export def "repos-actions-artifacts download" [
  owner: string
  repo: string
  artifact_id: int
  archive_format: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), artifact_id: (encode-path-segment $artifact_id), archive_format: (encode-path-segment $archive_format)} | format pattern "/repos/{owner}/{repo}/actions/artifacts/{artifact_id}/{archive_format}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a job for a workflow run
#
# GET /repos/{owner}/{repo}/actions/jobs/{job_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#get-a-job-for-a-workflow-run — API method documentation
# operationId: actions/get-job-for-workflow-run
export def "repos-actions-jobs get-for-workflow-run" [
  owner: string
  repo: string
  job_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<check_run_url: string, completed_at: string, conclusion: string, head_sha: string, html_url: string, id: int, name: string, node_id: string, run_attempt: int, run_id: int, run_url: string, started_at: string, status: string, steps: table<completed_at: string, conclusion: string, name: string, number: int, started_at: string, status: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), job_id: (encode-path-segment $job_id)} | format pattern "/repos/{owner}/{repo}/actions/jobs/{job_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download job logs for a workflow run
#
# GET /repos/{owner}/{repo}/actions/jobs/{job_id}/logs
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#download-job-logs-for-a-workflow-run — API method documentation
# operationId: actions/download-job-logs-for-workflow-run
export def "repos-actions-jobs-logs download-for-workflow-run" [
  owner: string
  repo: string
  job_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), job_id: (encode-path-segment $job_id)} | format pattern "/repos/{owner}/{repo}/actions/jobs/{job_id}/logs"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List self-hosted runners for a repository
#
# GET /repos/{owner}/{repo}/actions/runners
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#list-self-hosted-runners-for-a-repository — API method documentation
# operationId: actions/list-self-hosted-runners-for-repo
export def "repos-actions-runners list-self-hosted" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<runners: table<busy: bool, id: float, name: string, os: string, status: string>, total_count: float> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/actions/runners") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List runner applications for a repository
#
# GET /repos/{owner}/{repo}/actions/runners/downloads
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#list-runner-applications-for-a-repository — API method documentation
# operationId: actions/list-runner-applications-for-repo
export def "repos-actions-runners-downloads list-applications" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> table<architecture: string, download_url: string, filename: string, os: string, sha256_checksum: string, temp_download_token: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/actions/runners/downloads"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a registration token for a repository
#
# POST /repos/{owner}/{repo}/actions/runners/registration-token
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#create-a-registration-token-for-a-repository — API method documentation
# operationId: actions/create-registration-token-for-repo
export def "repos-actions-runners-registration-token create" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<expires_at: string, permissions: record, repositories: table<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, repository_selection: string, single_file: string, token: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/actions/runners/registration-token"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a remove token for a repository
#
# POST /repos/{owner}/{repo}/actions/runners/remove-token
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#create-a-remove-token-for-a-repository — API method documentation
# operationId: actions/create-remove-token-for-repo
export def "repos-actions-runners-remove-token create" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<expires_at: string, permissions: record, repositories: table<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, repository_selection: string, single_file: string, token: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/actions/runners/remove-token"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a self-hosted runner from a repository
#
# DELETE /repos/{owner}/{repo}/actions/runners/{runner_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#delete-a-self-hosted-runner-from-a-repository — API method documentation
# operationId: actions/delete-self-hosted-runner-from-repo
export def "repos-actions-runners delete-self-hosted" [
  owner: string
  repo: string
  runner_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), runner_id: (encode-path-segment $runner_id)} | format pattern "/repos/{owner}/{repo}/actions/runners/{runner_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a self-hosted runner for a repository
#
# GET /repos/{owner}/{repo}/actions/runners/{runner_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#get-a-self-hosted-runner-for-a-repository — API method documentation
# operationId: actions/get-self-hosted-runner-for-repo
export def "repos-actions-runners get-self-hosted" [
  owner: string
  repo: string
  runner_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<busy: bool, id: float, name: string, os: string, status: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), runner_id: (encode-path-segment $runner_id)} | format pattern "/repos/{owner}/{repo}/actions/runners/{runner_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List workflow runs for a repository
#
# GET /repos/{owner}/{repo}/actions/runs
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#list-workflow-runs-for-a-repository — API method documentation
# operationId: actions/list-workflow-runs-for-repo
export def "repos-actions-runs list-workflow" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --actor: string # Returns someone's workflow runs. Use the login for the user who created the `push` associated with the check suite or workflow run.
  --branch: string # Returns workflow runs associated with a branch. Use the name of the branch of the `push`.
  --event: string # Returns workflow run triggered by the event you specify. For example, `push`, `pull_request` or `issue`. For more information, see "[Events that trigger workflows](https://help.github.com/en/actions/automating-your-workflow-with-github-actions/events-that-trigger-workflows)."
  --status: string@status-completer # Returns workflow runs with the check run `status` or `conclusion` that you specify. For example, a conclusion can be `success` or a status can be `in_progress`. Only GitHub can set a status of `waiting` or `requested`. For a list of the possible `status` and `conclusion` options, see "[Create a check run](https://docs.github.com/enterprise-server@2.22/rest/reference/checks#create-a-check-run)."
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
  --created: string # format: date-time
  --exclude-pull-requests: oneof<nothing, bool> # If `true` pull requests are omitted from the response (empty array). (default: false)
]: nothing -> record<total_count: int, workflow_runs: table<artifacts_url: string, cancel_url: string, check_suite_id: int, check_suite_node_id: string, check_suite_url: string, conclusion: string, created_at: string, event: string, head_branch: string, head_commit: record, head_repository: record, head_repository_id: int, head_sha: string, html_url: string, id: int, jobs_url: string, logs_url: string, name: string, node_id: string, previous_attempt_url: string, pull_requests: list, repository: record, rerun_url: string, run_attempt: int, run_number: int, run_started_at: string, status: string, updated_at: string, url: string, workflow_id: int, workflow_url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "actor" $actor "scalar") (serialize-qp "branch" $branch "scalar") (serialize-qp "event" $event "scalar") (serialize-qp "status" $status "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "exclude_pull_requests" $exclude_pull_requests "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/actions/runs") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a workflow run
#
# DELETE /repos/{owner}/{repo}/actions/runs/{run_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#delete-a-workflow-run — API method documentation
# operationId: actions/delete-workflow-run
export def "repos-actions-runs delete-workflow" [
  owner: string
  repo: string
  run_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), run_id: (encode-path-segment $run_id)} | format pattern "/repos/{owner}/{repo}/actions/runs/{run_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a workflow run
#
# GET /repos/{owner}/{repo}/actions/runs/{run_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#get-a-workflow-run — API method documentation
# operationId: actions/get-workflow-run
export def "repos-actions-runs get-workflow" [
  owner: string
  repo: string
  run_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --exclude-pull-requests: oneof<nothing, bool> # If `true` pull requests are omitted from the response (empty array). (default: false)
]: nothing -> record<artifacts_url: string, cancel_url: string, check_suite_id: int, check_suite_node_id: string, check_suite_url: string, conclusion: string, created_at: string, event: string, head_branch: string, head_commit: record<author: record<email: string, name: string>, committer: record<email: string, name: string>, id: string, message: string, timestamp: string, tree_id: string>, head_repository: record<allow_forking: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record<body: string, html_url: string, key: string, name: string, url: string>, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<key: string, name: string, node_id: string, spdx_id: string, url: string>, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, head_repository_id: int, head_sha: string, html_url: string, id: int, jobs_url: string, logs_url: string, name: string, node_id: string, previous_attempt_url: string, pull_requests: table<base: record, head: record, id: int, number: int, url: string>, repository: record<allow_forking: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record<body: string, html_url: string, key: string, name: string, url: string>, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<key: string, name: string, node_id: string, spdx_id: string, url: string>, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, rerun_url: string, run_attempt: int, run_number: int, run_started_at: string, status: string, updated_at: string, url: string, workflow_id: int, workflow_url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "exclude_pull_requests" $exclude_pull_requests "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), run_id: (encode-path-segment $run_id)} | format pattern "/repos/{owner}/{repo}/actions/runs/{run_id}") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List workflow run artifacts
#
# GET /repos/{owner}/{repo}/actions/runs/{run_id}/artifacts
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#list-workflow-run-artifacts — API method documentation
# operationId: actions/list-workflow-run-artifacts
export def "repos-actions-runs-artifacts list-workflow" [
  owner: string
  repo: string
  run_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<artifacts: table<archive_download_url: string, created_at: string, expired: bool, expires_at: string, id: int, name: string, node_id: string, size_in_bytes: int, updated_at: string, url: string>, total_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), run_id: (encode-path-segment $run_id)} | format pattern "/repos/{owner}/{repo}/actions/runs/{run_id}/artifacts") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Cancel a workflow run
#
# POST /repos/{owner}/{repo}/actions/runs/{run_id}/cancel
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#cancel-a-workflow-run — API method documentation
# operationId: actions/cancel-workflow-run
export def "repos-actions-runs-cancel cancel-workflow" [
  owner: string
  repo: string
  run_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), run_id: (encode-path-segment $run_id)} | format pattern "/repos/{owner}/{repo}/actions/runs/{run_id}/cancel"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List jobs for a workflow run
#
# GET /repos/{owner}/{repo}/actions/runs/{run_id}/jobs
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#list-jobs-for-a-workflow-run — API method documentation
# operationId: actions/list-jobs-for-workflow-run
export def "repos-actions-runs-jobs list-for-workflow" [
  owner: string
  repo: string
  run_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string@filter-completer-2 # Filters jobs by their `completed_at` timestamp. Can be one of: \* `latest`: Returns jobs from the most recent execution of the workflow run. \* `all`: Returns all jobs for a workflow run, including from old executions of the workflow run. (default: latest)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<jobs: table<check_run_url: string, completed_at: string, conclusion: string, head_sha: string, html_url: string, id: int, name: string, node_id: string, run_attempt: int, run_id: int, run_url: string, started_at: string, status: string, steps: list, url: string>, total_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), run_id: (encode-path-segment $run_id)} | format pattern "/repos/{owner}/{repo}/actions/runs/{run_id}/jobs") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete workflow run logs
#
# DELETE /repos/{owner}/{repo}/actions/runs/{run_id}/logs
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#delete-workflow-run-logs — API method documentation
# operationId: actions/delete-workflow-run-logs
export def "repos-actions-runs-logs delete-workflow" [
  owner: string
  repo: string
  run_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), run_id: (encode-path-segment $run_id)} | format pattern "/repos/{owner}/{repo}/actions/runs/{run_id}/logs"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download workflow run logs
#
# GET /repos/{owner}/{repo}/actions/runs/{run_id}/logs
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#download-workflow-run-logs — API method documentation
# operationId: actions/download-workflow-run-logs
export def "repos-actions-runs-logs download-workflow" [
  owner: string
  repo: string
  run_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), run_id: (encode-path-segment $run_id)} | format pattern "/repos/{owner}/{repo}/actions/runs/{run_id}/logs"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Re-run a workflow
#
# POST /repos/{owner}/{repo}/actions/runs/{run_id}/rerun
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#re-run-a-workflow — API method documentation
# operationId: actions/re-run-workflow
@deprecated
export def "repos-actions-runs-rerun create-re-workflow" [
  owner: string
  repo: string
  run_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), run_id: (encode-path-segment $run_id)} | format pattern "/repos/{owner}/{repo}/actions/runs/{run_id}/rerun"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List repository secrets
#
# GET /repos/{owner}/{repo}/actions/secrets
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#list-repository-secrets — API method documentation
# operationId: actions/list-repo-secrets
export def "repos-actions-secrets list" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<secrets: table<created_at: string, name: string, updated_at: string>, total_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/actions/secrets") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a repository public key
#
# GET /repos/{owner}/{repo}/actions/secrets/public-key
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#get-a-repository-public-key — API method documentation
# operationId: actions/get-repo-public-key
export def "repos-actions-secrets-public-key get" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created_at: string, id: int, key: string, key_id: string, title: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/actions/secrets/public-key"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a repository secret
#
# DELETE /repos/{owner}/{repo}/actions/secrets/{secret_name}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#delete-a-repository-secret — API method documentation
# operationId: actions/delete-repo-secret
export def "repos-actions-secrets delete" [
  owner: string
  repo: string
  secret_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), secret_name: (encode-path-segment $secret_name)} | format pattern "/repos/{owner}/{repo}/actions/secrets/{secret_name}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a repository secret
#
# GET /repos/{owner}/{repo}/actions/secrets/{secret_name}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#get-a-repository-secret — API method documentation
# operationId: actions/get-repo-secret
export def "repos-actions-secrets get" [
  owner: string
  repo: string
  secret_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created_at: string, name: string, updated_at: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), secret_name: (encode-path-segment $secret_name)} | format pattern "/repos/{owner}/{repo}/actions/secrets/{secret_name}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create or update a repository secret
#
# PUT /repos/{owner}/{repo}/actions/secrets/{secret_name}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#create-or-update-a-repository-secret — API method documentation
# operationId: actions/create-or-update-repo-secret
export def "repos-actions-secrets create-or-update" [
  owner: string
  repo: string
  secret_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --encrypted-value: string # Value for your secret, encrypted with [LibSodium](https://libsodium.gitbook.io/doc/bindings_for_other_languages) using the public key retrieved from the [Get a repository public key](https://docs.github.com/enterprise-server@2.22/rest/reference/actions#get-a-repository-public-key) endpoint.
  --key-id: string # ID of the key you used to encrypt the secret.
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), secret_name: (encode-path-segment $secret_name)} | format pattern "/repos/{owner}/{repo}/actions/secrets/{secret_name}"))
  let req_body = {"encrypted_value": $encrypted_value, "key_id": $key_id} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List repository workflows
#
# GET /repos/{owner}/{repo}/actions/workflows
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#list-repository-workflows — API method documentation
# operationId: actions/list-repo-workflows
export def "repos-actions-workflows list" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<total_count: int, workflows: table<badge_url: string, created_at: string, deleted_at: string, html_url: string, id: int, name: string, node_id: string, path: string, state: string, updated_at: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/actions/workflows") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a workflow
#
# GET /repos/{owner}/{repo}/actions/workflows/{workflow_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#get-a-workflow — API method documentation
# operationId: actions/get-workflow
export def "repos-actions-workflows get" [
  owner: string
  repo: string
  workflow_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<badge_url: string, created_at: string, deleted_at: string, html_url: string, id: int, name: string, node_id: string, path: string, state: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), workflow_id: (encode-path-segment $workflow_id)} | format pattern "/repos/{owner}/{repo}/actions/workflows/{workflow_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a workflow dispatch event
#
# POST /repos/{owner}/{repo}/actions/workflows/{workflow_id}/dispatches
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#create-a-workflow-dispatch-event — API method documentation
# operationId: actions/create-workflow-dispatch
export def "repos-actions-workflows-dispatches create-dispatch" [
  owner: string
  repo: string
  workflow_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --inputs: record # Input keys and values configured in the workflow file. The maximum number of properties is 10. Any default properties configured in the workflow file will be used when `inputs` are omitted.
  ref: string # The git reference for the workflow. The reference can be a branch or tag name.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), workflow_id: (encode-path-segment $workflow_id)} | format pattern "/repos/{owner}/{repo}/actions/workflows/{workflow_id}/dispatches"))
  let req_body = {"inputs": $inputs, "ref": $ref} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List workflow runs
#
# GET /repos/{owner}/{repo}/actions/workflows/{workflow_id}/runs
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/actions#list-workflow-runs — API method documentation
# operationId: actions/list-workflow-runs
export def "repos-actions-workflows-runs list" [
  owner: string
  repo: string
  workflow_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --actor: string # Returns someone's workflow runs. Use the login for the user who created the `push` associated with the check suite or workflow run.
  --branch: string # Returns workflow runs associated with a branch. Use the name of the branch of the `push`.
  --event: string # Returns workflow run triggered by the event you specify. For example, `push`, `pull_request` or `issue`. For more information, see "[Events that trigger workflows](https://help.github.com/en/actions/automating-your-workflow-with-github-actions/events-that-trigger-workflows)."
  --status: string@status-completer # Returns workflow runs with the check run `status` or `conclusion` that you specify. For example, a conclusion can be `success` or a status can be `in_progress`. Only GitHub can set a status of `waiting` or `requested`. For a list of the possible `status` and `conclusion` options, see "[Create a check run](https://docs.github.com/enterprise-server@2.22/rest/reference/checks#create-a-check-run)."
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
  --created: string # format: date-time
  --exclude-pull-requests: oneof<nothing, bool> # If `true` pull requests are omitted from the response (empty array). (default: false)
]: nothing -> record<total_count: int, workflow_runs: table<artifacts_url: string, cancel_url: string, check_suite_id: int, check_suite_node_id: string, check_suite_url: string, conclusion: string, created_at: string, event: string, head_branch: string, head_commit: record, head_repository: record, head_repository_id: int, head_sha: string, html_url: string, id: int, jobs_url: string, logs_url: string, name: string, node_id: string, previous_attempt_url: string, pull_requests: list, repository: record, rerun_url: string, run_attempt: int, run_number: int, run_started_at: string, status: string, updated_at: string, url: string, workflow_id: int, workflow_url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "actor" $actor "scalar") (serialize-qp "branch" $branch "scalar") (serialize-qp "event" $event "scalar") (serialize-qp "status" $status "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "exclude_pull_requests" $exclude_pull_requests "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), workflow_id: (encode-path-segment $workflow_id)} | format pattern "/repos/{owner}/{repo}/actions/workflows/{workflow_id}/runs") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List assignees
#
# GET /repos/{owner}/{repo}/assignees
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#list-assignees — API method documentation
# operationId: issues/list-assignees
export def "repos-assignees list-issues" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/assignees") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Check if a user can be assigned
#
# GET /repos/{owner}/{repo}/assignees/{assignee}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#check-if-a-user-can-be-assigned — API method documentation
# operationId: issues/check-user-can-be-assigned
export def "repos-assignees check-issues-user-can-be-assigned" [
  owner: string
  repo: string
  assignee: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), assignee: (encode-path-segment $assignee)} | format pattern "/repos/{owner}/{repo}/assignees/{assignee}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List branches
#
# GET /repos/{owner}/{repo}/branches
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-branches — API method documentation
# operationId: repos/list-branches
export def "repos-branches list" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --protected: oneof<nothing, bool> # Setting to `true` returns only protected branches. When set to `false`, only unprotected branches are returned. Omitting this parameter returns all branches.
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<commit: record<sha: string, url: string>, name: string, protected: bool, protection: record<allow_deletions: record, allow_force_pushes: record, enabled: bool, enforce_admins: record, name: string, protection_url: string, required_conversation_resolution: record, required_linear_history: record, required_pull_request_reviews: record, required_signatures: record, required_status_checks: record, restrictions: record, url: string>, protection_url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "protected" $protected "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/branches") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a branch
#
# GET /repos/{owner}/{repo}/branches/{branch}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-a-branch — API method documentation
# operationId: repos/get-branch
export def "repos-branches get" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_links: record<html: string, self: string>, commit: record<author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, comments_url: string, commit: record<author: record, comment_count: int, committer: record, message: string, tree: record, url: string, verification: record>, committer: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, files: list<record>, html_url: string, node_id: string, parents: list<record>, sha: string, stats: record<additions: int, deletions: int, total: int>, url: string>, name: string, pattern: string, protected: bool, protection: record<allow_deletions: record<enabled: bool>, allow_force_pushes: record<enabled: bool>, enabled: bool, enforce_admins: record<enabled: bool, url: string>, name: string, protection_url: string, required_conversation_resolution: record<enabled: bool>, required_linear_history: record<enabled: bool>, required_pull_request_reviews: record<dismiss_stale_reviews: bool, dismissal_restrictions: record, require_code_owner_reviews: bool, required_approving_review_count: int, url: string>, required_signatures: record<enabled: bool, url: string>, required_status_checks: record<contexts: list, contexts_url: string, enforcement_level: string, strict: bool, url: string>, restrictions: record<apps: list, apps_url: string, teams: list, teams_url: string, url: string, users: list, users_url: string>, url: string>, protection_url: string, required_approving_review_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete branch protection
#
# DELETE /repos/{owner}/{repo}/branches/{branch}/protection
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#delete-branch-protection — API method documentation
# operationId: repos/delete-branch-protection
export def "repos-branches-protection delete" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get branch protection
#
# GET /repos/{owner}/{repo}/branches/{branch}/protection
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-branch-protection — API method documentation
# operationId: repos/get-branch-protection
export def "repos-branches-protection get" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<allow_deletions: record<enabled: bool>, allow_force_pushes: record<enabled: bool>, enabled: bool, enforce_admins: record<enabled: bool, url: string>, name: string, protection_url: string, required_conversation_resolution: record<enabled: bool>, required_linear_history: record<enabled: bool>, required_pull_request_reviews: record<dismiss_stale_reviews: bool, dismissal_restrictions: record<teams: list, teams_url: string, url: string, users: list, users_url: string>, require_code_owner_reviews: bool, required_approving_review_count: int, url: string>, required_signatures: record<enabled: bool, url: string>, required_status_checks: record<contexts: list<string>, contexts_url: string, enforcement_level: string, strict: bool, url: string>, restrictions: record<apps: list<record>, apps_url: string, teams: list<record>, teams_url: string, url: string, users: list<record>, users_url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update branch protection
#
# PUT /repos/{owner}/{repo}/branches/{branch}/protection
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#update-branch-protection — API method documentation
# operationId: repos/update-branch-protection
# --required_pull_request_reviews shape: {dismiss_stale_reviews?: bool, dismissal_restrictions?: record, require_code_owner_reviews?: bool, required_approving_review_count?: int}
# --required_status_checks shape: {contexts: list<string>, strict: bool}
# --restrictions shape: {apps?: list<string>, teams: list<string>, users: list<string>}
export def "repos-branches-protection update" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --allow-deletions: oneof<nothing, bool> # Allows deletion of the protected branch by anyone with write access to the repository. Set to `false` to prevent deletion of the protected branch. Default: `false`. For more information, see "[Enabling force pushes to a protected branch](https://help.github.com/en/github/administering-a-repository/enabling-force-pushes-to-a-protected-branch)" in the GitHub Help documentation.
  --allow-force-pushes: oneof<nothing, bool> # Permits force pushes to the protected branch by anyone with write access to the repository. Set to `true` to allow force pushes. Set to `false` or `null` to block force pushes. Default: `false`. For more information, see "[Enabling force pushes to a protected branch](https://help.github.com/en/github/administering-a-repository/enabling-force-pushes-to-a-protected-branch)" in the GitHub Help documentation." (nullable)
  --enforce-admins: oneof<nothing, bool> # Enforce all configured restrictions for administrators. Set to `true` to enforce required status checks for repository administrators. Set to `null` to disable. (nullable)
  --required-conversation-resolution: oneof<nothing, bool> # Requires all conversations on code to be resolved before a pull request can be merged into a branch that matches this rule. Set to `false` to disable. Default: `false`.
  --required-linear-history: oneof<nothing, bool> # Enforces a linear commit Git history, which prevents anyone from pushing merge commits to a branch. Set to `true` to enforce a linear commit history. Set to `false` to disable a linear commit Git history. Your repository must allow squash merging or rebase merging before you can enable a linear commit history. Default: `false`. For more information, see "[Requiring a linear commit history](https://help.github.com/github/administering-a-repository/requiring-a-linear-commit-history)" in the GitHub Help documentation.
  --required-pull-request-reviews: record # Require at least one approving review on a pull request, before merging. Set to `null` to disable. (nullable) — shape: {dismiss_stale_reviews?: bool, dismissal_restrictions?: record, require_code_owner_reviews?: bool, required_approving_review_count?: int}
  --required-status-checks: record # Require status checks to pass before merging. Set to `null` to disable. (nullable) — shape: {contexts: list<string>, strict: bool}
  --restrictions: record # Restrict who can push to the protected branch. User, app, and team `restrictions` are only available for organization-owned repositories. Set to `null` to disable. (nullable) — shape: {apps?: list<string>, teams: list<string>, users: list<string>}
]: any -> record<allow_deletions: record<enabled: bool>, allow_force_pushes: record<enabled: bool>, enforce_admins: record<enabled: bool, url: string>, required_conversation_resolution: record<enabled: bool>, required_linear_history: record<enabled: bool>, required_pull_request_reviews: record<dismiss_stale_reviews: bool, dismissal_restrictions: record<teams: list, teams_url: string, url: string, users: list, users_url: string>, require_code_owner_reviews: bool, required_approving_review_count: int, url: string>, required_signatures: record<enabled: bool, url: string>, required_status_checks: record<contexts: list<string>, contexts_url: string, strict: bool, url: string>, restrictions: record<apps: list<record>, apps_url: string, teams: list<record>, teams_url: string, url: string, users: list<record>, users_url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection"))
  let req_body = {"allow_deletions": $allow_deletions, "allow_force_pushes": $allow_force_pushes, "enforce_admins": $enforce_admins, "required_conversation_resolution": $required_conversation_resolution, "required_linear_history": $required_linear_history, "required_pull_request_reviews": $required_pull_request_reviews, "required_status_checks": $required_status_checks, "restrictions": $restrictions} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete admin branch protection
#
# DELETE /repos/{owner}/{repo}/branches/{branch}/protection/enforce_admins
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#delete-admin-branch-protection — API method documentation
# operationId: repos/delete-admin-branch-protection
export def "repos-branches-protection-enforce-admins delete" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/enforce_admins"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get admin branch protection
#
# GET /repos/{owner}/{repo}/branches/{branch}/protection/enforce_admins
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-admin-branch-protection — API method documentation
# operationId: repos/get-admin-branch-protection
export def "repos-branches-protection-enforce-admins get" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<enabled: bool, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/enforce_admins"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Set admin branch protection
#
# POST /repos/{owner}/{repo}/branches/{branch}/protection/enforce_admins
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#set-admin-branch-protection — API method documentation
# operationId: repos/set-admin-branch-protection
export def "repos-branches-protection-enforce-admins update" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<enabled: bool, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/enforce_admins"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete pull request review protection
#
# DELETE /repos/{owner}/{repo}/branches/{branch}/protection/required_pull_request_reviews
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#delete-pull-request-review-protection — API method documentation
# operationId: repos/delete-pull-request-review-protection
export def "repos-branches-protection-required-pull-request-reviews delete" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/required_pull_request_reviews"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get pull request review protection
#
# GET /repos/{owner}/{repo}/branches/{branch}/protection/required_pull_request_reviews
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-pull-request-review-protection — API method documentation
# operationId: repos/get-pull-request-review-protection
export def "repos-branches-protection-required-pull-request-reviews get" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<dismiss_stale_reviews: bool, dismissal_restrictions: record<teams: list<record>, teams_url: string, url: string, users: list<record>, users_url: string>, require_code_owner_reviews: bool, required_approving_review_count: int, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/required_pull_request_reviews"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update pull request review protection
#
# PATCH /repos/{owner}/{repo}/branches/{branch}/protection/required_pull_request_reviews
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#update-pull-request-review-protection — API method documentation
# operationId: repos/update-pull-request-review-protection
# --dismissal_restrictions shape: {teams?: list<string>, users?: list<string>}
export def "repos-branches-protection-required-pull-request-reviews update" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --dismiss-stale-reviews: oneof<nothing, bool> # Set to `true` if you want to automatically dismiss approving reviews when someone pushes a new commit.
  --dismissal-restrictions: record # Specify which users and teams can dismiss pull request reviews. Pass an empty `dismissal_restrictions` object to disable. User and team `dismissal_restrictions` are only available for organization-owned repositories. Omit this parameter for personal repositories. — shape: {teams?: list<string>, users?: list<string>}
  --require-code-owner-reviews: oneof<nothing, bool> # Blocks merging pull requests until [code owners](https://help.github.com/articles/about-code-owners/) have reviewed.
  --required-approving-review-count: int # Specifies the number of reviewers required to approve pull requests. Use a number between 1 and 6.
]: any -> record<dismiss_stale_reviews: bool, dismissal_restrictions: record<teams: list<record>, teams_url: string, url: string, users: list<record>, users_url: string>, require_code_owner_reviews: bool, required_approving_review_count: int, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/required_pull_request_reviews"))
  let req_body = {"dismiss_stale_reviews": $dismiss_stale_reviews, "dismissal_restrictions": $dismissal_restrictions, "require_code_owner_reviews": $require_code_owner_reviews, "required_approving_review_count": $required_approving_review_count} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete commit signature protection
#
# DELETE /repos/{owner}/{repo}/branches/{branch}/protection/required_signatures
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#delete-commit-signature-protection — API method documentation
# operationId: repos/delete-commit-signature-protection
export def "repos-branches-protection-required-signatures delete-commit" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/required_signatures"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get commit signature protection
#
# GET /repos/{owner}/{repo}/branches/{branch}/protection/required_signatures
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-commit-signature-protection — API method documentation
# operationId: repos/get-commit-signature-protection
export def "repos-branches-protection-required-signatures get-commit" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<enabled: bool, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/required_signatures"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create commit signature protection
#
# POST /repos/{owner}/{repo}/branches/{branch}/protection/required_signatures
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#create-commit-signature-protection — API method documentation
# operationId: repos/create-commit-signature-protection
export def "repos-branches-protection-required-signatures create-commit" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<enabled: bool, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/required_signatures"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove status check protection
#
# DELETE /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#remove-status-check-protection — API method documentation
# operationId: repos/remove-status-check-protection
export def "repos-branches-protection-required-status-checks delete" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get status checks protection
#
# GET /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-status-checks-protection — API method documentation
# operationId: repos/get-status-checks-protection
export def "repos-branches-protection-required-status-checks get" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<contexts: list<string>, contexts_url: string, strict: bool, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update status check protection
#
# PATCH /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#update-status-check-potection — API method documentation
# operationId: repos/update-status-check-protection
export def "repos-branches-protection-required-status-checks update" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --contexts: list<string> # The list of status checks to require in order to merge into this branch
  --strict: oneof<nothing, bool> # Require branches to be up to date before merging.
]: any -> record<contexts: list<string>, contexts_url: string, strict: bool, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks"))
  let req_body = {"contexts": $contexts, "strict": $strict} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Remove status check contexts
#
# DELETE /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#remove-status-check-contexts — API method documentation
# operationId: repos/remove-status-check-contexts
export def "repos-branches-protection-required-status-checks-contexts delete" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --contexts: list<string> # contexts parameter
]: any -> list<string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts"))
  let req_body = {"contexts": $contexts} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Get all status check contexts
#
# GET /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-all-status-check-contexts — API method documentation
# operationId: repos/get-all-status-check-contexts
export def "repos-branches-protection-required-status-checks-contexts get-list" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> list<string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add status check contexts
#
# POST /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#add-status-check-contexts — API method documentation
# operationId: repos/add-status-check-contexts
export def "repos-branches-protection-required-status-checks-contexts create" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --contexts: list<string> # contexts parameter
]: any -> list<string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts"))
  let req_body = {"contexts": $contexts} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Set status check contexts
#
# PUT /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#set-status-check-contexts — API method documentation
# operationId: repos/set-status-check-contexts
export def "repos-branches-protection-required-status-checks-contexts update" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --contexts: list<string> # contexts parameter
]: any -> list<string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts"))
  let req_body = {"contexts": $contexts} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete access restrictions
#
# DELETE /repos/{owner}/{repo}/branches/{branch}/protection/restrictions
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#delete-access-restrictions — API method documentation
# operationId: repos/delete-access-restrictions
export def "repos-branches-protection-restrictions delete-access" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get access restrictions
#
# GET /repos/{owner}/{repo}/branches/{branch}/protection/restrictions
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-access-restrictions — API method documentation
# operationId: repos/get-access-restrictions
export def "repos-branches-protection-restrictions get-access" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<apps: table<created_at: string, description: string, events: list, external_url: string, html_url: string, id: int, name: string, node_id: string, owner: record, permissions: record, slug: string, updated_at: string>, apps_url: string, teams: table<description: string, html_url: string, id: int, members_url: string, name: string, node_id: string, parent: string, permission: string, privacy: string, repositories_url: string, slug: string, url: string>, teams_url: string, url: string, users: table<avatar_url: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_url: string, subscriptions_url: string, type: string, url: string>, users_url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove app access restrictions
#
# DELETE /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#remove-app-access-restrictions — API method documentation
# operationId: repos/remove-app-access-restrictions
export def "repos-branches-protection-restrictions-apps delete-access" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --apps: list<string> # apps parameter
]: any -> table<client_id: string, client_secret: string, created_at: string, description: string, events: list<string>, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, pem: string, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string>, slug: string, updated_at: string, webhook_secret: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps"))
  let req_body = {"apps": $apps} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Get apps with access to the protected branch
#
# GET /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-apps-with-access-to-the-protected-branch — API method documentation
# operationId: repos/get-apps-with-access-to-protected-branch
export def "repos-branches-protection-restrictions-apps get-with-access-to-protected" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> table<client_id: string, client_secret: string, created_at: string, description: string, events: list<string>, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, pem: string, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string>, slug: string, updated_at: string, webhook_secret: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add app access restrictions
#
# POST /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#add-app-access-restrictions — API method documentation
# operationId: repos/add-app-access-restrictions
export def "repos-branches-protection-restrictions-apps create-access" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --apps: list<string> # apps parameter
]: any -> table<client_id: string, client_secret: string, created_at: string, description: string, events: list<string>, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, pem: string, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string>, slug: string, updated_at: string, webhook_secret: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps"))
  let req_body = {"apps": $apps} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Set app access restrictions
#
# PUT /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#set-app-access-restrictions — API method documentation
# operationId: repos/set-app-access-restrictions
export def "repos-branches-protection-restrictions-apps update-access" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --apps: list<string> # apps parameter
]: any -> table<client_id: string, client_secret: string, created_at: string, description: string, events: list<string>, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, pem: string, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string>, slug: string, updated_at: string, webhook_secret: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions/apps"))
  let req_body = {"apps": $apps} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Remove team access restrictions
#
# DELETE /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#remove-team-access-restrictions — API method documentation
# operationId: repos/remove-team-access-restrictions
export def "repos-branches-protection-restrictions-teams delete-access" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --teams: list<string> # teams parameter
]: any -> table<description: string, html_url: string, id: int, members_url: string, name: string, node_id: string, parent: record<description: string, html_url: string, id: int, ldap_dn: string, members_url: string, name: string, node_id: string, permission: string, privacy: string, repositories_url: string, slug: string, url: string>, permission: string, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, privacy: string, repositories_url: string, slug: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams"))
  let req_body = {"teams": $teams} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Get teams with access to the protected branch
#
# GET /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-teams-with-access-to-the-protected-branch — API method documentation
# operationId: repos/get-teams-with-access-to-protected-branch
export def "repos-branches-protection-restrictions-teams get-with-access-to-protected" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> table<description: string, html_url: string, id: int, members_url: string, name: string, node_id: string, parent: record<description: string, html_url: string, id: int, ldap_dn: string, members_url: string, name: string, node_id: string, permission: string, privacy: string, repositories_url: string, slug: string, url: string>, permission: string, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, privacy: string, repositories_url: string, slug: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add team access restrictions
#
# POST /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#add-team-access-restrictions — API method documentation
# operationId: repos/add-team-access-restrictions
export def "repos-branches-protection-restrictions-teams create-access" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --teams: list<string> # teams parameter
]: any -> table<description: string, html_url: string, id: int, members_url: string, name: string, node_id: string, parent: record<description: string, html_url: string, id: int, ldap_dn: string, members_url: string, name: string, node_id: string, permission: string, privacy: string, repositories_url: string, slug: string, url: string>, permission: string, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, privacy: string, repositories_url: string, slug: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams"))
  let req_body = {"teams": $teams} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Set team access restrictions
#
# PUT /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#set-team-access-restrictions — API method documentation
# operationId: repos/set-team-access-restrictions
export def "repos-branches-protection-restrictions-teams update-access" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --teams: list<string> # teams parameter
]: any -> table<description: string, html_url: string, id: int, members_url: string, name: string, node_id: string, parent: record<description: string, html_url: string, id: int, ldap_dn: string, members_url: string, name: string, node_id: string, permission: string, privacy: string, repositories_url: string, slug: string, url: string>, permission: string, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, privacy: string, repositories_url: string, slug: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions/teams"))
  let req_body = {"teams": $teams} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Remove user access restrictions
#
# DELETE /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#remove-user-access-restrictions — API method documentation
# operationId: repos/remove-user-access-restrictions
export def "repos-branches-protection-restrictions-users delete-access" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --users: list<string> # users parameter
]: any -> table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users"))
  let req_body = {"users": $users} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Get users with access to the protected branch
#
# GET /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-users-with-access-to-the-protected-branch — API method documentation
# operationId: repos/get-users-with-access-to-protected-branch
export def "repos-branches-protection-restrictions-users get-with-access-to-protected" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add user access restrictions
#
# POST /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#add-user-access-restrictions — API method documentation
# operationId: repos/add-user-access-restrictions
export def "repos-branches-protection-restrictions-users create-access" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --users: list<string> # users parameter
]: any -> table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users"))
  let req_body = {"users": $users} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Set user access restrictions
#
# PUT /repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#set-user-access-restrictions — API method documentation
# operationId: repos/set-user-access-restrictions
export def "repos-branches-protection-restrictions-users update-access" [
  owner: string
  repo: string
  branch: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --users: list<string> # users parameter
]: any -> table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), branch: (encode-path-segment $branch)} | format pattern "/repos/{owner}/{repo}/branches/{branch}/protection/restrictions/users"))
  let req_body = {"users": $users} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Create a check run
#
# POST /repos/{owner}/{repo}/check-runs
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/checks#create-a-check-run — API method documentation
# operationId: checks/create
# --actions item shape: {description: string, identifier: string, label: string}
# --output shape: {annotations?: list, images?: list, summary: string, text?: string, title: string}
export def "repos-check-runs create" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --actions: list # Displays a button on GitHub that can be clicked to alert your app to do additional tasks. For example, a code linting app can display a button that automatically fixes detected errors. The button created in this object is displayed after the check run completes. When a user clicks the button, GitHub sends the [`check_run.requested_action` webhook](https://docs.github.com/enterprise-server@2.22/webhooks/event-payloads/#check_run) to your app. Each action includes a `label`, `identifier` and `description`. A maximum of three actions are accepted. See the [`actions` object](https://docs.github.com/enterprise-server@2.22/rest/reference/checks#actions-object) description. To learn more about check runs and requested actions, see "[Check runs and requested actions](https://docs.github.com/enterprise-server@2.22/rest/reference/checks#check-runs-and-requested-actions)." — item shape: {description: string, identifier: string, label: string}
  --completed-at: string # The time the check completed. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --conclusion: string@conclusion-completer # **Required if you provide `completed_at` or a `status` of `completed`**. The final conclusion of the check. Can be one of `action_required`, `cancelled`, `failure`, `neutral`, `success`, `skipped`, `stale`, or `timed_out`. When the conclusion is `action_required`, additional details should be provided on the site specified by `details_url`. **Note:** Providing `conclusion` will automatically set the `status` parameter to `completed`. You cannot change a check run conclusion to `stale`, only GitHub can set this.
  --details-url: string # The URL of the integrator's site that has the full details of the check. If the integrator does not provide this, then the homepage of the GitHub app is used.
  --external-id: string # A reference for the run on the integrator's system.
  head_sha: string # The SHA of the commit.
  name: string # The name of the check. For example, "code-coverage".
  --output: record # Check runs can accept a variety of data in the `output` object, including a `title` and `summary` and can optionally provide descriptive details about the run. See the [`output` object](https://docs.github.com/enterprise-server@2.22/rest/reference/checks#output-object) description. — shape: {annotations?: list, images?: list, summary: string, text?: string, title: string}
  --started-at: string # The time that the check run began. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --status: string@status-completer-1 # The current status. Can be one of `queued`, `in_progress`, or `completed`. (default: queued)
]: any -> record<app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list<string>, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, pem: string, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string>, slug: string, updated_at: string, webhook_secret: string>, check_suite: record<id: int>, completed_at: string, conclusion: string, deployment: record<created_at: string, description: string, environment: string, id: int, node_id: string, original_environment: string, performed_via_github_app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record, pem: string, permissions: record, slug: string, updated_at: string, webhook_secret: string>, production_environment: bool, repository_url: string, statuses_url: string, task: string, transient_environment: bool, updated_at: string, url: string>, details_url: string, external_id: string, head_sha: string, html_url: string, id: int, name: string, node_id: string, output: record<annotations_count: int, annotations_url: string, summary: string, text: string, title: string>, pull_requests: list<record<base: record, head: record, id: int, number: int, url: string>>, started_at: string, status: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/check-runs"))
  let req_body = {"actions": $actions, "completed_at": $completed_at, "conclusion": $conclusion, "details_url": $details_url, "external_id": $external_id, "head_sha": $head_sha, "name": $name, "output": $output, "started_at": $started_at, "status": $status} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Get a check run
#
# GET /repos/{owner}/{repo}/check-runs/{check_run_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/checks#get-a-check-run — API method documentation
# operationId: checks/get
export def "repos-check-runs get" [
  owner: string
  repo: string
  check_run_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list<string>, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, pem: string, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string>, slug: string, updated_at: string, webhook_secret: string>, check_suite: record<id: int>, completed_at: string, conclusion: string, deployment: record<created_at: string, description: string, environment: string, id: int, node_id: string, original_environment: string, performed_via_github_app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record, pem: string, permissions: record, slug: string, updated_at: string, webhook_secret: string>, production_environment: bool, repository_url: string, statuses_url: string, task: string, transient_environment: bool, updated_at: string, url: string>, details_url: string, external_id: string, head_sha: string, html_url: string, id: int, name: string, node_id: string, output: record<annotations_count: int, annotations_url: string, summary: string, text: string, title: string>, pull_requests: list<record<base: record, head: record, id: int, number: int, url: string>>, started_at: string, status: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), check_run_id: (encode-path-segment $check_run_id)} | format pattern "/repos/{owner}/{repo}/check-runs/{check_run_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a check run
#
# PATCH /repos/{owner}/{repo}/check-runs/{check_run_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/checks#update-a-check-run — API method documentation
# operationId: checks/update
# --actions item shape: {description: string, identifier: string, label: string}
# --output shape: {annotations?: list, images?: list, summary: string, text?: string, title?: string}
export def "repos-check-runs update" [
  owner: string
  repo: string
  check_run_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --actions: list # Possible further actions the integrator can perform, which a user may trigger. Each action includes a `label`, `identifier` and `description`. A maximum of three actions are accepted. See the [`actions` object](https://docs.github.com/enterprise-server@2.22/rest/reference/checks#actions-object) description. To learn more about check runs and requested actions, see "[Check runs and requested actions](https://docs.github.com/enterprise-server@2.22/rest/reference/checks#check-runs-and-requested-actions)." — item shape: {description: string, identifier: string, label: string}
  --completed-at: string # The time the check completed. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --conclusion: string@conclusion-completer # **Required if you provide `completed_at` or a `status` of `completed`**. The final conclusion of the check. Can be one of `action_required`, `cancelled`, `failure`, `neutral`, `success`, `skipped`, `stale`, or `timed_out`. **Note:** Providing `conclusion` will automatically set the `status` parameter to `completed`. You cannot change a check run conclusion to `stale`, only GitHub can set this.
  --details-url: string # The URL of the integrator's site that has the full details of the check.
  --external-id: string # A reference for the run on the integrator's system.
  --name: string # The name of the check. For example, "code-coverage".
  --output: record # Check runs can accept a variety of data in the `output` object, including a `title` and `summary` and can optionally provide descriptive details about the run. See the [`output` object](https://docs.github.com/enterprise-server@2.22/rest/reference/checks#output-object-1) description. — shape: {annotations?: list, images?: list, summary: string, text?: string, title?: string}
  --started-at: string # This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --status: string@status-completer-1 # The current status. Can be one of `queued`, `in_progress`, or `completed`.
]: any -> record<app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list<string>, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, pem: string, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string>, slug: string, updated_at: string, webhook_secret: string>, check_suite: record<id: int>, completed_at: string, conclusion: string, deployment: record<created_at: string, description: string, environment: string, id: int, node_id: string, original_environment: string, performed_via_github_app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record, pem: string, permissions: record, slug: string, updated_at: string, webhook_secret: string>, production_environment: bool, repository_url: string, statuses_url: string, task: string, transient_environment: bool, updated_at: string, url: string>, details_url: string, external_id: string, head_sha: string, html_url: string, id: int, name: string, node_id: string, output: record<annotations_count: int, annotations_url: string, summary: string, text: string, title: string>, pull_requests: list<record<base: record, head: record, id: int, number: int, url: string>>, started_at: string, status: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), check_run_id: (encode-path-segment $check_run_id)} | format pattern "/repos/{owner}/{repo}/check-runs/{check_run_id}"))
  let req_body = {"actions": $actions, "completed_at": $completed_at, "conclusion": $conclusion, "details_url": $details_url, "external_id": $external_id, "name": $name, "output": $output, "started_at": $started_at, "status": $status} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List check run annotations
#
# GET /repos/{owner}/{repo}/check-runs/{check_run_id}/annotations
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/checks#list-check-run-annotations — API method documentation
# operationId: checks/list-annotations
export def "repos-check-runs-annotations list" [
  owner: string
  repo: string
  check_run_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<annotation_level: string, blob_href: string, end_column: int, end_line: int, message: string, path: string, raw_details: string, start_column: int, start_line: int, title: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), check_run_id: (encode-path-segment $check_run_id)} | format pattern "/repos/{owner}/{repo}/check-runs/{check_run_id}/annotations") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a check suite
#
# POST /repos/{owner}/{repo}/check-suites
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/checks#create-a-check-suite — API method documentation
# operationId: checks/create-suite
export def "repos-check-suites create" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  head_sha: string # The sha of the head commit.
]: any -> record<after: string, app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list<string>, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, pem: string, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string>, slug: string, updated_at: string, webhook_secret: string>, before: string, check_runs_url: string, conclusion: string, created_at: string, head_branch: string, head_commit: record<author: record<email: string, name: string>, committer: record<email: string, name: string>, id: string, message: string, timestamp: string, tree_id: string>, head_sha: string, id: int, latest_check_runs_count: int, node_id: string, pull_requests: table<base: record, head: record, id: int, number: int, url: string>, repository: record<allow_forking: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record<body: string, html_url: string, key: string, name: string, url: string>, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<key: string, name: string, node_id: string, spdx_id: string, url: string>, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, status: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/check-suites"))
  let req_body = {"head_sha": $head_sha} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Update repository preferences for check suites
#
# PATCH /repos/{owner}/{repo}/check-suites/preferences
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/checks#update-repository-preferences-for-check-suites — API method documentation
# operationId: checks/set-suites-preferences
# --auto_trigger_checks item shape: {app_id: int, setting: bool}
export def "repos-check-suites-preferences update" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --auto-trigger-checks: list # Enables or disables automatic creation of CheckSuite events upon pushes to the repository. Enabled by default. See the [`auto_trigger_checks` object](https://docs.github.com/enterprise-server@2.22/rest/reference/checks#auto_trigger_checks-object) description for details. — item shape: {app_id: int, setting: bool}
]: any -> record<preferences: record<auto_trigger_checks: list<record>>, repository: record<allow_forking: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record<body: string, html_url: string, key: string, name: string, url: string>, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<key: string, name: string, node_id: string, spdx_id: string, url: string>, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/check-suites/preferences"))
  let req_body = {"auto_trigger_checks": $auto_trigger_checks} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Get a check suite
#
# GET /repos/{owner}/{repo}/check-suites/{check_suite_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/checks#get-a-check-suite — API method documentation
# operationId: checks/get-suite
export def "repos-check-suites get" [
  owner: string
  repo: string
  check_suite_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<after: string, app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list<string>, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, pem: string, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string>, slug: string, updated_at: string, webhook_secret: string>, before: string, check_runs_url: string, conclusion: string, created_at: string, head_branch: string, head_commit: record<author: record<email: string, name: string>, committer: record<email: string, name: string>, id: string, message: string, timestamp: string, tree_id: string>, head_sha: string, id: int, latest_check_runs_count: int, node_id: string, pull_requests: table<base: record, head: record, id: int, number: int, url: string>, repository: record<allow_forking: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record<body: string, html_url: string, key: string, name: string, url: string>, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<key: string, name: string, node_id: string, spdx_id: string, url: string>, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, status: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), check_suite_id: (encode-path-segment $check_suite_id)} | format pattern "/repos/{owner}/{repo}/check-suites/{check_suite_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List check runs in a check suite
#
# GET /repos/{owner}/{repo}/check-suites/{check_suite_id}/check-runs
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/checks#list-check-runs-in-a-check-suite — API method documentation
# operationId: checks/list-for-suite
export def "repos-check-suites-check-runs list" [
  owner: string
  repo: string
  check_suite_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --check-name: string # Returns check runs with the specified `name`.
  --status: string@status-completer-1 # Returns check runs with the specified `status`. Can be one of `queued`, `in_progress`, or `completed`.
  --filter: string@filter-completer-2 # Filters check runs by their `completed_at` timestamp. Can be one of `latest` (returning the most recent check runs) or `all`. (default: latest)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<check_runs: table<app: record, check_suite: record, completed_at: string, conclusion: string, deployment: record, details_url: string, external_id: string, head_sha: string, html_url: string, id: int, name: string, node_id: string, output: record, pull_requests: list, started_at: string, status: string, url: string>, total_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "check_name" $check_name "scalar") (serialize-qp "status" $status "scalar") (serialize-qp "filter" $filter "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), check_suite_id: (encode-path-segment $check_suite_id)} | format pattern "/repos/{owner}/{repo}/check-suites/{check_suite_id}/check-runs") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Rerequest a check suite
#
# POST /repos/{owner}/{repo}/check-suites/{check_suite_id}/rerequest
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/checks#rerequest-a-check-suite — API method documentation
# operationId: checks/rerequest-suite
export def "repos-check-suites-rerequest create" [
  owner: string
  repo: string
  check_suite_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), check_suite_id: (encode-path-segment $check_suite_id)} | format pattern "/repos/{owner}/{repo}/check-suites/{check_suite_id}/rerequest"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List code scanning alerts for a repository
#
# GET /repos/{owner}/{repo}/code-scanning/alerts
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/code-scanning#list-code-scanning-alerts-for-a-repository — API method documentation
# operationId: code-scanning/list-alerts-for-repo
export def "repos-code-scanning-alerts list" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --tool-name: string # The name of a code scanning tool. Only results by this tool will be listed. You can specify the tool by using either `tool_name` or `tool_guid`, but not both.
  --tool-guid: string # The GUID of a code scanning tool. Only results by this tool will be listed. Note that some code scanning tools may not include a GUID in their analysis data. You can specify the tool by using either `tool_guid` or `tool_name`, but not both. (nullable)
  --page: int # Page number of the results to fetch. (default: 1)
  --per-page: int # Results per page (max 100) (default: 30)
  --ref: string # The Git reference for the results you want to list. The `ref` for a branch can be formatted either as `refs/heads/` or simply ``. To reference a pull request use `refs/pull//merge`.
  --state: string@state-completer-1 # Set to `open`, `fixed`, or `dismissed` to list code scanning alerts in a specific state.
]: nothing -> table<classification: string, created_at: string, dismissed_at: string, dismissed_by: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, dismissed_reason: string, html_url: string, instance: record<analysis_key: string, category: string, classifications: list, commit_sha: string, environment: string, html_url: string, location: record, message: record, ref: string, state: string>, number: int, rule: record<description: string, id: string, name: string, severity: string>, state: string, tool: record<guid: string, name: string, version: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "tool_name" $tool_name "scalar") (serialize-qp "tool_guid" $tool_guid "scalar") (serialize-qp "page" $page "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "ref" $ref "scalar") (serialize-qp "state" $state "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/code-scanning/alerts") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a code scanning alert
#
# GET /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/code-scanning#get-a-code-scanning-alert — API method documentation
# operationId: code-scanning/get-alert
export def "repos-code-scanning-alerts get" [
  owner: string
  repo: string
  alert_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created_at: string, dismissed_at: string, dismissed_by: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, dismissed_reason: string, html_url: string, instances: any, number: int, rule: record<description: string, full_description: string, help: string, id: string, name: string, severity: string, tags: list<string>>, state: string, tool: record<guid: string, name: string, version: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), alert_number: (encode-path-segment $alert_number)} | format pattern "/repos/{owner}/{repo}/code-scanning/alerts/{alert_number}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a code scanning alert
#
# PATCH /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/code-scanning#update-a-code-scanning-alert — API method documentation
# operationId: code-scanning/update-alert
export def "repos-code-scanning-alerts update" [
  owner: string
  repo: string
  alert_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --dismissed-reason: string@dismissed-reason-completer # **Required when the state is dismissed.** The reason for dismissing or closing the alert. Can be one of: `false positive`, `won't fix`, and `used in tests`. (nullable)
  state: string@state-completer-2 # Sets the state of the code scanning alert. Can be one of `open` or `dismissed`. You must provide `dismissed_reason` when you set the state to `dismissed`.
]: any -> record<created_at: string, dismissed_at: string, dismissed_by: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, dismissed_reason: string, html_url: string, instances: any, number: int, rule: record<description: string, full_description: string, help: string, id: string, name: string, severity: string, tags: list<string>>, state: string, tool: record<guid: string, name: string, version: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), alert_number: (encode-path-segment $alert_number)} | format pattern "/repos/{owner}/{repo}/code-scanning/alerts/{alert_number}"))
  let req_body = {"dismissed_reason": $dismissed_reason, "state": $state} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List code scanning analyses for a repository
#
# GET /repos/{owner}/{repo}/code-scanning/analyses
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/code-scanning#list-code-scanning-analyses-for-a-repository — API method documentation
# operationId: code-scanning/list-recent-analyses
export def "repos-code-scanning-analyses list-recent" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --tool-name: string # The name of a code scanning tool. Only results by this tool will be listed. You can specify the tool by using either `tool_name` or `tool_guid`, but not both.
  --tool-guid: string # The GUID of a code scanning tool. Only results by this tool will be listed. Note that some code scanning tools may not include a GUID in their analysis data. You can specify the tool by using either `tool_guid` or `tool_name`, but not both. (nullable)
  --page: int # Page number of the results to fetch. (default: 1)
  --per-page: int # Results per page (max 100) (default: 30)
  --ref: string # The Git reference for the analyses you want to list. The `ref` for a branch can be formatted either as `refs/heads/` or simply ``. To reference a pull request use `refs/pull//merge`.
  --sarif-id: string # Filter analyses belonging to the same SARIF upload. (e.g. 6c81cd8e-b078-4ac3-a3be-1dad7dbd0b53)
]: nothing -> table<analysis_key: string, category: string, commit_sha: string, created_at: string, deletable: bool, environment: string, error: string, id: int, ref: string, results_count: int, rules_count: int, sarif_id: string, tool: record<guid: string, name: string, version: string>, tool_name: string, url: string, warning: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "tool_name" $tool_name "scalar") (serialize-qp "tool_guid" $tool_guid "scalar") (serialize-qp "page" $page "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "ref" $ref "scalar") (serialize-qp "sarif_id" $sarif_id "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/code-scanning/analyses") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Upload an analysis as SARIF data
#
# POST /repos/{owner}/{repo}/code-scanning/sarifs
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/code-scanning#upload-a-sarif-file — API method documentation
# operationId: code-scanning/upload-sarif
export def "repos-code-scanning-sarifs upload" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --checkout-uri: string # The base directory used in the analysis, as it appears in the SARIF file. This property is used to convert file paths from absolute to relative, so that alerts can be mapped to their correct location in the repository. (format: uri, e.g. file:///github/workspace/)
  commit_sha: string # The SHA of the commit to which the analysis you are uploading relates.
  ref: string # The full Git reference, formatted as `refs/heads/`, `refs/pull//merge`, or `refs/pull//head`.
  sarif: string # A Base64 string representing the SARIF file to upload. You must first compress your SARIF file using [`gzip`](http://www.gnu.org/software/gzip/manual/gzip.html) and then translate the contents of the file into a Base64 encoding string. For more information, see "[SARIF support for code scanning](https://docs.github.com/enterprise-server@2.22/github/finding-security-vulnerabilities-and-errors-in-your-code/integrating-with-code-scanning/sarif-support-for-code-scanning)."
  --started-at: string # The time that the analysis run began. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --tool-name: string # The name of the tool used to generate the code scanning analysis. If this parameter is not used, the tool name defaults to "API". If the uploaded SARIF contains a tool GUID, this will be available for filtering using the `tool_guid` parameter of operations such as `GET /repos/{owner}/{repo}/code-scanning/alerts`.
]: any -> record<id: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/code-scanning/sarifs"))
  let req_body = {"checkout_uri": $checkout_uri, "commit_sha": $commit_sha, "ref": $ref, "sarif": $sarif, "started_at": $started_at, "tool_name": $tool_name} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List repository collaborators
#
# GET /repos/{owner}/{repo}/collaborators
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-repository-collaborators — API method documentation
# operationId: repos/list-collaborators
export def "repos-collaborators list" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --affiliation: string@affiliation-completer # Filter collaborators returned by their affiliation. Can be one of: \* `outside`: All outside collaborators of an organization-owned repository. \* `direct`: All collaborators with permissions to an organization-owned repository, regardless of organization membership status. \* `all`: All collaborators the authenticated user can see. (default: all)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, received_events_url: string, repos_url: string, site_admin: bool, starred_url: string, subscriptions_url: string, type: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "affiliation" $affiliation "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/collaborators") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove a repository collaborator
#
# DELETE /repos/{owner}/{repo}/collaborators/{username}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#remove-a-repository-collaborator — API method documentation
# operationId: repos/remove-collaborator
export def "repos-collaborators delete" [
  owner: string
  repo: string
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), username: (encode-path-segment $username)} | format pattern "/repos/{owner}/{repo}/collaborators/{username}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Check if a user is a repository collaborator
#
# GET /repos/{owner}/{repo}/collaborators/{username}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#check-if-a-user-is-a-repository-collaborator — API method documentation
# operationId: repos/check-collaborator
export def "repos-collaborators check" [
  owner: string
  repo: string
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), username: (encode-path-segment $username)} | format pattern "/repos/{owner}/{repo}/collaborators/{username}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add a repository collaborator
#
# PUT /repos/{owner}/{repo}/collaborators/{username}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#add-a-repository-collaborator — API method documentation
# operationId: repos/add-collaborator
export def "repos-collaborators create" [
  owner: string
  repo: string
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --permission: string@permission-completer-2 # The permission to grant the collaborator. **Only valid on organization-owned repositories.** Can be one of: \* `pull` - can pull, but not push to or administer this repository. \* `push` - can pull and push, but not administer this repository. \* `admin` - can pull, push and administer this repository. \* `maintain` - Recommended for project managers who need to manage the repository without access to sensitive or destructive actions. \* `triage` - Recommended for contributors who need to proactively manage issues and pull requests without write access. (default: push)
  --permissions: string # e.g. "push"
]: any -> record<created_at: string, expired: bool, html_url: string, id: int, invitee: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, inviter: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, node_id: string, permissions: string, repository: record<allow_forking: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record<body: string, html_url: string, key: string, name: string, url: string>, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<key: string, name: string, node_id: string, spdx_id: string, url: string>, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), username: (encode-path-segment $username)} | format pattern "/repos/{owner}/{repo}/collaborators/{username}"))
  let req_body = {"permission": $permission, "permissions": $permissions} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Get repository permissions for a user
#
# GET /repos/{owner}/{repo}/collaborators/{username}/permission
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-repository-permissions-for-a-user — API method documentation
# operationId: repos/get-collaborator-permission-level
export def "repos-collaborators-permission get-level" [
  owner: string
  repo: string
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<permission: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, received_events_url: string, repos_url: string, site_admin: bool, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), username: (encode-path-segment $username)} | format pattern "/repos/{owner}/{repo}/collaborators/{username}/permission"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List commit comments for a repository
#
# GET /repos/{owner}/{repo}/comments
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-commit-comments-for-a-repository — API method documentation
# operationId: repos/list-commit-comments-for-repo
export def "repos-comments list-commit" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<author_association: string, body: string, commit_id: string, created_at: string, html_url: string, id: int, line: int, node_id: string, path: string, position: int, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/comments") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a commit comment
#
# DELETE /repos/{owner}/{repo}/comments/{comment_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#delete-a-commit-comment — API method documentation
# operationId: repos/delete-commit-comment
export def "repos-comments delete-commit" [
  owner: string
  repo: string
  comment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), comment_id: (encode-path-segment $comment_id)} | format pattern "/repos/{owner}/{repo}/comments/{comment_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a commit comment
#
# GET /repos/{owner}/{repo}/comments/{comment_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-a-commit-comment — API method documentation
# operationId: repos/get-commit-comment
export def "repos-comments get-commit" [
  owner: string
  repo: string
  comment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<author_association: string, body: string, commit_id: string, created_at: string, html_url: string, id: int, line: int, node_id: string, path: string, position: int, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), comment_id: (encode-path-segment $comment_id)} | format pattern "/repos/{owner}/{repo}/comments/{comment_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a commit comment
#
# PATCH /repos/{owner}/{repo}/comments/{comment_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#update-a-commit-comment — API method documentation
# operationId: repos/update-commit-comment
export def "repos-comments update-commit" [
  owner: string
  repo: string
  comment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  body: string # The contents of the comment
]: any -> record<author_association: string, body: string, commit_id: string, created_at: string, html_url: string, id: int, line: int, node_id: string, path: string, position: int, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), comment_id: (encode-path-segment $comment_id)} | format pattern "/repos/{owner}/{repo}/comments/{comment_id}"))
  let req_body = {"body": $body} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List reactions for a commit comment
#
# GET /repos/{owner}/{repo}/comments/{comment_id}/reactions
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#list-reactions-for-a-commit-comment — API method documentation
# operationId: reactions/list-for-commit-comment
export def "repos-comments-reactions list-for-commit" [
  owner: string
  repo: string
  comment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --content: string@content-completer # Returns a single [reaction type](https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#reaction-types). Omit this parameter to list all reactions to a commit comment.
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<content: string, created_at: string, id: int, node_id: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "content" $content "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), comment_id: (encode-path-segment $comment_id)} | format pattern "/repos/{owner}/{repo}/comments/{comment_id}/reactions") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create reaction for a commit comment
#
# POST /repos/{owner}/{repo}/comments/{comment_id}/reactions
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#create-reaction-for-a-commit-comment — API method documentation
# operationId: reactions/create-for-commit-comment
export def "repos-comments-reactions create-for-commit" [
  owner: string
  repo: string
  comment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  content: string@content-completer # The [reaction type](https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#reaction-types) to add to the commit comment.
]: any -> record<content: string, created_at: string, id: int, node_id: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), comment_id: (encode-path-segment $comment_id)} | format pattern "/repos/{owner}/{repo}/comments/{comment_id}/reactions"))
  let req_body = {"content": $content} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a commit comment reaction
#
# DELETE /repos/{owner}/{repo}/comments/{comment_id}/reactions/{reaction_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#delete-a-commit-comment-reaction — API method documentation
# operationId: reactions/delete-for-commit-comment
export def "repos-comments-reactions delete-for-commit" [
  owner: string
  repo: string
  comment_id: int
  reaction_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), comment_id: (encode-path-segment $comment_id), reaction_id: (encode-path-segment $reaction_id)} | format pattern "/repos/{owner}/{repo}/comments/{comment_id}/reactions/{reaction_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List commits
#
# GET /repos/{owner}/{repo}/commits
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-commits — API method documentation
# operationId: repos/list-commits
export def "repos-commits list" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --sha: string # SHA or branch to start listing commits from. Default: the repository’s default branch (usually `master`).
  --path: string # Only commits containing this file path will be returned.
  --author: string # GitHub login or email address by which to filter by commit author.
  --since: string # Only show notifications updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --until: string # Only commits before this date will be returned. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, comments_url: string, commit: record<author: record, comment_count: int, committer: record, message: string, tree: record, url: string, verification: record>, committer: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, files: list<record>, html_url: string, node_id: string, parents: list<record>, sha: string, stats: record<additions: int, deletions: int, total: int>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "sha" $sha "scalar") (serialize-qp "path" $path "scalar") (serialize-qp "author" $author "scalar") (serialize-qp "since" $since "scalar") (serialize-qp "until" $until "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/commits") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List branches for HEAD commit
#
# GET /repos/{owner}/{repo}/commits/{commit_sha}/branches-where-head
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-branches-for-head-commit — API method documentation
# operationId: repos/list-branches-for-head-commit
export def "repos-commits-branches-where-head list" [
  owner: string
  repo: string
  commit_sha: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> table<commit: record<sha: string, url: string>, name: string, protected: bool> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), commit_sha: (encode-path-segment $commit_sha)} | format pattern "/repos/{owner}/{repo}/commits/{commit_sha}/branches-where-head"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List commit comments
#
# GET /repos/{owner}/{repo}/commits/{commit_sha}/comments
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-commit-comments — API method documentation
# operationId: repos/list-comments-for-commit
export def "repos-commits-comments list" [
  owner: string
  repo: string
  commit_sha: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<author_association: string, body: string, commit_id: string, created_at: string, html_url: string, id: int, line: int, node_id: string, path: string, position: int, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), commit_sha: (encode-path-segment $commit_sha)} | format pattern "/repos/{owner}/{repo}/commits/{commit_sha}/comments") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a commit comment
#
# POST /repos/{owner}/{repo}/commits/{commit_sha}/comments
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#create-a-commit-comment — API method documentation
# operationId: repos/create-commit-comment
export def "repos-commits-comments create" [
  owner: string
  repo: string
  commit_sha: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  body: string # The contents of the comment.
  --line: int # **Deprecated**. Use **position** parameter instead. Line number in the file to comment on.
  --path: string # Relative path of the file to comment on.
  --position: int # Line index in the diff to comment on.
]: any -> record<author_association: string, body: string, commit_id: string, created_at: string, html_url: string, id: int, line: int, node_id: string, path: string, position: int, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), commit_sha: (encode-path-segment $commit_sha)} | format pattern "/repos/{owner}/{repo}/commits/{commit_sha}/comments"))
  let req_body = {"body": $body, "line": $line, "path": $path, "position": $position} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List pull requests associated with a commit
#
# GET /repos/{owner}/{repo}/commits/{commit_sha}/pulls
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-pull-requests-associated-with-a-commit — API method documentation
# operationId: repos/list-pull-requests-associated-with-commit
export def "repos-commits-pulls list-requests-associated" [
  owner: string
  repo: string
  commit_sha: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<_links: record<comments: record, commits: record, html: record, issue: record, review_comment: record, review_comments: record, self: record, statuses: record>, active_lock_reason: string, assignee: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, assignees: list<record>, author_association: string, base: record<label: string, ref: string, repo: record, sha: string, user: record>, body: string, closed_at: string, comments_url: string, commits_url: string, created_at: string, diff_url: string, draft: bool, head: record<label: string, ref: string, repo: record, sha: string, user: record>, html_url: string, id: int, issue_url: string, labels: list<record>, locked: bool, merge_commit_sha: string, merged_at: string, milestone: record<closed_at: string, closed_issues: int, created_at: string, creator: record, description: string, due_on: string, html_url: string, id: int, labels_url: string, node_id: string, number: int, open_issues: int, state: string, title: string, updated_at: string, url: string>, node_id: string, number: int, patch_url: string, requested_reviewers: list<record>, requested_teams: list<record>, review_comment_url: string, review_comments_url: string, state: string, statuses_url: string, title: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), commit_sha: (encode-path-segment $commit_sha)} | format pattern "/repos/{owner}/{repo}/commits/{commit_sha}/pulls") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a commit
#
# GET /repos/{owner}/{repo}/commits/{ref}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-a-commit — API method documentation
# operationId: repos/get-commit
export def "repos-commits get" [
  owner: string
  repo: string
  ref: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --page: int # Page number of the results to fetch. (default: 1)
  --per-page: int # Results per page (max 100) (default: 30)
]: nothing -> record<author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, comments_url: string, commit: record<author: record<date: string, email: string, name: string>, comment_count: int, committer: record<date: string, email: string, name: string>, message: string, tree: record<sha: string, url: string>, url: string, verification: record<payload: string, reason: string, signature: string, verified: bool>>, committer: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, files: table<additions: int, blob_url: string, changes: int, contents_url: string, deletions: int, filename: string, patch: string, previous_filename: string, raw_url: string, sha: string, status: string>, html_url: string, node_id: string, parents: table<html_url: string, sha: string, url: string>, sha: string, stats: record<additions: int, deletions: int, total: int>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "page" $page "scalar") (serialize-qp "per_page" $per_page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), ref: (encode-path-segment $ref)} | format pattern "/repos/{owner}/{repo}/commits/{ref}") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List check runs for a Git reference
#
# GET /repos/{owner}/{repo}/commits/{ref}/check-runs
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/checks#list-check-runs-for-a-git-reference — API method documentation
# operationId: checks/list-for-ref
export def "repos-commits-check-runs list" [
  owner: string
  repo: string
  ref: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --check-name: string # Returns check runs with the specified `name`.
  --status: string@status-completer-1 # Returns check runs with the specified `status`. Can be one of `queued`, `in_progress`, or `completed`.
  --filter: string@filter-completer-2 # Filters check runs by their `completed_at` timestamp. Can be one of `latest` (returning the most recent check runs) or `all`. (default: latest)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
  --app-id: int
]: nothing -> record<check_runs: table<app: record, check_suite: record, completed_at: string, conclusion: string, deployment: record, details_url: string, external_id: string, head_sha: string, html_url: string, id: int, name: string, node_id: string, output: record, pull_requests: list, started_at: string, status: string, url: string>, total_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "check_name" $check_name "scalar") (serialize-qp "status" $status "scalar") (serialize-qp "filter" $filter "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar") (serialize-qp "app_id" $app_id "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), ref: (encode-path-segment $ref)} | format pattern "/repos/{owner}/{repo}/commits/{ref}/check-runs") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List check suites for a Git reference
#
# GET /repos/{owner}/{repo}/commits/{ref}/check-suites
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/checks#list-check-suites-for-a-git-reference — API method documentation
# operationId: checks/list-suites-for-ref
export def "repos-commits-check-suites list" [
  owner: string
  repo: string
  ref: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --app-id: int # Filters check suites by GitHub App `id`. (e.g. 1)
  --check-name: string # Returns check runs with the specified `name`.
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<check_suites: table<after: string, app: record, before: string, check_runs_url: string, conclusion: string, created_at: string, head_branch: string, head_commit: record, head_sha: string, id: int, latest_check_runs_count: int, node_id: string, pull_requests: list, repository: record, status: string, updated_at: string, url: string>, total_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "app_id" $app_id "scalar") (serialize-qp "check_name" $check_name "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), ref: (encode-path-segment $ref)} | format pattern "/repos/{owner}/{repo}/commits/{ref}/check-suites") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the combined status for a specific reference
#
# GET /repos/{owner}/{repo}/commits/{ref}/status
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-the-combined-status-for-a-specific-reference — API method documentation
# operationId: repos/get-combined-status-for-ref
export def "repos-commits-status get-combined" [
  owner: string
  repo: string
  ref: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<commit_url: string, repository: record<allow_forking: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record<body: string, html_url: string, key: string, name: string, url: string>, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<key: string, name: string, node_id: string, spdx_id: string, url: string>, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, sha: string, state: string, statuses: table<avatar_url: string, context: string, created_at: string, description: string, id: int, node_id: string, required: bool, state: string, target_url: string, updated_at: string, url: string>, total_count: int, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), ref: (encode-path-segment $ref)} | format pattern "/repos/{owner}/{repo}/commits/{ref}/status") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List commit statuses for a reference
#
# GET /repos/{owner}/{repo}/commits/{ref}/statuses
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-commit-statuses-for-a-reference — API method documentation
# operationId: repos/list-commit-statuses-for-ref
export def "repos-commits-statuses list" [
  owner: string
  repo: string
  ref: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<avatar_url: string, context: string, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, description: string, id: int, node_id: string, state: string, target_url: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), ref: (encode-path-segment $ref)} | format pattern "/repos/{owner}/{repo}/commits/{ref}/statuses") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Compare two commits
#
# GET /repos/{owner}/{repo}/compare/{basehead}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#compare-two-commits — API method documentation
# operationId: repos/compare-commits
export def "repos-compare get-commits" [
  owner: string
  repo: string
  basehead: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<ahead_by: int, base_commit: record<author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, comments_url: string, commit: record<author: record, comment_count: int, committer: record, message: string, tree: record, url: string, verification: record>, committer: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, files: list<record>, html_url: string, node_id: string, parents: list<record>, sha: string, stats: record<additions: int, deletions: int, total: int>, url: string>, behind_by: int, commits: table<author: record, comments_url: string, commit: record, committer: record, files: list, html_url: string, node_id: string, parents: list, sha: string, stats: record, url: string>, diff_url: string, files: table<additions: int, blob_url: string, changes: int, contents_url: string, deletions: int, filename: string, patch: string, previous_filename: string, raw_url: string, sha: string, status: string>, html_url: string, merge_base_commit: record<author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, comments_url: string, commit: record<author: record, comment_count: int, committer: record, message: string, tree: record, url: string, verification: record>, committer: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, files: list<record>, html_url: string, node_id: string, parents: list<record>, sha: string, stats: record<additions: int, deletions: int, total: int>, url: string>, patch_url: string, permalink_url: string, status: string, total_commits: int, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), basehead: (encode-path-segment $basehead)} | format pattern "/repos/{owner}/{repo}/compare/{basehead}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a content attachment
#
# POST /repos/{owner}/{repo}/content_references/{content_reference_id}/attachments
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps#create-a-content-attachment — API method documentation
# operationId: apps/create-content-attachment
export def "repos-content-references-attachments create-apps" [
  owner: string
  repo: string
  content_reference_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  body: string # The body of the attachment (e.g. Body of the attachment)
  title: string # The title of the attachment (e.g. Title of the attachment)
]: any -> record<body: string, id: int, node_id: string, title: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), content_reference_id: (encode-path-segment $content_reference_id)} | format pattern "/repos/{owner}/{repo}/content_references/{content_reference_id}/attachments"))
  let req_body = {"body": $body, "title": $title} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a file
#
# DELETE /repos/{owner}/{repo}/contents/{path}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#delete-a-file — API method documentation
# operationId: repos/delete-file
# --author shape: {email?: string, name?: string}
# --committer shape: {email?: string, name?: string}
export def "repos-contents delete-file" [
  owner: string
  repo: string
  path: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --author: record # object containing information about the author. — shape: {email?: string, name?: string}
  --branch: string # The branch name. Default: the repository’s default branch (usually `master`)
  --committer: record # object containing information about the committer. — shape: {email?: string, name?: string}
  message: string # The commit message.
  sha: string # The blob SHA of the file being replaced.
]: any -> record<commit: record<author: record<date: string, email: string, name: string>, committer: record<date: string, email: string, name: string>, html_url: string, message: string, node_id: string, parents: list<record>, sha: string, tree: record<sha: string, url: string>, url: string, verification: record<payload: string, reason: string, signature: string, verified: bool>>, content: record<_links: record<git: string, html: string, self: string>, download_url: string, git_url: string, html_url: string, name: string, path: string, sha: string, size: int, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), path: (encode-path-segment $path)} | format pattern "/repos/{owner}/{repo}/contents/{path}"))
  let req_body = {"author": $author, "branch": $branch, "committer": $committer, "message": $message, "sha": $sha} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Get repository content
#
# GET /repos/{owner}/{repo}/contents/{path}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-repository-content — API method documentation
# operationId: repos/get-content
export def "repos-contents get" [
  owner: string
  repo: string
  path: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --ref: string # The name of the commit/branch/tag. Default: the repository’s default branch (usually `master`)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "ref" $ref "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), path: (encode-path-segment $path)} | format pattern "/repos/{owner}/{repo}/contents/{path}") $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create or update file contents
#
# PUT /repos/{owner}/{repo}/contents/{path}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#create-or-update-file-contents — API method documentation
# operationId: repos/create-or-update-file-contents
# --author shape: {date?: string, email: string, name: string}
# --committer shape: {date?: string, email: string, name: string}
export def "repos-contents create-or-update-file" [
  owner: string
  repo: string
  path: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --author: record # The author of the file. Default: The `committer` or the authenticated user if you omit `committer`. — shape: {date?: string, email: string, name: string}
  --branch: string # The branch name. Default: the repository’s default branch (usually `master`)
  --committer: record # The person that committed the file. Default: the authenticated user. — shape: {date?: string, email: string, name: string}
  content: string # The new file content, using Base64 encoding.
  message: string # The commit message.
  --sha: string # **Required if you are updating a file**. The blob SHA of the file being replaced.
]: any -> record<commit: record<author: record<date: string, email: string, name: string>, committer: record<date: string, email: string, name: string>, html_url: string, message: string, node_id: string, parents: list<record>, sha: string, tree: record<sha: string, url: string>, url: string, verification: record<payload: string, reason: string, signature: string, verified: bool>>, content: record<_links: record<git: string, html: string, self: string>, download_url: string, git_url: string, html_url: string, name: string, path: string, sha: string, size: int, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), path: (encode-path-segment $path)} | format pattern "/repos/{owner}/{repo}/contents/{path}"))
  let req_body = {"author": $author, "branch": $branch, "committer": $committer, "content": $content, "message": $message, "sha": $sha} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List repository contributors
#
# GET /repos/{owner}/{repo}/contributors
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-repository-contributors — API method documentation
# operationId: repos/list-contributors
export def "repos-contributors list" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --anon: string # Set to `1` or `true` to include anonymous contributors in results.
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<avatar_url: string, contributions: int, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_url: string, subscriptions_url: string, type: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "anon" $anon "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/contributors") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List deployments
#
# GET /repos/{owner}/{repo}/deployments
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-deployments — API method documentation
# operationId: repos/list-deployments
export def "repos-deployments list" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --sha: string # The SHA recorded at creation time. (default: none)
  --ref: string # The name of the ref. This can be a branch, tag, or SHA. (default: none)
  --task: string # The name of the task for the deployment (e.g., `deploy` or `deploy:migrations`). (default: none)
  --environment: string # The name of the environment that was deployed to (e.g., `staging` or `production`). (nullable, default: none)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, description: string, environment: string, id: int, node_id: string, original_environment: string, payload: any, performed_via_github_app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record, pem: string, permissions: record, slug: string, updated_at: string, webhook_secret: string>, production_environment: bool, ref: string, repository_url: string, sha: string, statuses_url: string, task: string, transient_environment: bool, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "sha" $sha "scalar") (serialize-qp "ref" $ref "scalar") (serialize-qp "task" $task "scalar") (serialize-qp "environment" $environment "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/deployments") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a deployment
#
# POST /repos/{owner}/{repo}/deployments
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#create-a-deployment — API method documentation
# operationId: repos/create-deployment
export def "repos-deployments create" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --auto-merge: oneof<nothing, bool> # Attempts to automatically merge the default branch into the requested ref, if it's behind the default branch. (default: true)
  --description: string # Short description of the deployment. (nullable, default: )
  --environment: string # Name for the target deployment environment (e.g., `production`, `staging`, `qa`). (default: production)
  --payload: any
  --production-environment: oneof<nothing, bool> # Specifies if the given environment is one that end-users directly interact with. Default: `true` when `environment` is `production` and `false` otherwise. **Note:** This parameter requires you to use the [`application/vnd.github.ant-man-preview+json`](https://docs.github.com/enterprise-server@2.22/rest/overview/api-previews#enhanced-deployments) custom media type.
  ref: string # The ref to deploy. This can be a branch, tag, or SHA.
  --required-contexts: list<string> # The [status](https://docs.github.com/enterprise-server@2.22/rest/reference/repos#statuses) contexts to verify against commit status checks. If you omit this parameter, GitHub verifies all unique contexts before creating a deployment. To bypass checking entirely, pass an empty array. Defaults to all unique contexts.
  --task: string # Specifies a task to execute (e.g., `deploy` or `deploy:migrations`). (default: deploy)
  --transient-environment: oneof<nothing, bool> # Specifies if the given environment is specific to the deployment and will no longer exist at some point in the future. Default: `false` **Note:** This parameter requires you to use the [`application/vnd.github.ant-man-preview+json`](https://docs.github.com/enterprise-server@2.22/rest/overview/api-previews#enhanced-deployments) custom media type. (default: false)
]: any -> record<created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, description: string, environment: string, id: int, node_id: string, original_environment: string, payload: any, performed_via_github_app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list<string>, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, pem: string, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string>, slug: string, updated_at: string, webhook_secret: string>, production_environment: bool, ref: string, repository_url: string, sha: string, statuses_url: string, task: string, transient_environment: bool, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/deployments"))
  let req_body = {"auto_merge": $auto_merge, "description": $description, "environment": $environment, "payload": $payload, "production_environment": $production_environment, "ref": $ref, "required_contexts": $required_contexts, "task": $task, "transient_environment": $transient_environment} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a deployment
#
# DELETE /repos/{owner}/{repo}/deployments/{deployment_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#delete-a-deployment — API method documentation
# operationId: repos/delete-deployment
export def "repos-deployments delete" [
  owner: string
  repo: string
  deployment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), deployment_id: (encode-path-segment $deployment_id)} | format pattern "/repos/{owner}/{repo}/deployments/{deployment_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a deployment
#
# GET /repos/{owner}/{repo}/deployments/{deployment_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-a-deployment — API method documentation
# operationId: repos/get-deployment
export def "repos-deployments get" [
  owner: string
  repo: string
  deployment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, description: string, environment: string, id: int, node_id: string, original_environment: string, payload: any, performed_via_github_app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list<string>, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, pem: string, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string>, slug: string, updated_at: string, webhook_secret: string>, production_environment: bool, ref: string, repository_url: string, sha: string, statuses_url: string, task: string, transient_environment: bool, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), deployment_id: (encode-path-segment $deployment_id)} | format pattern "/repos/{owner}/{repo}/deployments/{deployment_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List deployment statuses
#
# GET /repos/{owner}/{repo}/deployments/{deployment_id}/statuses
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-deployment-statuses — API method documentation
# operationId: repos/list-deployment-statuses
export def "repos-deployments-statuses list" [
  owner: string
  repo: string
  deployment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, deployment_url: string, description: string, environment: string, environment_url: string, id: int, log_url: string, node_id: string, performed_via_github_app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record, pem: string, permissions: record, slug: string, updated_at: string, webhook_secret: string>, repository_url: string, state: string, target_url: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), deployment_id: (encode-path-segment $deployment_id)} | format pattern "/repos/{owner}/{repo}/deployments/{deployment_id}/statuses") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a deployment status
#
# POST /repos/{owner}/{repo}/deployments/{deployment_id}/statuses
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#create-a-deployment-status — API method documentation
# operationId: repos/create-deployment-status
export def "repos-deployments-statuses create-status" [
  owner: string
  repo: string
  deployment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --auto-inactive: oneof<nothing, bool> # Adds a new `inactive` status to all prior non-transient, non-production environment deployments with the same repository and `environment` name as the created status's deployment. An `inactive` status is only added to deployments that had a `success` state. Default: `true` **Note:** This parameter requires you to use the [`application/vnd.github.ant-man-preview+json`](https://docs.github.com/enterprise-server@2.22/rest/overview/api-previews#enhanced-deployments) custom media type.
  --description: string # A short description of the status. The maximum description length is 140 characters. (default: )
  --environment: string@environment-completer # Name for the target deployment environment, which can be changed when setting a deploy status. For example, `production`, `staging`, or `qa`.
  --environment-url: string # Sets the URL for accessing your environment. Default: `""` **Note:** This parameter requires you to use the [`application/vnd.github.ant-man-preview+json`](https://docs.github.com/enterprise-server@2.22/rest/overview/api-previews#enhanced-deployments) custom media type. (default: )
  --log-url: string # The full URL of the deployment's output. This parameter replaces `target_url`. We will continue to accept `target_url` to support legacy uses, but we recommend replacing `target_url` with `log_url`. Setting `log_url` will automatically set `target_url` to the same value. Default: `""` **Note:** This parameter requires you to use the [`application/vnd.github.ant-man-preview+json`](https://docs.github.com/enterprise-server@2.22/rest/overview/api-previews#enhanced-deployments) custom media type. (default: )
  state: string@state-completer-3 # The state of the status. Can be one of `error`, `failure`, `inactive`, `in_progress`, `queued` `pending`, or `success`. **Note:** To use the `inactive` state, you must provide the [`application/vnd.github.ant-man-preview+json`](https://docs.github.com/enterprise-server@2.22/rest/overview/api-previews#enhanced-deployments) custom media type. When you set a transient deployment to `inactive`, the deployment will be shown as `destroyed` in GitHub.
  --target-url: string # The target URL to associate with this status. This URL should contain output to keep the user updated while the task is running or serve as historical information for what happened in the deployment. **Note:** It's recommended to use the `log_url` parameter, which replaces `target_url`. (default: )
]: any -> record<created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, deployment_url: string, description: string, environment: string, environment_url: string, id: int, log_url: string, node_id: string, performed_via_github_app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list<string>, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, pem: string, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string>, slug: string, updated_at: string, webhook_secret: string>, repository_url: string, state: string, target_url: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), deployment_id: (encode-path-segment $deployment_id)} | format pattern "/repos/{owner}/{repo}/deployments/{deployment_id}/statuses"))
  let req_body = {"auto_inactive": $auto_inactive, "description": $description, "environment": $environment, "environment_url": $environment_url, "log_url": $log_url, "state": $state, "target_url": $target_url} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Get a deployment status
#
# GET /repos/{owner}/{repo}/deployments/{deployment_id}/statuses/{status_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-a-deployment-status — API method documentation
# operationId: repos/get-deployment-status
export def "repos-deployments-statuses get" [
  owner: string
  repo: string
  deployment_id: int
  status_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, deployment_url: string, description: string, environment: string, environment_url: string, id: int, log_url: string, node_id: string, performed_via_github_app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list<string>, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, pem: string, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string>, slug: string, updated_at: string, webhook_secret: string>, repository_url: string, state: string, target_url: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), deployment_id: (encode-path-segment $deployment_id), status_id: (encode-path-segment $status_id)} | format pattern "/repos/{owner}/{repo}/deployments/{deployment_id}/statuses/{status_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a repository dispatch event
#
# POST /repos/{owner}/{repo}/dispatches
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#create-a-repository-dispatch-event — API method documentation
# operationId: repos/create-dispatch-event
export def "repos-dispatches create-dispatch-event" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --client-payload: record # JSON payload with extra information about the webhook event that your action or worklow may use.
  event_type: string # A custom webhook event name.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/dispatches"))
  let req_body = {"client_payload": $client_payload, "event_type": $event_type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List repository events
#
# GET /repos/{owner}/{repo}/events
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#list-repository-events — API method documentation
# operationId: activity/list-repo-events
export def "repos-events list-activity" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<actor: record<avatar_url: string, display_login: string, gravatar_id: string, id: int, login: string, url: string>, created_at: string, id: string, org: record<avatar_url: string, display_login: string, gravatar_id: string, id: int, login: string, url: string>, payload: record<action: string, comment: record, issue: record, pages: list>, public: bool, repo: record<id: int, name: string, url: string>, type: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/events") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List forks
#
# GET /repos/{owner}/{repo}/forks
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-forks — API method documentation
# operationId: repos/list-forks
export def "repos-forks list" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --qp-sort: string@sort-completer-4 # The sort order. Can be either `newest`, `oldest`, or `stargazers`. (default: newest)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<allow_forking: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record<body: string, html_url: string, key: string, name: string, url: string>, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<key: string, name: string, node_id: string, spdx_id: string, url: string>, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "sort" $qp_sort "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/forks") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a fork
#
# POST /repos/{owner}/{repo}/forks
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#create-a-fork — API method documentation
# operationId: repos/create-fork
export def "repos-forks create" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --organization: string # Optional parameter to specify the organization name if forking into an organization.
]: any -> record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, anonymous_access_enabled: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record<html_url: string, key: string, name: string, url: string>, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, parent: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, source: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/forks"))
  let req_body = {"organization": $organization} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Create a blob
#
# POST /repos/{owner}/{repo}/git/blobs
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/git#create-a-blob — API method documentation
# operationId: git/create-blob
export def "repos-git-blobs create" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  content: string # The new blob's content.
  --encoding: string # The encoding used for `content`. Currently, `"utf-8"` and `"base64"` are supported. (default: utf-8)
]: any -> record<sha: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/git/blobs"))
  let req_body = {"content": $content, "encoding": $encoding} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Get a blob
#
# GET /repos/{owner}/{repo}/git/blobs/{file_sha}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/git#get-a-blob — API method documentation
# operationId: git/get-blob
export def "repos-git-blobs get" [
  owner: string
  repo: string
  file_sha: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<content: string, encoding: string, highlighted_content: string, node_id: string, sha: string, size: int, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), file_sha: (encode-path-segment $file_sha)} | format pattern "/repos/{owner}/{repo}/git/blobs/{file_sha}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a commit
#
# POST /repos/{owner}/{repo}/git/commits
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/git#create-a-commit — API method documentation
# operationId: git/create-commit
# --author shape: {date?: string, email: string, name: string}
# --committer shape: {date?: string, email?: string, name?: string}
export def "repos-git-commits create" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --author: record # Information about the author of the commit. By default, the `author` will be the authenticated user and the current date. See the `author` and `committer` object below for details. — shape: {date?: string, email: string, name: string}
  --committer: record # Information about the person who is making the commit. By default, `committer` will use the information set in `author`. See the `author` and `committer` object below for details. — shape: {date?: string, email?: string, name?: string}
  message: string # The commit message
  --parents: list<string> # The SHAs of the commits that were the parents of this commit. If omitted or empty, the commit will be written as a root commit. For a single parent, an array of one SHA should be provided; for a merge commit, an array of more than one should be provided.
  --signature: string # The [PGP signature](https://en.wikipedia.org/wiki/Pretty_Good_Privacy) of the commit. GitHub adds the signature to the `gpgsig` header of the created commit. For a commit signature to be verifiable by Git or GitHub, it must be an ASCII-armored detached PGP signature over the string commit as it would be written to the object database. To pass a `signature` parameter, you need to first manually create a valid PGP signature, which can be complicated. You may find it easier to [use the command line](https://git-scm.com/book/id/v2/Git-Tools-Signing-Your-Work) to create signed commits.
  tree: string # The SHA of the tree object this commit points to
]: any -> record<author: record<date: string, email: string, name: string>, committer: record<date: string, email: string, name: string>, html_url: string, message: string, node_id: string, parents: table<html_url: string, sha: string, url: string>, sha: string, tree: record<sha: string, url: string>, url: string, verification: record<payload: string, reason: string, signature: string, verified: bool>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/git/commits"))
  let req_body = {"author": $author, "committer": $committer, "message": $message, "parents": $parents, "signature": $signature, "tree": $tree} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Get a commit
#
# GET /repos/{owner}/{repo}/git/commits/{commit_sha}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/git#get-a-commit — API method documentation
# operationId: git/get-commit
export def "repos-git-commits get" [
  owner: string
  repo: string
  commit_sha: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<author: record<date: string, email: string, name: string>, committer: record<date: string, email: string, name: string>, html_url: string, message: string, node_id: string, parents: table<html_url: string, sha: string, url: string>, sha: string, tree: record<sha: string, url: string>, url: string, verification: record<payload: string, reason: string, signature: string, verified: bool>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), commit_sha: (encode-path-segment $commit_sha)} | format pattern "/repos/{owner}/{repo}/git/commits/{commit_sha}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List matching references
#
# GET /repos/{owner}/{repo}/git/matching-refs/{ref}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/git#list-matching-references — API method documentation
# operationId: git/list-matching-refs
export def "repos-git-matching-refs list" [
  owner: string
  repo: string
  ref: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<node_id: string, object: record<sha: string, type: string, url: string>, ref: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), ref: (encode-path-segment $ref)} | format pattern "/repos/{owner}/{repo}/git/matching-refs/{ref}") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a reference
#
# GET /repos/{owner}/{repo}/git/ref/{ref}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/git#get-a-reference — API method documentation
# operationId: git/get-ref
export def "repos-git-ref get" [
  owner: string
  repo: string
  ref: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<node_id: string, object: record<sha: string, type: string, url: string>, ref: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), ref: (encode-path-segment $ref)} | format pattern "/repos/{owner}/{repo}/git/ref/{ref}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a reference
#
# POST /repos/{owner}/{repo}/git/refs
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/git#create-a-reference — API method documentation
# operationId: git/create-ref
export def "repos-git-refs create" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --key: string # e.g. "refs/heads/newbranch"
  ref: string # The name of the fully qualified reference (ie: `refs/heads/master`). If it doesn't start with 'refs' and have at least two slashes, it will be rejected.
  sha: string # The SHA1 value for this reference.
]: any -> record<node_id: string, object: record<sha: string, type: string, url: string>, ref: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/git/refs"))
  let req_body = {"key": $key, "ref": $ref, "sha": $sha} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a reference
#
# DELETE /repos/{owner}/{repo}/git/refs/{ref}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/git#delete-a-reference — API method documentation
# operationId: git/delete-ref
export def "repos-git-refs delete" [
  owner: string
  repo: string
  ref: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), ref: (encode-path-segment $ref)} | format pattern "/repos/{owner}/{repo}/git/refs/{ref}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a reference
#
# PATCH /repos/{owner}/{repo}/git/refs/{ref}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/git#update-a-reference — API method documentation
# operationId: git/update-ref
export def "repos-git-refs update" [
  owner: string
  repo: string
  ref: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --force: oneof<nothing, bool> # Indicates whether to force the update or to make sure the update is a fast-forward update. Leaving this out or setting it to `false` will make sure you're not overwriting work. (default: false)
  sha: string # The SHA1 value to set this reference to
]: any -> record<node_id: string, object: record<sha: string, type: string, url: string>, ref: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), ref: (encode-path-segment $ref)} | format pattern "/repos/{owner}/{repo}/git/refs/{ref}"))
  let req_body = {"force": $force, "sha": $sha} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Create a tag object
#
# POST /repos/{owner}/{repo}/git/tags
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/git#create-a-tag-object — API method documentation
# operationId: git/create-tag
# --tagger shape: {date?: string, email: string, name: string}
export def "repos-git-tags create" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  message: string # The tag message.
  object: string # The SHA of the git object this is tagging.
  tag: string # The tag's name. This is typically a version (e.g., "v0.0.1").
  --tagger: record # An object with information about the individual creating the tag. — shape: {date?: string, email: string, name: string}
  type: string@type-completer-1 # The type of the object we're tagging. Normally this is a `commit` but it can also be a `tree` or a `blob`.
]: any -> record<message: string, node_id: string, object: record<sha: string, type: string, url: string>, sha: string, tag: string, tagger: record<date: string, email: string, name: string>, url: string, verification: record<payload: string, reason: string, signature: string, verified: bool>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/git/tags"))
  let req_body = {"message": $message, "object": $object, "tag": $tag, "tagger": $tagger, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Get a tag
#
# GET /repos/{owner}/{repo}/git/tags/{tag_sha}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/git#get-a-tag — API method documentation
# operationId: git/get-tag
export def "repos-git-tags get" [
  owner: string
  repo: string
  tag_sha: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<message: string, node_id: string, object: record<sha: string, type: string, url: string>, sha: string, tag: string, tagger: record<date: string, email: string, name: string>, url: string, verification: record<payload: string, reason: string, signature: string, verified: bool>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), tag_sha: (encode-path-segment $tag_sha)} | format pattern "/repos/{owner}/{repo}/git/tags/{tag_sha}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a tree
#
# POST /repos/{owner}/{repo}/git/trees
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/git#create-a-tree — API method documentation
# operationId: git/create-tree
# --tree item shape: {content?: string, mode?: "100644"|"100755"|"040000"|"160000"|"120000", path?: string, sha?: string, type?: "blob"|"tree"|"commit"}
export def "repos-git-trees create" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --base-tree: string # The SHA1 of an existing Git tree object which will be used as the base for the new tree. If provided, a new Git tree object will be created from entries in the Git tree object pointed to by `base_tree` and entries defined in the `tree` parameter. Entries defined in the `tree` parameter will overwrite items from `base_tree` with the same `path`. If you're creating new changes on a branch, then normally you'd set `base_tree` to the SHA1 of the Git tree object of the current latest commit on the branch you're working on. If not provided, GitHub will create a new Git tree object from only the entries defined in the `tree` parameter. If you create a new commit pointing to such a tree, then all files which were a part of the parent commit's tree and were not defined in the `tree` parameter will be listed as deleted by the new commit.
  tree: list # Objects (of `path`, `mode`, `type`, and `sha`) specifying a tree structure. — item shape: {content?: string, mode?: "100644"|"100755"|"040000"|"160000"|"120000", path?: string, sha?: string, type?: "blob"|"tree"|"commit"}
]: any -> record<sha: string, tree: table<mode: string, path: string, sha: string, size: int, type: string, url: string>, truncated: bool, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/git/trees"))
  let req_body = {"base_tree": $base_tree, "tree": $tree} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Get a tree
#
# GET /repos/{owner}/{repo}/git/trees/{tree_sha}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/git#get-a-tree — API method documentation
# operationId: git/get-tree
export def "repos-git-trees get" [
  owner: string
  repo: string
  tree_sha: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --recursive: string # Setting this parameter to any value returns the objects or subtrees referenced by the tree specified in `:tree_sha`. For example, setting `recursive` to any of the following will enable returning objects or subtrees: `0`, `1`, `"true"`, and `"false"`. Omit this parameter to prevent recursively returning objects or subtrees.
]: nothing -> record<sha: string, tree: table<mode: string, path: string, sha: string, size: int, type: string, url: string>, truncated: bool, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "recursive" $recursive "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), tree_sha: (encode-path-segment $tree_sha)} | format pattern "/repos/{owner}/{repo}/git/trees/{tree_sha}") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List repository webhooks
#
# GET /repos/{owner}/{repo}/hooks
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-repository-webhooks — API method documentation
# operationId: repos/list-webhooks
export def "repos-hooks list-webhooks" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<active: bool, config: record<content_type: string, digest: string, email: string, insecure_ssl: any, password: string, room: string, secret: string, subdomain: string, token: string, url: string>, created_at: string, events: list<string>, id: int, last_response: record<code: int, message: string, status: string>, name: string, ping_url: string, test_url: string, type: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/hooks") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a repository webhook
#
# POST /repos/{owner}/{repo}/hooks
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#create-a-repository-webhook — API method documentation
# operationId: repos/create-webhook
# --config shape: {content_type?: string, digest?: string, insecure_ssl?: any, secret?: string, token?: string, url?: string}
export def "repos-hooks create-webhook" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --active: oneof<nothing, bool> # Determines if notifications are sent when the webhook is triggered. Set to `true` to send notifications. (default: true)
  --config: record # Key/value pairs to provide settings for this webhook. [These are defined below](https://docs.github.com/enterprise-server@2.22/rest/reference/repos#create-hook-config-params). — shape: {content_type?: string, digest?: string, insecure_ssl?: any, secret?: string, token?: string, url?: string}
  --events: list<string> # Determines what [events](https://docs.github.com/enterprise-server@2.22/webhooks/event-payloads) the hook is triggered for. (default: [push])
  --name: string # Use `web` to create a webhook. Default: `web`. This parameter only accepts the value `web`.
]: any -> record<active: bool, config: record<content_type: string, digest: string, email: string, insecure_ssl: any, password: string, room: string, secret: string, subdomain: string, token: string, url: string>, created_at: string, events: list<string>, id: int, last_response: record<code: int, message: string, status: string>, name: string, ping_url: string, test_url: string, type: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/hooks"))
  let req_body = {"active": $active, "config": $config, "events": $events, "name": $name} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a repository webhook
#
# DELETE /repos/{owner}/{repo}/hooks/{hook_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#delete-a-repository-webhook — API method documentation
# operationId: repos/delete-webhook
export def "repos-hooks delete-webhook" [
  owner: string
  repo: string
  hook_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), hook_id: (encode-path-segment $hook_id)} | format pattern "/repos/{owner}/{repo}/hooks/{hook_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a repository webhook
#
# GET /repos/{owner}/{repo}/hooks/{hook_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-a-repository-webhook — API method documentation
# operationId: repos/get-webhook
export def "repos-hooks get-webhook" [
  owner: string
  repo: string
  hook_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<active: bool, config: record<content_type: string, digest: string, email: string, insecure_ssl: any, password: string, room: string, secret: string, subdomain: string, token: string, url: string>, created_at: string, events: list<string>, id: int, last_response: record<code: int, message: string, status: string>, name: string, ping_url: string, test_url: string, type: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), hook_id: (encode-path-segment $hook_id)} | format pattern "/repos/{owner}/{repo}/hooks/{hook_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a repository webhook
#
# PATCH /repos/{owner}/{repo}/hooks/{hook_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#update-a-repository-webhook — API method documentation
# operationId: repos/update-webhook
# --config shape: {address?: string, content_type?: string, insecure_ssl?: any, room?: string, secret?: string, url: string}
export def "repos-hooks update-webhook" [
  owner: string
  repo: string
  hook_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --active: oneof<nothing, bool> # Determines if notifications are sent when the webhook is triggered. Set to `true` to send notifications. (default: true)
  --add-events: list<string> # Determines a list of events to be added to the list of events that the Hook triggers for.
  --config: record # Key/value pairs to provide settings for this webhook. [These are defined below](https://docs.github.com/enterprise-server@2.22/rest/reference/repos#create-hook-config-params). — shape: {address?: string, content_type?: string, insecure_ssl?: any, room?: string, secret?: string, url: string}
  --events: list<string> # Determines what [events](https://docs.github.com/enterprise-server@2.22/webhooks/event-payloads) the hook is triggered for. This replaces the entire array of events. (default: [push])
  --remove-events: list<string> # Determines a list of events to be removed from the list of events that the Hook triggers for.
]: any -> record<active: bool, config: record<content_type: string, digest: string, email: string, insecure_ssl: any, password: string, room: string, secret: string, subdomain: string, token: string, url: string>, created_at: string, events: list<string>, id: int, last_response: record<code: int, message: string, status: string>, name: string, ping_url: string, test_url: string, type: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), hook_id: (encode-path-segment $hook_id)} | format pattern "/repos/{owner}/{repo}/hooks/{hook_id}"))
  let req_body = {"active": $active, "add_events": $add_events, "config": $config, "events": $events, "remove_events": $remove_events} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Ping a repository webhook
#
# POST /repos/{owner}/{repo}/hooks/{hook_id}/pings
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#ping-a-repository-webhook — API method documentation
# operationId: repos/ping-webhook
export def "repos-hooks-pings ping-webhook" [
  owner: string
  repo: string
  hook_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), hook_id: (encode-path-segment $hook_id)} | format pattern "/repos/{owner}/{repo}/hooks/{hook_id}/pings"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Test the push repository webhook
#
# POST /repos/{owner}/{repo}/hooks/{hook_id}/tests
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#test-the-push-repository-webhook — API method documentation
# operationId: repos/test-push-webhook
export def "repos-hooks-tests push-webhook" [
  owner: string
  repo: string
  hook_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), hook_id: (encode-path-segment $hook_id)} | format pattern "/repos/{owner}/{repo}/hooks/{hook_id}/tests"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a repository installation for the authenticated app
#
# GET /repos/{owner}/{repo}/installation
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps#get-a-repository-installation-for-the-authenticated-app — API method documentation
# operationId: apps/get-repo-installation
export def "repos-installation get-apps" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<access_tokens_url: string, account: any, app_id: int, app_slug: string, contact_email: string, created_at: string, events: list<string>, html_url: string, id: int, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string, organization_administration: string, pull_requests: string, statuses: string>, repositories_url: string, repository_selection: string, single_file_name: string, suspended_at: string, suspended_by: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, target_id: int, target_type: string, updated_at: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/installation"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List repository invitations
#
# GET /repos/{owner}/{repo}/invitations
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-repository-invitations — API method documentation
# operationId: repos/list-invitations
export def "repos-invitations list" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<created_at: string, expired: bool, html_url: string, id: int, invitee: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, inviter: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, node_id: string, permissions: string, repository: record<allow_forking: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/invitations") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a repository invitation
#
# DELETE /repos/{owner}/{repo}/invitations/{invitation_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#delete-a-repository-invitation — API method documentation
# operationId: repos/delete-invitation
export def "repos-invitations delete" [
  owner: string
  repo: string
  invitation_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), invitation_id: (encode-path-segment $invitation_id)} | format pattern "/repos/{owner}/{repo}/invitations/{invitation_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a repository invitation
#
# PATCH /repos/{owner}/{repo}/invitations/{invitation_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#update-a-repository-invitation — API method documentation
# operationId: repos/update-invitation
export def "repos-invitations update" [
  owner: string
  repo: string
  invitation_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --permissions: string@permissions-completer # The permissions that the associated user will have on the repository. Valid values are `read`, `write`, `maintain`, `triage`, and `admin`.
]: any -> record<created_at: string, expired: bool, html_url: string, id: int, invitee: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, inviter: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, node_id: string, permissions: string, repository: record<allow_forking: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record<body: string, html_url: string, key: string, name: string, url: string>, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<key: string, name: string, node_id: string, spdx_id: string, url: string>, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), invitation_id: (encode-path-segment $invitation_id)} | format pattern "/repos/{owner}/{repo}/invitations/{invitation_id}"))
  let req_body = {"permissions": $permissions} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List repository issues
#
# GET /repos/{owner}/{repo}/issues
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#list-repository-issues — API method documentation
# operationId: issues/list-for-repo
export def "repos-issues list" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --milestone: string # If an `integer` is passed, it should refer to a milestone by its `number` field. If the string `*` is passed, issues with any milestone are accepted. If the string `none` is passed, issues without milestones are returned.
  --state: string@state-completer # Indicates the state of the issues to return. Can be either `open`, `closed`, or `all`. (default: open)
  --assignee: string # Can be the name of a user. Pass in `none` for issues with no assigned user, and `*` for issues assigned to any user.
  --creator: string # The user that created the issue.
  --mentioned: string # A user that's mentioned in the issue.
  --labels: string # A list of comma separated label names. Example: `bug,ui,@high`
  --qp-sort: string@sort-completer-2 # What to sort results by. Can be either `created`, `updated`, `comments`. (default: created)
  --direction: string@direction-completer # One of `asc` (ascending) or `desc` (descending). (default: desc)
  --since: string # Only show notifications updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<active_lock_reason: string, assignee: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, assignees: list<record>, author_association: string, body: string, body_html: string, body_text: string, closed_at: string, closed_by: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, comments: int, comments_url: string, created_at: string, events_url: string, html_url: string, id: int, labels: list<any>, labels_url: string, locked: bool, milestone: record<closed_at: string, closed_issues: int, created_at: string, creator: record, description: string, due_on: string, html_url: string, id: int, labels_url: string, node_id: string, number: int, open_issues: int, state: string, title: string, updated_at: string, url: string>, node_id: string, number: int, performed_via_github_app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record, pem: string, permissions: record, slug: string, updated_at: string, webhook_secret: string>, pull_request: record<diff_url: string, html_url: string, merged_at: string, patch_url: string, url: string>, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, repository_url: string, state: string, timeline_url: string, title: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "milestone" $milestone "scalar") (serialize-qp "state" $state "scalar") (serialize-qp "assignee" $assignee "scalar") (serialize-qp "creator" $creator "scalar") (serialize-qp "mentioned" $mentioned "scalar") (serialize-qp "labels" $labels "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "direction" $direction "scalar") (serialize-qp "since" $since "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/issues") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an issue
#
# POST /repos/{owner}/{repo}/issues
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#create-an-issue — API method documentation
# operationId: issues/create
export def "repos-issues create" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --assignee: string # Login for the user that this issue should be assigned to. _NOTE: Only users with push access can set the assignee for new issues. The assignee is silently dropped otherwise. **This field is deprecated.**_ (nullable)
  --assignees: list<string> # Logins for Users to assign to this issue. _NOTE: Only users with push access can set assignees for new issues. Assignees are silently dropped otherwise._
  --body: string # The contents of the issue.
  --labels: list # Labels to associate with this issue. _NOTE: Only users with push access can set labels for new issues. Labels are silently dropped otherwise._
  --milestone: any # nullable
  title: any # The title of the issue.
]: any -> record<active_lock_reason: string, assignee: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, assignees: table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, author_association: string, body: string, body_html: string, body_text: string, closed_at: string, closed_by: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, comments: int, comments_url: string, created_at: string, events_url: string, html_url: string, id: int, labels: list<any>, labels_url: string, locked: bool, milestone: record<closed_at: string, closed_issues: int, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, description: string, due_on: string, html_url: string, id: int, labels_url: string, node_id: string, number: int, open_issues: int, state: string, title: string, updated_at: string, url: string>, node_id: string, number: int, performed_via_github_app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list<string>, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, pem: string, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string>, slug: string, updated_at: string, webhook_secret: string>, pull_request: record<diff_url: string, html_url: string, merged_at: string, patch_url: string, url: string>, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, repository_url: string, state: string, timeline_url: string, title: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/issues"))
  let req_body = {"assignee": $assignee, "assignees": $assignees, "body": $body, "labels": $labels, "milestone": $milestone, "title": $title} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List issue comments for a repository
#
# GET /repos/{owner}/{repo}/issues/comments
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#list-issue-comments-for-a-repository — API method documentation
# operationId: issues/list-comments-for-repo
export def "repos-issues-comments list" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --qp-sort: string@sort-completer-5 # One of `created` (when the repository was starred) or `updated` (when it was last pushed to). (default: created)
  --direction: string@direction-completer # Either `asc` or `desc`. Ignored without the `sort` parameter.
  --since: string # Only show notifications updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<author_association: string, body: string, body_html: string, body_text: string, created_at: string, html_url: string, id: int, issue_url: string, node_id: string, performed_via_github_app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record, pem: string, permissions: record, slug: string, updated_at: string, webhook_secret: string>, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "sort" $qp_sort "scalar") (serialize-qp "direction" $direction "scalar") (serialize-qp "since" $since "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/issues/comments") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an issue comment
#
# DELETE /repos/{owner}/{repo}/issues/comments/{comment_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#delete-an-issue-comment — API method documentation
# operationId: issues/delete-comment
export def "repos-issues-comments delete" [
  owner: string
  repo: string
  comment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), comment_id: (encode-path-segment $comment_id)} | format pattern "/repos/{owner}/{repo}/issues/comments/{comment_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get an issue comment
#
# GET /repos/{owner}/{repo}/issues/comments/{comment_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#get-an-issue-comment — API method documentation
# operationId: issues/get-comment
export def "repos-issues-comments get" [
  owner: string
  repo: string
  comment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<author_association: string, body: string, body_html: string, body_text: string, created_at: string, html_url: string, id: int, issue_url: string, node_id: string, performed_via_github_app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list<string>, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, pem: string, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string>, slug: string, updated_at: string, webhook_secret: string>, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), comment_id: (encode-path-segment $comment_id)} | format pattern "/repos/{owner}/{repo}/issues/comments/{comment_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update an issue comment
#
# PATCH /repos/{owner}/{repo}/issues/comments/{comment_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#update-an-issue-comment — API method documentation
# operationId: issues/update-comment
export def "repos-issues-comments update" [
  owner: string
  repo: string
  comment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  body: string # The contents of the comment.
]: any -> record<author_association: string, body: string, body_html: string, body_text: string, created_at: string, html_url: string, id: int, issue_url: string, node_id: string, performed_via_github_app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list<string>, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, pem: string, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string>, slug: string, updated_at: string, webhook_secret: string>, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), comment_id: (encode-path-segment $comment_id)} | format pattern "/repos/{owner}/{repo}/issues/comments/{comment_id}"))
  let req_body = {"body": $body} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List reactions for an issue comment
#
# GET /repos/{owner}/{repo}/issues/comments/{comment_id}/reactions
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#list-reactions-for-an-issue-comment — API method documentation
# operationId: reactions/list-for-issue-comment
export def "repos-issues-comments-reactions list" [
  owner: string
  repo: string
  comment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --content: string@content-completer # Returns a single [reaction type](https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#reaction-types). Omit this parameter to list all reactions to an issue comment.
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<content: string, created_at: string, id: int, node_id: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "content" $content "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), comment_id: (encode-path-segment $comment_id)} | format pattern "/repos/{owner}/{repo}/issues/comments/{comment_id}/reactions") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create reaction for an issue comment
#
# POST /repos/{owner}/{repo}/issues/comments/{comment_id}/reactions
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#create-reaction-for-an-issue-comment — API method documentation
# operationId: reactions/create-for-issue-comment
export def "repos-issues-comments-reactions create" [
  owner: string
  repo: string
  comment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  content: string@content-completer # The [reaction type](https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#reaction-types) to add to the issue comment.
]: any -> record<content: string, created_at: string, id: int, node_id: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), comment_id: (encode-path-segment $comment_id)} | format pattern "/repos/{owner}/{repo}/issues/comments/{comment_id}/reactions"))
  let req_body = {"content": $content} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete an issue comment reaction
#
# DELETE /repos/{owner}/{repo}/issues/comments/{comment_id}/reactions/{reaction_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#delete-an-issue-comment-reaction — API method documentation
# operationId: reactions/delete-for-issue-comment
export def "repos-issues-comments-reactions delete" [
  owner: string
  repo: string
  comment_id: int
  reaction_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), comment_id: (encode-path-segment $comment_id), reaction_id: (encode-path-segment $reaction_id)} | format pattern "/repos/{owner}/{repo}/issues/comments/{comment_id}/reactions/{reaction_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List issue events for a repository
#
# GET /repos/{owner}/{repo}/issues/events
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#list-issue-events-for-a-repository — API method documentation
# operationId: issues/list-events-for-repo
export def "repos-issues-events list" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<actor: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, assignee: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, assigner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, author_association: string, commit_id: string, commit_url: string, created_at: string, dismissed_review: record<dismissal_commit_id: string, dismissal_message: string, review_id: int, state: string>, event: string, id: int, issue: record<active_lock_reason: string, assignee: record, assignees: list, author_association: string, body: string, body_html: string, body_text: string, closed_at: string, closed_by: record, comments: int, comments_url: string, created_at: string, events_url: string, html_url: string, id: int, labels: list, labels_url: string, locked: bool, milestone: record, node_id: string, number: int, performed_via_github_app: record, pull_request: record, reactions: record, repository: record, repository_url: string, state: string, timeline_url: string, title: string, updated_at: string, url: string, user: record>, label: record<color: string, name: string>, lock_reason: string, milestone: record<title: string>, node_id: string, performed_via_github_app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record, pem: string, permissions: record, slug: string, updated_at: string, webhook_secret: string>, project_card: record<column_name: string, id: int, previous_column_name: string, project_id: int, project_url: string, url: string>, rename: record<from: string, to: string>, requested_reviewer: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, requested_team: record<description: string, html_url: string, id: int, members_url: string, name: string, node_id: string, parent: record, permission: string, permissions: record, privacy: string, repositories_url: string, slug: string, url: string>, review_requester: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/issues/events") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get an issue event
#
# GET /repos/{owner}/{repo}/issues/events/{event_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#get-an-issue-event — API method documentation
# operationId: issues/get-event
export def "repos-issues-events get" [
  owner: string
  repo: string
  event_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<actor: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, assignee: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, assigner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, author_association: string, commit_id: string, commit_url: string, created_at: string, dismissed_review: record<dismissal_commit_id: string, dismissal_message: string, review_id: int, state: string>, event: string, id: int, issue: record<active_lock_reason: string, assignee: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, assignees: list<record>, author_association: string, body: string, body_html: string, body_text: string, closed_at: string, closed_by: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, comments: int, comments_url: string, created_at: string, events_url: string, html_url: string, id: int, labels: list<any>, labels_url: string, locked: bool, milestone: record<closed_at: string, closed_issues: int, created_at: string, creator: record, description: string, due_on: string, html_url: string, id: int, labels_url: string, node_id: string, number: int, open_issues: int, state: string, title: string, updated_at: string, url: string>, node_id: string, number: int, performed_via_github_app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record, pem: string, permissions: record, slug: string, updated_at: string, webhook_secret: string>, pull_request: record<diff_url: string, html_url: string, merged_at: string, patch_url: string, url: string>, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, repository_url: string, state: string, timeline_url: string, title: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>>, label: record<color: string, name: string>, lock_reason: string, milestone: record<title: string>, node_id: string, performed_via_github_app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list<string>, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, pem: string, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string>, slug: string, updated_at: string, webhook_secret: string>, project_card: record<column_name: string, id: int, previous_column_name: string, project_id: int, project_url: string, url: string>, rename: record<from: string, to: string>, requested_reviewer: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, requested_team: record<description: string, html_url: string, id: int, members_url: string, name: string, node_id: string, parent: record<description: string, html_url: string, id: int, ldap_dn: string, members_url: string, name: string, node_id: string, permission: string, privacy: string, repositories_url: string, slug: string, url: string>, permission: string, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, privacy: string, repositories_url: string, slug: string, url: string>, review_requester: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), event_id: (encode-path-segment $event_id)} | format pattern "/repos/{owner}/{repo}/issues/events/{event_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get an issue
#
# GET /repos/{owner}/{repo}/issues/{issue_number}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#get-an-issue — API method documentation
# operationId: issues/get
export def "repos-issues get" [
  owner: string
  repo: string
  issue_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<active_lock_reason: string, assignee: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, assignees: table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, author_association: string, body: string, body_html: string, body_text: string, closed_at: string, closed_by: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, comments: int, comments_url: string, created_at: string, events_url: string, html_url: string, id: int, labels: list<any>, labels_url: string, locked: bool, milestone: record<closed_at: string, closed_issues: int, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, description: string, due_on: string, html_url: string, id: int, labels_url: string, node_id: string, number: int, open_issues: int, state: string, title: string, updated_at: string, url: string>, node_id: string, number: int, performed_via_github_app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list<string>, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, pem: string, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string>, slug: string, updated_at: string, webhook_secret: string>, pull_request: record<diff_url: string, html_url: string, merged_at: string, patch_url: string, url: string>, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, repository_url: string, state: string, timeline_url: string, title: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), issue_number: (encode-path-segment $issue_number)} | format pattern "/repos/{owner}/{repo}/issues/{issue_number}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update an issue
#
# PATCH /repos/{owner}/{repo}/issues/{issue_number}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues/#update-an-issue — API method documentation
# operationId: issues/update
export def "repos-issues update" [
  owner: string
  repo: string
  issue_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --assignee: string # Login for the user that this issue should be assigned to. **This field is deprecated.** (nullable)
  --assignees: list<string> # Logins for Users to assign to this issue. Pass one or more user logins to _replace_ the set of assignees on this Issue. Send an empty array (`[]`) to clear all assignees from the Issue. _NOTE: Only users with push access can set assignees for new issues. Assignees are silently dropped otherwise._
  --body: string # The contents of the issue. (nullable)
  --labels: list # Labels to associate with this issue. Pass one or more Labels to _replace_ the set of Labels on this Issue. Send an empty array (`[]`) to clear all Labels from the Issue. _NOTE: Only users with push access can set labels for issues. Labels are silently dropped otherwise._
  --milestone: any # nullable
  --state: string@state-completer-4 # State of the issue. Either `open` or `closed`.
  --title: any # The title of the issue. (nullable)
]: any -> record<active_lock_reason: string, assignee: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, assignees: table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, author_association: string, body: string, body_html: string, body_text: string, closed_at: string, closed_by: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, comments: int, comments_url: string, created_at: string, events_url: string, html_url: string, id: int, labels: list<any>, labels_url: string, locked: bool, milestone: record<closed_at: string, closed_issues: int, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, description: string, due_on: string, html_url: string, id: int, labels_url: string, node_id: string, number: int, open_issues: int, state: string, title: string, updated_at: string, url: string>, node_id: string, number: int, performed_via_github_app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list<string>, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, pem: string, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string>, slug: string, updated_at: string, webhook_secret: string>, pull_request: record<diff_url: string, html_url: string, merged_at: string, patch_url: string, url: string>, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, repository_url: string, state: string, timeline_url: string, title: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), issue_number: (encode-path-segment $issue_number)} | format pattern "/repos/{owner}/{repo}/issues/{issue_number}"))
  let req_body = {"assignee": $assignee, "assignees": $assignees, "body": $body, "labels": $labels, "milestone": $milestone, "state": $state, "title": $title} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Remove assignees from an issue
#
# DELETE /repos/{owner}/{repo}/issues/{issue_number}/assignees
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#remove-assignees-from-an-issue — API method documentation
# operationId: issues/remove-assignees
export def "repos-issues-assignees delete" [
  owner: string
  repo: string
  issue_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --assignees: list<string> # Usernames of assignees to remove from an issue. _NOTE: Only users with push access can remove assignees from an issue. Assignees are silently ignored otherwise._
]: any -> record<active_lock_reason: string, assignee: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, assignees: table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, author_association: string, body: string, body_html: string, body_text: string, closed_at: string, closed_by: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, comments: int, comments_url: string, created_at: string, events_url: string, html_url: string, id: int, labels: list<any>, labels_url: string, locked: bool, milestone: record<closed_at: string, closed_issues: int, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, description: string, due_on: string, html_url: string, id: int, labels_url: string, node_id: string, number: int, open_issues: int, state: string, title: string, updated_at: string, url: string>, node_id: string, number: int, performed_via_github_app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list<string>, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, pem: string, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string>, slug: string, updated_at: string, webhook_secret: string>, pull_request: record<diff_url: string, html_url: string, merged_at: string, patch_url: string, url: string>, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, repository_url: string, state: string, timeline_url: string, title: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), issue_number: (encode-path-segment $issue_number)} | format pattern "/repos/{owner}/{repo}/issues/{issue_number}/assignees"))
  let req_body = {"assignees": $assignees} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Add assignees to an issue
#
# POST /repos/{owner}/{repo}/issues/{issue_number}/assignees
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#add-assignees-to-an-issue — API method documentation
# operationId: issues/add-assignees
export def "repos-issues-assignees create" [
  owner: string
  repo: string
  issue_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --assignees: list<string> # Usernames of people to assign this issue to. _NOTE: Only users with push access can add assignees to an issue. Assignees are silently ignored otherwise._
]: any -> record<active_lock_reason: string, assignee: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, assignees: table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, author_association: string, body: string, body_html: string, body_text: string, closed_at: string, closed_by: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, comments: int, comments_url: string, created_at: string, events_url: string, html_url: string, id: int, labels: list<any>, labels_url: string, locked: bool, milestone: record<closed_at: string, closed_issues: int, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, description: string, due_on: string, html_url: string, id: int, labels_url: string, node_id: string, number: int, open_issues: int, state: string, title: string, updated_at: string, url: string>, node_id: string, number: int, performed_via_github_app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list<string>, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, pem: string, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string>, slug: string, updated_at: string, webhook_secret: string>, pull_request: record<diff_url: string, html_url: string, merged_at: string, patch_url: string, url: string>, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, repository_url: string, state: string, timeline_url: string, title: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), issue_number: (encode-path-segment $issue_number)} | format pattern "/repos/{owner}/{repo}/issues/{issue_number}/assignees"))
  let req_body = {"assignees": $assignees} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List issue comments
#
# GET /repos/{owner}/{repo}/issues/{issue_number}/comments
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#list-issue-comments — API method documentation
# operationId: issues/list-comments
export def "repos-issues-comments list-1" [
  owner: string
  repo: string
  issue_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --since: string # Only show notifications updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<author_association: string, body: string, body_html: string, body_text: string, created_at: string, html_url: string, id: int, issue_url: string, node_id: string, performed_via_github_app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record, pem: string, permissions: record, slug: string, updated_at: string, webhook_secret: string>, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "since" $since "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), issue_number: (encode-path-segment $issue_number)} | format pattern "/repos/{owner}/{repo}/issues/{issue_number}/comments") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an issue comment
#
# POST /repos/{owner}/{repo}/issues/{issue_number}/comments
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#create-an-issue-comment — API method documentation
# operationId: issues/create-comment
export def "repos-issues-comments create" [
  owner: string
  repo: string
  issue_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  body: string # The contents of the comment.
]: any -> record<author_association: string, body: string, body_html: string, body_text: string, created_at: string, html_url: string, id: int, issue_url: string, node_id: string, performed_via_github_app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list<string>, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, pem: string, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string>, slug: string, updated_at: string, webhook_secret: string>, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), issue_number: (encode-path-segment $issue_number)} | format pattern "/repos/{owner}/{repo}/issues/{issue_number}/comments"))
  let req_body = {"body": $body} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List issue events
#
# GET /repos/{owner}/{repo}/issues/{issue_number}/events
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#list-issue-events — API method documentation
# operationId: issues/list-events
export def "repos-issues-events list-1" [
  owner: string
  repo: string
  issue_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> list<any> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), issue_number: (encode-path-segment $issue_number)} | format pattern "/repos/{owner}/{repo}/issues/{issue_number}/events") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove all labels from an issue
#
# DELETE /repos/{owner}/{repo}/issues/{issue_number}/labels
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#remove-all-labels-from-an-issue — API method documentation
# operationId: issues/remove-all-labels
export def "repos-issues-labels delete-list" [
  owner: string
  repo: string
  issue_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), issue_number: (encode-path-segment $issue_number)} | format pattern "/repos/{owner}/{repo}/issues/{issue_number}/labels"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List labels for an issue
#
# GET /repos/{owner}/{repo}/issues/{issue_number}/labels
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#list-labels-for-an-issue — API method documentation
# operationId: issues/list-labels-on-issue
export def "repos-issues-labels list" [
  owner: string
  repo: string
  issue_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<color: string, default: bool, description: string, id: int, name: string, node_id: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), issue_number: (encode-path-segment $issue_number)} | format pattern "/repos/{owner}/{repo}/issues/{issue_number}/labels") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add labels to an issue
#
# POST /repos/{owner}/{repo}/issues/{issue_number}/labels
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#add-labels-to-an-issue — API method documentation
# operationId: issues/add-labels
export def "repos-issues-labels create" [
  owner: string
  repo: string
  issue_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --labels: list<string> # The names of the labels to add to the issue. You can pass an empty array to remove all labels. **Note:** Alternatively, you can pass a single label as a `string` or an `array` of labels directly, but GitHub recommends passing an object with the `labels` key.
]: any -> table<color: string, default: bool, description: string, id: int, name: string, node_id: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), issue_number: (encode-path-segment $issue_number)} | format pattern "/repos/{owner}/{repo}/issues/{issue_number}/labels"))
  let req_body = {"labels": $labels} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Set labels for an issue
#
# PUT /repos/{owner}/{repo}/issues/{issue_number}/labels
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#set-labels-for-an-issue — API method documentation
# operationId: issues/set-labels
export def "repos-issues-labels update" [
  owner: string
  repo: string
  issue_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --labels: list<string> # The names of the labels to add to the issue. You can pass an empty array to remove all labels. **Note:** Alternatively, you can pass a single label as a `string` or an `array` of labels directly, but GitHub recommends passing an object with the `labels` key.
]: any -> table<color: string, default: bool, description: string, id: int, name: string, node_id: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), issue_number: (encode-path-segment $issue_number)} | format pattern "/repos/{owner}/{repo}/issues/{issue_number}/labels"))
  let req_body = {"labels": $labels} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Remove a label from an issue
#
# DELETE /repos/{owner}/{repo}/issues/{issue_number}/labels/{name}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#remove-a-label-from-an-issue — API method documentation
# operationId: issues/remove-label
export def "repos-issues-labels delete" [
  owner: string
  repo: string
  issue_number: int
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> table<color: string, default: bool, description: string, id: int, name: string, node_id: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), issue_number: (encode-path-segment $issue_number), name: (encode-path-segment $name)} | format pattern "/repos/{owner}/{repo}/issues/{issue_number}/labels/{name}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Unlock an issue
#
# DELETE /repos/{owner}/{repo}/issues/{issue_number}/lock
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#unlock-an-issue — API method documentation
# operationId: issues/unlock
export def "repos-issues-lock unlock" [
  owner: string
  repo: string
  issue_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), issue_number: (encode-path-segment $issue_number)} | format pattern "/repos/{owner}/{repo}/issues/{issue_number}/lock"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Lock an issue
#
# PUT /repos/{owner}/{repo}/issues/{issue_number}/lock
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#lock-an-issue — API method documentation
# operationId: issues/lock
export def "repos-issues-lock lock" [
  owner: string
  repo: string
  issue_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --lock-reason: string@lock-reason-completer # The reason for locking the issue or pull request conversation. Lock will fail if you don't use one of these reasons: \* `off-topic` \* `too heated` \* `resolved` \* `spam`
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), issue_number: (encode-path-segment $issue_number)} | format pattern "/repos/{owner}/{repo}/issues/{issue_number}/lock"))
  let req_body = {"lock_reason": $lock_reason} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List reactions for an issue
#
# GET /repos/{owner}/{repo}/issues/{issue_number}/reactions
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#list-reactions-for-an-issue — API method documentation
# operationId: reactions/list-for-issue
export def "repos-issues-reactions list" [
  owner: string
  repo: string
  issue_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --content: string@content-completer # Returns a single [reaction type](https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#reaction-types). Omit this parameter to list all reactions to an issue.
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<content: string, created_at: string, id: int, node_id: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "content" $content "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), issue_number: (encode-path-segment $issue_number)} | format pattern "/repos/{owner}/{repo}/issues/{issue_number}/reactions") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create reaction for an issue
#
# POST /repos/{owner}/{repo}/issues/{issue_number}/reactions
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#create-reaction-for-an-issue — API method documentation
# operationId: reactions/create-for-issue
export def "repos-issues-reactions create" [
  owner: string
  repo: string
  issue_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  content: string@content-completer # The [reaction type](https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#reaction-types) to add to the issue.
]: any -> record<content: string, created_at: string, id: int, node_id: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), issue_number: (encode-path-segment $issue_number)} | format pattern "/repos/{owner}/{repo}/issues/{issue_number}/reactions"))
  let req_body = {"content": $content} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete an issue reaction
#
# DELETE /repos/{owner}/{repo}/issues/{issue_number}/reactions/{reaction_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#delete-an-issue-reaction — API method documentation
# operationId: reactions/delete-for-issue
export def "repos-issues-reactions delete" [
  owner: string
  repo: string
  issue_number: int
  reaction_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), issue_number: (encode-path-segment $issue_number), reaction_id: (encode-path-segment $reaction_id)} | format pattern "/repos/{owner}/{repo}/issues/{issue_number}/reactions/{reaction_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List timeline events for an issue
#
# GET /repos/{owner}/{repo}/issues/{issue_number}/timeline
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#list-timeline-events-for-an-issue — API method documentation
# operationId: issues/list-events-for-timeline
export def "repos-issues-timeline list-events" [
  owner: string
  repo: string
  issue_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> list<record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), issue_number: (encode-path-segment $issue_number)} | format pattern "/repos/{owner}/{repo}/issues/{issue_number}/timeline") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List deploy keys
#
# GET /repos/{owner}/{repo}/keys
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-deploy-keys — API method documentation
# operationId: repos/list-deploy-keys
export def "repos-keys list-deploy" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<created_at: string, id: int, key: string, read_only: bool, title: string, url: string, verified: bool> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/keys") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a deploy key
#
# POST /repos/{owner}/{repo}/keys
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#create-a-deploy-key — API method documentation
# operationId: repos/create-deploy-key
export def "repos-keys create-deploy" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  key: string # The contents of the key.
  --read-only: oneof<nothing, bool> # If `true`, the key will only be able to read repository contents. Otherwise, the key will be able to read and write. Deploy keys with write access can perform the same actions as an organization member with admin access, or a collaborator on a personal repository. For more information, see "[Repository permission levels for an organization](https://help.github.com/articles/repository-permission-levels-for-an-organization/)" and "[Permission levels for a user account repository](https://help.github.com/articles/permission-levels-for-a-user-account-repository/)."
  --title: string # A name for the key.
]: any -> record<created_at: string, id: int, key: string, read_only: bool, title: string, url: string, verified: bool> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/keys"))
  let req_body = {"key": $key, "read_only": $read_only, "title": $title} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a deploy key
#
# DELETE /repos/{owner}/{repo}/keys/{key_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#delete-a-deploy-key — API method documentation
# operationId: repos/delete-deploy-key
export def "repos-keys delete-deploy" [
  owner: string
  repo: string
  key_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), key_id: (encode-path-segment $key_id)} | format pattern "/repos/{owner}/{repo}/keys/{key_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a deploy key
#
# GET /repos/{owner}/{repo}/keys/{key_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-a-deploy-key — API method documentation
# operationId: repos/get-deploy-key
export def "repos-keys get-deploy" [
  owner: string
  repo: string
  key_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created_at: string, id: int, key: string, read_only: bool, title: string, url: string, verified: bool> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), key_id: (encode-path-segment $key_id)} | format pattern "/repos/{owner}/{repo}/keys/{key_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List labels for a repository
#
# GET /repos/{owner}/{repo}/labels
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#list-labels-for-a-repository — API method documentation
# operationId: issues/list-labels-for-repo
export def "repos-labels list-issues" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<color: string, default: bool, description: string, id: int, name: string, node_id: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/labels") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a label
#
# POST /repos/{owner}/{repo}/labels
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#create-a-label — API method documentation
# operationId: issues/create-label
export def "repos-labels create-issues" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string # The [hexadecimal color code](http://www.color-hex.com/) for the label, without the leading `#`.
  --description: string # A short description of the label.
  name: string # The name of the label. Emoji can be added to label names, using either native emoji or colon-style markup. For example, typing `:strawberry:` will render the emoji ![:strawberry:](https://github.githubassets.com/images/icons/emoji/unicode/1f353.png ":strawberry:"). For a full list of available emoji and codes, see "[Emoji cheat sheet](https://github.com/ikatyang/emoji-cheat-sheet)."
]: any -> record<color: string, default: bool, description: string, id: int, name: string, node_id: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/labels"))
  let req_body = {"color": $color, "description": $description, "name": $name} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a label
#
# DELETE /repos/{owner}/{repo}/labels/{name}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#delete-a-label — API method documentation
# operationId: issues/delete-label
export def "repos-labels delete-issues" [
  owner: string
  repo: string
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), name: (encode-path-segment $name)} | format pattern "/repos/{owner}/{repo}/labels/{name}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a label
#
# GET /repos/{owner}/{repo}/labels/{name}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#get-a-label — API method documentation
# operationId: issues/get-label
export def "repos-labels get-issues" [
  owner: string
  repo: string
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<color: string, default: bool, description: string, id: int, name: string, node_id: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), name: (encode-path-segment $name)} | format pattern "/repos/{owner}/{repo}/labels/{name}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a label
#
# PATCH /repos/{owner}/{repo}/labels/{name}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#update-a-label — API method documentation
# operationId: issues/update-label
export def "repos-labels update-issues" [
  owner: string
  repo: string
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string # The [hexadecimal color code](http://www.color-hex.com/) for the label, without the leading `#`.
  --description: string # A short description of the label.
  --new-name: string # The new name of the label. Emoji can be added to label names, using either native emoji or colon-style markup. For example, typing `:strawberry:` will render the emoji ![:strawberry:](https://github.githubassets.com/images/icons/emoji/unicode/1f353.png ":strawberry:"). For a full list of available emoji and codes, see "[Emoji cheat sheet](https://github.com/ikatyang/emoji-cheat-sheet)."
]: any -> record<color: string, default: bool, description: string, id: int, name: string, node_id: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), name: (encode-path-segment $name)} | format pattern "/repos/{owner}/{repo}/labels/{name}"))
  let req_body = {"color": $color, "description": $description, "new_name": $new_name} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List repository languages
#
# GET /repos/{owner}/{repo}/languages
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-repository-languages — API method documentation
# operationId: repos/list-languages
export def "repos-languages list" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/languages"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the license for a repository
#
# GET /repos/{owner}/{repo}/license
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/licenses/#get-the-license-for-a-repository — API method documentation
# operationId: licenses/get-for-repo
export def "repos-license get" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_links: record<git: string, html: string, self: string>, content: string, download_url: string, encoding: string, git_url: string, html_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, name: string, path: string, sha: string, size: int, type: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/license"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Merge a branch
#
# POST /repos/{owner}/{repo}/merges
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#merge-a-branch — API method documentation
# operationId: repos/merge
export def "repos-merges create" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --body-base: string # The name of the base branch that the head will be merged into.
  --commit-message: string # Commit message to use for the merge commit. If omitted, a default message will be used.
  head: string # The head to merge. This can be a branch name or a commit SHA1.
]: any -> record<author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, comments_url: string, commit: record<author: record<date: string, email: string, name: string>, comment_count: int, committer: record<date: string, email: string, name: string>, message: string, tree: record<sha: string, url: string>, url: string, verification: record<payload: string, reason: string, signature: string, verified: bool>>, committer: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, files: table<additions: int, blob_url: string, changes: int, contents_url: string, deletions: int, filename: string, patch: string, previous_filename: string, raw_url: string, sha: string, status: string>, html_url: string, node_id: string, parents: table<html_url: string, sha: string, url: string>, sha: string, stats: record<additions: int, deletions: int, total: int>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/merges"))
  let req_body = {"base": $body_base, "commit_message": $commit_message, "head": $head} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List milestones
#
# GET /repos/{owner}/{repo}/milestones
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#list-milestones — API method documentation
# operationId: issues/list-milestones
export def "repos-milestones list-issues" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --state: string@state-completer # The state of the milestone. Either `open`, `closed`, or `all`. (default: open)
  --qp-sort: string@sort-completer-6 # What to sort results by. Either `due_on` or `completeness`. (default: due_on)
  --direction: string@direction-completer # The direction of the sort. Either `asc` or `desc`. (default: asc)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<closed_at: string, closed_issues: int, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, description: string, due_on: string, html_url: string, id: int, labels_url: string, node_id: string, number: int, open_issues: int, state: string, title: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "state" $state "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "direction" $direction "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/milestones") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a milestone
#
# POST /repos/{owner}/{repo}/milestones
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#create-a-milestone — API method documentation
# operationId: issues/create-milestone
export def "repos-milestones create-issues" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string # A description of the milestone.
  --due-on: string # The milestone due date. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --state: string@state-completer-4 # The state of the milestone. Either `open` or `closed`. (default: open)
  title: string # The title of the milestone.
]: any -> record<closed_at: string, closed_issues: int, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, description: string, due_on: string, html_url: string, id: int, labels_url: string, node_id: string, number: int, open_issues: int, state: string, title: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/milestones"))
  let req_body = {"description": $description, "due_on": $due_on, "state": $state, "title": $title} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a milestone
#
# DELETE /repos/{owner}/{repo}/milestones/{milestone_number}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#delete-a-milestone — API method documentation
# operationId: issues/delete-milestone
export def "repos-milestones delete-issues" [
  owner: string
  repo: string
  milestone_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), milestone_number: (encode-path-segment $milestone_number)} | format pattern "/repos/{owner}/{repo}/milestones/{milestone_number}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a milestone
#
# GET /repos/{owner}/{repo}/milestones/{milestone_number}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#get-a-milestone — API method documentation
# operationId: issues/get-milestone
export def "repos-milestones get-issues" [
  owner: string
  repo: string
  milestone_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<closed_at: string, closed_issues: int, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, description: string, due_on: string, html_url: string, id: int, labels_url: string, node_id: string, number: int, open_issues: int, state: string, title: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), milestone_number: (encode-path-segment $milestone_number)} | format pattern "/repos/{owner}/{repo}/milestones/{milestone_number}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a milestone
#
# PATCH /repos/{owner}/{repo}/milestones/{milestone_number}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#update-a-milestone — API method documentation
# operationId: issues/update-milestone
export def "repos-milestones update-issues" [
  owner: string
  repo: string
  milestone_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string # A description of the milestone.
  --due-on: string # The milestone due date. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --state: string@state-completer-4 # The state of the milestone. Either `open` or `closed`. (default: open)
  --title: string # The title of the milestone.
]: any -> record<closed_at: string, closed_issues: int, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, description: string, due_on: string, html_url: string, id: int, labels_url: string, node_id: string, number: int, open_issues: int, state: string, title: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), milestone_number: (encode-path-segment $milestone_number)} | format pattern "/repos/{owner}/{repo}/milestones/{milestone_number}"))
  let req_body = {"description": $description, "due_on": $due_on, "state": $state, "title": $title} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List labels for issues in a milestone
#
# GET /repos/{owner}/{repo}/milestones/{milestone_number}/labels
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#list-labels-for-issues-in-a-milestone — API method documentation
# operationId: issues/list-labels-for-milestone
export def "repos-milestones-labels list-issues" [
  owner: string
  repo: string
  milestone_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<color: string, default: bool, description: string, id: int, name: string, node_id: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), milestone_number: (encode-path-segment $milestone_number)} | format pattern "/repos/{owner}/{repo}/milestones/{milestone_number}/labels") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List repository notifications for the authenticated user
#
# GET /repos/{owner}/{repo}/notifications
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#list-repository-notifications-for-the-authenticated-user — API method documentation
# operationId: activity/list-repo-notifications-for-authenticated-user
export def "repos-notifications list-activity-for-authenticated-user" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --all: oneof<nothing, bool> # If `true`, show notifications marked as read. (default: false)
  --participating: oneof<nothing, bool> # If `true`, only shows notifications in which the user is directly participating or mentioned. (default: false)
  --since: string # Only show notifications updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --before: string # Only show notifications updated before the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<id: string, last_read_at: string, reason: string, repository: record<allow_forking: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, subject: record<latest_comment_url: string, title: string, type: string, url: string>, subscription_url: string, unread: bool, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "all" $all "scalar") (serialize-qp "participating" $participating "scalar") (serialize-qp "since" $since "scalar") (serialize-qp "before" $before "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/notifications") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Mark repository notifications as read
#
# PUT /repos/{owner}/{repo}/notifications
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#mark-repository-notifications-as-read — API method documentation
# operationId: activity/mark-repo-notifications-as-read
export def "repos-notifications get-activity-mark-as" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --last-read-at: string # Describes the last point that notifications were checked. Anything updated since this time will not be marked as read. If you omit this parameter, all notifications are marked as read. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. Default: The current timestamp. (format: date-time)
]: any -> record<message: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/notifications"))
  let req_body = {"last_read_at": $last_read_at} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a GitHub Enterprise Server Pages site
#
# DELETE /repos/{owner}/{repo}/pages
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#delete-a-github-pages-site — API method documentation
# operationId: repos/delete-pages-site
export def "repos-pages delete-site" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/pages"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a GitHub Enterprise Server Pages site
#
# GET /repos/{owner}/{repo}/pages
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-a-github-pages-site — API method documentation
# operationId: repos/get-pages
export def "repos-pages get" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<cname: string, custom_404: bool, html_url: string, https_certificate: record<description: string, domains: list<any>, expires_at: string, state: string>, https_enforced: bool, pending_domain_unverified_at: string, protected_domain_state: string, public: bool, source: record<branch: string, path: string>, status: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/pages"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a GitHub Enterprise Server Pages site
#
# POST /repos/{owner}/{repo}/pages
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#create-a-github-pages-site — API method documentation
# operationId: repos/create-pages-site
# --source shape: {branch: string, path?: "/"|"/docs"}
export def "repos-pages create-site" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --body-source: record # The source branch and directory used to publish your Pages site. — shape: {branch: string, path?: "/"|"/docs"}
]: any -> record<cname: string, custom_404: bool, html_url: string, https_certificate: record<description: string, domains: list<any>, expires_at: string, state: string>, https_enforced: bool, pending_domain_unverified_at: string, protected_domain_state: string, public: bool, source: record<branch: string, path: string>, status: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/pages"))
  let req_body = {"source": $body_source} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Update information about a GitHub Enterprise Server Pages site
#
# PUT /repos/{owner}/{repo}/pages
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#update-information-about-a-github-pages-site — API method documentation
# operationId: repos/update-information-about-pages-site
export def "repos-pages update-information-about-site" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cname: string # Specify a custom domain for the repository. Sending a `null` value will remove the custom domain. For more about custom domains, see "[Using a custom domain with GitHub Pages](https://help.github.com/articles/using-a-custom-domain-with-github-pages/)." (nullable)
  --https-enforced: oneof<nothing, bool> # Specify whether HTTPS should be enforced for the repository.
  --public: oneof<nothing, bool> # Configures access controls for the GitHub Pages site. If public is set to `true`, the site is accessible to anyone on the internet. If set to `false`, the site will only be accessible to users who have at least `read` access to the repository that published the site. This includes anyone in your Enterprise if the repository is set to `internal` visibility. This feature is only available to repositories in an organization on an Enterprise plan.
  --body-source: any
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/pages"))
  let req_body = {"cname": $cname, "https_enforced": $https_enforced, "public": $public, "source": $body_source} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List GitHub Enterprise Server Pages builds
#
# GET /repos/{owner}/{repo}/pages/builds
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-github-pages-builds — API method documentation
# operationId: repos/list-pages-builds
export def "repos-pages-builds list" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<commit: string, created_at: string, duration: int, error: record<message: string>, pusher: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, status: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/pages/builds") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Request a GitHub Enterprise Server Pages build
#
# POST /repos/{owner}/{repo}/pages/builds
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#request-a-github-pages-build — API method documentation
# operationId: repos/request-pages-build
export def "repos-pages-builds request" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<status: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/pages/builds"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get latest Pages build
#
# GET /repos/{owner}/{repo}/pages/builds/latest
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-latest-pages-build — API method documentation
# operationId: repos/get-latest-pages-build
export def "repos-pages-builds-latest get" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<commit: string, created_at: string, duration: int, error: record<message: string>, pusher: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, status: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/pages/builds/latest"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get GitHub Enterprise Server Pages build
#
# GET /repos/{owner}/{repo}/pages/builds/{build_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-github-pages-build — API method documentation
# operationId: repos/get-pages-build
export def "repos-pages-builds get" [
  owner: string
  repo: string
  build_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<commit: string, created_at: string, duration: int, error: record<message: string>, pusher: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, status: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), build_id: (encode-path-segment $build_id)} | format pattern "/repos/{owner}/{repo}/pages/builds/{build_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List pre-receive hooks for a repository
#
# GET /repos/{owner}/{repo}/pre-receive-hooks
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#list-pre-receive-hooks-for-a-repository — API method documentation
# operationId: enterprise-admin/list-pre-receive-hooks-for-repo
export def "repos-pre-receive-hooks list-enterprise-admin" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
  --direction: string@direction-completer # One of `asc` (ascending) or `desc` (descending). (default: desc)
  --qp-sort: string@sort-completer-1 # default: created
]: nothing -> table<configuration_url: string, enforcement: string, id: int, name: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar") (serialize-qp "direction" $direction "scalar") (serialize-qp "sort" $qp_sort "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/pre-receive-hooks") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove pre-receive hook enforcement for a repository
#
# DELETE /repos/{owner}/{repo}/pre-receive-hooks/{pre_receive_hook_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#remove-pre-receive-hook-enforcement-for-a-repository — API method documentation
# operationId: enterprise-admin/remove-pre-receive-hook-enforcement-for-repo
export def "repos-pre-receive-hooks delete-enterprise-admin-enforcement" [
  owner: string
  repo: string
  pre_receive_hook_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<configuration_url: string, enforcement: string, id: int, name: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), pre_receive_hook_id: (encode-path-segment $pre_receive_hook_id)} | format pattern "/repos/{owner}/{repo}/pre-receive-hooks/{pre_receive_hook_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a pre-receive hook for a repository
#
# GET /repos/{owner}/{repo}/pre-receive-hooks/{pre_receive_hook_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#get-a-pre-receive-hook-for-a-repository — API method documentation
# operationId: enterprise-admin/get-pre-receive-hook-for-repo
export def "repos-pre-receive-hooks get-enterprise-admin" [
  owner: string
  repo: string
  pre_receive_hook_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<configuration_url: string, enforcement: string, id: int, name: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), pre_receive_hook_id: (encode-path-segment $pre_receive_hook_id)} | format pattern "/repos/{owner}/{repo}/pre-receive-hooks/{pre_receive_hook_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update pre-receive hook enforcement for a repository
#
# PATCH /repos/{owner}/{repo}/pre-receive-hooks/{pre_receive_hook_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#update-pre-receive-hook-enforcement-for-a-repository — API method documentation
# operationId: enterprise-admin/update-pre-receive-hook-enforcement-for-repo
export def "repos-pre-receive-hooks update-enterprise-admin-enforcement" [
  owner: string
  repo: string
  pre_receive_hook_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --enforcement: string@enforcement-completer # The state of enforcement for the hook on this repository.
]: any -> record<configuration_url: string, enforcement: string, id: int, name: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), pre_receive_hook_id: (encode-path-segment $pre_receive_hook_id)} | format pattern "/repos/{owner}/{repo}/pre-receive-hooks/{pre_receive_hook_id}"))
  let req_body = {"enforcement": $enforcement} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List repository projects
#
# GET /repos/{owner}/{repo}/projects
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/projects#list-repository-projects — API method documentation
# operationId: projects/list-for-repo
export def "repos-projects list" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --state: string@state-completer # Indicates the state of the projects to return. Can be either `open`, `closed`, or `all`. (default: open)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<body: string, columns_url: string, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, html_url: string, id: int, name: string, node_id: string, number: int, organization_permission: string, owner_url: string, private: bool, state: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "state" $state "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/projects") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a repository project
#
# POST /repos/{owner}/{repo}/projects
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/projects#create-a-repository-project — API method documentation
# operationId: projects/create-for-repo
export def "repos-projects create" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --body: string # The description of the project.
  name: string # The name of the project.
]: any -> record<body: string, columns_url: string, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, html_url: string, id: int, name: string, node_id: string, number: int, organization_permission: string, owner_url: string, private: bool, state: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/projects"))
  let req_body = {"body": $body, "name": $name} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List pull requests
#
# GET /repos/{owner}/{repo}/pulls
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#list-pull-requests — API method documentation
# operationId: pulls/list
export def "repos-pulls list" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --state: string@state-completer # Either `open`, `closed`, or `all` to filter by state. (default: open)
  --head: string # Filter pulls by head user or head organization and branch name in the format of `user:ref-name` or `organization:ref-name`. For example: `github:new-script-format` or `octocat:test-branch`.
  --qp-base: string # Filter pulls by base branch name. Example: `gh-pages`.
  --qp-sort: string@sort-completer-7 # What to sort results by. Can be either `created`, `updated`, `popularity` (comment count) or `long-running` (age, filtering by pulls updated in the last month). (default: created)
  --direction: string@direction-completer # The direction of the sort. Can be either `asc` or `desc`. Default: `desc` when sort is `created` or sort is not specified, otherwise `asc`.
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<_links: record<comments: record, commits: record, html: record, issue: record, review_comment: record, review_comments: record, self: record, statuses: record>, active_lock_reason: string, assignee: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, assignees: list<record>, author_association: string, base: record<label: string, ref: string, repo: record, sha: string, user: record>, body: string, closed_at: string, comments_url: string, commits_url: string, created_at: string, diff_url: string, draft: bool, head: record<label: string, ref: string, repo: record, sha: string, user: record>, html_url: string, id: int, issue_url: string, labels: list<record>, locked: bool, merge_commit_sha: string, merged_at: string, milestone: record<closed_at: string, closed_issues: int, created_at: string, creator: record, description: string, due_on: string, html_url: string, id: int, labels_url: string, node_id: string, number: int, open_issues: int, state: string, title: string, updated_at: string, url: string>, node_id: string, number: int, patch_url: string, requested_reviewers: list<record>, requested_teams: list<record>, review_comment_url: string, review_comments_url: string, state: string, statuses_url: string, title: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "state" $state "scalar") (serialize-qp "head" $head "scalar") (serialize-qp "base" $qp_base "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "direction" $direction "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/pulls") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a pull request
#
# POST /repos/{owner}/{repo}/pulls
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#create-a-pull-request — API method documentation
# operationId: pulls/create
export def "repos-pulls create" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --body-base: string # The name of the branch you want the changes pulled into. This should be an existing branch on the current repository. You cannot submit a pull request to one repository that requests a merge to a base of another repository.
  --body: string # The contents of the pull request.
  --draft: oneof<nothing, bool> # Indicates whether the pull request is a draft. See "[Draft Pull Requests](https://help.github.com/en/articles/about-pull-requests#draft-pull-requests)" in the GitHub Help documentation to learn more.
  head: string # The name of the branch where your changes are implemented. For cross-repository pull requests in the same network, namespace `head` with a user like this: `username:branch`.
  --issue: int # e.g. 1
  --maintainer-can-modify: oneof<nothing, bool> # Indicates whether [maintainers can modify](https://help.github.com/articles/allowing-changes-to-a-pull-request-branch-created-from-a-fork/) the pull request.
  --title: string # The title of the new pull request.
]: any -> record<_links: record<comments: record<href: string>, commits: record<href: string>, html: record<href: string>, issue: record<href: string>, review_comment: record<href: string>, review_comments: record<href: string>, self: record<href: string>, statuses: record<href: string>>, active_lock_reason: string, additions: int, assignee: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, assignees: table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, author_association: string, base: record<label: string, ref: string, repo: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, sha: string, user: record<avatar_url: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_url: string, subscriptions_url: string, type: string, url: string>>, body: string, changed_files: int, closed_at: string, comments: int, comments_url: string, commits: int, commits_url: string, created_at: string, deletions: int, diff_url: string, draft: bool, head: record<label: string, ref: string, repo: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, sha: string, user: record<avatar_url: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_url: string, subscriptions_url: string, type: string, url: string>>, html_url: string, id: int, issue_url: string, labels: table<color: string, default: bool, description: string, id: int, name: string, node_id: string, url: string>, locked: bool, maintainer_can_modify: bool, merge_commit_sha: string, mergeable: bool, mergeable_state: string, merged: bool, merged_at: string, merged_by: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, milestone: record<closed_at: string, closed_issues: int, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, description: string, due_on: string, html_url: string, id: int, labels_url: string, node_id: string, number: int, open_issues: int, state: string, title: string, updated_at: string, url: string>, node_id: string, number: int, patch_url: string, rebaseable: bool, requested_reviewers: table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, requested_teams: table<description: string, html_url: string, id: int, ldap_dn: string, members_url: string, name: string, node_id: string, permission: string, privacy: string, repositories_url: string, slug: string, url: string>, review_comment_url: string, review_comments: int, review_comments_url: string, state: string, statuses_url: string, title: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/pulls"))
  let req_body = {"base": $body_base, "body": $body, "draft": $draft, "head": $head, "issue": $issue, "maintainer_can_modify": $maintainer_can_modify, "title": $title} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List review comments in a repository
#
# GET /repos/{owner}/{repo}/pulls/comments
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#list-review-comments-in-a-repository — API method documentation
# operationId: pulls/list-review-comments-for-repo
export def "repos-pulls-comments list" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --qp-sort: string@sort-completer-8
  --direction: string@direction-completer # Can be either `asc` or `desc`. Ignored without `sort` parameter.
  --since: string # Only show notifications updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<_links: record<html: record, pull_request: record, self: record>, author_association: string, body: string, body_html: string, body_text: string, commit_id: string, created_at: string, diff_hunk: string, html_url: string, id: int, in_reply_to_id: int, line: int, node_id: string, original_commit_id: string, original_line: int, original_position: int, original_start_line: int, path: string, position: int, pull_request_review_id: int, pull_request_url: string, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, side: string, start_line: int, start_side: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "sort" $qp_sort "scalar") (serialize-qp "direction" $direction "scalar") (serialize-qp "since" $since "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/pulls/comments") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a review comment for a pull request
#
# DELETE /repos/{owner}/{repo}/pulls/comments/{comment_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#delete-a-review-comment-for-a-pull-request — API method documentation
# operationId: pulls/delete-review-comment
export def "repos-pulls-comments delete-review" [
  owner: string
  repo: string
  comment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), comment_id: (encode-path-segment $comment_id)} | format pattern "/repos/{owner}/{repo}/pulls/comments/{comment_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a review comment for a pull request
#
# GET /repos/{owner}/{repo}/pulls/comments/{comment_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#get-a-review-comment-for-a-pull-request — API method documentation
# operationId: pulls/get-review-comment
export def "repos-pulls-comments get-review" [
  owner: string
  repo: string
  comment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_links: record<html: record<href: string>, pull_request: record<href: string>, self: record<href: string>>, author_association: string, body: string, body_html: string, body_text: string, commit_id: string, created_at: string, diff_hunk: string, html_url: string, id: int, in_reply_to_id: int, line: int, node_id: string, original_commit_id: string, original_line: int, original_position: int, original_start_line: int, path: string, position: int, pull_request_review_id: int, pull_request_url: string, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, side: string, start_line: int, start_side: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), comment_id: (encode-path-segment $comment_id)} | format pattern "/repos/{owner}/{repo}/pulls/comments/{comment_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a review comment for a pull request
#
# PATCH /repos/{owner}/{repo}/pulls/comments/{comment_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#update-a-review-comment-for-a-pull-request — API method documentation
# operationId: pulls/update-review-comment
export def "repos-pulls-comments update-review" [
  owner: string
  repo: string
  comment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  body: string # The text of the reply to the review comment.
]: any -> record<_links: record<html: record<href: string>, pull_request: record<href: string>, self: record<href: string>>, author_association: string, body: string, body_html: string, body_text: string, commit_id: string, created_at: string, diff_hunk: string, html_url: string, id: int, in_reply_to_id: int, line: int, node_id: string, original_commit_id: string, original_line: int, original_position: int, original_start_line: int, path: string, position: int, pull_request_review_id: int, pull_request_url: string, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, side: string, start_line: int, start_side: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), comment_id: (encode-path-segment $comment_id)} | format pattern "/repos/{owner}/{repo}/pulls/comments/{comment_id}"))
  let req_body = {"body": $body} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List reactions for a pull request review comment
#
# GET /repos/{owner}/{repo}/pulls/comments/{comment_id}/reactions
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#list-reactions-for-a-pull-request-review-comment — API method documentation
# operationId: reactions/list-for-pull-request-review-comment
export def "repos-pulls-comments-reactions list-for-request-review" [
  owner: string
  repo: string
  comment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --content: string@content-completer # Returns a single [reaction type](https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#reaction-types). Omit this parameter to list all reactions to a pull request review comment.
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<content: string, created_at: string, id: int, node_id: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "content" $content "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), comment_id: (encode-path-segment $comment_id)} | format pattern "/repos/{owner}/{repo}/pulls/comments/{comment_id}/reactions") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create reaction for a pull request review comment
#
# POST /repos/{owner}/{repo}/pulls/comments/{comment_id}/reactions
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#create-reaction-for-a-pull-request-review-comment — API method documentation
# operationId: reactions/create-for-pull-request-review-comment
export def "repos-pulls-comments-reactions create-for-request-review" [
  owner: string
  repo: string
  comment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  content: string@content-completer # The [reaction type](https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#reaction-types) to add to the pull request review comment.
]: any -> record<content: string, created_at: string, id: int, node_id: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), comment_id: (encode-path-segment $comment_id)} | format pattern "/repos/{owner}/{repo}/pulls/comments/{comment_id}/reactions"))
  let req_body = {"content": $content} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a pull request comment reaction
#
# DELETE /repos/{owner}/{repo}/pulls/comments/{comment_id}/reactions/{reaction_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#delete-a-pull-request-comment-reaction — API method documentation
# operationId: reactions/delete-for-pull-request-comment
export def "repos-pulls-comments-reactions delete-for-request" [
  owner: string
  repo: string
  comment_id: int
  reaction_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), comment_id: (encode-path-segment $comment_id), reaction_id: (encode-path-segment $reaction_id)} | format pattern "/repos/{owner}/{repo}/pulls/comments/{comment_id}/reactions/{reaction_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a pull request
#
# GET /repos/{owner}/{repo}/pulls/{pull_number}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#get-a-pull-request — API method documentation
# operationId: pulls/get
export def "repos-pulls get" [
  owner: string
  repo: string
  pull_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_links: record<comments: record<href: string>, commits: record<href: string>, html: record<href: string>, issue: record<href: string>, review_comment: record<href: string>, review_comments: record<href: string>, self: record<href: string>, statuses: record<href: string>>, active_lock_reason: string, additions: int, assignee: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, assignees: table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, author_association: string, base: record<label: string, ref: string, repo: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, sha: string, user: record<avatar_url: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_url: string, subscriptions_url: string, type: string, url: string>>, body: string, changed_files: int, closed_at: string, comments: int, comments_url: string, commits: int, commits_url: string, created_at: string, deletions: int, diff_url: string, draft: bool, head: record<label: string, ref: string, repo: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, sha: string, user: record<avatar_url: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_url: string, subscriptions_url: string, type: string, url: string>>, html_url: string, id: int, issue_url: string, labels: table<color: string, default: bool, description: string, id: int, name: string, node_id: string, url: string>, locked: bool, maintainer_can_modify: bool, merge_commit_sha: string, mergeable: bool, mergeable_state: string, merged: bool, merged_at: string, merged_by: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, milestone: record<closed_at: string, closed_issues: int, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, description: string, due_on: string, html_url: string, id: int, labels_url: string, node_id: string, number: int, open_issues: int, state: string, title: string, updated_at: string, url: string>, node_id: string, number: int, patch_url: string, rebaseable: bool, requested_reviewers: table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, requested_teams: table<description: string, html_url: string, id: int, ldap_dn: string, members_url: string, name: string, node_id: string, permission: string, privacy: string, repositories_url: string, slug: string, url: string>, review_comment_url: string, review_comments: int, review_comments_url: string, state: string, statuses_url: string, title: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), pull_number: (encode-path-segment $pull_number)} | format pattern "/repos/{owner}/{repo}/pulls/{pull_number}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a pull request
#
# PATCH /repos/{owner}/{repo}/pulls/{pull_number}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls/#update-a-pull-request — API method documentation
# operationId: pulls/update
export def "repos-pulls update" [
  owner: string
  repo: string
  pull_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --body-base: string # The name of the branch you want your changes pulled into. This should be an existing branch on the current repository. You cannot update the base branch on a pull request to point to another repository.
  --body: string # The contents of the pull request.
  --maintainer-can-modify: oneof<nothing, bool> # Indicates whether [maintainers can modify](https://help.github.com/articles/allowing-changes-to-a-pull-request-branch-created-from-a-fork/) the pull request.
  --state: string@state-completer-4 # State of this Pull Request. Either `open` or `closed`.
  --title: string # The title of the pull request.
]: any -> record<_links: record<comments: record<href: string>, commits: record<href: string>, html: record<href: string>, issue: record<href: string>, review_comment: record<href: string>, review_comments: record<href: string>, self: record<href: string>, statuses: record<href: string>>, active_lock_reason: string, additions: int, assignee: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, assignees: table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, author_association: string, base: record<label: string, ref: string, repo: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, sha: string, user: record<avatar_url: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_url: string, subscriptions_url: string, type: string, url: string>>, body: string, changed_files: int, closed_at: string, comments: int, comments_url: string, commits: int, commits_url: string, created_at: string, deletions: int, diff_url: string, draft: bool, head: record<label: string, ref: string, repo: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, sha: string, user: record<avatar_url: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_url: string, subscriptions_url: string, type: string, url: string>>, html_url: string, id: int, issue_url: string, labels: table<color: string, default: bool, description: string, id: int, name: string, node_id: string, url: string>, locked: bool, maintainer_can_modify: bool, merge_commit_sha: string, mergeable: bool, mergeable_state: string, merged: bool, merged_at: string, merged_by: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, milestone: record<closed_at: string, closed_issues: int, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, description: string, due_on: string, html_url: string, id: int, labels_url: string, node_id: string, number: int, open_issues: int, state: string, title: string, updated_at: string, url: string>, node_id: string, number: int, patch_url: string, rebaseable: bool, requested_reviewers: table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, requested_teams: table<description: string, html_url: string, id: int, ldap_dn: string, members_url: string, name: string, node_id: string, permission: string, privacy: string, repositories_url: string, slug: string, url: string>, review_comment_url: string, review_comments: int, review_comments_url: string, state: string, statuses_url: string, title: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), pull_number: (encode-path-segment $pull_number)} | format pattern "/repos/{owner}/{repo}/pulls/{pull_number}"))
  let req_body = {"base": $body_base, "body": $body, "maintainer_can_modify": $maintainer_can_modify, "state": $state, "title": $title} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List review comments on a pull request
#
# GET /repos/{owner}/{repo}/pulls/{pull_number}/comments
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#list-review-comments-on-a-pull-request — API method documentation
# operationId: pulls/list-review-comments
export def "repos-pulls-comments list-review" [
  owner: string
  repo: string
  pull_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --qp-sort: string@sort-completer-5 # One of `created` (when the repository was starred) or `updated` (when it was last pushed to). (default: created)
  --direction: string@direction-completer # Can be either `asc` or `desc`. Ignored without `sort` parameter.
  --since: string # Only show notifications updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<_links: record<html: record, pull_request: record, self: record>, author_association: string, body: string, body_html: string, body_text: string, commit_id: string, created_at: string, diff_hunk: string, html_url: string, id: int, in_reply_to_id: int, line: int, node_id: string, original_commit_id: string, original_line: int, original_position: int, original_start_line: int, path: string, position: int, pull_request_review_id: int, pull_request_url: string, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, side: string, start_line: int, start_side: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "sort" $qp_sort "scalar") (serialize-qp "direction" $direction "scalar") (serialize-qp "since" $since "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), pull_number: (encode-path-segment $pull_number)} | format pattern "/repos/{owner}/{repo}/pulls/{pull_number}/comments") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a review comment for a pull request
#
# POST /repos/{owner}/{repo}/pulls/{pull_number}/comments
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#create-a-review-comment-for-a-pull-request — API method documentation
# operationId: pulls/create-review-comment
export def "repos-pulls-comments create-review" [
  owner: string
  repo: string
  pull_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  body: string # The text of the review comment.
  --commit-id: string # The SHA of the commit needing a comment. Not using the latest commit SHA may render your comment outdated if a subsequent commit modifies the line you specify as the `position`.
  --in-reply-to: int # e.g. 2
  --line: int # The line of the blob in the pull request diff that the comment applies to. For a multi-line comment, the last line of the range that your comment applies to.
  --path: string # The relative path to the file that necessitates a comment.
  --position: int # The position in the diff where you want to add a review comment. Note this value is not the same as the line number in the file. For help finding the position value, read the note above.
  --side: string@side-completer # In a split diff view, the side of the diff that the pull request's changes appear on. Can be `LEFT` or `RIGHT`. Use `LEFT` for deletions that appear in red. Use `RIGHT` for additions that appear in green or unchanged lines that appear in white and are shown for context. For a multi-line comment, side represents whether the last line of the comment range is a deletion or addition. For more information, see "[Diff view options](https://help.github.com/en/articles/about-comparing-branches-in-pull-requests#diff-view-options)" in the GitHub Help documentation.
  --start-line: int # **Required when using multi-line comments**. The `start_line` is the first line in the pull request diff that your multi-line comment applies to. To learn more about multi-line comments, see "[Commenting on a pull request](https://help.github.com/en/articles/commenting-on-a-pull-request#adding-line-comments-to-a-pull-request)" in the GitHub Help documentation.
  --start-side: string@start-side-completer # **Required when using multi-line comments**. The `start_side` is the starting side of the diff that the comment applies to. Can be `LEFT` or `RIGHT`. To learn more about multi-line comments, see "[Commenting on a pull request](https://help.github.com/en/articles/commenting-on-a-pull-request#adding-line-comments-to-a-pull-request)" in the GitHub Help documentation. See `side` in this table for additional context.
]: any -> record<_links: record<html: record<href: string>, pull_request: record<href: string>, self: record<href: string>>, author_association: string, body: string, body_html: string, body_text: string, commit_id: string, created_at: string, diff_hunk: string, html_url: string, id: int, in_reply_to_id: int, line: int, node_id: string, original_commit_id: string, original_line: int, original_position: int, original_start_line: int, path: string, position: int, pull_request_review_id: int, pull_request_url: string, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, side: string, start_line: int, start_side: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), pull_number: (encode-path-segment $pull_number)} | format pattern "/repos/{owner}/{repo}/pulls/{pull_number}/comments"))
  let req_body = {"body": $body, "commit_id": $commit_id, "in_reply_to": $in_reply_to, "line": $line, "path": $path, "position": $position, "side": $side, "start_line": $start_line, "start_side": $start_side} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Create a reply for a review comment
#
# POST /repos/{owner}/{repo}/pulls/{pull_number}/comments/{comment_id}/replies
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#create-a-reply-for-a-review-comment — API method documentation
# operationId: pulls/create-reply-for-review-comment
export def "repos-pulls-comments-replies create-reply-for-review" [
  owner: string
  repo: string
  pull_number: int
  comment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  body: string # The text of the review comment.
]: any -> record<_links: record<html: record<href: string>, pull_request: record<href: string>, self: record<href: string>>, author_association: string, body: string, body_html: string, body_text: string, commit_id: string, created_at: string, diff_hunk: string, html_url: string, id: int, in_reply_to_id: int, line: int, node_id: string, original_commit_id: string, original_line: int, original_position: int, original_start_line: int, path: string, position: int, pull_request_review_id: int, pull_request_url: string, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, side: string, start_line: int, start_side: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), pull_number: (encode-path-segment $pull_number), comment_id: (encode-path-segment $comment_id)} | format pattern "/repos/{owner}/{repo}/pulls/{pull_number}/comments/{comment_id}/replies"))
  let req_body = {"body": $body} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List commits on a pull request
#
# GET /repos/{owner}/{repo}/pulls/{pull_number}/commits
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#list-commits-on-a-pull-request — API method documentation
# operationId: pulls/list-commits
export def "repos-pulls-commits list" [
  owner: string
  repo: string
  pull_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, comments_url: string, commit: record<author: record, comment_count: int, committer: record, message: string, tree: record, url: string, verification: record>, committer: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, files: list<record>, html_url: string, node_id: string, parents: list<record>, sha: string, stats: record<additions: int, deletions: int, total: int>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), pull_number: (encode-path-segment $pull_number)} | format pattern "/repos/{owner}/{repo}/pulls/{pull_number}/commits") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List pull requests files
#
# GET /repos/{owner}/{repo}/pulls/{pull_number}/files
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#list-pull-requests-files — API method documentation
# operationId: pulls/list-files
export def "repos-pulls-files list" [
  owner: string
  repo: string
  pull_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<additions: int, blob_url: string, changes: int, contents_url: string, deletions: int, filename: string, patch: string, previous_filename: string, raw_url: string, sha: string, status: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), pull_number: (encode-path-segment $pull_number)} | format pattern "/repos/{owner}/{repo}/pulls/{pull_number}/files") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Check if a pull request has been merged
#
# GET /repos/{owner}/{repo}/pulls/{pull_number}/merge
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#check-if-a-pull-request-has-been-merged — API method documentation
# operationId: pulls/check-if-merged
export def "repos-pulls-merge check-if-merged" [
  owner: string
  repo: string
  pull_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), pull_number: (encode-path-segment $pull_number)} | format pattern "/repos/{owner}/{repo}/pulls/{pull_number}/merge"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Merge a pull request
#
# PUT /repos/{owner}/{repo}/pulls/{pull_number}/merge
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#merge-a-pull-request — API method documentation
# operationId: pulls/merge
export def "repos-pulls-merge update" [
  owner: string
  repo: string
  pull_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --commit-message: string # Extra detail to append to automatic commit message.
  --commit-title: string # Title for the automatic commit message.
  --merge-method: string@merge-method-completer # Merge method to use. Possible values are `merge`, `squash` or `rebase`. Default is `merge`.
  --sha: string # SHA that pull request head must match to allow merge.
]: any -> record<merged: bool, message: string, sha: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), pull_number: (encode-path-segment $pull_number)} | format pattern "/repos/{owner}/{repo}/pulls/{pull_number}/merge"))
  let req_body = {"commit_message": $commit_message, "commit_title": $commit_title, "merge_method": $merge_method, "sha": $sha} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Remove requested reviewers from a pull request
#
# DELETE /repos/{owner}/{repo}/pulls/{pull_number}/requested_reviewers
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#remove-requested-reviewers-from-a-pull-request — API method documentation
# operationId: pulls/remove-requested-reviewers
export def "repos-pulls-requested-reviewers delete" [
  owner: string
  repo: string
  pull_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  reviewers: list<string> # An array of user `login`s that will be removed.
  --team-reviewers: list<string> # An array of team `slug`s that will be removed.
]: any -> record<_links: record<comments: record<href: string>, commits: record<href: string>, html: record<href: string>, issue: record<href: string>, review_comment: record<href: string>, review_comments: record<href: string>, self: record<href: string>, statuses: record<href: string>>, active_lock_reason: string, assignee: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, assignees: table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, author_association: string, base: record<label: string, ref: string, repo: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, sha: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>>, body: string, closed_at: string, comments_url: string, commits_url: string, created_at: string, diff_url: string, draft: bool, head: record<label: string, ref: string, repo: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, sha: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>>, html_url: string, id: int, issue_url: string, labels: table<color: string, default: bool, description: string, id: int, name: string, node_id: string, url: string>, locked: bool, merge_commit_sha: string, merged_at: string, milestone: record<closed_at: string, closed_issues: int, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, description: string, due_on: string, html_url: string, id: int, labels_url: string, node_id: string, number: int, open_issues: int, state: string, title: string, updated_at: string, url: string>, node_id: string, number: int, patch_url: string, requested_reviewers: table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, requested_teams: table<description: string, html_url: string, id: int, members_url: string, name: string, node_id: string, parent: record, permission: string, permissions: record, privacy: string, repositories_url: string, slug: string, url: string>, review_comment_url: string, review_comments_url: string, state: string, statuses_url: string, title: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), pull_number: (encode-path-segment $pull_number)} | format pattern "/repos/{owner}/{repo}/pulls/{pull_number}/requested_reviewers"))
  let req_body = {"reviewers": $reviewers, "team_reviewers": $team_reviewers} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List requested reviewers for a pull request
#
# GET /repos/{owner}/{repo}/pulls/{pull_number}/requested_reviewers
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#list-requested-reviewers-for-a-pull-request — API method documentation
# operationId: pulls/list-requested-reviewers
export def "repos-pulls-requested-reviewers list" [
  owner: string
  repo: string
  pull_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<teams: table<description: string, html_url: string, id: int, members_url: string, name: string, node_id: string, parent: record, permission: string, permissions: record, privacy: string, repositories_url: string, slug: string, url: string>, users: table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), pull_number: (encode-path-segment $pull_number)} | format pattern "/repos/{owner}/{repo}/pulls/{pull_number}/requested_reviewers") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Request reviewers for a pull request
#
# POST /repos/{owner}/{repo}/pulls/{pull_number}/requested_reviewers
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#request-reviewers-for-a-pull-request — API method documentation
# operationId: pulls/request-reviewers
export def "repos-pulls-requested-reviewers request" [
  owner: string
  repo: string
  pull_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --reviewers: list<string> # An array of user `login`s that will be requested.
  --team-reviewers: list<string> # An array of team `slug`s that will be requested.
]: any -> record<_links: record<comments: record<href: string>, commits: record<href: string>, html: record<href: string>, issue: record<href: string>, review_comment: record<href: string>, review_comments: record<href: string>, self: record<href: string>, statuses: record<href: string>>, active_lock_reason: string, assignee: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, assignees: table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, author_association: string, base: record<label: string, ref: string, repo: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, sha: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>>, body: string, closed_at: string, comments_url: string, commits_url: string, created_at: string, diff_url: string, draft: bool, head: record<label: string, ref: string, repo: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, sha: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>>, html_url: string, id: int, issue_url: string, labels: table<color: string, default: bool, description: string, id: int, name: string, node_id: string, url: string>, locked: bool, merge_commit_sha: string, merged_at: string, milestone: record<closed_at: string, closed_issues: int, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, description: string, due_on: string, html_url: string, id: int, labels_url: string, node_id: string, number: int, open_issues: int, state: string, title: string, updated_at: string, url: string>, node_id: string, number: int, patch_url: string, requested_reviewers: table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, requested_teams: table<description: string, html_url: string, id: int, members_url: string, name: string, node_id: string, parent: record, permission: string, permissions: record, privacy: string, repositories_url: string, slug: string, url: string>, review_comment_url: string, review_comments_url: string, state: string, statuses_url: string, title: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), pull_number: (encode-path-segment $pull_number)} | format pattern "/repos/{owner}/{repo}/pulls/{pull_number}/requested_reviewers"))
  let req_body = {"reviewers": $reviewers, "team_reviewers": $team_reviewers} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List reviews for a pull request
#
# GET /repos/{owner}/{repo}/pulls/{pull_number}/reviews
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#list-reviews-for-a-pull-request — API method documentation
# operationId: pulls/list-reviews
export def "repos-pulls-reviews list" [
  owner: string
  repo: string
  pull_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<_links: record<html: record, pull_request: record>, author_association: string, body: string, body_html: string, body_text: string, commit_id: string, html_url: string, id: int, node_id: string, pull_request_url: string, state: string, submitted_at: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), pull_number: (encode-path-segment $pull_number)} | format pattern "/repos/{owner}/{repo}/pulls/{pull_number}/reviews") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a review for a pull request
#
# POST /repos/{owner}/{repo}/pulls/{pull_number}/reviews
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#create-a-review-for-a-pull-request — API method documentation
# operationId: pulls/create-review
# --comments item shape: {body: string, line?: int, path: string, position?: int, side?: string, start_line?: int, start_side?: string}
export def "repos-pulls-reviews create" [
  owner: string
  repo: string
  pull_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --body: string # **Required** when using `REQUEST_CHANGES` or `COMMENT` for the `event` parameter. The body text of the pull request review.
  --comments: list # Use the following table to specify the location, destination, and contents of the draft review comment. — item shape: {body: string, line?: int, path: string, position?: int, side?: string, start_line?: int, start_side?: string}
  --commit-id: string # The SHA of the commit that needs a review. Not using the latest commit SHA may render your review comment outdated if a subsequent commit modifies the line you specify as the `position`. Defaults to the most recent commit in the pull request when you do not specify a value.
  --event: string@event-completer # The review action you want to perform. The review actions include: `APPROVE`, `REQUEST_CHANGES`, or `COMMENT`. By leaving this blank, you set the review action state to `PENDING`, which means you will need to [submit the pull request review](https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#submit-a-review-for-a-pull-request) when you are ready.
]: any -> record<_links: record<html: record<href: string>, pull_request: record<href: string>>, author_association: string, body: string, body_html: string, body_text: string, commit_id: string, html_url: string, id: int, node_id: string, pull_request_url: string, state: string, submitted_at: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), pull_number: (encode-path-segment $pull_number)} | format pattern "/repos/{owner}/{repo}/pulls/{pull_number}/reviews"))
  let req_body = {"body": $body, "comments": $comments, "commit_id": $commit_id, "event": $event} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a pending review for a pull request
#
# DELETE /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#delete-a-pending-review-for-a-pull-request — API method documentation
# operationId: pulls/delete-pending-review
export def "repos-pulls-reviews delete-pending" [
  owner: string
  repo: string
  pull_number: int
  review_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_links: record<html: record<href: string>, pull_request: record<href: string>>, author_association: string, body: string, body_html: string, body_text: string, commit_id: string, html_url: string, id: int, node_id: string, pull_request_url: string, state: string, submitted_at: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), pull_number: (encode-path-segment $pull_number), review_id: (encode-path-segment $review_id)} | format pattern "/repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a review for a pull request
#
# GET /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#get-a-review-for-a-pull-request — API method documentation
# operationId: pulls/get-review
export def "repos-pulls-reviews get" [
  owner: string
  repo: string
  pull_number: int
  review_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_links: record<html: record<href: string>, pull_request: record<href: string>>, author_association: string, body: string, body_html: string, body_text: string, commit_id: string, html_url: string, id: int, node_id: string, pull_request_url: string, state: string, submitted_at: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), pull_number: (encode-path-segment $pull_number), review_id: (encode-path-segment $review_id)} | format pattern "/repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a review for a pull request
#
# PUT /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#update-a-review-for-a-pull-request — API method documentation
# operationId: pulls/update-review
export def "repos-pulls-reviews update" [
  owner: string
  repo: string
  pull_number: int
  review_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  body: string # The body text of the pull request review.
]: any -> record<_links: record<html: record<href: string>, pull_request: record<href: string>>, author_association: string, body: string, body_html: string, body_text: string, commit_id: string, html_url: string, id: int, node_id: string, pull_request_url: string, state: string, submitted_at: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), pull_number: (encode-path-segment $pull_number), review_id: (encode-path-segment $review_id)} | format pattern "/repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}"))
  let req_body = {"body": $body} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List comments for a pull request review
#
# GET /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}/comments
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#list-comments-for-a-pull-request-review — API method documentation
# operationId: pulls/list-comments-for-review
export def "repos-pulls-reviews-comments list" [
  owner: string
  repo: string
  pull_number: int
  review_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<_links: record<html: record, pull_request: record, self: record>, author_association: string, body: string, body_html: string, body_text: string, commit_id: string, created_at: string, diff_hunk: string, html_url: string, id: int, in_reply_to_id: int, line: int, node_id: string, original_commit_id: string, original_line: int, original_position: int, original_start_line: int, path: string, position: int, pull_request_review_id: int, pull_request_url: string, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, side: string, start_line: int, start_side: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), pull_number: (encode-path-segment $pull_number), review_id: (encode-path-segment $review_id)} | format pattern "/repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}/comments") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Dismiss a review for a pull request
#
# PUT /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}/dismissals
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#dismiss-a-review-for-a-pull-request — API method documentation
# operationId: pulls/dismiss-review
export def "repos-pulls-reviews-dismissals update-dismiss" [
  owner: string
  repo: string
  pull_number: int
  review_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --event: string # e.g. "APPROVE"
  message: string # The message for the pull request review dismissal
]: any -> record<_links: record<html: record<href: string>, pull_request: record<href: string>>, author_association: string, body: string, body_html: string, body_text: string, commit_id: string, html_url: string, id: int, node_id: string, pull_request_url: string, state: string, submitted_at: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), pull_number: (encode-path-segment $pull_number), review_id: (encode-path-segment $review_id)} | format pattern "/repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}/dismissals"))
  let req_body = {"event": $event, "message": $message} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Submit a review for a pull request
#
# POST /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}/events
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#submit-a-review-for-a-pull-request — API method documentation
# operationId: pulls/submit-review
export def "repos-pulls-reviews-events submit" [
  owner: string
  repo: string
  pull_number: int
  review_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --body: string # The body text of the pull request review
  event: string@event-completer # The review action you want to perform. The review actions include: `APPROVE`, `REQUEST_CHANGES`, or `COMMENT`. When you leave this blank, the API returns _HTTP 422 (Unrecognizable entity)_ and sets the review action state to `PENDING`, which means you will need to re-submit the pull request review using a review action.
]: any -> record<_links: record<html: record<href: string>, pull_request: record<href: string>>, author_association: string, body: string, body_html: string, body_text: string, commit_id: string, html_url: string, id: int, node_id: string, pull_request_url: string, state: string, submitted_at: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), pull_number: (encode-path-segment $pull_number), review_id: (encode-path-segment $review_id)} | format pattern "/repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}/events"))
  let req_body = {"body": $body, "event": $event} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Update a pull request branch
#
# PUT /repos/{owner}/{repo}/pulls/{pull_number}/update-branch
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/pulls#update-a-pull-request-branch — API method documentation
# operationId: pulls/update-branch
export def "repos-pulls-update-branch update" [
  owner: string
  repo: string
  pull_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --expected-head-sha: string # The expected SHA of the pull request's HEAD ref. This is the most recent commit on the pull request's branch. If the expected SHA does not match the pull request's HEAD, you will receive a `422 Unprocessable Entity` status. You can use the "[List commits](https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-commits)" endpoint to find the most recent commit SHA. Default: SHA of the pull request's current HEAD ref.
]: any -> record<message: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), pull_number: (encode-path-segment $pull_number)} | format pattern "/repos/{owner}/{repo}/pulls/{pull_number}/update-branch"))
  let req_body = {"expected_head_sha": $expected_head_sha} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Get a repository README
#
# GET /repos/{owner}/{repo}/readme
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-a-repository-readme — API method documentation
# operationId: repos/get-readme
export def "repos-readme get" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --ref: string # The name of the commit/branch/tag. Default: the repository’s default branch (usually `master`)
]: nothing -> record<_links: record<git: string, html: string, self: string>, content: string, download_url: string, encoding: string, git_url: string, html_url: string, name: string, path: string, sha: string, size: int, submodule_git_url: string, target: string, type: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "ref" $ref "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/readme") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a repository README for a directory
#
# GET /repos/{owner}/{repo}/readme/{dir}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-a-repository-directory-readme — API method documentation
# operationId: repos/get-readme-in-directory
export def "repos-readme get-in-directory" [
  owner: string
  repo: string
  dir: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --ref: string # The name of the commit/branch/tag. Default: the repository’s default branch (usually `master`)
]: nothing -> record<_links: record<git: string, html: string, self: string>, content: string, download_url: string, encoding: string, git_url: string, html_url: string, name: string, path: string, sha: string, size: int, submodule_git_url: string, target: string, type: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "ref" $ref "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), dir: (encode-path-segment $dir)} | format pattern "/repos/{owner}/{repo}/readme/{dir}") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List releases
#
# GET /repos/{owner}/{repo}/releases
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-releases — API method documentation
# operationId: repos/list-releases
export def "repos-releases list" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<assets: list<record>, assets_url: string, author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, body: string, body_html: string, body_text: string, created_at: string, draft: bool, html_url: string, id: int, name: string, node_id: string, prerelease: bool, published_at: string, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, tag_name: string, tarball_url: string, target_commitish: string, upload_url: string, url: string, zipball_url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/releases") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a release
#
# POST /repos/{owner}/{repo}/releases
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#create-a-release — API method documentation
# operationId: repos/create-release
export def "repos-releases create" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --body: string # Text describing the contents of the tag.
  --draft: oneof<nothing, bool> # `true` to create a draft (unpublished) release, `false` to create a published one. (default: false)
  --name: string # The name of the release.
  --prerelease: oneof<nothing, bool> # `true` to identify the release as a prerelease. `false` to identify the release as a full release. (default: false)
  tag_name: string # The name of the tag.
  --target-commitish: string # Specifies the commitish value that determines where the Git tag is created from. Can be any branch or commit SHA. Unused if the Git tag already exists. Default: the repository's default branch (usually `master`).
]: any -> record<assets: table<browser_download_url: string, content_type: string, created_at: string, download_count: int, id: int, label: string, name: string, node_id: string, size: int, state: string, updated_at: string, uploader: record, url: string>, assets_url: string, author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, body: string, body_html: string, body_text: string, created_at: string, draft: bool, html_url: string, id: int, name: string, node_id: string, prerelease: bool, published_at: string, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, tag_name: string, tarball_url: string, target_commitish: string, upload_url: string, url: string, zipball_url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/releases"))
  let req_body = {"body": $body, "draft": $draft, "name": $name, "prerelease": $prerelease, "tag_name": $tag_name, "target_commitish": $target_commitish} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a release asset
#
# DELETE /repos/{owner}/{repo}/releases/assets/{asset_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#delete-a-release-asset — API method documentation
# operationId: repos/delete-release-asset
export def "repos-releases-assets delete" [
  owner: string
  repo: string
  asset_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), asset_id: (encode-path-segment $asset_id)} | format pattern "/repos/{owner}/{repo}/releases/assets/{asset_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a release asset
#
# GET /repos/{owner}/{repo}/releases/assets/{asset_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-a-release-asset — API method documentation
# operationId: repos/get-release-asset
export def "repos-releases-assets get" [
  owner: string
  repo: string
  asset_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<browser_download_url: string, content_type: string, created_at: string, download_count: int, id: int, label: string, name: string, node_id: string, size: int, state: string, updated_at: string, uploader: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), asset_id: (encode-path-segment $asset_id)} | format pattern "/repos/{owner}/{repo}/releases/assets/{asset_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a release asset
#
# PATCH /repos/{owner}/{repo}/releases/assets/{asset_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#update-a-release-asset — API method documentation
# operationId: repos/update-release-asset
export def "repos-releases-assets update" [
  owner: string
  repo: string
  asset_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --label: string # An alternate short description of the asset. Used in place of the filename.
  --name: string # The file name of the asset.
  --state: string # e.g. "uploaded"
]: any -> record<browser_download_url: string, content_type: string, created_at: string, download_count: int, id: int, label: string, name: string, node_id: string, size: int, state: string, updated_at: string, uploader: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), asset_id: (encode-path-segment $asset_id)} | format pattern "/repos/{owner}/{repo}/releases/assets/{asset_id}"))
  let req_body = {"label": $label, "name": $name, "state": $state} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Get the latest release
#
# GET /repos/{owner}/{repo}/releases/latest
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-the-latest-release — API method documentation
# operationId: repos/get-latest-release
export def "repos-releases-latest get" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<assets: table<browser_download_url: string, content_type: string, created_at: string, download_count: int, id: int, label: string, name: string, node_id: string, size: int, state: string, updated_at: string, uploader: record, url: string>, assets_url: string, author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, body: string, body_html: string, body_text: string, created_at: string, draft: bool, html_url: string, id: int, name: string, node_id: string, prerelease: bool, published_at: string, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, tag_name: string, tarball_url: string, target_commitish: string, upload_url: string, url: string, zipball_url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/releases/latest"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a release by tag name
#
# GET /repos/{owner}/{repo}/releases/tags/{tag}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-a-release-by-tag-name — API method documentation
# operationId: repos/get-release-by-tag
export def "repos-releases-tags get" [
  owner: string
  repo: string
  tag: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<assets: table<browser_download_url: string, content_type: string, created_at: string, download_count: int, id: int, label: string, name: string, node_id: string, size: int, state: string, updated_at: string, uploader: record, url: string>, assets_url: string, author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, body: string, body_html: string, body_text: string, created_at: string, draft: bool, html_url: string, id: int, name: string, node_id: string, prerelease: bool, published_at: string, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, tag_name: string, tarball_url: string, target_commitish: string, upload_url: string, url: string, zipball_url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), tag: (encode-path-segment $tag)} | format pattern "/repos/{owner}/{repo}/releases/tags/{tag}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a release
#
# DELETE /repos/{owner}/{repo}/releases/{release_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#delete-a-release — API method documentation
# operationId: repos/delete-release
export def "repos-releases delete" [
  owner: string
  repo: string
  release_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), release_id: (encode-path-segment $release_id)} | format pattern "/repos/{owner}/{repo}/releases/{release_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a release
#
# GET /repos/{owner}/{repo}/releases/{release_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-a-release — API method documentation
# operationId: repos/get-release
export def "repos-releases get" [
  owner: string
  repo: string
  release_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<assets: table<browser_download_url: string, content_type: string, created_at: string, download_count: int, id: int, label: string, name: string, node_id: string, size: int, state: string, updated_at: string, uploader: record, url: string>, assets_url: string, author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, body: string, body_html: string, body_text: string, created_at: string, draft: bool, html_url: string, id: int, name: string, node_id: string, prerelease: bool, published_at: string, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, tag_name: string, tarball_url: string, target_commitish: string, upload_url: string, url: string, zipball_url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), release_id: (encode-path-segment $release_id)} | format pattern "/repos/{owner}/{repo}/releases/{release_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a release
#
# PATCH /repos/{owner}/{repo}/releases/{release_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#update-a-release — API method documentation
# operationId: repos/update-release
export def "repos-releases update" [
  owner: string
  repo: string
  release_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --body: string # Text describing the contents of the tag.
  --draft: oneof<nothing, bool> # `true` makes the release a draft, and `false` publishes the release.
  --name: string # The name of the release.
  --prerelease: oneof<nothing, bool> # `true` to identify the release as a prerelease, `false` to identify the release as a full release.
  --tag-name: string # The name of the tag.
  --target-commitish: string # Specifies the commitish value that determines where the Git tag is created from. Can be any branch or commit SHA. Unused if the Git tag already exists. Default: the repository's default branch (usually `master`).
]: any -> record<assets: table<browser_download_url: string, content_type: string, created_at: string, download_count: int, id: int, label: string, name: string, node_id: string, size: int, state: string, updated_at: string, uploader: record, url: string>, assets_url: string, author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, body: string, body_html: string, body_text: string, created_at: string, draft: bool, html_url: string, id: int, name: string, node_id: string, prerelease: bool, published_at: string, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, tag_name: string, tarball_url: string, target_commitish: string, upload_url: string, url: string, zipball_url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), release_id: (encode-path-segment $release_id)} | format pattern "/repos/{owner}/{repo}/releases/{release_id}"))
  let req_body = {"body": $body, "draft": $draft, "name": $name, "prerelease": $prerelease, "tag_name": $tag_name, "target_commitish": $target_commitish} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List release assets
#
# GET /repos/{owner}/{repo}/releases/{release_id}/assets
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-release-assets — API method documentation
# operationId: repos/list-release-assets
export def "repos-releases-assets list" [
  owner: string
  repo: string
  release_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<browser_download_url: string, content_type: string, created_at: string, download_count: int, id: int, label: string, name: string, node_id: string, size: int, state: string, updated_at: string, uploader: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), release_id: (encode-path-segment $release_id)} | format pattern "/repos/{owner}/{repo}/releases/{release_id}/assets") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Upload a release asset
#
# POST /repos/{owner}/{repo}/releases/{release_id}/assets
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#upload-a-release-asset — API method documentation
# operationId: repos/upload-release-asset
export def "repos-releases-assets upload" [
  owner: string
  repo: string
  release_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --name: string
  --label: string
  --body: record
]: any -> record<browser_download_url: string, content_type: string, created_at: string, download_count: int, id: int, label: string, name: string, node_id: string, size: int, state: string, updated_at: string, uploader: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default "{origin}")
  let qp = [(serialize-qp "name" $name "scalar") (serialize-qp "label" $label "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), release_id: (encode-path-segment $release_id)} | format pattern "/repos/{owner}/{repo}/releases/{release_id}/assets") $qp)
  let req_body = $body
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "*/*" $req_body
}

# List stargazers
#
# GET /repos/{owner}/{repo}/stargazers
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#list-stargazers — API method documentation
# operationId: activity/list-stargazers-for-repo
export def "repos-stargazers list-activity" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/stargazers") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the weekly commit activity
#
# GET /repos/{owner}/{repo}/stats/code_frequency
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-the-weekly-commit-activity — API method documentation
# operationId: repos/get-code-frequency-stats
export def "repos-stats-code-frequency get" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> list<list<int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/stats/code_frequency"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the last year of commit activity
#
# GET /repos/{owner}/{repo}/stats/commit_activity
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-the-last-year-of-commit-activity — API method documentation
# operationId: repos/get-commit-activity-stats
export def "repos-stats-commit-activity get" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> table<days: list<int>, total: int, week: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/stats/commit_activity"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get all contributor commit activity
#
# GET /repos/{owner}/{repo}/stats/contributors
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-all-contributor-commit-activity — API method documentation
# operationId: repos/get-contributors-stats
export def "repos-stats-contributors get" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> table<author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, total: int, weeks: list<record>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/stats/contributors"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the weekly commit count
#
# GET /repos/{owner}/{repo}/stats/participation
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-the-weekly-commit-count — API method documentation
# operationId: repos/get-participation-stats
export def "repos-stats-participation get" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<all: list<int>, owner: list<int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/stats/participation"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the hourly commit count for each day
#
# GET /repos/{owner}/{repo}/stats/punch_card
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-the-hourly-commit-count-for-each-day — API method documentation
# operationId: repos/get-punch-card-stats
export def "repos-stats-punch-card get" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> list<list<int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/stats/punch_card"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a commit status
#
# POST /repos/{owner}/{repo}/statuses/{sha}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#create-a-commit-status — API method documentation
# operationId: repos/create-commit-status
export def "repos-statuses create-commit-status" [
  owner: string
  repo: string
  sha: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --context: string # A string label to differentiate this status from the status of other systems. This field is case-insensitive. (default: default)
  --description: string # A short description of the status.
  state: string@state-completer-5 # The state of the status. Can be one of `error`, `failure`, `pending`, or `success`.
  --target-url: string # The target URL to associate with this status. This URL will be linked from the GitHub UI to allow users to easily see the source of the status. For example, if your continuous integration system is posting build status, you would want to provide the deep link for the build output for this specific SHA: `http://ci.example.com/user/repo/build/sha`
]: any -> record<avatar_url: string, context: string, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, description: string, id: int, node_id: string, state: string, target_url: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), sha: (encode-path-segment $sha)} | format pattern "/repos/{owner}/{repo}/statuses/{sha}"))
  let req_body = {"context": $context, "description": $description, "state": $state, "target_url": $target_url} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List watchers
#
# GET /repos/{owner}/{repo}/subscribers
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#list-watchers — API method documentation
# operationId: activity/list-watchers-for-repo
export def "repos-subscribers list-activity-watchers" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/subscribers") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a repository subscription
#
# DELETE /repos/{owner}/{repo}/subscription
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#delete-a-repository-subscription — API method documentation
# operationId: activity/delete-repo-subscription
export def "repos-subscription delete-activity" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/subscription"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a repository subscription
#
# GET /repos/{owner}/{repo}/subscription
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#get-a-repository-subscription — API method documentation
# operationId: activity/get-repo-subscription
export def "repos-subscription get-activity" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created_at: string, ignored: bool, reason: string, repository_url: string, subscribed: bool, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/subscription"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Set a repository subscription
#
# PUT /repos/{owner}/{repo}/subscription
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#set-a-repository-subscription — API method documentation
# operationId: activity/set-repo-subscription
export def "repos-subscription update-activity" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --ignored: oneof<nothing, bool> # Determines if all notifications should be blocked from this repository.
  --subscribed: oneof<nothing, bool> # Determines if notifications should be received from this repository.
]: any -> record<created_at: string, ignored: bool, reason: string, repository_url: string, subscribed: bool, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/subscription"))
  let req_body = {"ignored": $ignored, "subscribed": $subscribed} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List repository tags
#
# GET /repos/{owner}/{repo}/tags
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-repository-tags — API method documentation
# operationId: repos/list-tags
export def "repos-tags list" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<commit: record<sha: string, url: string>, name: string, node_id: string, tarball_url: string, zipball_url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/tags") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download a repository archive (tar)
#
# GET /repos/{owner}/{repo}/tarball/{ref}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#download-a-repository-archive — API method documentation
# operationId: repos/download-tarball-archive
export def "repos-tarball download-archive" [
  owner: string
  repo: string
  ref: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), ref: (encode-path-segment $ref)} | format pattern "/repos/{owner}/{repo}/tarball/{ref}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List repository teams
#
# GET /repos/{owner}/{repo}/teams
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-repository-teams — API method documentation
# operationId: repos/list-teams
export def "repos-teams list" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<description: string, html_url: string, id: int, members_url: string, name: string, node_id: string, parent: record<description: string, html_url: string, id: int, ldap_dn: string, members_url: string, name: string, node_id: string, permission: string, privacy: string, repositories_url: string, slug: string, url: string>, permission: string, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, privacy: string, repositories_url: string, slug: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/teams") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get all repository topics
#
# GET /repos/{owner}/{repo}/topics
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#get-all-repository-topics — API method documentation
# operationId: repos/get-all-topics
export def "repos-topics get-list" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --page: int # Page number of the results to fetch. (default: 1)
  --per-page: int # Results per page (max 100) (default: 30)
]: nothing -> record<names: list<string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "page" $page "scalar") (serialize-qp "per_page" $per_page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/topics") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Replace all repository topics
#
# PUT /repos/{owner}/{repo}/topics
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#replace-all-repository-topics — API method documentation
# operationId: repos/replace-all-topics
export def "repos-topics update-list" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  names: list<string> # An array of topics to add to the repository. Pass one or more topics to _replace_ the set of existing topics. Send an empty array (`[]`) to clear all topics from the repository. **Note:** Topic `names` cannot contain uppercase letters.
]: any -> record<names: list<string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/topics"))
  let req_body = {"names": $names} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Transfer a repository
#
# POST /repos/{owner}/{repo}/transfer
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#transfer-a-repository — API method documentation
# operationId: repos/transfer
export def "repos-transfer create" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  new_owner: string # The username or organization name the repository will be transferred to.
  --team-ids: list<int> # ID of the team or teams to add to the repository. Teams can only be added to organization-owned repositories.
]: any -> record<allow_forking: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record<body: string, html_url: string, key: string, name: string, url: string>, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<key: string, name: string, node_id: string, spdx_id: string, url: string>, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/repos/{owner}/{repo}/transfer"))
  let req_body = {"new_owner": $new_owner, "team_ids": $team_ids} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Download a repository archive (zip)
#
# GET /repos/{owner}/{repo}/zipball/{ref}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#download-a-repository-archive — API method documentation
# operationId: repos/download-zipball-archive
export def "repos-zipball download-archive" [
  owner: string
  repo: string
  ref: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo), ref: (encode-path-segment $ref)} | format pattern "/repos/{owner}/{repo}/zipball/{ref}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a repository using a template
#
# POST /repos/{template_owner}/{template_repo}/generate
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#create-a-repository-using-a-template — API method documentation
# operationId: repos/create-using-template
export def "repos-generate create" [
  template_owner: string
  template_repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string # A short description of the new repository.
  --include-all-branches: oneof<nothing, bool> # Set to `true` to include the directory structure and files from all branches in the template repository, and not just the default branch. Default: `false`. (default: false)
  name: string # The name of the new repository.
  --owner: string # The organization or person who will own the new repository. To create a new repository in an organization, the authenticated user must be a member of the specified organization.
  --private: oneof<nothing, bool> # Either `true` to create a new private repository or `false` to create a new public one. (default: false)
]: any -> record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues_count: int, owner: record<avatar_url: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({template_owner: (encode-path-segment $template_owner), template_repo: (encode-path-segment $template_repo)} | format pattern "/repos/{template_owner}/{template_repo}/generate"))
  let req_body = {"description": $description, "include_all_branches": $include_all_branches, "name": $name, "owner": $owner, "private": $private} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List public repositories
#
# GET /repositories
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-public-repositories — API method documentation
# operationId: repos/list-public
export def "repositories list-repos-public" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --since: int # A repository ID. Only return repositories with an ID greater than this ID.
  --visibility: string@visibility-completer-3 # Specifies the types of repositories to return. Can be one of `all` or `public`. Default: `public`. Note: For GitHub Enterprise Server and GitHub AE, this endpoint will only list repositories available to all users on the enterprise. (default: public, e.g. all)
]: nothing -> table<allow_forking: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record<body: string, html_url: string, key: string, name: string, url: string>, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<key: string, name: string, node_id: string, spdx_id: string, url: string>, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "since" $since "scalar") (serialize-qp "visibility" $visibility "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/repositories" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Search code
#
# GET /search/code
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/search#search-code — API method documentation
# operationId: search/code
export def "search-code list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --q: string # The query contains one or more search keywords and qualifiers. Qualifiers allow you to limit your search to specific areas of GitHub. The REST API supports the same qualifiers as GitHub.com. To learn more about the format of the query, see [Constructing a search query](https://docs.github.com/enterprise-server@2.22/rest/reference/search#constructing-a-search-query). See "[Searching code](https://help.github.com/articles/searching-code/)" for a detailed list of qualifiers.
  --qp-sort: string@sort-completer-9 # Sorts the results of your query. Can only be `indexed`, which indicates how recently a file has been indexed by the GitHub Enterprise Server search infrastructure. Default: [best match](https://docs.github.com/enterprise-server@2.22/rest/reference/search#ranking-search-results)
  --order: string@order-completer # Determines whether the first search result returned is the highest number of matches (`desc`) or lowest number of matches (`asc`). This parameter is ignored unless you provide `sort`. (default: desc)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<incomplete_results: bool, items: table<file_size: int, git_url: string, html_url: string, language: string, last_modified_at: string, line_numbers: list, name: string, path: string, repository: record, score: float, sha: string, text_matches: list, url: string>, total_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "q" $q "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "order" $order "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/search/code" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Search commits
#
# GET /search/commits
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/search#search-commits — API method documentation
# operationId: search/commits
export def "search-commits list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --q: string # The query contains one or more search keywords and qualifiers. Qualifiers allow you to limit your search to specific areas of GitHub. The REST API supports the same qualifiers as GitHub.com. To learn more about the format of the query, see [Constructing a search query](https://docs.github.com/enterprise-server@2.22/rest/reference/search#constructing-a-search-query). See "[Searching commits](https://help.github.com/articles/searching-commits/)" for a detailed list of qualifiers.
  --qp-sort: string@sort-completer-10 # Sorts the results of your query by `author-date` or `committer-date`. Default: [best match](https://docs.github.com/enterprise-server@2.22/rest/reference/search#ranking-search-results)
  --order: string@order-completer # Determines whether the first search result returned is the highest number of matches (`desc`) or lowest number of matches (`asc`). This parameter is ignored unless you provide `sort`. (default: desc)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<incomplete_results: bool, items: table<author: record, comments_url: string, commit: record, committer: record, html_url: string, node_id: string, parents: list, repository: record, score: float, sha: string, text_matches: list, url: string>, total_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "q" $q "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "order" $order "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/search/commits" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Search issues and pull requests
#
# GET /search/issues
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/search#search-issues-and-pull-requests — API method documentation
# operationId: search/issues-and-pull-requests
export def "search-issues pull-and-requests" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --q: string # The query contains one or more search keywords and qualifiers. Qualifiers allow you to limit your search to specific areas of GitHub. The REST API supports the same qualifiers as GitHub.com. To learn more about the format of the query, see [Constructing a search query](https://docs.github.com/enterprise-server@2.22/rest/reference/search#constructing-a-search-query). See "[Searching issues and pull requests](https://help.github.com/articles/searching-issues-and-pull-requests/)" for a detailed list of qualifiers.
  --qp-sort: string@sort-completer-11 # Sorts the results of your query by the number of `comments`, `reactions`, `reactions-+1`, `reactions--1`, `reactions-smile`, `reactions-thinking_face`, `reactions-heart`, `reactions-tada`, or `interactions`. You can also sort results by how recently the items were `created` or `updated`, Default: [best match](https://docs.github.com/enterprise-server@2.22/rest/reference/search#ranking-search-results)
  --order: string@order-completer # Determines whether the first search result returned is the highest number of matches (`desc`) or lowest number of matches (`asc`). This parameter is ignored unless you provide `sort`. (default: desc)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<incomplete_results: bool, items: table<active_lock_reason: string, assignee: record, assignees: list, author_association: string, body: string, body_html: string, body_text: string, closed_at: string, comments: int, comments_url: string, created_at: string, draft: bool, events_url: string, html_url: string, id: int, labels: list, labels_url: string, locked: bool, milestone: record, node_id: string, number: int, performed_via_github_app: record, pull_request: record, reactions: record, repository: record, repository_url: string, score: float, state: string, text_matches: list, timeline_url: string, title: string, updated_at: string, url: string, user: record>, total_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "q" $q "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "order" $order "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/search/issues" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Search labels
#
# GET /search/labels
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/search#search-labels — API method documentation
# operationId: search/labels
export def "search-labels list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --repository-id: int # The id of the repository.
  --q: string # The search keywords. This endpoint does not accept qualifiers in the query. To learn more about the format of the query, see [Constructing a search query](https://docs.github.com/enterprise-server@2.22/rest/reference/search#constructing-a-search-query).
  --qp-sort: string@sort-completer-5 # Sorts the results of your query by when the label was `created` or `updated`. Default: [best match](https://docs.github.com/enterprise-server@2.22/rest/reference/search#ranking-search-results)
  --order: string@order-completer # Determines whether the first search result returned is the highest number of matches (`desc`) or lowest number of matches (`asc`). This parameter is ignored unless you provide `sort`. (default: desc)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<incomplete_results: bool, items: table<color: string, default: bool, description: string, id: int, name: string, node_id: string, score: float, text_matches: list, url: string>, total_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "repository_id" $repository_id "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "order" $order "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/search/labels" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Search repositories
#
# GET /search/repositories
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/search#search-repositories — API method documentation
# operationId: search/repos
export def "search-repositories list-repos" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --q: string # The query contains one or more search keywords and qualifiers. Qualifiers allow you to limit your search to specific areas of GitHub. The REST API supports the same qualifiers as GitHub.com. To learn more about the format of the query, see [Constructing a search query](https://docs.github.com/enterprise-server@2.22/rest/reference/search#constructing-a-search-query). See "[Searching for repositories](https://help.github.com/articles/searching-for-repositories/)" for a detailed list of qualifiers.
  --qp-sort: string@sort-completer-12 # Sorts the results of your query by number of `stars`, `forks`, or `help-wanted-issues` or how recently the items were `updated`. Default: [best match](https://docs.github.com/enterprise-server@2.22/rest/reference/search#ranking-search-results)
  --order: string@order-completer # Determines whether the first search result returned is the highest number of matches (`desc`) or lowest number of matches (`asc`). This parameter is ignored unless you provide `sort`. (default: desc)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<incomplete_results: bool, items: table<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, score: float, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, text_matches: list, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, total_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "q" $q "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "order" $order "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/search/repositories" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Search topics
#
# GET /search/topics
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/search#search-topics — API method documentation
# operationId: search/topics
export def "search-topics list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --q: string # The query contains one or more search keywords and qualifiers. Qualifiers allow you to limit your search to specific areas of GitHub. The REST API supports the same qualifiers as GitHub.com. To learn more about the format of the query, see [Constructing a search query](https://docs.github.com/enterprise-server@2.22/rest/reference/search#constructing-a-search-query).
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<incomplete_results: bool, items: table<aliases: list, created_at: string, created_by: string, curated: bool, description: string, display_name: string, featured: bool, logo_url: string, name: string, related: list, released: string, repository_count: int, score: float, short_description: string, text_matches: list, updated_at: string>, total_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "q" $q "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/search/topics" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Search users
#
# GET /search/users
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/search#search-users — API method documentation
# operationId: search/users
export def "search-users list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --q: string # The query contains one or more search keywords and qualifiers. Qualifiers allow you to limit your search to specific areas of GitHub. The REST API supports the same qualifiers as GitHub.com. To learn more about the format of the query, see [Constructing a search query](https://docs.github.com/enterprise-server@2.22/rest/reference/search#constructing-a-search-query). See "[Searching users](https://help.github.com/articles/searching-users/)" for a detailed list of qualifiers.
  --qp-sort: string@sort-completer-13 # Sorts the results of your query by number of `followers` or `repositories`, or when the person `joined` GitHub Enterprise Server. Default: [best match](https://docs.github.com/enterprise-server@2.22/rest/reference/search#ranking-search-results)
  --order: string@order-completer # Determines whether the first search result returned is the highest number of matches (`desc`) or lowest number of matches (`asc`). This parameter is ignored unless you provide `sort`. (default: desc)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<incomplete_results: bool, items: table<avatar_url: string, bio: string, blog: string, company: string, created_at: string, email: string, events_url: string, followers: int, followers_url: string, following: int, following_url: string, gists_url: string, gravatar_id: string, hireable: bool, html_url: string, id: int, location: string, login: string, name: string, node_id: string, organizations_url: string, public_gists: int, public_repos: int, received_events_url: string, repos_url: string, score: float, site_admin: bool, starred_url: string, subscriptions_url: string, suspended_at: string, text_matches: list, type: string, updated_at: string, url: string>, total_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "q" $q "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "order" $order "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/search/users" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the configuration status
#
# GET /setup/api/configcheck
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#get-the-configuration-status — API method documentation
# operationId: enterprise-admin/get-configuration-status
export def "setup-configcheck get-enterprise-admin-configuration-status" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<progress: table<key: string, status: string>, status: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/setup/api/configcheck")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Start a configuration process
#
# POST /setup/api/configure
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#start-a-configuration-process — API method documentation
# operationId: enterprise-admin/start-configuration-process
export def "setup-configure start-enterprise-admin-configuration-process" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/setup/api/configure")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the maintenance status
#
# GET /setup/api/maintenance
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#get-the-maintenance-status — API method documentation
# operationId: enterprise-admin/get-maintenance-status
export def "setup-maintenance get-enterprise-admin-status" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<connection_services: table<name: string, number: int>, scheduled_time: string, status: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/setup/api/maintenance")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Enable or disable maintenance mode
#
# POST /setup/api/maintenance
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#enable-or-disable-maintenance-mode — API method documentation
# operationId: enterprise-admin/enable-or-disable-maintenance-mode
export def "setup-maintenance enable-enterprise-admin-or-disable-mode" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  maintenance: string # A JSON string with the attributes `enabled` and `when`. The possible values for `enabled` are `true` and `false`. When it's `false`, the attribute `when` is ignored and the maintenance mode is turned off. `when` defines the time period when the maintenance was enabled. The possible values for `when` are `now` or any date parseable by [mojombo/chronic](https://github.com/mojombo/chronic).
]: any -> record<connection_services: table<name: string, number: int>, scheduled_time: string, status: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/setup/api/maintenance")
  let req_body = {"maintenance": $maintenance} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  let req_body = ($req_body | transpose k v | where {|p| $p.v != null} | each {|p| $"(encode-path-segment $p.k)=(encode-path-segment $p.v)" } | str join "&")
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/x-www-form-urlencoded" $req_body
}

# Get settings
#
# GET /setup/api/settings
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#get-settings — API method documentation
# operationId: enterprise-admin/get-settings
export def "setup-settings get-enterprise-admin" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<enterprise: record<admin_password: string, assets: string, auth_mode: string, avatar: record<enabled: bool, uri: string>, cas: record<url: string>, collectd: record<enabled: bool, encryption: string, password: string, port: int, server: string, username: string>, configuration_id: int, configuration_run_count: int, customer: record<email: string, name: string, public_key_data: string, secret_key_data: string, uuid: string>, expire_sessions: bool, github_hostname: string, github_oauth: record<client_id: string, client_secret: string, organization_name: string, organization_team: string>, github_ssl: record<cert: string, enabled: bool, key: string>, http_proxy: string, identicons_host: string, ldap: record<admin_group: string, base: list, bind_dn: string, host: string, method: string, password: string, port: int, posix_support: bool, profile: record, reconciliation: record, recursive_group_search: bool, search_strategy: string, sync_enabled: bool, team_sync_interval: int, uid: string, user_groups: list, user_sync_emails: bool, user_sync_interval: int, user_sync_keys: bool, virtual_attribute_enabled: bool>, license: record<cluster_support: bool, evaluation: bool, expire_at: string, perpetual: bool, seats: int, ssh_allowed: bool, support_key: string, unlimited_seating: bool>, load_balancer: string, mapping: record<basemap: string, enabled: bool, tileserver: string, token: string>, ntp: record<primary_server: string, secondary_server: string>, pages: record<enabled: bool>, private_mode: bool, public_pages: bool, saml: record<certificate: string, certificate_path: string, disable_admin_demote: bool, idp_initiated_sso: bool, issuer: string, sso_url: string>, signup_enabled: bool, smtp: record<address: string, authentication: string, discard_to_noreply_address: bool, domain: string, enable_starttls_auto: bool, enabled: bool, noreply_address: string, password: string, port: string, support_address: string, support_address_type: string, user_name: string, username: string>, snmp: record<community: string, enabled: bool>, subdomain_isolation: bool, syslog: record<enabled: bool, protocol_name: string, server: string>, timezone: string>, run_list: list<string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/setup/api/settings")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Set settings
#
# PUT /setup/api/settings
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#set-settings — API method documentation
# operationId: enterprise-admin/set-settings
export def "setup-settings update-enterprise-admin" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  settings: string # A JSON string with the new settings. Note that you only need to pass the specific settings you want to modify. For a list of the available settings, see the [Get settings endpoint](https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#get-settings).
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/setup/api/settings")
  let req_body = {"settings": $settings} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  let req_body = ($req_body | transpose k v | where {|p| $p.v != null} | each {|p| $"(encode-path-segment $p.k)=(encode-path-segment $p.v)" } | str join "&")
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/x-www-form-urlencoded" $req_body
}

# Remove an authorized SSH key
#
# DELETE /setup/api/settings/authorized-keys
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#remove-an-authorized-ssh-key — API method documentation
# operationId: enterprise-admin/remove-authorized-ssh-key
export def "setup-settings-authorized-keys delete-enterprise-admin-ssh" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  authorized_key: string # The public SSH key.
]: any -> table<key: string, pretty_print: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/setup/api/settings/authorized-keys")
  let req_body = {"authorized_key": $authorized_key} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  let req_body = ($req_body | transpose k v | where {|p| $p.v != null} | each {|p| $"(encode-path-segment $p.k)=(encode-path-segment $p.v)" } | str join "&")
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/x-www-form-urlencoded" $req_body
}

# Get all authorized SSH keys
#
# GET /setup/api/settings/authorized-keys
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#get-all-authorized-ssh-keys — API method documentation
# operationId: enterprise-admin/get-all-authorized-ssh-keys
export def "setup-settings-authorized-keys get-enterprise-admin-list-ssh" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> table<key: string, pretty_print: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/setup/api/settings/authorized-keys")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add an authorized SSH key
#
# POST /setup/api/settings/authorized-keys
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#add-an-authorized-ssh-key — API method documentation
# operationId: enterprise-admin/add-authorized-ssh-key
export def "setup-settings-authorized-keys create-enterprise-admin-ssh" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  authorized_key: string # The public SSH key.
]: any -> table<key: string, pretty_print: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/setup/api/settings/authorized-keys")
  let req_body = {"authorized_key": $authorized_key} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  let req_body = ($req_body | transpose k v | where {|p| $p.v != null} | each {|p| $"(encode-path-segment $p.k)=(encode-path-segment $p.v)" } | str join "&")
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/x-www-form-urlencoded" $req_body
}

# Create a GitHub license
#
# POST /setup/api/start
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#create-a-github-enterprise-server-license — API method documentation
# operationId: enterprise-admin/create-enterprise-server-license
export def "setup-start create-enterprise-admin-enterprise-server-license" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  license: string # The content of your _.ghl_ license file.
  --password: string # You **must** provide a password _only if_ you are uploading your license for the first time. If you previously set a password through the web interface, you don't need this parameter.
  --settings: string # An optional JSON string containing the installation settings. For a list of the available settings, see the [Get settings endpoint](https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#get-settings).
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/setup/api/start")
  let req_body = {"license": $license, "password": $password, "settings": $settings} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  let req_body = ($req_body | transpose k v | where {|p| $p.v != null} | each {|p| $"(encode-path-segment $p.k)=(encode-path-segment $p.v)" } | str join "&")
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/x-www-form-urlencoded" $req_body
}

# Upgrade a license
#
# POST /setup/api/upgrade
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#upgrade-a-license — API method documentation
# operationId: enterprise-admin/upgrade-license
export def "setup-upgrade create-enterprise-admin-license" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --license: string # The content of your new _.ghl_ license file.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/setup/api/upgrade")
  let req_body = {"license": $license} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  let req_body = ($req_body | transpose k v | where {|p| $p.v != null} | each {|p| $"(encode-path-segment $p.k)=(encode-path-segment $p.v)" } | str join "&")
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/x-www-form-urlencoded" $req_body
}

# Delete a team (Legacy)
#
# DELETE /teams/{team_id}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams/#delete-a-team-legacy — API method documentation
# operationId: teams/delete-legacy
@deprecated
export def "teams delete-legacy" [
  team_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id)} | format pattern "/teams/{team_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a team (Legacy)
#
# GET /teams/{team_id}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams/#get-a-team-legacy — API method documentation
# operationId: teams/get-legacy
@deprecated
export def "teams get-legacy" [
  team_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created_at: string, description: string, html_url: string, id: int, ldap_dn: string, members_count: int, members_url: string, name: string, node_id: string, organization: record<avatar_url: string, billing_email: string, blog: string, collaborators: int, company: string, created_at: string, default_repository_permission: string, description: string, disk_usage: int, email: string, events_url: string, followers: int, following: int, has_organization_projects: bool, has_repository_projects: bool, hooks_url: string, html_url: string, id: int, is_verified: bool, issues_url: string, location: string, login: string, members_allowed_repository_creation_type: string, members_can_create_internal_repositories: bool, members_can_create_pages: bool, members_can_create_private_pages: bool, members_can_create_private_repositories: bool, members_can_create_public_pages: bool, members_can_create_public_repositories: bool, members_can_create_repositories: bool, members_url: string, name: string, node_id: string, owned_private_repos: int, plan: record<filled_seats: int, name: string, private_repos: int, seats: int, space: int>, private_gists: int, public_gists: int, public_members_url: string, public_repos: int, repos_url: string, total_private_repos: int, twitter_username: string, two_factor_requirement_enabled: bool, type: string, updated_at: string, url: string>, parent: record<description: string, html_url: string, id: int, ldap_dn: string, members_url: string, name: string, node_id: string, permission: string, privacy: string, repositories_url: string, slug: string, url: string>, permission: string, privacy: string, repos_count: int, repositories_url: string, slug: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id)} | format pattern "/teams/{team_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a team (Legacy)
#
# PATCH /teams/{team_id}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams/#update-a-team-legacy — API method documentation
# operationId: teams/update-legacy
@deprecated
export def "teams update-legacy" [
  team_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string # The description of the team.
  name: string # The name of the team.
  --parent-team-id: int # The ID of a team to set as the parent team. (nullable)
  --permission: string@permission-completer # **Deprecated**. The permission that new repositories will be added to the team with when none is specified. Can be one of: \* `pull` - team members can pull, but not push to or administer newly-added repositories. \* `push` - team members can pull and push, but not administer newly-added repositories. \* `admin` - team members can pull, push and administer newly-added repositories. (default: pull)
  --privacy: string@privacy-completer # The level of privacy this team should have. Editing teams without specifying this parameter leaves `privacy` intact. The options are: **For a non-nested team:** \* `secret` - only visible to organization owners and members of this team. \* `closed` - visible to all members of this organization. **For a parent or child team:** \* `closed` - visible to all members of this organization.
]: any -> record<created_at: string, description: string, html_url: string, id: int, ldap_dn: string, members_count: int, members_url: string, name: string, node_id: string, organization: record<avatar_url: string, billing_email: string, blog: string, collaborators: int, company: string, created_at: string, default_repository_permission: string, description: string, disk_usage: int, email: string, events_url: string, followers: int, following: int, has_organization_projects: bool, has_repository_projects: bool, hooks_url: string, html_url: string, id: int, is_verified: bool, issues_url: string, location: string, login: string, members_allowed_repository_creation_type: string, members_can_create_internal_repositories: bool, members_can_create_pages: bool, members_can_create_private_pages: bool, members_can_create_private_repositories: bool, members_can_create_public_pages: bool, members_can_create_public_repositories: bool, members_can_create_repositories: bool, members_url: string, name: string, node_id: string, owned_private_repos: int, plan: record<filled_seats: int, name: string, private_repos: int, seats: int, space: int>, private_gists: int, public_gists: int, public_members_url: string, public_repos: int, repos_url: string, total_private_repos: int, twitter_username: string, two_factor_requirement_enabled: bool, type: string, updated_at: string, url: string>, parent: record<description: string, html_url: string, id: int, ldap_dn: string, members_url: string, name: string, node_id: string, permission: string, privacy: string, repositories_url: string, slug: string, url: string>, permission: string, privacy: string, repos_count: int, repositories_url: string, slug: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id)} | format pattern "/teams/{team_id}"))
  let req_body = {"description": $description, "name": $name, "parent_team_id": $parent_team_id, "permission": $permission, "privacy": $privacy} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List discussions (Legacy)
#
# GET /teams/{team_id}/discussions
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#list-discussions-legacy — API method documentation
# operationId: teams/list-discussions-legacy
@deprecated
export def "teams-discussions list-legacy" [
  team_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --direction: string@direction-completer # One of `asc` (ascending) or `desc` (descending). (default: desc)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, body: string, body_html: string, body_version: string, comments_count: int, comments_url: string, created_at: string, html_url: string, last_edited_at: string, node_id: string, number: int, pinned: bool, private: bool, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, team_url: string, title: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "direction" $direction "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id)} | format pattern "/teams/{team_id}/discussions") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a discussion (Legacy)
#
# POST /teams/{team_id}/discussions
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#create-a-discussion-legacy — API method documentation
# operationId: teams/create-discussion-legacy
@deprecated
export def "teams-discussions create-legacy" [
  team_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  body: string # The discussion post's body text.
  --private: oneof<nothing, bool> # Private posts are only visible to team members, organization owners, and team maintainers. Public posts are visible to all members of the organization. Set to `true` to create a private post. (default: false)
  title: string # The discussion post's title.
]: any -> record<author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, body: string, body_html: string, body_version: string, comments_count: int, comments_url: string, created_at: string, html_url: string, last_edited_at: string, node_id: string, number: int, pinned: bool, private: bool, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, team_url: string, title: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id)} | format pattern "/teams/{team_id}/discussions"))
  let req_body = {"body": $body, "private": $private, "title": $title} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a discussion (Legacy)
#
# DELETE /teams/{team_id}/discussions/{discussion_number}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#delete-a-discussion-legacy — API method documentation
# operationId: teams/delete-discussion-legacy
@deprecated
export def "teams-discussions delete-legacy" [
  team_id: int
  discussion_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id), discussion_number: (encode-path-segment $discussion_number)} | format pattern "/teams/{team_id}/discussions/{discussion_number}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a discussion (Legacy)
#
# GET /teams/{team_id}/discussions/{discussion_number}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#get-a-discussion-legacy — API method documentation
# operationId: teams/get-discussion-legacy
@deprecated
export def "teams-discussions get-legacy" [
  team_id: int
  discussion_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, body: string, body_html: string, body_version: string, comments_count: int, comments_url: string, created_at: string, html_url: string, last_edited_at: string, node_id: string, number: int, pinned: bool, private: bool, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, team_url: string, title: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id), discussion_number: (encode-path-segment $discussion_number)} | format pattern "/teams/{team_id}/discussions/{discussion_number}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a discussion (Legacy)
#
# PATCH /teams/{team_id}/discussions/{discussion_number}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#update-a-discussion-legacy — API method documentation
# operationId: teams/update-discussion-legacy
@deprecated
export def "teams-discussions update-legacy" [
  team_id: int
  discussion_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --body: string # The discussion post's body text.
  --title: string # The discussion post's title.
]: any -> record<author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, body: string, body_html: string, body_version: string, comments_count: int, comments_url: string, created_at: string, html_url: string, last_edited_at: string, node_id: string, number: int, pinned: bool, private: bool, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, team_url: string, title: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id), discussion_number: (encode-path-segment $discussion_number)} | format pattern "/teams/{team_id}/discussions/{discussion_number}"))
  let req_body = {"body": $body, "title": $title} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List discussion comments (Legacy)
#
# GET /teams/{team_id}/discussions/{discussion_number}/comments
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#list-discussion-comments-legacy — API method documentation
# operationId: teams/list-discussion-comments-legacy
@deprecated
export def "teams-discussions-comments list-legacy" [
  team_id: int
  discussion_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --direction: string@direction-completer # One of `asc` (ascending) or `desc` (descending). (default: desc)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, body: string, body_html: string, body_version: string, created_at: string, discussion_url: string, html_url: string, last_edited_at: string, node_id: string, number: int, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "direction" $direction "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id), discussion_number: (encode-path-segment $discussion_number)} | format pattern "/teams/{team_id}/discussions/{discussion_number}/comments") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a discussion comment (Legacy)
#
# POST /teams/{team_id}/discussions/{discussion_number}/comments
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#create-a-discussion-comment-legacy — API method documentation
# operationId: teams/create-discussion-comment-legacy
@deprecated
export def "teams-discussions-comments create-legacy" [
  team_id: int
  discussion_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  body: string # The discussion comment's body text.
]: any -> record<author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, body: string, body_html: string, body_version: string, created_at: string, discussion_url: string, html_url: string, last_edited_at: string, node_id: string, number: int, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id), discussion_number: (encode-path-segment $discussion_number)} | format pattern "/teams/{team_id}/discussions/{discussion_number}/comments"))
  let req_body = {"body": $body} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a discussion comment (Legacy)
#
# DELETE /teams/{team_id}/discussions/{discussion_number}/comments/{comment_number}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#delete-a-discussion-comment-legacy — API method documentation
# operationId: teams/delete-discussion-comment-legacy
@deprecated
export def "teams-discussions-comments delete-legacy" [
  team_id: int
  discussion_number: int
  comment_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id), discussion_number: (encode-path-segment $discussion_number), comment_number: (encode-path-segment $comment_number)} | format pattern "/teams/{team_id}/discussions/{discussion_number}/comments/{comment_number}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a discussion comment (Legacy)
#
# GET /teams/{team_id}/discussions/{discussion_number}/comments/{comment_number}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#get-a-discussion-comment-legacy — API method documentation
# operationId: teams/get-discussion-comment-legacy
@deprecated
export def "teams-discussions-comments get-legacy" [
  team_id: int
  discussion_number: int
  comment_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, body: string, body_html: string, body_version: string, created_at: string, discussion_url: string, html_url: string, last_edited_at: string, node_id: string, number: int, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id), discussion_number: (encode-path-segment $discussion_number), comment_number: (encode-path-segment $comment_number)} | format pattern "/teams/{team_id}/discussions/{discussion_number}/comments/{comment_number}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a discussion comment (Legacy)
#
# PATCH /teams/{team_id}/discussions/{discussion_number}/comments/{comment_number}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#update-a-discussion-comment-legacy — API method documentation
# operationId: teams/update-discussion-comment-legacy
@deprecated
export def "teams-discussions-comments update-legacy" [
  team_id: int
  discussion_number: int
  comment_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  body: string # The discussion comment's body text.
]: any -> record<author: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, body: string, body_html: string, body_version: string, created_at: string, discussion_url: string, html_url: string, last_edited_at: string, node_id: string, number: int, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id), discussion_number: (encode-path-segment $discussion_number), comment_number: (encode-path-segment $comment_number)} | format pattern "/teams/{team_id}/discussions/{discussion_number}/comments/{comment_number}"))
  let req_body = {"body": $body} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List reactions for a team discussion comment (Legacy)
#
# GET /teams/{team_id}/discussions/{discussion_number}/comments/{comment_number}/reactions
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/reactions/#list-reactions-for-a-team-discussion-comment-legacy — API method documentation
# operationId: reactions/list-for-team-discussion-comment-legacy
@deprecated
export def "teams-discussions-comments-reactions list-for-legacy" [
  team_id: int
  discussion_number: int
  comment_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --content: string@content-completer # Returns a single [reaction type](https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#reaction-types). Omit this parameter to list all reactions to a team discussion comment.
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<content: string, created_at: string, id: int, node_id: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "content" $content "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id), discussion_number: (encode-path-segment $discussion_number), comment_number: (encode-path-segment $comment_number)} | format pattern "/teams/{team_id}/discussions/{discussion_number}/comments/{comment_number}/reactions") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create reaction for a team discussion comment (Legacy)
#
# POST /teams/{team_id}/discussions/{discussion_number}/comments/{comment_number}/reactions
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/reactions/#create-reaction-for-a-team-discussion-comment-legacy — API method documentation
# operationId: reactions/create-for-team-discussion-comment-legacy
@deprecated
export def "teams-discussions-comments-reactions create-for-legacy" [
  team_id: int
  discussion_number: int
  comment_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  content: string@content-completer # The [reaction type](https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#reaction-types) to add to the team discussion comment.
]: any -> record<content: string, created_at: string, id: int, node_id: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id), discussion_number: (encode-path-segment $discussion_number), comment_number: (encode-path-segment $comment_number)} | format pattern "/teams/{team_id}/discussions/{discussion_number}/comments/{comment_number}/reactions"))
  let req_body = {"content": $content} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List reactions for a team discussion (Legacy)
#
# GET /teams/{team_id}/discussions/{discussion_number}/reactions
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/reactions/#list-reactions-for-a-team-discussion-legacy — API method documentation
# operationId: reactions/list-for-team-discussion-legacy
@deprecated
export def "teams-discussions-reactions list-for-legacy" [
  team_id: int
  discussion_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --content: string@content-completer # Returns a single [reaction type](https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#reaction-types). Omit this parameter to list all reactions to a team discussion.
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<content: string, created_at: string, id: int, node_id: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "content" $content "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id), discussion_number: (encode-path-segment $discussion_number)} | format pattern "/teams/{team_id}/discussions/{discussion_number}/reactions") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create reaction for a team discussion (Legacy)
#
# POST /teams/{team_id}/discussions/{discussion_number}/reactions
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/reactions/#create-reaction-for-a-team-discussion-legacy — API method documentation
# operationId: reactions/create-for-team-discussion-legacy
@deprecated
export def "teams-discussions-reactions create-for-legacy" [
  team_id: int
  discussion_number: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  content: string@content-completer # The [reaction type](https://docs.github.com/enterprise-server@2.22/rest/reference/reactions#reaction-types) to add to the team discussion.
]: any -> record<content: string, created_at: string, id: int, node_id: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id), discussion_number: (encode-path-segment $discussion_number)} | format pattern "/teams/{team_id}/discussions/{discussion_number}/reactions"))
  let req_body = {"content": $content} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List team members (Legacy)
#
# GET /teams/{team_id}/members
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#list-team-members-legacy — API method documentation
# operationId: teams/list-members-legacy
@deprecated
export def "teams-members list-legacy" [
  team_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --role: string@role-completer-2 # Filters members returned by their role in the team. Can be one of: \* `member` - normal members of the team. \* `maintainer` - team maintainers. \* `all` - all members of the team. (default: all)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "role" $role "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id)} | format pattern "/teams/{team_id}/members") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove team member (Legacy)
#
# DELETE /teams/{team_id}/members/{username}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#remove-team-member-legacy — API method documentation
# operationId: teams/remove-member-legacy
@deprecated
export def "teams-members delete-legacy" [
  team_id: int
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id), username: (encode-path-segment $username)} | format pattern "/teams/{team_id}/members/{username}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get team member (Legacy)
#
# GET /teams/{team_id}/members/{username}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#get-team-member-legacy — API method documentation
# operationId: teams/get-member-legacy
@deprecated
export def "teams-members get-legacy" [
  team_id: int
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id), username: (encode-path-segment $username)} | format pattern "/teams/{team_id}/members/{username}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add team member (Legacy)
#
# PUT /teams/{team_id}/members/{username}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#add-team-member-legacy — API method documentation
# operationId: teams/add-member-legacy
@deprecated
export def "teams-members create-legacy" [
  team_id: int
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id), username: (encode-path-segment $username)} | format pattern "/teams/{team_id}/members/{username}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove team membership for a user (Legacy)
#
# DELETE /teams/{team_id}/memberships/{username}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#remove-team-membership-for-a-user-legacy — API method documentation
# operationId: teams/remove-membership-for-user-legacy
@deprecated
export def "teams-memberships delete-for-user-legacy" [
  team_id: int
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id), username: (encode-path-segment $username)} | format pattern "/teams/{team_id}/memberships/{username}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get team membership for a user (Legacy)
#
# GET /teams/{team_id}/memberships/{username}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#get-team-membership-for-a-user-legacy — API method documentation
# operationId: teams/get-membership-for-user-legacy
@deprecated
export def "teams-memberships get-for-user-legacy" [
  team_id: int
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<role: string, state: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id), username: (encode-path-segment $username)} | format pattern "/teams/{team_id}/memberships/{username}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add or update team membership for a user (Legacy)
#
# PUT /teams/{team_id}/memberships/{username}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#add-or-update-team-membership-for-a-user-legacy — API method documentation
# operationId: teams/add-or-update-membership-for-user-legacy
@deprecated
export def "teams-memberships create-or-update-for-user-legacy" [
  team_id: int
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --role: string@role-completer-3 # The role that this user should have in the team. Can be one of: \* `member` - a normal member of the team. \* `maintainer` - a team maintainer. Able to add/remove other team members, promote other team members to team maintainer, and edit the team's name and description. (default: member)
]: any -> record<role: string, state: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id), username: (encode-path-segment $username)} | format pattern "/teams/{team_id}/memberships/{username}"))
  let req_body = {"role": $role} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List team projects (Legacy)
#
# GET /teams/{team_id}/projects
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams/#list-team-projects-legacy — API method documentation
# operationId: teams/list-projects-legacy
@deprecated
export def "teams-projects list-legacy" [
  team_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<body: string, columns_url: string, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, html_url: string, id: int, name: string, node_id: string, number: int, organization_permission: string, owner_url: string, permissions: record<admin: bool, read: bool, write: bool>, private: bool, state: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id)} | format pattern "/teams/{team_id}/projects") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove a project from a team (Legacy)
#
# DELETE /teams/{team_id}/projects/{project_id}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams/#remove-a-project-from-a-team-legacy — API method documentation
# operationId: teams/remove-project-legacy
@deprecated
export def "teams-projects delete-legacy" [
  team_id: int
  project_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id), project_id: (encode-path-segment $project_id)} | format pattern "/teams/{team_id}/projects/{project_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Check team permissions for a project (Legacy)
#
# GET /teams/{team_id}/projects/{project_id}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams/#check-team-permissions-for-a-project-legacy — API method documentation
# operationId: teams/check-permissions-for-project-legacy
@deprecated
export def "teams-projects check-permissions-for-legacy" [
  team_id: int
  project_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<body: string, columns_url: string, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, html_url: string, id: int, name: string, node_id: string, number: int, organization_permission: string, owner_url: string, permissions: record<admin: bool, read: bool, write: bool>, private: bool, state: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id), project_id: (encode-path-segment $project_id)} | format pattern "/teams/{team_id}/projects/{project_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add or update team project permissions (Legacy)
#
# PUT /teams/{team_id}/projects/{project_id}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams/#add-or-update-team-project-permissions-legacy — API method documentation
# operationId: teams/add-or-update-project-permissions-legacy
@deprecated
export def "teams-projects create-or-update-permissions-legacy" [
  team_id: int
  project_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --permission: string@permission-completer-1 # The permission to grant to the team for this project. Can be one of: \* `read` - team members can read, but not write to or administer this project. \* `write` - team members can read and write, but not administer this project. \* `admin` - team members can read, write and administer this project. Default: the team's `permission` attribute will be used to determine what permission to grant the team on this project. Note that, if you choose not to pass any parameters, you'll need to set `Content-Length` to zero when calling out to this endpoint. For more information, see "[HTTP verbs](https://docs.github.com/enterprise-server@2.22/rest/overview/resources-in-the-rest-api#http-verbs)."
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id), project_id: (encode-path-segment $project_id)} | format pattern "/teams/{team_id}/projects/{project_id}"))
  let req_body = {"permission": $permission} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List team repositories (Legacy)
#
# GET /teams/{team_id}/repos
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams/#list-team-repositories-legacy — API method documentation
# operationId: teams/list-repos-legacy
@deprecated
export def "teams-repos list-legacy" [
  team_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<allow_forking: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record<body: string, html_url: string, key: string, name: string, url: string>, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<key: string, name: string, node_id: string, spdx_id: string, url: string>, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id)} | format pattern "/teams/{team_id}/repos") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove a repository from a team (Legacy)
#
# DELETE /teams/{team_id}/repos/{owner}/{repo}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams/#remove-a-repository-from-a-team-legacy — API method documentation
# operationId: teams/remove-repo-legacy
@deprecated
export def "teams-repos delete-legacy" [
  team_id: int
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id), owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/teams/{team_id}/repos/{owner}/{repo}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Check team permissions for a repository (Legacy)
#
# GET /teams/{team_id}/repos/{owner}/{repo}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams/#check-team-permissions-for-a-repository-legacy — API method documentation
# operationId: teams/check-permissions-for-repo-legacy
@deprecated
export def "teams-repos check-permissions-for-legacy" [
  team_id: int
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id), owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/teams/{team_id}/repos/{owner}/{repo}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add or update team repository permissions (Legacy)
#
# PUT /teams/{team_id}/repos/{owner}/{repo}
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams/#add-or-update-team-repository-permissions-legacy — API method documentation
# operationId: teams/add-or-update-repo-permissions-legacy
@deprecated
export def "teams-repos create-or-update-permissions-legacy" [
  team_id: int
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --permission: string@permission-completer # The permission to grant the team on this repository. Can be one of: \* `pull` - team members can pull, but not push to or administer this repository. \* `push` - team members can pull and push, but not administer this repository. \* `admin` - team members can pull, push and administer this repository. If no permission is specified, the team's `permission` attribute will be used to determine what permission to grant the team on this repository.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id), owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/teams/{team_id}/repos/{owner}/{repo}"))
  let req_body = {"permission": $permission} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List child teams (Legacy)
#
# GET /teams/{team_id}/teams
# DEPRECATED
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams/#list-child-teams-legacy — API method documentation
# operationId: teams/list-child-legacy
@deprecated
export def "teams-teams list-child-legacy" [
  team_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<description: string, html_url: string, id: int, members_url: string, name: string, node_id: string, parent: record<description: string, html_url: string, id: int, ldap_dn: string, members_url: string, name: string, node_id: string, permission: string, privacy: string, repositories_url: string, slug: string, url: string>, permission: string, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, privacy: string, repositories_url: string, slug: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({team_id: (encode-path-segment $team_id)} | format pattern "/teams/{team_id}/teams") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the authenticated user
#
# GET /user
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users#get-the-authenticated-user — API method documentation
# operationId: users/get-authenticated
export def "user get-authenticated" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/user")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update the authenticated user
#
# PATCH /user
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users/#update-the-authenticated-user — API method documentation
# operationId: users/update-authenticated
export def "user update-authenticated" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --bio: string # The new short biography of the user.
  --blog: string # The new blog URL of the user. (e.g. blog.example.com)
  --company: string # The new company of the user. (e.g. Acme corporation)
  --email: string # The publicly visible email address of the user. (e.g. omar@example.com)
  --hireable: oneof<nothing, bool> # The new hiring availability of the user.
  --location: string # The new location of the user. (e.g. Berlin, Germany)
  --name: string # The new name of the user. (e.g. Omar Jahandar)
  --twitter-username: string # The new Twitter username of the user. (nullable, e.g. therealomarj)
]: any -> record<avatar_url: string, bio: string, blog: string, business_plus: bool, collaborators: int, company: string, created_at: string, disk_usage: int, email: string, events_url: string, followers: int, followers_url: string, following: int, following_url: string, gists_url: string, gravatar_id: string, hireable: bool, html_url: string, id: int, ldap_dn: string, location: string, login: string, name: string, node_id: string, organizations_url: string, owned_private_repos: int, plan: record<collaborators: int, name: string, private_repos: int, space: int>, private_gists: int, public_gists: int, public_repos: int, received_events_url: string, repos_url: string, site_admin: bool, starred_url: string, subscriptions_url: string, suspended_at: string, total_private_repos: int, two_factor_authentication: bool, type: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/user")
  let req_body = {"bio": $bio, "blog": $blog, "company": $company, "email": $email, "hireable": $hireable, "location": $location, "name": $name, "twitter_username": $twitter_username} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete an email address for the authenticated user
#
# DELETE /user/emails
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users#delete-an-email-address-for-the-authenticated-user — API method documentation
# operationId: users/delete-email-for-authenticated-user
export def "user-emails delete-for-authenticated" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --emails: list<string> # Email addresses associated with the GitHub user account.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/user/emails")
  let req_body = {"emails": $emails} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List email addresses for the authenticated user
#
# GET /user/emails
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users#list-email-addresses-for-the-authenticated-user — API method documentation
# operationId: users/list-emails-for-authenticated-user
export def "user-emails list-for-authenticated" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<email: string, primary: bool, verified: bool, visibility: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/user/emails" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add an email address for the authenticated user
#
# POST /user/emails
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users#add-an-email-address-for-the-authenticated-user — API method documentation
# operationId: users/add-email-for-authenticated-user
export def "user-emails create-for-authenticated" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --emails: list<string> # Adds one or more email addresses to your GitHub account. Must contain at least one email address. **Note:** Alternatively, you can pass a single email address or an `array` of emails addresses directly, but we recommend that you pass an object using the `emails` key. (e.g. [])
]: any -> table<email: string, primary: bool, verified: bool, visibility: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/user/emails")
  let req_body = {"emails": $emails} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List followers of the authenticated user
#
# GET /user/followers
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users#list-followers-of-the-authenticated-user — API method documentation
# operationId: users/list-followers-for-authenticated-user
export def "user-followers list-for-authenticated" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/user/followers" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List the people the authenticated user follows
#
# GET /user/following
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users#list-the-people-the-authenticated-user-follows — API method documentation
# operationId: users/list-followed-by-authenticated-user
export def "user-following list-followed-by-authenticated" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/user/following" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Unfollow a user
#
# DELETE /user/following/{username}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users#unfollow-a-user — API method documentation
# operationId: users/unfollow
export def "user-following delete-unfollow" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/user/following/{username}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Check if a person is followed by the authenticated user
#
# GET /user/following/{username}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users#check-if-a-person-is-followed-by-the-authenticated-user — API method documentation
# operationId: users/check-person-is-followed-by-authenticated
export def "user-following check-person-is-followed-by-authenticated" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/user/following/{username}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Follow a user
#
# PUT /user/following/{username}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users#follow-a-user — API method documentation
# operationId: users/follow
export def "user-following update-follow" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/user/following/{username}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List GPG keys for the authenticated user
#
# GET /user/gpg_keys
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users#list-gpg-keys-for-the-authenticated-user — API method documentation
# operationId: users/list-gpg-keys-for-authenticated-user
export def "user-gpg-keys list-for-authenticated" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<can_certify: bool, can_encrypt_comms: bool, can_encrypt_storage: bool, can_sign: bool, created_at: string, emails: list<record>, expires_at: string, id: int, key_id: string, primary_key_id: int, public_key: string, raw_key: string, subkeys: list<record>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/user/gpg_keys" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a GPG key for the authenticated user
#
# POST /user/gpg_keys
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users#create-a-gpg-key-for-the-authenticated-user — API method documentation
# operationId: users/create-gpg-key-for-authenticated-user
export def "user-gpg-keys create-for-authenticated" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  armored_public_key: string # A GPG key in ASCII-armored format.
]: any -> record<can_certify: bool, can_encrypt_comms: bool, can_encrypt_storage: bool, can_sign: bool, created_at: string, emails: table<email: string, verified: bool>, expires_at: string, id: int, key_id: string, primary_key_id: int, public_key: string, raw_key: string, subkeys: table<can_certify: bool, can_encrypt_comms: bool, can_encrypt_storage: bool, can_sign: bool, created_at: string, emails: list, expires_at: string, id: int, key_id: string, primary_key_id: int, public_key: string, raw_key: string, subkeys: list>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/user/gpg_keys")
  let req_body = {"armored_public_key": $armored_public_key} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a GPG key for the authenticated user
#
# DELETE /user/gpg_keys/{gpg_key_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users#delete-a-gpg-key-for-the-authenticated-user — API method documentation
# operationId: users/delete-gpg-key-for-authenticated-user
export def "user-gpg-keys delete-for-authenticated" [
  gpg_key_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({gpg_key_id: (encode-path-segment $gpg_key_id)} | format pattern "/user/gpg_keys/{gpg_key_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a GPG key for the authenticated user
#
# GET /user/gpg_keys/{gpg_key_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users#get-a-gpg-key-for-the-authenticated-user — API method documentation
# operationId: users/get-gpg-key-for-authenticated-user
export def "user-gpg-keys get-for-authenticated" [
  gpg_key_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<can_certify: bool, can_encrypt_comms: bool, can_encrypt_storage: bool, can_sign: bool, created_at: string, emails: table<email: string, verified: bool>, expires_at: string, id: int, key_id: string, primary_key_id: int, public_key: string, raw_key: string, subkeys: table<can_certify: bool, can_encrypt_comms: bool, can_encrypt_storage: bool, can_sign: bool, created_at: string, emails: list, expires_at: string, id: int, key_id: string, primary_key_id: int, public_key: string, raw_key: string, subkeys: list>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({gpg_key_id: (encode-path-segment $gpg_key_id)} | format pattern "/user/gpg_keys/{gpg_key_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List app installations accessible to the user access token
#
# GET /user/installations
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps#list-app-installations-accessible-to-the-user-access-token — API method documentation
# operationId: apps/list-installations-for-authenticated-user
export def "user-installations list-apps-for-authenticated" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<installations: table<access_tokens_url: string, account: any, app_id: int, app_slug: string, contact_email: string, created_at: string, events: list, html_url: string, id: int, permissions: record, repositories_url: string, repository_selection: string, single_file_name: string, suspended_at: string, suspended_by: record, target_id: int, target_type: string, updated_at: string>, total_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/user/installations" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List repositories accessible to the user access token
#
# GET /user/installations/{installation_id}/repositories
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps#list-repositories-accessible-to-the-user-access-token — API method documentation
# operationId: apps/list-installation-repos-for-authenticated-user
export def "user-installations-repositories list-apps-repos-for-authenticated" [
  installation_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> record<repositories: table<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, repository_selection: string, total_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({installation_id: (encode-path-segment $installation_id)} | format pattern "/user/installations/{installation_id}/repositories") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove a repository from an app installation
#
# DELETE /user/installations/{installation_id}/repositories/{repository_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps#remove-a-repository-from-an-app-installation — API method documentation
# operationId: apps/remove-repo-from-installation-for-authenticated-user
export def "user-installations-repositories delete-apps-repo-from-for-authenticated" [
  installation_id: int
  repository_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({installation_id: (encode-path-segment $installation_id), repository_id: (encode-path-segment $repository_id)} | format pattern "/user/installations/{installation_id}/repositories/{repository_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add a repository to an app installation
#
# PUT /user/installations/{installation_id}/repositories/{repository_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps#add-a-repository-to-an-app-installation — API method documentation
# operationId: apps/add-repo-to-installation-for-authenticated-user
export def "user-installations-repositories create-apps-repo-to-for-authenticated" [
  installation_id: int
  repository_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({installation_id: (encode-path-segment $installation_id), repository_id: (encode-path-segment $repository_id)} | format pattern "/user/installations/{installation_id}/repositories/{repository_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List user account issues assigned to the authenticated user
#
# GET /user/issues
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/issues#list-user-account-issues-assigned-to-the-authenticated-user — API method documentation
# operationId: issues/list-for-authenticated-user
export def "user-issues list-for-authenticated" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string@filter-completer # Indicates which sorts of issues to return. Can be one of: \* `assigned`: Issues assigned to you \* `created`: Issues created by you \* `mentioned`: Issues mentioning you \* `subscribed`: Issues you're subscribed to updates for \* `all` or `repos`: All issues the authenticated user can see, regardless of participation or creation (default: assigned)
  --state: string@state-completer # Indicates the state of the issues to return. Can be either `open`, `closed`, or `all`. (default: open)
  --labels: string # A list of comma separated label names. Example: `bug,ui,@high`
  --qp-sort: string@sort-completer-2 # What to sort results by. Can be either `created`, `updated`, `comments`. (default: created)
  --direction: string@direction-completer # One of `asc` (ascending) or `desc` (descending). (default: desc)
  --since: string # Only show notifications updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<active_lock_reason: string, assignee: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, assignees: list<record>, author_association: string, body: string, body_html: string, body_text: string, closed_at: string, closed_by: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, comments: int, comments_url: string, created_at: string, events_url: string, html_url: string, id: int, labels: list<any>, labels_url: string, locked: bool, milestone: record<closed_at: string, closed_issues: int, created_at: string, creator: record, description: string, due_on: string, html_url: string, id: int, labels_url: string, node_id: string, number: int, open_issues: int, state: string, title: string, updated_at: string, url: string>, node_id: string, number: int, performed_via_github_app: record<client_id: string, client_secret: string, created_at: string, description: string, events: list, external_url: string, html_url: string, id: int, installations_count: int, name: string, node_id: string, owner: record, pem: string, permissions: record, slug: string, updated_at: string, webhook_secret: string>, pull_request: record<diff_url: string, html_url: string, merged_at: string, patch_url: string, url: string>, reactions: record<_1: int, _1: int, confused: int, eyes: int, heart: int, hooray: int, laugh: int, rocket: int, total_count: int, url: string>, repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, repository_url: string, state: string, timeline_url: string, title: string, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "state" $state "scalar") (serialize-qp "labels" $labels "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "direction" $direction "scalar") (serialize-qp "since" $since "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/user/issues" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List public SSH keys for the authenticated user
#
# GET /user/keys
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users#list-public-ssh-keys-for-the-authenticated-user — API method documentation
# operationId: users/list-public-ssh-keys-for-authenticated-user
export def "user-keys list-public-ssh-for-authenticated" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<created_at: string, id: int, key: string, read_only: bool, title: string, url: string, verified: bool> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/user/keys" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a public SSH key for the authenticated user
#
# POST /user/keys
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users#create-a-public-ssh-key-for-the-authenticated-user — API method documentation
# operationId: users/create-public-ssh-key-for-authenticated-user
export def "user-keys create-public-ssh-for-authenticated" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  key: string # The public SSH key to add to your GitHub account.
  --title: string # A descriptive name for the new key. (e.g. Personal MacBook Air)
]: any -> record<created_at: string, id: int, key: string, read_only: bool, title: string, url: string, verified: bool> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/user/keys")
  let req_body = {"key": $key, "title": $title} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Delete a public SSH key for the authenticated user
#
# DELETE /user/keys/{key_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users#delete-a-public-ssh-key-for-the-authenticated-user — API method documentation
# operationId: users/delete-public-ssh-key-for-authenticated-user
export def "user-keys delete-public-ssh-for-authenticated" [
  key_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({key_id: (encode-path-segment $key_id)} | format pattern "/user/keys/{key_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a public SSH key for the authenticated user
#
# GET /user/keys/{key_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users#get-a-public-ssh-key-for-the-authenticated-user — API method documentation
# operationId: users/get-public-ssh-key-for-authenticated-user
export def "user-keys get-public-ssh-for-authenticated" [
  key_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created_at: string, id: int, key: string, read_only: bool, title: string, url: string, verified: bool> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({key_id: (encode-path-segment $key_id)} | format pattern "/user/keys/{key_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List organization memberships for the authenticated user
#
# GET /user/memberships/orgs
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#list-organization-memberships-for-the-authenticated-user — API method documentation
# operationId: orgs/list-memberships-for-authenticated-user
export def "user-memberships-orgs list-for-authenticated" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --state: string@state-completer-6 # Indicates the state of the memberships to return. Can be either `active` or `pending`. If not specified, the API returns both active and pending memberships.
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<organization: record<avatar_url: string, description: string, events_url: string, hooks_url: string, id: int, issues_url: string, login: string, members_url: string, node_id: string, public_members_url: string, repos_url: string, url: string>, organization_url: string, permissions: record<can_create_repository: bool>, role: string, state: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "state" $state "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/user/memberships/orgs" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get an organization membership for the authenticated user
#
# GET /user/memberships/orgs/{org}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#get-an-organization-membership-for-the-authenticated-user — API method documentation
# operationId: orgs/get-membership-for-authenticated-user
export def "user-memberships-orgs get-for-authenticated" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<organization: record<avatar_url: string, description: string, events_url: string, hooks_url: string, id: int, issues_url: string, login: string, members_url: string, node_id: string, public_members_url: string, repos_url: string, url: string>, organization_url: string, permissions: record<can_create_repository: bool>, role: string, state: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/user/memberships/orgs/{org}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update an organization membership for the authenticated user
#
# PATCH /user/memberships/orgs/{org}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#update-an-organization-membership-for-the-authenticated-user — API method documentation
# operationId: orgs/update-membership-for-authenticated-user
export def "user-memberships-orgs update-for-authenticated" [
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  state: string@state-completer-7 # The state that the membership should be in. Only `"active"` will be accepted.
]: any -> record<organization: record<avatar_url: string, description: string, events_url: string, hooks_url: string, id: int, issues_url: string, login: string, members_url: string, node_id: string, public_members_url: string, repos_url: string, url: string>, organization_url: string, permissions: record<can_create_repository: bool>, role: string, state: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({org: (encode-path-segment $org)} | format pattern "/user/memberships/orgs/{org}"))
  let req_body = {"state": $state} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List organizations for the authenticated user
#
# GET /user/orgs
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#list-organizations-for-the-authenticated-user — API method documentation
# operationId: orgs/list-for-authenticated-user
export def "user-orgs list-for-authenticated" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<avatar_url: string, description: string, events_url: string, hooks_url: string, id: int, issues_url: string, login: string, members_url: string, node_id: string, public_members_url: string, repos_url: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/user/orgs" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a user project
#
# POST /user/projects
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/projects#create-a-user-project — API method documentation
# operationId: projects/create-for-authenticated-user
export def "user-projects create-for-authenticated" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --body: string # Body of the project (nullable, e.g. This project represents the sprint of the first week in January)
  name: string # Name of the project (e.g. Week One Sprint)
]: any -> record<body: string, columns_url: string, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, html_url: string, id: int, name: string, node_id: string, number: int, organization_permission: string, owner_url: string, private: bool, state: string, updated_at: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/user/projects")
  let req_body = {"body": $body, "name": $name} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List public email addresses for the authenticated user
#
# GET /user/public_emails
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users#list-public-email-addresses-for-the-authenticated-user — API method documentation
# operationId: users/list-public-emails-for-authenticated-user
export def "user-public-emails list-for-authenticated" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<email: string, primary: bool, verified: bool, visibility: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/user/public_emails" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List repositories for the authenticated user
#
# GET /user/repos
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-repositories-for-the-authenticated-user — API method documentation
# operationId: repos/list-for-authenticated-user
export def "user-repos list-for-authenticated" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --visibility: string@visibility-completer-4 # Can be one of `all`, `public`, or `private`. Note: For GitHub AE, can be one of `all`, `internal`, or `private`. (default: all)
  --affiliation: string # Comma-separated list of values. Can include: \* `owner`: Repositories that are owned by the authenticated user. \* `collaborator`: Repositories that the user has been added to as a collaborator. \* `organization_member`: Repositories that the user has access to through being a member of an organization. This includes every repository on every team that the user is on. (default: owner,collaborator,organization_member)
  --type: string@type-completer-2 # Can be one of `all`, `owner`, `public`, `private`, `member`. Note: For GitHub AE, can be one of `all`, `owner`, `internal`, `private`, `member`. Default: `all` Will cause a `422` error if used in the same request as **visibility** or **affiliation**. Will cause a `422` error if used in the same request as **visibility** or **affiliation**. (default: all)
  --qp-sort: string@sort-completer-3 # Can be one of `created`, `updated`, `pushed`, `full_name`. (default: full_name)
  --direction: string@direction-completer # Can be one of `asc` or `desc`. Default: `asc` when using `full_name`, otherwise `desc`
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
  --since: string # Only show notifications updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --before: string # Only show notifications updated before the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
]: nothing -> table<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "visibility" $visibility "scalar") (serialize-qp "affiliation" $affiliation "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "direction" $direction "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar") (serialize-qp "since" $since "scalar") (serialize-qp "before" $before "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/user/repos" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a repository for the authenticated user
#
# POST /user/repos
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#create-a-repository-for-the-authenticated-user — API method documentation
# operationId: repos/create-for-authenticated-user
export def "user-repos create-for-authenticated" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --allow-merge-commit: oneof<nothing, bool> # Whether to allow merge commits for pull requests. (default: true, e.g. true)
  --allow-rebase-merge: oneof<nothing, bool> # Whether to allow rebase merges for pull requests. (default: true, e.g. true)
  --allow-squash-merge: oneof<nothing, bool> # Whether to allow squash merges for pull requests. (default: true, e.g. true)
  --auto-init: oneof<nothing, bool> # Whether the repository is initialized with a minimal README. (default: false)
  --delete-branch-on-merge: oneof<nothing, bool> # Whether to delete head branches when pull requests are merged (default: false, e.g. false)
  --description: string # A short description of the repository.
  --gitignore-template: string # The desired language or platform to apply to the .gitignore. (e.g. Haskell)
  --has-downloads: oneof<nothing, bool> # Whether downloads are enabled. (default: true, e.g. true)
  --has-issues: oneof<nothing, bool> # Whether issues are enabled. (default: true, e.g. true)
  --has-projects: oneof<nothing, bool> # Whether projects are enabled. (default: true, e.g. true)
  --has-wiki: oneof<nothing, bool> # Whether the wiki is enabled. (default: true, e.g. true)
  --homepage: string # A URL with more information about the repository.
  --is-template: oneof<nothing, bool> # Whether this repository acts as a template that can be used to generate new repositories. (default: false, e.g. true)
  --license-template: string # The license keyword of the open source license for this repository. (e.g. mit)
  name: string # The name of the repository. (e.g. Team Environment)
  --private: oneof<nothing, bool> # Whether the repository is private. (default: false)
  --team-id: int # The id of the team that will be granted access to this repository. This is only valid when creating a repository in an organization.
]: any -> record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues_count: int, owner: record<avatar_url: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/user/repos")
  let req_body = {"allow_merge_commit": $allow_merge_commit, "allow_rebase_merge": $allow_rebase_merge, "allow_squash_merge": $allow_squash_merge, "auto_init": $auto_init, "delete_branch_on_merge": $delete_branch_on_merge, "description": $description, "gitignore_template": $gitignore_template, "has_downloads": $has_downloads, "has_issues": $has_issues, "has_projects": $has_projects, "has_wiki": $has_wiki, "homepage": $homepage, "is_template": $is_template, "license_template": $license_template, "name": $name, "private": $private, "team_id": $team_id} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# List repository invitations for the authenticated user
#
# GET /user/repository_invitations
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-repository-invitations-for-the-authenticated-user — API method documentation
# operationId: repos/list-invitations-for-authenticated-user
export def "user-repository-invitations list-repos-for-authenticated" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<created_at: string, expired: bool, html_url: string, id: int, invitee: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, inviter: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, node_id: string, permissions: string, repository: record<allow_forking: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/user/repository_invitations" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Decline a repository invitation
#
# DELETE /user/repository_invitations/{invitation_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#decline-a-repository-invitation — API method documentation
# operationId: repos/decline-invitation-for-authenticated-user
export def "user-repository-invitations delete-repos-decline-for-authenticated" [
  invitation_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({invitation_id: (encode-path-segment $invitation_id)} | format pattern "/user/repository_invitations/{invitation_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Accept a repository invitation
#
# PATCH /user/repository_invitations/{invitation_id}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#accept-a-repository-invitation — API method documentation
# operationId: repos/accept-invitation-for-authenticated-user
export def "user-repository-invitations update-repos-accept-for-authenticated" [
  invitation_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({invitation_id: (encode-path-segment $invitation_id)} | format pattern "/user/repository_invitations/{invitation_id}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List repositories starred by the authenticated user
#
# GET /user/starred
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#list-repositories-starred-by-the-authenticated-user — API method documentation
# operationId: activity/list-repos-starred-by-authenticated-user
export def "user-starred list-activity-repos-by-authenticated" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-1 # Response content type
  --qp-sort: string@sort-completer-5 # One of `created` (when the repository was starred) or `updated` (when it was last pushed to). (default: created)
  --direction: string@direction-completer # One of `asc` (ascending) or `desc` (descending). (default: desc)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<html_url: string, key: string, name: string, node_id: string, spdx_id: string, url: string>, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues_count: int, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "sort" $qp_sort "scalar") (serialize-qp "direction" $direction "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/user/starred" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Unstar a repository for the authenticated user
#
# DELETE /user/starred/{owner}/{repo}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#unstar-a-repository-for-the-authenticated-user — API method documentation
# operationId: activity/unstar-repo-for-authenticated-user
export def "user-starred delete-activity-unstar-for-authenticated" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/user/starred/{owner}/{repo}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Check if a repository is starred by the authenticated user
#
# GET /user/starred/{owner}/{repo}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#check-if-a-repository-is-starred-by-the-authenticated-user — API method documentation
# operationId: activity/check-repo-is-starred-by-authenticated-user
export def "user-starred check-activity-is-by-authenticated" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/user/starred/{owner}/{repo}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Star a repository for the authenticated user
#
# PUT /user/starred/{owner}/{repo}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#star-a-repository-for-the-authenticated-user — API method documentation
# operationId: activity/star-repo-for-authenticated-user
export def "user-starred update-activity-star-for-authenticated" [
  owner: string
  repo: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({owner: (encode-path-segment $owner), repo: (encode-path-segment $repo)} | format pattern "/user/starred/{owner}/{repo}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List repositories watched by the authenticated user
#
# GET /user/subscriptions
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#list-repositories-watched-by-the-authenticated-user — API method documentation
# operationId: activity/list-watched-repos-for-authenticated-user
export def "user-subscriptions list-activity-watched-repos-for-authenticated" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<allow_forking: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record<body: string, html_url: string, key: string, name: string, url: string>, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<key: string, name: string, node_id: string, spdx_id: string, url: string>, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/user/subscriptions" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List teams for the authenticated user
#
# GET /user/teams
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/teams#list-teams-for-the-authenticated-user — API method documentation
# operationId: teams/list-for-authenticated-user
export def "user-teams list-for-authenticated" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<created_at: string, description: string, html_url: string, id: int, ldap_dn: string, members_count: int, members_url: string, name: string, node_id: string, organization: record<avatar_url: string, billing_email: string, blog: string, collaborators: int, company: string, created_at: string, default_repository_permission: string, description: string, disk_usage: int, email: string, events_url: string, followers: int, following: int, has_organization_projects: bool, has_repository_projects: bool, hooks_url: string, html_url: string, id: int, is_verified: bool, issues_url: string, location: string, login: string, members_allowed_repository_creation_type: string, members_can_create_internal_repositories: bool, members_can_create_pages: bool, members_can_create_private_pages: bool, members_can_create_private_repositories: bool, members_can_create_public_pages: bool, members_can_create_public_repositories: bool, members_can_create_repositories: bool, members_url: string, name: string, node_id: string, owned_private_repos: int, plan: record, private_gists: int, public_gists: int, public_members_url: string, public_repos: int, repos_url: string, total_private_repos: int, twitter_username: string, two_factor_requirement_enabled: bool, type: string, updated_at: string, url: string>, parent: record<description: string, html_url: string, id: int, ldap_dn: string, members_url: string, name: string, node_id: string, permission: string, privacy: string, repositories_url: string, slug: string, url: string>, permission: string, privacy: string, repos_count: int, repositories_url: string, slug: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/user/teams" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List users
#
# GET /users
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users#list-users — API method documentation
# operationId: users/list
export def "users list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --since: int # A user ID. Only return users with an ID greater than this ID.
  --per-page: int # Results per page (max 100) (default: 30)
]: nothing -> table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "since" $since "scalar") (serialize-qp "per_page" $per_page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/users" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a user
#
# GET /users/{username}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users#get-a-user — API method documentation
# operationId: users/get-by-username
export def "users get" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/users/{username}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List events for the authenticated user
#
# GET /users/{username}/events
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#list-events-for-the-authenticated-user — API method documentation
# operationId: activity/list-events-for-authenticated-user
export def "users-events list-activity-for-authenticated" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<actor: record<avatar_url: string, display_login: string, gravatar_id: string, id: int, login: string, url: string>, created_at: string, id: string, org: record<avatar_url: string, display_login: string, gravatar_id: string, id: int, login: string, url: string>, payload: record<action: string, comment: record, issue: record, pages: list>, public: bool, repo: record<id: int, name: string, url: string>, type: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/users/{username}/events") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List organization events for the authenticated user
#
# GET /users/{username}/events/orgs/{org}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#list-organization-events-for-the-authenticated-user — API method documentation
# operationId: activity/list-org-events-for-authenticated-user
export def "users-events-orgs list-activity-for-authenticated" [
  username: string
  org: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<actor: record<avatar_url: string, display_login: string, gravatar_id: string, id: int, login: string, url: string>, created_at: string, id: string, org: record<avatar_url: string, display_login: string, gravatar_id: string, id: int, login: string, url: string>, payload: record<action: string, comment: record, issue: record, pages: list>, public: bool, repo: record<id: int, name: string, url: string>, type: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({username: (encode-path-segment $username), org: (encode-path-segment $org)} | format pattern "/users/{username}/events/orgs/{org}") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List public events for a user
#
# GET /users/{username}/events/public
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#list-public-events-for-a-user — API method documentation
# operationId: activity/list-public-events-for-user
export def "users-events-public list-activity" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<actor: record<avatar_url: string, display_login: string, gravatar_id: string, id: int, login: string, url: string>, created_at: string, id: string, org: record<avatar_url: string, display_login: string, gravatar_id: string, id: int, login: string, url: string>, payload: record<action: string, comment: record, issue: record, pages: list>, public: bool, repo: record<id: int, name: string, url: string>, type: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/users/{username}/events/public") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List followers of a user
#
# GET /users/{username}/followers
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users#list-followers-of-a-user — API method documentation
# operationId: users/list-followers-for-user
export def "users-followers list" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/users/{username}/followers") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List the people a user follows
#
# GET /users/{username}/following
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users#list-the-people-a-user-follows — API method documentation
# operationId: users/list-following-for-user
export def "users-following list" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/users/{username}/following") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Check if a user follows another user
#
# GET /users/{username}/following/{target_user}
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users#check-if-a-user-follows-another-user — API method documentation
# operationId: users/check-following-for-user
export def "users-following check" [
  username: string
  target_user: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({username: (encode-path-segment $username), target_user: (encode-path-segment $target_user)} | format pattern "/users/{username}/following/{target_user}"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List gists for a user
#
# GET /users/{username}/gists
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/gists#list-gists-for-a-user — API method documentation
# operationId: gists/list-for-user
export def "users-gists list" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --since: string # Only show notifications updated after the given time. This is a timestamp in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format: `YYYY-MM-DDTHH:MM:SSZ`. (format: date-time)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<comments: int, comments_url: string, commits_url: string, created_at: string, description: string, files: record, forks: list<any>, forks_url: string, git_pull_url: string, git_push_url: string, history: list<any>, html_url: string, id: string, node_id: string, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, public: bool, truncated: bool, updated_at: string, url: string, user: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "since" $since "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/users/{username}/gists") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List GPG keys for a user
#
# GET /users/{username}/gpg_keys
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users#list-gpg-keys-for-a-user — API method documentation
# operationId: users/list-gpg-keys-for-user
export def "users-gpg-keys list" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<can_certify: bool, can_encrypt_comms: bool, can_encrypt_storage: bool, can_sign: bool, created_at: string, emails: list<record>, expires_at: string, id: int, key_id: string, primary_key_id: int, public_key: string, raw_key: string, subkeys: list<record>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/users/{username}/gpg_keys") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get contextual information for a user
#
# GET /users/{username}/hovercard
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users#get-contextual-information-for-a-user — API method documentation
# operationId: users/get-context-for-user
export def "users-hovercard get-context" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --subject-type: string@subject-type-completer # Identifies which additional information you'd like to receive about the person's hovercard. Can be `organization`, `repository`, `issue`, `pull_request`. **Required** when using `subject_id`.
  --subject-id: string # Uses the ID for the `subject_type` you specified. **Required** when using `subject_type`.
]: nothing -> record<contexts: table<message: string, octicon: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "subject_type" $subject_type "scalar") (serialize-qp "subject_id" $subject_id "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/users/{username}/hovercard") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a user installation for the authenticated app
#
# GET /users/{username}/installation
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/apps#get-a-user-installation-for-the-authenticated-app — API method documentation
# operationId: apps/get-user-installation
export def "users-installation get-apps" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<access_tokens_url: string, account: any, app_id: int, app_slug: string, contact_email: string, created_at: string, events: list<string>, html_url: string, id: int, permissions: record<checks: string, contents: string, deployments: string, issues: string, metadata: string, organization_administration: string, pull_requests: string, statuses: string>, repositories_url: string, repository_selection: string, single_file_name: string, suspended_at: string, suspended_by: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, target_id: int, target_type: string, updated_at: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/users/{username}/installation"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List public keys for a user
#
# GET /users/{username}/keys
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/users#list-public-keys-for-a-user — API method documentation
# operationId: users/list-public-keys-for-user
export def "users-keys list-public" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<id: int, key: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/users/{username}/keys") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List organizations for a user
#
# GET /users/{username}/orgs
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/orgs#list-organizations-for-a-user — API method documentation
# operationId: orgs/list-for-user
export def "users-orgs list" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<avatar_url: string, description: string, events_url: string, hooks_url: string, id: int, issues_url: string, login: string, members_url: string, node_id: string, public_members_url: string, repos_url: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/users/{username}/orgs") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List user projects
#
# GET /users/{username}/projects
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/projects#list-user-projects — API method documentation
# operationId: projects/list-for-user
export def "users-projects list" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --state: string@state-completer # Indicates the state of the projects to return. Can be either `open`, `closed`, or `all`. (default: open)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<body: string, columns_url: string, created_at: string, creator: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, html_url: string, id: int, name: string, node_id: string, number: int, organization_permission: string, owner_url: string, private: bool, state: string, updated_at: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "state" $state "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/users/{username}/projects") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List events received by the authenticated user
#
# GET /users/{username}/received_events
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#list-events-received-by-the-authenticated-user — API method documentation
# operationId: activity/list-received-events-for-user
export def "users-received-events list-activity" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<actor: record<avatar_url: string, display_login: string, gravatar_id: string, id: int, login: string, url: string>, created_at: string, id: string, org: record<avatar_url: string, display_login: string, gravatar_id: string, id: int, login: string, url: string>, payload: record<action: string, comment: record, issue: record, pages: list>, public: bool, repo: record<id: int, name: string, url: string>, type: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/users/{username}/received_events") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List public events received by a user
#
# GET /users/{username}/received_events/public
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#list-public-events-received-by-a-user — API method documentation
# operationId: activity/list-received-public-events-for-user
export def "users-received-events-public list-activity" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<actor: record<avatar_url: string, display_login: string, gravatar_id: string, id: int, login: string, url: string>, created_at: string, id: string, org: record<avatar_url: string, display_login: string, gravatar_id: string, id: int, login: string, url: string>, payload: record<action: string, comment: record, issue: record, pages: list>, public: bool, repo: record<id: int, name: string, url: string>, type: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/users/{username}/received_events/public") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List repositories for a user
#
# GET /users/{username}/repos
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/repos#list-repositories-for-a-user — API method documentation
# operationId: repos/list-for-user
export def "users-repos list" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --type: string@type-completer-3 # Can be one of `all`, `owner`, `member`. (default: owner)
  --qp-sort: string@sort-completer-3 # Can be one of `created`, `updated`, `pushed`, `full_name`. (default: full_name)
  --direction: string@direction-completer # Can be one of `asc` or `desc`. Default: `asc` when using `full_name`, otherwise `desc`
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<allow_forking: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record<body: string, html_url: string, key: string, name: string, url: string>, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<key: string, name: string, node_id: string, spdx_id: string, url: string>, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "type" $type "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "direction" $direction "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/users/{username}/repos") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Demote a site administrator
#
# DELETE /users/{username}/site_admin
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#demote-a-site-administrator — API method documentation
# operationId: enterprise-admin/demote-site-administrator
export def "users-site-admin delete-enterprise-demote-administrator" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/users/{username}/site_admin"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Promote a user to be a site administrator
#
# PUT /users/{username}/site_admin
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#promote-a-user-to-be-a-site-administrator — API method documentation
# operationId: enterprise-admin/promote-user-to-be-site-administrator
export def "users-site-admin update-enterprise-promote-to-be-administrator" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/users/{username}/site_admin"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List repositories starred by a user
#
# GET /users/{username}/starred
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#list-repositories-starred-by-a-user — API method documentation
# operationId: activity/list-repos-starred-by-user
export def "users-starred list-activity-repos" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --qp-sort: string@sort-completer-5 # One of `created` (when the repository was starred) or `updated` (when it was last pushed to). (default: created)
  --direction: string@direction-completer # One of `asc` (ascending) or `desc` (descending). (default: desc)
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "sort" $qp_sort "scalar") (serialize-qp "direction" $direction "scalar") (serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/users/{username}/starred") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List repositories watched by a user
#
# GET /users/{username}/subscriptions
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/activity#list-repositories-watched-by-a-user — API method documentation
# operationId: activity/list-repos-watched-by-user
export def "users-subscriptions list-activity-repos-watched" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --per-page: int # Results per page (max 100) (default: 30)
  --page: int # Page number of the results to fetch. (default: 1)
]: nothing -> table<allow_forking: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, code_of_conduct: record<body: string, html_url: string, key: string, name: string, url: string>, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record<key: string, name: string, node_id: string, spdx_id: string, url: string>, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, owner: record<avatar_url: string, email: string, events_url: string, followers_url: string, following_url: string, gists_url: string, gravatar_id: string, html_url: string, id: int, login: string, name: string, node_id: string, organizations_url: string, received_events_url: string, repos_url: string, site_admin: bool, starred_at: string, starred_url: string, subscriptions_url: string, type: string, url: string>, permissions: record<admin: bool, maintain: bool, pull: bool, push: bool, triage: bool>, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record<allow_forking: bool, allow_merge_commit: bool, allow_rebase_merge: bool, allow_squash_merge: bool, archive_url: string, archived: bool, assignees_url: string, blobs_url: string, branches_url: string, clone_url: string, collaborators_url: string, comments_url: string, commits_url: string, compare_url: string, contents_url: string, contributors_url: string, created_at: string, default_branch: string, delete_branch_on_merge: bool, deployments_url: string, description: string, disabled: bool, downloads_url: string, events_url: string, fork: bool, forks: int, forks_count: int, forks_url: string, full_name: string, git_commits_url: string, git_refs_url: string, git_tags_url: string, git_url: string, has_downloads: bool, has_issues: bool, has_pages: bool, has_projects: bool, has_wiki: bool, homepage: string, hooks_url: string, html_url: string, id: int, is_template: bool, issue_comment_url: string, issue_events_url: string, issues_url: string, keys_url: string, labels_url: string, language: string, languages_url: string, license: record, master_branch: string, merges_url: string, milestones_url: string, mirror_url: string, name: string, network_count: int, node_id: string, notifications_url: string, open_issues: int, open_issues_count: int, organization: record, owner: record, permissions: record, private: bool, pulls_url: string, pushed_at: string, releases_url: string, size: int, ssh_url: string, stargazers_count: int, stargazers_url: string, starred_at: string, statuses_url: string, subscribers_count: int, subscribers_url: string, subscription_url: string, svn_url: string, tags_url: string, teams_url: string, temp_clone_token: string, template_repository: record, topics: list, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int>, topics: list<string>, trees_url: string, updated_at: string, url: string, visibility: string, watchers: int, watchers_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "per_page" $per_page "scalar") (serialize-qp "page" $page "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/users/{username}/subscriptions") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Unsuspend a user
#
# DELETE /users/{username}/suspended
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#unsuspend-a-user — API method documentation
# operationId: enterprise-admin/unsuspend-user
export def "users-suspended delete-enterprise-admin-unsuspend" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --reason: string # The reason the user is being unsuspended. This message will be logged in the [audit log](https://help.github.com/enterprise/admin/articles/audit-logging/). If you don't provide a `reason`, it will default to "Unsuspended via API by _SITE\_ADMINISTRATOR_", where _SITE\_ADMINISTRATOR_ is the person who performed the action.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/users/{username}/suspended"))
  let req_body = {"reason": $reason} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Suspend a user
#
# PUT /users/{username}/suspended
# Docs: https://docs.github.com/enterprise-server@2.22/rest/reference/enterprise-admin#suspend-a-user — API method documentation
# operationId: enterprise-admin/suspend-user
export def "users-suspended update-enterprise-admin-suspend" [
  username: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --reason: string # The reason the user is being suspended. This message will be logged in the [audit log](https://help.github.com/enterprise/admin/articles/audit-logging/). If you don't provide a `reason`, it will default to "Suspended via API by _SITE\_ADMINISTRATOR_", where _SITE\_ADMINISTRATOR_ is the person who performed the action.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({username: (encode-path-segment $username)} | format pattern "/users/{username}/suspended"))
  let req_body = {"reason": $reason} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $req_body
}

# Get the Zen of GitHub
#
# GET /zen
# operationId: meta/get-zen
export def "zen get-meta" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/zen")
  let accept_val = "text/plain"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}
